# Use the public npm registry so the portal builds on a clean machine (mirrors
# the Maven "target Maven Central, not a private mirror" decision in AGENTS.md).
# Override locally if you build behind a corporate mirror.
registry=https://registry.npmjs.org/
