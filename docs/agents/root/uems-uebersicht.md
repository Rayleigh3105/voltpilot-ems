# UEMS — Unternehmens-Energiemanagement: Wegweiser

Diese Seite sammelt die Wegweiser-Einträge aller Bau-Pakete des Programms Unternehmens-Energiemanagement (UEMS), damit der Themen-Index von `AGENTS.md` nur EINE Zeile dafür trägt. Die Begriffe stehen im Glossar [`docs/fachmodell/README.md`](../../fachmodell/README.md), die bindenden Schnittstellen in [`docs/contracts/v2/README.md`](../../contracts/v2/README.md).

Die Dateinamen am Zeilenende sind relativ zu diesem Ordner (`docs/agents/root/`). **Nicht ganze Dateien in den Kontext lesen — greppen.**

## Verträge

- **Plattform-Kontenentzug am IP-9-Prüfpunkt** — `disable`/`delete` mit Sofortwirkung, letztem Kundenadministrator und beiden Protokollen; `enable` erhält frühere Entzüge; E12 und parallele Entzüge geprüft · [Wege und Grenzen](uems-admin-kontoentzug.md)

- **Netzanschluss-Vorschlag für vorhandene Anlagen** — Anlage, Bindungsbeginn und Name vorausgewählt; Vertragsangaben ergänzen, Übernahme atomar bestätigen, Verwerfen merken; keine Preisübernahme · [Vorschlag und Grenzen](uems-netzanschluss-vorschlag.md)
- **Benutzerverwaltung im Portal (AP-03 IP-13)** — Avatar → Unternehmen/Einstellungen/Benutzer; Energiemanager liest, Kundenadministrator verwaltet; N2 hängt N3/Startpasswort ein. Atomare Zuweisungswechsel, Sperren/Entfernen, Protokoll und Parallelitätsschutz · [Routen und Nachweise](uems-benutzerverwaltung.md)
- **Ein Summenwert-Assistent (H-5/H-6)** — Geräte-/Anlagen-/Kennzahlen-Einstieg in dieselben fünf Schritte, optional atomare Rolle (H-10), Live-Einmal-Lesung samt Stand, Boxzuordnung über PushJeBox; stabile Öffnen-Funktion und Pflichtläufe · [Assistent](uems-summenwert-assistent.md)

- **Netzanschlüsse im Portal (AP-10 IP-13)** — Standort-Reiter nur wenn er misst, Anlegen und Binden/Wechseln ab wählbarem Tag über IP-6; Rechte aus `rollen.ts`; Bilanzkopf mit ausgewiesenem Stichtag. Eine Formatierstelle: Vereinbartes ganzzahlig, echte Dezimalstellen bleiben (Ergebnis-Vertrag 1.12) · [Portal und Fallen](uems-netzanschluss-portal.md)

- **Bezugsgrößen im Portal (AP-09 IP-9)** — Unternehmenswelt für Messkunden, Strukturflächen als eigene Zeilen, Anlegen mit Arten-Vokabular und `VpPicker`, Standort-/Prozessfilter, Archivieren und Rechte über `rollen.ts`; Werteingabe/Fassungen seit IP-10. Navigation ersetzt den überholten Konzeptort „Struktur“ · [Portal und Fallen](uems-bezugsgroessen-portal.md)
- **Werte und Ablesungen im Portal (AP-09 IP-10)** — gemeinsame Zahleneingabe, Berichtigung mit Begründung, Herkunft/Fassungen, Zeitpunkt-Picker mit ausdrücklicher Stundenwahl und Monatszuordnung; Rechte und API-Grenzen · [Eingabe und Nachweise](uems-werte-portal.md)
- **Summenwerte am Gerät (H-7/H-8/H-10)** — eine Karte auch ohne Rolle, Live-Wert mit Stand und Zeilenmenü; Rolle ab jetzt mit Protokoll, atomar Anlegen + Rolle, Zuordnungen beim Komponenten-Löschen aufräumen. Geräte-GET prüft aktuelle Fassungen, Formular-/Rollenrechte über `rollen.ts`, Fokus und 375/1440 px geprüft · [Wege und Tests](uems-summenwerte-geraetekarte.md)

- **Rollen-Zuordnung (H-1/H-2/H-3): Summenwert mit PV-Produktion/Verbrauch/Netz/keine** — ein Wert je Anlage und Rolle einmal, enthaltene Kanäle/innere Summen nicht zusätzlich; Netz höchstens ein Wert (409), Frische überall 300 s; Java `RollenZuordnungRegeln` ⟷ TS `uemsRollen.ts`, gemeinsame Vektoren. API-Laufzeit über `RollenZuordnungService`/`RollenQuellen`, Anlagen-PUT und Geräte-DELETE, Netz-Sperre auch für `topology-roles`, Protokoll in `ort_aenderung`; Rollen-GET bleibt RLS-lesend. Cockpit/Flotte lesen alle drei Rollen mit Rohwert-Rückfall, v2-Fluss als lokale Topologie-Kopie, Stand und Aufschlüsselung ohne zweite Summenrechnung. `SUMMENWERT` und Textwächter mit engem Übergangsbestand für H-5/H-7 · `uems-rollen-zuordnung.md`
- **Startpasswort und erste Anmeldung (AP-03 IP-14)** — Kundenanlage und Neuvergabe über `benutzer.verwalten`, Pflichtwechsel in Keycloak, einmalige maskierte Antwort, `/me` aktiviert den Spiegel. Plattform nur erster Kundenadministrator; N3 und N1-Hebel für IP-13 bereit. Realm-Policy und Nachweise · `uems-startpasswort.md`

- **Portal-Rechte-Weiche (AP-03 IP-12)** — `rollen.ts` liest die wirksamen Aktionen und sichtbaren Standorte aus `/me`; alle Kunden-Hebel fragen diese Quelle. Teilansicht, L3 und Entzug bestimmen Schale/Startansicht. `/sites`, `/devices`, `/edge-versions` liefern `{eintraege, teilansicht}`; Tests müssen den Umschlag stellen · `uems-portal-rechte.md`

- **UEMS-Geltungsbereich für Kennzahlen, Berichte und Exporte (AP-03 IP-11)** — `zugriff/Geltungsbereich.requireScope` prüft R-A1 plus R-A6 vor Detail, Fassungen, Paar-Auswahl, Werten/Versionen, Vorschau und Berichtsausgaben; `KennzahlUmfang` löst alle Eingänge rekursiv auf, ohne Werte zu rechnen. Die Liste trägt nur `ausserhalb_zugriff {anzahl, text}`; kein Name/keine Kennung einer unsichtbaren Kennzahl. ⚠ W3 bleibt: auch Claudia sieht KZ-0003 nicht (korrigierte A15-Abnahme); CSV-Teilansicht nennt sichtbare Namen + Anzahl, volle Sicht und freigegebene Abzüge bleiben gleich; keine Migration · `uems-geltungsbereich.md`

- **UEMS-Zustandsvokabular „liefert Daten“ / „steuert“ als Vertrag mit Vektoren** — Toleranz `min(max(3 × Kadenz, 300 s), 86 400 s)`, Lücke ab 2 × Kadenz, GENAU EIN „steuert nicht“-Grund in fester Reihenfolge; Zwillinge Java `uems/ZustandAbleitung` ⟷ TS `uemsZustand.ts` gegen `docs/contracts/v2/uems-zustand-vectors.json` — „liefert Daten“ ruft seit AP-04 IP-15 das Messstellen-Register an (die bestehenden Live-Flächen behalten ihr 5-Minuten-Fenster), „steuert“ noch niemand · `uems-zustandsvokabular-liefert-daten-st.md`
- **UEMS-Funktions-Zustand je Standort und Teilnahme je Anlage als Vertrag mit Vektoren** — Rang `kein_objekt < archiviert < entwurf < eingerichtet < angehalten < aktiv`, Standort = höchster Zustand seiner Teilnahmen; Prüfliste vor Start UND Fortsetzen nennt, was fehlt; `steuert` bekommt den ZUSTAND, nie den rohen Ruhe-Eintrag; Zwillinge Java `uems/FunktionZustandAbleitung` ⟷ TS `uemsFunktion.ts` gegen `docs/contracts/v2/funktion-zustand-vectors.json` — `bestand`/`standort` ruft seit IP-2 der Umstieg an (`uems-funktionen.md`), `teilnahme`/`standort`/`messen`/Übergänge seit IP-3 die Routen (`uems-funktionen-routen.md`) · `uems-funktions-zustand-je-standort-und-t.md`
- **UEMS-Ruhe bis zum Start: die Anlage ruht OHNE Enddatum (AP-01 IP-4, R0)** — `V20260914193000` lockert `device_override`: `herkunft = 'funktion'` nur an `pause` und IMMER ohne Ende, ohne Herkunft bleibt das Ende Pflicht (drei CHECKs in Namensreihenfolge, Vektor-Block `zeilen` gegen die echte Tabelle); Push additiv `automation_paused_until_revoked: true` + rollierendes `automation_paused_until` (+4 h, Erneuerung nach 3 h) NUR für ältere Boxen; Go `Registry.Paused` ruht bis auf Widerruf (EIN Tor für Fahrplan, Arbiter und v1-Pfad, Wächter laufen); kein Handweg hebt sie auf (409), `GET /interventions` zeigt sie nicht; `putRuhe`/`clearRuhe` ruft seit IP-3 `FunktionService` an — Zwillinge Java `uems/RuheRegel` ⟷ Go `entities` gegen `docs/contracts/v2/override-vectors.json`; wirkt erst mit einem Edge-Release, `RUNTIME_VERSION` unberührt · `uems-ruhe-bis-zum-start.md`
- **UEMS-Herkunftsvertrag je Messwert als Vertrag mit Vektoren** — 15 Angaben je Wert, Ableitung Zeit (E13) → Sequenz → Herkunft vollständig → Rolle zur MESSZEIT (E4) → Idempotenz Reihe + Messzeit JE SPUR (E3) → Zustellart; Java `uems/MesswertHerkunft` gegen `docs/contracts/v2/messwert-herkunft-vectors.json`, jeder Fall gegen das Referenzunternehmen geprüft — seit AP-07 IP-7 ruft der WRITER an (eigener Zwilling, dieselbe Vektor-Datei) · `uems-herkunftsvertrag-je-messwert-als-v.md`
- **UEMS-Ortsbaum: zeitgültige Zuordnungen als Vertrag mit Vektoren** — „gültig ab“ = Tag 00:00 am Standort, `bis` = LETZTER Tag (DB: `daterange(ab, bis, '[]')`), neues „gültig ab“ beendet das laufende am Vortag, Standort einer Messstelle = Wurzel ihres Ortes an dem Tag; Zwillinge Java `uems/OrtsbaumAbleitung` ⟷ TS `uemsOrtsbaum.ts` gegen `docs/contracts/v2/ortsbaum-vectors.json` — noch ruft niemand an · `uems-ortsbaum-zeitgueltige-zuordnungen-a.md`
- **UEMS-Messstellen-Vertrag: Kennzeichen, Größen, Quellenbindung und Stellung als Vertrag mit Vektoren** — Kennzeichen `MS-0001` nie an eine andere Messstelle weitergegeben (auch früher/archiviert belegt), Quellenbindung in fester Prüfreihenfolge (neue Quelle beendet die laufende genau zu ihrem Beginn, Lücken sichtbar), je Anlage und Richtung ein Hauptzähler am selben Zähler; Zwillinge Java `uems/MessstelleRegeln` ⟷ TS `uemsMessstelle.ts` gegen `docs/contracts/v2/messstelle-vectors.json` — noch ruft niemand an · `uems-messstellen-vertrag-kennzeichen-gro.md`
- **UEMS-Datenquelle und Zuständigkeit als Vertrag mit Vektoren** — Zeiträume halboffen auf die Minute, nie überlappend/rückwirkend, beendet statt überschrieben; Eindeutigkeit je Box, Doppel-Lesen nur als bestätigte Vergleichsquelle (E10 = B), Prüfung von GENAU der Ziel-Box; Fehlerklassen = Verbindungstest-Wörter; Fähigkeiten-Tabelle `edge-capabilities.json` (`supports[]` übersteuert); Zwillinge Java `uems/DatenquelleRegeln` ⟷ TS `uemsDatenquelle.ts` gegen `docs/contracts/v2/data-source-vectors.json` — Status-Ingest ruft die Fehlerklasse an, die übrigen Regeln haben ihre eigenen IP-Aufrufer · `uems-datenquelle-und-zustaendigkeit-als.md`
- **Rückmeldung je Datenquelle aus dem Box-Herzschlag (AP-06 IP-14)** — `DataSourceStatusListener` nimmt den additiven Block `data_sources[]` an, prüft Topic-/Payload-Identität und die laufende Zuständigkeit und ersetzt die RLS-Senke `device_data_source_status` je Box; fehlender Block einer Bestandsbox bleibt ausdrücklich „Box meldet noch nicht je Quelle“, nie ein erfundener Grund; Schalter in Produktion AN, in Surefire AUS · `uems-datenquellen-status.md`
- **UEMS-Rechte-Matrix als Daten und Rechte-Vertrag mit Vektoren** — 48 Aktionen × 7 Rollen mit Kennung je Zeile (`docs/contracts/v2/rechte-matrix.json`, die `.md` wird ERZEUGT, `tools/rechte_matrix.py --check`); `darf` prüft Geltungsbereich vor Aktion (404 → 403 + `rolle_noetig`), Anlage über ihren Standort zum Stichtag, Teilansicht ab zwei Standorten, OCPP-Stufe aus der Zuweisung (E13); Zwillinge Java `uems/RechteAbleitung` ⟷ TS `rechte.ts` gegen `rechte-vectors.json` — noch ruft niemand an · `uems-rechte-matrix-als-daten-und-rechte-v.md`
- **UEMS-Rechte-Matrix: Nachträge aus AP-04 §6.7, AP-05 §6, AP-06 §4.8, AP-07 §4.10 und geprüfte Routen-Kennungen** — Zeile ohne `nachtrag` = Konzept-Tabelle AP-03 §4.3 (byte-gleich, `konzept_tabelle.sha256`), Zeile mit `nachtrag` = Rechte-Abschnitt des späteren Pakets (`herkunft` beginnt damit, `wie` = Zellen von X); `nachtraege` ordnet JEDE Handlung einer Kennung zu; neu `messstelle.ansehen`, `ereignisse.ansehen`, `datenquelle.bearbeiten`/`.zustaendigkeit`/`.ansehen`, umbenannt `messstelle.quelle`; W-R8 … W-R11 (⚠ W-R9: Zuständigkeit ohne Energiemanager); `RechteKennungenDerRoutenTest` hält jede Kennung der UEMS-Routen-Kommentare an die Matrix · `uems-rechte-matrix-nachtraege.md`
- **UEMS-Ereignis-Vertrag Box → Cloud und das geschlossene Ereignis-Vokabular** — 23 Arten mit Urheber (`box`·`datenannahme`·`writer`·`cloud`·`kunde`), Bezug, Zeitform (halboffen/geschlossen, offen nur `data_gap`/`handover`, nie von der Box) und Kundensatz; Umschlag `…/v2/events` 2.1 (Einheit, ohne `box`) + `events.raw` 1.0; append-only per Fortschreibung; Java `uems/EreignisVokabular` prüft, TS `uemsEreignis.ts` spricht, `services/ingest` `EventsContractSchemaTest` — noch ruft niemand an; ⚠ ein Messwert trägt immer `raw`·`decoded`·`qualitaet`, `decoded: null` = Kanal ohne decodierten Wert (so schreibt ihn der Writer), weggelassen bleibt `schema_verletzt` · `uems-ereignis-vertrag-box-cloud-und-das.md`
- **UEMS: `measurement-samples` 2.1 — optionale Herkunftsfelder, 2.0 bleibt gültig** — `applied_revision` je Umschlag + `entity_id` je Sample NUR unter 2.1 (eigene Schema-Datei, 2.0 byte-gleich, fremde Felder weiter abgewiesen); Ingest nimmt beide an und entfernt `entity_id` vor `measurements.raw` (Writer streng, Durchreichen erst mit dem Writer, IP-6/IP-7); Box-Core `parseBatch` ist streng → IP-18 braucht Core + Palette zusammen · `uems-measurement-samples-2-1-herkunftsfelder.md`
- **UEMS-Verbrauchsvertrag: eine Wahrheit, zwei Prüfungen (Python ⟷ Java)** — 23 handgerechnete Ahrenberg-Fälle (+ F24 aus IP-3) in `docs/contracts/v2/verbrauch-vectors.json` legen fest, wie aus den Rohwerten EINER Reihe die Menge EINER Periode wird (Z1–Z9 Zählerstand, I1–I5 Intervallmenge, M1–M6 Momentanwert); Zwillinge Python `voltpilot_optimization/verbrauch.py` ⟷ Java `uems/VerbrauchRegeln`, BEIDE grün gegen dieselbe Datei per Pfad (kein ausgeschalteter Test). ⚠ Abdeckung ist NICHT Vollständigkeit (vollständiger Tag bei 85 %), eine Lücke beginnt ÜBER 2 × Kadenz (strikt >, dieselbe Schwelle wie `ZustandAbleitung.LUECKE_FAKTOR`), ein Umstellungstag hat 23 oder 25 Stunden, `kennzeichen` ist Vertrag nach Text UND Reihenfolge; `_abweichungen` nennt jede bewusste Abweichung von `referenzfaelle.json` — noch ruft niemand an (Verdichtungs-Job ab IP-2) · `uems-verbrauchsvertrag-python-java-zwill.md`
- **UEMS-Ergebnis-Zustand und Kennzeichen als Vertrag (AP-08 IP-8): eine Zahl sagt selbst, wie belastbar sie ist** — GESCHLOSSENES Vokabular vollständig · unvollständig · keine Werte · mit Ersatzwert (je Wort: Zahl Pflicht/erlaubt/verboten, Fehlbestand-Regel) und GESCHLOSSENE Liste aller 17 Kennzeichen-Sätze, die die Verbrauchsregel heute spricht, mit Rang (Reihenfolge ist Vertrag) — `VerbrauchRegeln` formuliert keinen Satz mehr, sie RUFT `uems/ErgebnisZustand` an (Wortlaut Zeichen für Zeichen gleich); ⚠ gerechnet UNGERUNDET, gerundet NUR angezeigt vereinbarte Werte stehen ganzzahlig (echte Dezimalstellen bleiben, seit 1.12); sonst bestimmt die EBENE die Stellen (E11: kWh Viertelstunde/Stunde 1, Tag/Monat/Jahr 0, kW 1, % 0, m³ 1, Tausenderpunkt, U+00A0, U+2212, kein Wert „—“); ⚠ Sommerzeit (E10) in der Zone des STANDORTS, doppelte Beschriftung mit MESZ/MEZ, fehlende Stunde erscheint nicht, „25/23 Stunden (Zeitumstellung)“; Befunde (u. a. „Zuwachs 337.600“) benannt, nicht umformuliert; seit 1.1 „mit Ableseständen“ und Uhrzeit mit MESZ/MEZ an der doppelten Stunde (`ErgebnisZustand.uhr`), alter Wortlaut bleibt über `fruehere_fassungen` lesbar; seit 1.3 kommen Einheit und Zone jedes Kennzeichens aus EINEM Träger `uems/ReihenKontext` (Katalog-Einheit + Standort-Zone, gebildet in den Verdichtern und `ZeitraumMenge`) — „Zuwachs 337,6 kWh“ über `ErgebnisZustand.menge` (Anzeige-Einheit kWh · kvarh · m³, seit 1.8 kVAh und Wmin → kWh, Stellen der Viertelstunde); Zwillinge Java `uems/ErgebnisZustand` ⟷ TS `uemsErgebnis.ts` gegen `docs/contracts/v2/ergebnis-zustand-vectors.json` + jede Kennzeichen-Erwartung von `verbrauch-vectors.json`, `copy.test.ts` liest jeden Satz — Flächen rufen ab IP-10/IP-11 an · `uems-ergebnis-zustand.md`
- **UEMS-Katalog-Einheiten: welcher Zähler seine Menge in einer Zahl sagt (Befund aus PR 726)** — `ergebnis-zustand` 1.8: VAh/kVAh → kVAh (eigene Anzeige-Einheit, NIE kWh), Wmin → kWh über `teiler` 60000 (exakter Quotient gerundet), drei Zwillinge Java/TS/Python gegen dieselbe Datei; OCPP-Einheit aus dem konkreten Schlüssel (`MeasurementCatalog.einheit`, `unit[none]` bleibt unbekannt); 45 Zähler ohne Anzeige-Einheit BENANNT (`semantics.ZAEHLER_OHNE_ANZEIGE_EINHEIT`, `validate.py`, `test_semantics.py` gegen den Vertrag); Bestandsabfrage `tools/betriebsabfragen/katalog-zaehler-einheiten-nutzung.sql` (Entwicklungs-DB 0 von 71). ⚠ **`unit` ist ein Box-Laufzeitfeld** — ein Nachtrag hebt `RUNTIME_VERSION` und braucht ein Edge-Release (Captain-Frage); die Box liest `unit` nicht, der Faktor steht in `scale`, gerechnet wird mit `decoded` → KACO „0,1 kWh“ bleibt bis dahin OHNE Zahl · `uems-katalog-einheiten.md`
- **UEMS-Bezugsdaten-Vertrag: eine Wahrheit, zwei Prüfungen (Java ⟷ TypeScript)** — 14 handgerechnete Ahrenberg-Fälle (B1–B14) in `docs/contracts/v2/bezugsdaten-vectors.json` mit 78 Prüfungen legen fest, wie aus einer gelieferten Zeile ein Wert einer Bezugsgröße wird — und wann sie ein BEFUND wird; Zwillinge Java `uems/BezugsdatenRegeln` ⟷ TS `bezugsdaten.ts`, BEIDE grün gegen dieselbe Datei per Pfad (kein roter und kein ausgeschalteter Test). `zwillinge` ist Teil des Vertrags: es sagt je Regel, WER sie prüft, und `zwillinge_grund` nennt je Lücke den Grund — beide Tests lesen das, eine Regel kann nicht stillschweigend ungeprüft bleiben. Die Menge eines Ablesezeitraums und die 23-/25-Stunden-Tage kommen aus der Verbrauchsregel AP-08 (AUFGERUFEN, nie nachgebaut). ⚠ Die Prüfreihenfolge ist ergebnisrelevant; die Kanal-Abdeckung ist ZEITBASIERT (nicht die wertbasierte aus AP-08); jeder Betrag reist als Dezimaltext und wird numerisch verglichen; `null` ist nie 0. E3: EIGENER Vertrag — `messwert-herkunft.*` bleibt unberührt. Plus sieben Glossar-Nachträge über den Fachmodell-Generator. Seit 13.09.2026 Block `arten`: das Vokabular der ARTEN (§4.2, neun Arten mit Einheiten, Wertart, Perioden, Geltungsbereichen, Herkünften) NUR aus Wörtern der vorhandenen Vokabulare, Zwillinge `uems/BezugsArt` ⟷ `bezugsArt.ts` (rufen `BezugsEinheit` an) — keine Spalte, keine Route — noch ruft niemand an · `uems-bezugsdaten-vertrag-java-ts-zwill.md`
- **UEMS-Bezugsdaten: Einheiten und Perioden als reine Module (AP-09 IP-3)** — der Vertrag aus IP-1, aber die Rechnung EINMAL: Java `uems/BezugsEinheit` ⟷ TS `bezugsEinheit.ts` (geschlossenes Vokabular JE GRÖSSE, Umrechnung nur INNERHALB derselben Größe und nur mit FESTEM Faktor — ein Kundenfaktor ist ausgeschlossen; Synonyme sind Text-Ersetzung, keine Umrechnung) und Java `uems/BezugsPeriode` ⟷ TS `bezugsPeriode.ts` (Deutung der Datumsspalte, Zeitzone des STANDORTS, 23-/25-Stunden-Tage). ⚠ Drei Fallen: ein Tag hat nicht 24 h (Java BESTELLT die Zahl bei `VerbrauchRegeln.stunden`); der mehrdeutige und der nicht existierende Zeitpunkt sind BEFUNDE mit beiden Möglichkeiten, nie eine stille Wahl; `periode_passt_nicht` (wird NIE geteilt) ist nicht `periode_nicht_zu_ende` (läuft noch) — zwei Sätze, nicht einer. Die Kundensätze wohnen IM Modul und werden gegen `befund_saetze` geprüft; `BezugsdatenRegeln`/`bezugsdaten.ts` RUFEN nur noch an (Quelltext-Wächter). Verhalten unverändert: dieselben 89 + 70 Vektor-Tests grün · `uems-einheiten-perioden-module.md`
- **UEMS-Bezugsdaten: der CSV-Leser als reines Modul (AP-09 IP-11)** — Java `uems/CsvLeser` (kein Spring, kein TS-Zwilling) liest Bytes und gibt Zeilen zurück: ≤ 5 242 880 Bytes und ≤ 100 000 Datenzeilen, UTF-8 (BOM entscheidet) vor Windows-1252 streng, Trennzeichen `;`/`,`/Tab per Mehrheit der ersten 20 Zeilen, Kopfzeile, RFC 4180; Vertrag Block `csv` + Regel `csv` an B1/B12/B13 in `bezugsdaten-vectors.json`. ⚠ Kein neues Befund-Wort (C8 geschlossen): jede Ablehnung ist `datei_zu_gross`/`kodierung_unlesbar`/`keine_datenzeilen` + `zusatz` + `zeile`, nie eine Exception; ⚠ Formel-Neutralisierung (`'` vor `= + - @` Tab `\r`, Spiegel des Exports) NUR in `anzeige()`, `felder` bleiben der Dateitext; ⚠ reine ASCII-Datei heißt UTF-8. Fingerabdruck, Vorschau, Tabellen = IP-12 · `uems-csv-leser.md`
- **UEMS-Bilanz-Verträge (AP-10 IP-1): Bilanz · Verteilung · Netzanschluss · Bilanzwert-Herkunft** — vier neue Verträge plus `messstelle-formel.md` ADDITIV erweitert (§0 Typen, §1.1 Term-Art `verteilung` + `anteil`, §2.1 Richtung je Typ, §6 Fassungen); die Bilanz-Rolle kommt aus der STELLUNG (nie gewählt), der Rest aus Zufluss − Abfluss − zugeordnet. ⚠ **Die Richtung ist JE FORMEL-TYP eine Regel**, keine Ableitung aus Vorzeichen: 100 − 60 − 30 = 10 kWh **Bezug** (E1, Plan-Abnahme F1); `rest` speichert keine Terme (je Tag aus der Stellung), `saldo` ergibt `Wirkenergie · saldiert` nur für berechnete Messstellen. ⚠ Summe und Differenz verhalten sich GEGENSÄTZLICH bei fehlendem Eingang („mindestens …“ gegen „keine Werte“); ein Rest ist eine DIFFERENZ und nennt nie eine Ursache (`verbotene_woerter` prüft jeden Satz); Verteilungen wirken je TAG, nie zum Stichtag. Die Zahl in Bilanz- und Netzanschluss-Sätzen kommt aus dem Ergebnis-Zustands-Vertrag (`ErgebnisZustand.zahl` ⟷ `uemsErgebnis.zahl`, E11) mit der Ebene der Periode als `zahl_ebene` — nie eigene Formatierung. PR #688/#689 bleiben und werden AUFGERUFEN, nicht nachgebaut (E7 = A, snake_case wie `MessstelleFormelDto`); W9 in der Auswirkungs-Karte korrigiert. Zwillinge Java `uems/BilanzAbleitung`+`BilanzwertHerkunft`·`VerteilungRegeln`·`NetzanschlussRegeln` ⟷ TS `uemsBilanz.ts`·`uemsVerteilung.ts`·`uemsNetzanschluss.ts` gegen `bilanz-/verteilung-/netzanschluss-vectors.json` — noch ruft niemand an, kein Produktionsweg berührt · `uems-bilanz-vertraege-ap10.md`
- **UEMS-Kennzahl-Vertrag (AP-11 IP-1 bis IP-3): Vertrag, Referenzdatei 1.3, Regel-Module — Summe durch Summe, nie ein Mittel** — `kennzahl.md` + `kennzahl-vectors.json` (K1–K22, 120 Prüfungen) + `kennzahlwert-herkunft.md`; additiv `ergebnis-zustand` 1.9 (Block `kennzahl_kennzeichen`: eigene Rangfolge, Erbregeln), Ereignis-Vokabular Block `reserviert` (`correction`/`bezugsgroesse` für AP-09 IP-7, `kennzahl_neu_gebildet` — keine Migration), `bezugsdaten.md` §11 „Lesart als Nenner“, Referenzunternehmen 1.3 (BZ-6 + BZ-7 = BZ-2, `kennzahlen[]` KZ-0001 … KZ-0005, Diff-Test gegen den Fingerabdruck von 1.2). Zwillinge Java `uems/KennzahlRegeln` ⟷ TS `uemsKennzahl.ts`: Rechenformen, Perioden P1–P6, Einheiten U1–U4, Q1–Q10, Σ ÷ Σ über Ebenen UND Zeit (0,20 und 0,2361; 0,32 und 0,2346 entstehen nirgends, Quelltext-Wächter), G1/G3, R3 = R-A1 ∧ R-A6 über `RechteAbleitung.darf`. ⚠ Kreis, Fassung und Stichtag werden AUFGERUFEN (`MessstelleFormelRegeln.zyklus`/`fassungEintrag`/`fassungAm`, `BezugsdatenRegeln.wertAm`), die letzten drei ohne TS-Zwilling; ⚠ Anteil in Prozent, Nenner-Zustand abgeleitet, ohne Zahl und ohne früheren Wert keine Version; ⚠ Befund K18 (Claudia) folgt W3 — noch ruft niemand an · `uems-kennzahl-vertrag.md`
- **UEMS-Bericht-Vertrag (AP-12 IP-1 bis IP-3): Vertrag, Referenzdatei 1.4, Regel-Module — ein Berichtsstand ist eine Kopie mit Prüfsumme, nie ein Verweis** — `bericht.md` + `bericht-vectors.json` (B1–B16, 106 Prüfungen, Abzüge im Block `abzuege`) + `bericht.schema.json` (`$defs` Abzug, Stand, Quelle, Anstoß, Vorlage) + `bericht-vorlagen.json` (vier Vorlagen, Fassung 1); additiv `ergebnis-zustand` 1.10 (Block `bericht_kennzeichen`), Ereignis-Vokabular Block `reserviert` (vier `bericht_*`), Referenzunternehmen 1.4 (`korrekturen[]` K-2026-0007, `berichte[]` BR-2026-0001 Nr. 1/Nr. 2, Diff-Test gegen den Fingerabdruck von 1.3), Glossar (Bericht … Quellenverzeichnis). Zwillinge Java `uems/BerichtRegeln` ⟷ TS `uemsBericht.ts`: Zeitraum/Vergleich, F1, D2–D4, Betroffenheit Pfad 1/2, Abweichung, kanonische Form byte-gleich (sha256:b79d0fb8…), CSV-Kopf/-Zeile, Teilansicht. ⚠ Aufgerufen: `BezugsPeriode.spanneVon`, `TagRegeln.endgueltigAb`, `ErgebnisZustand.uhr`/`zoneKurz` (additiv), `KennzahlRegeln.anzeige`/`periodeText`, `RechteAbleitung.darf`, Typen `BerichteNaht.Bericht`/`KorrekturKaskade.Betroffen`; ⚠ das Recht prüft die Route VOR F1; ⚠ `rechte-matrix.json` bleibt unberührt (Anmerkung in `bericht.md` §9, Konzept-Fingerabdruck) · `uems-bericht-vertrag.md`
- **UEMS-Führende Box: `LeadDeviceService` ersetzt die Einzel-Gateway-Weiche in Registry-Push und Flow-Aktivierung** — EINE Ableitung (`uems/FuehrendeBoxAbleitung`, Vorrang aus `DatenquelleRegeln.fuehrung`): `site.lead_device_id` nur, wenn die Box in DIESER Anlage angemeldet ist (sonst `gespeichert_nicht_in_anlage`, nie ausweichen), sonst Speicher-Box, sonst einzige Box, sonst `keine_wahl`/`keine_box`; für den Bestand (lead NULL) verhaltensgleich zur alten Weiche, `gatewayAmbiguous` = nur `keine_wahl`; Vektoren `lead-device-vectors.json` · `uems-fuehrende-box-lead-device-service.md`
- **UEMS-Einmal-Aufträge (AP-06 IP-8)** — `EinmalAuftragZiel`: Quellen-Ausführung vor Plan, sonst `LeadDeviceService`; Probe/Register/Handeingriffe/Mess-Selektion gemeinsam, pending bleibt an alter Box, Entzug scheitert benannt; `pushReason` für nicht versandte Handeingriffe, Mess-Vollmengen auch im Reconciler nach Zuständigkeit gefiltert · [Einmal-Aufträge](uems-einmalauftraege.md)
- **UEMS-Quellenübergabe zum Zeitpunkt (AP-06 IP-7)** — `QuellenUebergabe` sichert alle Registry-Pushes: alt ohne → angewandte Fassung/Registry-Herzschlag → neu mit; dauerhafte Phase je Quelle, „Übergabe ausstehend“ im Datenquellen-DTO, `handover` im bestehenden Ereignisweg. Takt `voltpilot.uems.uebergabe.enabled` (`VOLTPILOT_UEMS_UEBERGABE_ENABLED`): Produktion AN, surefire AUS; GitOps ohne Override geprüft. ⚠ AP-07-Gate geschlossen: Writer nimmt `site_id` noch aus dem Box-Umschlag; anlagenfremder A3-Test begründet deaktiviert. Kein Edge-Release; Planzeiten und Mess-Selektion bleiben getrennt · `uems-quellen-uebergabe.md`
- **UEMS-Registry-Push je Box: jede Box genau ihre Quellen, Anlagen-Rollen nur an die führende (AP-06 IP-6, W7)** — `V20260915040000`: `entity_registry_state` Schlüssel (tenant_id, site_id, device_id) statt site_id (keine Zeile geändert, NULL-Box-Zeilen bleiben), gelesen wird die jüngste Zeile; Verteilung NUR `uems/PushJeBox` (rein: ohne führende Box kein Push, ohne jede Datenquelle der Bestandsweg Byte für Byte, Anlagen-Rollen `grid-meter`/`house-load`/`battery-hybrid` nur führend, sonst die Box der Quelle ZUM ZEITPUNKT, benannte Auslässe, Box außerhalb der Anlage bis zum AP-07-Gate gesperrt); eine Box mit Soll bekommt ihre (auch leere) Vollmenge; alles oder nichts je Box (bauen → Soll in EINER Anweisung → zustellen), `PushOutcome.boxen` + `teilweiseZugestellt()`, die Übernahme stellt nach Teilzustellung beide Boxen zurück (`nachAbbruchZurueckstellen` in Takt und Admin-Route); ⚠ seit IP-7 führt das gemeinsame Übergabe-Tor die zeitgesteuerten und sonstigen Pushes zusammen · `uems-registry-push-je-box.md`
- **UEMS-Oberflächen-Vertrag (AP-13 IP-1): Grund-Sätze, Oberflächen-Wörter, Referenzfälle, reines Modul** — `ergebnis-zustand` 1.11 additiv (Block `grund`: je Code der Werte-Route EIN Satz, `grundSatz` Java ⟷ TS, Codes = `OhneZahl` in Reihenfolge), drei Sätze bewusst anders als O15 (Tageskarten-Satz, kein „Zustand bekannt“, keine Tageswerte-Zusage), Glossar `UEMS_WERTE` … `UEMS_MANUELL_ABGELESEN` + fünf Fachmodell-Nachträge, `copy.test` „Welt Oberflächen“ (E15-Verbote, `ABDECKUNG_BESTAND`, `CHART_FILES_OBERFLAECHEN`), `test/oberflaechenFaelle.json` Byte-Kopie mit sha256 + Gleichheitstest, `uemsOberflaechen.ts` (passend · kacheln · sprungziel · verlaufRaster · zoneSatz) · `uems-oberflaechen-vertrag.md`
- **UEMS-WAGO: Erhebungsbogen, Hardwareblatt-Vorlage und Referenzdatensatz-Schema (AP-05 IP-1)** — `docs/wago/erhebungsbogen.md` (A–E, 22 Fragen, jede mit „weiß nicht“ → Prüfaufgabe, Auswertung = die drei Antworten E8, Ahrenberg-Beispiel aus der Referenzdatei), `docs/wago/hardwareblatt-vorlage.md` (Quelle je Feld, vier Nachweis-Wörter, Standardsatz E9 + Summe + Herzschlag, zerstörende Prüfungen nur im Wartungsfenster), `wago-referenzdatensatz.schema.json` + `fixtures/wago-referenzdatensatz/` (2 gültig, 1 ungültig, `wagoReferenzdatensatz.test.ts`); ⚠ die Beweisregel „belegt nur aus dem Pilot mit Nachweis“ ist `anyOf`, weil der Schema-Läufer `if`/`not`/`format` still übergeht (der Test hält die Teilmenge fest); ⚠ Fixtures nie nach `examples/` (ingest listet dort jede Datei); ⚠ Energie-Faktor Register 35 bei 5-A-Karten offen (0,01 oder 0,05 kWh), die Beispiele sind keine Vektor-Datei · `uems-wago-erhebung.md`
- **UEMS-WAGO: Vertrag „VoltPilot-Registerbild WAGO v1“ (AP-05 IP-2)** — `docs/contracts/v2/wago-registerbild.md` (Kopf 12 Wörter mit Signatur 0x5650 0x5242, Längen, Herzschlag, Wortfolge-Prüfwert, Controller-Kennung; Karten-Block 42 Wörter mit Steckplatz, Kartentyp, Gültigkeit, Kartenregister 32/35 roh, Statuswörtern je Gruppe, 12 Messwerten; Basisadresse, Funktionscode, Wortfolge als Parameter; additiv über Längen im Kopf) + Schema + `wago-registerbild-vectors.json` (jede Zahl festlegung/handbuch/repo/zu erheben, V1–V13 `belegt: false`), geprüft von `wagoRegisterbild.test.ts` mit unabhängigem Leser; ⚠ Handbuch-Angaben gelten nur für die 750-495, bei der 750-494 (C-1) ist fast alles zu erheben; ⚠ Vertrags- und Hardwareblatt-Tabellen werden vom Test gelesen; ⚠ 4 Karten = 2 Anfragen, nicht 1 · `uems-wago-registerbild.md`
- **UEMS-WAGO: Katalog-Quelle `wago` und Schema `modbus_input`/`range` (AP-05 IP-4 + IP-5)** — Inhaltsstand 2026.09.16.1: `sources/wago/registerbild-v1.json` → Familien `wago.pm494`/`wago.pm495` mit je 27 Vorlagen `…karte[*].<feld>` (Quellenart `wago_registerbild`, Adresse `12+index*42+<offset>`, Basisadresse/Funktionscode/Wortfolge = Parameter), Herkunft je Zahl in `angaben` wie die Registerbild-Vektoren, `range {min,max,invalid}` und `modbus_input` im Schema; ⚠ die 750-494 erbt NICHTS von der 750-495 — alle zwölf Messwerte `value_type`/`scale` `unknown`, ohne Einheit (`validate.py`, `WagoQuelleTest`); ⚠ Laufzeitstand bleibt 2026.08.26.3: `cataloglib.NOCH_NICHT_AN_DER_BOX` hält die Familien aus Box-Sicht, Palette und Metadaten-Migration, die api lässt sie beim Laden aus — IP-6 streicht den Eintrag nur mit Edge-Release und MUSS den Leser für `invalid` bringen · `uems-wago-katalog.md`

## Tabellen

- **UEMS-Ortsstruktur-Tabellen: Unternehmen, Standort, Anlagen-Zuordnung, Änderungsprotokoll** — `V20260911100000`: Backfill NUR je Mandant ein `unternehmen`; alle FKs `ON DELETE RESTRICT`, darum räumt `TenantRepository.offboard` sie ausdrücklich ab; `tenant_id` vorn in jedem Exklusions-/Unique-Schlüssel (prüft vor dem FK, ohne RLS); CHECK nimmt NULL an → `coalesce(…, false)` · `uems-ortsstruktur-tabellen-unternehmen-st.md`
- **UEMS-Funktionen: Tabellen `funktion` je Standort und `funktion_teilnahme` je Anlage, Umstieg aus dem Bestand (AP-01 IP-2)** — `V20260914190000` legt beide LEER an (RLS + FORCE, kein DELETE, nur Zustand/Zeitpunkte änderbar, eine laufende Funktion je Standort und Art, eine laufende Teilnahme je Anlage, Teilnahme nur an `steuern` per FK, kein FK auf `site` W5); ⚠ gefüllt wird NICHT in der Migration, sondern vom Start-Läufer `FunktionBestandLaeufer` NACH der Standort-Übernahme (`@Order(BestandsuebernahmeLaeufer.ORDER + 1)`, kein Publisher, Schalter `voltpilot.uems.funktion-bestand.enabled`) über die IP-1-Regel `FunktionZustandAbleitung.bestand`/`standort`; ⚠ Abbildung Bestand → Fakten in `FunktionBestandFakten`, im Zweifel „läuft“; `site_profile_state` byte-gleich, Messen bekommt kein Objekt · `uems-funktionen.md`
- **UEMS-Messstellen-Tabellen: Messstelle, Nebengröße, Kennzeichen-Belegung, Zähler, Änderungsprotokoll** — `V20260911140000`: jedes je getragene Kennzeichen steht per Trigger in `messstelle_kennzeichen` (nie weitergegeben, auch früher/archiviert); automatische Vergabe `MS-0001` nur in EINER Transaktion unter Zeilensperre; gespeichert nur die Zustands-Eingänge, nie die Stufe; Postgres prüft CHECKs in Namensreihenfolge · `uems-messstellen-tabellen-messstelle-ne.md`
- **UEMS-Orte-Tabellen: Gebäude, Bereich, Ort-Zuordnung, Bezugsfläche** — `V20260911110000`: `ort` (Gebäude UND Bereich), woran er hängt nur je Tag in `ort_zuordnung`; die Art-Regel als zwei FKs über berechnete Art-Spalten (nie Bereich unter Bereich); Fläche = ganze m² > 0, „nicht erhoben“ ist keine Zeile; „Ziel besteht“/Name je Eltern prüft der Schreibweg · `uems-orte-tabellen-gebaeude-bereich-ort.md`
- **UEMS-Datenquellen-Tabellen: Datenquelle, zeitgültige Zuständigkeit je Box, führende Box** — `V20260911150000`: Zeiträume HALBOFFEN auf die Minute (`tstzrange … '[)'`, nicht Tage), Exklusion je Quelle und je Box+Weg (Kopie von Protokoll/Adresse per FK `ON UPDATE CASCADE`); `device_id` ohne FK (Trigger prüft beim Eintragen, Unclaim bricht nicht), `site.lead_device_id` `SET NULL (spalte)`; Offboarding löst `measurement_point.data_source_id` zuerst · `uems-datenquellen-tabellen-datenquelle-z.md`
- **UEMS-Geräte-Tabellen: Einbau, Karten, Speisung der Komponenten — und die Lese-API** — `V20260911200000`: EINE Zeile `geraet` = EIN Einbau (`kennzeichen` GR-4 bleibt, `einbau_kennzeichen` Z-5a/Z-5b wechselt, Vorgänger = gleiches Kennzeichen), Speisung zeitgültig in `geraet_komponente` statt Spalte an `measurement_point`, Karten in `geraet_teil` nur am Controller; Ableitung je Bestands-Komponente genau ein Gerät (Hybrid trägt seine komponierten Geschwister ohne eigene Verbindung, Seriennummer NUR aus `kaco_http` — Deye-`serial` ist der Logger), `→ site` CASCADE; `GET /api/v1/sites/{id}/geraete`, `GET /api/v1/geraete/{id}`; neue Komponenten bekommen ihr Gerät im Anlege-Weg nach derselben Regel (`V20260911240000`: Constraint-Trigger zur Commit-Zeit → `uems_geraet_ableiten_fuer`), das Messkanal-Read-Model zeigt es als `geraet` · `uems-geraete-tabellen-einbau-karten-spei.md`
- **UEMS-Messwert-Rohtabelle: Reihe je Komponente, Herkunftsspalten, neuer Doppel-Schlüssel** — `V20260912140000`: die Reihe ist Mandant + Komponente + Messkanal (E2), der Doppel-Erkennungsschlüssel Reihe + Messzeit (E3) als PARTIELLER Unique-Index (der Spiegel liegt außerhalb, Vertrag §7.4); der ALTE Index bleibt bis zur Umschaltung (IP-7) der Schlüssel des Writers; Bestand: Komponente nur wo die Auswahl EINDEUTIG ist, bei doppelter Messzeit bleibt der zuerst entgegengenommene in der Reihe, alles andere `role = 'spiegel'` — Einbau, Fassung, Wertart und Zustellart bleiben leer (`null` = nicht nachgeschlagen, `value_kind` NIE aus `aggregation_kind`); kein Fremdschlüssel (IP-11), Rechte/RLS/Aufbewahrung unverändert · `uems-messwert-rohtabelle-reihe-herkunft.md`
- **UEMS-Bezugsgrößen-Tabellen: Werte als Fassungen, Kennzeichen-Verlauf, Protokoll (AP-09 IP-4)** — `V20260913104500`: `bezugsgroesse` (genau EIN Geltungsbereich, E1), `bezugsgroesse_kennzeichen_verlauf` (nie weitergegeben), `bezugsgroesse_wert` (Fassungen, APPEND-ONLY per Trigger — auch die Verwaltungsrolle mit Recht scheitert; Berichtigung/Rücknahme = Fassung n + 1, lückenlos), `bezugsgroesse_aenderung` (`actor_*`); ⚠ Vokabulare NUR in `bezugsdaten_vokabular()`, Zeile für Zeile die Vektor-Datei, jeder CHECK fragt sie (Weiten = Funktion ersetzen, der Test druckt den Block); ⚠ Wertart/Einheit/Periodenart als FK-Kopie je Wert → nach dem ersten Wert unveränderlich (M1, Geltung per Trigger + Schlüssel-Spalten gegen das Rennen); Periode als Tage mit letztem Tag einschließlich + `zeitzone` je Zeile, E16 als CHECK gegen `created_at` (setzt nur die DB); Prozess/Kostenstelle seit AP-10 IP-7 mit Verweis-Spalte; Offboarding räumt alle vier ab · `uems-bezugsgroessen-tabellen.md`
- **UEMS-Kennzahl-Tabellen: Berechnung als Fassungen, Werte als Versionen (AP-11 IP-4)** — `V20260915003000`: `kennzahl` (genau EIN Geltungsbereich, Rechenform), `kennzahl_kennzeichen_verlauf`, `kennzahl_fassung` (Spalten ZEILENGLEICH zur Formel-Fassung + `komplement`/`faktor`/`einheit`, Exklusion je Tag, nur verkürzt), `kennzahl_eingang` (genau ein Verweis, nie die eigene Kennzahl), `kennzahl_wert` + `_eingang`, `kennzahl_aenderung`; ⚠ Werte append-only für JEDE Rolle, auch UPDATE UND DELETE der Verwaltungsrolle — eine vorläufige Version zieht als NEUE ZEILE derselben Nummer nach, aktuell = höchste Version, dann jüngstes `berechnet_am` (Trigger `kennzahl_wert_version_folgt`: ohne Zahl keine Version, vorläufig → dieselbe, endgültig → n + 1 mit Anlass); einziger Ausgang `uems_kennzahlwerte_des_kundenbereichs_entfernen` im Offboarding; ⚠ Vokabulare NUR in `kennzahl_vokabular()` — Protokoll-Wörter sind die DREI des Vertrags, nicht die fünf aus Konzept §6.1; Rechenform und Geltung nach der ersten Fassung fest (FK bzw. Trigger); ⚠ für IP-5: Ort-/Bezugsgrößen-Löschvorschau kennt die Kennzahl noch nicht · `uems-kennzahl-tabellen.md`
- **UEMS-Kennzahl-Schreibwege: die Kennzahl wird definierbar (AP-11 IP-5, Meilenstein 1)** — `/api/v1/kennzahlen` anlegen (Fassung 1, KZ-0001 …), Stammdaten, archivieren, löschen, Fassung n + 1 ab Tag (rückwirkend), Fassung am Stichtag, `POST …/vorschau` in EINER Nur-Lese-Transaktion (Befunde + letzte drei Perioden); geschlossener Satz `kennzahl-vectors.json → schnittstelle.ablehnungen` = `KennzahlAbgelehnt` = OpenAPI `KennzahlFehler`; `V20260915020000`: Löschen nur ohne Wert und ohne Leser über `uems_kennzahl_loeschen`, Kennzeichen als Grabstein; Ort-Löschvorschau `hat_kennzahlen`. ⚠ Definieren durchgesetzt über `KennzahlRechte` (403 `recht_fehlt` + `rolle_noetig`, Satz aus `RechteAbleitung.TEXTE`; fremder Standort 404), Lesen nur Kennung — Personen im Test über `KennzahlAufrufer`; ⚠ Eingänge per Kennzeichen, `periode_art` nur Wunsch, kein `faktor`; ⚠ neue Migration auf Kennzahl-Tabellen → `UemsKennzahlMigrationTest.BAUEN_DARAUF_AUF`; ⚠ offen: Bezugsgröße mit lesender Kennzahl löschen = 500 · `uems-kennzahl-schreibwege.md`
- **UEMS-Kennzahl-Rechenlauf: der Kennzahl-Schritt im Stundentakt NACH den berechneten Messstellen (AP-11 IP-6, E3/E4/E5)** — `uems/KennzahlLauf` im `EndgueltigkeitLaeufer` (Flag `voltpilot.uems.kennzahlen.enabled`): Ordnung über `BerechnetePeriode.reihenfolge` (Kreis benannt `formel_kreis`/`haengt_an_kreis`, nie gerechnet), Lesen über `uems/KennzahlEingangLeser` (mit der Vorschau GETEILT: Messstelle über ihr Lesemodell, Bezugsgröße wirksame Fassung, Stammdatum am Stichtag, Kennzahl gespeicherte Zeile), gerechnet NUR `KennzahlRegeln.wert`: Grundperiode direkt, Zusammenfassung Σ ÷ Σ über die Paare, gröbere Periode einer Zusammenfassung bzw. mit feinerer Periodenwert-Bezugsgröße Σ ÷ Σ über die eigenen Teilperioden (K14) — nie verteilt, nie gemittelt (Quelltext-Wächter); append-only als Verwaltungsrolle, vorläufig zieht als Zeile derselben Version nach, unverändert schreibt nichts. ⚠ ohne Periodenwert-Eingang keine Zeile (P4), laufende Periode mit Periodenwert-Nenner `periode_nicht_zu_ende` (K21); ⚠ endgültig bleibt stehen — Version n + 1 und `kennzahl_neu_gebildet` sind IP-8/IP-9; ⚠ keine Migration · `uems-kennzahl-rechenlauf.md`
- **UEMS-Kennzahl-Werte lesen: die Kennzahl wird lesbar (AP-11 IP-7, Meilenstein „Kennzahl lesbar“)** — `GET /api/v1/kennzahlen/{id}/werte?periode=&von=&bis=[&version=]` (Trägerform des Messstellen-Werts + `richtung`, `grund`, `definition_fassung`; Dezimaltext, ungerundet) und `…/werte/versionen?periode=&von=` (wer, wann, warum, was vorher — Muster AP-08 IP-18) in `KennzahlWerteController` → `uems/KennzahlWerteService`; Herkunft = `KennzahlRegeln.herkunft` über die gespeicherten Eingänge, byte-gleich zu allen 13 Herkunfts-Prüfungen der Vektoren. ⚠ Zeit-Perioden (IP-6 über die eigenen Teilperioden) haben keine Eingänge → `satz` null, `fehlt` eingaenge — K14 mit Paaren ist IP-11; ⚠ Belege ohne `K-…`/`EW-…` (IP-9: Import, Berichtigung) haben noch keine Entscheidung; ⚠ `MessstelleWerteService.entscheidung` ist jetzt paketweit und wird aufgerufen; keine Migration · `uems-kennzahl-werte-lesen.md`
- **UEMS-Bericht-Tabellen und Berichts-Ereignisse: der Berichtsstand als Kopie, append-only für jede Rolle (AP-12 IP-4, E1/E2/E8/E13)** — `V20260915050000`: `bericht` (Vorlage × Geltung × Zeitraum, `BR-<Jahr>-<Nr.>` über `uems_bericht_kennung`, Jahr des Anlegens), `bericht_entwurf` (genau einer), `bericht_stand` (Abzug `text`, Prüfsumme als CHECK über `sha256()`), `bericht_quelle` (Tage einschließlich, `objekt_id` ohne FK), `bericht_revision_anstoss` (`NULLS NOT DISTINCT` → idempotent, B7), `bericht_abruf`, `bericht_aenderung`, `bericht_kennung_seq`; ⚠ Stand, Quellen eines Stands, Anstöße (nur ihr Abschluss), Abrufe und Protokoll append-only für JEDE Rolle — nur `ersetzt_durch_nr` genau einmal, einziger Ausgang `uems_berichte_des_kundenbereichs_entfernen` im Offboarding; kein bestehender Weg wird enger (keine FK auf Löschbares, Test `keinBestehenderWegWirdEnger`); ⚠ Vokabulare NUR `bericht_vokabular()`/`bericht_vorlage()`, Protokoll-Wörter = `handlung`, nicht Konzept §4.2; `uems_berichts_belege(objekt)` (INVOKER, jeder Stand inkl. Vergleich und ersetzt) ruft erst IP-12; `V20260915050100`: `bericht_freigegeben` · `bericht_revision_angestossen` · `bericht_entwurf_neu_gebildet` · `bericht_abgerufen` (29.–32. Wort, Bezug `bericht`; ⚠ der Art-CHECK ist eine Literal-Liste) — noch schreibt niemand · `uems-bericht-tabellen.md`
- **UEMS-Berichts-Abzug bilden und Berichtsvorlagen: der Entwurf entsteht aus dem Bestand (AP-12 IP-5, Meilenstein „Abzug existiert“)** — `GET /api/v1/bericht-vorlagen` (Ressource = Vertrag = Portal-Kopie, Byte für Byte) + `uems/BerichtAbzugBildung.bilden(con, bericht, jetzt, gebildetVon)`: Messstellen der Geltung je Tag, Werte über das Lesemodell auf DER Verbindung des Aufrufers, Formel der Reste aus `bilanzwert_eingang`, Namen/Orte zum Datenstand, Regelwerk (`spring-boot:build-info` + `VOLTPILOT_BUILD`); schreibt `bericht_entwurf` + `bericht_quelle`, Prüfsumme aus `BerichtRegeln`. ⚠ Mandant ausdrücklich (Kaskade = Verwaltungsrolle ohne RLS, `findeNachKennzeichen` filtert nicht); ⚠ Verwaltungsrolle darf den Entwurf nur ersetzen (UPDATE vor INSERT); ⚠ D2 wirft vor dem Schreiben; ⚠ fünf benannte Lücken zu B1 (Kennzahlen, 4 Quellen, MS-04 netto, `speicher_*`, MS-03-Kennzeichen) als Ist-Zustand im Test — Folgepaket `vp-uems-b12-tagesverlauf-speicher` · `uems-bericht-abzug.md`
- **UEMS-Berichts-Abzug des Unternehmens, Kostenstellen und Kennzahlen (AP-12 IP-6, E3/Q4)** — Vertrag `bericht` 1.1 (additiv `$defs/abzug` um `standorte`/`kostenstellen`) + `V20260915113000` `bericht_kennzahl_abwahl` (keine Zeile = gewählt, kein DELETE, Offboarding): `uems/BerichtUnternehmen` (Netzbezugs-Zähler je Standort, MS-19 am Unternehmen, MS-20 ohne Ort; Standort-Abschnitte; Kostenstellen über `KostenstelleEnergieService.energie(tenant, …)` AUFGERUFEN, Verteilungs-Sätze zum Tag) und `uems/BerichtKennzahlen` (Q4, `kennzahl_wert` + gespeicherte Eingänge in der Form von AP-11 IP-7, hinter `to_regclass`); IP-5-Lücken 1 und 2 geschlossen. ⚠ Q4 am Unternehmen folgt den Vektoren B3/B4 (Unternehmen/Prozess/Kostenstelle), nicht dem „alle“ des Reports; ⚠ Kennzahl-Wert ungerundet (10 Stellen, B1-Vektor 4); ⚠ K14-Jahr ohne gespeicherte Eingänge → Zähler/Nenner an der Definitions-Fassung; ⚠ R1 vergleicht noch keine Kostenstellen (IP-7); die 501 `unternehmensbericht_folgt` aus IP-7 ist eingelöst — Anlegen und D4 bilden den Abzug des Unternehmens, `BerichtAbzugBildung.hatMessstellen` fragt vorher die Geltung · `uems-bericht-abzug-unternehmen.md`
- **UEMS-Berichts-Routen und Rechte: der Bericht wird freigebbar (AP-12 IP-7, E4/E5/E7/E12)** — `GET`/`POST /api/v1/berichte`, `…/{kennung}`, `…/entwurf` (D4 = Abfrage + Fristen, sonst Neubildung `abruf`), `…/entwurf/vergleich?gegen=` (R1), `POST …/freigeben` (F1–F5: Bericht `FOR NO KEY UPDATE`, Entwurf `FOR SHARE`, Abzug in SQL byte-gleich kopiert, Nr. = letzte + 1, Quellen eingefroren, „ersetzt durch“, Anstöße erledigt, `bericht_freigegeben`, Protokoll; dieselbe Freigabe = 200), `GET …/staende/{nr}` (Prüfsumme geprüft, sonst 500 `abzug_beschaedigt`), `…/anstoesse/{id}/verwerfen`, `…/archivieren` in `BerichtController` → `uems/BerichtService`; Rechte `uems/BerichtRechte` (G1–G3 über `RechteAbleitung.darf`, Aufrufer `KennzahlAufrufer`). ⚠ neue Ablehnungen NUR an diesen Routen (fremd/fremder Standort 404 vor 403, Unterstützer nie); ⚠ `jetzt` sekundengenau (Ereignis-Zeit); ⚠ Unternehmensbericht anlegen/neu bilden seit AP-12 IP-6 frei (501 `unternehmensbericht_folgt` aus dem Vokabular genommen); keine Migration · `uems-bericht-routen.md`
- **UEMS-Bericht-Kaskade: ein Bericht merkt von selbst, dass sich unter ihm etwas geändert hat (AP-12 IP-8, E6/E7/E8, Pfad 1, Meilenstein „Revision ausgelöst“)** — Bean `uems/BerichtKaskade` ersetzt `BerichteNaht.Keine` (Flag `voltpilot.uems.berichte.enabled`: yml AN, surefire AUS, aus → `Keine`): `betroffene` = `KennzahlKaskade.messstellen` (Reihe → führende `messstelle_quelle`, dazu `Betroffen.messstellen`) × `bericht_quelle` (Mandant, Tage) → `BerichtRegeln.betroffene` (gültiger Stand vor Entwurf, nie ein ersetzter); `entwurfNeuBilden` → `BerichtAbzugBildung.bilden(con, …, "kaskade")` in DERSELBEN Transaktion + `bericht_entwurf_neu_gebildet` (`datenstand`, `anlass_kennung` — die Route liest ihn); `revisionAusloesen` → `bericht_revision_anstoss` `ON CONFLICT DO NOTHING` (B7) + nur bei neuer Zeile `bericht_revision_angestossen`. ⚠ Datenstand = `max(jetzt, clock_timestamp())` auf die Sekunde AUFGERUNDET — `jetzt` liegt vor der Transaktion, sonst würfe D2 in jedem Takt; ⚠ jeder Fehler wirft und rollt die ganze Kaskade zurück; ⚠ Bezugsgrößen und Kennzahl-Fassungen trägt Pfad 1 seit AP-11 IP-9 (`uems-kennzahl-ausloeser.md`); keine Migration · `uems-bericht-kaskade.md`
- **UEMS-Ausgabe CSV, Abruf-Protokoll und Bestand-Geräte-CSV (AP-12 IP-10, E11/E12 G1)** — `GET /api/v1/berichte/{kennung}/staende/{nr}/csv` (rein `uems/BerichtCsv`: BOM, CRLF, 16 Kopfzeilen, `# wasserzeichen=` am ersetzten Stand, 13 Spalten, `# abschnitt=` nach Vorlage) NUR aus dem Abzug; jeder Abruf `bericht_abruf` + `bericht_abgerufen` in EINER Transaktion; Bestand-Geräte-CSV mit neun Kopfzeilen additiv und `export.standort` über `uems/BestandGeraeteCsv`. ⚠ die EINE sichtbare Bestandsänderung von AP-12: Unterstützer 403 statt der Datei, das Portal zeigt dazu nichts; ⚠ Kennzahl-Zeilen `ort`/`endgueltig_ab` leer bis Abzug 1.2 (`vp-uems-b12-tagesverlauf-speicher`); ⚠ die Knöpfe der Berichtsseite hängen noch nicht (`AUSGABE_EINGEHAENGT`, `darfNachLesen` kennt `export.*` nicht); keine Migration · `uems-bericht-ausgabe-csv.md`
- **UEMS-Ausgabe PDF (AP-12 IP-11, E11 DA2)** — `GET /api/v1/berichte/{kennung}/staende/{nr}/pdf` (rein `uems/BerichtPdf`, Apache PDFBox 3.0.8 unter Apache-2.0, Liberation Sans eingebettet) NUR aus dem Abzug, Recht wie Abrufen (G1); Abruf-Protokoll über denselben Weg wie der CSV (`BerichtService.ausgabe`, `bericht_abruf` mit Format `pdf`). ⚠ byte-gleich für JEDEN Abrufer: keine Abrufzeit, kein Abrufer, keine Teilansicht in der Datei, `/ID` fest aus der Prüfsumme; ⚠ das Wasserzeichen steht schräg UND als Zeile oben rechts (gedrehter Text kommt bei der Text-Extraktion zerhackt heraus); ⚠ der Portal-Knopf hängt noch nicht (`AUSGABE_EINGEHAENGT.pdf`, `onAbruf` in `BerichtePage`); keine Migration · `uems-bericht-ausgabe-pdf.md`
- **UEMS-Nachweis nach den Fristen: die Abnahme des Captains in Code (AP-12 IP-16, E1 = A, B1/B16, Meilenstein 6)** — `uems/UemsBerichtNachDenFristenTest` (Testcontainers): echte Kette bis Nr. 1, Korrektur durch die Kaskade mit der echten `BerichtKaskade`, Nr. 2; dann `messreihe_periode`/`_tag`/`_viertelstunde`, `device_measurement_sample`, `telemetry_v2` bis Ende Oktober per `DELETE` gelöscht UND gezählt → Stände, Prüfsummen, PDF, CSV gleich; `GET …/messstellen/{k}/werte` antwortet über `werteDerRoute` 404 `wert_nicht_mehr_gespeichert` mit dem Satz aus B16 (`MessstelleWerteRegeln.WertNichtMehrGespeichert`, `AUFBEWAHRUNG_TAGE` = Migrationen). ⚠ nur an der Route, nur EINE Periode Monat/Jahr einer gemessenen Reihe in Version 1 jenseits Beginn + 3 653 Tage; ⚠ ohne `version` nach einer Korrektur Version 2 (200, B16-Abweichung); ⚠ ohne Docker LAUT übersprungen (`DockerPflicht`); keine Migration · `uems-bericht-nach-den-fristen.md`
- **UEMS-Strukturänderungs-Läufer: ein Bericht merkt auch, was keine Korrektur ist (AP-12 IP-9, E6 = A, Pfad 2)** — `uems/StrukturAenderungLaeufer` (Flag `voltpilot.uems.berichte.struktur.enabled` UND `voltpilot.uems.berichte.enabled`: yml AN, surefire AUS) liest `ort_aenderung`/`messstelle_aenderung` mit dem Wasserzeichen `bericht_struktur_gelesen` (`V20260915173000`, eine Zeile je Eintrag in der Transaktion der Naht) und ruft die drei Naht-Methoden als Überladungen mit `StrukturBetroffen`; Objekte `uems/StrukturAufloesung` (IDs), Anlass-Kennung `<Art>/<Kennzeichen>/<gilt ab>/<eingetragen>/<Protokoll>-<Zeile>` (Satz über `anlass`, Java ⟷ TS, Vektoren B8), Vorschau `GET /api/v1/berichte/betroffen` → Zeile „Freigegebene Berichte: …“ in Fläche-, Umzugs- und Archivieren-Dialog (`berichteFolgen.ts`). ⚠ nur Stände/Entwürfe mit Datenstand VOR dem Eintrag; ⚠ Umzug löst auf nichts auf, Gebäude/Bereich nur bei Standortwechsel, Fläche = der Ort (heute zitiert sie kein Bericht); ⚠ Bezugsgrößen bleiben Pfad 1 (sonst zwei Anstöße, die Idempotenz fängt das nicht) · `uems-bericht-struktur.md`
- **UEMS-Kennzahl-Kaskade: eine Korrektur zieht ohne Zutun bis in jede Kennzahl durch, die alte Zahl bleibt lesbar (AP-11 IP-8, E8 = A, Reihen-Pfad)** — Bean `uems/KennzahlKaskade` ersetzt `KennzahlenNaht.Keine` (keine Bean mehr): `Betroffen.reihen` → `messstelle_quelle` (führend) plus `Betroffen.messstellen` → Kennzahlen mit diesem Eingang → rekursiv die abhängigen, Ordnung `BerechnetePeriode.reihenfolge`; gerechnet in `KennzahlLauf.nachKorrektur` (derselbe Code wie der Regellauf) in DERSELBEN Transaktion: endgültig und geändert → Version n + 1 „korrigiert (Version n + 1)“ mit dem Beleg „K-… (freigegeben TT.MM.JJJJ)“ als Anlass, vorläufig zieht nach, unverändert schreibt nichts (V3 ohne Versionssatz); Meldung `kennzahl_neu_gebildet` (`V20260915061500`, Zwillinge wie `bilanz_neu_berechnet`); `Betroffen.jetzt` additiv. ⚠ in der Transaktion lesen NUR Messstellen-Versionen ab 2 und gespeicherte Kennzahl-Werte über `con` (`KennzahlEingangLeser.mit`) — nie das Lesemodell auf die BYPASSRLS-Verbindung; ⚠ jeder Fehler wirft und rollt die ganze Kaskade zurück; ⚠ Nachziehen trägt jetzt den Anlass seiner Version · `uems-kennzahl-kaskade.md`
- **UEMS-Kennzahl-Auslöser: Nenner und Definition bilden die Kennzahl neu (AP-11 IP-9, E8 = A, W1, Meilenstein 3 „Kennzahl korrigiert sich“)** — `KorrekturKaskade` liest drei weitere Anlässe und ruft für sie NUR Kennzahl- und Berichts-Naht (`ohneStufen`): `correction` mit Bezug `bezugsgroesse` (`BK-…`, Fassung `fassung_neu`), rückwirkende `kennzahl_fassung` (`kennzahl_fassung:<ID>`), rückwirkendes `stammdatum_eingetragen` (`bezugsgroesse_stammdatum:<ID>`) — Wirkung in `messreihe_kaskade_wirkung` (`V20260915150000`: CHECK erweitert, Teilindex); `Betroffen.bezugsgroessen` additiv (15. Feld, alte Konstruktoren bleiben), Status `berechnung_geaendert`/`stammdatum_eingetragen`; `KennzahlLauf.nachKorrektur(…, Ausloeser, …)`: die Kennzahl der geänderten Berechnung wird Version n + 1 „Berechnung geändert (Fassung n)“ (Anlass `definition`), jede andere „korrigiert“; Pfad 1 der Berichte trägt Bezugsgrößen und Kennzahl-Fassungen (`bezugsgroesse_fassung`, `kennzahl_fassung_rueckwirkend`, Java ⟷ TS, B9). ⚠ eine Fläche ist KEIN Kennzahl-Eingang — kein Auslöser aus `ort_aenderung`; ⚠ `ausloeser` der Meldung nie ein bloßes Kennzeichen (`KZ-0004/Fassung-2`, `BZ-8/ab-…`); ⚠ nur rückwirkende Einträge, ab `gueltig_ab`, keinen Tag früher; ⚠ `KorrekturKaskade.lauf` ist global — liegen gebliebene Test-Anlässe blockieren frühere Uhren · `uems-kennzahl-ausloeser.md`
- **UEMS-Fläche als Kennzahl-Nenner: „Netzbezug je m²“ ist anlegbar (AP-11 §5.1, Befund aus IP-9)** — eine Anfrage nennt den Nenner mit `art: bezugsflaeche` und dem KURZZEICHEN des Orts (`G-2`, Wort nur in `eingang_art_anfrage`); gebunden wird eine `bezugsgroesse`-Zeile als ZEIGER (Name „Bezugsfläche“, `stammdatum`, m², Geltung = der Ort) OHNE eigene Wert-Zeile — gelesen wird `flaeche_gueltigkeit` über `BezugsflaecheLesemodell.stammdatum`, das `BezugsgroesseService.stammdatum` für diesen Zeiger aufruft (`istBezugsflaeche`); ⚠ die Vorschau bindet NIE (Nur-Lese-Transaktion, `id == null` + `flaecheObjekt`), ⚠ `flaecheZeitraum` geht Tag für Tag (Fenster immer begrenzen), ⚠ die Fläche-Route ist camelCase (`gueltigAb`); vierter Auslöser `ort_flaeche:<Ort-ID>` aus `ort_aenderung`/`flaeche_geaendert` bildet ab `gilt ab` neu und keinen Tag früher (Berichte bekommen ihn NICHT — AP-12 IP-9 liest dieselbe Zeile), `V20260916130000` erweitert nur den Kennungs-CHECK; `flaeche_aus_struktur` nennt jetzt den Weg · `uems-flaeche-als-nenner.md`
- **UEMS-Kennzahl-Vorlagen: acht VoltPilot-Vorlagen belegen den Assistenten vor (AP-11 IP-10, E9 = A)** — `services/api/src/main/resources/kennzahlen/kennzahl-vorlagen.json` (kanonisch) ⟷ Portal-Kopie `src/kennzahlen/` byte-gleich (`kennzahlVorlagen.sync.test.ts`), Schema `kennzahl-vorlagen.schema.json` (Aufzählungen = Wörter von Messstelle und Bezugsdaten, verglichen), `GET /api/v1/kennzahl-vorlagen` (keine eigene Kennung), `KennzahlVorlagen.vorbelegung` ⟷ `kennzahlVorlagen.ts` (nur Rechenform, Name, Zweck, Komplement beim Anteil — die Vorschau-Route bleibt unverändert). ⚠ jede Vorlage geht durch die Einheiten-Regel beider Zwillinge; ⚠ der Name endet auf „ — {Geltungsbereich}“ (die Kopie tauscht genau das); ⚠ Kundentext im JSON liest `copy.test.ts`; ⚠ Eigenverbrauchsanteil verlangt am Gesamtwert keine Richtung (Befund) · `uems-kennzahl-vorlagen.md`
- **UEMS-Kennzahl-Zusammenfassung über Ebenen: Summe durch Summe vollständig — Paare als Herkunft, Nenner 0 zählt mit, Paare für den Assistenten (AP-11 IP-11, E5 = A)** — `KennzahlLauf.zeit` schreibt bei einer Zusammenfassung die Paare DERSELBEN Periode als Eingänge (Herkunft K14 statt `fehlt eingaenge`; gerechnet bleibt Σ ÷ Σ über die eigenen Teilperioden), `KennzahlLauf.zaehltMit` macht ein Paar ohne Zahl (K9: 410 kWh ÷ 0 Stück) endgültig, wenn seine Eingänge es sind (die Tabelle speichert ohne Version keinen Zustand); dieselbe Form in der Naht (`nachKorrektur`) und im Lesemodell (`GET …/werte`); `GET /api/v1/kennzahlen/paare?rechenform=&einheit=&standort_id=` (`messwerte.ansehen`, streng) gruppiert nicht archivierte Kennzahlen nach Rechenform und Einheit, `api.ts` `KennzahlPaare`. ⚠ 0,3244 und 0,2346 entstehen nirgends, auch nicht in der Herkunft; ⚠ ein Paar ohne Zahl aus Teilperioden oder Paaren bleibt als Teil vorläufig, ein Paar ohne jede Zeile ebenso; keine Migration · `uems-kennzahl-zusammenfassung.md`
- **UEMS-Kennzahl-Wochen: die Woche einer Kennzahl liegt in der Zone des Standorts (AP-11 IP-12, E3/P5)** — `KennzahlLauf` und `KennzahlVorschauService` bilden `woche` wie jede Periode (Grundperiode Tag → Woche · Monat · Jahr; Wochen-Bezugsgröße als Nenner → nur Woche); die Messstelle je Woche über `MessstelleWerteService.wochen` (keine Route): Grenzen `MessstelleWerteRegeln.woche` = Montag 00:00 bis Montag 00:00 Ortszeit (167/168/169 h), Reihe über `deckung`, Menge `ZeitraumMenge.zeitraum` (über `SpeicherklasseHistorie.zeitraum`) aus den Periodenständen, nie Summe der Tage. ⚠ Passung unverändert — Monatskennzahl aus Wochenwerten bleibt 422 `periode_passt_nicht` (K13 Versuch 2); ⚠ die Woche hat keine Versionen: eine Viertelstunde ab Version 2 → `version_nicht_gebildet`, Kennzahl-Woche ohne Zahl; berechnete Messstelle ohne Woche (Befunde); ⚠ bestehende Tages-Kennzahlen bekommen Wochen-Zeilen, Monat und Jahr bleiben; ⚠ DB-Zonen haben alle Berliner Versatz — die abweichende Zone belegt die Regel; keine Migration · `uems-kennzahl-wochen.md`
- **UEMS-Ereignis-Tabelle `messreihe_ereignis`: append-only, nie gelöscht — Bestands-Spiegel und `events.raw`** — `V20260911260000`: keine Retention/Kompression, kein FK auf Löschbares (nur Offboarding räumt, `tenant` RESTRICT), App nur SELECT+INSERT, UPDATE-Trigger; eine Zeile je Meldung (Fortschreibung = weitere Zeile, gleiche `ereignis_id`), Idempotenz über den Fingerabdruck `meldung`; Bezug wörtlich in `kennungen` + eindeutig aufgelöste `*_id`; Vokabular in `messreihe_ereignis_vokabular()` (CHECKs); Writer spiegelt jedes neue `device_measurement_event` in derselben Transaktion (`aus_bestand`) und hängt `events.raw` an (`EventsRawConsumer` + Writer-Zwilling `EreignisVokabular`) · `uems-messreihe-ereignis-tabelle.md`
- **UEMS-Korrektur und Ersatzwert: zwei Beleg-Tabellen, ein Vokabular — ein Ersatzwert ist kein Messwert (AP-08 IP-12)** — `V20260913190000`: `messreihe_ersatzwert` (`EW-<Jahr>-<Nr.>`, Methode a–g aus E7, Pflicht-Begründung, widerrufbar) und `messreihe_korrektur` (`K-<Jahr>-<Nr.>`, Art, `reihen` JSONB, Vorschau, beginnt IMMER als `vorschlag`, E14) — APPEND-ONLY als Fassungen (Fassung 1 legt an, jede weitere NUR Status + Grund + Urheber; `folgt_auf` im Vokabular; UPDATE scheitert für jede Rolle), RLS + FORCE, App nur SELECT + spaltenweises INSERT, Offboarding Korrektur vor Ersatzwert; kein Rohwert und keine Periode wird angefasst. ⚠ **E7 in der DB:** a–c (`zuwachs = gemessen`) nur mit der genannten geschlossenen `data_gap` GENAU dieses Zuwachses an derselben Reihe und in ihren Viertelstunden, e–g (`keiner`) nie, wo eine `data_gap` mit Zuwachs Reihe und Zeitraum berührt (Trigger `messreihe_ersatzwert_luecke`; e aus §4.6 + F21-Invariante), d = Ablesestand zur Minute in (von, bis] einer Viertelstunde. ⚠ Wörter NUR in `messreihe_korrektur_vokabular()`, Zeile für Zeile `vokabular.ersatzwert_methode|…_status|korrektur_art|korrektur_status` der Ereignis-Vektoren; `substitute`/`correction` als 25./26. Art (Funktion + Art-CHECK, beide Java-Zwillinge, TS, `events-raw`-Schema, Ingest; `cloud` meldet nur `vorschlag`). ⚠ `messreihe_korrektur_vorschlag` (AP-07) bleibt die ERKENNUNG, dies ist der VORGANG (IP-14 verbindet); keine Rechenmethode (IP-13), keine Route (IP-16), keine Kaskade (IP-17) · `uems-korrektur-ersatzwert.md`
- **UEMS-Ersatzwert-Methoden: die geschätzte Verteilung ändert den gemessenen Betrag nie (AP-08 IP-13, E7)** — `VerbrauchRegeln` ⟷ `verbrauch.py` gegen `verbrauch-vectors.json` Block `ersatzwerte` (F11 Version 2, F21 Widerruf/Version 3, je Methode und je Ablehnung); ⚠ **Invariante:** a–c verteilen den Zuwachs der `data_gap` über `[Boden(von), Decke(bis))`, Anteile auf 9 Stellen ABGESCHNITTEN, der Rest in der LETZTEN Viertelstunde → Summe der gespeicherten Anteile EXAKT der Zuwachs (`ErsatzwertInvarianteTest`); ⚠ fehlt ein Profil (Vorperiode/Vergleichsquelle) → BENANNTE Ablehnung, nie stiller Rückfall auf a (12 Wörter `regeln.ersatzwert_ablehnungen` + `rohwerte_fehlen` des Laufs); ⚠ `V20260913210000` `messreihe_viertelstunde_version` (Version ≥ 2, append-only, lückenlos; Version 1 bleibt die Verdichtungszeile) + `messreihe_ersatzwert_wirkung`; `ErsatzwertLauf` rechnet IMMER vom Bestand + heute geltenden (Widerruf = Zahlen von V1, früherer hält überlappende Viertelstunden), wiederholbar, eine Transaktion je Ersatzwert; Kennzeichen „mit Ersatzwert (Methode „…“, EW-…)“ Rang 70 (`ergebnis-zustand` 1.4); Tag/Monat/Jahr = Kaskade IP-17 · `uems-ersatzwert-methoden.md`
- **UEMS-Korrektur-Vorschläge: das System schlägt vor, freigegeben wird von Hand — nie automatisch, auch nicht bei reinen Lücken (AP-08 IP-14, E14)** — `V20260913224500` gibt der BYPASSRLS-Rolle nur INSERT auf die anlegenden Spalten von `messreihe_korrektur` + Trigger `messreihe_korrektur_system_nur_vorschlag` (nur Fassung 1 = `vorschlag`, eine Freigabe über den System-Weg scheitert); `uems/KorrekturVorschlagLauf` als letzter Schritt des Stundenlaufs: (1) BRÜCKE — offene, aufeinanderfolgende Zeilen von `messreihe_korrektur_vorschlag` werden EINE Korrektur `nachlieferung_nach_endgueltigkeit` und gehen auf `erledigt` (vorher neu gezählt, `RUHE` 15 min, Nachbar mit geändertem Randstand kommt dazu, ohne Änderung `verworfen`), (2) `device_boundary` mit Ableseständen nach der Frist → `ablesestaende_nachgetragen` (F12), (3) `umklassifizierung(...)` auf Anfrage des Bearbeiters (E4, F7); „neu“ = `ViertelstundeVerdichter.waereZeile` (dieselbe Regel, nichts geschrieben), „alt“ = neueste Version; Begründung/Notizen aus `uems/KorrekturVorschlagRegeln` gegen `korrektur-vorschlag-vectors.json` (+ `copy.test.ts`), Marker `correction` von `cloud` nur `vorschlag`. ⚠ **Doppelvorschlag-Sperre über Kundenbereich + Art + Reihe + Zeitraum**, nie über einen Zeitstempel: `liegt_schon_vor` (offener überschneidet) · `schon_entschieden` (entschiedener, gleicher Zeitraum, gleiche NEUE Werte); ⚠ benannter Test `bisZurFreigabeAendertSichKeineZahlUndKeineVersion` (Bestandsschutz aller übrigen Tabellen); keine Freigabe (IP-15), Route (IP-16), Kaskade (IP-17) · `uems-korrektur-vorschlaege.md`
- **UEMS-Korrektur-Kaskade: automatisch bis zum Jahr, aber NIE ein freigegebener Bericht (AP-08 IP-17, E9 = A)** — `V20260914120000`: `messreihe_periode_version` (Tag/Monat/Jahr der Reihe, Viertelstunde–Jahr berechneter Messstellen, append-only, lückenlos; nur die neueste VORLÄUFIGE zieht nach), `messreihe_viertelstunde_version` + Anlass `K-…` + Rohwert-Fakten, `messreihe_kaskade_wirkung`; `uems/KorrekturKaskade` (Takt 5 min) je Freigabe/Rücknahme/gerechnetem Ersatzwert in EINER Transaktion: Viertelstunden = freigegebene Vorschau „neu“, Tag/Monat/Jahr über `KaskadeStufen` mit den Regeln der Verdichtung, AP-10 an `BerechnetePeriodenLauf.nachKorrektur`, jede Stufe „korrigiert (Version n)“ (`ergebnis-zustand` 1.5); ⚠ freigegebener Bericht nur `revisionAusloesen` (Meldung `correction`, Urheber kunde), Entwurf `entwurfNeuBilden` — Grenze in `berichteBenachrichtigen`, Naht `BerichteNaht` leer, `KennzahlenNaht` seit AP-11 IP-8 gefüllt; ⚠ Rücknahme = Stand VOR der Korrektur; benannte Ablehnungen statt halber Wahrheit (`vorschau_veraltet`, `ersatzwert_ohne_periodenregel` …) · `uems-korrektur-kaskade.md`

- **UEMS-Speicherklasse Viertelstundenwerte: Tabelle für zehn Jahre, Arbeitsliste und der Fünf-Minuten-Lauf** — `V20260912170000` `messreihe_viertelstunde` (Hypertable, Chunk 30 Tage, Aufbewahrung 3 653 Tage, RLS + FORCE, OHNE Kompression — Layout `segmentby Reihe / orderby Zeit` VORBEREITET und über `messreihe_viertelstunde_kompression_layout()` abrufbar, E7) mit den Feldern aus §4.4; durable Arbeitsliste (Schlüssel IST der Eintrag, Entnahme unter `FOR UPDATE SKIP LOCKED`, entnehmen und schreiben in EINER Transaktion → ein Abbruch verliert nichts) plus Laufzustand (Eingangs-Zeiger mit 2 min Sicherheit/Überlappung, Rückrechnung der 90 Tage in Tagesscheiben). ⚠ Die Rechenregeln werden AUFGERUFEN (`VerbrauchRegeln.ergebnis`/`periodenstand`), nie nachgebaut — es gibt darum KEINE `menge`-Spalte (die Differenz über die Grenze ist AP-08) und der Faktor einer Fassung wird nie angewendet; Abdeckung aus der Kadenz ZUM INTERVALL (IP-10), nie auf 100 % gerundet; Anker auch aus `device_boundary`/`handover` (A5/A6), ein dritter wird GEZÄHLT. ⚠ Grenze zu IP-13: der Lauf schreibt nur `vorlaeufig` und rührt eine endgültige Zeile nie an — Umschalten, `late_arrival` und Tageswerte sind IP-13 · `uems-viertelstundenwerte-speicherklasse.md`

- **UEMS-Menge je Viertelstunde: aus Fakten wird Verbrauch (AP-08 IP-2)** — `V20260912180000` ergänzt `messreihe_viertelstunde` um `menge` (NULL = keine Menge bildbar, NIE 0), `menge_zustand` (vollständig · unvollständig · keine Werte), `kennzeichen` (jsonb-ARRAY von Klartext-Sätzen, Wortlaut UND Reihenfolge sind Vertrag) und `faktor`; `abdeckung_prozent`, `erhalten`/`erwartet` und `zustand` standen schon da und wurden NICHT doppelt angelegt. Gerechnet wird nichts Neues: der Lauf wirft `menge`/`zustand`/`kennzeichen` desselben `VerbrauchRegeln.Ergebnis` nur nicht mehr weg. ⚠ `zustand` (vorläufig/endgültig, AP-07 E5) ist NICHT `menge_zustand` (Vollständigkeit). ⚠ Der Faktor der Fassung wirkt BEIM ERFASSEN und darum hier NIE — `FAKTOR_DER_FASSUNG = 1` nach `quelle-einstellung.md` §3 (`angewendet` = beim Erfassen, `dokumentiert` = im Gerät); für eine Fassung, die die Cloud umrechnen müsste, fehlt dem Vokabular `anwendung` heute das Wort (Befund). Vier Fallen: eine Lücke ist nie eine Null (eine Viertelstunde ohne Rohwert bekommt gar keine Zeile), Abdeckung ist nicht Vollständigkeit, eine Lücke beginnt ÜBER 2 × Kadenz MIT DER KADENZ ZUM INTERVALL, der Faktor wirkt genau einmal. Grenzen: endgültige Zeilen unberührt (IP-13), keine Korrekturen (IP-12 ff.), keine Ersatzwerte (IP-9), Perioden über der Viertelstunde (IP-5) — F16/F20 tragen NUR Monats-/Tages-Erwartungen, geprüft ist ihre Viertelstunden-Hälfte · `uems-viertelstundenmenge.md`
- **UEMS-Endgültigkeit und Tageswerte: wann eine Viertelstunde feststeht** — `V20260912190000`: der STUNDENLAUF setzt jede fällige Viertelstunde auf `endgueltig` (idempotent über `zustand = 'vorlaeufig'`, abbruchsicher je Stapel, `FOR UPDATE SKIP LOCKED`, `berechnet_am` bleibt stehen); ⚠ **die Frist gehört dem INTERVALL, nicht der Zeile** (`TagRegeln.geschlossen` fragt Uhr und Frist — eine Lücke hat gar keine Zeile). SPÄTANKUNFT = **speichern, melden, vorschlagen — nicht anwenden**: ein Rohwert mit Eingangszeit nach der Frist verhindert das Bilden (seit AP-08 IP-19 für JEDEN Grund der Arbeitsliste, auch Rückrechnung/Bruch; vor der Frist wird automatisch neu gebildet — Paar F9/F10 und Monat F16 in `UemsFristVorschlagTest`), erzeugt `late_arrival` (Urheber NEU auch `cloud` — Vokabular geweitet in DB-Funktion, beiden Java-Zwillingen und den Vektoren; BYPASSRLS bekam INSERT auf `messreihe_ereignis`, append-only bleibt) und eine Zeile in `messreihe_korrektur_vorschlag` — alles in EINER Transaktion, Ereignis-Kennung ABGELEITET (Wiederholung schreibt nichts). Die Liste ist die SCHNITTSTELLE, nie die Korrektur (AP-08 IP-12 ff.). TAGESKLASSE `messreihe_tag` (Hypertable, Chunk 1 Jahr, Aufbewahrung 3 653 Tage, RLS + FORCE, Kompressions-Layout nur VORBEREITET): Tagesgrenze in der **Zeitzone des Standorts**, Zone + `zeitzone_herkunft` GESPEICHERT (W10/A8); ⚠ 96 ist nicht immer 96 — 25.10.2026 = 100 Slots, 28.03.2027 = 92, gezählt von `VerbrauchRegeln.stunden` über `BezugsPeriode`, nie hier. Endgültig erst, wenn alle vorhandenen Slots endgültig sind UND die Tagesfrist abgelaufen ist. ⚠ **Grenze zu AP-08 IP-5**: die Zeile trägt FAKTEN (Periodenstände, Abdeckung, Qualität, Anker, Zustand) und **keine `summe`** — die `menge` kam mit IP-5 aus den Periodenständen (Eintrag Periodenmengen) · `uems-endgueltigkeit-tageswerte.md`

- **UEMS-Periodenmengen: Tag, Monat, Jahr und freier Zeitraum AUS DEN PERIODENSTÄNDEN (AP-08 IP-5)** — `V20260912205000`: `messreihe_tag` + `menge`/`menge_zustand`/`kennzeichen`/`kadenz_s`, neu `messreihe_periode` (Monat/Jahr, Hypertable, RLS + FORCE) mit durable Arbeitsliste, freier Zeitraum `ZeitraumMenge` (nur Viertelstunden-Raster); gerechnet NUR von `VerbrauchRegeln.zaehlerstandAusTeilperioden` ⟷ Python-Zwilling (Lockstep an jeder Zählerstand-Erwartung). ⚠ **NIE als Summe der Viertelstunden** (F8: Summe 1 966,4, Tag 2 304,0; F16: 31 gerundete Tage = 55 100,013 statt 55 100,000); fehlt ein Stand an einer Grenze, ist die Menge der gemessene Teil und `unvollständig` — nie ein erfundener Stand. ⚠ Fortpflanzung: ein vorläufiger Tag/eine vorläufige Viertelstunde macht Monat und Jahr vorläufig; Abdeckung = Summe erhalten ÷ Summe erwartet, fehlende Viertelstunden zählen mit (auch am Tag: F8 85 %). ⚠ Tag und MONAT aus Viertelstunden (endgültige Tage von vor IP-5 tragen keine Menge und bleiben unberührt), Jahr aus Monaten; `stand_anfang`/`stand_ende` am Tag sind jetzt die Stände an den Tagesgrenzen. Zeitzone des Standorts gespeichert, 23-/25-Stunden-Tage über `TagRegeln` → `BezugsPeriode` → `VerbrauchRegeln` · `uems-periodenmengen.md`
- **UEMS-Periodenwerte berechneter Messstellen: gespeichert NACH den gemessenen, in der Ordnung ihrer Eingänge (AP-10 IP-10, E6 = A)** — `V20260914100300`: Spur `berechnet` in `messreihe_viertelstunde`/`_tag`/`messreihe_periode` (`messstelle_id`, `formel_fassung_id`, `formel_typ`; OHNE Reihe und ohne Rohwert-Zählung, `…_spur_chk`, Teil-Index je Messstelle + Periode) plus `bilanzwert_eingang` (Eingänge je Zeile) und `messreihe_berechnet_stand`; `uems/BerechnetePeriodenLauf` im Stundentakt nach Tag/Monat/Jahr, Abhängigkeitsordnung aus den Fassungen (beim `rest` aus der Stellung), Kreis benannt abgelehnt (`formel_kreis`/`haengt_an_kreis`); Wert je Periode NUR `MessstelleFormelRegeln.periodenwert` über die Perioden der Eingänge (Lese-Modell „Werte je Messstelle“), vorläufig/endgültig über die Eingänge (`TagRegeln.zustand`). ⚠ Quellen ohne Reihe (`EndgueltigkeitLauf`, `TagVerdichter`, `PeriodeVerdichter`) filtern `entity_id IS NOT NULL`; ⚠ Monat/Jahr nur bei gleichen Termen (`terme_wechseln`); ⚠ Anteil-/Verteilungs-Terme ohne Menge (IP-11); Live-Wert bleibt live, das befristete Kennzeichen ist entfallen · `uems-berechnete-periodenwerte.md`
- **UEMS-Momentanwert und Intervallmenge je Periode: Mittel, gemessene Zeit und die gekennzeichnete Integration (AP-08 IP-3)** — `V20260912213000`: an Viertelstunde, Tag und Monat/Jahr `energie`, `gemessen_s`, `luecke_innen` (+ `summe` am Tag/Monat, `mittel`/`min_wert`/`max_wert` am Monat); gerechnet NUR von `VerbrauchRegeln.momentanwertTeil`/`momentanwertAusTeilperioden` ⟷ Python-Zwilling (Lockstep, F3/F18/F24, F2 halbe Stunde). Drei Wertarten: Zählerstand → `menge`; Momentanwert → Mittel/Min/Max/gemessene Zeit, NIE `menge` (M6, CHECK); Intervallmenge → Regel verdrahtet, aber ⚠ KEIN Rohwert-Wort (Befund). ⚠ **Energie nur mit Quellenbindung `integration`, nur mit Kennzeichen „aus Leistung integriert“ (CHECK), nie über eine Lücke, nie Mittel × Länge**; ein Tag integriert nur, wenn JEDE Viertelstunde mit Werten ihre Energie trägt. ⚠ Mittel = Summe ÷ erhalten, nie Mittel von Mitteln (vorher im Tageslauf aus gerundeten Mitteln); ein Wert hält bis zum nächsten auch HINTER der Grenze (F24: 18,667 statt 18,444) — der Viertelstunden-Lauf liest dafür zwei Kadenzen weiter · `uems-intervall-momentanwert.md`
- **UEMS-Zählerbrüche: Gerätegrenze, Rücksetzung, Überlauf, Neustart in der Strecke (AP-08 IP-4)** — `V20260912220000` + `V20260912221000`: `counter_overflow` als 24. Ereignis-Art (nur `writer`, Rechnung als Pflichtfelder; DB-Funktion ganz abgeschrieben + Art-CHECK, beide Java-Zwillinge, TS, Vektoren, Schema, Ingest-Artenliste — Box-Draht unverändert), `messreihe_zaehler_deklaration()` als EINE Quelle für Wertebereich/Höchstzuwachs/Neustart-Verlust — ⚠ LEER (IP-7 baute nur die Türen: Katalog `wertebereich_modul`/`laeuft_ueber`, Messstelle `anschlussleistung_kw`; der Wertebereich hat noch keinen Weg in die DB): jeder fallende Stand ist eine Rücksetzung, jeder Neustart „bis zu 255 s“, nie eine geratene Zahl; die Z6-Entscheidung `VerbrauchRegeln.ueberlauf` ⟷ `verbrauch.ueberlauf` ⟷ Writer `UeberlaufRegel` gegen dieselbe Datei. ⚠ **Writer-Erkennung im eigenen Savepoint: ein Fehler kostet nie einen Messwert** (Rollback, Log, `voltpilot_writer_ueberlauf_erkennung_total{ergebnis="fehler"}`, keine Meldung; die Menge bildet der Lauf ohnehin aus Werten + Deklaration). Ein SPÄTER eingegangener Bruch trägt Viertelstunde UND Tag neu ein (Grund `ereignis`, derselbe Zeiger); endgültige Zeilen bleiben stehen. ⚠ Ereignisse der Viertelstunde in `(von, bis]` (Wechsel um 10:45 gehört zu 10:30), `device_restart` über die Datenquelle; der Bestand schreibt weiter `counter_reset` · `uems-zaehlerbrueche.md`

- **UEMS-Lücken-Melder: fehlende Werte je Reihe und je Box, Schließen bei Rückkehr und Nachlieferung (AP-07 IP-9)** — `V20260913130000` (Vertrag: `backfill` und `erkannt_aus = kadenz` auch von `cloud`, additiv) + `V20260913130500` (`messreihe_luecke_stand` = Stand UND Arbeitsliste je Box/Reihe, Zeiger `messreihe_luecke_lauf`); ⚠ die Lücke ist NICHT „liefert keine Daten“: über 2 × Kadenz ZUM ZEITPUNKT, ohne Boden und Deckel, Schwelle aus `ZustandAbleitung.liefertDaten(...).lueckeOffen()`; zwei Wege — je Reihe (`von` = letzter guter Wert + Kadenz, Loch zwischen zwei Takten geschlossen gemeldet) und je Box aus dem letzten EINGANG (300 s; EINE Lücke je Box = Kern-Pfad, EINE je zuständiger Datenquelle = UEMS-Pfad; kein eigener Herzschlag-Speicher, `telemetry_v2` nicht gelesen); Schließen = Fortschreibung: Reihe beim ersten RECHTZEITIGEN Wert (bei 900 s vor der Rückkehr der Box!), Box beim ersten Eingang, Quelle spätestens am Ende der Zuständigkeit; `nachgeliefert_am` aus der Zustellart des Writers, `backfill` je Box+Quelle nach 300 s Ruhe, `erwartet` nur aus gezählten Reihen-Lücken; Takt 5 min, `voltpilot.uems.luecken.enabled` (yml AN, surefire AUS); hält ein zweiter Melder den Zeiger, entfällt der GANZE Takt · `uems-luecken-melder.md`
- **UEMS-Zuwachs über eine Lücke: GEMESSEN, aber NICHT VERTEILBAR (AP-08 IP-6, E2 = A)** — `V20260913170000` weitet nur die Vokabular-Funktion: `data_gap` trägt `zuwachs`, `einheit`, `stand_vor`, `stand_nach` (nur zusammen, nur geschlossen, nur an EINER Reihe, nie von der Box, `zuwachs = stand_nach − stand_vor`, Einheit aus `einheit_zuwachs` = Zählerstand-Einheiten des Messstellen-Größen-Katalogs), der Lücken-Melder setzt sie beim Schließen (rechnet nichts: `VerbrauchRegeln.lueckenZuwachs`; Box-Ausfall war seit IP-9 schon eine Reihen-Lücke aus der Kadenz). ⚠ **Genau einmal je Stufe:** `VerbrauchRegeln.zaehltZu` ⟷ `verbrauch.zaehlt_zu` — die Periode, die die Lücke GANZ enthält (Messzeit davor `> von − Kadenz`, danach `≤ bis`), zählt ihn mit Kennzeichen; eine angeschnittene nie; die Viertelstunden der Lücke bleiben „keine Werte“; `kleinsterZeitraum` über Viertelstunde → Stunde → Tag → Monat → Jahr (über den Jahreswechsel keiner). Abnahme `luecken_zuwachs` an F8/F11/F20/F23 + `luecken_zuordnung` in beiden Zwillingen; Einheit und Uhrzeit des Satzes aus dem Träger `ReihenKontext` (seit ergebnis-zustand 1.3 „Zuwachs 337,6 kWh“). ⚠ Append-only: eine spätere Nachlieferung entfernt den Zuwachs nicht — der Marker zeigt ihn nur ohne `nachgeliefert_am`; nur Katalog-Messkanäle mit Einheit bekommen ihn; kein Ersatzwert (IP-13) · `uems-luecken-zuwachs.md`

- **UEMS-Writer: die Herkunft je Messwert wird ZUR MESSZEIT nachgeschlagen** — AP-07 IP-7, KEINE Migration: zwei Spuren, und die Datenquelle der Komponente entscheidet welche (ohne sie bleibt der Wert Zeichen für Zeichen ein Bestandswert); gecacht wird die ZEITLEISTE, nicht die Antwort (fünf Nachschläge je Messkanal, nicht je Wert); die Zuständigkeit zur Messzeit entscheidet nur die ROLLE und verwirft nie (W8, Spiegel statt Verwerfen), der `enabled_at`-Riegel bleibt; der ALTE Index bleibt, solange Zeilen ohne Komponente entstehen; jeder Nachschlag und jede Meldung in EIGENEM Savepoint — ein Fehler kostet nie einen Messwert · `uems-writer-herkunft-zur-messzeit.md`
- **UEMS-Zugriffs-Tabellen: Benutzer-Spiegel, Zuweisung, Zugriffsprotokoll und die Bestandsübernahme E12 (AP-03 IP-2)** — `V20260915030000`: `zugriff` = Rolle × Standort (NULL = mandantenweit) × Zeit (`gueltig_ab` Zeitpunkt, `gueltig_bis` Enddatum EINSCHLIESSLICH + `endet_am` als Zeitpunkt, nur der Notfall allein Zeitpunkt; wirksam = `zugriff_zeitraum(…) @> t`), nie umgeschrieben (Trigger, einmal beenden), Überlappungsverbot je Benutzer + Rolle + Standort, Vokabular und Rollen aus `zugriff_vokabular()`/`zugriff_rolle()` zeilengleich zum Rechte-Vertrag; Bestand: Start-Lauf `ZugriffBestandLaeufer` (Keycloak je Kundenbereich) + Ereignis `KundenbenutzerAngelegt` → wer hier NIE eine Zuweisung hatte, wird Kundenadministrator; ⚠ noch setzt niemand durch (IP-4 ff.) · `uems-zugriff-tabellen.md`
- **UEMS-Keycloak: Rolle „partner“, Kundenkonto ohne Realm-Rolle, Kontoart aus dem Token (AP-03 IP-3)** — Realm-Exporte dev + prod mit `partner` (Konto OHNE `tenant_id`), Betriebsschritt für den Live-Realm in `infra/prod/keycloak/README.md`; `KeycloakAdminClient.createCustomerUser` ohne Realm-Rolle, neu `createPartnerUser`; der Konverter legt additiv genau eine `KONTO_benutzer|partner|plattform` ab (Vokabular `RechteAbleitung.Konto`), `TenantFilter` gibt einem Partner nie einen Kundenbereich (Claim und `X-Tenant-Id` ignoriert); Token unverändert. ⚠ kein Recht an `ROLE_operator` hängen — OCPP-Gates lassen `KONTO_benutzer` mit der Stufe von `operator` durch (Brücke bis IP-7); ⚠ `TestingAuthenticationToken`/`jwt()` haben kein `KONTO_*` · `uems-keycloak-partner.md`
- **UEMS-Zugriff-Kontext je Anfrage und Selbstauskunft `/me` (AP-03 IP-4)** — `ZugriffFilter` nach dem `TenantFilter` lädt `ZugriffContext` (Kontoart aus `KONTO_*`, Kundenbereich, wirksame Zuweisungen, Zugang `konto|unterstuetzung|umschalter`); `X-Kundenbereich` für Partner/Plattform NUR gegen eine wirksame Unterstützung, sonst 404 auf JEDER Kundenroute (`/me` 200 ohne Kundenbereich); `X-Tenant-Id` der Plattform bleibt bis IP-8 (W3); `TenantAwareDataSource` setzt `app.zugriff` (`unternehmen|standorte`) und `app.standort_ids` (`{uuid,…}`) in derselben Anweisung und setzt sie zurück; `GET /api/v1/me` leitet nur mit `RechteAbleitung` ab (Standorte mit Rechten je Aktion der ganzen Matrix aus dem Jar, OCPP-Stufe, künftig, Teilansicht, Unterstützungen mit Banner, Kundenadministratoren). ⚠ `site_scope` (IP-5) liest die Einstellungen, leer = kein Standort-Zaun; ⚠ ein angenommener Unterstützer SCHREIBT bis IP-6 wie ein Kundenkonto; ⚠ `KennzahlAufrufer`/`ProtokollAkteur` unverändert · `uems-zugriff-kontext.md`
- **UEMS-Standort-Zaun: Policy `site_scope`, Prüfpunkt `Geltungsbereich`, Architektur-Test gegen Messdaten ohne `site` (AP-03 IP-5)** — `V20260915190000`: RESTRICTIVE `site_scope` auf `site`, `standort`, `anlage_standort`, `ort`, `measurement_point` UND `device` (Nachtrag: Geräte-Routen `/devices/{id}` lasen ohne Anlage); `app.zugriff` leer oder `unternehmen` = alle Zeilen des Kundenbereichs, sonst Standort HEUTE (Zone des Unternehmens) in `app.standort_ids`, jeder andere Wert = enger Zaun; WITH CHECK auf standort/anlage_standort/measurement_point/device, `site`/`ort` anlegen frei (Recht IP-6); `zugriff/Geltungsbereich.requireSite` (404) / `siteVisible` ersetzt `existsForCurrentTenant` an 39 Einstiegen (Methode entfernt), Geräte-Verlauf/-CSV prüft Anlage des Geräts UND `?siteId=`; `SiteScopeArchitekturTest` zählt je Datei und Tabelle Anweisungen auf `telemetry*`/Rollups ohne `site` gegen eine Liste mit Grund und nachgelesenem Beleg. ⚠ `messstelle` ungezäunt (Register `OFFEN`, IP-10/IP-11); ⚠ `/overview`/`/earnings` lesen alle Anlagen, zeigen nur sichtbare (Summen seit IP-10 ebenso, `uems-teilansicht.md`); ⚠ Anlage ohne gültige Zuordnung für standortbeschränkte unsichtbar; ⚠ Kundenkonto NIE zugewiesen = unternehmensweit (E12 in der Anfrage), nur beendete Zuweisungen = nichts; ⚠ `/me` und `TeilansichtDienst` lesen per `ganzenKundenbereichLesen` ohne Zaun (die einzigen zwei Aufrufer) · `uems-standort-zaun.md`
- **UEMS-Rechte an den Schreibrouten: `@Recht`, `RechtInterceptor`, `RechtPruefung` (AP-03 IP-6)** — jede Kunden-Schreibroute trägt `@Recht(kennung, ziel)` unter der Mapping-Zeile oder steht mit Grund in `RechtRoutenArchitekturTest.OHNE_RECHT` (113 mit Recht, 52 in der Liste, davon 46 Steuerung = IP-7); Geltungsbereich vor Aktion: unsichtbar → die Route antwortet selbst, sichtbar außerhalb (Messstelle, Bezugsgröße, `geraet`) → dieselbe 404 wie eine unbekannte Kennung, sonst 403 `recht_fehlt` mit `rolle_noetig`/`umfang_noetig`; Urteil `RechteAbleitung.darf` mit der Matrix-Datei und dem Aufrufer aus `ZugriffContext` (E12, Unterstützung, `P` für die Plattform); `DIENST` = Vorprüfung „irgendwo“ + genaue Prüfung im Dienst/Controller (Kennzahl, Bericht, Korrektur, Box anmelden, Bezugsgröße); `aenderung.rueckwirkend` bei „gültig ab“ vor heute; Umschalter und Token ohne Kontoart ungeprüft bis IP-8; `KennzahlAufrufer`/`KorrekturRechte.aufrufer` lesen den Kontext; `OrtRepository.anlegen` ohne `RETURNING` (enger Zaun) · `uems-rechte-schreibrouten.md`
- **UEMS-Rechte an der Steuerung und das Akteur-Vokabular (AP-03 IP-7)** — die 50 Schreibwege der Gruppe 4 tragen `@Recht` (163 Routen gesamt, nur noch zwei öffentliche ohne): Handeingriff und Betriebsweise sind Betrieb im Rahmen (Bedienberechtigt, Unterstützer „Einrichten und Bedienen“, E4), Freigabe · Grenze · OCPP-Regelung · Preisblatt bleiben beim Kundenadministrator, Anbinden · Schalt-Test · Register sind Einrichten; zwei Rechte in einem Aufruf lösen `DIENST` + genaue Prüfung im Handler (`…/funktionen/steuern` je Aktion, `…/charging-config` je Feld); Vorschau und Ersparnis-Simulation hängen am Lese-Recht; OCPP-Stufen kommen aus der Zuweisung (E13, `RechtPruefung.ocppStufe`), Realm-Rollen nur noch ohne Zugriff-Kontext und am Umschalter; `V20260916010000` bringt EIN Akteur-Vokabular (`actor_sub/name/rolle/art`) in Register-Journal, Handeingriff, Befehls-Verlauf und Änderungsprotokoll — `ort_aenderung.akteur_*` heißt jetzt `actor_*`, der Bestand bekommt nur die Art, nie eine geratene Rolle; ⚠ **die Stufen-Route ist die EINZIGE lesende Kundenroute, die seit IP-7 nicht mehr byte-gleich zu vor IP-4/IP-5 antwortet** — `ZugriffZaunApiTest` nagelt sie als `stufenwechsel` auf den Stufennamen fest, statt sie durchzulassen; ⚠ **die Steuerungs-Achse folgt erst einer ECHTEN Zuweisung:** ein Bestandskonto (E12, nie zugewiesen, Kundenbereich ohne Stichtag) führt `RechtPruefung.benutzer` unternehmensweit als KUNDENADMINISTRATOR — `ocppStufe` gibt für es aber LEER zurück, damit die Realm-Rolle wie vor IP-7 stehen bleibt und nicht jedes bestehende Kundenkonto still den Hardware-Befehlssatz bekommt (firstmate 16.09.2026); die volle Stufe holt sich ein Kundenadministrator, indem er eine Zuweisung gibt · `uems-rechte-steuerung.md`
- **UEMS-Stichtag der Bestandsregel E12: `zugriff_bestand` (AP-03, Befund aus IP-5)** — `V20260916060000`: EINE Zeile je Kundenbereich (`stichtag`, `herkunft` = `bestandslauf`|`neuer_kundenbereich`, `konten`), RLS + FORCE, App-Rolle nur `SELECT`/`INSERT` (nie `UPDATE`); **keine Zeile = die Regel E12 gilt** (kein Bestandskonto wird ausgesperrt, wenn der Start-Lauf aus ist, scheiterte oder noch nicht dran war), **Zeile da = sie gilt nicht mehr** (ein Konto ohne Zuweisung ist NEU: `standorte` + `{}`, keine Zuweisung aus `RechtPruefung.benutzer`, Protokolleintrag ohne Rolle). Gesetzt nur von wem die VOLLSTÄNDIGE Kontenliste sieht: `ZugriffBestand.bestandAbschliessen` aus `ZugriffBestandLaeufer` bzw. `KundenbenutzerAngelegt.neuerKundenbereich` (nur `RegistrationController`); `AdminController.createUser` nie. `ZugriffContext.Zugriff.nieZugewiesen` heißt jetzt `bestandskonto` (beide Hälften in EINER Anweisung). ⚠ kein Rechnen gegen den Zeitpunkt — er ist die Aussage „übernommen"; ⚠ eine an `KeycloakAdminClient.MAX_KONTEN_JE_KUNDENBEREICH` abgeschnittene Liste setzt keinen Stichtag; ⚠ im Testlauf ist der Start-Lauf aus, also messen alle Bestandstests weiter die alte Regel; ⚠ die Tabelle gehört ins Offboarding (`TenantRepository.deleteById`) · `uems-zugriff-stichtag.md`
- **UEMS-Unterstützung: gewähren, verlängern, beenden, Anfrage und Notfall-Zugriff (AP-03 IP-8)** — `POST/PUT/DELETE /api/v1/unterstützung` (Recht `unterstuetzung.verwalten` = U, Lesewege prüfen es im Rumpf), `…/anfragen` + `…/hinweise`, dazu `POST /api/v1/admin/tenants/{t}/unterstuetzung/anfrage|notfall`; `V20260916070000`: `unterstuetzung_anfrage` (+ Standorte) und `unterstuetzung_hinweis` — Wunsch und Postfach, NIE Erlaubnis. Eine Unterstützung IST eine Zuweisung (`zugriff`, IP-2), ihr Griff die kleinste `id` der Gewährung; sie ENDET VON SELBST (`zugriff_zeitraum`), der `AblaufLaeufer` (minuetlich, Testlauf AUS, prod AN) trägt nur `ablaufen` nach und erinnert 7 Tage vorher; Notfall = 24 h, Grund PFLICHT (sonst 422 und nichts angelegt), Hinweis an jeden Kundenadministrator; Entzug wirkt SOFORT; Verlängern = neue Zeile + Protokollwort `verlaengern` (zwei neue Wörter im `aenderung`-Vokabular, vier Zwillinge). ⚠ Partner-Spiegel `aktiv` statt `angelegt` bis IP-14; Startpasswort genau EINMAL; `X-Tenant-Id`-Umschalter bleibt an (Schalter `umschalter-enabled` liegt bereit, umgelegt mit IP-15); Migration in `UemsZugriffMigrationTest.BAUEN_DARAUF_AUF`; Offboarding räumt vor `zugriff` ab · `uems-unterstuetzung.md`
- **UEMS-Entzug: Sofortwirkung, Fehlerbild `zugriff_beendet` und die zwei 409 (AP-03 IP-9)** — `POST`/`DELETE /api/v1/zugriff` (Recht `zuweisung.verwalten` = U); der Entzug wirkt mit der NÄCHSTEN Anfrage, weil `ZugriffKontextLader` die Zuweisungen je Anfrage liest (`ZugriffRepository.stand` = wirksame UND vorbeie in EINER Abfrage) — kein Zwischenspeicher, kein Token-Ablauf. Wer JEDE wirksame Zuweisung verloren hat, bekommt im `ZugriffFilter` 404 `zugriff_beendet` auf jeder Kundenroute außer `/api/v1/me` (A6); wer EINEN Standort verliert und andere behält, bekommt denselben Grund aus `RechteAbleitung.darf`; wer den Zugang NIE hatte, weiter die stumme 404 (W2). Die zwei 409 spricht der Vertrag (`RechteAbleitung.zuweisungAendern`): `eigene_zuweisung` (W12) und `letzter_kundenadministrator`; der EINE Prüfpunkt ist `ZugriffAenderung`, und `ZugriffAenderungArchitekturTest` hält die Liste der drei Klassen, die `zugriff` überhaupt schreiben. Handeingriffe BLEIBEN (E15): `device_override` unverändert, nur das neue Feld `etikett` der Jetzt-Zone sagt „(Bedienrecht beendet am …)“. `V20260916150000` weitet `ort_aenderung_art_chk` um `zugriff_zugewiesen`/`zugriff_entzogen` — zwei Protokolle je Vorgang (Kundenbereich + Standort, AP-02 §4.4). ⚠ `standortName` trägt den Standort-Zaun (nur vor dem Zaun gelesen brauchbar); ⚠ eine nur KÜNFTIGE Zuweisung ist nicht „beendet“; ⚠ die Unterstützung geht bewusst NICHT durch den Prüfpunkt (Notfall gewährt sich selbst, E8); ⚠ die neuen Arten sind Punkte (`gilt_bis` = `gilt_ab`) und keine Strukturänderung; ⚠ Standort archivieren beendet seine Zuweisungen nicht (Befund) · `uems-zugriff-entzug.md`
- **UEMS-Teilansicht serverseitig: Summen über die sichtbaren Standorte und `teilansicht` (AP-03 IP-10)** — `/overview`, `/earnings` und `/standorte` tragen additiv `teilansicht {sichtbar, gesamt}` (`gesamt` = ANZAHL der Standorte des Kundenbereichs, nie eine Energie- oder Geldsumme), `/earnings` nimmt eine Standort-MENGE (`?standort=…`, wiederholbar) statt „eine oder alle“ und sein `vergleich` summiert über die Zeilen der Antwort; `TeilansichtDienst` liest `gesamt` per `ganzenKundenbereichLesen` in einer EIGENEN Transaktion (`REQUIRES_NEW`), `Geltungsbereich.requireStandort` ist der 404-Prüfpunkt der Menge (A14). Der EINE echte Befund: `OverviewRepository.storageTotals` las `FROM asset` ohne `JOIN site` — `asset` trägt nur die Mandanten-Policy, also bekam ein Bearbeiter EINES Standorts die Speicher-Summe ALLER (gemessen: 210 600 statt 9 100) und konnte die fremde Kapazität als Differenz ableiten; jetzt über `site` gezäunt. ⚠ eine Σ über eine UNGEZÄUNTE Tabelle (`asset`, Rollups, `schedule`, `flow_definition`) muss selbst über `site` gehen — je Anlage ist sie harmlos, als Summe ein Leck; ⚠ `/sites`, `/devices`, `/edge-versions` tragen das Feld NICHT (nackte Liste, Umschlag = Vertragsbruch), benannte Abweichung, einzulösen mit AP-03 IP-12; ⚠ ein Zeichenvergleich über zwei Stände mit unterschiedlich vielen Standorten muss `teilansicht` ausblenden (`BestandsuebernahmeApiTest.ohneStandort`) · `uems-teilansicht.md`

## Schnittstellen

- **UEMS-Funktionen-Routen: lesen, starten, anhalten, fortsetzen, beenden (AP-01 IP-3, R1/R2)** — `GET /api/v1/funktionen` (je Standort beide Funktionen mit Zustand/„es fehlt“, je Anlage Teilnahme + Prüfliste + `wege` + erlaubte `aktionen`, „läuft an x von y Standorten“), `PUT /api/v1/sites/{id}/funktionen/steuern` und `PUT /api/v1/standorte/{id}/funktionen/steuern` (`{aktion}`; 404 fremd vor 400, 409 = Grund des Vertrags mit `fehlt`/`wege`), keine Migration; Regel NUR `FunktionZustandAbleitung`, Eingänge `FunktionFakten`. ⚠ Prüfliste in DERSELBEN Transaktion nach `UnternehmenRepository.sperren`; gespeichert ist der Lebenslauf, entwurf/eingerichtet entscheidet jedes Lesen neu; Anhalten UND Beenden `putRuhe`, Starten/Fortsetzen `clearRuhe`, Push `afterCommit`; ⚠ Grenze grün nur mit heute gebundenem Netzanschluss mit `vereinbart_kw` (gesetzte Netzgrenze ≤ vereinbart) — ohne ihn kein Start und kein Fortsetzen (bewusst, R2); Selbstbau-Schalter brauchen einen Schalt-Test ≤ 90 Tage · `uems-funktionen-routen.md`
- **UEMS-Messstellen-API: anlegen, lesen, bearbeiten, anhalten, fortsetzen, archivieren** — `/api/v1/messstellen` in snake_case (die Antwort IST die Messstelle des Schemas + `id`/`fehlt`/`angehalten_ab`/`archiviert_am`), Anfrage streng (unbekanntes Feld 400), Regeln nur aus `MessstelleRegeln`, Urheber an EINER Stelle (`ProtokollAkteur`), Übergänge auf die Minute, nie Zukunft, streng nach dem Vorgänger · `uems-messstellen-api-anlegen-lesen-bear.md`
- **UEMS-Messstelle zuordnen: Ort und elektrische Stellung, zeitgültig je Tag** — `V20260911230000` `messstelle_ort`/`messstelle_stellung`, `PUT /api/v1/messstellen/{id}/ort|stellung` (Tag, Vortag beendet, `korrektur`, rückwirkend im Protokoll), `GET …/{id}/standort?am=`; Ort über `OrtsbaumAbleitung.eintrag` am Baum von `StandortService`, Stellung über `MessstelleRegeln.stellungPruefen` an jedem Änderungstag inkl. der Unterzähler; der zweite Hauptzähler braucht die führende Quelle (IP-13) desselben Zählers; Haken `OrtsbaumMessstellen` gefüllt (Archiv-Sperre, `messstellenZahl`); Prozess-Zuordnung seit AP-10 IP-7 · `uems-messstelle-ort-stellung-zuordnung.md`
- **UEMS-Standort-Lesemodell: Unternehmen und Standorte zum Stichtag** — `GET /api/v1/unternehmen`, `GET /api/v1/standorte?stichtag=` (+ `/{id}`), additiv `standort {id, name, kurzzeichen, gueltigAb}` an `/overview` und am NEUEN `/sites/{id}`; Ableitung = `OrtsbaumAbleitung.standAm`, ein Standort besteht ab `created_at` ODER früher, wenn etwas früher an ihm hängt; ohne Unternehmen-Zeile `nicht_angelegt`, nie 500; neuer Mandant bekommt sein Unternehmen im selben Statement (`TenantRepository.create`) · `uems-standort-lesemodell-unternehmen-st.md`
- **UEMS-Datenquellen-API: anlegen, bearbeiten, von genau der Box prüfen, zuweisen, Protokoll** — `/api/v1/sites/{siteId}/data-sources` in snake_case, Urteil nur aus `DatenquelleRegeln` (erste Box = `anlegen`, weitere = `wechsel`), Prüfergebnis nie aus der Anfrage sondern aus `data_source_aenderung` (jüngste Prüfung von GENAU der Box für die heutige Adresse), `ProbeService.probeBox` fragt auf dem Topic der Box (Heimat-Anlage), Wechsel = beenden + eintragen in EINER Transaktion · `uems-datenquellen-api-anlegen-bearbeiten.md`
- **UEMS-Lesebudget je Box beim Anlegen/Wechseln: Rechnung + zwei Auswege** — `MeasurementBudget.estimateSources` rechnet Kanäle × Takt, Blockanfragen und heutige Java-Kosten vor JEDEM Zeitraumsschreiben; 422 `budget_ueberschritten` trägt Quellen-/Box-Zahlen, freie Kapazität der Standort-Boxen sowie „Takt … s“/„Box …“; Ablehnung lässt jeden bestehenden Zeitraum unberührt; noch KEIN AP-07-Kostenvertrag und kein Edge-Release · `uems-datenquelle-budget-je-box.md`
- **UEMS-Vorschlagsliste Bestand: Komponenten zu Datenquellen je Box, Übernahme erst nach Bestätigung** — `GET …/data-sources/vorschlag` liest nur, `POST …/vorschlag/uebernehmen` schreibt Quelle + Zuständigkeit ab Reihenbeginn + beide Verweise + EIN Protokoll in EINER Transaktion; Gruppierung nur aus `DatenquelleRegeln.vorschlagsliste` (Geschwister beim Wechselrichter, benannt ausgelassen), Box = eigene/Station/führende, Push-Gleichheit bewiesen · `uems-datenquelle-vorschlagsliste-bestand.md`
- **UEMS-Messkanal-Read-Model und Größe/Richtung je Messpunkt (Katalog 2026.09.11.1)** — `GET /api/v1/sites/{siteId}/komponenten/{entityId}/messkanaele` liefert je Selektion Kanal, Anzeigename, Einheit, Wertart, Vertrags-Größe/-Richtung (Eingänge der Passung) + Katalogwörter `quantity`/`direction`; der Katalog trennt Inhaltsstand (`VERSION`) und Laufzeitstand der Box (`RUNTIME_VERSION`, `MeasurementCatalog.version()`), Box-Ableitungen bleiben byte-gleich · `uems-messkanal-read-model-richtung-groesse.md`
- **UEMS-Standort-Schreibrouten: anlegen, bearbeiten, archivieren, wiederherstellen + `PUT /api/v1/unternehmen`** — Regeln NUR aus `OrtsbaumAbleitung` (Baum des Lesemodells, auf Kurzzeichen umgeschlüsselt); `V20260911210000`: EINE Kurzzeichen-Belegung für Standort UND Gebäude/Bereich (nie wiederverwendet) + Zähler `uems_ort_kurzzeichen`; je Schreibvorgang genau ein `ort_aenderung`-Eintrag über `OrtProtokoll`; die Archiv-Lücke lebt nur im Protokoll-Paar archiviert → wiederhergestellt; Messstellen-Sperre über den Haken `OrtsbaumMessstellen` (AP-04 IP-7) · `uems-standort-schreibrouten-kurzzeichen-archiv.md`
- **UEMS-Quellenbindung: welche Messstelle ab wann aus welchem Messkanal liest (führend · Vergleich)** — `V20260911250000` `messstelle_quelle`: Messkanal = Komponente + Einbau (aus der Speisung zu `gueltig_ab`, nie gewählt) + Kanal, halboffen auf die Minute, nie überschrieben (nur EINMAL beendet, Trigger + Spaltenrechte), drei Exklusionen (eine führende je Größe, Vergleich nie doppelt, ein Messwert führt eine Messstelle); `POST/GET …/messstellen/{id}/quellen`, `GET …/quellen/{qid}`, `PUT …/quellen/{qid}/beenden`; Regeln + `kein_geraet_zum_zeitpunkt`/`bindung_bereits_beendet`/Rückwirkung aus `MessstelleRegeln`; `speist` im Messkanal-Read-Model; der Hauptzähler vergleicht die Komponente der Quelle; der Vorzeichen-Wert (`import_export`) bindet seit AP-08 IP-7 nur mit `anteil` · `uems-quellenbindung-messstelle-messkanal.md`
- **UEMS-Anteil an der Quellenbindung: EIN Vorzeichen-Kanal speist Bezug UND Abgabe — je Rohwert, nie je Mittelwert (AP-08 IP-7, E15/E12 = A)** — `V20260913180000`: `messstelle_quelle.anteil` (`positiv` → Bezug `max(0, P)`, `negativ` → Abgabe `max(0, −P)`, NULL = ganzer Wert), Regel 7 mit der Ausnahme „Anteil“ statt 422 (ohne Anteil bleibt Grund `richtung`, neu Grund `anteil`), `kanal_bereits_fuehrend`/Exklusion JE Anteil; geteilt nur in `VerbrauchRegeln.anteilJeRohwert` ⟷ `verbrauch.py` (F19: MS-01 12,8 kW · 3,2 kWh, MS-02 22,8 kW · 5,7 kWh, `gegenprobe` = Option C), gelesen von `QuelleAnteilWerte` (noch ruft niemand an) und dem Register-Letztwert. ⚠ Box-Vorzeichen ist schon im Rohwert — nie zweimal; ⚠ kein stiller Saldo: eine Bindung mit Anteil macht die Viertelstunde des ganzen Werts nie zur Energie; ⚠ Formel-Term `anteil_wartet_auf_ap08` bleibt (Verlauf aus `rollup_15m`-Mitteln). Dazu optional `messstelle.anschlussleistung_kw` (Höchstzuwachs, über die Zeile gelesen) und Katalog `wertebereich_modul`/`laeuft_ueber` (`validate.py --ohne-wertebereich`) · `uems-quelle-anteil.md`
- **UEMS-Einstellungs-Fassungen je Quelle: Wandler, Skalierung, Vorzeichen zeitgültig, Wirkung nur ab Gültigkeitsbeginn** — `V20260911280000` `quelle_einstellung` an Einbau (+ Komponente + Kanal), halboffen auf die Minute, je Quelle und Art EINE, nur verkürzt; Fassung 1 aus der heutigen Verbindung („gilt seit Beginn“), kein gespeicherter Wert ändert sich; `GET/POST /api/v1/geraete/{id}/einstellungen`, Protokoll `einstellung_geaendert` an jeder gespeisten Messstelle; der Hebel „Auf ×10 stellen“ schreibt im PUT der Komponente eine Fassung „ab jetzt“ · `uems-quelle-einstellung-fassungen.md`
- **UEMS-Gebäude/Bereich-Schreibrouten und Ortsbaum je Standort** — `GET /api/v1/standorte/{id}/orte?stichtag=`, `POST …/orte`, `PUT /api/v1/orte/{id}`, `PUT /api/v1/orte/{id}/flaeche`; jede Regel aus `OrtsbaumAbleitung` gegen den Baum des Lesemodells (Anlegen = `eintrag` mit dem neuen Knoten ohne Intervall → 400/422/409 mit Vertragsgrund; Name = `nameBelegt` am ersten Tag UND heute; Fläche = NEU `flaecheEintrag`, gleicher Tag = Korrektur); Zeitzone des STANDORTS für „gültig ab“ und „rückwirkend“; dieselben Bausteine wie IP-4 (`OrtFelder`, `OrtKurzzeichen`, `OrtProtokoll`, `OrtAnfrage` — ganze Zahlen streng), Schreibvorgänge je Kundenbereich nacheinander (Sperre auf `unternehmen`); `messstellenZahl` = `null` bis AP-04 IP-7 · `uems-orte-schreibweg-gebaeude-bereich-fl.md`
- **UEMS-Messstellen-Register: `GET /api/v1/messstellen` mit Stichtag, Ort, Stellung, Quelle und „davor …“** — additiv zur Liste aus IP-3: `register` (Zeile je Messstelle zum Stichtag), `stichtag`, `zeitpunkt`, `teilansicht`; Ort + ABGELEITETER Standort über `OrtsbaumAbleitung.verortung` (wie `…/standort?am=`), Quelle der Hauptgröße zum Zeitpunkt mit Gerät, Messwert-Anzeigename und `davor` (Z-5a), `stand` = `gebunden`·`berechnet`·`keine_datenquelle`; Filter Standort · Ort (mit Teilbaum) · Anlage · Zustand · `ohneQuelle` — eine unbekannte ID findet NICHTS; Beobachtung und letzter Wert bleiben `null` (IP-15); alle Messstellen-Daten aus EINER Abfrage (`MessstelleRegisterRepository`), Ortsbaum aus dem festen Lesezug — 100 Messstellen ~210 ms, am DataSource nachgezählt · `uems-messstellen-register-lesemodell.md`
- **UEMS-Beobachtung je Messstelle: „liefert Daten“, letzter Wert und „x von y“** — der ERSTE Aufrufer des Zustandsvertrags: `beobachtung`/`letzter_wert` je Größe über ihre FÜHRENDE Bindung (Zustand nur aus `ZustandAbleitung`, Toleranz `min(max(3 × Kadenz, 300 s), 86 400 s)` — 2 × Kadenz ist die LÜCKE und NICHT hier), Wert-Schlüssel = Komponente + Kanal + **Beginn der Bindung** (sonst hielte Z-5a die Bindung von Z-5b am Leben), „Wartet auf erste Daten von Z-5b“ hängt den Gerätenamen NUR an den Satz; `aggregat` „x von y Messstellen liefern Daten“ je Standort und Unternehmen über die gezeigten Zeilen (berechnete zählen seit AP-10 IP-9 mit: `berechnung`, Eintrag Bilanz je Anlage); EIN zusätzlicher Werte-Zug ohne Messstellen-Tabelle — 100 Messstellen weiter 10 Abfragen, 27 ms · `uems-messstellen-beobachtung-letzter-wert.md`
- **UEMS-Datenannahme: Messzeit-Plausibilität (E13), je Wert verwerfen, Kern-`seq` weiterreichen, Ereignisse auf `events.raw`** — Umschlag = Einheit für Fassung/Form/Kennung, WERT = Einheit für Inhalt und Messzeit (> 300 s vor `clock_ahead`, > 90 Tage `too_old`, Uhr des Umschlags falsch → nichts); Ablehnungen gebündelt je Umschlag+Art+Grund mit `anzahl` (Urheber `datenannahme`, `box` = device_id aus dem Topic), Box-Umschläge `…/v2/events` je Eintrag ein `events.raw` (Zwilling `BoxEventsValidator`); Topic `events.raw` legt die Datenannahme seit 15.09.2026 selbst an (3 × 1, anlegen → zurücklesen → bereit, ein vorhandenes nie angepasst, Abweichung = WARN + Health-Detail, abschaltbar; ersetzt Punkt 4 von b07-recreate D, der Grund steht in Falle 1) — ⚠ fehlt es trotzdem (aus, keine Rechte, Broker weg), Rückfall aufs Verhalten vor IP-5 (nur Log + Zähler, Box-Adapter wartet, Readiness DOWN), `clock_jump` offen (zustandslos) · `uems-datenannahme-ereignisse.md`
- **UEMS-Bestandsübernahme der Standorte: eine Anlage automatisch, mehrere als Vorschlag** — Start-Läufer (kein Takt, Vorgabe AN, im Testlauf aus) wendet je Kundenbereich die reine Regel an: schon ein Standort — auch ein archivierter — oder schon Vorschläge → nichts; GENAU EINE Anlage → Standort mit ihrem Namen (Entwurf, „es fehlt: Adresse“) + Zuordnung ab dem TAG von `site.created_at`; MEHRERE → je Anlage eine Zeile `standort_vorschlag` (`V20260911290000`), keine Zuordnung. `POST /api/v1/sites` nimmt additiv `standortId` (vorbelegt bei genau einem, 422 `standort_waehlen` bei mehreren); W5: die Anlage darf gehen, ihre Zuordnung bleibt als beendetes Intervall (der Fremdschlüssel auf `site` fällt, ein Trigger hält die Einfüge-Hälfte); Zwillinge `uems/BestandsuebernahmeAbleitung` ⟷ `uemsBestandsuebernahme.ts` gegen die Familie `bestandsuebernahme` von `ortsbaum-vectors.json` · `uems-bestandsuebernahme-der-standorte.md`
- **UEMS-Vorschlagsliste der Bestandsübernahme: aus Komponenten werden Messstellen (je Standort)** — `GET /api/v1/standorte/{id}/messstellen-vorschlag` liest NUR, `POST …/uebernehmen` legt an: je Komponente und Fluss höchstens EIN Vorschlag (Zählerstand vor Leistung, `integration` gekennzeichnet; Ladestand als Nebengröße), Stellung aus der Topologie (`grid`+`primary` → Hauptzähler, sonst „Unterzähler von“ oder LEER), Name = Alias, Ort = Standort, `ab` = Beginn der LAUFENDEN Speisung (nie über einen Gerätewechsel, nie vor dem Standort); nicht Vorgeschlagenes mit Grund (`attribut_kanal`, `abgeleitet`, `vergleich_kandidat`, `vorzeichen_wert`, …); die Übernahme schreibt in EINER Transaktion über die Wege von IP-3/IP-7/IP-13 (Herkunft `bestandsuebernahme`, `V20260911310000`), bestätigt wird nur Gezeigtes (409 `vorschlag_geaendert`), zweiter Aufruf = 0 neue · `uems-messstellen-vorschlagsliste-bestand.md`
- **Portal-Zählerwechsel (AP-04 IP-18)** — Modal aus Quelle-Karte und Geräte-/Komponentenseite, zwei Schritte und EIN POST; bestätigte Folgen aus 201, Quelle/Beobachtung neu gelesen, gespeicherte Komponentenmarken über Ereignissätze. Keine Vorschau-/Einstellungs-Erweiterung der Route; Grenzen und Prüfungen · `uems-zaehlerwechsel-portal.md`
- **UEMS-Zählerwechsel: EIN Vorgang, zwei Einstiege — alles oder nichts** — `POST …/messstellen/{id}/quellen/wechsel` und `POST /api/v1/geraete/{id}/austausch` führen in EINER Transaktion Ausbau, Einbau, Wandern der Speisung, Bindungswechsel genau zum Zeitpunkt, Ablesestände, übernommene Einstellungen und je Messstelle EIN Protokoll `zaehler_gewechselt` aus; alles wird geprüft, BEVOR die erste Zeile geschrieben wird; NEU `MessstelleRegeln.wechselPruefen` (Familie `wechsel`) ⟷ `uemsMessstelle.ts`, Migration `V20260912120000` weitet nur zwei Vokabulare; Komponenten-Fassung nur bei wirklich neuer Verbindung, Marke `device_replaced` als isolierter Savepoint; Karten = IP-19 · `uems-zaehlerwechsel-ein-vorgang.md`
- **UEMS-AP-10 „Gesamtwert": die Formel der berechneten Messstelle (gewichtete Summe)** — `messstelle_formel_term` (`V20260912093000`, ein Term = Messkanal `entity_id`+`point_key` ODER Baustein `quell_messstelle_id`, je Vorzeichen+Faktor); Hauptgröße aus den Termen ABGELEITET (gemischt → `groessen_gemischt`, undirekter Kanal ist kein Term), Cloud-Berechnung on-the-fly (Live-Wert + 15-min-Verlauf, `null` statt Teilsumme), Edge unberührt; Zwillinge `MessstelleFormelRegeln`⟷`uemsMessstelleFormel.ts` gegen `messstelle-formel-vectors.json`; `POST /api/v1/messstellen/berechnet`, `GET …/{id}/formel|wert|verlauf`; der Formel-Stand geht ehrlich in `MessstelleService.lebenszyklus` ein · `gesamtwert-berechnete-messstelle-formel-ap10.md`
- **UEMS-Formel-Fassungen je Tag (AP-10 IP-3, PR #688 nachziehen 1/4)** — `V20260912210000` `messstelle_formel_fassung` (Tage `[]`, `aufgehoben_am`, `formel_typ`), `fassung_id` am Term, Bestand = Fassung 1; Regel `MessstelleFormelRegeln.fassungEintrag` (nach der jüngsten, Vortag beendet, 422 `formel_fassung_ueberlappt`, `rueckwirkend` + Abzeichen); `GET …/{id}/formel?am=`, `POST …/{id}/formel/fassungen`; Live-Wert, Verlauf und Bausteine rechnen mit der Fassung DES TAGES. ⚠ Fassung 1 hat KEINEN ersten Tag (`gueltig_ab` null — sonst verlöre der Verlauf die Tage vor dem Anlegen); ⚠ ohne `am` ist die Antwort zeichengleich zu vorher (`fassung_am` nur mit `am`); `formel_fassung_ueberlappt` NICHT in `Fehler` (Vektor-Datei unberührt); Rechte E15: `messstelle.formel` für Anlegen + Fassung (Matrix-Nachtrag, keine Durchsetzung) · `uems-formel-fassungen-je-tag.md`
- **UEMS-Formel-Typen `rest` und `saldo`: feste Ergebnis-Richtung, Terme aus der Stellung (AP-10 IP-4, PR #688 nachziehen 2/4)** — KEINE Migration, keine Route: `MessstelleFormelRegeln.hauptgroesse`/`.periodenwert` ⟷ `uemsMessstelleFormel.ts` VERZWEIGEN je `formel_typ` (gewichtete Summe bleibt `formelGroesse`, vektor-gleich; `rest`/`saldo` rechnet `BilanzAbleitung` ⟷ `uemsBilanz.ts`). ⚠ **Richtung je Typ, nie aus Vorzeichen:** `rest` fest Wirkenergie · Bezug (100 − 60 − 30 = 10 kWh Bezug, F1), `saldo` fest Wirkenergie · `saldiert`; Kundensatz wörtlich aus `saetze` („10 kWh sind keiner Messstelle zugeordnet“ — nie „Verlust“). ⚠ **`saldiert` nur berechnet:** `richtungenNurBerechnet` am Größen-Katalog der MESSSTELLE + `groessePruefen(medium, art, g)`; ohne Art wie die DB-Funktion; `$defs/richtung` unverändert (Messkanäle/OpenAPI), Messpunkt-Katalog und Box-Laufzeitstand unberührt; speichern kann eine `rest`/`saldo`-Messstelle erst der erste Schreibweg mit Migration. NEU `BilanzAbleitung.restAusStellung` (Regel `rest_aus_stellung`, Stellungen aus dem Referenzunternehmen, nur Hauptzähler) · `uems-formel-typen-rest-saldo.md`
- **UEMS-Term-Art `verteilung` und `anteil`: benannte Ablehnung bis AP-08 IP-7 / AP-10 IP-8 (AP-10 IP-5, PR #688 nachziehen 3/4)** — `V20260913143000` additiv am Term (`eingang_art` + `verteilung`, `anteil` NULL = gesamt, `verteilung_ziel` mit Fremdschlüssel auf `kostenstelle` seit AP-10 IP-7); EINE Stelle `uems/AnteilLeseweg#lies` für Schreibweg, Live-Wert und Verlauf: `positiv`/`negativ` → 422 `anteil_wartet_auf_ap08`, Verteilungs-Term → 422 `verteilung_wartet_auf_ip8` (✅ seit AP-10 IP-8 eingelöst: liest den Anteil des Tages, `uems-verteilung.md`), Kundensätze im Block `leseweg` von `verteilung-vectors.json`, in Wert/Verlauf ein fehlender Term (Grund = Code) — nie geraten, nie `nicht_verteilt`; der Anteil gilt je TAG (Bucket-Tag, F13), der Aufruf für IP-8 ist verdrahtet (`tagesanteil` → `VerteilungRegeln.term`); `terme[]` ohne die Felder zeichengleich · `uems-term-art-verteilung.md`
- **UEMS-Löschwege: Abmelden baut die Box aus, Purge und Anlage-Entfernen lehnen mit Liste ab (AP-07 IP-11)** — `V20260913150000`: `device_measurement_*` → `device`/`site` `ON DELETE RESTRICT` (`NOT VALID`, gleiche Namen), `device.ausgebaut_am` + `status = 'ausgebaut'` (endgültig per Trigger), Aufkleber-Kennung nur unter nicht ausgebauten Boxen eindeutig; `DELETE /api/v1/devices/{id}` löscht NICHTS Aufgezeichnetes (Captain: auch v1-Telemetrie und OCPP bleiben) und löst nur die Topologie; Purge und `DELETE /api/v1/sites/{id}` → `409 messstellen_belege` mit der Liste, wenn eine Reihe JE gebunden war (`uems_messreihen_belege`: Bindung, Protokoll `quelle_gebunden`, Formel-Term), und schreiben nichts. ⚠ **Live-Regel:** jede Live-Fläche filtert `ausgebaut_am IS NULL`, Historie nie — umgestellt sind die Box-Listen, OCPP, Komponenten-Ist-Zustand, Messkanal-Auswahl, Lücken-Melder, Live-Karte und das Writer-Tor; 19 schon heute leckende Herzschlag-Leser sind Folgepaket · `uems-loeschwege.md`
- **UEMS-Belegschutz: was ein freigegebener Bericht zitiert, lässt sich nicht löschen (AP-12 IP-12, E13 S2, B12)** — `uems/BerichtsBelege` (Komponente → jede je gespeiste Messstelle → `uems_berichts_belege`) VOR dem ersten Schreiben an fünf Wegen: Komponente entfernen (`v2-entities`, `components/custom`, `components/battery`), Batterie am Standort abmelden und Verbraucher entfernen (beide über die §8-Zelle hinaus, firstmate 001 = A) → 409 `berichts_belege`; Purge und Anlage entfernen → 409 `messstellen_belege` wie IP-11 plus zweite Liste `berichtsstaende` (keine neue Ablehnung); `BelegeImWeg` mit Gegenstand KOMPONENTE und `codes`, OpenAPI-Schema `BelegeImWeg`; Gefahrenzone zeigt Vertragssatz + „Zur Messstelle …“; Box abmelden und Ort archivieren bleiben frei; ⚠ ein neuer Weg, der `measurement_point` löscht, braucht `pruefeKomponente`; Verwaltungs-Löschen ungeschützt (`vp-uems-belegschutz-verwaltungswege`) · `uems-belegschutz.md`
- **UEMS-Kostenstelle, Prozess und Messstelle → Prozess: beenden statt löschen, eine Ebene, nie länger als das Ziel (AP-10 IP-7)** — `V20260913160000` `kostenstelle` (FLACH), `prozess` (Eltern 0..1, EINE Ebene per Trigger `prozess_eine_ebene`), `messstelle_prozess` (Tage, ohne Anteil); `…/unternehmen/kostenstellen`, `…/unternehmen/prozesse` (+ `/{id}/beenden`), `GET/PUT …/messstellen/{id}/prozesse` (Satz ab Tag, neues Intervall endet mit seinem Prozess); kein DELETE, ein Ende nur vorgezogen, 409 `zuordnung_besteht` mit Liste. ⚠ **Ein Trigger-Paar hält „nie länger als das Ziel“ von beiden Seiten** (`uems_zuordnung_im_ziel` an der Zuordnung, `uems_ziel_deckt_zuordnungen` am Ziel, die Zuordnungen findet `uems_zuordnungen_ausserhalb` über `pg_trigger`) — die Verteilung (IP-8) hängt nur `uems_zuordnung_im_ziel('kostenstelle', …)` an; ⚠ `EXECUTE … INTO` setzt kein `FOUND`. Eingelöst: Bezugsgröße an Prozess/Kostenstelle (BZ-1…BZ-3 passen, `GELTUNG_WAEHLBAR` alle sieben), Fremdschlüssel auf `verteilung_ziel` (422 `verteilung_wartet_auf_ip8` bleibt); Rechte `kostenstelle.verwalten`/`prozess.verwalten` als Matrix-Nachtrag · `uems-kostenstelle-prozess.md`
- **UEMS-Verteilung auf Kostenstellen: 100 % je Tag zur Commit-Zeit, oder ausdrücklich „nicht verteilt“ (AP-10 IP-8, E11/E12 = A)** — `V20260913230000` `messstelle_verteilung` (Tage, `anteil_prozent` mit höchstens einer Nachkommastelle, `aufgehoben_am`), Constraint-Trigger `messstelle_verteilung_hundert_prozent` DEFERRABLE INITIALLY DEFERRED (ein Satz mit zwei Zielen ist zwischen den Anweisungen nie 100 %), Ende mit der Kostenstelle NUR über das Trigger-Paar aus IP-7; `PUT/GET /api/v1/messstellen/{id}/verteilung?am=`, Regel `VerteilungRegeln.satzAbTag` ⟷ TS (Satz ab Tag, Rest nach Ziel-Ende, Wiederholung = nichts, `korrektur` am selben Tag, leerer Satz = nicht verteilt); Ereignis `verteilung_geaendert` (27. Art, Bezug NUR Messstelle — beide Java-Zwillinge, TS, Schema, Ingest); Recht `messstelle.verteilung` eingetragen, nicht durchgesetzt; ⚠ `verteilung_wartet_auf_ip8` eingelöst: `AnteilLeseweg#lies` liest den Anteil des Tages, der Formel-Schreibweg prüft an der Stelle „Kostenstelle da“ (404) · `uems-verteilung.md`
- **UEMS-Netzanschluss als eigenes Objekt am Standort, Bindung 1 : 1 je Tag (AP-10 IP-6, E8)** — `V20260913235000` `netzanschluss` (Tage, `gueltig_ab` darf fehlen) + `anlage_netzanschluss` mit ZWEI Exklusionen (je Anlage UND je Anschluss): zweite Bindung derselben Anlage am selben Tag = 409 `bindung_ueberlappt`, ein Wechsel beendet die laufende am VORTAG (`NetzanschlussRegeln.bindung` sieht seit IP-6 alle Tage), eine Bindung endet mit ihrem Anschluss (Trigger-Paar aus IP-7 aufgerufen); Kennzeichen `NA-0001` nie weitergegeben, auch nach Beenden/Umbenennen (Belegung per Trigger, Zähler-Tabelle); `GET/POST /api/v1/standorte/{id}/netzanschluesse`, `GET/PUT …/{id}`, `POST …/{id}/anlagen`, geschlossener Satz `NetzanschlussAbgelehnt`, fremd = 404; `StandortLesemodell` füllt `netzanschluss` der Anlage (vorher `null`); die Anlage darf gehen (kein FK auf `site`, `beimLoeschen`). ⚠ Preis-/Grenzspalten bleiben an der Anlage (W9); MaLo ohne Prüfziffer (Befund); Recht `netzanschluss.verwalten` eingetragen, nicht durchgesetzt · `uems-netzanschluss.md`
- **UEMS-Bilanz je Anlage: der Rest ist eine Rechnung aus der Stellung, eine Rest-Messstelle nur auf Klick (AP-10 IP-9, E3/E18 = A)** — `GET /api/v1/sites/{siteId}/bilanz?periode=&am=` je Hauptzähler Bezug: Abschnitte gleicher Terme aus `BilanzAbleitung.restAusStellung` je TAG, Zufluss/Abfluss/zugeordnet aus `.summe` („mindestens …“, x von y), Rest aus `MessstelleFormelRegeln.periodenwert("rest")` über die Periodenwerte von `MessstelleWerteService`, Live (F18: 1,6 kW, `null` bei veraltet) über den PR-688-Weg; `POST …/bilanz/rest` legt MS-xxxx (Typ `rest`, OHNE Terme) mit Urheber an, nie zweimal (Sperre + Teil-Index `V20260913235700`, `rest_hauptzaehler_id`); ⚠ Stellungswechsel in der Periode = Tag für Tag, die Periodenzahl bringt IP-10; ⚠ Register-Aggregat zählt berechnete mit (`berechnung`, ohne Formel im Nenner); das befristete „vorläufig (Geräte-Verdichtung)“ an `wert`/`verlauf`/`live` ist mit IP-10 ENTFALLEN (Eintrag Periodenwerte berechneter Messstellen) · `uems-bilanz-lesemodell.md`
- **UEMS-Kostenstellen-Sicht: gemessen · verteilt · berechnet — und was niemandem gehört (AP-10 IP-11, E12/E13)** — `GET /api/v1/unternehmen/kostenstellen/{id}/energie?periode=&am=&version=`, keine Tabelle: `KostenstelleEnergieRegeln` (Regel `kostenstelle`, `verteilung-vectors.json` 1.2) RUFT `amTag`/`erbe`/Summenregel je TAG, die Periode ist die Summe der Tage (F13 10 000/5 500); 100 % gemessen → gemessen, < 100 % → verteilt, berechnete Messstelle → berechnet, Wert ohne Zeile → **nicht verteilt — nie aufgeteilt, nie weggelassen, in jeder Sicht gleich, zählt in `summe` nie mit**; ohne Zuordnung `menge: null` + `keine_zuordnung`, nie 0; Versionen ≥ 2 aus der Kaskade, `version=1` bleibt lesbar (F14 v2 = 12); Herkunft je Posten über `BilanzwertHerkunft` (Art verteilt); Ereignis `bilanz_neu_berechnet` (`V20260914140000`, nur cloud) aus `KorrekturKaskade` über `BilanzNeuBerechnet` — keine zweite Kaskade; seit 14.09.2026 warnt `doppelzaehlung` vor Posten, die in einem anderen enthalten sind, ohne eine Zahl zu ändern · `uems-kostenstelle-energie.md`
- **UEMS-Kostenstellen-Sicht warnt vor doppelter Zählung — und ändert keine Zahl (Captain-Entscheid 14.09.2026)** — keine Migration: `KostenstelleDoppelzaehlung.pruefe` (rein, Regel `doppelzaehlung` in `verteilung-vectors.json` 1.4, Sätze `doppelt_*`, nur Java) über dieselben Quellen wie die Zahlen und die Formeln je Tag aus `BerechnetePeriodenLauf.formelnJeTag` (`TermRef.verteilungZiel` neu), rekursiv über `BerechnetePeriode.reihenfolge`, Kreis benannt (`nicht_pruefbar`); Feld `doppelzaehlung` als LETZTES der Antwort. ⚠ Anteile ohne Menge: „ganz“ ⟺ Anteil der Summe × Beitrag ≥ 100 %, „Anteil 4200 von MS-07“ überdeckt MS-07 an 4100 nicht, abgezogen ist nicht enthalten; ⚠ `dieZahlenSindZeichengleich` hält jede Antwort bis zur Warnung byte-gleich zu `src/test/resources/uems/doppelzaehlung-vorher/` (aufgenommen auf cf9b8bb7) · `uems-kostenstelle-energie.md`
- **UEMS-Herkunft in den Routen: jede berechnete Zahl sagt, woraus sie entstanden ist (AP-10 IP-12, E13)** — Hülle `herkunft: {satz, fehlt}` an `…/messstellen/{kennzeichen}/werte` (jede gespeicherte berechnete Zahl), am `rest` von `…/sites/{id}/bilanz` und in der Kostenstellen-Sicht (`posten[].herkunft` verteilt, `tage[].herkunft` berechnet); `null` = nicht berechnet, nie eine leere Hülle; Eingänge in der VERSION aus `bilanzwert_eingang`, Anlass aus `messreihe_periode_version`, nichts nachgerechnet (`BilanzwertHerkunft.ausGespeichert` über `BilanzwertHerkunftLeser`); ⚠ BYTE-gleich zum Vektor (`BilanzwertHerkunftVectorsTest` schreibt mit dem Jackson-Stand der Routen), `periode_ende` an jedem Satz, Beträge ohne nachgestellte Nullen, Auslöser `correction MS-17 2026-10-18 Version 2` (auch die Kostenstellen-Sicht, vorher mit K-Kennung); `zuordnung` in `bilanzwert-herkunft-vectors.json` nennt je §7-Satz den Weg oder den Grund (F4/F7/F9/F11/F13 heute ohne Route) · `uems-bilanzwert-herkunft-routen.md`
- **UEMS-Änderungsprotokoll: drei Lesewege, zwei Zeitachsen (AP-04 IP-21)** — `GET …/messstellen/{id}/aenderungen`, `…/geraete/{id}/aenderungen`, `/api/v1/unternehmen/aenderungen` lesen die BESTEHENDEN Journale (kein neuer Schreibweg); ⚠ ZWEI ZEITACHSEN — `achse=wirkung` (Vorgabe, `gilt_ab`) vs. `eintrag` (`created_at`), die gewirkte steht in der ANTWORT und jede Zeile trägt beide Zeitpunkte plus `zeitform`; drei überbrückte Spalten-Unterschiede (Urheber `akteur_*`/`actor_*`, Tag vs. Zeitpunkt, fehlendes `rueckwirkend` der Datenquelle — AP-03 IP-7 vereinheitlicht sie); ein Gerät hat KEIN eigenes Journal (Bindeglied ist das Einbau-Kennzeichen im JSON, ein Wechsel steht in beiden Geräte-Protokollen); Unternehmens-Weg = EINE Abfrage über drei Journale mit stabilem Zeiger `<millis>:<quelle>:<id>` · `uems-aenderungsprotokoll-lesewege.md`
- **UEMS-Änderungsprotokoll der Ortsstruktur: je Gebäude/Bereich, je Standort samt Kindern und Anlagen, Zeitraum über die GÜLTIGKEIT (AP-02 IP-14, H2)** — `GET /api/v1/orte/{id}/aenderungen`, `…/standorte/{id}/aenderungen`, `/api/v1/unternehmen/aenderungen?achse=gueltigkeit` im Lesemodell von IP-21 (keine Migration, kein Schreibweg); ⚠ `gueltigkeit` = `reicht_in_zeitraum` des Ortsbaum-Vertrags in TAGEN (beide zählen mit), die Vorgabe bleibt `wirkung` — AP-12 fragt ausdrücklich; ⚠ `gilt_bis` kommt aus dem Journal je Sachverhalt (eine neue `ort_aenderung`-Art dort ergänzen), Messstellen/Datenquellen haben keins; ⚠ Standort-Umfang `OrtProtokollUmfang` (Tag oder Vortag, Anlagen-Umzug nur einmal); Portal `OrtAenderungen` = `ProtokollDialog` nach dem Eintrag, „rückwirkend (n Tage)“ aus dem TS-Zwilling, Menüeintrag „Änderungsprotokoll“ hinter „Verschieben …“ · `uems-ort-aenderungen.md`
- **UEMS-Kadenz zeitgültig an der Quellenbindung (E9)** — `quelle_kadenz` (`V20260912160000`) als Fassung je Bindung, halboffen auf die Minute, nur verkürzt, in ihrer Bindung; Vorgabe-Kette `KadenzRegeln.wirksam` = Fassung → Mess-Selektion → Katalog → 300 s (die drei hinteren sind die Ableitung von VOR IP-10, zeichengleich); Bestand nur bei EINDEUTIGER Auswahl übernommen, sonst nichts geraten; ⚠ der DRAHT bleibt (`…/v2/measurement-config` 2.0, `cadence_s` — nur die Quelle der Zahl wechselt, darum trägt `erwartet_s` genau dessen Schranken 1…86 400 s); Lesewege holen die Zahl ZUM ZEITPUNKT (`QuelleKadenzRepository.jeBindung`), Messkanal-Read-Model und `MeasurementBudget` noch nicht (IP-14 / AP-06); `GET/POST …/messstellen/{id}/quellen/{qid}/kadenz` · `uems-quelle-kadenz-zeitgueltig.md`
- **UEMS-Bezugsgrößen-Routen: anlegen, ändern, archivieren, löschen und die Werte mit Fassungen und Herkunft (AP-09 IP-5)** — `/api/v1/bezugsgroessen` (+ `…/{id}`, `…/{id}/archivieren`, `…/{id}/werte?von&bis&fassungen=wirksam|alle`) in snake_case; M1–M6 und der GESCHLOSSENE Satz der Ablehnungen (Status + Kundensatz) stehen im Vertrag (Block `verwalten`, Regel `verwalten`), Java `uems/BezugsgroesseRegeln` ⟷ OpenAPI ⟷ `bezugsgroesse.ts` gepinnt, jede Ablehnung schreibt nichts; Vokabular aus `bezugsdaten_vokabular()`, Kennzeichen = höchste je belegte Nummer + 1. ⚠ `V20260913120000`: DELETE ENG (nur ohne Wert, Trigger für jede Rolle), Verlauf-FK `SET NULL` → Grabstein, der Append-only-Trigger lässt NUR diesen Übergang durch; Protokoll-Art `geloescht`. ⚠ Stand der Kette aus `BezugsdatenRegeln.fassungen`; mit Vier-Augen-Fassung `stand_offen` (IP-7); nach Rücknahme `wirksamer_betrag` null. Rechte `bezugsgroesse.verwalten` (W8-Nachtrag) / `messwerte.ansehen`, keine Durchsetzung · `uems-bezugsgroessen-routen.md`
- **UEMS-Bezugsflächen aus der Ortsstruktur und Bezugs-Stammdaten mit Gültigkeit (AP-09 IP-6, E15/E17)** — eine Fläche wird GELESEN, nie ein zweites Mal gepflegt: `GET /api/v1/bezugsflaechen?periode_art&von&bis` (nur GET, 405 auf jeden Schreibweg) + additiv `bezugsflaechen` an `GET /api/v1/bezugsgroessen`, ohne ID/BZ-Kennzeichen, `schreibbar: false`; Fläche je Tag aus AP-02 `OrtsbaumAbleitung.flaecheZeitraum` (eigen · aus Gebäuden summiert), Wert am Stichtag = LETZTER Tag der Periode aus `BezugsdatenRegeln.stammdatum`, jeder Übergang NACH dem ersten Tag als S3-Kennzeichen („Fläche geändert am 01.01.2027 (3.100 → 3.400 m²)“, `stammdatum_saetze`), `null` statt 0. `V20260914151500` `bezugsgroesse_stammdatum` (Muster `flaeche_gueltigkeit`, M1-FK, ENABLE+FORCE RLS, kein DELETE, Offboarding) + `GET/PUT /api/v1/bezugsgroessen/{id}/stammdatum` (Regel `stammdatum_eintrag` ≡ `flaecheEintrag`, `StammdatumEintragWieFlaecheTest`); ⚠ CHECK `…_keine_flaeche_chk` über `bezugsdaten_groesse()`, neue Ablehnungen `wert_ungueltig`/`kein_stammdatum`, Lösch-/Identitäts-Trigger zählen Stammdaten mit · `uems-bezugsflaechen-stammdaten.md`
- **UEMS-Import-Vorschau der Bezugsdaten: sie zeigt alles und schreibt nichts (AP-09 IP-12)** — `POST /api/v1/bezugsdaten/importe/vorschau` (multipart `datei` + `zuordnung`) in EINER Nur-Lese-Transaktion: Regel `uems/ImportVorschau` (rein, ruft `CsvLeser`/`BezugsdatenRegeln`/`BezugsEinheit`/`BezugsPeriode` an) gegen Regel `vorschau` an B1, B2, B9–B13; ⚠ benannter Test `vorschauZweimalIdentischUndNullZeilenInBezugsgroesseWert` (Bestandsschutz aller Tabellen, Antwort Zeichen für Zeichen); ⚠ Datei-Fingerabdruck = SHA-256 der Bytes, Zeilen-Fingerabdruck = Bezugsgrößen-ID + Periode/Zeitpunkt UTC + normalisierter Betrag, NIE der Zeilentext; `datei_bekannt` ist ein Hinweis (nur schreibende Importe); Vorschau-Kennung 30 min, reserviert nichts; `V20260914173000` `bezugsdaten_import` (Fassungen, nie `vorschau`) + `_zeile` (append-only, Zeilentext zwei Jahre per Trigger + täglichem `ZeilentextAufbewahrung`) + `bezugsdaten_vorlage` (Fassungen), Vokabular-Funktion neu gefasst; die Datei selbst hat keine Spalte (E14) · `uems-import-vorschau.md`
- **UEMS-Ablesungen (AP-09 IP-8)** — Rohwert-Herkunft ohne Box, Monatszuordnung Z6/E5, Berichtigung über Korrektur-/Versionsweg und Cloud-Lücke bei Überfälligkeit; B8/F17 und Aufbewahrungs-/Löschwege · [uems-ablesungen.md](uems-ablesungen.md)
- **UEMS-Bezugsgrößen-Werte: eingeben, berichtigen, freigeben (AP-09 IP-7, F1–F5, U5/U6, Z4)** — `POST …/bezugsgroessen/{id}/werte` (Erstwert = Fassung 1 ohne Begründung und Freigabe, derselbe Betrag = 200 `wiederholung`, ein anderer = 409 `konflikt_anderer_wert`) und `POST …/{id}/werte/{periode}/berichtigung` (Vier-Augen aus: Fassung n + 1 + `correction`; an: Vorschlag `BK-…`, freigegeben über `POST /api/v1/korrekturen/{kennung}/freigeben` — nie die Erstellerin); `V20260915010000`: Vorgang `bezugsgroesse_berichtigung` + `correction` mit Bezug `bezugsgroesse` (Vokabular additiv: beide Java-Zwillinge, TS, `events-raw`-Schema). ⚠ ein offener Vorschlag ist KEINE Wert-Fassung (B5-Nummerierung), er steht als `vorschlag` am Wert; ⚠ `bezug_pflicht` von `correction` ist leer — das Entweder-oder prüft der Schreibweg; ⚠ erst `FOR SHARE` auf `unternehmen`, dann die Kundenbereich-Sperre · `uems-bezugswert-eingeben.md`
- **UEMS-Lesepfad: Verlauf mit Herkunft, Rückfall auf die Speicherklassen, Export (AP-07 IP-14)** — der erste LESER der Messdatenstrecke, keine Migration: der bestehende Weg antwortet zuerst, die Speicherklassen sind ein RÜCKFALL und greifen NUR, wenn der Zeitraum über die Rohdaten-Frist (`jetzt − 90 Tage`) hinausreicht UND der bestehende Weg nichts hergibt (`messreihe_viertelstunde` ≤ 90 d, sonst `messreihe_tag`); die Antwort nennt IMMER ihre Quelle (`meta.quelle`, `quelleErklaerung`, `rohGrenze`), ein grenzüberschreitender Zeitraum wird aus GENAU EINER Quelle beantwortet und nie zusammengenäht. ⚠ **Jenseits der Frist fällt der 400er weg** (`rawAvailable=false` + Werte, Abnahme A7), INNERHALB bleibt er stehen — dort ist er wahr. Neue Felder je Wert (`Datum.herkunft`) und Marken für sieben Ereignisarten; der Export trägt die Herkunfts-Spalten und die je Wert GESPEICHERTE Katalogfassung, `# catalog_version=` bleibt die heutige. ⚠ Nacharbeit (§8): Tagesklasse zeigt `messreihe_tag.menge`, ein Raster GRÖBER als 15 min ist je Schritt ein freier Zeitraum der Regel (`ZeitraumMenge.raster`), nie die Summe der Viertelstunden — seine Abdeckung ebenso aus Zeitraum und Kadenz zur Messzeit, nie aus den vorhandenen Zeilen; Energie aus Leistung nur als `energieAusLeistung {wert, kennzeichen}`; `counter_overflow` verdrängt die Bestands-Rücksetzung derselben Messzeit · `uems-lesepfad-verlauf-herkunft-rueckfall.md`
- **UEMS-Lese-Modell „Werte je Messstelle“: nie eine Zahl ohne ihren Zustand (AP-08 IP-9)** — `GET /api/v1/messstellen/{kennzeichen}/werte?raster=viertelstunde|stunde|tag|monat|jahr&von&bis[&version]`, keine Migration: JEDER Schritt steht da (Standort-Zone, Tag `bis` einschließlich, Zeitpunkt halboffen, nie gerundet), gelesen über den Lesepfad (`SpeicherklasseHistorie` + neu `perioden`/`ereignisVerweise`/`mitDaten`), nur die FÜHRENDE Bindung der Hauptgröße einer Reihe (`MessstelleWerteRegeln.deckung`, sonst `grund`); ⚠ eine Lücke hat keine Zeile → „keine Werte“ 0 von `erwartet` der Kadenz-Kette, liegen Rohwerte darunter → `noch_nicht_gebildet`; ⚠ Tag/Monat/Jahr aus den Periodenständen (F8: 2 304 ≠ 1 966,4 Summe), die Stunde = `ZeitraumMenge.raster` samt Abdeckung aus Zeitraum und Kadenz zur Messzeit (F8 17:00: 29 von 60); ⚠ `zustand` = Spalte `menge_zustand`, `fassung` = Spalte `zustand`; ⚠ 23/25 Stunden und MESZ/MEZ nur aus `TagRegeln`/`ErgebnisZustand`; Anteil-Bindungen haben noch keine Menge (Befund); 400 `anfrage_ungueltig` mit `feld`+`grund`, fremd 404; Recht `messwerte.ansehen` (Nachtrag AP-08 §4.8, keine Durchsetzung) · `uems-werte-je-messstelle.md`
- **UEMS-Versionen lesen: wer, wann, warum, und was vorher dastand (AP-08 IP-18)** — keine Migration: `…/werte` zeigt ohne Angabe die NEUESTE Version (vorher immer Version 1 — `KostenstelleEnergieService` und `BerechnetePeriodenLauf` fragen darum ausdrücklich `version=1`), `version=n` genau die damalige Zahl der gespeicherten Zeile; `GET …/werte/versionen` je EINER Periode: `wert_alt`/`wert_neu` genau wie `version=n`, `entscheidungen[]` aus den Fassungen von Ersatzwert/Korrektur (`wer`, `wann`, `warum` = Text DIESER Fassung, `angelegt` = anlegende). ⚠ Eine Version, die es an keinem Schritt gibt = 404 `version_gibt_es_nicht`, nie leer/nie die höchste; ⚠ fehlt das „warum“ → `fehlt: ["warum"]`, nie erfunden; ⚠ die Stunde hat keine eigenen Versionen (`version_nicht_gebildet`, Befund F11-Stunde); die Portal-Fläche „Versionen am Wert“ liest `versionen` am Wert der Tageskarte (`uems-tageskarte.md` Falle 8) · `uems-versionen-lesen.md`
- **UEMS Vier Augen bei Korrekturen: Vorgabe aus, bei an nie der Ersteller, der Unterstützer nie — die ersten Routen, die ihr Recht DURCHSETZEN (AP-08 IP-15, E8)** — `GET`/`PUT /api/v1/unternehmen/vieraugen` (`vieraugen.einstellen`, nur Kundenadministrator, Protokoll in `ort_aenderung`), `POST /api/v1/korrekturen/{kennung}/freigeben|zuruecknehmen` über `KorrekturRechte` → `RechteAbleitung.korrekturEntscheiden` (Familie `vieraugen`, TS `rechte.ts`); `V20260914201500`: `unternehmen.vieraugen_freigabe` NULL = Vorgabe aus, `messreihe_korrektur.freigabe_vieraugen` an der Freigabe + Trigger `…_zweite_person`. ⚠ fremd 404 vor 403; die Einstellung wirkt zum Zeitpunkt der FREIGABE (`FOR SHARE`/`FOR UPDATE` auf der Unternehmen-Zeile, gespeichert an der Fassung); Plattform-Admin = Unterstützer; `KorrekturRechte.MATRIX` ist eine per Test gepinnte Kopie · `uems-vieraugen-freigabe.md`

## Flächen

- **UEMS-Tages- und Monatskarte je Messstelle: Menge · Zustand samt Herkunft · Verlauf, nie nur die Zahl (AP-08 IP-11)** — `WerteDialog` (Wirt heute: Zeilenmenü der `GesamtwertKarten`) liest `GET /api/v1/messstellen/{kennzeichen}/werte` (Tag + Stunden bzw. Monat + Tage), die reine `uemsWerteKarte.ts` leitet ab; jede Zahl über `menge` (E11), Zustand/Verlauf/Kennzeichen über `teile`, Beschriftung („02:00–03:00 MESZ“) und Tagesdauer („25 Stunden (Zeitumstellung)“) aus der Route. ⚠ `null` = „—“; ein Schritt ohne Zustand (`grund`) oder mit Vertragsverstoß spricht NICHTS, auch nicht seine Zahl; ⚠ die Herkunft „(Menge aus Zählerständen)“ ist ergebnis-zustand 1.6 (`mengen_herkunft`, Java ⟷ TS), nur an der Karte, nur mit Zahl; ⚠ 375 px: `.dbody` schneidet Querlauf unsichtbar ab — `e2e/tageskarte.spec.ts` prüft Element-Rechtecke und legt mit `TAGESKARTE_BILDER` die Vorschau-Bilder ab; ⚠ die Fassung „vorläufig“/„endgültig“ steht IMMER im Kopf (ergebnis-zustand 1.7, Rang 90), je gezeigter Periode aus `fassung` der Route, nie abgeleitet und nie mit dem Zustand vermischt; ⚠ ab zwei Versionen öffnet der Einstieg unten in der Karte die Historie im gestapelten Dialog — vorher → danach über `anzeige`, „eingetragen/vorgeschlagen von …“ statt „geändert“, fehlendes „warum“ als ehrlicher Satz, der alte Wert nie durchgestrichen (AP-08 IP-18) · `uems-tageskarte.md`
- **UEMS-Tageskarte verfeinert: Anzahl der Lücken und „noch nicht gerechnet“ (Captain 15.09.2026, beide JA)** — reines Portal, keine Vertragsdatei: die Anzahl steht IM Abzeichen des Verlaufs („Verlauf 85 % · 1 Lücke“, `data-testid="werte-verlauf"`); `uemsWerteKarte.luecken` zählt jedes `data_gap`, das die Route an den Schritt hängt, EINMAL — nur an der Karte, nur am gesprochenen Schritt, nie bei Verlauf 100 % (dann nachgeliefert, F9); der Grund `noch_nicht_gebildet` der Route (kein Wort des Ergebnis-Vertrags) bekommt an der Karte den Satz im Platz `werte-grund`, in der Zeile das Wort — beide NUR in `glossar.ts` (`UEMS_NOCH_NICHT_GERECHNET`, `…_SATZ`, `UEMS_LUECKE`); ⚠ die Anzahl nie als eigenes Abzeichen neben „vollständig“ (liest sich wie ein Widerspruch); ohne Lücke bleibt die Karte pixelgleich · `uems-tageskarte.md` Falle 9/10

- **UEMS-Standort-Dialog und Liste „Unternehmen › Standorte“ (AP-02 IP-6, T1/T2)** — reines Modul `standorte.ts` (Fassung anlegen · bearbeiten · vervollständigen, Zeitzone vom Unternehmen vorbelegt, Prüfung mit den Sätzen des Servers, Namensregel über `nameBelegt` AUFGERUFEN), `StandortDialog`, `StandortKopf` („Bearbeiten“ / „Adresse nachtragen“), `StandortePage` mit „Archiviert“ und „Noch nicht zugeordnet“ (nur solange sie etwas enthält); Wirt bis AP-01 IP-5/IP-7 der Reiter `#/portfolio/standorte` (`#/standorte` ist Alt-Adresse). ⚠ Die Bezugsfläche eines STANDORTS hat keine Schreibroute — nur lesend; ⚠ PUT ist die ganze Menge, die Lage reist mit; ⚠ Ablehnungen tragen ihren Körper in `ApiError.body`; ⚠ vier Reiter am Telefon nur mit `vp-bereich-tabs-dicht` (347 → 315 px bei 343 px Platz) · `uems-standort-dialog.md`
- **UEMS-Ortsbaum „Standort › Gebäude“ mit Gebäude- und Bereich-Dialog (AP-02 IP-7, T3/T4/T5/L1)** — reines Modul `ortsbaum.ts` (Baum aus `GET …/orte`, „Direkt am Standort“ nur wenn dort etwas hängt, L1 nur ohne Gebäude UND Bereiche, Ziel-Regel über `eintrag` AUFGERUFEN — die Zielliste „Hängt an“ enthält nie einen Bereich), `Ortsbaum` in jeder Karte der Liste „Standorte“, `OrtDialog` in der Schale des Standort-Dialogs. ⚠ Messstellen-Zahl am Gebäude MIT seinen Bereichen, `null` bleibt weg; ⚠ die Datenlage kennt die Antwort nicht — die Zahl steht an ihrer Stelle (Spalte ab 520 px Baumbreite); ⚠ kein Kurzzeichen-Vorschlag für G/B; ⚠ Fläche nur die ERSTE (anlegen oder „Fläche eintragen“), ändern = IP-8 · `uems-ortsbaum-portal.md`
- **UEMS-Geräteseite ergänzt: Gerät, Einstellungen „Ändern ab …“ und Messkanäle mit „speist MS-06 (führend)“ (AP-04 IP-12)** — in der BESTEHENDEN Sektion „Komponenten“ der Geräteseite, keine neue Seite: `GeraetHerkunft` (Weg Komponente → Gerät = `geraetZuKomponenten` wie das Protokoll, ohne Gerät nichts), Dialog `EinstellungAendernDialog` (VpDatePicker + VpTimePicker, Folgen-Karte VOR dem Eintragen), Ableitung nur in `geraetEinstellungen.ts`; ⚠ jeder Satz einer Fassung kommt aus dem Zwilling `uemsEinstellung.ts` (A5-Wortlaut der Vektoren), Einstellungen filtern auf die Komponenten DIESER Seite, `aus_bestand` heißt „In VoltPilot seit“, Kanäle aus `…/messkanaele` statt aus „Beobachtete Messwerte“ (die fällt bei Familie ohne Katalog weg); E2E-Bühne mit fester Uhr · `uems-geraeteseite-geraet-einstellungen-kanaele.md`
- **UEMS-„Fläche ändern“ mit Verlauf, Zeile „Standort“ in „Meine Anlage“ und Standort-Picker im Assistenten (AP-02 IP-8, T7/T6a/W4)** — reines Modul `flaecheAendern.ts` (Folgen VORHER aus `rueckwirkung` am Eintragstag heute, Verlauf NACHHER aus der PUT-Antwort) + `FlaecheDialog` (Einstieg im `OrtDialog`), `anlageStandort.ts` + `AnlageStandortZeile` in `TechnikSection` („Standort“ = Objekt mit Kurzzeichen, Adresse, „seit“ und „Adresse nachtragen“; die Koordinaten-Zeile heißt dann „Standort auf der Karte“), Picker in `AnlageStep` (vorbelegt bei genau einem, Pflicht bei mehreren); kein neuer Server-Weg. ⚠ OHNE Standort-Objekt byte-identisch (Snapshots VOR IP-8 aufgenommen, nur `useId` normalisiert) und `POST` ohne `standortId`; ⚠ kein Lese-Weg für den Verlauf vor dem Speichern — nie geraten; ⚠ kein Satz über freigegebene Berichte (AP-12) · `uems-flaeche-aendern-portal.md`
- **UEMS-„Stand am …“ auf der Liste „Standorte“ und im Ortsbaum (AP-02 IP-13, H1/A12)** — reines Modul `standAm.ts` (Banner-Satz, heute ≠ Stichtag, Liste am Stichtag), `StandAm` (Banner + `VpDatePicker`) über der Liste, `Ortsbaum` nimmt `stichtag`; kein neuer Server-Weg (`?stichtag=` aus IP-3/IP-5). ⚠ EIN Datumsfeld je Seite, die Bäume folgen; ⚠ mit Stichtag KEIN Schreibweg (einziger Knopf „Zurück zu heute“); ⚠ „gab es noch nicht“ steht als Karte mit dem Satz des Servers an ihrem Platz, Gebäude/Bereiche fehlen (die Antwort nennt sie nicht); ⚠ „Noch nicht zugeordnet“ nur heute (das Lesemodell zählt die Anlagen von HEUTE); ⚠ jede Antwort gilt nur für ihren Tag (Anfrage-Nummer, sonst stünde der Baum von gestern unter dem Banner von morgen) · `uems-stand-am-portal.md`
- **UEMS-Anlage zuordnen/umziehen mit „gültig ab“ und Folgen-Vorschau (AP-02 IP-11, T6/T6b/A11)** — `PUT /api/v1/sites/{id}/standort` + `GET …/standort/vorschau` (EIN `planen`, Regel `OrtsbaumAbleitung.eintrag` AUFGERUFEN, gleiche Form, Vorschau schreibt nichts); keine Migration, geschrieben NUR `anlage_standort` + `ort_aenderung` (je ein Eintrag an Anlage, neuem und bisherigem Standort, Begründung als `grund`); `AnlageUmzugApiTest` zählt JEDEN MQTT-Sender (0 Aufrufe) und vergleicht die ganze Datenbank; Portal `anlageUmziehen.ts` + `AnlageStandortDialog` (Folgen-Karte „ändert sich“/„bleibt“, „kein Befehl“) an der Zeile „Standort“. ⚠ die Funktionen-Teilnahme bleibt an der Funktion des ALTEN Standorts (Befund, A11-Satz offen); ⚠ keine Korrektur für `gleicher_tag` · `uems-anlage-umziehen.md`
- **UEMS-Gebäude/Bereich verschieben mit „gültig ab“ und Folgen-Vorschau (AP-02 IP-12, V1–V4/A13)** — `GET /api/v1/orte/{id}/verschieben/vorschau` + `POST …/verschieben` (EIN `planen`, `eintrag`/`verschiebenFolgen`/`nameFrei` AUFGERUFEN, Vorschau schreibt nichts); keine Migration, geschrieben NUR `ort_zuordnung` + GENAU EIN `ort_aenderung` am Ort; additiv `aktionen.verschieben {erlaubt, text, ziele}` und `danach` (Abzeichen „ab … → …“) an `GET …/orte`; `OrtVerschiebenApiTest` zählt A13 je Ding (3 Bereiche + 5 Messstellen ziehen mit; Anlage, NA-2, MS-14 bleiben) plus MQTT-Mocks und ganze Datenbank; Portal `ortVerschieben.ts` + `VerschiebenDialog` + `Zeitstrahl`. ⚠ „bleibt“ nennt nur Anlagen der wechselnden Messstellen (Stellung von HEUTE); ⚠ kein Satz über freigegebene Berichte (AP-12); ⚠ Karte spricht nicht von Steuern · `uems-ort-verschieben.md`
- **UEMS-Startansicht-Weiche E1 und Pfad „Unternehmen › Standort › Anlage“ (AP-01 IP-5)** — reine Weiche in `betriebsart.ts` (`orteAus` → `startEbene` → `canonicalShellRoute`, Pfad `kopfPfad`): 1 Standort/1 Anlage → Cockpit, 1 Standort/n Anlagen → `#/standort/{id}` (`StandortUebersichtPage`), n Standorte → `#/portfolio`; Betreiber, Admin und Kunden ohne Standorte wie vorher. ⚠ Standorte im SELBEN `Promise.all` wie die Anlagen (sonst zwei Adress-Ersetzungen); ⚠ `migration.test.ts` „Einzel-Anlagen-Kunde byte-identisch“ rendert die Schale mit und ohne Pfad; ⚠ die Standort-Übersicht nimmt das Portfolio-Cockpit nur, wenn jede Anlage dort hängt (das Cockpit hat keinen Standort-Filter); ⚠ am Telefon steht der Pfad einer Anlage im Umschalter, nicht in der Kopfzeile · `uems-startansicht.md`
- **UEMS-Archivieren, Wiederherstellen und Löschen im Ortsbaum (AP-02 IP-15, Z1–Z3, A7–A9)** — `POST /api/v1/orte/{id}/archivieren` · `…/wiederherstellen` · `DELETE /api/v1/orte/{id}` (nur ohne Historie, sonst 409 `loeschen_gesperrt` mit `historie`), additiv `aktionen` je Knoten und `archiviert` (archivierte Knoten mit Archivtag) an `GET …/orte`; `V20260914233000` legt NUR die Funktion `uems_ort_loeschen` an (kein DELETE-Recht, keine Tabelle). Portal: `OrtMenue` je Knoten (auch am Standort-Kopf), `ArchivierenDialog` (Z1 Grund + Weg, Z2 Folgenliste, Z3 Namensprüfung, Löschen-Rückfrage), reine Texte `ortArchiv.ts`. ⚠ das Menü baut NUR aus den `aktionen` des Servers (kein Knopf, der dann 409 sagt), auf dem Kurzzeichen-Baum von `StandortService.baum`; ⚠ gesperrtes Archivieren bleibt ein Eintrag, gesperrtes Löschen ist nur ein Hinweis; ⚠ `hat_bezugsgroessen` ergänzt E1 außerhalb des Vertrags · `uems-ort-archivieren.md`
- **UEMS-Unternehmens- und Standort-Übersicht = Portfolio-Cockpit mit Ebene (AP-01 IP-6)** — `PortfolioCockpit` Prop `ebene`: Kopfzeile „Name · 2 Standorte · 3 Anlagen · 1 steuert · 3 von 3 Anlagen liefern Daten“, Standort-Gruppen der Anlagen-Tabelle (ein `tbody` je Standort, Karte mit den Funktionen aus `GET /funktionen`, E6 = C, Steuern seit 15.09.2026 nur mit teilnehmender Anlage), Standort-Übersicht = dieselbe Seite mit Filter `anlagenDerEbene`; Katalog-Bausteine `datenlage` (`je_anlage`, Kopfzeile) und `netzbezug-gesamt` (`summe`) NUR mit Ebene. ⚠ Geld-Regel (A13): Geld-Bausteine, „Heute €“ und Reiter „Erlöse“ nur über Anlagen, die steuern oder Erzeuger/Speicher haben — wer einen Euro-Baustein ergänzt, trägt ihn in `GELD_BAUSTEINE` ein (Proxy-Wächter in `uebersicht.test.ts`); ohne Ebene zeichengleich wie vorher; ⚠ Datenlage = Anlagen-Datenlage bis AP-04 · `uems-unternehmens-uebersicht.md`
- **UEMS-Karte „Funktionen", Leerzustände und Geld-Regel JE ANLAGE (AP-01 IP-8)** — Karte unter der Anlagen-Tabelle beider Übersichten (je Funktion, je Standort Zustand + benannter nächster Schritt „einrichten“/„aufnehmen“, KEIN Knopf bis IP-9a/IP-10a), Steuerungsseite einer Anlage, die nur misst, seit 15.09.2026 OHNE Hinweis (`uems-steuern-still.md`), Leerzustand der Standort-Übersicht; Geld-Regel je Anlage in `useAnlageSurface` (`anlageGeld.ohneGeld`: kein Geld-Held, keine Steuerungs-Karte, keine Marktpreise, keine Reiter Erlöse/Marktpreise/Lastspitzen, Lesezeichen → Messwerte). ⚠ Schritt aus dem Zustand, nie aus `aktionen`; ⚠ derselbe Fakt wie `geldAnlagen`, nie `hasPv`/`hasStorage`; ⚠ neuer Cockpit-Baustein, Block oder Reiter → Eintrag in `anlageGeld.ts` (Record + Render-Wächter) · `uems-leerzustaende.md`
- **UEMS-Steuern-Regel: wer nur misst, hört nichts vom Steuern — und findet trotzdem hin (Captain 15.09.2026, über AP-01 IP-6/IP-8)** — EIN Fakt `uebersicht.steuernSpricht`: ein Standort spricht von „Steuern & Optimieren“, sobald eine seiner Anlagen teilnimmt (Entwurf/eingerichtet/angehalten/aktiv; nur `kein_objekt`/`archiviert` schweigen); Standort-Karte, Kopf der Standort-Übersicht und Karte „Funktionen“ zeigen Steuern nur dann (kein „… einrichten“, kein „Läuft an 0 von 2“), der Hinweis „Diese Anlage misst nur.“ der Steuerungsseite ist entfernt. ⚠ löst „immer beide Funktionen, nie eine Leerstelle“ (PR 771) BEWUSST ab — nur für Steuern, Messen bleibt immer benannt; ⚠ „reine Messung“ bleibt; ⚠ der Bereich „Steuerung“ bleibt still erreichbar (Steuerart, „＋ Neue Regel“) — verboten ist das Aufdrängen, nicht die Erreichbarkeit · `uems-steuern-still.md`
- **UEMS-Modus „nur messen“ beim Anlegen einer Anlage (Captain 15.09.2026, Empfehlung A: im bestehenden Weg)** — derselbe Anlege-Fluss (`AnlageFlow` im Drawer und im Einrichtungs-Assistenten), EIN Fakt `anlegeNurMessen.anlegeArt` = `steuernSpricht` des Standorts, an dem die Anlage entsteht (ohne Wahl das Unternehmen, Wiedereinstieg der Standort der Anlage; ohne Funktionen oder Standort-Objekt wie heute): kein Schritt „Betrieb“, weder Veräußerungsform noch Feineinstellungen, „In drei Schritten“, Fertig mit „Zu den Messstellen“ (`standortMessstellenRoute`) und „Zur Anlage“; geprüft gegen die Wortliste `STEUER_GELD_WOERTER`, Bestandsschutz über Schnappschüsse vom Stand davor. ⚠ Funktionen IN der Entscheidung laden; ⚠ Verborgenes wird nicht gesendet; ⚠ `onDone(ziel)` nie als `onClick={onDone}`; ⚠ die Ebene ist der Standort, nicht die neue Anlage · `uems-messanlage.md`
- **UEMS-Telefon-Leiste je Ebene: Bereiche aus dem Read-Model, Kachel erst mit Seite (AP-01 IP-7, E4 = A)** — `anlageNav.ts` heißt jetzt `ebenenNav.ts` (Anlage unverändert); `ebenenBereiche` leitet die Bereiche von Unternehmen (Übersicht · Standorte ab 2 · Messstellen und Berichte, sobald ein Standort misst · Kennzahlen zusätzlich mit ≥ 1 Kennzahl) und Standort (Übersicht · Gebäude ab 1 · Anlagen ab 2 · Messstellen) aus `GET /standorte`, `/funktionen`, `/kennzahlen` ab; `ebenenLeiste` zeigt nur Bereiche MIT Seite (`EBENEN_SEITEN`) und erst ab drei, Prop `ebenen` der `AppShell`. ⚠ heute haben nur Übersicht und Standorte eine Seite → Ahrenberg bleibt OHNE Leiste (firstmate 001: kein Knopf ohne Ziel) — wer Messstellen (AP-04 IP-5), Kennzahlen, Berichte, Gebäude oder Anlagen einhängt, trägt die Route in `EBENEN_SEITEN` ein, und die Leiste erscheint von selbst; ⚠ keine Steuerungs-Kachel auf den Ebenen, der Weg führt über die Übersicht in die Anlage · `uems-telefonleiste.md`
- **UEMS-Messstellen-Register im Portal: „Unternehmen › Messstellen“ und „Standort › Messstellen“ (AP-04 IP-5)** — reines Modul `messstellen.ts` (Zeile → Wörter, Filter als Parameter DERSELBEN Route, Leerzustände §5.11, „Stand am“), `MessstellenPage` (Tabelle am Rechner, Karten ≤ 720 px), Routen `#/portfolio/messstellen` und `#/standort/{id}/messstellen` (`Route.standortBereich`), `EBENEN_SEITEN` trägt beide. ⚠ das Unternehmen Ahrenberg hat damit DREI Kacheln, die Telefon-Leiste erscheint (Standorte bleiben bei zwei); ⚠ was die Leiste trägt, ist am Telefon kein Reiter (`vp-nur-rechner`), am Rechner bleiben die Reiter der Weg (`EbenenTabs` am Standort unter einem Unternehmen); ⚠ „gab es noch nicht“ = Stichtag vor dem frühesten Beginn von Ort, Stellung oder Quelle der Vertrags-Form, „x von y“ nur heute; Glossar `UEMS_QUELLE`/`UEMS_FUEHREND`/`UEMS_VERGLEICH`, Werkstatt-Wörter im `copy.test.ts` · `uems-messstellen-register-portal.md`
- **UEMS-Assistent „Messen & Auswerten" — der Rahmen mit Schritt 1 Standort und Schritt 2 Datenquelle (AP-01 IP-9a)** — `PUT /api/v1/standorte/{id}/funktionen/messen` `{aktion: einrichten}` legt die Funktion im Entwurf an (Vertrag `uebergangMessen`, 409 `bereits_angelegt`/`standort_archiviert`, keine Migration); reine Regel `messenAssistent.ts` (Schrittfolge, „Schritt n von 5", Entwurf im Browser, Start/Wiedereinstieg, `messenEinstieg` für die Karte), Fläche `MessenAssistent` in der Schale `AnlegenDialog`, `StandortDialog`, `AddDeviceDrawer` und `AnlegenFlow` ersetzen sie als Unterabläufe. ⚠ nie über die Schale stapeln (Modal-Schleier Ebene 60 < Schale 61); ⚠ der Server entscheidet vor dem Browser, 409 `bereits_angelegt` heißt „weiter"; ⚠ `AnlageFlow` gehört NICHT hinein (Netzladen, Betriebsmodell) — ohne Anlage nennt Schritt 2 den Ort als Hinweis ohne Knopf; ⚠ Schritte 3–5 = Rumpf + `GEBAUTE_SCHRITTE`; ⚠ kein Einstiegsknopf in der Karte (IP-8) · `uems-messen-assistent.md`
- **UEMS-Messstellen-Dialog: anlegen und bearbeiten in drei Schritten — Identität · Zuordnung · Quelle (AP-04 IP-6)** — `components/MessstelleDialog.tsx` rendert, `messstelleDialog.ts` entscheidet; JEDER Schritt speichert seinen Teil über seine Route (POST · PUT ort/stellung · POST quellen), eine Ablehnung lässt den Schritt offen und speichert von ihm nichts; Sätze NUR aus der FEHLER-Tabelle §5.12, aus den Fakten gebaut und im Test wörtlich gepinnt (Hauptzähler-409 → Anlagen-Name aus den Standorten); Kennzeichen vorbelegt, unverändert = automatisch, nichts umgewandelt; nach dem ersten Speichern Hauptgröße und Nebengrößen fest, PUT schickt die Anschlussleistung mit; bearbeiten bietet für eine Größe mit laufender Quelle keinen Messwert (kein stiller Zählerwechsel); Prozess/Kostenstelle nicht im Dialog; geöffnet aus dem Register („Messstelle anlegen“ im Kopf bzw. Leerzustand, danach neu gelesen) · `uems-messstellen-dialog.md`
- **UEMS „Quelle binden“ und die Quelle-Karte: beide Werte nebeneinander (AP-04 IP-14, E3, A8)** — `quelleBinden.ts` entscheidet, `components/QuelleBindenDialog.tsx` + `components/QuelleKarte.tsx` rendern; ZWEI Einstiege, EINE Regel 7 (`messstelleDialog.messwertZeilen`, Zwilling `uemsMessstelle.passung`) — von der Messstelle aus Komponente → Messwert, von der Komponente aus („Als Messstelle verwenden“) Messwert → Messstellen-Größe; ⚠ was nicht passt, VERSCHWINDET nie: grau mit Grund, in beide Richtungen; ⚠ der Anteil eines Vorzeichen-Werts wird aus der Richtung der Zielgröße ABGELEITET, nie gewählt; ⚠ E3 bewertet nichts (kein Prozent, keine Ampel); additiv am Server: `letzter_wert` und `kanal_name` je Quelle in `GET …/quellen` (nur an einer Bindung, die GILT; gebildet von `MessstelleBeobachtung.letzterWert` wie der Register-Letztwert) und `anteil` an `MesskanalDto.Speist` · `uems-quelle-binden-portal.md`
- **UEMS-Messstellen-Seite: drei Zuordnungs-Karten Ort · Elektrisch · Organisation mit „Ändern ab <Tag>“, Historie je Karte und Protokoll nach der Eintragung (AP-04 IP-8)** — `pages/MessstelleSeite.tsx` + `components/ZuordnungAendernDialog.tsx` rendern, `messstelleZuordnung.ts` entscheidet (Abschnitte aus den Intervallen, Zustand über `zuordnungZustand`, „rückwirkend (n Tage)“/„geplant“ über den Ortsbaum-Zwilling `rueckwirkung` am Eintragstag heute, 100 % über `uemsVerteilung.satz`, Sätze von Ort/Stellung über `messstelleDialog.ablehnung`); Routen `#/portfolio/messstellen/{id}` und `#/standort/{sid}/messstellen/{id}` (`Route.messstelleId`, `messstelleRoute`), der Name jeder Registerzeile ist der Einstieg, „Bearbeiten“ öffnet den `MessstelleDialog`; ⚠ Historie = Zeitstrahl nach „gilt ab“, Protokoll = `achse=eintrag` (Captain-Entscheid 15.09.2026); ⚠ „heute“ = Stichtag des Registers; derselbe Tag wie ein laufender Beginn = `korrektur` · `uems-messstelle-seite-portal.md`
- **UEMS-Werte an der Messstelle: Abschnitt „Werte“ unter dem Kopf der Messstellen-Seite, Zone im Kopf, Periode und Version in der Adresse (AP-13 IP-3, E9/E12 = A)** — der Inhalt des `WerteDialog` ist EINE Sektion `components/WerteSektion.tsx` (Zeit-Leiste, „Zeiten in Europe/Berlin (Zeitzone des Standorts …)“ aus `zeitzone`/`zeitzone_herkunft` über `uemsWerteKarte.zeitenKopf`, Karte, Liste); die Seite trägt sie, der Dialog an den `GesamtwertKarten` öffnet sie im `Modal` (`rahmen`); Adresse `?periode=&version=` (`nav.parseMessstelleWerte`, `sprungziel`, `App.springe`), ⚠ `version` fragt nur die Karte, Hinweis „Sie sehen Version n — heute die neueste“ (`versionHinweis`, nur über einer gesprochenen Karte); Einstiege im Register nur mit Wirt (`onWerte`): „Letzter Wert“ + Zeilenmenü „Werte“ am Rechner, die ganze Karte am Telefon, Periode = Vortag bzw. Stichtag; Wegweiser-Zeile „Wirte“ in `uems-tageskarte.md` berichtigt (W2) · `uems-tageskarte.md` (Falle 11), `uems-messstelle-seite-portal.md` (Falle 9)
- **UEMS-Verlauf einer Messstelle: Lücken als Flächen, Marker mit dem Satz des Vokabulars, Tag · Woche · Monat · Jahr im Raster der Route (AP-13 IP-4 = AP-08 IP-10, E5 = A, Meilenstein 2)** — `WerteSektion` trägt unter der Karte den Verlauf (`components/MessstellenVerlauf.tsx`, rein `uemsVerlauf.ts`): Tag in Viertelstunden, Woche in Stunden (ohne Karte, nichts summiert), Monat in Tagen, Jahr in Monaten (dort ist der Verlauf die Liste, EINE Anfrage), Adresse `periode=JJJJ-Www` · `JJJJ`; ⚠ `null` = kein Balken, eine Folge „keine Werte“ = schraffierte Fläche (auch mit Grund `keine_quelle`), Marker aus `ereignisse[]` über `uemsEreignis.ereignisSatz` nur für `data_gap` (jede andere Art: Name + Zeit, die Route liefert ihre Felder nicht), höchstens drei im Bild (K6), jeder in der Liste; Kernaussage aus der Karte (K1), Tooltip als Satz (K7), Tipp → Schritt-Karte `verlauf-schritt-karte` mit ‹ › je 44 px; ⚠ Farben blau/bernstein (Grün/Bernstein trennt bei Rot-Grün-Schwäche nicht), Ersatzwert als Textur; Wächter `CHART_FILES_OBERFLAECHEN` (Verlauf + Kennzahl-Balken), `messstellenVerlaufKontrast.test.ts`, `verlaufMobil.test.ts` · `uems-verlauf-messstelle.md`
- **UEMS-Vergleich einer Messstelle: der Umschalter aus · Vorperiode · Vorjahr und bis drei passende Reihen — aber NIE ein Δ zwischen zwei Messstellen (AP-13 IP-5, E6 = A, VG1–VG5)** — reines `uemsVergleich.ts` + Render `components/WerteVergleich.tsx` in der `WerteSektion`; die Differenz und der Prozentsatz kommen aus dem Zwilling `uemsBericht.vergleich` (AP-12 IP-3), die Gründe aus `grund_ohne_vergleich` — AP-13 rechnet kein eigenes Δ. Δ-Zeile direkt unter der Karte („+260 kWh (+4,3 %) gegenüber Oktober 2026 · korrigiert (Version 2)“, Zusatz nur bei nicht vollständiger Basis und ab Version 2), ohne Vergleichswert der Grund statt einer 0 („November 2025: keine Werte — vor Beginn“); Adresse `v=` (roh gelesen, `wahlAus` entscheidet je Zeitraum, „aus“ steht nie im Hash, überlebt den Zeitraum-Wechsel), Abruf über `uemsWerteCache.ts` (stale-while-revalidate 60 s). ⚠ „passend“ liest die `hauptgroesse` des REGISTERS und filtert über KENNZEICHEN (die Werte-Route schreibt `bezug`, das Register `Bezug`) — ohne Register kein Vergleich; ⚠ die Woche hat keine Zahl und das Jahr nur `aus · Vorjahr`; ⚠ `bild(…, weitere, gruppiert)` skaliert über ALLE Reihen und zeichnet die Vergleichsreihe VOR der eigenen; mit mehreren Messstellen ersetzt die Reihen-Legende die Zustands-Legende (Farben in `messstellenVerlaufKontrast.test.ts` nachgerechnet); Befund: der Beginn des Energiemanagements steht in keiner Route, deshalb „vor Beginn“ ohne Datum · `uems-vergleich-messstelle.md`
- **UEMS-Warum eine Zahl fehlt: Grund-Sätze unter dem Strich, Auskunft statt Fehlermeldung, Leerzustand ohne Datenquelle (AP-13 IP-6, E11 = A, Z1–Z8)** — reines Portal: die Karte spricht `grund` über `uemsWerteKarte.grundDes` (Platzhalter aus der Antwort, NAMEN der Bindungen aus dem Register des Wirts — ohne Namen kein Satz), 400 je Grund der Werte-Route ein Satz (`uemsOberflaechen.auskunft`, Gründe gegen `MessstelleWerteRegeln.Grund` geprüft), 404 „Diese Messstelle gibt es nicht.“, `wert_nicht_mehr_gespeichert` mit dem Satz der Route und OHNE Sprung bis AP-12 IP-16, nur `nicht_abrufbar` mit „Erneut versuchen“; ganzer Zeitraum ohne Bindung = Leerzustand „Keine Datenquelle“ mit „Quelle zuordnen“ (Dialog ab Schritt 3, nur mit Recht) bzw. „Ab … zeigen“; Zeile „Weitere Größen“ (V8) ohne Sprung; ⚠ `{kanal}` nennt den Komponenten-NAMEN (Register kennt kein K-Kennzeichen), kein „Ablesung eintragen“ (keine Route), `vergleichOhnePassende`/`OHNE_HAUPTZAEHLER` warten auf IP-5/IP-8 · `uems-werte-gruende.md`
- **UEMS-Welt „Kennzahlen“ im Portal: Liste und Kennzahl-Seite (AP-11 IP-13, E12)** — Portfolio-Welt `#/portfolio/kennzahlen` und `…/kennzahlen/{id}` (bis AP-13), reines Modul `kennzahlKarte.ts` (Listen-Karte, Werte-Karte in der Form von `uemsWerteKarte.Karte`, Satz ohne Zahl, Verlauf, Herkunft, Berechnung mit Fassungs-Verlauf, Stammdaten, Versionen), `KennzahlenPage`/`KennzahlSeite` mit `WerteKarte` (+ `grund`) und `VersionenModal` wiederverwendet; Fixtures K1/K7/K8/K10/K11 aus `kennzahl-vectors.json`. ⚠ Ahrenberg hat damit VIER Kacheln und den Reiter „Kennzahlen“ (`EBENEN_SEITEN`); ⚠ Versionen mit vier Stellen, die Karte mit zwei; ⚠ Herkunft ohne „gemessen“/„eingegeben von“ (Vertrag trägt es nicht, Befund); ⚠ R-A7 = 404 der Werte-Route, bis AP-03 IP-11 ein Feld bringt; `copy.test.ts` §4.13 mit `KENNZAHL_BESTAND` · `uems-kennzahlen-portal.md`
- **UEMS-Assistent „Kennzahl anlegen“ und „Kopieren“: das Gesicht des Kennzahlen-Konzepts (AP-11 IP-14, E2/E9/E12)** — `KennzahlAnlegenDialog` im Rahmen des Gesamtwert-Assistenten: Vorlage (acht Karten + ohne Vorlage) · Menge (Register gefiltert auf die Erwartung; Hebel „Mehrere Messstellen?“ öffnet `GesamtwertDialog` gestapelt und kehrt mit dem Gesamtwert zurück) · Bezugsgröße bzw. Ganzes (Einheit und Periode live aus `uemsKennzahl.ts`, K13 rot, Perioden-Wunsch) · Geltungsbereich, Name, Verantwortlich, Zweck (Vorschlag aus der Bezugsgröße, Rechte-Geltungsbereich G1, G3 rot) · Vorschau (`POST …/vorschau`, drei Perioden) · Fertig; Zusammenfassung ohne Schritt 3; „Kopieren“ an der Kennzahl-Seite (Form mit Komplement, Name, Zweck); reines Modul `kennzahlAnlegen.ts`; Eigene Auswertung „3 · Zeitbezug“. ⚠ vor „Anlegen“ nur die Vorschau (`__kennzahlAufrufe`); ⚠ Befunde: Flächen der Struktur sind kein Eingang (Vorlage „je m²“ läuft leer), die Vorschau liefert nie `vor_bestehen`, Bezugsgröße ohne `art` (Filter nur Einheit und Wertart) · `uems-kennzahl-anlegen.md`
- **UEMS-Welt „Berichte“ im Portal: Liste und Berichtsseite (AP-12 IP-13, E14)** — Portfolio-Welt `#/portfolio/berichte` und `…/berichte/{kennung}` (bis AP-13), reines Modul `berichtSeite.ts` (Listen-Karte mit Vermerk R5, Reiter „Nr. 1 · Nr. 2 · Entwurf“, Seitenkopf D5 mit geprüfter Prüfsumme, Abschnitte nach Vorlage mit Nachweis je Zahl in der Form der `WerteKarte`, Verlauf der Stände, „heute: …“ A5, „heutigen Wert zeigen“ B16), `BerichtePage`/`BerichtSeite`; Fixtures = Vektor-Abzüge BR-2026-0001/1 und /2. ⚠ der Abzug wird wörtlich gesprochen, nie gegen `uemsErgebnis` nachgeprüft; ⚠ Abschnitte ohne Inhalt im Abzug (Tagesverlauf …) erscheinen nicht; ⚠ PDF/CSV nur mit `AUSGABE_EINGEHAENGT` + `onAbruf` (IP-10/IP-11 hängen sich dort ein); ⚠ Ahrenberg hat FÜNF Kacheln; ⚠ `copy.test.ts` liest JSX-Bedingungen als Text („onAusgabe“ → `onAbruf`) · `uems-berichte-portal.md`
- **UEMS-Berichts-Dialoge: anlegen, freigeben, vergleichen, Anstoß verwerfen (AP-12 IP-14, Meilenstein 5 „Abnahme im Portal“)** — reines Modul `berichtDialoge.ts` + `BerichtAnlegenDialog`/`BerichtFreigebenDialog`/`BerichtVergleichDialog`/`AnstossVerwerfenDialog`, Banner „Revision nötig“ auf `BerichtSeite`, Rechte aus `GET /api/v1/me` (`useBerichtRechte`); `POST /api/v1/berichte` additiv `kennzahlen_abgewaehlt` → `bericht_kennzahl_abwahl` vor der ersten Bildung; W2-Pin `ortArchivBerichte.test.ts`; Folgen-Zeile auch in Verschieben und „Zuordnung ändern“. ⚠ F1 zählt Werte UND Kennzahlen („18“, nicht 16); ⚠ Vergleich als Karten, Kennzahl mit vier Stellen; ⚠ `.vp-br-hebel` gehört „heutigen Wert zeigen“ — Hebel-Leisten heißen `.vp-br-aktionen`; ⚠ Person/Quelle am Anstoß liefert die Route nicht · `uems-bericht-dialoge.md`
- **UEMS-Kennzahl ändern, archivieren, löschen: die Kennzahl-Fläche ist vollständig (AP-11 IP-15, §5.4/§5.5/§5.7)** — „Berechnung ändern ab …“ = `KennzahlAnlegenDialog` mit `aendern` (Gilt ab · Menge · Bezugsgröße · Vorschau; heutige Eingänge vorbelegt; „rückwirkend (n Tage)“ und „Ab diesem Tag gilt schon Fassung 2.“ vorab über `kennzahlAendern.fassungEintrag` = Satz der Route, gemessen an K17; Begründung ≥ 10 Zeichen; `POST …/vorschau` zweimal → „März 2027: 0,30 statt 0,29“; nur „Speichern“ schreibt `POST …/fassungen`), `KennzahlStammdatenDialog` (`PUT` ohne Fassung), Karte „Archivieren und löschen“ (`ConfirmDialog` nennt die Leser, `DangerZone` nur ohne Wert: „KZ-0001 hat Werte — archivieren Sie sie.“), „Eingang archiviert (KZ-0001)“ an der Berechnung des Lesers, Fassungs-Verlauf nach der Eintragung; Versionen weiter `WertVersionen.tsx`. ⚠ Befund: die Vorschau-Route kennt nur abgeschlossene Perioden — am Eintragstag vergleicht die Fläche den Vormonat; ⚠ Befund: „Eingang archiviert“ erzeugt kein Rechenlauf, Wiederherstellen hat keine Route; ⚠ rückwirkend zieht nur Vorläufiges nach (IP-9 offen); keine Migration · `uems-kennzahl-aendern.md`
- **UEMS-Assistent „Messen & Auswerten" — Schritte 3 Messstellen, 4 Prüfen, 5 Fertig (AP-01 IP-9b)** — keine Route, keine Migration: Schritt 3 ruft `GET`/`POST …/standorte/{id}/messstellen-vorschlag` (AP-04 IP-16) und `PUT …/messstellen/{id}/ort` (Gebäude/Bereich als `korrektur` am Tag der Übernahme, eigener Schreibweg danach), die Hauptzähler-Regel spricht den Satz des Servers VOR dem Senden; Schritt 4 urteilt mit `messen().fehlt` (`fehltArt`) und spricht in Fakten (Standort, `/devices`, Register, Datenlage), „Weiter“ nur bei `aktiv`; Schritt 5 löscht den Entwurf, kein Start-Knopf; ⚠ `funktion.eingerichtet_am` schreibt für Messen niemand · `uems-messen-assistent.md`
- **UEMS-Ebenen-Seiten am Standort: Gebäude, Anlagen, Kennzahlen und Berichte dieses Standorts (AP-13 IP-2, E4/Ü6–Ü8, Meilenstein 3)** — `EBENEN_SEITEN` um vier Seiten (`#/standort/{id}/gebaeude|anlagen|kennzahlen|berichte`), die Leiste am Standort erscheint von selbst: Werk Ahrenberg VIER Kacheln, Werk Lindach DREI (O17); `StandortGebaeudePage` (Ortsbaum + Stand am, Hülle `Ortsbaum.gebaeudeKarte` für IP-10), `StandortAnlagenPage` (`PortfolioCockpit nurAnlagen`), `KennzahlenPage`/`BerichtePage` mit `standort` (`amStandort`), Einstiege `standortEinstiege` unter dem Standort-Kopf, `PortfolioTabs.standortBereiche` für den obersten Standort. ⚠ Kennzahlen und Berichte sind am Standort Seiten OHNE Bereich (keine Kachel, AP-01 §4.6/O17); ⚠ Lesezeichen `#/portfolio/kennzahlen|berichte|messstellen` unverändert, Kennzahl/Bericht aus der Standort-Liste öffnen im Standort; O18 (IP-14): auch mit Gebäuden und zwei Anlagen keine neuen Reiter/Kacheln ohne Messfunktion; direkte AP-13-Adressen zeigen den Bestand (E2/Q2) · `uems-oberflaechen-ebenen.md`
- **UEMS-Übersichts-Bausteine je Ebene: Messstellen · Energiebilanz · Kennzahlen — und EINE Datenlage-Zählung (AP-13 IP-7, E3/E13, Ü1–Ü5)** — unter der Anlagen-Tabelle beider Übersichten, vor „Funktionen“: rein `uebersichtBausteine.ts` (Register-Zeilen wörtlich, Summe über `uemsBilanz.ebene`, Gebäude-Zeilen über `uemsBilanz.gebaeude`, Kennzahlen lebend mit Geltung), Render + Laden `components/UebersichtBausteine.tsx`; Katalog-Bausteine `messstellen`/`energiebilanz`/`kennzahlen` NUR mit Ebene UND Inhalt (`UEMS_UEBERSICHT_BAUSTEINE`, `uebersicht.uems`). ⚠ E13 ändert eine gebaute Zeile: `FunktionZustandAbleitung.datenlage` liest `register_zeilen` (`MessstelleRegisterService.aggregatZustand`) — berechnete UND archivierte Messstellen zählen jetzt mit (Ahrenberg ST-1 „13 von 14“ → „15 von 16“); ⚠ die Bilanz-Route kennt nur Hauptzähler BEZUG — keine Abgabe-Summe; ⚠ Register-Filter `ort` mit Kurzzeichen (die Bühnen-Fixture kennt sonst nur ihre eigenen Ort-IDs) · `uems-uebersicht-bausteine.md`

## Fachmodell

- **UEMS-Fachmodell: das EINE Glossar für alle Pakete des Unternehmens-Energiemanagements** — Standort · Anlage · Netzanschluss · Messstelle · Messkanal · Datenquelle mit Kardinalität, Zeitgültigkeit und Zustandsvokabular; `docs/fachmodell/` ist die Referenz, die Auswirkungs-Karte sagt je heutiger Tabelle, was aus ihr wird · `uems-fachmodell-das-eine-glossar-fuer-al.md`
- **UEMS-Referenzunternehmen Ahrenberg: die EINE Beispielquelle aller Pakete** — Kennzeichen, Werte und Zeitpunkte nur aus `docs/contracts/v2/uems-referenzunternehmen.json`; tagesgenau = Tag + LETZTER Tag, minutengenau = halboffen, `U` = Unternehmen als Ort, rückwirkend mit `eingetragen_am` + Ortsbaum-Abzeichen, Enddatum einer Unterstützung einschließlich; die Zwillinge prüfen „kein Ziel vor seinem Bestehen“ — seit **Fassung 1.2** (12.09.2026, AP-09 IP-2 + AP-10 IP-2) dazu 22 Messstellen mit MS-22 als Rest von AN-3, `formel_typ`, die Tageswerte 100/60/30/10 des 18.10.2026, `MS-21.ablesungen[]`, BZ-5 und die Bezugsgrößen-Kennungen; ZWEI entschiedene Berichtigungen (MS-09 = 54 580 kWh, Ende der 9000-Anteile am 31.12.2026) — wer die Datei ändert, fährt alle UEMS-Tests · `uems-referenzunternehmen-die-eine-beispi.md`

## Prüfmuster

- **Bestandsschutz-Fingerabdruck in Migrationstests: `uems/Bestandsschutz` (Testquellen), nie `isEqualTo` auf der Tabellen-Map** — die Tests nehmen den ersten Fingerabdruck auf IHRER Fassung und migrieren danach bis zum neuesten Stand; jede SPÄTERE Migration mit neuer Tabelle stand darum als Unterschied da (`messstelle_formel_fassung` aus #706 machte drei Tests rot), und die alte Abhilfe — die neue Tabelle mit Namen ausnehmen — musste jedes Paket in allen älteren Tests wiederholen. `Bestandsschutz.fingerabdruck(db, ausnahmen)` misst jede Tabelle des Schemas (`LIKE`-Ausnahmen nur für die Tabellen des EIGENEN Pakets), `abweichungen(vorher, nachher)` meldet geänderten Inhalt einer bestehenden Tabelle, eine verschwundene Tabelle und eine NEUE Tabelle MIT Inhalt; eine neue LEERE Tabelle ist keine Abweichung. Dieselbe Unterscheidung eine Ebene tiefer: die Zeile zählt als jsonb-Objekt OHNE ihre SQL-NULL-Spalten (nicht `t::text`), eine spätere Spalte, die überall NULL ist, ändert nichts — eine mit Wert (auch per `DEFAULT`) ist eine Abweichung. `mutationsprobe(...)` beweist je Testklasse auf der echten Datenbank (zurückgerollte Transaktionen), dass eine geänderte Bestandszeile, eine gefüllte neue Tabelle und eine befüllte neue Spalte auffallen und eine leere Tabelle/Spalte nicht. Wer nur EINE Tabelle oder einen Ausschnitt misst (etwa die pünktlichen Rohwerte), nimmt `inhalt(db, tabelle, bedingung, args)` und beweist ihn mit `inhaltsprobe(...)` — nie ein eigenes `md5(string_agg(t::text …))`, das an einer später angehängten leeren Spalte rot wird. ⚠ Legt eine spätere Migration ihre Tabelle MIT Startzeile an (Laufzustand wie `messreihe_tag_lauf`), ist das eine Abweichung: die Tabelle mit Begründung in die Ausnahmen des älteren Tests, nie den Vergleich aufweichen · `../../../services/api/src/test/java/com/voltpilot/api/uems/Bestandsschutz.java`
- **E2E-Wirte verhalten sich wie die echten Wirte; eine rote Spec wird auf Vorgänger UND Kandidat unter gleicher Last eingegrenzt (`gesamtwert.spec`, 15.09.2026)** — die Bühne `e2e/gesamtwert.tsx` schloss den Gesamtwert-Assistenten schon bei `onGespeichert`, „ist angelegt" stand nur ~180 ms; `retries: 2` und `click({ force: true })` machten daraus „flaky" (Exit 0). Der Befund „rot seit #806" war Last-Zufall: 84f8307f 6/24, 43cd7834 5/24 Wackelfälle bei `--repeat-each=6 --workers=8`, keine Datei aus #805/#806 im Importgraphen der Bühne; jetzt `stehtOffen` (kein `is-closing` nach „ist angelegt"), 24/24 grün, Gegenprobe gegen den alten Wirt 4/4 rot. ⚠ einen Verursacher erst benennen, wenn beide Commits unter gleicher Last gelaufen sind; ⚠ `flaky` ist ein Befund, kein Grün; ⚠ `force`/`retries` in einer Spec brauchen einen belegten Grund; ⚠ Playwright-Port 4174 ist fest — zwei Bahnen gleichzeitig enden mit „is already used" · `../portal/gesamtwert-assistent-und-anzeige.md`
- **UEMS-Kennzahlen-Abschluss: Bestandsschutz der ganzen Kennzahl-Maschine, Flag-Nachweis, Stand am Tag der Freigabe (AP-11 IP-16, Meilenstein 6 „freigabefähig“)** — `UemsKennzahlenBestandsschutzTest`: Anlegen über die Route, Kennzahl-Schritt des Takts, rückwirkende Fassung durch die Kaskade, Naht auf dem Reihen-Pfad — danach jede Tabelle außerhalb `kennzahl%`, sechs Rollup-Tabellen und Verlauf plus Export eines Messwerts byte-gleich zum Takt in der Verdrahtung von vor AP-11; AP-11s Zeilen in geteilten Tabellen stehen mit Namen da (`kennzahl_neu_gebildet`, `kennzahl_fassung:…`), die Datenbank-Jobs sind im Test aus. ⚠ `voltpilot.uems.kennzahlen.enabled` (Vorgabe AN) steht in keiner Deploy-Konfiguration und nimmt NUR den Stundenschritt — Naht, Routen und Portal haben keinen Schalter; ⚠ nicht in surefire eintragen (direkte Lauf-Tests würden still leer); ⚠ am Tag der Freigabe kann ein Kunde Stück-/kg-Werte im Portal weder eingeben noch importieren (AP-09 IP-9/IP-10), eine Fläche ist kein Nenner, Lesen ist nicht je Person begrenzt (AP-03 IP-11) · `uems-kennzahlen-abschluss.md`
- **UEMS-Berichte-Abschluss: Bestandsschutz der ganzen Berichts-Maschine, Flag-Nachweis, Stand am Tag der Freigabe (AP-12 IP-15, Meilenstein 7 „freigabefähig“)** — `UemsBerichteBestandsschutzTest`: der Oktober von MS-12 an der echten Kette, dann Anlegen, Freigabe Nr. 1, Stand/PDF/CSV, Pfad 2 (Strukturänderungs-Läufer), Pfad 1 (echte `BerichtKaskade` in der Transaktion der Kaskade), Revision Nr. 2 — danach jede Tabelle außerhalb `bericht%`, sechs Rollups, Verlauf/Export und der Bestand-Geräte-CSV über seine Route (ohne die neun Kopfzeilen) byte-gleich; AP-12s vier Meldungen in `messreihe_ereignis` mit Namen ausgenommen. Schalter: nur Naht und Läufer (`voltpilot.uems.berichte.enabled`, `….struktur.enabled`, Vorgabe AN, in gitops nicht gesetzt) — Routen, Portal, PDF/CSV, Belegschutz und das 403 am Bestand-CSV haben keinen; die Welt erscheint erst, wenn ein Standort misst. Größte Kundenlücke: PDF/CSV-Knöpfe nicht eingehängt. Release-Note „Unterstützer: kein CSV mehr“ · `uems-berichte-abschluss.md`
- **UEMS-Energiebilanz je Anlage: Zufluss · Abfluss · zugeordnet · nicht zugeordnet mit Herkunft, Balken ohne Prozentzahl (AP-13 IP-8 = AP-10 IP-14, E7 = A, B1/B2, Meilenstein 4)** — Reiter Anlage › Verlauf › „Energiebilanz“ nur mit Hauptzähler in der Stellung (`surface.energiebilanz` aus `GET …/bilanz?periode=tag`, nur auf einer Ebene), rein `anlageEnergiebilanz.ts`, Render `pages/EnergiebilanzSection.tsx`: je Hauptzähler Live-Zeile mit Stand und Grund-Satz, Vorschlag „Rest anlegen“ nur mit `messstelle.formel`, Zeilen aus der Route (`anzeige` wörtlich, „— keine Werte (MS-14 fehlt)“), Speicher-Anteile als zwei Zeilen, `MiniShareBar` je Unterzähler in kWh, Herkunfts-Karte des Rests (Fassung · Verteilung · berechnet am · Version · Eingänge), Abschnitte bei Stellungswechsel untereinander; der Baustein „Energiebilanz“ der Übersicht springt jetzt hierher, „Standort › Anlagen“ trägt je Zeile den Weg (das Versprechen aus IP-2). ⚠ „nicht zugeordnet“ steht immer; ⚠ „gemessen“ aus dem Register, Zone ohne Herkunft (zwei Befunde an AP-10); ⚠ Leerzustand Z4 ohne Hauptzähler, Bestand ohne Fakt zeichengleich; ⚠ GesamtwertKarten-Chip bewusst nicht · `uems-bilanz-flaeche.md`
- **UEMS-Gebäude-Karte auf „Standort › Gebäude“: Energie · Messstellen · Kennzahlen je Gebäude (AP-13 IP-10 = AP-10 IP-17 Portal-Teil, E4 = A, Ü6/B4, Meilenstein 5)** — die Hülle `Ortsbaum.gebaeudeKarte` aus IP-2 bekommt ihre Blöcke: rein `gebaeudeKarte.ts`, Render + Laden `components/GebaeudeKarte.tsx` (EINMAL je Seite: Bilanz je Anlage, Register je Gebäude heute und am letzten Tag des Zeitraums, Kennzahlen, Selbstauskunft), EIN Zeitraum über dem Baum. Energie **je System** über `uemsBilanz.gebaeude` (gemessen im Gebäude mit Posten · außerhalb im selben System · Rest des Systems „nicht verortet“ mit Sprung in die Anlagen-Bilanz), Messstellen = Register-Aggregat mit Filter Ort und Sprung `…/messstellen?ort=G-2` (`messstellen.ortAus`/`ortSchluessel`), Kennzahlen mit Geltung Gebäude ODER Bereich + „Kennzahl anlegen“ mit vorgeschlagener Menge (`kennzahlAnlegen.MengenVorschlag`/`mitVorschlag`, Satz in Schritt 2). ⚠ NIE eine Gebäude-Summe (36.900 gehört der Anlage); ⚠ Datenlage „5 von 5“ statt O4s „3 von 3“ (E13 zählt Hauptzähler und berechnete mit — Befund am Referenzfall); ⚠ „Kennzahl anlegen“ nur mit `kennzahl.standort_definieren`, mit Stichtag gar nicht; ⚠ die Zeile „Versorgung“ wartet auf die Route AP-10 IP-17 und ist als Test BENANNT, nicht gebaut · `uems-gebaeude-karte.md`
- **UEMS-Kostenstellen und Prozesse nebeneinander: die Fläche sagt, warum es keine Gesamtsumme gibt (AP-13 IP-9 = AP-10 IP-15 Listen-Teil, E8 = A, B6/B7)** — Reiter „Liste · Kostenstellen · Prozesse“ der Welt Messstellen am Unternehmen (`?reiter=`, Zeitraum in der Adresse), rein `kostenstellenUebersicht.ts`, Render `pages/KostenstellenSection.tsx`, Wirt `MessstellenWelt` in `pages/MessstellenPage.tsx`; je Kostenstelle EIN Aufruf `api.kostenstelleEnergie` (Zwischenspeicher 120 s, Mandanten-Schlüssel) mit gemessen · verteilt · berechnet · Summe, Posten als Sprung zur Messstelle mit Periode; „nicht verteilt“ EINMAL oben aus einer Antwort, Doppelzählungs-Warnung als Zeile (ändert keine Zahl), „gültig bis“ an beendeten (F12), Prozesse mit ihrer Prozess-Summe (MS-20, eine berechnete Messstelle). ⚠ **Keine Summe über Kostenstellen** — der Satz statt des Fußes, das Bild hat kein Feld dafür; ⚠ `keine_zuordnung` steht auch an einem einzelnen Block; ⚠ die Liste heißt „Liste“ (Reiter-Namen sind Teilwort-Treffer in Playwright), CSS-Präfix `vp-ks-` · `uems-kostenstellen-flaeche.md`
- **Zwei rote Oberflächen-Prüfungen aus dem Durchlauf vom 15.09.2026 — zwei verschiedene Fälle, keiner ein kaputtes Produkt (16.09.2026)** — `erloes-formel.spec.ts:33` war **veraltet**: #626 benannte die Messlatte in „Ohne smarte Steuerung" um (die Vergleichs-Anlage hat DENSELBEN Speicher und fährt ihn nur stur; „Ohne Steuerung" las sich wie „ohne Speicher"), zog die jsdom-Zwillinge `steuerungFormel.test.ts`/`SteuerungFormel.test.tsx` mit — die Playwright-Spec nicht; 5/5 rot, jetzt 0/30. `help.spec.ts:7` war die **Bühne**: der Vite-Entwicklungsserver übersetzt jedes Modul erst beim ersten Abruf, die ERSTE Seite eines Laufs stand nach 5,9–8,9 s, jede weitere nach 0,4–1,3 s, dieselbe Seite 0,5 s später neu geladen nach 0,5 s — am Produkt ändert sich dazwischen nichts, und der ausgelieferte Build kennt die Wartezeit gar nicht. Wer die Rechnung zahlte, entschied die Worker-Verteilung (mal `:7`, mal `:17`/`:35`/`:67`); `webServer.url` zeigt auf `e2e/edit-flow.html`, das die ganze `App` gar nicht mountet. Repariert mit `playwright.global-setup.ts` (lädt `e2e/help.html#/hilfe/fahrplan` einmal vor der ersten Zusicherung), abwechselnd gemessen: **ohne 6/8 rot, mit 0/8**, Laufzeit 22–72 s → 16–24 s. ⚠ NICHT über ein größeres Budget, `retries` oder `force` — jede Zusicherung behält ihre 5 s; ⚠ Vite `server.warmup` half nicht (6,7–8,9 s, eher länger); ⚠ wer ein Kundenwort ändert, sucht es in `src` UND `e2e` · `../portal/e2e-kaltstart-und-veraltete-zusicherung.md`
- **UEMS-Sprünge der Kette: aus Herkunft, Nachweis und Quelle werden Wege — je mit Periode und Version (AP-13 IP-11, E2/E10 = A, D1–D4, Meilenstein 6)** — der Befund „Die Kette bricht am Text ab“ ist behoben: rein `uemsOberflaechen.herkunftsZeile`/`kennzeichenSprung`/`periodeSchluessel` schneidet einen Satz in `Stueck[]` (zusammengefügt zeichengleich der Satz von vorher), Render `components/HerkunftsZeile.tsx`. Kanten an acht Stellen: Kennzahl-Herkunft (`eingaengeStuecke`/`paareStuecke`), Bericht-Nachweis + „Zur Messstelle/Zur Kennzahl“ und das Quellenverzeichnis (Periode = Zeitraum des Berichts), Bilanz-Zeilen (`TeilBild.sprung`, Kostenstelle der Verteilung), Kostenstellen-Posten (IP-9), Register-Quelle → Geräteseite › Komponente, `berechneteHerkunft` an der Werte-Karte (D4: Formel · Zeitpunkt · Version · Eingänge), Cockpit-Weg `cockpitWeg` (E2 = A, kein Zahlentausch) mit Filter `?anlage=` (`messstellen.anlageAus`). ⚠ **Jeder Sprung trägt Periode UND Version** — ohne sie landet der Kunde bei einer anderen Zahl als der angeklickten; ⚠ eine Adresse darf `MS-12` nennen, die Route braucht die UUID → `MessstelleSeite` löst das Kennzeichen EINMAL über das Register auf; ⚠ Bezugsgröße/Ereignis/Box bleiben Text (D3); ⚠ Tippflächen am Telefon: Innenabstände am Inline-Sprung, `min-height: 44px` am Unterzähler-Namen; ⚠ den Rahmen der Bilanz VOR dem Aufklappen der Herkunft messen (Prozent-Wächter) · `uems-spruenge-kette.md`
- **UEMS-Box an der Quelle: die Quelle-Karte und die Register-Spalte nennen, WELCHE Box liest und seit wann (AP-13 IP-12, L6 · W10)** — reines Modul `frontend/portal/src/boxAnQuelle.ts` hält den EINEN Satz „gelesen von {box} seit {zeitpunkt}“; die Fläche reicht nur ihren Zeitpunkt-Text herein (Register „04.11.2026 09:38“, Karte „04.11.2026, 09:38 Uhr“). Kanten: `messstellen.ZeileWoerter.quelle.box` (`.vp-ms-box`), `quelleBinden.QuelleWort.box` (`.vp-qk-box`), `uemsVerlauf.markerSatz`/`marker` für `handover`; geladen von `useBoxenAnQuellen.ts`, erster Portal-Aufrufer von `GET …/sites/{id}/data-sources` (AP-06 IP-3). ⚠ **W10: eine Box JE QUELLE, nicht je Anlage** — das Anlagen-Modell-Wort „genau EINE Box“ ist nur in `komponenten.ts` und `AnlagenModellSection.tsx` berichtigt (dort Bedingung für den WEG); `edgeBoxLine` und die Box-Seite bleiben AP-06 IP-16. ⚠ **Befund an AP-06:** `…/data-sources` nennt weder Geräte noch Komponenten einer Quelle (`geraete_ids` = Modbus-IDs) — der Weg Komponente → Quelle geht über `…/geraete` und `data_source_id`, also ZWEI Aufrufe je Anlage. ⚠ Box-Tausch vs. Übergabe entscheidet die Zeitachse (gibt die alte Box ALLE Zuständigkeiten ab? AP-06 E7) und gilt nur über die geladenen Anlagen. ⚠ Nichts wird erfunden: keine Zuständigkeit, keine Box ohne Kundennamen, keine 0 — dann steht kein Satz; Urteil „welche Box“ nur über den Zwilling `uemsDatenquelle.zustaendigeBox` (halboffen, der Tausch-Augenblick gehört der neuen Box) · `uems-box-an-quelle.md`
- **UEMS-Der gemessene Weg: Ebene → Welt → Zahl → Nachweis in EINEM Lauf, bei 375 und 1440 px (AP-13 IP-13, E14 = A, M1–M4, O17, Meilenstein 7)** — keine neue Fläche, sondern der Beweis der gebauten: `e2e/weg.spec.ts` geht mit ECHTEN Klicks Standort-Übersicht → Gebäude (Halle 2 aufgeklappt) → Register → „Stand am …“ 03.11.2026 → MS-10 › Werte → Verlauf → Vergleich (Monat, Vorperiode) → Versionen und misst je Station Querlauf 0 (Dokument, jedes Element von `.vp-main`/`.vp-modal`, Dialog-Körper), Tippflächen ≥ 44 px und die Konsole; dazu jede Station der Bühne einzeln (`ansicht=werk-gebaeude` · `werte&ms=&tag=` · `verlauf&mon=` · `vergleich&v=` · `bilanz&an=AN-2` · `kostenstellen`). Die Bühne `startansicht.tsx` stellt das Register jetzt mit seinem Wirt wie `App.tsx` (`onOeffnen`/`onWerte`, Periode/Version/Vergleich als Bühnen-Zustand an `data-route`) und beantwortet die Werte-Route aus den Fixtures der Referenzfälle; `&stand=` ist der Stichtag, den das Register als „heute“ ausgibt. ⚠ **Eine Tippfläche ist nicht die Höhe des Knopfes** — am Telefon deckt ein `::after { inset: 0 }` die ganze Register-Karte ab (Knopf 20 px, Fläche 56 px); ⚠ „Letzter Wert“ gibt es nur an einer Zeile MIT Wert (Momentanwerte nur am 20.10.2026) — sonst führt am Rechner das Zeilenmenü; ⚠ der Einstieg landet auf dem Vortag, ein älterer Tag kommt über „Stand am …“; ⚠ die Spalte „Aktionen“ ist neu in `messstellen.spec.ts`; ⚠ die Werte-Route der Bühne antwortet nur im Weg-Bild, sonst behält der Bericht-Nachweis seine Antwort; Befund an AP-08: der Verlauf zeigt die gemessenen Viertelstunden mit der Lücke, während die Karte den Ersatzwert nennt · `uems-weg-gemessen.md`

- **UEMS-Oberflächen-Abschluss (AP-13 IP-14, Q2/O18): Ebenen und Bestandsschutz** — HTML-Snapshots gegen `84f8307f`, `startEbene`, sechs Verlauf-Reiter und Portfolio-Navigation; IP-2-Standortseiten nur mit Messfunktion; Release-Note zur Registerzählung von IP-7 · [Ebenen und Abschluss](uems-oberflaechen-ebenen.md).
- **UEMS-Oberflächen: Werte und Verlauf (AP-13 IP-3–IP-6)** — Quelle Messstellen, Zone einmal im Kopf, Raster der Route, Vergleich nur über den Zwilling; AST-Wächter `uemsKeineRechnung.test.ts` mit Mutationsproben · [Werte und Verlauf](uems-oberflaechen-werte-verlauf.md).
- **UEMS-Oberflächen: Energiebilanz (AP-13 IP-7–IP-10)** — Quellen je Ebene, Ort × Stellung, kein Gebäude-Rest, keine Kostenstellen-Gesamtsumme; ausschließlich Zeichnungsgeometrie in der Fläche · [Energiebilanz](uems-oberflaechen-energiebilanz.md).
- **UEMS-Oberflächen: Sprünge (AP-13 IP-11–IP-13)** — Periode und Version am Eingang, Box je Quelle, Cockpit ausschließlich mit gefiltertem Weg; Kanten und Prüfnachweise · [Sprünge](uems-oberflaechen-spruenge.md).

## Pflegeregel

Neue UEMS-Pakete tragen ihren Eintrag HIER ein, nicht im Themen-Index von `AGENTS.md`.
