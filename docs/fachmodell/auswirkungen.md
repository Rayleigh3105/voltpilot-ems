# Auswirkungs-Karte: was aus dem heutigen Stand wird

Diese Karte ist AP-00 IP-6. Sie sagt je heutiger Tabelle, Klasse und Portal-Datei, welches
Objekt des Fachmodells sie künftig trägt und **welcher Art** die Änderung ist. Sie ist
**kein Schema und keine Migration** — sie ist die Landkarte, aus der die Bau-Pakete von
AP-02, AP-04, AP-06, AP-07 und AP-14 ihre Migrationen ableiten.

Grundlage: AP-00 §6.2 (10.09.2026), verfeinert durch AP-02 §6.2, AP-04 §6.2, AP-06 §6 und
AP-07 §6.1 — alle vier am 10.09.2026 entschieden. Die Begriffe stehen in
[`glossar.md`](glossar.md), die Kardinalitäten in [`beziehungen.md`](beziehungen.md).

**Die vier Arten der Änderung:**

| Art | Bedeutung |
|---|---|
| **bleibt** | Tabelle, Spalten und Bedeutung unverändert; nichts zieht um. |
| **bekommt Verweis** | Die Tabelle bleibt und erhält additiv einen nullable Verweis auf ein neues Objekt. Eine alte Zeile ohne Verweis bleibt gültig. |
| **Felder ziehen um** | Die Wahrheit wandert auf ein neues Objekt; die alte Spalte bleibt lesbar, bis das zuständige Paket sie stilllegt — **nie Doppelpflege**. |
| **neu daneben** | Ein neues Objekt entsteht neben dem alten, ohne es zu verändern. |

`tools/check_auswirkungen.sh` prüft, dass jede unten genannte Tabelle in den Migrationen
wirklich existiert.

## Datenhaltung

| Heute | Zielobjekt | Art | Was genau |
|---|---|---|---|
| `tenant` (`MIG/V1__core_schema.sql:36-44`) | Kundenbereich | **bleibt** + **neu daneben** | Der Zaun bleibt, wie er ist (RLS, `tenant_id` als Keycloak-Attribut). Daneben entsteht `unternehmen` 1 : 1 mit Name, Kurzname, Zeitzone-Vorgabe, Sitz und Rechtsform; 1 : n für Konzerne bleibt vorbereitet (AP-00 E2, AP-02 §6.2). |
| `site` (`MIG/V1__core_schema.sql:45-52` + 17 Zusatzspalten) | Anlage | **bleibt** + **Felder ziehen um** | Kennung, Adresse `#/anlage/…`, MQTT-Topics, Betriebsmodell, Fahrplan und Erlöse bleiben unangetastet (AP-00 E1 = A). Die Zuordnung zum Standort liegt in einer eigenen zeitgültigen Beziehung `anlage_standort`, **nicht** in einer Spalte `standort_id` — nur so tragen Umzüge Historie (AP-02 §6.2, revidiert AP-00 §6.2). Die Koordinaten `latitude`/`longitude` (`MIG/V20260701010000__site_geo_and_data_feeds.sql:29-30`) ziehen zum Standort, bleiben aber lesbar; das Portal-Feld heißt weiter „Standort“ (AP-00 E9 = B). Tarif-, Leistungs- und Vergütungsfelder (`tarif_art`, `leistungspreis_eur_kw`, `max_feed_in_kw`, `anzulegender_wert_ct_kwh`, `netzladen_erlaubt`) bleiben in AP-10 unverändert an der Anlage; sie ziehen erst mit dem Folgepaket „Netzanschluss-Preisblatt“ zum Netzanschluss (AP-10 E8/W9). AP-10 gibt der Anlage nur den Verweis auf ihren Netzanschluss. |
| `site` (`MIG/V20260911150000__uems_datenquelle_zustaendigkeit.sql:307-316`) | führende Box | **bekommt Verweis** (gebaut, AP-06 IP-2) | `site.lead_device_id` — die führende Box je Anlage als gespeicherter, sichtbarer Fakt, nie geraten (AP-06 E3, §6). Nullable, Fremdschlüssel `(lead_device_id, tenant_id)` → `device` mit `ON DELETE SET NULL (lead_device_id)`: das heutige Unclaim löscht die Box weiter, die Anlage behält ihre Zeile; NULL heißt „keine gespeicherte Wahl“ (dann Speicher-Box → einzige Box → keine, Vertrag `docs/contracts/v2/data-source-assignment.md` §8). Sonst ist `site` zeichengleich; kein Backfill — die Wahl schreibt erst IP-5. |
| `site_supply_price` (`MIG/V20260729000000__site_supply_price.sql`) | Preise am Netzanschluss | **bleibt** | **bleibt — bis zum Paket „Netzanschluss-Preisblatt“.** AP-02 W6 und die frühere Fassung dieser Zeile kündigten den Umzug „mit AP-10“ an; AP-10 E8 (12.09.2026) legt nur das Objekt `netzanschluss` und seine zeitgültige Bindung an. Die zwei GRENZEN bekommen mit AP-15 IP-3 ein Grenzblatt am Netzanschluss (zeitgültig; es gilt der engere Wert aus Anlage und Netzanschluss). Das Preisblatt bleibt ein eigenes, ungeplantes Paket und ist keine Voraussetzung von AP-15 (AP-15 W1). An dieser Tabelle ändert sich nichts. |
| `unternehmen` (`MIG/V20260911100000__uems_unternehmen_standort.sql:71-111`) | Unternehmen | **neu daneben** (gebaut, AP-02 IP-2a) | 1 : 1 neben `tenant` (UNIQUE `tenant_id`; ein Konzern-Paket löst den Schlüssel bewusst): Name 1–120, Kurzname, Zeitzonen-Vorgabe (DACH), Sitz, Rechtsform. Der Backfill legt je Mandant genau eines an (Name des Kundenbereichs, Europe/Berlin) — sonst nichts; ein später angelegter Kundenbereich bekommt seins im selben Statement wie der Mandant samt Protokolleintrag (`TenantRepository.create`, AP-02 IP-3). Die App-Rolle liest und bearbeitet, legt nicht an und löscht nicht; es endet nur mit dem Offboarding, das es ausdrücklich abräumt (`services/api/src/main/java/com/voltpilot/api/repo/TenantRepository.java`), denn alle Fremdschlüssel sind `ON DELETE RESTRICT`. |
| `standort` (`MIG/V20260911100000__uems_unternehmen_standort.sql:116-179`) | Standort | **neu daneben** (gebaut, AP-02 IP-2a) | Name (eindeutig je Unternehmen unter nicht archivierten), Kurzzeichen (eindeutig je Kundenbereich, nie wieder frei), Adresse mit Land DE · AT · CH und PLZ-Format, Zeitzone (DACH), Nutzung[] aus dem geschlossenen Vokabular `uems_nutzung_gueltig` (das `ort` mitbenutzt), Notiz, Lage, Zustand (Entwurf · eingerichtet · aktiv · archiviert), Archiv-Felder. RLS + FORCE nach dem Muster `site_vehicle_tag` (`MIG/V20260866000000__fahrzeug_profile.sql:38-89`); archivieren statt löschen. |
| `anlage_standort` (`MIG/V20260911100000__uems_unternehmen_standort.sql:203-236`) | Zuordnung Anlage → Standort | **neu daneben** (gebaut, AP-02 IP-2a) | Zeitgültig in Tagen, `gueltig_bis` = letzter Tag einschließlich; Exklusions-Constraint auf `daterange(gueltig_ab, gueltig_bis, '[]')` je Anlage, aufgehobene ausgenommen — dieselben Regeln wie `docs/contracts/v2/ortsbaum-vectors.json`. `site` bleibt zeichengleich. Die App-Rolle ändert nur `gueltig_bis` und `aufgehoben_am`. Bis die Bestandsübernahme (AP-02 IP-9) Zeilen anlegt, ist sie leer; den Grabstein beim Anlagen-Löschen (AP-02 W5) entscheidet IP-9/AP-14 — bis dahin verweigert `ON DELETE RESTRICT` das Löschen einer zugeordneten Anlage. |
| `ort` (`MIG/V20260911110000__uems_gebaeude_bereich_flaeche.sql:61-114`) | Gebäude, Bereich | **neu daneben** (gebaut, AP-02 IP-2b) | Art `gebaeude` \| `bereich` (die App-Rolle ändert sie nie), Name 1–120, Kurzzeichen G-x/B-x (eindeutig je Kundenbereich über beide Arten, nie wieder frei), Nutzung[] über `uems_nutzung_gueltig`, Baujahr nur am Gebäude (1800–9999; „bis zum laufenden Jahr“ prüft der Schreibweg), Notiz, Zustand (Entwurf · eingerichtet · aktiv · archiviert), Archiv-Felder. Woran ein Ort hängt, steht NICHT hier, sondern je Tag in `ort_zuordnung`; der Name eindeutig unter Geschwistern ist deshalb Sache des Schreibwegs (AP-02 IP-5). Archivieren statt löschen. |
| `ort_zuordnung` (`MIG/V20260911110000__uems_gebaeude_bereich_flaeche.sql:127-181`) | Zuordnung Gebäude/Bereich → Elternknoten | **neu daneben** (gebaut, AP-02 IP-2b) | Genau einer von `eltern_standort_id` / `eltern_ort_id`; die Art-Regel (`erlaubte_eltern` der Ortsbaum-Vektoren: nie Bereich unter Bereich, Gebäude nur am Standort) als zwei Fremdschlüssel über berechnete Art-Spalten gegen `ort.art`. Zeitgültig in Tagen wie `anlage_standort`: Exklusions-Constraint auf `daterange(gueltig_ab, gueltig_bis, '[]')` je Ort, aufgehobene ausgenommen. „Das Ziel besteht an jedem Tag“ prüft der Schreibweg (IP-5/IP-12). Die App-Rolle ändert nur `gueltig_bis` und `aufgehoben_am`. |
| `flaeche_gueltigkeit` (`MIG/V20260911110000__uems_gebaeude_bereich_flaeche.sql:199-233`) | Bezugsfläche | **neu daneben** (gebaut, AP-02 IP-2b) | Je Standort ODER Ort, ganze m² > 0 — „nicht erhoben“ ist keine Zeile, nie 0 (AP-02 E3, Regel 12); je Objekt ein Intervall je Tag (ein Exklusions-Constraint je Objektart), Korrektur durch Aufheben, nie durch Umschreiben von `m2`. Die Summe der Gebäude ist eine Ableitung des Lesewegs. AP-09 liest sie als Bezugsgröße. |
| — | Netzanschluss | **neu daneben** | Kennzeichen, Name, Marktlokation, Netzbetreiber, Anschlussleistung (kVA), vereinbarte Leistung (kW), Messung RLM \| SLP (AP-00 E6, AP-10 E8); zeitgültig an eine Anlage gebunden, je Tag höchstens eine je Seite. Tarif, Vergütung und Einspeisegrenze ziehen NICHT mit AP-10 um — sie bleiben an der Anlage (AP-10 W9). Einspeise- und Bezugsgrenze liest AP-15 über das Grenzblatt am Netzanschluss (`netzanschluss_grenze`, AP-15 IP-3); die Spalten der Anlage bleiben, ohne gebundenen Netzanschluss gilt der alte Wert. Voraussetzung von AP-15 ist das Grenzblatt, nicht ein Preisblatt (AP-15 W1). Vertrag: `docs/contracts/v2/netzanschluss.md` (AP-10 IP-1), Tabellen: AP-10 IP-6. |
| — | Prozess, Kostenstelle | **neu daneben** | Prozessbaum mit einer Ebene Verschachtelung; flache Kostenstellen mit festen Prozentanteilen, Summe 100 % (AP-00 E5). |
| `messstelle` (`MIG/V20260911140000__uems_messstelle.sql:92-134`) | Messstelle | **neu daneben** (gebaut, AP-04 IP-2) | Das heutige Kennzeichen (UNIQUE `(tenant_id, kennzeichen)`, Form `^[A-Z0-9./-]{2,16}$` wie `MessstelleRegeln`), Name (NULL = fehlt noch: ein Entwurf), Art `gemessen`\|`berechnet`, Medium (das volle Vokabular des Schemas; „nur Strom wählbar“ ist Sache der Fläche), die EINE Hauptgröße (Größe · Richtung · Einheit · Wertart, geprüft gegen den Größen-Katalog), Notiz. Art, Medium und Hauptgröße sind per Trigger nie änderbar. Gespeichert werden die Zustands-Eingänge `angehalten_ab`/`archiviert_am`, nie die Stufe Entwurf/eingerichtet/aktiv — die leitet `MessstelleRegeln.lebenszyklus` ab. Die App-Rolle löscht nicht; alle Fremdschlüssel sind `ON DELETE RESTRICT`, das Offboarding räumt ausdrücklich ab (`services/api/src/main/java/com/voltpilot/api/repo/TenantRepository.java`). |
| `messstelle_groesse` (`MIG/V20260911140000__uems_messstelle.sql:149-168`) | Nebengröße | **neu daneben** (gebaut, AP-04 IP-2) | 0..n je Messstelle (AP-04 E1): je `(groesse, richtung)` einmal und nie die Hauptgröße, Größen-Katalog wie oben, das Medium über den Verweis `(messstelle_id, tenant_id, medium)` gleich dem der Messstelle; archivierbar, ihre Größe nie änderbar. |
| `messstelle_kennzeichen` (`MIG/V20260911140000__uems_messstelle.sql:175-184`) | Kennzeichen-Belegung | **neu daneben** (gebaut, AP-04 IP-2) | Jedes Kennzeichen, das eine Messstelle je trug — archiviert UND früher (Regel 9, Widerspruch 7 in `docs/contracts/v2/messstelle.md`); der Primärschlüssel `(tenant_id, kennzeichen)` verbietet die Weitergabe an eine andere, die Messstelle selbst darf zurück. Nur der Trigger an `messstelle` schreibt, die App-Rolle liest. |
| `messstelle_kennzeichen_seq` (`MIG/V20260911140000__uems_messstelle.sql:197-202`) | Kennzeichen-Zähler | **neu daneben** (gebaut, AP-04 IP-2) | Eine Zeile je Mandant — eine Tabelle, keine Sequenz: die automatische Vergabe `MS-0001` … sperrt sie (zwei gleichzeitige Vergaben, zwei Nummern), der Zähler rückt nur vor und nur mit einem gespeicherten automatischen Kennzeichen (`services/api/src/main/java/com/voltpilot/api/uems/MessstelleRepository.java`). |
| — | Zuordnungen und Quellen der Messstelle | **neu daneben** | Die zeitgültigen Zuordnungen `messstelle_ort`, `messstelle_stellung`, `messstelle_prozess`, `messstelle_kostenstelle` (je `gueltig_ab DATE`/`gueltig_bis DATE`, EXCLUDE gegen Überlappung, `rueckwirkend`, Akteur; AP-04 IP-7) — die Quellen stehen seit AP-04 IP-13 in `messstelle_quelle` (Zeile unten). **Nie in die Reihen hinein** (AP-04 §6.2). |
| `messstelle_quelle` (`MIG/V20260911250000__uems_messstelle_quelle.sql:68-188`) | Quellenbindung (führend · Vergleich) | **neu daneben** (gebaut, AP-04 IP-13) | Eine Zeile = eine Bindung einer Größe (Haupt- oder Nebengröße, `(groesse, richtung)`, Trigger: gehört zur Messstelle) an EINEN Messkanal = Komponente (`entity_id`) + Einbau (`geraet_id`, der die Komponente zu `gueltig_ab` speist — nie vom Kunden gewählt) + Kanalname (`point_key`); Rolle `fuehrend`\|`vergleich`, Zweck nur und immer beim Vergleich, Herleitung aus der Wertart (`integration` für AP-08 gekennzeichnet), halboffen `tstzrange(gueltig_ab, gueltig_bis, '[)')` auf die Minute, Ablesestände mit optionaler Einheit, `rueckwirkend` + `eingetragen_am` + Akteur. Drei Exklusionen mit `tenant_id` vorn: EINE führende je Größe und Zeitpunkt, derselbe Vergleichs-Messwert nie doppelt, ein Messwert führt je Zeitpunkt EINE Messstelle. Nie überschrieben: kein DELETE, nur `UPDATE (gueltig_bis, endstand, endstand_einheit)`, und der Trigger lässt `gueltig_bis` genau einmal von offen auf einen Zeitpunkt gehen. `→ measurement_point`/`geraet` CASCADE (wie `geraet_komponente`), `→ messstelle`/`tenant` RESTRICT (Offboarding räumt sie zuerst ab). Schnittstelle `POST/GET /api/v1/messstellen/{id}/quellen`, `PUT …/quellen/{qid}/beenden` (`MessstelleQuelleService`), `speist` im Messkanal-Read-Model. |
| `data_source` (`MIG/V20260911150000__uems_datenquelle_zustaendigkeit.sql:82-137`) + `data_source_kennzeichen_seq` (`:156-186`) | Datenquelle | **neu daneben** (gebaut, AP-06 IP-2) | Kennzeichen DQ-n je Kundenbereich aus einem Zähler als TABELLE (`uems_datenquelle_kennzeichen`: nächste freie Nummer, eine von Hand belegte wird übersprungen, der Zähler rückt nur mit einer gespeicherten Quelle vor), Form wie Messstellen-Kennzeichen; Anlage (die der Verdrahtung), Name (optional), Protokoll aus dem geschlossenen Vokabular, Adresse ohne Randleerzeichen, Geräte-IDs aufsteigend (`uems_geraete_ids_gueltig`), Netzlage, `mehrere_leser` (Vorgabe nein), `steuerquelle`, `kadenz_s` 1–86 400, Archiv-Felder. Lebenszyklus und Beobachtung sind keine Spalten (sie folgen aus Prüfung, Zuständigkeit und Herzschlag). RLS + FORCE; die App-Rolle löscht nie (archivieren). Alle Fremdschlüssel `ON DELETE RESTRICT`, das Offboarding räumt ausdrücklich ab. |
| `data_source_assignment` (`MIG/V20260911150000__uems_datenquelle_zustaendigkeit.sql:210-296`) | Zuständigkeit Box → Datenquelle | **neu daneben** (gebaut, AP-06 IP-2) | HALBOFFEN auf die Minute (`tstzrange(effective_from, effective_to, '[)')`, NICHT Tage wie `anlage_standort`); Exklusions-Constraints „je Quelle und Zeitpunkt eine Box“ und „je Box und Zeitpunkt ein Weg (Protokoll + Adresse)“ — dafür trägt jeder Zeitraum eine Kopie von Protokoll und Adresse, die der zusammengesetzte Fremdschlüssel zur Quelle `ON UPDATE CASCADE` gleich hält. `device_id` bewusst OHNE Fremdschlüssel: ein Trigger prüft Box und Mandant beim Eintragen, danach überlebt der Zeitraum seine Box (Unclaim bricht nicht, die Herkunft bleibt); den echten `ON DELETE RESTRICT` bekommt die Spalte, wenn AP-07 E8 das Löschen durch „ausgebaut“ ersetzt. Die App-Rolle ändert nur `effective_to`. Dieselben Regeln wie `docs/contracts/v2/data-source-vectors.json` (Familien `zeitraeume`, `antrag`). |
| — | Rückmeldung je Box × Quelle | **neu daneben** | `device_data_source_status` je Box × Quelle, `device_status_seen_at` (AP-06 E5, IP-14/IP-15). |
| `geraet` (`MIG/V20260911200000__uems_geraet.sql:120-201`) + `geraet_kennzeichen_seq` (`:324-356`) | Gerät (physisch) | **neu daneben** (gebaut, AP-04 IP-10) | EINE Zeile = EIN Einbau: ein Zählerwechsel ist ein neues Gerät (AP-04 E2) — `kennzeichen` GR-n bleibt über den Wechsel, `einbau_kennzeichen` (Z-5a, Z-5b) wechselt; genau das Paar `geraet`/`einbau` der Referenzdatei, des Herkunftsvertrags (Angabe 10) und der Quellenbindung. Geräteart geschlossen (Energiekarte ist ein Teil, keine Geräteart), Hersteller, Typ, Seriennummer, Bezeichnung, Datenquelle + Geräte-ID, `eingebaut_am`/`ausgebaut_am` halboffen auf die Minute; je Gerät und Zeitpunkt EIN Einbau (Exklusion, `tenant_id` vorn); Kennzeichen GR-n aus einem Zähler als TABELLE (`uems_geraet_kennzeichen`). `→ site` bewusst `ON DELETE CASCADE` (die Ableitung gibt jeder Bestandsanlage Geräte; das heutige Löschen einer Anlage bleibt), `→ tenant`/`data_source` RESTRICT; die App-Rolle hat kein DELETE und ändert nur, was der Kunde nachträgt oder der Ausbau setzt. **Ableitung** (`uems_geraete_ableiten`, `:423-527`, wiederholbar): je Bestands-Komponente genau ein laufendes Gerät; komponierte Geschwister ohne Pin/Marke/Modell/Verbindung hängen am ersten `battery-hybrid` ihrer Box (die Regel von `komponenten.ts` Schritt 2 + `ComponentAdoptionService.reusableComposedRow`), sonst je Komponente eines; `eingebaut_am` = das Frühere aus `created_at` und dem ersten Tag in `telemetry_v2_rollup_1d`; Seriennummer nur aus `kaco_http` (`serial` bei `solarman_v5` ist die des Datenloggers), `aus_bestand = true`. Lese-API `GET /api/v1/sites/{id}/geraete`, `GET /api/v1/geraete/{id}` mit Vorgängern (`GeraetService`). |
| `geraet_teil` (`MIG/V20260911200000__uems_geraet.sql:212-259`) | Energiekarte (Teil des Controllers) | **neu daneben** (gebaut, AP-04 IP-10) | Identität = (Gerät, Steckplatz) (AP-05 E4): je Steckplatz und Zeitpunkt EINE Karte, ein nicht erhobener Steckplatz kollidiert nie; Karten trägt nur ein Controller (zusammengesetzter Fremdschlüssel über die Geräteart); ein Kartenwechsel ist eine neue Zeile im selben Steckplatz — Gerätegrenze ohne Gerätewechsel (AP-05 E6). |
| `geraet_komponente` (`MIG/V20260911200000__uems_geraet.sql:271-316`) | Speisung Komponente ← Gerät | **neu daneben** (gebaut, AP-04 IP-10) | Statt einer Spalte `measurement_point.geraet_id`: welche Komponente wann von welchem Einbau (und über welche Karte) gespeist wird, halboffen auf die Minute — die Frage des Herkunfts-Nachschlags „welcher Einbau speiste K-5 um 10:39?“. Je Komponente und Zeitpunkt EIN Gerät (§4.6 Regel 6), je Karte und Zeitpunkt EINE Komponente; `→ measurement_point` `ON DELETE CASCADE` wie jede Tabelle an einer Komponente (das Gerät bleibt). |
| `device` (`MIG/V1__core_schema.sql:54-62`) | Box (Edge) | **bleibt** + **bekommt Verweis** | Enrollment, Zertifikat, Topic-Adresse und Heimat-Anlage bleiben (AP-00 E7). Neu daneben: die Zuständigkeit je Datenquelle. Ein Box-Tausch überträgt Heimat, Rolle, Zuständigkeiten, Mess-Selektionen, Freigaben und OTA-Zuordnung auf die Nachfolgerin; die alte Box wird „ausgebaut“, **nie gelöscht** (AP-06 E7, AP-07 E8). |
| `device_enrollment` (`MIG/V20260702040000__device_enrollment.sql:27-37`) | Anmeldung einer Box | **bleibt** | Der Claim-Weg über den Aufkleber ist auch der Weg der Nachfolge-Box (AP-06 E7). |
| `device_edge_version` (`MIG/V20260803000000__device_edge_version.sql:24-30`) | Fähigkeiten einer Box | **bleibt** + **neu daneben** | Daneben eine Cloud-Tabelle „Version → Fähigkeiten“, die jede Bestandsbox abdeckt; ab dem nächsten Edge-Release übersteuert ein additiver Block `supports[]` im Herzschlag die Tabelle je Box (AP-06 E12). |
| `device_source_status` (`MIG/V20260721000000__device_source_status.sql:24-41`) | Zustand je Datenquelle | **bleibt** | Die heutige Ist-Rückmeldung je Quelle bleibt; sie bekommt mit `data_sources[]` im Herzschlag ihren Vertrag (AP-06 E5). |
| `device_site_assignment` (`MIG/V20260843000000__component_edit_contract.sql:54-67`) | Umzug einer Box | **bleibt** | Unangetastet — AP-02 benutzt es nicht, weil die Heimat der Box die Anlage bleibt (AP-00 E7, AP-02 §6.2). |
| `measurement_point` (`MIG/V20260709000000__measurement_point.sql:27-60`, `entity_type` in `MIG/V20260718000000__v2_entity_registry.sql:22-24`) | Komponente | **bleibt** + **bekommt Verweis** | Rolle, Marke, Modell, Verbindung und Freigabe bleiben Attribute der Komponente. Neu additiv: `data_source_id` (nullable, gebaut in AP-06 IP-2, `MIG/V20260911150000__uems_datenquelle_zustaendigkeit.sql:322-332`: Fremdschlüssel `(data_source_id, tenant_id)` → `data_source` `ON DELETE RESTRICT`; kein Backfill — die Zuordnung schreibt die Vorschlagsliste IP-4; `device_id` bleibt, wie es ist). Das Gerät hängt NICHT als Spalte `geraet_id` an der Komponente, sondern zeitgültig in `geraet_komponente` (gebaut, AP-04 IP-10) — die Tabelle bleibt zeichengleich. Der Lebenszyklus „angehalten“/„archiviert“ kommt als additiver Zustand (AP-00 §6.2, AP-04 §6.2, AP-06 §6). |
| `measurement_point.folder` (`MIG/V20260709000000__measurement_point.sql:33`) | Anzeige-Ordner | **bleibt** | Wird **nicht** zum Ort umgedeutet — Messstellen verortet der Ortsbaum (AP-02 §6.2). |
| `component_definition` (`MIG/V20260817000000__component_authority_and_definitions.sql:74-109`) | Fassungs-Historie der Komponente | **bleibt** | Die Zeitgültigkeit der Messstellen-Zuordnungen kommt **nicht** aus diesem Journal: ein Journal sagt, WANN sich etwas geändert hat, eine zeitgültige Zuordnung sagt, WAS in einem Zeitraum galt (AP-00 §6.2). |
| `component_change_event` (`MIG/V20260843000000__component_edit_contract.sql:8-21`) | Verlauf der Komponente | **bleibt** + additive Art | Neue Art `device_replaced` als Marke „Zähler gewechselt“ (AP-04 §6.2, E10). |
| `component_activation_outbox` (`MIG/V20260843000000__component_edit_contract.sql:92`) | Zustellung an die Box | **bleibt** | Der Zustellweg bleibt; neu ist nur, WAS je Box hineingehört (die disjunkte Menge ihrer Zuständigkeiten, AP-06 E4). |
| `quelle_einstellung` (`MIG/V20260911280000__uems_quelle_einstellung.sql:134-230`) | Einstellungs-Fassung je Quelle | **neu daneben** (gebaut, AP-04 IP-11) | Eine Zeile = eine Fassung an einer Quelle = Einbau (`geraet_id`), optional Komponente (`entity_id`) und Kanal; Art aus dem geschlossenen Vokabular (Wandler Strom/Spannung, Skalierung, Offset, Vorzeichen umgekehrt, Impulswertigkeit, Zählerkonstante), Wert je Art per CHECK (`uems_einstellung_wert_gueltig`), Anwendung `angewendet`\|`dokumentiert`, Herkunft `bestand`\|`verbindung`\|`eintrag`, halboffen `tstzrange(gueltig_ab, gueltig_bis, '[)')` auf die Minute, `tatsaechlich_ab`, `rueckwirkend` + Akteur; EINE Fassung je Quelle, Art und Zeitpunkt (Exklusion, `tenant_id` vorn). Fassung 1 aus den heutigen Werten abgeleitet (`scale`/`offset` je Selbstbau-Kanal, `invert_*_sign`/`power_scale` je Verbindung, „gilt seit Beginn“ der Speisung; `signed` ist der Datentyp, kein umgekehrtes Vorzeichen). Die heutigen Felder bleiben die Wahrheit der Box, bis AP-06 die Fassungen zustellt (AP-04 §6.2, E4; AP-05 E5); kein gespeicherter Wert ändert sich. Nur verkürzt, nie verlängert oder umgeschrieben (Trigger), kein DELETE; `→ geraet`/`measurement_point` CASCADE, `→ tenant` RESTRICT. Schnittstelle `GET/POST /api/v1/geraete/{id}/einstellungen` (`QuelleEinstellungService`), Protokoll an jeder gespeisten Messstelle als `messstelle_aenderung` Art `einstellung_geaendert`; der Hebel „Auf ×10 stellen“ (PUT der Komponente) schreibt eine Fassung „gültig ab jetzt“. |
| `telemetry_v2` (`MIG/V20260718010000__telemetry_v2.sql:27-36`) + Rollups | Kern-Telemetrie | **bleibt** | Der Betriebs-Pfad: Cockpit, Fahrplan, Regelung und Erlöse lesen sie wie heute. Sie trägt **keine** Messstellen-Reihe (AP-07 E1). |
| `device_measurement_sample` (`MIG/V20260848000000__additional_measurement_pipeline.sql:27-55`) | Messreihe (Rohwert) | **bekommt Verweis** + **Felder ziehen um** | ⚠ Der Schlüssel der Reihe wird **Komponente + Messkanal** statt Gerät + `point_key`: die Tabelle bekommt die Schlüsselspalte `entity_id`, Herkunftsspalten (Gerät + Einbau, lesende Box, Einstellungs-Fassung, Katalogstand, Rolle führend \| vergleich \| spiegel) und einen neuen Unique-Index. Das **ersetzt** AP-00 §6.4 „die Reihe bleibt am Gerät geschlüsselt“ (AP-07 E2, W1). |
| — | Verdichtete Messreihen | **neu daneben** | Hypertables `messreihe_viertelstunde` (Chunk 30 d, Retention 3 653 d) und `messreihe_tag` (Chunk 1 Jahr, Retention 3 653 d) — zehn Jahre für JEDEN Messkanal, eine Regel ohne Sonderfall; RLS, ohne Kompression, Kompressions-Layout vorbereitet (AP-07 E6/E7). |
| `messreihe_ereignis` (`MIG/V20260911260000__uems_messreihe_ereignis.sql:254-320`, Vokabular `:68-189`) | Ereignisse | **neu daneben** (gebaut, AP-07 IP-8) | Je Mandant, **append-only und nie gelöscht**: keine Retention, keine Kompression (RLS, E7), KEIN Fremdschlüssel auf Anlage/Box/Komponente/Datenquelle/Messstelle — Unclaim, Purge und Anlage-Löschen lassen die Zeilen stehen, nur das Offboarding räumt (`tenant` RESTRICT, E8); App-Rolle nur SELECT + INSERT, ein Trigger verbietet UPDATE. EINE Zeile je Meldung — ein offenes Ereignis wird mit derselben `ereignis_id` fortgeschrieben (Vertrag §3), Idempotenz über den Fingerabdruck `meldung`. Art, Urheber je Art, Zeitform, Bezug und Nutzlast-Schlüssel prüfen CHECKs aus `messreihe_ereignis_vokabular()` — dieselben Fakten wie `EreignisVokabular` und `events-vocabulary-vectors.json` (E11). Bezug wörtlich (`kennungen`) und eindeutig aufgelöst (`device_id`, `data_source_id`, `entity_id` + `messkanal`, `messstelle_id`). Der Writer spiegelt jedes neue `device_measurement_event` in derselben Transaktion (`aus_bestand`: Box + Punkt, keine Komponente) und hängt `events.raw` an; `device_measurement_event` bleibt unverändert, bis der Lesepfad umzieht (IP-14). |
| `device_measurement_selection` (`MIG/V20260841000000__device_measurement_selection.sql:38-39`, je Komponente `MIG/V20260855000000__measurement_selection_per_component.sql:44-45`) | Mess-Selektion | **bleibt** | Sie bleibt die Wahrheit, WELCHE Kanäle die Box liest; die Messstelle bindet einen davon (AP-04 §6.2). Der Push wird je Box aus den Zuständigkeiten zusammengesetzt — **ohne** neue Felder im Mess-Plan, weil alte Boxen unbekannte Felder ablehnen (AP-06 E4/E9). |
| `entity_registry_state` (`MIG/V20260719030000__entity_sync_state.sql:24-27`) | Soll-Zustand je Box | **Felder ziehen um** | Der Soll-Zustand wird je **(Anlage, Box)** geführt statt je Anlage, und je Box eine disjunkte Menge; optional `data_source_id` je Entität, die eine alte Box überliest. Ein Zuständigkeitswechsel sind zwei Pushes — erst die alte Box ohne die Quelle, dann die neue mit ihr (AP-06 E4/E13). |
| `entity_role_assignment` (`MIG/V20260719040000__entity_role_assignment.sql`) | Rolle im Energiefluss | **bleibt** | `is_primary` bleibt der Vorschlag für die Hauptzähler-Quelle; bei Abweichung erscheint ein Hinweis (AP-04 W7). |
| `device_control_activation` (`MIG/V20260814000000__inverter_control_certification.sql:99-111`) | Steuer-Freigabe | **bleibt** | Die Freigabe je Komponente bleibt der eigene Schritt; sie zieht bei einem Box-Tausch mit (AP-06 E7). |
| `site_profile_state` (`MIG/V20260723000000__site_profile_state.sql:26-36`), `flow_definition` (`MIG/V20260719000000__flow_definition.sql:31`), `consumer_policy` (`MIG/V20260810000000__consumer_profile_and_policy.sql:94`), `site_charging_config` (`MIG/V20260829000000__site_charging_config.sql`) | Betrieb (Betriebsmodell, Regel, Steuerart, Ladepark) | **bleibt** | Unverändert. Die Ruhe einer Anlage ist der heutige Pause-Zustand ohne Enddatum: alles bleibt gespeichert, nur Fahrplan, Regeln und Steuerarten wirken nicht (AP-01 E7/E8). |
| `ort_aenderung` (`MIG/V20260911100000__uems_unternehmen_standort.sql:262-283`) | Änderungsprotokoll der Ortsstruktur | **neu daneben** (gebaut, AP-02 IP-2a) | Append-only per Trigger `reject_audit_mutation` wie `component_change_event`, ohne Fremdschlüssel (das Protokoll überlebt sein Objekt und das Offboarding); Objekt, Art (geschlossen), alt/neu als JSON, gilt ab, rückwirkend, Akteur `sub` + Anzeigename. Der Backfill schreibt je Unternehmen einen Eintrag „angelegt“ durch „VoltPilot (Bestandsübernahme)“. |
| `messstelle_aenderung` (`MIG/V20260911140000__uems_messstelle.sql:224-254`) | Änderungsprotokoll der Messstelle | **neu daneben** (gebaut, AP-04 IP-2) | Wie `ort_aenderung`: append-only per Trigger `reject_audit_mutation`, ohne Fremdschlüssel (überlebt Objekt und Offboarding); Art (geschlossen), alt/neu als JSON, gilt ab auf die Minute (AP-04 E2), rückwirkend, Grund, Urheber im Akteur-Vokabular von AP-03 (`actor_sub`, `actor_name`, `actor_rolle` = Rollen-Kennung aus `docs/contracts/v2/rechte-matrix.json`, `actor_art`). |
| `prozess`, `messstelle_prozess` (`MIG/V20260913160000__uems_kostenstelle_prozess.sql`) | Prozess · Zuordnung Messstelle → Prozess | **bleibt** (AP-16 E1 = A, W2) | Der Prozess bleibt, was er ist; ein Energieeinsatz verweist auf ihn. Die Zuordnung bleibt ohne Anteil — der 70/30-Anteil von P-3 lebt an der Kostenstellen-Verteilung, die Bewertung zählt MS-07 einmal (E4 = A, B3). |
| — (`MIG/V20260922210000__uems_energieeinsatz.sql`, `…237000__uems_energieeinsatz_einstufung.sql`) | Energieeinsatz · Einflussgröße · Einstufungs-Fassung | **neu daneben** (gebaut, AP-16 IP-3/IP-11) | `energieeinsatz` (Prozess × Träger, EE-Zähler), `energieeinsatz_einflussgroesse`, `energieeinsatz_einstufung`, Protokoll `energieeinsatz_aenderung`; RLS + FORCE. Bei einem Kunden ohne Einsatz leer (R11, `UemsBewertungBestandsschutzTest`). |
| — (`MIG/V20260922220000__uems_bewertung_umfang.sql`, `…230000__uems_bewertung_kriterien_fassung.sql`, `…246000__uems_messbedarf.sql`, `…251700__uems_vergleich_toleranz.sql`) | Betrachtungsumfang · Kriterien-Fassung · Messbedarf · Toleranz | **neu daneben** (gebaut, AP-16 IP-5/IP-8/IP-17/IP-19) | `bewertung_umfang*`, `bewertung_kriterien_fassung`, Protokoll `bewertung_aenderung`, `messbedarf` + `messbedarf_aenderung`, `vergleich_toleranz` (Startwert 2 % nie gespeichert). |
| `geraet` (`MIG/V20260922245000__uems_messmittel_angaben.sql`) | Messmittel-Angabe am Einbau | **bleibt** + nullable Spalten (gebaut, AP-16 IP-15) | Klasse, Prüfung, gültig bis, Beleg als Verweis mit SHA-256; „nicht erhoben“ = NULL, nie ein Wert. Journal `geraet_aenderung` (siehe nächste Zeile). |
| `bericht` (`MIG/V20260922251800__uems_energetische_bewertung.sql`) | Bewertungsstand | **bleibt** + fünfte Vorlage (gebaut, AP-16 IP-21) | Vorlage `energetische_bewertung` in `bericht_vorlage()` (Funktion, keine Zeile); `wiedervorlage_monate NOT NULL DEFAULT 12` steht in jeder Bestandszeile, wird aber nur für diese Vorlage gelesen und ausgegeben. |
| — (`MIG/V20260924071500__uems_bezugsbasis.sql`, `…200500__uems_bezugsbasis_anstoss.sql`) | Bezugsbasis · Fassung · Einflussgröße · statischer Faktor · Anstoß | **neu daneben** (gebaut, AP-17 IP-6/IP-7/IP-8/IP-15/IP-16) | `bezugsbasis` (BB-Zähler `bezugsbasis_kennzeichen_seq`), `bezugsbasis_fassung` (eingefroren nach Freigabe), `bezugsbasis_variable`, `bezugsbasis_faktor`, `bezugsbasis_anstoss`, Protokoll `bezugsbasis_aenderung`, Wasserzeichen `bezugsbasis_struktur_gelesen`; RLS + FORCE. An `kennzahl*` ändert sich nichts (Invariante 1). Bei einem Kunden ohne Bezugsbasis leer (R10, `UemsBezugsbasisBestandsschutzTest`). |
| `bezugsgroesse_wert` (`MIG/V20260924192700__uems_wetter_archiv_bezug.sql`) | Wetterbezug einer Gradtagzahl | **bleibt** + nullable Spalten, **neu daneben** (gebaut, AP-17 IP-12b/c) | `bezug_quelle`, `abgerufen_am`, `bezug_herkunft` nur an bezogenen Fassungen (Herkunft `bezogen`), in jeder Bestandszeile NULL; neue Tabelle `bezugsgroesse_wetterbezug` (leer ohne Bindung). Kein Kalender, kein Arbeitszeitmodell (W2, E10 = A). |
| `bericht` (`MIG/V20260924071945__uems_leistungsvergleich_vorlage.sql`, `…211800__uems_leistungsvergleich_kennzahl.sql`) | Leistungsvergleich als Bericht | **bleibt** + sechste Vorlage, nullable Spalte (gebaut, AP-17 IP-21a/b) | Vorlage `leistungsvergleich` in `bericht_vorlage()` (Funktion, keine Zeile), Quellenart `bezugsbasis`; `bericht.kennzahl_id` nur für diese Vorlage gesetzt, in jeder Bestandszeile NULL. |
| — (`MIG/V20260924223000__uems_verbesserung.sql`, `…233000__uems_massnahme.sql`, `…235130__uems_abweichung.sql`, `V20260925001000__uems_verbesserung_admin_anstoss.sql`, `…002000__uems_auffaelligkeit_admin.sql`) | Energieziel · Maßnahme · Abweichung · Auffälligkeit · Anstoß am Vorgang | **neu daneben** (gebaut, AP-18 IP-5/IP-9/IP-14, Naht IP-15/IP-17) | `energieziel`, `massnahme`, `abweichung` (Zähler `verbesserung_kennung_seq` für EZ/M/AW je Kundenbereich, Art und Jahr), Protokolle `energieziel_aenderung`, `massnahme_aenderung`, `abweichung_aenderung` (Ursache-Aussage als Eintrag einer Person), Stände `massnahme_bewertung`, Vermerke `auffaelligkeit` und `vorgang_anstoss`; Kopien mit Prüfsumme, Verantwortliche per Fremdschlüssel auf `benutzer` plus Schnappschuss; RLS + FORCE, Wörter in der eigenen Funktion `verbesserung_vokabular()`. An `kennzahl*`, `bezugsbasis*` und `bericht` ändert sich nichts — keine Spalte, kein Wort. Bei einem Kunden ohne Vorgang leer; die Naht schreibt nur an einer Energieleistungskennzahl (R13, `UemsVerbesserungBestandsschutzTest`). |
| — (`MIG/V20260925013500__uems_energiemanagement.sql`, `…031500__uems_audit_feststellung.sql`, `…093000__uems_managementbewertung_beschluesse.sql`; an fremden Tabellen `…030000__uems_rolle_einsicht.sql`, `…040000__uems_massnahme_herkunft_weiten.sql`, `…061500__uems_managementbewertung_vorlage.sql`) | Person · Aufgabe · Dokument · Fassung · Anwendungsbereich · internes Audit · Feststellung · Wirksamkeit · Rolle „Einsicht“ · Managementbewertung mit Sitzung, Beschluss, Folge | **neu daneben** (gebaut, AP-19 IP-5/IP-16/IP-23), an drei fremden Stellen **geweitet** (IP-12, IP-17, IP-22) | `energiemanagement_person`, `…_aufgabe`, `…_dokument`, `…_dokument_fassung`, `…_dokument_eintrag`, `…_anwendungsbereich`, `…_einstellung`, Protokoll `…_aenderung`, Zähler `energiemanagement_kennung_seq` (D ohne Jahr, AU/F je Jahr), `internes_audit`, `internes_audit_eintrag`, `feststellung`, `feststellung_eintrag`, Stände `feststellung_wirksamkeit`, `managementbewertung_sitzung`, `…_beschluss`, `…_folge` (am Bericht der Vorlage Nr. 7, ohne Fremdschlüssel auf `bericht`); RLS + FORCE, Wörter in der eigenen Funktion `energiemanagement_vokabular()`, keine Datei (Verweis mit Prüfsumme, E2 = A). Geweitet als Vereinigung, keine Bestandszeile geändert: `zugriff_rolle()` + CHECK-Tausch `bericht_abruf_actor_rolle_chk` (Einsicht), `verbesserung_vokabular()` + CHECK-Tausch `massnahme_herkunft_chk` (Feststellung, internes Audit, Managementbewertung), `bericht_vokabular()`/`bericht_vorlage()` (Vorlage Nr. 7, acht Quellenarten). Bei einem Kunden ohne Eintrag leer; kein Schalter, kein Läufer (R15, `UemsEnergiemanagementBestandsschutzTest`). |
| — | Weitere Änderungsprotokolle | **neu daneben** | `geraet_aenderung` — append-only nach dem Muster `messstelle_aenderung` bzw. `component_change_event` mit Akteur-Anzeigename wie `device_measurement_selection_event` (`MIG/V20260841000000__device_measurement_selection.sql:87-88`); rückwirkende Änderungen tragen ein Kennzeichen (AP-02 E2, §6.2). |

Jede neue mandantengebundene Tabelle braucht in ihrer eigenen Migration: `tenant_id`, Policy
mit `USING` **und** `WITH CHECK`, `ENABLE` + `FORCE ROW LEVEL SECURITY`, Grants an
`voltpilot_app` (bei `BIGSERIAL` ein eigenes `GRANT USAGE ON SEQUENCE`) und CHECKs für
Vokabulare und Namenslängen — Muster
`MIG/V20260866000000__fahrzeug_profile.sql:38-89`. Überlappungsverbote auf
Gültigkeitsintervallen sind Exklusions-Constraints.

## Rechte

| Heute | Zielobjekt | Art | Was genau |
|---|---|---|---|
| Keycloak-Realm `voltpilot` (`infra/local/keycloak/voltpilot-realm.json:29-45`) | Identität | **bleibt** | Keycloak bleibt Identität (`tenant_id`, `platform-admin`, neu `partner`). |
| — | Rechte-Zuweisung | **neu daneben** | Eine Zuweisungstabelle im API: Benutzer × Rolle × Standort × Gültigkeit, unter RLS, dazu `/me`. Das API prüft je Anfrage (AP-03 E11). |
| Realm-Rolle `site-admin` (`infra/local/keycloak/voltpilot-realm.json:37`, geprüft in `services/api/src/main/java/com/voltpilot/api/web/SiteOcppControlController.java:51`) | — | **Felder ziehen um** | ⚠ Namensfalle (AP-00 W3): die Rolle hat heute **keinen** Anlagen- oder Standortbezug. Sie wird abgeschafft — OCPP-Stufen hängen künftig an der Zuweisung, bestehende Träger werden Kundenadministrator; die Realm-Rollen bleiben als Leichen bis zur nächsten Realm-Pflege (AP-03 E13/E12). |
| Benutzerverwaltung nur durch Plattform-Admin (`services/api/src/main/java/com/voltpilot/api/web/AdminController.java:238-323`) | Kundenadministrator | **neu daneben** | Benutzer, Rollen und Unterstützungen verwaltet der Kundenadministrator selbst; das Startpasswort vergibt er wie heute (AP-03 E2/E14). |

## Portal

| Heute | Zielobjekt | Art | Was genau |
|---|---|---|---|
| `PORTAL/nav.ts:280-286`, `:504`, `:612-641`, `:881` | Navigation | **bekommt Verweis** | Über der Anlage entstehen die Ebenen Unternehmen und Standort; das Portfolio-Cockpit bleibt und wird die Unternehmens-Übersicht. Die Startebene ist die tiefste, die alles zeigt: 1 Anlage → Cockpit, 1 Standort mit n Anlagen → Standort-Übersicht, n Standorte → Unternehmens-Übersicht (AP-01 E1/E2). Die Anlagen-Adressen `#/anlage/{siteId}/…` bleiben unverändert. |
| `PORTAL/ebenenNav.ts:16-17`, `:245` | Anlagen-Bereiche | **bleibt** | Die fünf Bereiche Cockpit · Fahrplan · Verlauf · Steuerung · Anlage bleiben, wie sie sind. Neu ist nur der Rückweg: der Pfad im Seitenkopf wird dreigliedrig (Unternehmen → Standort → Anlage), es entsteht **kein** neuer Platz in der Leiste (AP-01 E3/E4). |
| `PORTAL/pages/AnlageTechnik.tsx:508`, `:1207` | Feld „Standort“ (Koordinaten) | **bleibt** | Behält seinen Namen: beide Bedeutungen bleiben, die Unterscheidung leistet der Kontext der Fläche — heute schon „Standort auf der Karte“ (AP-00 E9 = B; IP-5 entfällt). |
| `PORTAL/copy.test.ts:77-80`, `:103-113`, `PORTAL/glossar.ts` | Kundensprache | **bleibt** (dieser PR) | Die neuen Kundenwörter (Unternehmen, Standort, Gebäude, Bereich, Netzanschluss, Messstelle; „Datenquelle“ nur auf Einrichtungsflächen) und die verbotenen internen Wörter kommen in AP-00 **IP-4** dazu — nicht hier. Diese Karte ändert keine Sprachregel. |
| `PORTAL/komponenten.ts:754-766`, `:865-877`, `:306-307` | Zustandswörter | **bleibt** | „Liefert Daten / Meldet sich gerade nicht / Wartet auf die ersten Daten“, „Verbunden“ und „Wird von VoltPilot gesteuert“ bleiben die Kundenwörter des gemeinsamen Zustandsvokabulars ([`zustaende.md`](zustaende.md)). Die harte 5-Minuten-Ableitung wird erst mit AP-00 IP-3 durch die zeitgültige Kadenz ersetzt (AP-07 E9). |
| `PORTAL/betriebsmodelle.ts:110-137`, `PORTAL/components/Betriebsmodelle.tsx:85` | Betriebsmodell | **bleibt** | Radiogruppe und „läuft seit …“ bleiben; „Marktoptimierung“ bleibt das Kundenwort, nicht „Arbitrage“ (AP-01 E11). |

## Verträge und Dienste

| Heute | Zielobjekt | Art | Was genau |
|---|---|---|---|
| MQTT-Topics `ems/{tenant}/{site}/{device}/…` (`docs/architecture.md:90-94`) | Erfassungsweg | **bleibt** | Keine Änderung. Die Box kennt keine Standorte und keine Messstellen; die Zuständigkeit je Datenquelle ist ein Cloud-Konzept, das in die vorhandene Entity- und Measurement-Config übersetzt wird (AP-00 §6.1, AP-06 E4). |
| `docs/contracts/v2/mqtt-telemetry-2.0.schema.json:8` | Kern-Telemetrie | **bleibt** | Unverändert; `seq` wird weitergereicht (AP-07 §6.1). |
| `docs/contracts/v2/edge-entity.schema.json:296`, `:348-353`, `:365-367` | Entity-Push | **bekommt Verweis** | Additiv `data_source_id` je Entität — eine ältere Box überliest das Feld (AP-06 §6, Vertrags-Hausregel „Verträge sind additiv“). |
| `docs/contracts/v2/topology-read-model.md:14-26` | Energiefluss-Read-Model | **bleibt** | Rolle `grid` bleibt „die maßgebliche Messung, nie eine Summe“; das elektrische System bleibt implizit die Anlage mit ihrem einen maßgeblichen Netzpunkt (AP-00 E1/E6). |
| — | Referenzunternehmen, Zustands-Vektoren | **neu daneben** | `docs/contracts/v2/uems-referenzunternehmen.json` (AP-00 IP-2) und `docs/contracts/v2/uems-zustand-vectors.json` (IP-3) als geteilte Vektor-Dateien mit Zwillingstests in `services/api` und im Portal. **Nicht Teil dieses PR.** |
| `GET /api/v1/overview`, `GET /api/v1/sites/{id}` (`docs/contracts/openapi.yaml`) | Standort je Anlage | **bekommt Verweis** (gebaut, AP-02 IP-3) | Additiv `standort {id, name, kurzzeichen, gueltigAb}` — der Standort der Anlage heute, `null` solange sie keinem zugeordnet ist (bis IP-9 jede Bestandsanlage); jedes bestehende Feld bleibt zeichengleich. `GET /api/v1/sites/{id}` gab es vorher nicht: er liefert die Listen-Zeile plus `standort`. Daneben neu: `GET /api/v1/unternehmen` und `GET /api/v1/standorte?stichtag=` (`services/api/src/main/java/com/voltpilot/api/uems/StandortLesemodell.java` über `OrtsbaumAbleitung.standAm`) — die Grundlage der Startansicht-Weiche AP-01 IP-5. |
| `services/ingest`, `services/timescale-writer` | Messwert-Strecke | **bekommt Verweis** | Ingest: Messzeit-Plausibilität, `seq` weiterreichen, je Sample statt je Batch verwerfen, Ablehnungen als Ereignis, neues Topic `events.raw`. Writer: Herkunfts-Anreicherung, Idempotenz nach Reihe + Messzeit, Zuständigkeitsprüfung zur Messzeit, Verdichtung (AP-07 E3/E4/E5). |
| `services/api/src/main/java/com/voltpilot/api/repo/OverviewRepository.java:29-31`, `services/api/src/main/java/com/voltpilot/api/repo/AdminFleetRepository.java:50` | „liefert Daten“ | **Felder ziehen um** | Das harte 5-Minuten-Fenster wird durch die zeitgültige Kadenz der Quellenbindung ersetzt (Lücke ab 2 × Kadenz); Ableitung als geteilte reine Funktion mit Vektoren (AP-07 E9, AP-00 IP-3). |
| `infra/local/timescale/*.sql`, `infra/local/keycloak/voltpilot-realm.json` | Dev-Seed „Ahrenberg“ | **neu daneben** | Ein dritter Mandant mit den drei Anlagen, drei Boxen und den Komponenten des Referenzunternehmens (AP-00 IP-7, erweitert um Ortsstruktur in AP-02 IP-16). **Nicht Teil dieses PR.** |

## Was diese Karte NICHT sagt

- **Kein Schema.** Spaltennamen und -typen oben sind Absicht, nicht Vertrag; die Migration
  jedes Bau-Pakets entscheidet sie und ist danach unveränderlich.
- **Keine Reihenfolge.** Die Bau-Reihenfolge steht im Programm-Plan, nicht hier.
- **Keine Doppelpflege.** Wo „Felder ziehen um“ steht, gibt es genau EINEN Zeitpunkt, ab dem
  das neue Objekt die Wahrheit trägt; bis dahin bleibt die alte Spalte die Wahrheit. Zwei
  gleichzeitig gepflegte Orte für dieselbe Zahl sind ein Fehler, keine Übergangslösung.
