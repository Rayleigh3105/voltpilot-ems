openapi: 3.1.0
info:
  title: Voltpilot-EMS Portal API
  version: 0.1.0-mvp
  description: >
    Vertrag der Portal-API (Spring Boot): Anlagen, Geräte, Messwerte,
    Planung, Verbrauchersteuerung und Administration. Implementierungsstand
    und Betriebsregeln stehen in docs/api.md.

    Standardmäßig ist ein Bearer-JWT aus dem Realm voltpilot erforderlich.
    Öffentliche Routen wie Registrierung und Enrollment weisen security: []
    aus. Mandantenrouten verwenden tenant_id und Postgres Row-Level-Security;
    Plattformverwaltung besitzt gesonderte Rollen und Datenzugriffe.
  contact:
    name: Voltpilot-EMS
servers:
  - url: http://localhost:8090
    description: Local dev (docker compose / `mvn spring-boot:run`)
security:
  - bearerAuth: []
tags:
  - name: system
  - name: registration
    description: >
      Public self-service registration: a new customer creates their own tenant
      and login in one step (no operator involved). Unauthenticated by design;
      can be disabled platform-wide via VOLTPILOT_REGISTRATION_ENABLED=false.
  - name: sites
  - name: ortsstruktur
    description: >
      UEMS-Ortsstruktur (AP-02): das Unternehmen des Kundenbereichs und seine
      Standorte zum Stichtag. Mandantengebunden wie /api/v1/sites/** (RLS ist
      der Zaun; ein fremdes Objekt ist 404, nie 403; Admins über X-Tenant-Id).
      Lesend seit IP-3; die Schreibrouten folgen mit IP-4/IP-5.
  - name: devices
  - name: enrollment
    description: >
      First-boot device enrollment over HTTPS (the "kinderleicht" mTLS
      onboarding): a fresh edge device knows only its printed reference and the
      portal URL. It generates a keypair ON the device, uploads the CSR (the
      private key never travels), and polls for its certificate. Once the
      customer claims the reference in the portal, the poll returns a
      device-CA-signed client certificate - identity ENFORCED from the claim
      (CN=device_id, O=tenant_id, OU=site_id + SPIFFE URI SAN), never from the
      CSR - plus the CA and broker connection params for the mTLS listener
      (8883). Unauthenticated by design and strictly rate-limited; a pending
      reference answers exactly like an unknown one, so polling cannot
      enumerate refs. Polling etiquette for device agents: start at ~10 s
      intervals, back off to >= 60 s after the first minutes, and keep polling
      indefinitely (claiming may happen days after first boot); on 429 back
      off harder (>= 5 min). Disabled unless the deployment stages the device
      CA (VOLTPILOT_ENROLLMENT_ENABLED; see docker-compose.prod.yml).
  - name: measurements
    description: >
      Zusätzliche Messwerte eines Geräts: der gewünschte, revisionierte
      Pollplan, sein Katalog, seine Budget-Vorschau und der Verlauf je Punkt.
      Mandanten-Zaun ist der normale Geräte-/RLS-Pfad (fremdes Gerät = 404),
      Plattform-Admins nutzen den X-Tenant-Id-Umschalter.
      Seit Geräteseite Stufe 3b trägt jede Auswahl optional die KOMPONENTE,
      der sie gehört (entityId). Ohne den Parameter antwortet jede Route
      Zeichen für Zeichen wie vorher: die Auswahl gehört dann dem Gerät als
      Ganzem. Zwei baugleiche Wechselrichter hinter EINER Box führen damit
      getrennte Listen - während Revision und Budget geräteweit bleiben (es
      gibt EIN veröffentlichtes Plan-Dokument und EINEN physischen Bus).
  - name: telemetry
  - name: market
  - name: weather
  - name: schedules
  - name: history
  - name: registry
    description: >
      Optional MaStR "Anlage verknüpfen" step: look a unit up in the German
      Marktstammdatenregister (preview only), then persist the
      customer-confirmed values onto the site's assets. Tenant-scoped like all
      site endpoints (foreign site = 404 via RLS).
  - name: kpis
  - name: rules
    description: >
      Das Regel-Protokoll (Einheitsmodell Stufe 5b): der Verlaufsspeicher der
      Regeln einer Anlage. Die drei Momentaufnahmen-Tabellen des Herzschlags
      werden je Takt ersetzt - dieser Speicher haelt die WECHSEL fest und
      beantwortet damit "diese Regel hat heute 3x geschaltet". Kundendaten,
      also mandantenbezogen (RLS) und unter /sites/**, nicht unter /admin/**.
  - name: commands
    description: >
      Der Kommando-Verlauf (Kommando-Transparenz V1, Konzept
      vp-kommando-transparenz-k3): "was schickt VoltPilot wirklich an meine
      Geraete?". Der Verlaufsspeicher haelt HALTEPERIODEN + Punkt-Ereignisse
      fest, die die drei Momentaufnahmen-Tabellen des Herzschlags je Takt
      wegwerfen - abgeleitet AUSSCHLIESSLICH aus den bestehenden Stroemen,
      ohne eine einzige Edge-Aenderung. READ-ONLY: es entsteht kein
      Schreibpfad zu einem Geraet. Kundendaten, also mandantenbezogen (RLS)
      und unter /sites/**, nicht unter /admin/**.
  - name: register-write
    description: >
      Einmaliges Beschreiben eines Geraete-Registers ueber das Portal (Konzept
      vp-reg-schreib-konzept-p8, Stufen 1-3): erst den
      Ist-Wert LESEN (die Vorschau ueber DIESELBE Lane, ueber die geschrieben
      wuerde - sie beweist damit, dass der Weg offen ist), dann EINMAL
      schreiben, danach ein Beleg mit Rueckleseprobe. Jeder Schreibvorgang
      hinterlaesst eine append-only Papier-Spur (register_write_event) mit
      beweisbarer Herkunft und den VERBATIM getippten Begriffen.
      WELCHES Register eine Box ausfuehrt, entscheidet die BOX (seit Stufe 2
      ueber LANE-Regeln statt einer Adress-Allowlist) - die Cloud entscheidet
      nur, WER welche Anlage erreicht. Kundendaten, also mandantenbezogen (RLS)
      und unter /sites/**, nicht unter /admin/**.
      Seit Stufe 3 "Bis zum Endkunden" faehrt der ENDKUNDE dieselbe Strecke auf
      seiner eigenen Anlage (Reichweite = RLS, Herkunft `kunde` im Journal); die
      Rollen unterscheiden nur die Reichweite, nie die Register. Der
      plattformweite NOT-AUS ist voltpilot.register-write.enabled: auf false
      refuesieren die zwei SCHREIBENDEN Routen mit 503 und deutschem Grund,
      waehrend Ziel-Liste, Register-Wissen und VERLAUF lesbar bleiben.
  - name: topology
    description: >
      Anlagen-Topologie-Read-Model (AE1, adaptive EMS-UI): the site as an
      entity graph aggregated into the role-grouped hub topology
      (PV-Erzeugung / Speicher / Netz / Verbraucher) + directed flows the
      adaptive energy-flow diagram renders. Each capability is assignable to a
      role (customer/admin); the maßgebliche grid measurement is markable.
      Tenant-scoped like every site endpoint (foreign site = 404 via RLS); the
      shared derivation is the Go/TS/Java twin
      (docs/contracts/v2/topology-read-model.md). Role ASSIGNMENT is
      platform-admin-only.
  - name: components
    description: >
      Der EINE Anlege-Weg (Einheitsmodell Stufe 1): a component is a TEMPLATE
      plus its connection plus a role, saved versioned on the customer's own
      plant. Tenant-fenced by RLS like every /sites/** route (foreign site =>
      404, never 403); admins reach any plant through the X-Tenant-Id switcher.
  - name: templates
    description: >
      Komponenten-VORLAGEN (Einheitsmodell Stufe 0a, "Vorlagen werden Daten"):
      the device catalog the LATER one-assistant renders its brand/model choice
      from. Read-only in this stage and consumed by NOTHING yet - the surfaces
      still render the edge-side Go catalog. Authenticated + tenant-agnostic
      (the register is global platform data like edge_release /
      provisioned_device, no tenant_id, no RLS); the fence is the ORIGIN filter -
      only `builtin` + `certified` are served, never a `custom` (private
      per-Anlage) template, which has no tenant fence until Stufe 3. Managing
      templates (create/version) is Stufe 6 and does not exist here.
  - name: profile
    description: >
      Nutzungsprofil / EMS-Modus (AE7, the second adaptation axis): the usage
      profile (arbitrage | peak | private) DERIVED from the site's strategy nodes
      + entity mix + money master data, driving the adaptive portal/edge EMPHASIS
      (money/peak/flow/devices). Explicitly overridable; ONE truth - not a
      competing concept (docs/contracts/v2/usage-profile.md). Tenant-scoped.
  - name: simulation
    description: >
      Ersparnis-Simulation: asynchronous what-if jobs over a full historical
      reference year (3 scenarios - ohne Speicher / Standard-Speicher /
      VoltPilot - plus a battery-size sweep and the Netzladen variant),
      computed by the internal simulation service with the UNCHANGED
      production optimizer. The customer route is tenant-scoped like every
      site endpoint (defaults from the site's master data, body fields
      override them, foreign site = 404 via RLS); the prospect route is
      platform-admin-only with explicit inputs and persists nothing (V1 is
      stateless - results live in the service's job cache for ~1 h).
  - name: consumer
    description: >
      Steuerbare Verbraucher (docs/verbrauchssteuerung.md): a Portal-User models,
      names and rule-configures a controllable consumer (Wallbox / Heizstab /
      Pumpe / generische Last) of THEIR OWN site. A consumer STAYS a v2 entity;
      these routes carry the extra master data (consumer_profile) + the
      declarative operating requirements (consumer_policy, contract
      docs/contracts/v2/consumer-policy.schema.json). Tenant-scoped like every
      site endpoint (foreign site = 404 via RLS; a body tenant_id/site_id is
      never trusted; the requested control kind is mapped onto the INTERSECTION
      of the device's reported capabilities, never widened). Increment 1 ships
      consumer-options + CRUD + policy GET/PUT only; a policy is stored as a
      DRAFT (no compiler / optimizer / edge command yet, so every surface reports
      "Steuerung noch nicht aktiviert").
  - name: probe
    description: >
      Probe-Kanal (Einheitsmodell Stufe 0b): a one-shot "read these registers on
      my plant, once" - the groundwork every live preview and connection test of
      the later component assistant will run on. The route publishes a
      NON-retained request on the device's own v2/probe topic (contract
      docs/contracts/mqtt-probe.schema.json), waits a few seconds for the
      answer on v2/probe-result and persists NOTHING. Tenant-scoped like every
      site endpoint (foreign site = 404 via RLS). It is a READ: the contract's
      reserved switch_test op belongs to the later release wizard and is
      refused by the box itself with an honest not_supported. Every rule that
      can stop a device from being touched - private target, expiry, rate
      limit - lives ON THE BOX, which is the only party that knows its own LAN.
  - name: flows
    description: >
      Customer flow editor (E3b "Kunden-Freigabe"): a Portal-User builds,
      validates, simulates, activates and deactivates the flows of THEIR OWN
      sites (tenant from the JWT, RLS-scoped; a Portal-Admin reaches any site via
      the X-Tenant-Id switcher). The lifecycle + gates are identical to the admin
      flow API (draft -> simulated -> active -> retired) and never relaxed for a
      customer: a flow carrying a GATED strategy node activates only after a
      Portal-Admin enables that node type for the site. Governance WRITE and the
      auto-start seed stay platform-admin-only (the admin flow API).
  - name: messstellen
    description: >
      UEMS-Messstellen (AP-04 IP-3): die logische Messstelle des Kundenbereichs
      anlegen, lesen, bearbeiten, anhalten, fortsetzen und archivieren. Die Regeln
      sind der Vertrag docs/contracts/v2/messstelle.md (Java-Zwilling
      MessstelleRegeln); die Antwort IST eine Messstelle nach messstelle.schema.json
      - deshalb, anders als der Rest dieser API, in snake_case - plus `id`,
      `fehlt`, `angehalten_ab`, `archiviert_am`. Mandanten-Zaun ist die RLS
      (fremde Messstelle = 404, nie 403; Plattform-Admins nutzen X-Tenant-Id). Die
      Durchsetzung der Rechte-Matrix (docs/contracts/v2/rechte-matrix.json:
      `messstelle.bearbeiten`, `messwerte.ansehen`, `aenderung.rueckwirkend`) kommt
      mit AP-03; bis dahin gilt authenticated(). Jeder Schreibvorgang schreibt
      GENAU EINEN Eintrag in das Änderungsprotokoll (messstelle_aenderung) mit
      Urheber. Ort, Stellung, Prozesse, Kostenstellen (IP-7) und Quellen (IP-13)
      kommen additiv; bis dahin sind diese Listen leer und eine gemessene
      Messstelle ehrlich ein Entwurf (`fehlt: [ort]`).
  - name: datenquellen
    description: >
      UEMS-Datenquellen (AP-06 IP-3): der Erfassungsweg einer Anlage (Protokoll +
      Adresse, Kennzeichen DQ-n) anlegen, bearbeiten, von GENAU der gewählten Box
      prüfen und ihr ab einem Zeitpunkt zuweisen - mit Protokoll und Urheber je
      Quelle. Die Regeln sind der Vertrag docs/contracts/v2/data-source-assignment.md
      (Java-Zwilling DatenquelleRegeln; kein zweites Regelwerk im Dienst), die Wörter
      stehen in seiner Schreibweise (snake_case) wie bei den Messstellen.
      Zuständigkeiten sind halboffen auf die volle Minute (`effective_to` gehört
      nicht dazu), nie rückwirkend, nie überschrieben: ein Wechsel beendet den
      laufenden Zeitraum und trägt den neuen ein, in EINER Transaktion. Eine
      Zuständigkeit braucht die bestandene Erreichbarkeitsprüfung von genau der
      Ziel-Box - das Ergebnis steht im Protokoll, nie in der Anfrage. Mandanten-Zaun
      ist die RLS (fremde Anlage, Quelle oder Box = 404, nie 403; Plattform-Admins
      nutzen X-Tenant-Id). Rechte (AP-06 §4.8): `datenquelle.bearbeiten` /
      `datenquelle.zustaendigkeit` - noch NICHT in rechte-matrix.json, nächste
      Zeile dort `geraet.einrichten`; bis AP-03 durchsetzt, gilt authenticated().
      Push an die Box (IP-6), Übergabe zum Zeitpunkt (IP-7), Budget (IP-10) und die
      Bestands-Vorschlagsliste (IP-4) kommen mit ihren Paketen.
  - name: kennzahlen
    description: >
      UEMS-Kennzahlen (AP-11 IP-5): Menge je Bezugsgröße, Anteil, Zusammenfassung (Summe durch
      Summe) anlegen, lesen, Stammdaten ändern, archivieren und - nur ohne einen einzigen Wert
      und ohne lesende Kennzahl - löschen; die Berechnung ab einem Tag als Fassung n + 1 (auch
      rückwirkend) und die Fassung an einem Stichtag; die Vorschau prüft und rechnet die letzten
      drei Perioden in einer Nur-Lese-Transaktion. Regeln: docs/contracts/v2/kennzahl.md.
  - name: berichte
    description: >
      UEMS-Berichte (AP-12): die vier VoltPilot-Vorlagen mit Fassungsnummer und festen Abschnitten
      (IP-5, V2); seit IP-7 anlegen, lesen, Entwurf (D4), Vergleich (R1), Freigabe als Berichtsstand
      (F1–F5, byte-gleiche Kopie mit Prüfsumme), Stand lesen (Prüfsumme geprüft), Anstoß verwerfen,
      archivieren. Rechte DURCHGESETZT (E12): `bericht.standort_abrufen`,
      `bericht.standort_freigeben`, `bericht.unternehmen` über uems/BerichtRechte → RechteAbleitung;
      fremder Kundenbereich oder Standort 404 `nicht_gefunden`, fehlendes Recht 403 `recht_fehlt`, die
      Unterstützung bekommt nie Entwurf oder Stand. Regeln: docs/contracts/v2/bericht.md.
  - name: bezugsgroessen
    description: >
      UEMS-Bezugsgrößen (AP-09 IP-5): die Zahl, mit der Energie verglichen wird
      (Produktionsmenge, Gutteile, Betriebszeit, Ablesung …) anlegen, lesen, ändern,
      archivieren und - nur ohne einen einzigen Wert - löschen; dazu das Lesemodell der
      Werte mit ihren Fassungen und der Herkunft je Fassung. Die Regeln M1–M6 und der
      GESCHLOSSENE Satz der Ablehnungen (Code, Status, Kundensatz) stehen im Vertrag
      docs/contracts/v2/bezugsdaten-vectors.json, Block `verwalten` (Java-Zwilling
      uems/BezugsgroesseRegeln, TS bezugsgroesse.ts); snake_case wie der Vertrag.
      Mandanten-Zaun ist die RLS (fremd = 404 `nicht_gefunden`, nie 403). Rechte
      (AP-09 §4.11, W8): `bezugsgroesse.verwalten` für die Schreibwege,
      `messwerte.ansehen` fürs Lesen - noch nicht durchgesetzt (AP-03), es gilt
      authenticated(). Werte eingeben/berichtigen (IP-7), Import (IP-11 ff.) und
      Kanalbindung (IP-17) kommen mit ihren Paketen.
  - name: korrekturen
    description: >
      UEMS Vier Augen bei Korrekturen (AP-08 IP-15, Entscheid E8 = A): „Freigabe durch eine zweite
      Person“ je Unternehmen, Vorgabe AUS, eingestellt nur vom Kundenadministrator; bei an gibt nie
      der Ersteller frei, ein Unterstützer gibt nie frei. Die ersten UEMS-Routen, die ihr Recht
      DURCHSETZEN (rechte-matrix.json über RechteAbleitung): fremd ist 404 `nicht_gefunden`, ein 403
      gilt nur im eigenen Kundenbereich. Die Einstellung wirkt zum Zeitpunkt der Freigabe.
  - name: kostenstellen-prozesse
    description: >
      UEMS-Kostenstellen und -Prozesse (AP-10 IP-7, Konzept §4.2/§5.7, AP-00 E5): die
      Kostenstelle ist FLACH, der Prozess hat höchstens ein Elternteil und nur EINE Ebene
      (Trigger `prozess_eine_ebene`); beide bestehen von `gueltig_ab` bis `gueltig_bis`
      (Tage, der letzte einschließlich) und werden BEENDET, nie gelöscht - es gibt keine
      DELETE-Route. Eine Messstelle gehört je Tag zu 0..n Prozessen (ohne Anteil); eine
      Zuordnung gilt nie länger als ihr Ziel, und ein Ende, das eine Zuordnung abschneiden
      würde, ist 409 `zuordnung_besteht` mit der Liste. Mandanten-Zaun ist die RLS (fremd =
      404 `nicht_gefunden`, nie 403). Rechte (AP-10 §4.10, E15): `kostenstelle.verwalten`,
      `prozess.verwalten`, die Zuordnung `messstelle.bearbeiten` - noch nicht durchgesetzt
      (AP-03), es gilt authenticated(). Die Verteilung auf Kostenstellen steht seit AP-10 IP-8
      unter dem Tag `verteilung`; das Kostenstellen-Lesemodell (IP-11) kommt mit seinem Paket.
  - name: bilanz
    description: >
      UEMS-Bilanz je Anlage (AP-10 IP-9, E3 = A, E18 = A): Zufluss, Abfluss, zugeordnet und Rest je
      Hauptzähler Bezug. Der Rest ist eine RECHNUNG, keine gespeicherte Zahl - seine Terme kommen je
      Tag aus der elektrischen Stellung (BilanzAbleitung.restAusStellung); ändert sich die Stellung,
      ändert sich der Rest von selbst. Eine Rest-MESSSTELLE entsteht nur auf menschlichen Wunsch
      („Rest anlegen“), mit protokolliertem Urheber und nie zweimal. Rechte eingetragen, nicht
      durchgesetzt (AP-03).
  - name: funktionen
    description: >
      UEMS-Funktionen je Standort (AP-01 IP-3, E6 = C, R1/R2): „Messen & Auswerten“ und „Steuern &
      Optimieren“ mit Zustand, „es fehlt“ und je Anlage der Teilnahme samt Prüfliste; die Regel ist der
      Vertrag docs/contracts/v2/funktion-zustand.schema.json (FunktionZustandAbleitung). Starten und
      Fortsetzen prüfen die Liste in derselben Transaktion erneut; Anhalten und Beenden setzen die Ruhe
      bis zum Start (device_override, herkunft `funktion`, ohne Ende). Rechte eingetragen, nicht
      durchgesetzt (AP-03); fremd = 404.
  - name: verteilung
    description: >
      UEMS-Verteilung einer Messstelle auf Kostenstellen (AP-10 IP-8, Konzept §4.6, E11 = A,
      E12 = A; Vertrag docs/contracts/v2/verteilung.md, Regel `satz_ab_tag`): eine eigene
      zeitgültige Beziehung Messstelle → Kostenstelle mit Anteil, geschrieben als SATZ je Tag.
      An jedem Tag mit Zeilen sind es GENAU 100 % (die Datenbank prüft es zur Commit-Zeit); ohne
      Zeile ist die Messstelle ausdrücklich „nicht verteilt“ - nie „zu 0 % verteilt“. Ein Anteil
      gilt nie länger als seine Kostenstelle. Sie wirkt je TAG auf die Tagesmenge (E12).
      Mandanten-Zaun ist die RLS (fremd = 404 `nicht_gefunden`, nie 403). Rechte (AP-10 §4.10,
      E15): `messstelle.verteilung`, lesen `messstelle.ansehen` - noch nicht durchgesetzt (AP-03).
  - name: netzanschluesse
    description: >
      UEMS-Netzanschlüsse (AP-10 IP-6, Konzept §4.2/§5.1, Entscheid E8 = A): der Netzanschluss
      ist ein eigenes Objekt am STANDORT (Kennzeichen wie AP-00 E10 - nie weitergegeben, auch
      nicht nach dem Beenden; Marktlokation elf Ziffern; Messung RLM · SLP) und hängt je Tag an
      höchstens EINER Anlage - und eine Anlage an jedem Tag an genau EINEM Anschluss: eine zweite
      Bindung derselben Anlage am selben Tag ist 409 `bindung_ueberlappt`, ein Wechsel beendet
      die laufende Bindung am Vortag. Beendet, nie gelöscht - es gibt keine DELETE-Route. Regeln
      aus uems/NetzanschlussRegeln (Vertrag docs/contracts/v2/netzanschluss.md). Mandanten-Zaun
      ist die RLS (fremder Standort oder Anschluss eines anderen Standorts = 404
      `nicht_gefunden`, nie 403). Recht `netzanschluss.verwalten` (AP-10 §4.10, E15) - noch nicht
      durchgesetzt (AP-03), es gilt authenticated(). Die Preis- und Grenzspalten der Anlage
      bleiben an der Anlage (W9, eigenes Folgepaket).
  - name: geraete
    description: >
      UEMS-Geräte (AP-04 IP-10): das physische Kästchen hinter der VoltPilot-Box
      (Wechselrichter, Zähler, Ladestation, Controller mit Energiekarten) -
      NICHT die Box (`device`). Nur lesend. Eine Zeile ist EIN Einbau: ein
      Zählerwechsel ist ein neues Gerät an derselben Komponente, `kennzeichen`
      (GR-4) bleibt, `einbau_kennzeichen` (Z-5a, Z-5b) wechselt - das Paar
      `geraet`/`einbau` des Herkunfts- und des Messstellen-Vertrags; die
      früheren Einbauten desselben Geräts sind die `vorgaenger`. Welche
      Komponente ein Einbau wann speist, steht zeitgültig halboffen auf die
      Minute in `komponenten`. Wie die übrigen UEMS-Schnittstellen in
      snake_case. Jede Bestands-Komponente hat nach der Migration V20260911200000
      genau ein laufendes Gerät, jede neu angelegte bekommt es im Anlege-Weg nach
      derselben Regel (V20260911240000; `aus_bestand`: aus der Komponente
      abgeleitet, eingebaut_am ist dann der Beginn ihres Verlaufs, nicht der
      Einbautag); was nicht erhoben ist, ist `null`,
      nie geraten. Mandanten-Zaun ist die RLS (fremd = 404, nie 403;
      Plattform-Admins nutzen X-Tenant-Id). Recht `messwerte.ansehen`
      (docs/contracts/v2/rechte-matrix.json) - die Durchsetzung kommt mit AP-03,
      bis dahin gilt authenticated(). Zähler- und Controllerwechsel (IP-17/IP-19)
      kommen additiv mit dem Recht `geraet.einrichten`.
  - name: zugriff
    description: >
      UEMS AP-03 Rollen und Standortrechte. `GET /api/v1/me` ist die Selbstauskunft (IP-4). Partner- und
      Plattform-Konten wählen den Kundenbereich mit `X-Kundenbereich`, angenommen nur gegen eine wirksame
      Unterstützung - sonst 404 auf jeder Kundenroute.
  - name: energiemanagement
    description: >
      UEMS AP-19 Dokumentation und Managementabläufe. VoltPilot hält fest, der Kunde entscheidet, und jede Zeile
      sagt, wo das Original liegt. IP-6: Personen im Energiemanagement (auch ohne Konto) und Aufgaben mit
      „entschieden von“; die Leitung ist, wer am Tag die Aufgabe „Leitung des Unternehmens“ trägt. IP-10: „Wer ist
      wofür verantwortlich“ liest die Verantwortlichen der Objekte und die Freigaben der Bezugsbasen daneben.
  - name: admin
    description: >
      Platform-admin API (Portal-Admins only). Every operation requires the
      `platform-admin` realm role and is refused with 403 for customer tokens
      (`operator`). These endpoints are cross-tenant by design and do NOT rely on
      the tenant_id claim / RLS; they use a dedicated admin database role and the
      Keycloak Admin API.
paths:
  /health:
    get:
      tags: [system]
      summary: Liveness/readiness (Spring Boot Actuator).
      security: []
      responses:
        "200":
          description: Service is up.
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: UP }
  /api/v1/registration:
    post:
      tags: [registration]
      summary: Self-register - create a tenant and its first login in one step.
      description: >
        The front door of customer onboarding. Creates a B2C tenant named after
        the customer and a Keycloak user (username = email, `tenant_id` attribute
        + customer role), so the standard OIDC + RLS spine isolates the new
        account exactly like operator-provisioned customers. If the user cannot
        be created (e.g. email already registered), the tenant is rolled back -
        no orphans. Implemented in services/api (RegistrationController).
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RegistrationRequest" }
      responses:
        "201":
          description: Account created; the customer can now log in via OIDC.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RegistrationResponse" }
        "400": { description: Invalid input (blank name, invalid email, password shorter than 8 chars). }
        "409": { description: An account with this email already exists. }
        "429": { description: Too many registration attempts (sliding-window rate limit per client address plus a global cap; nothing was created). }
  /api/v1/enrollment/{ref}/csr:
    post:
      tags: [enrollment]
      summary: Upload the device-generated CSR for a reference (first boot).
      description: >
        Stores the pending CSR keyed by the canonical reference (trimmed;
        VP- sticker refs uppercased, exactly like the claim endpoint). The CSR
        must be a PEM PKCS#10 request, self-signed by the enclosed key (proof
        of possession), with an RSA >= 2048 or EC P-256/P-384 key; the subject
        it requests is irrelevant - issuance overrides it with the
        claim-derived identity. Sticker refs (VP-) must exist in the
        provisioned-device registry (422 otherwise). Idempotent per ref:
        re-POSTing replaces the pending CSR until a certificate is issued;
        afterwards the enrolled key is fixed (409) and re-keying goes through
        the operator (revoke + unclaim/re-claim). Implemented in services/api
        (EnrollmentController).
      security: []
      parameters:
        - name: ref
          in: path
          required: true
          schema: { type: string, maxLength: 64 }
          description: The device reference printed on the device (Edge-Referenz / sticker Geräte-ID).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnrollmentCsrRequest" }
      responses:
        "202":
          description: CSR stored; poll GET .../certificate until the ref is claimed.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnrollmentPending" }
        "400": { description: Invalid reference, or the CSR is unparsable / badly signed / off key policy. }
        "409": { description: A certificate was already issued for this reference (the enrolled key is fixed). }
        "422": { description: Unknown sticker Geräte-ID (not in the provisioned-device registry). }
        "429": { description: Rate limit (per client address + global sliding window). Back off >= 5 min. }
  /api/v1/enrollment/{ref}/certificate:
    get:
      tags: [enrollment]
      summary: Poll for the issued device certificate (long-poll-friendly 404 while pending).
      description: >
        404 with body status=pending while the reference is unclaimed - and
        identically for a reference that was never enrolled (no enumeration).
        Once the reference is claimed in the portal, the response carries the
        CA-signed client certificate (subject CN=device_id, O=tenant_id,
        OU=site_id, SPIFFE URI SAN), the CA PEM to verify the broker, the
        broker host/port and the topic identity. The certificate stays
        retrievable for device retries (the private key is the secret and
        never left the device); the per-device broker ACL grant is written at
        issuance. Revocation stays the operator kill switch
        (tools/pki/voltpilot-ca.sh revoke - api-issued certs are recorded in
        the same CA database). After an unclaim the poll returns to pending;
        a re-claim re-issues against the stored CSR with the new identity.
      security: []
      parameters:
        - name: ref
          in: path
          required: true
          schema: { type: string, maxLength: 64 }
      responses:
        "200":
          description: Certificate issued (or re-fetched) - the device switches to mTLS operation.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnrollmentCertificate" }
        "400": { description: Invalid reference. }
        "404":
          description: Pending (unclaimed ref, unknown ref, or no CSR yet - deliberately indistinguishable). Keep polling with backoff.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnrollmentPending" }
        "429": { description: Rate limit. Back off >= 5 min. }
  /api/v1/tenant-context:
    get:
      tags: [sites]
      summary: The caller's tenant context (U0 login bootstrap).
      description: >
        The tenant half of the portal's login bootstrap (composition-driven UI
        overhaul, U0): the caller's tenant row with the EFFECTIVE Betriebsart
        ('endkunde' | 'betreiber') that picks the navigation shell - the
        explicit tenant.betriebsart override when set, else null = unknown
        (the portal then falls back to its site-count heuristic; the segment is
        deliberately NOT used). Derived in the api only; the portal never
        re-derives. RLS-scoped: a customer gets exactly their
        own tenant; a Portal-Admin gets the tenant selected via the X-Tenant-Id
        switcher, and without a selected tenant is default-deny -> 404.
      responses:
        "200":
          description: The caller's tenant context.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TenantContext" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: No tenant context (admin without a selected tenant). }
  /api/v1/unternehmen:
    get:
      tags: [ortsstruktur]
      summary: Das Unternehmen des Kundenbereichs mit den Zahlen von heute (AP-02 IP-3).
      description: >
        Name, Kurzname und Zeitzonen-Vorgabe des Unternehmens (genau eines je
        Kundenbereich, AP-00 E2) und die Zahlen, auf die die Startansicht-Weiche
        (AP-01 E1/IP-5) wartet: Standorte, die es heute gibt; Anlagen des
        Kundenbereichs; Anlagen, die heute keinem Standort zugeordnet sind.
        Ein Kundenbereich ohne Unternehmen-Zeile bekommt den benannten Zustand
        `nicht_angelegt` (Stammdaten null, Zahlen trotzdem richtig) - nie einen
        Fehler. RLS-scoped; ein Admin ohne gewählten Mandanten ist default-deny -> 404.
        Rechte (rechte-matrix.json): lesend ohne eigene Kennung; Bearbeiten ist
        `unternehmen.bearbeiten` (IP-4).
      responses:
        "200":
          description: Das Unternehmen und seine Zahlen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Unternehmen" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: No tenant context (admin without a selected tenant). }
    put:
      tags: [ortsstruktur]
      summary: Das Unternehmen bearbeiten - Name, Kurzname, Zeitzonen-Vorgabe, Sitz, Rechtsform (AP-02 IP-4).
      description: >
        Die ganze Menge der bearbeitbaren Stammdaten (§4.1); ein fehlendes Feld
        ist leer. Kein Anlegen, kein Löschen: das Unternehmen gibt es genau
        einmal je Kundenbereich. Die Zeitzonen-Vorgabe belegt nur NEUE
        Standorte vor (Regel 11). Ändert sich etwas, schreibt die Route in
        einer Transaktion genau einen Eintrag "bearbeitet" ins Änderungsprotokoll
        (`ort_aenderung`, alt/neu nur der geänderten Felder, Urheber); ohne
        Änderung wird nichts geschrieben. Ein Feld, das es hier nicht gibt, ist
        400 `anfrage_ungueltig`. Rechte (rechte-matrix.json): `unternehmen.bearbeiten`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UnternehmenBearbeiten" }
            example:
              name: Kunststoffwerk Ahrenberg GmbH
              kurzname: Ahrenberg
              zeitzone: Europe/Berlin
              sitz: { strasse: Gewerbering 7, plz: null, ort: Ahrenberg, land: DE }
              rechtsform: GmbH
      responses:
        "200":
          description: Das Unternehmen, wie GET /api/v1/unternehmen es jetzt zeigt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Unternehmen" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/unternehmen/aenderungen:
    parameters:
      - $ref: "#/components/parameters/ProtokollVon"
      - $ref: "#/components/parameters/ProtokollBis"
      - $ref: "#/components/parameters/ProtokollAchse"
      - $ref: "#/components/parameters/ProtokollLimit"
      - $ref: "#/components/parameters/ProtokollNach"
    get:
      tags: [unternehmen]
      summary: Das Aenderungsprotokoll des ganzen Unternehmens ueber einen Zeitraum (AP-04 IP-21).
      description: >
        Recht `aenderungsprotokoll.lesen`. Fuehrt die Eintraege der drei Journale
        zusammen: Messstellen (samt Quellenbindung, Einstellungs-Fassung und
        Zaehlerwechsel), Ortsstruktur (Unternehmen, Standort, Gebaeude, Bereich,
        Anlage) und Datenquellen. Sortiert nach der gewaehlten Achse absteigend,
        stabil nach `quelle` und laufender Nummer - deshalb springt die Seitenweise
        auch bei gleichen Zeitstempeln nicht. `limit` Vorgabe 100, hoechstens 500;
        `nach` ist der Wert `weiter` der vorigen Seite (fehlt er, gibt es keine).
        Mit `achse=gueltigkeit` (AP-02 IP-14, die Schnittstelle fuer AP-12) liefert
        er die Eintraege, deren Gueltigkeit in den Zeitraum REICHT - `von`/`bis`
        sind dann Tage und zaehlen beide mit: `?von=2027-02-01&bis=2027-02-28&achse=gueltigkeit`
        nennt auch den Umzug, der am 10.03.2027 eingetragen wurde und ab 01.02.2027
        gilt, mit `zeitform: rueckwirkend` - und die Maerz-Abfrage nennt ihn ebenso.
      responses:
        "200":
          description: Das Protokoll des Unternehmens.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Protokoll" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/orte/{id}/aenderungen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - $ref: "#/components/parameters/ProtokollVon"
      - $ref: "#/components/parameters/ProtokollBis"
      - $ref: "#/components/parameters/ProtokollAchse"
      - $ref: "#/components/parameters/ProtokollLimit"
      - $ref: "#/components/parameters/ProtokollNach"
    get:
      tags: [ortsstruktur]
      summary: Das Aenderungsprotokoll EINES Gebaeudes oder Bereichs (AP-02 IP-14).
      description: >
        Recht `aenderungsprotokoll.lesen`. Nur die eigenen Eintraege des Knotens:
        angelegt, bearbeitet, Flaeche, verschoben, archiviert, wiederhergestellt -
        und das Loeschen eines Bereichs, der an ihm hing. Nur lesend. Ein fremder
        oder unbekannter Ort ist 404, nie 403.
      responses:
        "200":
          description: Das Protokoll des Gebaeudes oder Bereichs.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Protokoll" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/standorte/{id}/aenderungen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - $ref: "#/components/parameters/ProtokollVon"
      - $ref: "#/components/parameters/ProtokollBis"
      - $ref: "#/components/parameters/ProtokollAchse"
      - $ref: "#/components/parameters/ProtokollLimit"
      - $ref: "#/components/parameters/ProtokollNach"
    get:
      tags: [ortsstruktur]
      summary: Das Aenderungsprotokoll EINES Standorts samt Gebaeuden, Bereichen und Anlagen-Zuordnungen (AP-02 IP-14).
      description: >
        Recht `aenderungsprotokoll.lesen`. Die eigenen Eintraege des Standorts und
        die seiner Gebaeude, Bereiche und Anlagen - jedes Kind mit den Eintraegen
        aus der Zeit, in der es an diesem Standort hing (am Tag seines "gilt ab"
        oder am Vortag: ein Umzug steht bei BEIDEN Standorten). Ein Umzug einer
        Anlage steht nur einmal da. Ein fremder oder unbekannter Standort ist 404.
      responses:
        "200":
          description: Das Protokoll des Standorts.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Protokoll" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/standorte:
    get:
      tags: [ortsstruktur]
      summary: Die Standorte zum Stichtag und die Gruppe "noch nicht zugeordnet" (AP-02 IP-3).
      description: >
        Das Standort-Read-Model: je Standort Kurzzeichen, Name, Adresse,
        Zeitzone, Zustand und "es fehlt", die Anlagen mit am Stichtag gültiger
        Zuordnung, Gebäude- und Bereichszahl und die Fläche (eigene ODER Summe
        der Gebäude nur, wenn jedes eine hat; Bereiche nie addiert; fehlend null,
        nie 0) - alles so, wie es am Stichtag galt (dieselbe Ableitung wie der
        Vertrag docs/contracts/v2/ortsbaum-vectors.json). Standorte, die es an
        dem Tag nicht gab oder die archiviert waren, stehen mit Grund und Satz
        unter `nichtGezeigt`. `nochNichtZugeordnet` ist null, sobald jede
        Anlage zugeordnet ist (A15). Ohne Standorte ist `standorte` eine leere
        Liste. Name, Adresse, Zustand sind einfache Felder und stehen wie heute.
        Rechte (rechte-matrix.json): heute lesend ohne eigene Kennung; ein
        Stichtag in der Vergangenheit ("Stand am") ist `aenderungsprotokoll.lesen`.
        Die gezeigte Menge schneidet der Standort-Zaun (AP-03 IP-5); additiv
        traegt die Antwort `teilansicht {sichtbar, gesamt}` (AP-03 IP-10).
      parameters:
        - name: stichtag
          in: query
          required: false
          description: Der Tag (ISO), Vorgabe heute in der Zeitzone des Unternehmens.
          schema: { type: string, format: date, example: "2027-02-15" }
      responses:
        "200":
          description: Die Standorte zum Stichtag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StandorteAmStichtag" }
        "400": { description: stichtag ist kein ISO-Tag. }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [ortsstruktur]
      summary: Einen Standort anlegen (AP-02 IP-4).
      description: >
        Ohne `kurzzeichen` vergibt die Datenbank das nächste ST-n (E8: automatisch,
        änderbar, eindeutig je Kundenbereich über Standorte UND Gebäude/Bereiche,
        nie wiederverwendet - auch ein früher getragenes oder archiviertes bleibt
        belegt). Ohne `zeitzone` gilt die Vorgabe des Unternehmens. Die Adresse
        ist Pflicht: Straße, Ort, Land (DE, AT, CH); die PLZ ist optional und
        wird im Format je Land geprüft ("Die PLZ 84xxx passt nicht zu Österreich
        (vierstellig)."). Mit Adresse ist der Standort sofort eingerichtet und
        aktiv. Geprüft wird erst die Form (400), dann Name und Kurzzeichen
        (409 `name_belegt` / `kurzzeichen_belegt` mit `verweis`): der Name ist
        eindeutig unter den Standorten, die es heute gibt (ohne Groß-/Klein-
        schreibung und Randleerzeichen; ein archivierter gibt seinen Namen frei).
        Standort, Kurzzeichen und genau ein Protokolleintrag "angelegt" entstehen
        in EINER Transaktion. Rechte: `standort.verwalten`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StandortStammdaten" }
            example:
              name: Werk Ahrenberg
              adresse: { strasse: Gewerbering 7, plz: null, ort: Ahrenberg, land: DE }
              zeitzone: Europe/Berlin
              nutzung: [produktion, buero]
              notiz: Zwei Netzanschlüsse (NA-1, NA-2)
              lage: { breitengrad: 48.25, laengengrad: 11.43 }
      responses:
        "201":
          description: Angelegt; `Location` zeigt auf den neuen Standort (so, wie er heute im Lesemodell steht).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StandortAmStichtag" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
        "409": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/standorte/kurzzeichen-vorschlag:
    get:
      tags: [ortsstruktur]
      summary: Das Kurzzeichen, das ein neuer Standort jetzt bekäme (AP-02 IP-4, E8).
      description: >
        Für den Anlege-Dialog ("vorbelegt, meist unberührt"). Bewegt den Zähler
        nicht; wer es unverändert schickt, bekommt es - außer ein anderer war
        schneller (dann 409 mit Verweis). Rechte: `standort.verwalten`.
      responses:
        "200":
          description: Der Vorschlag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StandortKurzzeichenVorschlag" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/standorte/vorschlag:
    get:
      tags: [ortsstruktur]
      summary: Vorschau der Standort-Zuordnung für Bestandsanlagen (AP-02 IP-10).
      description: >
        Liefert je noch nicht zugeordneter Bestandsanlage zunächst eine Gruppe.
        Der Abruf schreibt nichts; Standorte und Zuordnungen entstehen erst mit
        der Bestätigung. Rechte: `standort.verwalten` am Unternehmen.
      responses:
        "200":
          description: Die aktuelle, rein lesende Vorschau.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StandortZuordnungVorschau" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/standorte/vorschlag/bestaetigen:
    post:
      tags: [ortsstruktur]
      summary: Die Vorschau-Zuordnung atomar bestätigen (AP-02 IP-10, A6/A15).
      description: >
        Jede aktuelle Vorschlagszeile muss genau einmal vorkommen. Legt je Gruppe
        einen aktiven Standort mit Adresse an, ordnet die Anlagen ab ihrem
        ursprünglichen `created_at`-Tag zu und protokolliert beides. Bei einer
        veralteten Vorschau wird nichts geschrieben. Rechte: `standort.verwalten`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StandortZuordnungBestaetigen" }
      responses:
        "200":
          description: Alle Standorte und Zuordnungen wurden angelegt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StandortZuordnungErgebnis" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/standorte/{standortId}:
    get:
      tags: [ortsstruktur]
      summary: Ein Standort zum Stichtag (AP-02 IP-3).
      description: >
        Derselbe Eintrag wie in der Liste. Gab es den Standort am Stichtag nicht
        (noch nicht / archiviert), sagt es `bestand` + `bestandText`; die
        zeitgültigen Teile sind dann leer bzw. null. Ein fremder Standort
        (anderer Kundenbereich) ist 404, nie 403 (A14).
      parameters:
        - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
        - name: stichtag
          in: query
          required: false
          schema: { type: string, format: date }
      responses:
        "200":
          description: Der Standort zum Stichtag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StandortAmStichtag" }
        "400": { description: stichtag ist kein ISO-Tag. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Standort not found for this tenant. }
    put:
      tags: [ortsstruktur]
      summary: Einen Standort bearbeiten (AP-02 IP-4).
      description: >
        Die ganze Menge der Stammdaten; ein fehlendes Feld ist leer.
        `kurzzeichen` und `zeitzone` sind Pflicht. Die Adresse (Straße, Ort,
        Land) darf nur einem Entwurf fehlen (E10); wird sie einem Entwurf
        nachgetragen, ist er ab jetzt eingerichtet und aktiv. Name und
        Kurzzeichen wie beim Anlegen (409 mit Verweis; der Standort kollidiert
        nie mit sich selbst und darf zu seinem früheren Kurzzeichen zurück).
        Ein archivierter Standort wird nicht bearbeitet (409 `archiviert`).
        Genau ein Protokolleintrag "bearbeitet" mit alt/neu NUR der geänderten
        Felder; ohne Änderung wird nichts geschrieben. Ein fremder Standort ist
        404, nie 403. Rechte: `standort.verwalten`.
      parameters:
        - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StandortStammdaten" }
      responses:
        "200":
          description: Der Standort, wie er heute im Lesemodell steht.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StandortAmStichtag" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
        "409": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/standorte/{standortId}/archivieren:
    post:
      tags: [ortsstruktur]
      summary: Einen Standort archivieren - nur ohne aktive Anlagen und Messstellen (AP-02 IP-4, E12).
      description: >
        Archiviert ab HEUTE in der Zeitzone des Standorts: sein Bestehen endet am
        Vortag. Gesperrt (409 `archivieren_gesperrt`), solange heute eine Anlage
        an ihm aktiv ist, eine Messstelle in seinem Teilbaum aktiv ist oder eine
        Zuordnung hinein oder heraus geplant ist - `gruende` ist die GANZE Liste
        in fester Reihenfolge (Anlagen, Messstellen, geplante Zuordnungen), je
        Grund mit `weg`; `message` ist der Satz mit Grund und Weg
        ("Werk Lindach kann nicht archiviert werden: die Anlage Werk Lindach ist
        aktiv. Ordnen Sie die Anlage einem anderen Standort zu oder archivieren
        Sie sie zuerst."). Die Regel ist docs/contracts/v2/ortsbaum-vectors.json
        (Familie `archiv`). Messstellen sperren, sobald AP-04 IP-7 sie an Orte
        hängt - bis dahin kennt die Sperre nur Anlagen. Leere Gebäude und
        Bereiche werden mitarchiviert; keine Kaskade auf Anlagen oder
        Messstellen. Genau ein Protokolleintrag "archiviert" am Standort
        (`neu.mitarchiviert` nennt die Kinder). Kein Inhalt oder `{}`; jedes
        Feld ist 400. Ein Löschen gibt es für den Standort nicht (§4.1, E1).
        Rechte: `standort.verwalten`.
      parameters:
        - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Archiviert - der Standort heute (`bestand` archiviert, `archiviertAm` gesetzt).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StandortAmStichtag" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
        "409": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/standorte/{standortId}/flaeche:
    put:
      tags: [ortsstruktur]
      summary: Eine neue zeitgültige Bezugsflächen-Fassung des Standorts eintragen.
      description: >
        Beendet die laufende Fassung am Vortag und legt ab `gueltigAb` eine neue
        an; eine Korrektur am selben Beginn hebt die alte Zeile auf, statt sie zu
        überschreiben. Fehlendes `gueltigAb` bedeutet heute in der Zeitzone des
        Standorts. Rechte: `standort.verwalten`, rückwirkend zusätzlich
        `aenderung.rueckwirkend`.
      parameters:
        - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OrtFlaeche" }
      responses:
        "200":
          description: Der Standort heute mit der dann geltenden Fläche.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StandortAmStichtag" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
        "409": { $ref: "#/components/responses/OrtAbgelehnt" }
        "422": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/standorte/{standortId}/ausfall:
    get:
      tags: [ortsstruktur, messstellen]
      summary: Belegte offene Box-Ausfälle und unvollständige Messstellen eines Standorts (AP-06 IP-17).
      description: >
        Reine Lesesicht auf die jüngste Fortschreibung offener data_gap-Ereignisse mit der
        belegten Fehlerklasse box_meldet_sich_nicht. Eine gemessene Messstelle nennt Box und
        Beginn nur über das offene Ereignis ihrer laufenden Datenquelle; berechnete Messstellen
        nennen ausschließlich ihre fehlenden Eingänge. Fehlt der Fakt, fehlt die Ursache in der
        Antwort. Ein fremder Standort ist 404, nie 403. Recht: `messstelle.ansehen`; als GET ohne
        `@Recht`, Sichtbarkeit durch Standort-Zaun und RLS.
      parameters:
        - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Die aktuell belegte Ausfall-Lage des Standorts.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StandortAusfall" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Standort not found for this tenant. }
  /api/v1/standorte/{standortId}/versorgung:
    get:
      tags: [ortsstruktur, messstellen]
      summary: Versorgung der Gebäude am Standort zum Stichtag (AP-10 IP-17/F15).
      description: >
        Reine Sicht aus den am Stichtag wirksamen Orten und elektrischen Stellungen der Messstellen:
        Ein System versorgt ein Gebäude genau dann, wenn eine seiner Messstellen mit Stellung ungleich
        `keine` im Gebäude oder einem seiner Bereiche liegt. Messstellen direkt am Standort bzw. in einem
        Bereich ohne Gebäude stehen unter `ausserhalbGebaeude`; ein Gebäude ohne zugeordnetes System ist
        `messbar: false`, nie vermeintlich mit 0 versorgt. Ein fremder Standort ist 404, nie 403.
        Recht: `messstelle.ansehen`.
      parameters:
        - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
        - name: stichtag
          in: query
          required: false
          schema: { type: string, format: date }
      responses:
        "200":
          description: Die aus Ort und Stellung abgeleitete Versorgung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Versorgung" }
        "400": { description: stichtag ist kein ISO-Tag. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Standort not found for this tenant. }
  /api/v1/standorte/{standortId}/wiederherstellen:
    post:
      tags: [ortsstruktur]
      summary: Einen archivierten Standort wiederherstellen - ein neues Bestehen ab heute (AP-02 IP-4, A8).
      description: >
        Ein NEUES Bestehen ab heute; die Lücke seit dem Archivieren bleibt
        sichtbar und wird nie aufgefüllt (die Sicht "Stand am" zeigt ihn dort als
        archiviert; das Paar archiviert → wiederhergestellt im Protokoll trägt
        sie, `neu.luecke`). Der Name muss unter den Standorten, die es heute
        gibt, frei sein - sonst 409 `wiederherstellen_gesperrt` mit
        `grund: name_belegt` und `verweis`; optional `{"name": …}` benennt ihn im
        selben Dialog um. Mitarchivierte Gebäude und Bereiche kommen nicht still
        mit zurück. Danach aktiv (mit Adresse) bzw. wieder Entwurf. Genau ein
        Protokolleintrag "wiederhergestellt". Rechte: `standort.verwalten`.
      parameters:
        - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StandortWiederherstellen" }
      responses:
        "200":
          description: Wiederhergestellt - der Standort heute.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StandortAmStichtag" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
        "409": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/standorte/{standortId}/orte:
    get:
      tags: [ortsstruktur]
      summary: Der Ortsbaum eines Standorts zum Stichtag (AP-02 IP-5).
      description: >
        Die Gebäude mit ihren Bereichen, die Bereiche "direkt am Standort" und
        je Knoten die Fläche mit ihrer Quelle, so wie sie am Stichtag galten -
        dieselbe Ableitung wie GET /api/v1/standorte (Vertrag
        docs/contracts/v2/ortsbaum-vectors.json, "Stand am"). Ein Knoten, den
        es am Stichtag nicht gab, fehlt; einer, der heute archiviert ist, es an
        dem Tag aber gab, steht mit `zustand: archiviert` da. Gab es den
        Standort am Stichtag nicht, sagt es `standort.bestand`, und die
        zeitgültigen Teile sind leer bzw. null. `messstellenZahl` (AP-04 IP-7): je
        Knoten die Messstellen, deren Ort am Stichtag GENAU dieser Knoten ist (am
        Gebäude nicht die seiner Bereiche; `direktAmStandort` die am Standort
        selbst), 0 wenn keine. Gebäude und Bereiche
        tragen daneben `datenlage` in derselben Form und Rechnung wie das
        Messstellen-Register (E13: gezählt werden seine Zeilen); am Gebäude
        einschließlich Bereiche. Gebäude und Bereiche tragen keine Zeitzone -
        sie erben die des Standorts (A16). Ein fremder
        Standort ist 404, nie 403 (A14). Rechte (rechte-matrix.json): heute
        lesend ohne eigene Kennung; ein Stichtag in der Vergangenheit ("Stand
        am") ist `aenderungsprotokoll.lesen`. Additiv vorgesehen: `teilansicht`
        (AP-03 IP-10).
      parameters:
        - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
        - name: stichtag
          in: query
          required: false
          description: Der Tag (ISO), Vorgabe heute in der Zeitzone des Standorts.
          schema: { type: string, format: date, example: "2026-10-20" }
      responses:
        "200":
          description: Der Ortsbaum zum Stichtag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OrtsbaumAmStichtag" }
        "400": { description: stichtag ist kein ISO-Tag. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      tags: [ortsstruktur]
      summary: Ein Gebäude oder einen Bereich anlegen (AP-02 IP-5).
      description: >
        Legt den Ort mit seiner Zuordnung ab `gueltigAb` an (fehlend: heute in
        der Zeitzone des Standorts; rückwirkend erlaubt und sichtbar, E2),
        auf Wunsch mit der ersten Fläche ab demselben Tag, und schreibt GENAU
        EINEN Eintrag ins Änderungsprotokoll ("angelegt"). Ohne `kurzzeichen`
        wird das nächste vergeben (G-1 ... / B-1 ..., je Kundenbereich, nie
        wiederverwendet, E8). Ein Bereich hängt an einem Gebäude dieses
        Standorts (`elternId`) oder direkt am Standort (E7); ein Gebäude nur
        am Standort. Die Regeln urteilt der Ortsbaum-Vertrag in fester
        Reihenfolge - ein Ziel falscher Art ist 400 `ziel_art_unzulaessig`
        (Bereich unter Bereich, Gebäude unter Bereich), ein Ziel, das es am
        Tag noch nicht gab, 422 `ziel_gab_es_noch_nicht`, ein archiviertes 409
        `ziel_archiviert`; danach der Name unter den Geschwistern am Tag (409
        `name_belegt` mit Verweis) und das Kurzzeichen (409
        `kurzzeichen_belegt`, auch gegen einen Standort). Rechte:
        `gebaeude.pflegen`, mit `gueltigAb` vor heute zusätzlich
        `aenderung.rueckwirkend`.
      parameters:
        - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OrtAnlegen" }
      responses:
        "201":
          description: Angelegt; `Location` ist /api/v1/orte/{id}.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Ort" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
        "409": { $ref: "#/components/responses/OrtAbgelehnt" }
        "422": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/standorte/{standortId}/orte/kurzzeichen-vorschlag:
    get:
      tags: [ortsstruktur]
      summary: Das nächste freie G-/B-Kurzzeichen für den Anlege-Dialog.
      description: >
        Liest dieselbe mandantenweite, nie wiederverwendete Kurzzeichen-Folge wie
        der Schreibweg, ohne ihren Zähler zu bewegen. Reine Leseroute; kein
        `@Recht`, Sichtbarkeit über RLS/404.
      parameters:
        - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: art, in: query, required: true, schema: { type: string, enum: [gebaeude, bereich] } }
      responses:
        "200":
          description: Der Vorschlag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StandortKurzzeichenVorschlag" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/standorte/{standortId}/messstellen-vorschlag:
    parameters:
      - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [messstellen]
      summary: Die Vorschlagsliste der Bestandsübernahme - aus den Komponenten des Standorts werden Messstellen (AP-04 IP-16).
      description: >
        Recht `messstelle.ansehen` (AP-04 E6, §5.10/§5.15, Abnahme A9; Portal-Hälfte
        AP-01 IP-9b). Liest nur - nichts entsteht ungefragt: keine Messstelle, keine
        Bindung, keine Zeile an der Anlage. Aus den Komponenten der Anlagen des
        Standorts und ihren Messkanälen (IP-9) wird je Komponente und Fluss HÖCHSTENS
        EIN Vorschlag (MessstelleRegeln.vorschlagsliste, Vektoren Familie `vorschlag`):
        Zählerstand vor Leistung (aus einer Leistung wird die Wirkenergie integriert,
        `herleitung: integration`), der Ladestand steht als Nebengröße neben dem
        Speicher-Fluss derselben Komponente. Die Stellung kommt aus der Topologie -
        maßgebliche Netzmessung → Hauptzähler (nur mit Netzanschluss), Erzeugung →
        Erzeuger, Speicher → Speicher, jeder andere Bezug → "Unterzähler von" dem
        Bezug-Hauptzähler seiner Anlage (vorgeschlagen oder bestehend); findet sich
        keiner, bleibt sie leer - nie geraten. Der Name ist der Alias der Komponente
        ("getippt gewinnt"), der Ort der Standort, `ab` der Beginn des Verlaufs (nie vor
        dem Standort und nie über einen Gerätewechsel hinweg). Was NICHT vorgeschlagen
        wird, steht mit Grund und Satz unter `ausgelassen` (abgeleitet · ohne_messkanal ·
        ohne_geraet · attribut_kanal · keine_messgroesse · ohne_richtung · weitere_groesse ·
        vorzeichen_wert · vergleich_kandidat · gleicher_fluss · passt_nicht). Ist alles
        übernommen, ist die Liste leer und sagt es (`leer`, `text`).
      responses:
        "200":
          description: Die Vorschläge und die ausgelassenen Messwerte.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleVorschlagsliste" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/standorte/{standortId}/messstellen-vorschlag/uebernehmen:
    parameters:
      - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [messstellen]
      summary: Die ausgewählten Vorschläge übernehmen - Messstellen mit rückwirkender Bindung (AP-04 IP-16).
      description: >
        Rechte `messstelle.bearbeiten` und `messstelle.quelle`; die Bindung beginnt am
        Beginn des Verlaufs, also zusätzlich `aenderung.rueckwirkend`. Je Zeile entstehen
        in EINER Transaktion: die Messstelle (Kennzeichen vom Server, E7), ihr Ort (der
        Standort) ab dem Verlaufsbeginn, die führende Quelle ab dem Verlaufsbeginn
        (rückwirkend, `herkunft: bestandsuebernahme`), die Nebengrößen und zuletzt die
        Stellung. Geschrieben wird in der Reihenfolge der Liste, damit ein Unterzähler
        seinen Hauptzähler schon vorfindet; fehlt dieser (nicht mitbestätigt), ist das
        422 `stellung_ungueltig` (`grund: bezug_fehlt`). Bestätigt wird nur, was gezeigt
        wurde - eine abweichende Zeile ist 409 `vorschlag_geaendert`, und nichts wird
        geschrieben. Ein Messwert, der dieselbe Messstelle schon speist, zählt als
        `unveraendert`: ein zweiter Aufruf legt nichts an (A9). Der Name darf
        überschrieben werden ("benennt um"); an Komponenten, Mess-Selektion, Geräten und
        Reihen ändert sich nichts.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MessstelleVorschlagUebernehmen" }
      responses:
        "200":
          description: Die angelegten (und die schon vorhandenen) Messstellen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleVorschlagUebernommen" }
        "400":
          description: Die Anfrage ist unvollständig (`anfrage_ungueltig`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleFehler" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Der Vorschlag hat sich geändert (`vorschlag_geaendert`) oder ein Kennzeichen ist belegt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleFehler" }
        "422":
          description: Eine Regel des Vertrags lehnt ab (Passung, Gerät zum Zeitpunkt, Stellung).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleFehler" }

  /api/v1/orte/{ortId}:
    put:
      tags: [ortsstruktur]
      summary: Ein Gebäude oder einen Bereich bearbeiten (AP-02 IP-5).
      description: >
        Die einfachen Felder (§4.3: ohne Gültigkeit, sofort wirksam) - ganz:
        fehlend ist leer, Name und Kurzzeichen sind Pflicht. Das Protokoll
        ("bearbeitet", gilt ab heute) trägt nur die geänderten Felder alt ->
        neu; ohne Änderung gibt es keinen Eintrag. Woran der Ort hängt, ändert
        das Verschieben (IP-12) - `elternId` an dieser Route ist 400. Ein
        archivierter Ort ist 409 `archiviert`. Rechte: `gebaeude.pflegen`.
      parameters:
        - { name: ortId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OrtBearbeiten" }
      responses:
        "200":
          description: Der Ort nach dem Bearbeiten.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Ort" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
        "409": { $ref: "#/components/responses/OrtAbgelehnt" }
    delete:
      tags: [ortsstruktur]
      summary: Ein Gebäude oder einen Bereich löschen - nur ohne jede Historie (AP-02 IP-15, E1, A9).
      description: >
        Endgültig, und nur für einen Ort, an dem nie etwas hing: keine Messstelle
        (auch keine beendete oder aufgehobene Zuordnung), keine Fläche, kein
        Bereich darunter, keine Bezugsgröße. Die Regel ist
        docs/contracts/v2/ortsbaum-vectors.json (Familie `archiv`, Ableitung
        `loeschen`); die Datenbank prüft dasselbe noch einmal (`uems_ort_loeschen`).
        Sonst 409 `loeschen_gesperrt` mit `historie` und dem Satz, dass alles
        andere archiviert wird - nichts ist gelöscht. Das Kurzzeichen bleibt
        belegt und wird nie wiederverwendet; die eigenen Protokolleinträge
        bleiben, und am Knoten, an dem der Ort zuletzt hing, steht genau ein
        Eintrag "geloescht" (`alt` = Art, ID, Kurzzeichen, Name). Ob Löschen geht,
        steht vorher in `aktionen.loeschen` des Ortsbaums. Rechte: `gebaeude.pflegen`.
      parameters:
        - { name: ortId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Gelöscht. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
        "409": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/orte/{ortId}/archivieren:
    post:
      tags: [ortsstruktur]
      summary: Ein Gebäude oder einen Bereich archivieren - nur ohne aktive Messstellen (AP-02 IP-15, Z1/Z2, A7/A8).
      description: >
        Archiviert ab HEUTE in der Zeitzone des Standorts: das laufende Intervall
        endet am Vortag (begann es heute, wird es aufgehoben). Gesperrt (409
        `archivieren_gesperrt`), solange eine Messstelle im Teilbaum aktiv ist
        oder eine Zuordnung hinein oder heraus geplant ist - `gruende` ist die
        GANZE Liste in fester Reihenfolge, je Grund mit `weg`; `message` ist der
        Satz mit Grund und Weg ("Montagehalle Lindach kann nicht archiviert
        werden: 1 Messstelle ist hier aktiv (MS-18 Montagehalle Lindach gesamt).
        Ziehen Sie die Messstelle zuerst um oder legen Sie sie still
        (Messstellen)."). Dieselbe Regel wie am Standort
        (docs/contracts/v2/ortsbaum-vectors.json, Familie `archiv`). Leere
        Bereiche eines Gebäudes werden mitarchiviert; keine Kaskade auf
        Messstellen. Alte Berichte und "Stand am" vor dem Archivtag bleiben
        unverändert. Genau ein Protokolleintrag "archiviert" am Ort
        (`neu.mitarchiviert`). Kein Inhalt oder `{}`; jedes Feld ist 400.
        Rechte: `gebaeude.pflegen`.
      parameters:
        - { name: ortId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Archiviert - der Ort mit seinen Zuordnungen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Ort" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
        "409": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/orte/{ortId}/wiederherstellen:
    post:
      tags: [ortsstruktur]
      summary: Ein archiviertes Gebäude oder einen archivierten Bereich wiederherstellen - ein neues Intervall ab heute (AP-02 IP-15, Z3, A8).
      description: >
        Ein NEUES Intervall ab heute an dem Knoten, an dem der Ort zuletzt hing;
        die Lücke seit dem Archivieren bleibt sichtbar und wird nie aufgefüllt
        (`neu.luecke` im Protokoll). Gesperrt (409 `wiederherstellen_gesperrt`
        mit `grund`), solange der Elternknoten archiviert ist (`eltern_archiviert`)
        oder der Name unter den Geschwistern heute vergeben ist (`name_belegt`
        mit `verweis`) - optional `{"name": …}` benennt ihn im selben Dialog um.
        Das Kurzzeichen bleibt seins; mitarchivierte Bereiche kommen nicht still
        mit. Genau ein Protokolleintrag "wiederhergestellt". Rechte: `gebaeude.pflegen`.
      parameters:
        - { name: ortId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StandortWiederherstellen" }
      responses:
        "200":
          description: Wiederhergestellt - der Ort mit seinen Zuordnungen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Ort" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
        "409": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/orte/{ortId}/flaeche:
    put:
      tags: [ortsstruktur]
      summary: Die Bezugsfläche ab einem Tag setzen oder ändern (AP-02 IP-5, E3).
      description: >
        Ganze m² > 0 ab `gueltigAb` (fehlend: heute in der Zeitzone des
        Standorts). Die laufende Fläche endet am Vortag, die neue erbt deren
        Ende; beginnt am Tag schon eine, wird sie ersetzt (Korrektur - sie
        bleibt aufgehoben lesbar). Die Regel ist die des Vertrags
        (ortsbaum-vectors.json, Familie flaeche/eintrag). Rückwirkend ist
        erlaubt und sichtbar: `rueckwirkung` sagt "rückwirkend (14 Tage)"
        (A3), das Protokoll ("flaeche_geaendert", alt -> neu) trägt es.
        Abgelehnt: 400 `flaeche_ungueltig` (keine ganze Zahl > 0 - 3100.5
        wird nie gerundet), 400 `gleiche_flaeche`, 422 `gab_es_noch_nicht`
        (vor dem ersten Tag des Orts), 409 `archiviert`. Rechte:
        `gebaeude.pflegen`, mit `gueltigAb` vor heute zusätzlich
        `aenderung.rueckwirkend`.
      parameters:
        - { name: ortId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OrtFlaeche" }
      responses:
        "200":
          description: Der Ort mit seinen Flächen nach dem Eintrag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Ort" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
        "409": { $ref: "#/components/responses/OrtAbgelehnt" }
        "422": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/orte/{ortId}/verschieben/vorschau:
    get:
      tags: [ortsstruktur]
      summary: Was das Verschieben eines Gebäudes oder Bereichs bewirken würde - schreibt nichts (AP-02 IP-12, V2/V3, A13).
      description: >
        Dieselbe Regel und dieselben Fakten wie POST .../verschieben
        (ortsbaum-vectors.json, Familien `ueberlappung` und `verschieben`) und
        dieselbe Antwortform - nur `protokoll` ist leer. Die Folgen-Karte
        (E11): `ziehenMit` sind die Kinder-Bereiche, `messstellenWechselnStandort`
        die Messstellen, deren Standort sich ABGELEITET ändert (sie bleiben an
        ihrem Ort); es BLEIBEN `bleibenAnlagen`, ihre `bleibenNetzanschluesse`
        und ihre übrigen `bleibenMessstellen` - etwa eine, die direkt am
        Standort hängt (A13). Nichts Elektrisches zieht mit, und es wird nichts
        gesendet (`befehle` = 0). Lehnt ab wie der Eintrag: Status, Code,
        `feld` und der Satz des Vertrags. Rechte: `gebaeude.pflegen`.
      parameters:
        - { name: ortId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: zielId, in: query, required: true, description: "Ein Standort - für einen Bereich auch ein Gebäude.", schema: { type: string, format: uuid } }
        - { name: gueltigAb, in: query, required: false, description: Fehlend = heute in der Zeitzone des Standorts., schema: { type: string, format: date } }
      responses:
        "200":
          description: Die Vorschau - dieselbe Form wie der Eintrag, `protokoll` leer.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OrtVerschiebung" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
        "409": { $ref: "#/components/responses/OrtAbgelehnt" }
        "422": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/orte/{ortId}/verschieben:
    post:
      tags: [ortsstruktur]
      summary: Ein Gebäude oder einen Bereich ab einem Tag verschieben (AP-02 IP-12, V4, A1/A2/A13).
      description: >
        Das laufende Intervall des Orts endet am Vortag von `gueltigAb`, das
        neue am Ziel erbt dessen Ende (Invariante 4: nie überschrieben). Ein
        Gebäude zieht zu einem anderen Standort, ein Bereich zu einem Gebäude
        oder direkt an einen Standort - nie in einen Bereich. Geschrieben
        werden NUR `ort_zuordnung` und GENAU EIN Protokolleintrag "verschoben"
        am Ort ("Gebäude verschoben: Werk Ahrenberg -> Werk Ahrenberg Nord
        (ST-3)"). Bereiche ziehen mit, Messstellen bleiben an ihrem Ort;
        Anlagen, Netzanschlüsse, Box, Topics und alles andere bleiben (A13).
        Zukunft = geplant, Vergangenheit = rückwirkend (sichtbar in
        `rueckwirkung` und im Protokoll, E2). Abgelehnt: 400
        `ziel_ist_bisheriger_eltern` / `ziel_art_unzulaessig` / `anfrage_ungueltig`,
        409 `gleicher_tag` / `ziel_archiviert` / `objekt_archiviert` / `name_belegt`,
        422 `vor_dem_ersten_intervall` / `ziel_gab_es_noch_nicht` - je mit dem
        Satz des Vertrags und `feld`. Rechte: `gebaeude.pflegen`, mit
        `gueltigAb` vor heute zusätzlich `aenderung.rueckwirkend`.
      parameters:
        - { name: ortId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/OrtVerschiebenAnfrage" }
      responses:
        "200":
          description: Verschoben - die Zuordnungen frisch gelesen, dazu der Protokolleintrag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OrtVerschiebung" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
        "409": { $ref: "#/components/responses/OrtAbgelehnt" }
        "422": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/overview:
    get:
      tags: [sites]
      summary: Tenant-wide fleet overview for the adaptive Uebersicht.
      description: >
        One aggregate for the portal's fleet mode (customers with several
        sites): per site the device status (liveness from each device's newest
        telemetry ARRIVAL, the store-and-forward rule), the newest live
        telemetry snapshot, and today's planned battery savings (ex-ante, from
        the persisted optimizer plans, Europe/Berlin day, latest run per slot);
        plus fleet totals and the fleet-wide per-day savings of the last 14
        Berlin days for the hero mini chart. RLS-scoped: the fleet is exactly
        the caller's tenant. Admins read another tenant's fleet via the
        X-Tenant-Id switcher.
      responses:
        "200":
          description: The caller's fleet overview.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Overview" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/edge-versions:
    get:
      tags: [sites]
      summary: Reported edge software version per device (tenant-wide).
      description: >
        The `core_version` / `palette_version` each device reports in the
        `flows` block of its status heartbeat, persisted by the flow-status
        listener - the data behind the platform overview's "Edge-Stand" column.
        RLS-scoped like /overview (admins read another tenant's devices via the
        X-Tenant-Id switcher). A device is ABSENT until it reports: the edge
        only builds the `flows` block once it has seen a flow deployment set, so
        a device without a rolled-out automation reports no version at all.
        Absent therefore means "unknown", never "outdated". Since Geräteseiten
        Stufe 1 (R2a) each row additionally carries the JUDGEMENT against the
        release register (`newestRelease`/`upToDate`) - the yardstick never
        reached the customer before, so their box showed a version nobody could
        place. The judgement travels, NEVER the register.

        Die Menge schneidet der Standort-Zaun (UEMS AP-03 IP-5, `site_scope`):
        die Liste zeigt ausschliesslich die SICHTBAREN Anlagen des Aufrufers.

        AP-03 IP-12: Der Umschlag nennt die sichtbaren Einträge und mit
        `teilansicht {sichtbar, gesamt}` den Umfang dieser Antwort.
      responses:
        "200":
          description: One entry per device that reported a version.
          content:
            application/json:
              schema:
                type: object
                required: [eintraege, teilansicht]
                properties:
                  eintraege:
                    type: array
                    items: { $ref: "#/components/schemas/EdgeVersion" }
                  teilansicht: { $ref: "#/components/schemas/Teilansicht" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/earnings:
    get:
      tags: [sites]
      summary: Realized earnings - measured "mit VoltPilot vs. ungeregelte Anlage".
      description: >
        The measured money numbers behind the portal hero (fleet AND
        single-site), computed per 15-min slot from the telemetry rollups
        valued at the slot's day-ahead price in each site's own bidding zone
        (PT15M rows preferred, PT60M fallback): baseline = (load_kwh - pv_kwh)
        * price/1000 (the UNREGULATED plant - same generation and consumption,
        fed in immediately, battery idle), actual = (grid_import_kwh -
        grid_export_kwh) * price/1000, saved = baseline - actual. Both are
        signed COSTS (negative = revenue); with symmetric spot pricing, saved
        equals the battery's dispatch valued at spot, so round-trip losses
        debit VoltPilot automatically. Windows are Europe/Berlin calendar
        periods containing `at`; range=all starts at the fleet's first covered
        slot and the response's `from` says so. RLS-scoped like every customer
        endpoint; admins use the X-Tenant-Id switcher.

        Die MENGE ist die Standort-Menge des Aufrufers (UEMS AP-03 IP-10):
        `sites` sind die SICHTBAREN Anlagen (Standort-Zaun `site_scope`, IP-5)
        und, wenn `standort` genannt ist, nur die an diesen Standorten.
        `totals` und `vergleich` summieren ueber genau diese Liste - nie
        mandantenweit (Regel R-A2). Additiv: `teilansicht {sichtbar, gesamt}`.
      parameters:
        - name: range
          in: query
          schema: { type: string, enum: [day, month, year, all], default: month }
        - name: at
          in: query
          description: The date whose period to report (default today, Europe/Berlin).
          schema: { type: string, format: date }
        - name: strip
          in: query
          description: >
            Opt in to the tappable 12-month strip (`monthlyStrip`). It is a fixed
            ~173 ms 12-month scan and no portal surface renders the fleet strip's
            values today, so it is computed ONLY on demand (audit
            vp-portal-perf-a4, finding B5); `monthlyStrip` is [] when omitted.
          schema: { type: boolean, default: false }
        - name: standort
          in: query
          required: false
          description: >
            Die STANDORT-MENGE (UEMS AP-03 IP-10): wiederholbar
            (?standort=...&standort=...). Ohne sie antwortet die Route ueber
            ALLE sichtbaren Anlagen - zeichengleich zu heute. Mit ihr ueber die
            Anlagen, die HEUTE an einem der genannten Standorte haengen (AP-02
            IP-3, Zuordnung zum heutigen Tag); eine Anlage ohne gueltige
            Zuordnung gehoert zu keinem gewaehlten Standort. Sie ersetzt das
            heutige "eine (/sites/{id}/earnings) oder alle" durch eine echte
            Menge. Ein Standort ausserhalb des Zugriffs ist 404, nie 403 (A14).
            `teilansicht.sichtbar` ist dann die Anzahl der gewaehlten Standorte.
          schema:
            type: array
            items: { type: string, format: uuid }
          style: form
          explode: true
      responses:
        "200":
          description: The caller's realized earnings.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Earnings" }
        "400": { description: Unknown range. }
        "404":
          description: >
            Ein genannter Standort ist fuer diese Anfrage nicht sichtbar (AP-03
            IP-10) - nie 403, die Existenz wird nicht bestaetigt (A14).
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/sites:
    get:
      tags: [sites]
      summary: List sites for the caller's tenant (tenant_id from OIDC token).
      description: >
        Die Menge schneidet der Standort-Zaun (UEMS AP-03 IP-5, `site_scope`):
        die Liste zeigt ausschliesslich die SICHTBAREN Anlagen des Aufrufers.

        AP-03 IP-12: Der Umschlag nennt die sichtbaren Einträge und mit
        `teilansicht {sichtbar, gesamt}` den Umfang dieser Antwort.
      responses:
        "200":
          description: Sites (only the caller's VISIBLE sites, via RLS).
          content:
            application/json:
              schema:
                type: object
                required: [eintraege, teilansicht]
                properties:
                  eintraege:
                    type: array
                    items: { $ref: "#/components/schemas/Site" }
                  teilansicht: { $ref: "#/components/schemas/Teilansicht" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [sites]
      summary: Create a site for the caller's own tenant (self-service onboarding).
      description: >
        Creates a site (Standort) for the CALLER's tenant. The tenant is taken from
        the OIDC `tenant_id` claim (via TenantContext) and enforced by RLS' WITH
        CHECK - never from the request body - so a customer can only ever create a
        site for themselves. This unblocks onboarding: a fresh customer creates a
        site, then claims devices into it. Implemented in services/api.
        Additively (UEMS AP-02 IP-9) the new site is assigned to its Standort in
        the SAME transaction, from the day of its `created_at` in the Standort's
        time zone: `standortId` if given; without it the one Standort of the
        Kundenbereich when there is exactly one; none when there is none (as
        before - the site stays "noch nicht zugeordnet" until the
        Bestandsübernahme or the Vorschau); with several the choice is required
        (422 `standort_waehlen`). The response is the unchanged `Site`; `GET
        /sites/{siteId}` shows the Standort. Today several Standorte can only
        come from the Standort API (`POST /api/v1/standorte`).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateSiteRequest" }
      responses:
        "201":
          description: Site created (for the caller's tenant).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Site" }
        "400":
          description: >
            Invalid input (blank name, out-of-range coordinates, or bad zone).
            An unknown or foreign `standortId` answers in the form of the
            Ortsstruktur - `anfrage_ungueltig` with `feld: standortId`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OrtFehler" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409":
          description: "`ziel_archiviert` - the chosen Standort is archived (`feld: standortId`)."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OrtFehler" }
        "422":
          description: >
            `standort_waehlen` - no `standortId` although the Kundenbereich has
            several (not archived) Standorte; `standorte` lists them.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OrtFehler" }
  /api/v1/sites/{siteId}:
    get:
      tags: [sites]
      summary: One site, plus its Standort today.
      description: >
        Exactly the `Site` fields of the list and of PUT, unchanged, plus -
        additively - `standort` (UEMS AP-02 IP-3): the Standort the site is
        assigned to today, or null while it is not assigned (a Kundenbereich
        with several sites until its Vorschau is confirmed; the
        Bestandsübernahme IP-9 assigns the one site of a single-site
        Kundenbereich, `POST /sites` a new one). RLS makes a foreign site a 404.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The site.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteDetail" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
    put:
      tags: [sites]
      summary: Update a site (name/bidding zone/coordinates).
      description: >
        Validation mirrors create. RLS makes a foreign site a 404 and pins the
        row to its tenant, so a site can never be moved across tenants. Admins
        edit any tenant's sites through the same endpoint via the X-Tenant-Id
        tenant switcher.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateSiteRequest" }
      responses:
        "200":
          description: Updated site.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Site" }
        "400": { description: Invalid input (blank name, out-of-range coordinates, or bad zone). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
    delete:
      tags: [sites]
      summary: Delete a site (guarded; cascades assets + series data).
      description: >
        Refused (409) while the site still has devices that are not ausgebaut -
        they must be removed first. Refused (409 `messstellen_belege`, with the
        list) when the site's measurements are Belege of Messstellen. On success
        the site's assets cascade by FK and its series data (telemetry, rollups,
        forecast, schedule, weather, forecast-quality rows, the measurement
        pipeline, its ausgebaut boxes) is deleted in the same transaction. Use the deletion-preview endpoint to
        show the concrete consequences before confirming. Its Standort
        assignment is not deleted (UEMS AP-02 W5): it ends today and stays as an
        ended interval with a log entry (`geloescht`) at the Standort.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Site and its recorded data deleted. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
        "409":
          description: >
            Still has devices (plain `message`; remove them first), or its measurements are Belege of Messstellen (UEMS AP-07 E8/IP-11): a series
            that was EVER bound to a Messstelle (binding, its log entry
            `quelle_gebunden`, or a formula term) is never deleted. Nothing was
            written; the body names every Messstelle in the way (`code`
            `messstellen_belege`) and, in the same answer, every released
            Berichtsstand citing one of them (`codes` adds `berichts_belege`,
            list `berichtsstaende` - UEMS AP-12 E13 S2/IP-12).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BelegeImWeg" }
  /api/v1/sites/{siteId}/standort/vorschau:
    get:
      tags: [ortsstruktur]
      summary: Folgen-Vorschau - was die Zuordnung der Anlage zu einem Standort bewirken würde (UEMS AP-02 IP-11, T6).
      description: >
        Schreibt nichts. Urteilt mit derselben Regel und denselben Fakten wie
        `PUT /api/v1/sites/{siteId}/standort` und antwortet in DERSELBEN Form
        (`protokoll` leer) - was die Vorschau ankündigt, tritt beim Eintrag ein.
        Eine Ablehnung ist dieselbe wie beim Eintrag (Status, Code, Satz).
        Rechte: `anlage.zuordnen`.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: standortId, in: query, required: true, schema: { type: string, format: uuid } }
        - name: gueltigAb
          in: query
          required: false
          description: Der erste Tag der neuen Zuordnung; fehlend = heute in der Zeitzone des Ziels.
          schema: { type: string, format: date }
      responses:
        "200":
          description: Die Folgen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AnlageUmzug" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
        "409": { $ref: "#/components/responses/OrtAbgelehnt" }
        "422": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/sites/{siteId}/standort:
    put:
      tags: [ortsstruktur]
      summary: Eine Anlage einem Standort zuordnen oder umziehen, gültig ab einem Tag (UEMS AP-02 IP-11, T6, A11).
      description: >
        Die Zuordnung ist eine Aussage über Zugehörigkeit, kein Eingriff in den
        Betrieb: geschrieben werden NUR `anlage_standort` (die laufende
        Zuordnung endet am Vortag, die neue erbt ihr Ende) und das
        Änderungsprotokoll. Box-Heimat, MQTT-Topics `ems/{t}/{Anlage}/…`,
        Freigaben, Betriebsmodell, Ladepark-Rahmen, Fahrpläne, Messstellen,
        Netzanschluss und die Teilnahme an "Steuern & Optimieren" bleiben; es
        wird kein Befehl, keine Konfiguration und kein Override gesendet
        (`befehle` = 0). Ab `gueltigAb` zählen Standort-Karten und Berichte die
        Anlage beim neuen Standort; ein Tag in der Zukunft ist geplant und wirkt
        erst dann, einer in der Vergangenheit ist erlaubt und rückwirkend
        sichtbar (E2). Die Regel ist docs/contracts/v2/ortsbaum-vectors.json
        (Familie `ueberlappung`/`eintrag`, Vorgang verschieben). Abgelehnt mit
        dem Satz des Vertrags: 400 `ziel_ist_bisheriger_eltern` (`feld`
        standortId), 422 `vor_dem_ersten_intervall`, 409 `gleicher_tag` (am Tag
        beginnt schon eine Zuordnung), 409 `objekt_archiviert`, 422
        `ziel_gab_es_noch_nicht`, 409 `ziel_archiviert`; 400
        `anfrage_ungueltig` (unbekannter Standort, fehlende oder falsche
        Felder, Begründung über 500 Zeichen); eine fremde Anlage ist 404. Je
        ein Protokolleintrag "verschoben" an der Anlage, am neuen und - wenn es
        einen gab - am bisherigen Standort, mit Urheber und Begründung. Rechte:
        `anlage.zuordnen`, mit `gueltigAb` vor heute zusätzlich
        `aenderung.rueckwirkend`.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AnlageUmzugAnfrage" }
      responses:
        "200":
          description: Eingetragen - die Folgen mit den frisch gelesenen Zuordnungen und den Protokolleinträgen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AnlageUmzug" }
        "400": { $ref: "#/components/responses/OrtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/OrtAbgelehnt" }
        "409": { $ref: "#/components/responses/OrtAbgelehnt" }
        "422": { $ref: "#/components/responses/OrtAbgelehnt" }
  /api/v1/sites/{siteId}/deletion-preview:
    get:
      tags: [sites]
      summary: What deleting this site would remove (for the confirm dialog).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Device count and recorded-data extent/counts.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteDeletionPreview" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/devices:
    get:
      tags: [devices]
      summary: List devices for the caller's tenant.
      description: >
        Die Menge schneidet der Standort-Zaun (UEMS AP-03 IP-5, `site_scope`):
        die Liste zeigt ausschliesslich die SICHTBAREN Anlagen des Aufrufers.

        AP-03 IP-12: Der Umschlag nennt die sichtbaren Einträge und mit
        `teilansicht {sichtbar, gesamt}` den Umfang dieser Antwort.
      responses:
        "200":
          description: Devices (only the caller's VISIBLE sites, via RLS).
          content:
            application/json:
              schema:
                type: object
                required: [eintraege, teilansicht]
                properties:
                  eintraege:
                    type: array
                    items: { $ref: "#/components/schemas/Device" }
                  teilansicht: { $ref: "#/components/schemas/Teilansicht" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/devices/{deviceId}:
    put:
      tags: [devices]
      summary: Update a device (type + label only; the ref is immutable).
      description: >
        Only `kind` and the optional customer-facing label `name` are editable.
        The `externalRef` is the device's identity (MQTT topics, registry
        gating, the printed sticker) and cannot be changed. Editing never
        unclaims/re-claims a device: identity, history, commands and audit stay
        attached to the same id.
      parameters:
        - { name: deviceId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateDeviceRequest" }
      responses:
        "200":
          description: Updated device.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Device" }
        "400": { description: Invalid kind or over-long label. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Device not found for this tenant. }
    delete:
      tags: [devices]
      summary: Unclaim a device - the box is ausgebaut, nothing recorded is deleted.
      description: >
        UEMS AP-07 E8/IP-11 (AP-06 E7): the box is kept as ausgebaut
        (`status: ausgebaut`) with its identity, and its raw telemetry, OCPP
        recordings, measurements and events stay - the history keeps naming
        it. It takes no part in operation anymore (every device route answers
        404, no list shows it, components and the site no longer point at it).
        The broker's retained `provision/{ref}/config` and schedule topics are
        cleared (best-effort, like the on-claim publish) so the physical device
        falls back to its watchdog default. The ref becomes claimable again (as
        a new device); a sticker ID stays registered in the manufacturing
        registry.
      parameters:
        - { name: deviceId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Device unclaimed (ausgebaut); nothing recorded deleted. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Device not found for this tenant. }
  /api/v1/devices/{deviceId}/purge-data:
    post:
      tags: [devices]
      summary: Purge the recorded data of a device WITHOUT unclaiming it.
      description: >
        "Datenaufzeichnungen löschen": irreversibly deletes every recording of
        this device that is not a Beleg of a Messstelle (UEMS AP-07 W6) - a box
        carrying a series ever bound to a Messstelle is refused (409) before
        anything is written. Deleted are raw telemetry plus the site's
        derived rollups, which are rebuilt from the remaining devices - while
        the device itself stays claimed and fully operational (enrollment,
        certificates, config and the retained schedule are untouched). A purge
        watermark (`purgedBefore`) is stamped on the device: the ingest path
        refuses any sample OBSERVED at or before it, so a store-and-forward
        edge replaying old buffered samples can never resurrect the deleted
        history. The device is additionally told to wipe its local buffers via
        a retained MQTT `purge_data` command (contract:
        mqtt-data-purge.schema.json) - delivered on reconnect if it is offline.
        New data recorded after the purge flows and charts normally.
      parameters:
        - { name: deviceId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Purge completed (cloud data is gone).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DevicePurgeResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Device not found for this tenant. }
        "409":
          description: >
            The box's recordings are Belege of Messstellen (UEMS AP-07 E8/IP-11): a series
            that was EVER bound to a Messstelle (binding, its log entry
            `quelle_gebunden`, or a formula term) is never deleted. Nothing was
            written; the body names every Messstelle in the way (`code`
            `messstellen_belege`) and, in the same answer, every released
            Berichtsstand citing one of them (`codes` adds `berichts_belege`,
            list `berichtsstaende` - UEMS AP-12 E13 S2/IP-12).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BelegeImWeg" }

  /api/v1/devices/{deviceId}/measurement-selection:
    get:
      tags: [measurements]
      summary: Gewünschter Pollplan, Papier-Spur und Jahresvolumen-Schätzung.
      description: >
        Der gespeicherte SOLL-Zustand. `applyStatus` ist niemals eine
        Anwendungs-Zusage: `pending_edge` heißt, die Box hat noch nicht
        quittiert. `entityId` schneidet Auswahl UND Papier-Spur auf EINE
        Komponente zu; `desiredRevision` (das Optimistic-Concurrency-Token des
        einen veröffentlichten Plans) und `volumeEstimate` (die physische Last
        des einen Busses) bleiben dabei geräteweit.
      parameters:
        - name: deviceId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: entityId
          in: query
          required: false
          schema: { type: string, format: uuid }
          description: >
            Die Komponente (measurement_point) dieses Geräts. Sie muss an
            derselben Anlage stehen; eine fremde ist 404. Ohne den Parameter
            antwortet die Route für das GANZE Gerät.
      responses:
        "200":
          description: Soll-Zustand des Geräts bzw. der Komponente.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MeasurementSelectionState" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Gerät oder Komponente nicht gefunden (RLS). }
  /api/v1/devices/{deviceId}/measurement-selection/catalog:
    get:
      tags: [measurements]
      summary: Der versionierte Messpunkt-Katalog, durchsuchbar und facettiert.
      description: >
        Liefert nur lesbare Punkte. `availableOnly` filtert auf die
        Anbindungs-Familien, die zu diesem Ziel passen: MIT `entityId` ist das
        die Familie DIESER Komponente, ohne ihn die Vereinigung der Punkte, die
        die Geräte-UUID tragen - auf einer Box mit mehreren Komponenten also
        die des komponierten Hybriden und damit die falsche Frage je Komponente.
      parameters:
        - name: deviceId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: entityId
          in: query
          required: false
          schema: { type: string, format: uuid }
          description: Die Komponente, deren Familie und Auswahl gelten sollen.
        - name: q
          in: query
          required: false
          schema: { type: string }
          description: >
            Freitext über Label, Selektor/Adresse/API-Schlüssel, Einheit,
            Gruppe und Punkt-Schlüssel.
        - name: family
          in: query
          required: false
          schema: { type: array, items: { type: string } }
          description: >
            Katalog-Familien, etwa `goe.api_v2` oder `sunspec.model_103`.
        - name: group
          in: query
          required: false
          schema: { type: string }
        - name: semanticStatus
          in: query
          required: false
          schema: { type: string }
          description: >
            `known`, `vendor_label_only` oder `unknown` - Letzteres trägt weder
            Einheit noch Semantik.
        - name: recorded
          in: query
          required: false
          schema: { type: boolean }
          description: >
            Nur schon (bzw. noch nicht) gemessene Punkte. Geräteweit - Samples
            tragen bis Stufe 3c keine Komponenten-Dimension.
        - name: availableOnly
          in: query
          required: false
          schema: { type: boolean, default: false }
        - name: selectedOnly
          in: query
          required: false
          schema: { type: boolean, default: false }
          description: >
            Nur aktuell ausgewählte Punkte der angefragten Geräte- bzw.
            Komponenten-Sicht. Dieser kompakte Statusabruf ist unabhängig von
            der Position eines Punkts im paginierten Gesamtkatalog.
        - name: offset
          in: query
          required: false
          schema: { type: integer, default: 0 }
        - name: limit
          in: query
          required: false
          schema: { type: integer, default: 100 }
      responses:
        "200":
          description: >
            Seite des Katalogs samt Facetten-Zählern, je Punkt mit Label,
            Einheit, Adresse/Selektor, Kadenz-Vorgaben, Aufbewahrung, ob er
            ausgewählt bzw. gemessen ist und dem letzten bekannten Wert.
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        "400": { description: Unbekannte Facette oder ungültige Seite. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Gerät oder Komponente nicht gefunden (RLS). }
  /api/v1/devices/{deviceId}/measurement-selection/estimate:
    get:
      tags: [measurements]
      summary: Volumen- und Gerätelast-Vorschau vor dem Klick; schreibt nichts.
      parameters:
        - name: deviceId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: entityId
          in: query
          required: false
          schema: { type: string, format: uuid }
          description: >
            Die Komponente, deren Auswahl ersetzt wird. Die Schätzung zählt
            weiterhin den GANZEN Bus.
        - name: pointKey
          in: query
          required: true
          schema: { type: string }
        - name: enabled
          in: query
          required: false
          schema: { type: boolean, default: true }
        - name: cadenceS
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 86400 }
          description: >
            Ohne Angabe die Katalog-Vorgabe des Punktes. Unterhalb seiner
            sicheren Geräteempfehlung ist die Anfrage 400.
      responses:
        "200":
          description: >
            Samples/min, Requests/min, Duty-Cycle und die Jahresvolumen-Spanne,
            dazu die Grenzen (Warnung ab 120 Samples/min; hart bei 600
            Samples/min, 30 Requests/min oder 20 % Duty). Es gibt kein
            Punktzahl-Limit.
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        "400": { description: Unbekannter Punkt oder unzulässige Kadenz. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Gerät oder Komponente nicht gefunden (RLS). }
  /api/v1/devices/{deviceId}/measurement-selection/custom/estimate:
    post:
      tags: [measurements]
      summary: Dieselbe Vorschau für ein noch nicht angelegtes freies Register.
      parameters:
        - name: deviceId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: entityId
          in: query
          required: false
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CustomMeasurementDefinition" }
      responses:
        "200":
          description: Budget-Vorschau.
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        "400":
          description: >
            Ungültige Definition - unter anderem ein mitgeschickter
            Schreibparameter oder eine selbst gewählte Kostenannahme.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Gerät oder Komponente nicht gefunden (RLS). }
  /api/v1/devices/{deviceId}/measurement-selection/custom:
    post:
      tags: [measurements]
      summary: Eigenen Messwert hinzufügen - ein validiertes, NUR LESBARES freies Register.
      description: >
        Der Punkt-Schlüssel wird serverseitig aus dem Idempotenzschlüssel
        gebildet (`custom.<hex>`); die Definition ist per Kontrakt read-only und
        trägt keine Schreibfunktion. Die Request-Kostenannahme setzt
        ausschließlich der Server (konservativ), ein Client-Wert ist 400.
      parameters:
        - name: deviceId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: entityId
          in: query
          required: false
          schema: { type: string, format: uuid }
          description: Die Komponente, der das Register gehört.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [expectedRevision, idempotencyKey, definition]
              properties:
                expectedRevision: { type: integer, format: int64, minimum: 0 }
                idempotencyKey: { type: string, format: uuid }
                definition: { $ref: "#/components/schemas/CustomMeasurementDefinition" }
      responses:
        "200":
          description: Soll-Zustand nach dem Anlegen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MeasurementSelectionState" }
        "400": { description: Ungültige Definition oder fehlender Idempotenzschlüssel. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Gerät oder Komponente nicht gefunden (RLS). }
        "409":
          description: >
            Veraltete Revision, oder derselbe Idempotenzschlüssel für eine
            andere Änderung.
        "422": { description: Technisches Messwertbudget überschritten. }
  /api/v1/devices/{deviceId}/measurement-selection/{pointKey}:
    put:
      tags: [measurements]
      summary: Revisionierte An-/Abwahl eines Katalogpunktes.
      description: >
        Abwählen ist ein UPDATE, nie ein DELETE: die Auswahlzeile und ihre
        Ereignisse bleiben, nur künftige Proben hören auf. Aktivieren stempelt
        `enabledAt` serverseitig - es wird nichts rückwirkend ergänzt. Die
        Wiederholung mit demselben Idempotenzschlüssel und derselben Nutzlast
        ist ein echter Replay und erzeugt keine neue Revision.
      parameters:
        - name: deviceId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: pointKey
          in: path
          required: true
          schema: { type: string }
        - name: entityId
          in: query
          required: false
          schema: { type: string, format: uuid }
          description: >
            Die Komponente, der diese Auswahl gehört. Derselbe Punkt kann so auf
            zwei baugleichen Geräten hinter einer Box getrennt beobachtet
            werden. Ohne den Parameter gehört die Auswahl dem Gerät.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [expectedRevision, idempotencyKey, enabled]
              properties:
                expectedRevision: { type: integer, format: int64, minimum: 0 }
                idempotencyKey: { type: string, format: uuid }
                enabled: { type: boolean }
                cadenceS: { type: integer, minimum: 1, maximum: 86400, nullable: true }
      responses:
        "200":
          description: >
            Soll-Zustand nach der Änderung, auf die Komponente zugeschnitten,
            wenn eine genannt wurde.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MeasurementSelectionState" }
        "400":
          description: >
            Unbekannter oder nicht lesbarer Punkt, unzulässige Kadenz oder
            fehlender Idempotenzschlüssel.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Gerät oder Komponente nicht gefunden (RLS). }
        "409":
          description: >
            Veraltete Revision, oder derselbe Idempotenzschlüssel für eine
            andere Änderung.
        "422": { description: Technisches Messwertbudget überschritten. }
  /api/v1/devices/{deviceId}/measurement-selection/{pointKey}/history:
    get:
      tags: [measurements]
      summary: Verlauf EINES Messpunktes, semantisch aggregiert.
      description: >
        Bis 48 h wird qualitätsgefiltertes Raw gebucketet, längere Zeiträume
        lesen die dauerhaften 5-/15-min-Rollups. Gauge liefert gewichtetes
        Mittel/Min/Max, Counter nur positive Deltas (Resets zählen nicht als
        Verbrauch), Zustände den letzten Wert. `representation=raw` gibt es nur,
        wo echte Rohdaten vorliegen, sonst 400.
      parameters:
        - name: deviceId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: pointKey
          in: path
          required: true
          schema: { type: string }
        - name: range
          in: query
          required: false
          schema: { type: string, default: "24h" }
          description: >
            `24h`, `7d`, `30d`, `90d`, `year` oder frei über from/to.
        - name: from
          in: query
          required: false
          schema: { type: string, format: date-time }
        - name: to
          in: query
          required: false
          schema: { type: string, format: date-time }
        - name: representation
          in: query
          required: false
          schema: { type: string, enum: [decoded, raw], default: decoded }
        - name: siteId
          in: query
          required: false
          schema: { type: string, format: uuid }
          description: >
            Der historische Standort nach einem Geräte-Umzug; ohne Angabe der
            aktuelle.
        - name: entityId
          in: query
          required: false
          schema: { type: string, format: uuid }
          description: >
            Die Komponente, deren Familienwechsel als Marker mitreisen sollen.
            Sie muss an der abgefragten Anlage stehen - dieselbe Regel wie bei
            einer Auswahl je Komponente.
      responses:
        "200":
          description: >
            Meta, Datenpunkte und Marker (Auswahl-, Lücken-, Reset-, Zustands-
            und Familienwechsel).
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        "400":
          description: >
            Unbekannter Zeitraum, oder für diesen Zeitraum liegen keine echten
            Rohdaten vor.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404":
          description: >
            Gerät, Anlage, Komponente oder Messwert nicht gefunden (RLS).
  /api/v1/devices/{deviceId}/measurement-selection/{pointKey}/export:
    get:
      tags: [measurements]
      summary: Derselbe Verlauf als CSV.
      description: >
        Trägt Punkt-Schlüssel, Standort, Labels, Einheit, Aggregation, Semantik,
        Katalogfassung und Darstellung im Kopf und neutralisiert Formel-Präfixe
        von Tabellenkalkulationen. Recht `export.standort` (UEMS AP-12 IP-10, E12 G1,
        durchgesetzt über uems/BestandGeraeteCsv): ein fremdes Gerät bleibt 404, wer das
        Recht nicht hat (heute die VoltPilot-Unterstützung), bekommt 403 statt der Datei.
        Maschinenform (UTC `Z`, Punkt, Komma); seit AP-12 IP-10 (E11 DA4) additiv neun
        Kopfzeilen direkt hinter `# catalog_version_gespeichert=`: `zeitraum_von`,
        `zeitraum_bis`, `erzeugt_am`, `erzeugt_von`, `zeitzone="UTC"`, `dezimal="."`,
        `trenner=","`, `standort`, `unternehmen` (leer, wo es kein Objekt gibt) - jede
        Kopfzeile davor, die Spalten und die Zeilen bleiben Byte für Byte.
      parameters:
        - name: deviceId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: pointKey
          in: path
          required: true
          schema: { type: string }
        - name: range
          in: query
          required: false
          schema: { type: string, default: "24h" }
        - name: from
          in: query
          required: false
          schema: { type: string, format: date-time }
        - name: to
          in: query
          required: false
          schema: { type: string, format: date-time }
        - name: representation
          in: query
          required: false
          schema: { type: string, enum: [decoded, raw], default: decoded }
        - name: siteId
          in: query
          required: false
          schema: { type: string, format: uuid }
        - name: entityId
          in: query
          required: false
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: CSV.
          content:
            "text/csv":
              schema: { type: string }
        "400":
          description: >
            Unbekannter Zeitraum, oder für diesen Zeitraum liegen keine echten
            Rohdaten vor.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: >
            Recht `export.standort` fehlt (UEMS AP-12 IP-10) - mit dem Satz der
            Rechte-Ableitung; die VoltPilot-Unterstützung bekommt nie eine Datei.
        "404":
          description: >
            Gerät, Anlage, Komponente oder Messwert nicht gefunden (RLS), oder der
            Standort der Anlage liegt außerhalb des Geltungsbereichs der Person.
  /api/v1/devices/claim:
    post:
      tags: [devices]
      summary: Claim an edge device into a site (onboarding = one insert).
      description: >
        Claims an unclaimed edge device (identified by `externalRef`) into one of
        the caller's sites. `externalRef` is canonicalized before use: trimmed,
        and sticker Geräte-IDs (prefix `VP-`, case-insensitive) uppercased, so
        the typed case never creates a second device. Sticker IDs additionally
        validate against the provisioned-device registry (manufactured IDs,
        maintained via the admin API): an unknown sticker ID yields 422 instead
        of creating a ghost device, and the claimed device inherits the
        registry's `kind`. Non-sticker refs stay ungated. The claim is idempotent
        within the tenant: re-claiming a ref the caller already owns returns 200
        with the existing device (at its original site) instead of a conflict.
        `externalRef` is globally unique, so a device claimed by ANOTHER tenant -
        which RLS hides - yields 409. The target site must belong to the caller's
        tenant, else 404.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DeviceClaimRequest" }
      responses:
        "200":
          description: Already claimed by the caller's own tenant - the existing device.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Device" }
        "201":
          description: Device claimed.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Device" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Target site not found for this tenant. }
        "409": { description: Device already claimed by another tenant. }
        "422": { description: Unknown sticker Geräte-ID (not in the provisioned-device registry). }
  /api/v1/sites/{siteId}/telemetry:
    get:
      tags: [telemetry]
      summary: Query telemetry timeseries for a site.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: from, in: query, required: false, schema: { type: string, format: date-time } }
        - { name: to, in: query, required: false, schema: { type: string, format: date-time } }
      responses:
        "200":
          description: >
            Telemetry points (tenant-scoped via RLS). `from`/`to` default to the
            last 24h. Windows longer than 3h are downsampled server-side to
            1-minute averages so the series stays complete AND current for chatty
            devices; when the row cap would bite, the oldest points are dropped,
            never the newest.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/TelemetryPoint" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/prices:
    get:
      tags: [market]
      summary: Day-ahead spot prices (15-min slots) for a site's bidding zone.
      description: >
        Returns the day-ahead electricity spot prices for the site's bidding zone
        (DE-LU). Prices are market-wide PER ZONE (not per tenant), but the endpoint
        is site-scoped so the portal reads "the price for this site": the site is
        resolved via RLS (a foreign site is 404), then its zone's series is
        returned. Collected KEYLESS from energy-charts.info (Fraunhofer ISE), with
        ENTSO-E as an alternative source. `from`/`to` default to roughly
        today+tomorrow. Implemented in services/api.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: from, in: query, required: false, schema: { type: string, format: date-time } }
        - { name: to, in: query, required: false, schema: { type: string, format: date-time } }
      responses:
        "200":
          description: Price series for the site's zone.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PriceSeries" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/price-history:
    get:
      tags: [market]
      summary: Day-ahead prices aggregated over one range (Tag/Woche/Monat/Jahr).
      description: >
        Day-ahead spot prices for the site's bidding zone, aggregated over one
        historical period, mirroring the Historie period pattern. `at` picks the
        period containing that date; boundaries are Europe/Berlin. Aggregation
        happens in SQL (TimescaleDB time_bucket): 15-min buckets for the day,
        hourly for the week, Europe/Berlin-daily for month/year - so a year is
        ~365 rows, not ~35k slots. Each bucket carries avg/min/max (the band the
        chart draws), and the response includes range summary stats (avg/min/max,
        cheapest & most expensive slot, slot count, actual data coverage). The
        day range anchored on today extends `to` into tomorrow so the
        forward-looking day-ahead curve stays visible; past days show one day.
        Prices are market-wide per zone (no tenant); the site is resolved via RLS
        (foreign site => 404). Because the collector only fetches today+tomorrow,
        historical depth grows one day per collector day - empty/partial windows
        are expected and returned as empty bucket lists. Implemented in
        services/api.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - name: range
          in: query
          required: false
          schema: { type: string, enum: [day, week, month, year], default: day }
        - name: at
          in: query
          required: false
          description: Any ISO date (YYYY-MM-DD) inside the wanted period (Europe/Berlin); defaults to today.
          schema: { type: string, format: date }
      responses:
        "200":
          description: Aggregated price series for the range (empty buckets if no data).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PriceHistory" }
        "400": { description: Invalid range value. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/weather:
    get:
      tags: [weather]
      summary: Latest weather forecast (hourly) for a site.
      description: >
        Returns the most recent weather-forecast run for the site (temperature,
        cloud cover and shortwave/GHI radiation useful for PV), hourly over the
        coming days. The `weather_forecast` table carries `tenant_id` and is
        RLS-scoped, and the site lookup is RLS-gated, so a foreign site is 404 and
        no other tenant's weather can leak. Collected KEYLESS from Open-Meteo
        (EU-hosted). Implemented in services/api.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Latest weather forecast (empty points array if none stored yet).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/WeatherForecast" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/schedule:
    get:
      tags: [schedules]
      summary: Current 24h optimization schedule (15-min slots) for a site.
      description: >
        Two readings of the same hypertable, selected by `mode`.


        `mode=latest` (DEFAULT, unchanged): the latest optimizer run - what the
        device is executing right now. Per-slot planned battery power (+charge /
        -discharge), projected grid power and SoC, the day-ahead price and the
        projected cost vs. the no-battery baseline, plus the headline savingsEur
        over the horizon. Empty slots array (null metadata) when no plan has
        been produced yet.


        `mode=day` ("wie der Tag geplant war", Tages-Splice): per 15-min slot of
        the Europe/Berlin day the value from the NEWEST run that planned the
        slot BEFORE it began - the plan that was in force when the quarter hour
        started. The per-slot winner uses the same DISTINCT-ON splice the
        savings math has always used. Slots that had not started yet resolve to
        the newest run, so the tail of the response IS the current plan
        (including its slots on the following day). A slot no run planned
        ex-ante simply has no row, so a day whose optimizer only started at noon
        honestly begins at noon. Run-level facts describe ONE run and are
        therefore null on a spliced day (planId, bankedValueEur, socStartPct,
        socEndPct, peakTargetKw, fallback14a); generatedAt/deviceId name the
        newest run that contributed a slot, and savingsEur sums the spliced
        slots. Feeds the portal's "Film des Tages", which ticks off the elapsed
        phases as PLANNED (never as measured).


        Both modes are RLS-scoped by tenant and carry the recomposed decision
        prices (importPriceCtKwh / exportValueCtKwh / importPriceSource).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - name: mode
          in: query
          required: false
          description: Which reading of the plan; unknown values are rejected (400).
          schema: { type: string, enum: [latest, day], default: latest }
      responses:
        "200":
          description: The site's current plan (empty slots array if none yet).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Schedule" }
        "400": { description: Unknown mode. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/control-status:
    get:
      tags: [schedules]
      summary: Latest inverter-control confirmation for a site ("Steuerung").
      description: >
        What the schedule commanded vs. what the inverter read back for this
        site's device(s), ingested from the additive `control` block of the
        device status heartbeat (report §5). Feeds the portal's calm "Steuerung"
        strip ("Fahrplan-Sollwert X -> Wechselrichter bestätigt Y", healthy |
        mismatch | stale). RLS-scoped by tenant. 204 (no body) until a device has
        reported a control readback.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The site's latest control confirmation.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ControlStatus" }
        "204": { description: No control readback reported yet for this site. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/curtailment-status:
    get:
      tags: [schedules]
      summary: Latest feed-in curtailment ("Abregeln") truth for a site.
      description: >
        Does the plant actually throttle its PV in a planned "Abregeln" slot, or
        is the plan a plan because the curtailment actor is not released yet?
        Ingested from the additive `curtailment` block of the device status
        heartbeat. Deliberately its OWN read next to `control-status` rather than
        extra fields there: the two heartbeat blocks arrive independently (a
        device may report either alone) and each carries its own freshness
        anchor. RLS-scoped by tenant. 204 (no body) until a device has reported
        the block - the portal then keeps its plan wording, which is exactly the
        honest fallback.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The site's latest curtailment truth.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CurtailmentStatus" }
        "204": { description: No curtailment block reported yet for this site. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/sources:
    get:
      tags: [sites]
      summary: The site's measurement points as the edge reports them.
      description: >
        The primary inverter plus every configured additional source
        (Erzeuger/Netz-Zähler/Verbraucher) with its OWN latest reading and
        freshness, ingested from the additive `sources` block of the device
        status heartbeat. It makes a multi-inverter site's composite PV
        explainable in the portal ("39,0 kW = Deye 8,3 + Fronius 21,3 + …")
        instead of one opaque number. Display-only - the composite telemetry
        path is untouched. RLS-scoped by tenant; an empty list means no device
        has reported the block yet.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The site's reported measurement points (primary first).
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SiteSource" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/charging-config:
    get:
      tags: [sites]
      summary: Die im Portal gepflegte Lastmanagement-Konfiguration.
      description: >
        Die Anschlussgrenze der Anlage und die Vorrang-Wahl je Ladesäule
        (Lastmanagement Stufe 3). `gridLimitKw: null` heißt „noch nicht
        gepflegt" - und dann gibt die Box keine Ladeleistung frei; das ist der
        ehrliche Zustand einer Anlage, die niemand eingerichtet hat, nie eine
        erfundene Grenze. RLS-gefenced (fremde Anlage 404).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Die gepflegte Konfiguration (leer, solange nichts gepflegt ist).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ChargingConfig" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
    put:
      tags: [sites]
      summary: Anschlussgrenze und Vorrang speichern (PATCH-Semantik).
      description: >
        Speichert, was der Kunde über SEINEN Anschluss entscheidet, und schickt
        es als retained Dokument an die Box (Kontrakt
        `mqtt-charging-config.schema.json`). GERECHNET und DURCHGESETZT wird
        dort - die Anschlussgrenze ist eine physische Grenze, ihr Wächter darf
        nicht am WAN hängen; es entsteht kein zweiter Verteiler und kein
        Schreibpfad auf eine Ladesäule.


        PATCH-Semantik: ein ABWESENDES Feld behält den gespeicherten Wert (ein
        Dialog, der nur den Vorrang stellt, darf die Anschlussgrenze nicht
        löschen); eine LEERE Vorrang-Liste ist dagegen eine Aussage („keine
        Säule hat Vorrang"). Eine Grenze ≤ 0 und eine unbekannte ChargePointId
        sind 400 mit deutschem Grund - beides würde sonst still zu einer
        Einstellung, die niemand wollte.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                gridLimitKw:
                  type: number
                  nullable: true
                  description: "Die Anschlussgrenze in kW (> 0). Abwesend = unverändert."
                priorityChargePointIds:
                  type: array
                  nullable: true
                  description: "Die Vorrang-Säulen. Abwesend = unverändert, leer = ausdrücklich keine."
                  items: { type: string }
                surplusPolicy:
                  type: string
                  nullable: true
                  enum: [nur_sonne, sonne_zuerst, schnell]
                  description: >
                    Die Überschuss-Priorität (Stufe 4): woher der Ladestrom
                    kommen soll. Sie ändert KEINE Grenze - die zwei Bahnen
                    komponieren most-restrictive-wins. Abwesend = unverändert;
                    das ist NICHT dasselbe wie `schnell` (eine eigene Aussage).
                storagePriority:
                  type: string
                  nullable: true
                  enum: [speicher_vor_auto, auto_vor_speicher]
                  description: >
                    Wer den Sonnenüberschuss zuerst bekommt. `auto_vor_speicher`
                    klemmt dafür auf der Box die Ladung des Speichers auf den
                    Rest - sonst wäre die Wahl nur ein Wunsch.
      responses:
        "200":
          description: Die gespeicherte Konfiguration.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ChargingConfig" }
        "400": { description: Unplausible Grenze oder unbekannte Ladesäule (deutscher Grund im Körper). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/charging-config/charge-points:
    post:
      tags: [sites]
      summary: Eine Ladesäule eintragen (Anbinde-Assistent, Schritt 1).
      description: >
        Trägt EINE ChargePointId in die Allowlist der Anlage ein und schickt sie
        als retained Dokument an die Box (Kontrakt
        `mqtt-charging-config.schema.json`). Bis hierher liess sich die
        Allowlist nur am Gerät selbst pflegen (`:8484`), also musste jemand im
        LAN am Gerät stehen, um eine Säule anzubinden.


        DIE ALLOWLIST BLEIBT, NUR IHR PFLEGE-ORT WANDERT. Eine unbekannte
        Kennung wird von der Box weiterhin abgewiesen und protokolliert; es
        entsteht KEIN Anlern-Fenster, in dem eine fremde Säule hereinkäme.


        Es ist ein POST, kein PUT, und das ist die Aussage: die Liste fügt nur
        hinzu. Ein PUT lüde dazu ein, sie als Ganzes zu setzen - und damit einen
        Eintrag durch WEGLASSEN zu entfernen. Eine Kennung zurückzunehmen ist
        eine eigene, ausdrückliche Handlung (DELETE auf genau diese Kennung).
        Dieselbe Kennung erneut einzutragen ist ein No-op, das nur die
        Betreiber-Angaben auffrischt - und eine zurückgenommene Kennung wird
        dadurch WIEDER eingetragen. RLS-gefenced (fremde Anlage 404).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [chargePointId]
              properties:
                chargePointId:
                  type: string
                  maxLength: 64
                  description: >
                    Die OCPP-ChargePointId, zeichengleich wie sie in der Säule
                    eingetragen ist. Erlaubt sind Buchstaben, Ziffern, Punkt,
                    Bindestrich und Unterstrich - dieselbe Prüfung, die die Box
                    an ihrem eigenen Formular führt.
                label: { type: string, nullable: true, maxLength: 120, description: "Der Name für den Kunden; abwesend = die Box nimmt die Kennung." }
                ratedKw: { type: number, nullable: true, description: "Nennleistung je Stecker, sofern bekannt. Abwesend = unbekannt, nie 0." }
                connectors: { type: integer, nullable: true, description: "Steckerzahl, sofern bekannt. Abwesend = die Box lernt sie von der Säule." }
                deviceId: { type: string, format: uuid, nullable: true, description: "Die Box, mit der sich die Säule verbinden soll. Bei mehreren Boxen Pflicht; bei einer Ein-Box-Anlage darf ein älterer Client das Feld weglassen." }
                source: { type: string, nullable: true, enum: [nur_sonne, sonne_zuerst, schnell], description: "Die STEUERART dieser Säule (P5) - woher IHR Ladestrom kommen soll. Abwesend = für sie äußert sich das Portal nicht und es gilt der ANLAGEN-STANDARD (surplusPolicy); nie \"schnell\", das wäre eine Netzstrom-Freigabe, die niemand erteilt hat." }
                minKw: { type: number, nullable: true, description: "Ab welcher Leistung diese Säule überhaupt anfängt. Abwesend = unbekannt, nie 0." }
      responses:
        "200":
          description: Die gespeicherte Konfiguration inklusive der Allowlist.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ChargingConfig" }
        "400": { description: Unzulässige Kennung, unplausible Angabe oder Deckel erreicht (deutscher Grund im Körper). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
        "422": { description: Box fehlt, gehört nicht zur Anlage oder eine andere Box führt bereits Ladepunkte (deutscher Grund im Körper). }
  /api/v1/sites/{siteId}/charging-config/charge-points/{chargePointId}:
    delete:
      tags: [sites]
      summary: Eine eingetragene Ladepunkt-Kennung zurücknehmen.
      description: >
        Nimmt GENAU EINE ChargePointId aus der Allowlist der Anlage und schickt
        die Rücknahme als retained Dokument an die Box (Feld
        `removed_charge_point_ids` im Kontrakt
        `mqtt-charging-config.schema.json`).


        DIE FOLGE AM GERÄT: die Säule wird getrennt und ein Wiederverbinden
        abgewiesen. Ihr zuletzt hinterlegtes Sicherheitsprofil behält sie - es
        liegt IN der Säule -, ein laufender Ladevorgang endet dadurch also
        nicht, er fällt auf dieses Profil zurück.


        SIE IST EIN GRABSTEIN, KEIN LÖSCHEN: das retained Dokument wird als
        Ganzes ersetzt, also würde eine Kennung nur wegzulassen von einer Box,
        die gerade offline war, nie gesehen (`charge_points` fügt nur hinzu).
        Die Rücknahme wird deshalb dauerhaft geführt und in JEDEM folgenden
        Dokument genannt, bis die Kennung wieder eingetragen wird. Eine ÄLTERE
        Box kennt das Feld nicht und behält die Kennung - der Vorzustand, nie
        eine falsche Handlung; die Rücknahme wirkt dort erst nach dem nächsten
        Edge-Release. RLS-gefenced (fremde Anlage 404).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: chargePointId, in: path, required: true, schema: { type: string, maxLength: 64 } }
      responses:
        "200":
          description: Die gespeicherte Konfiguration ohne diese Kennung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ChargingConfig" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Anlage unbekannt, oder diese Kennung ist hier nicht eingetragen (deutscher Grund im Körper). }
  /api/v1/sites/{siteId}/charging-config/charge-points/{chargePointId}/source:
    put:
      tags: [sites]
      summary: Die Steuerart EINER Ladesäule setzen (Verbrauchsmanagement v1 / P5).
      description: >
        Setzt QUELLE und Mindestleistung GENAU EINER eingetragenen Säule und
        schickt sie als retained Dokument an die Box
        (`charge_points[].source` / `.min_kw` im Kontrakt
        `mqtt-charging-config.schema.json`).


        SIE ÄNDERT KEINE GRENZE. Die Quelle sagt, WOHER der Ladestrom dieser
        Säule kommen soll; die physische Bahn (Anschlussgrenze,
        Sicherheitsabstand, §14a, Ausfall-Profil) bindet sie unverändert - die
        zwei komponieren most-restrictive-wins, und keine kann die andere
        aufweichen.


        DER ANLAGEN-STANDARD BLEIBT DANEBEN STEHEN (`surplusPolicy` auf
        `PUT /charging-config`): eine Säule OHNE eigene Wahl folgt ihm weiter.
        Beides zu verschmelzen hiesse, eine Wahl für alle zu treffen.


        PATCH-Semantik: ein abwesendes Feld behält den gespeicherten Wert; ein
        Rumpf, der zu beidem schweigt, ist eine benannte Ablehnung. Eine
        Kennung, die diese Anlage nicht (mehr) führt, ist 404 - das Eintragen
        ist eine eigene, bewusste Handlung. RLS-gefenced (fremde Anlage 404).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: chargePointId, in: path, required: true, schema: { type: string, maxLength: 64 } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                source: { type: string, nullable: true, enum: [nur_sonne, sonne_zuerst, schnell] }
                minKw: { type: number, nullable: true }
      responses:
        "200":
          description: Die gespeicherte Konfiguration inklusive der Allowlist.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ChargingConfig" }
        "400": { description: Unbekannte Quelle, unplausible Mindestleistung oder ein leerer Rumpf (deutscher Grund im Körper). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Anlage unbekannt, oder diese Kennung ist hier nicht eingetragen. }
  /api/v1/sites/{siteId}/charging-frame:
    put:
      tags: [sites]
      summary: Die Anschlussgrenze mit Netzanschluss-Plausibilität setzen (AP-01 IP-13).
      description: >
        Setzt die Anschlussgrenze erst, wenn sie die heute am Netzanschluss
        vereinbarte Leistung nicht überschreitet und Grundlast der letzten
        sieben Tage plus Hausreserve ein positives Ladebudget lassen. Der
        Ladepark-Rahmen der Box bleibt unverändert.


        Ist heute kein Netzanschluss gebunden, muss `vereinbartKw` als
        Übergang im Dialog angegeben werden. Bei einer Bindung wird dieser
        Eingabewert ignoriert und ausschließlich `vereinbart_kw` des
        gebundenen Netzanschlusses verwendet. Recht `grenze.eintragen`,
        RLS-gefenced; eine fremde Anlage ist 404.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [gridLimitKw]
              properties:
                gridLimitKw: { type: number, exclusiveMinimum: 0, description: "Anschlussgrenze des Ladeparks in kW." }
                vereinbartKw: { type: number, nullable: true, exclusiveMinimum: 0, description: "Nur ohne gebundenen Netzanschluss: vereinbarte Leistung aus dem Dialog." }
      responses:
        "200":
          description: Die gespeicherte Konfiguration; der Box-Push verwendet unverändert den bestehenden Vertrag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ChargingConfig" }
        "400": { description: Anschlussgrenze oder Übergangswert sind keine positive Leistung. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Das Recht `grenze.eintragen` fehlt. }
        "404": { description: Anlage unbekannt oder außerhalb des Kundenbereichs. }
        "422": { description: Grenze über vereinbarter Leistung, fehlende Anschlussdaten oder kein verbleibendes Ladebudget; deutscher Grund im Körper. }
  /api/v1/admin/sites/{siteId}/gemeinsame-steuerung:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [admin]
      summary: Das Betreiber-Blatt der Gemeinsamen Steuerung lesen (AP-15 IP-24, §5.3/§5.4).
      description: >
        Recht `plattform.betrieb` (nur Plattform-Rolle; ein Kundenkonto bekommt 403), lesend. Je Box, was nur der
        Betreiber sieht: Fähigkeit (`gemeldet` · `versions_tabelle` · `fehlt`), Messpunkt und sein Alter, Wächter-Stufe
        je Richtung aus dem Herzschlag-Block, `plan_id` veröffentlicht/angenommen, Anteils-Revision gesendet/quittiert
        und die WIRKSAMEN Anteile, wie die Box sie meldet; dazu der Zweischritt des jüngsten Anteils-Dokuments und alle
        Sprungprobe-Protokolle mit den Messungen je Sprung sowie die benannten Steckerproben mit ihrem Grenz-Nachweis.
        Unbekannt ist nie eine Null — was die Box nicht meldet, ist
        `null` bzw. `nicht_gemeldet`. Zustand, `fehlt`, Bilanz und Vorbehalt stehen in `GET …/sites/{siteId}/gemeinsame-steuerung`,
        Geräte, Rückfall und Auslegung in `GET …/gemeinsame-steuerung/einrichten`. Ohne Gemeinsame Steuerung leer;
        eine fremde Anlage ist 404.
      responses:
        "200":
          description: Das Blatt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerungBetreiberblatt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/admin/sites/{siteId}/gemeinsame-steuerung/anteil-verlust:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [admin]
      summary: Pilot-Bericht des Anteils-Verlusts — Untergrenze der Boxen und Schätzung der Cloud (AP-15, Folgepaket zu IP-22, E1).
      description: >
        Recht `plattform.betrieb` (nur Plattform-Rolle; ein Kundenkonto bekommt 403), lesend, über den
        `X-Tenant-Id`-Umschalter auf dem RLS-Pfad. Je Box und in `summe` über [`von`, `bis`] (Tage der Anlage,
        Europe/Berlin, beide zählen mit, höchstens 366): `verlust_kwh` die Summe der UNTERGRENZE, die die Boxen melden,
        und GETRENNT `schaetzung_kwh`, die Summe der Schätzung der Cloud (PV-Prognose der Anlage × kWp-Teil der Box −
        gemessene PV der Box in der gebundenen Zeit) über die `tage_geschaetzt` Tage, die eine haben;
        `tage_ohne_prognose` und `tage_nicht_gerechnet` nennen, was in dieser Summe fehlt. Die Zahl, an der nach dem
        Pilot über die Zuteilung auf Zeit (S4) entschieden wird. Ohne gemeldeten Tag (auch ohne Gemeinsame Steuerung)
        `boxen` leer und `summe` null — nie 0 kWh. Eine fremde Anlage ist 404, ein unlesbarer oder zu langer
        Zeitraum 400 `anfrage_ungueltig`.
      parameters:
        - { name: von, in: query, required: true, schema: { type: string, format: date } }
        - { name: bis, in: query, required: true, schema: { type: string, format: date } }
      responses:
        "200":
          description: Der Bericht.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerungPilotBericht" }
        "400": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/admin/sites/{siteId}/gemeinsame-steuerung/scharfschalten:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [admin]
      summary: Gemeinsame Steuerung scharfschalten — Handgriff des Betreibers (UEMS AP-15 IP-5, I4/I5, W9).
      description: >
        Recht `plattform.betrieb` (nur Plattform-Rolle; ein Kundenkonto, auch der Kundenadministrator, bekommt 403).
        Über den `X-Tenant-Id`-Umschalter auf dem RLS-Pfad, eine fremde Anlage ist 404. Prüft ALLE Bedingungen aus I1
        und lehnt mit genau einem Wort ab (dem ersten in Vokabular-Reihenfolge; `fehlt` nennt alle):
        `box_nicht_in_anlage` · `kein_netzanschluss` · `grenze_fehlt` · `faehigkeit_fehlt` · `nachweis_fehlt` ·
        `auslegung_passt_nicht` · `fuehrende_box_misst_nicht` · `vorgabe_signal_nicht_an_jeder_box` ·
        `mitsteuernde_box_misst_nicht`. Zulässig: neue Epoche, S3 `anteile_aktiv`, alle wirksamen Mitglieder
        bestätigt. Bis IP-7/IP-17/IP-21 ihre Quellen liefern, fehlen Fähigkeit, Sprungprobe und Auslegung immer.
      responses:
        "200":
          description: Scharf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerung" }
        "400": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "409": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/admin/sites/{siteId}/gemeinsame-steuerung/fortsetzen:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [admin]
      summary: Gemeinsame Steuerung fortsetzen — auch nach einem Anhalten des Betreibers (W9/I5).
      description: >
        Recht `plattform.betrieb` (nur Plattform-Rolle). Wie das Fortsetzen des Kunden: nur aus `angehalten`
        (sonst 409 `nicht_angehalten`), dieselbe Epoche, alle Bedingungen aus I1 erneut geprüft (409 mit dem ersten
        Wort und `fehlt`). Der Betreiber hebt jedes Anhalten auf, auch das des Kunden.
      responses:
        "200":
          description: Fortgesetzt, `anteile_aktiv`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerung" }
        "400": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "409": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/admin/sites/{siteId}/gemeinsame-steuerung/mitglieder/{boxId}/bestaetigen:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: boxId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [admin]
      summary: Ein Mitglied der Gemeinsamen Steuerung bestätigen — nach dem Box-Tausch (I4, R17).
      description: >
        Recht `plattform.betrieb` (nur Plattform-Rolle). Setzt `bestaetigt_am`; 409 `kein_mitglied` für eine Box,
        die kein wirksames Mitglied ist, `bereits_bestaetigt` beim zweiten Mal, `nicht_eingerichtet` ohne
        Gemeinsame Steuerung.
      responses:
        "200":
          description: Bestätigt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerung" }
        "400": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "409": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/admin/sites/{siteId}/gemeinsame-steuerung/mitglieder/{boxId}/ausscheiden-bestaetigen:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: boxId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [admin]
      summary: Die Geräte einer ausscheidenden Box sind vom Netz — bestätigen (AP-15 §5.5, I4).
      description: >
        Recht `plattform.betrieb` (nur Plattform-Rolle). Setzt `vom_netz_bestaetigt_am` am ausscheidenden Mitglied:
        ihr Rückfall bleibt nicht reserviert, der Zielstand geht ohne ihre Quittung (R12 endet hier), sobald keine
        andere verengte Box mehr aussteht; danach endet die Mitgliedschaft. 409 `kein_mitglied`, `scheidet_nicht_aus`
        für eine Box, die nicht ausscheidet, `bereits_bestaetigt` beim zweiten Mal, `nicht_eingerichtet` ohne
        Gemeinsame Steuerung.
      responses:
        "200":
          description: Bestätigt; der Zustand danach.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerung" }
        "400": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "409": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/admin/sites/{siteId}/gemeinsame-steuerung/vorbehalt/freigeben:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [admin]
      summary: Den Vorschlag zum Senken des Vorbehalts freigeben (AP-15 IP-13, B4/G5).
      description: >
        Recht `plattform.betrieb` (nur Plattform-Rolle). Der tägliche Lauf „Vorbehalt aus Messwerten“ ERHÖHT den
        Vorbehalt der Bezugsseite selbsttätig (verengt nur); eine SENKUNG erweitert und bleibt ein Vorschlag
        (`vorbehalt.vorschlag` im GET) — erst diese Freigabe setzt ihn, danach der Zweischritt in derselben Epoche.
        409 `kein_vorschlag` ohne offenen Vorschlag (oder wenn er nicht mehr zum geltenden Vorbehalt passt),
        `nicht_eingerichtet` ohne Gemeinsame Steuerung. Eine Kundenroute zum Senken gibt es nicht; kein Körper.
      responses:
        "200":
          description: Freigegeben; der Vorbehalt ist gesenkt, der Zweischritt angestoßen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerung" }
        "400": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "409": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/admin/sites/{siteId}/gemeinsame-steuerung/sprungprobe:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [admin]
      summary: Die Sprungprobe an einer Box auslösen (AP-15 IP-21, E3 = A, T5, I4).
      description: >
        Recht `plattform.betrieb` (nur Plattform-Rolle; ein Kundenkonto bekommt 403). Die Box senkt EINE ihrer
        Stellgrößen zweimal für 60 s um höchstens `sprung_kw` (immer die sichere Richtung) und berichtet
        (`docs/contracts/v2/mqtt-sprungprobe.md`); die Cloud vergleicht mit dem Netzzähler der führenden Box
        (`telemetry.power_kw`, Sekundenwerte): Richtung und Größe ± max(10 %, 2 kW), innerhalb von 20 s, zweimal.
        Urteil im Protokoll und im GET je Mitglied (`sprungprobe`); die letzte bestandene Probe hebt S1 auf S2
        `geprueft`, jede Strukturänderung entwertet (I3). 409 `nicht_beobachtet` außerhalb von S1,
        `kein_mitglied`, `sprungprobe_nicht_gemeldet` (Fähigkeit `sprungprobe` fehlt), `sprungprobe_laeuft`
        (eine Probe zur Zeit je Anlage), `netzpunkt_nicht_frisch` (kein Wert der führenden Box in den letzten 60 s),
        `nicht_zugestellt` (die Box war nicht erreichbar; nichts gespeichert), `nicht_eingerichtet`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [box_id, art, sprung_kw]
              properties:
                box_id: { type: string, format: uuid }
                art: { type: string, enum: [erzeugung_senken, verbrauch_senken] }
                sprung_kw: { type: number, exclusiveMinimum: 0, maximum: 50 }
      responses:
        "200":
          description: Ausgelöst; der Auftrag ist zugestellt, das Urteil steht aus (`ausgeloest`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerungSprungprobe" }
        "400": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "409": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/admin/sites/{siteId}/gemeinsame-steuerung/steckerprobe:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [admin]
      summary: Eine Steckerprobe als benannten Zeitraum eintragen (AP-15 NW-8, I4).
      description: >
        Recht `plattform.betrieb` (nur Plattform-Rolle; ein Kundenkonto bekommt 403). Speichert den Handgriff des
        Betreibers und rechnet mit derselben Regel wie der Grenz-Nachweis über exakt `[von,bis)`, höchstens 31 Tage.
        Der Mandant kommt aus dem `X-Tenant-Id`-Umschalter, nie aus dem Körper; eine fremde Anlage ist 404.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [von, bis, box_id]
              properties:
                von: { type: string, format: date-time }
                bis: { type: string, format: date-time }
                box_id: { type: string, format: uuid }
                bemerkung: { type: string, maxLength: 500 }
      responses:
        "200":
          description: Eingetragen und mit dem aktuellen Grenz-Nachweis beantwortet.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerungSteckerprobe" }
        "400": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "409": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/admin/sites/{siteId}/charging-frame:
    put:
      tags: [admin]
      summary: Den Ladepark-RAHMEN setzen (Verbrauchsmanagement v1 / P5, E10).
      description: >
        Setzt Hausreserve, Sicherheitsabstand, Mindestleistung, Wechsel-Takt,
        höchste bekannte Gebäudelast und statisch/gemessen - den RAHMEN, den
        bis P5 nur `:8484` pflegen konnte.


        ADMIN-ONLY, mit Begründung: das sind Auslegungs-Zahlen des
        Anschlusses, die VoltPilot beim Einrichten misst (die Mockups nennen
        sie „Von VoltPilot eingerichtet"). Der Kunde LIEST sie seit P5 über
        `GET /api/v1/sites/{siteId}/charging-config`; die EINE Zahl, die ihm
        gehört, bleibt die Anschlussgrenze.


        Über den `X-Tenant-Id`-Umschalter auf dem RLS-Pfad - KEIN BYPASSRLS,
        eine fremde Anlage ist 404. PATCH-Semantik: ein abwesendes Feld behält
        den gespeicherten Wert, und ein Rumpf ohne einen einzigen Wert ist eine
        benannte Ablehnung. Die Plausibilität gegen die eigenen Säulen prüft am
        Ende die BOX (`lastmgmt.Settings.Apply`).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LadeparkRahmen" }
      responses:
        "200":
          description: Die gespeicherte Konfiguration inklusive des Rahmens.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ChargingConfig" }
        "400": { description: Unplausibler oder leerer Rahmen (deutscher Grund im Körper). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Kein Plattform-Admin. }
        "404": { description: Anlage im gewählten Mandanten nicht gefunden. }
  /api/v1/sites/{siteId}/charging-boost:
    post:
      tags: [sites]
      summary: Einen EINZELNEN Ladevorgang übersteuern - „Jetzt voll laden" oder „Laden pausieren".
      description: >
        Die Übersteuerung der Überschuss-Priorität für GENAU EINEN laufenden
        Ladevorgang (Lastmanagement Stufe 4). Sie geht als NICHT-retained
        Einmal-Nachricht an die Box (Kontrakt
        `mqtt-charging-boost.schema.json`) und mündet dort in denselben Kern wie
        die `:8484`-Taste - es gibt keinen zweiten Weg zu übersteuern.


        SIE IST KEINE GRENZE: sie erlaubt diesem einen Ladevorgang, auch
        Netzstrom zu ziehen. Anschlussgrenze, Sicherheitsabstand, §14a,
        Mindestleistung, Vorrang und das in der Säule hinterlegte Ausfall-Profil
        binden ihn unverändert, und die Priorität ALLER ANDEREN Ladevorgänge
        bleibt, wie sie ist. Sie gilt bis das Fahrzeug voll ist, längstens 4
        Stunden, und endet mit der Sitzung.


        `action` wählt die RICHTUNG (Verbrauchsmanagement v1 / P3b, Entscheid
        E5): `voll` (die Vorgabe, das oben Beschriebene) oder `pause` - „Laden
        pausieren", das GENAU DIESEN Ladevorgang auf 0 kW deckelt, während jeder
        andere unverändert weiterlädt. Der Deckel ist restrict-only: er kann
        keine Grenze weiten. ABWESEND = `voll`, also ist ein älterer Client
        zeichengleich wie vorher; ein unbekanntes Wort ist 400.


        `cancel: true` nimmt BEIDE Richtungen sofort zurück (auf jeder Fläche
        „Automatik fortsetzen"). Ein Stecker ohne laufenden Ladevorgang ist 409 -
        eine Zusage über ein Fahrzeug, das nicht da ist, wäre erfunden. Ein nicht
        erreichbarer Broker ist 503, und dann entsteht AUCH KEIN Beleg: die
        Nachricht IST die Freigabe. RLS-gefenced (fremde Anlage 404).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [chargePointId, connectorId]
              properties:
                chargePointId: { type: string, description: "Die OCPP-ChargePointId der Säule." }
                connectorId: { type: integer, minimum: 1, description: "Der Stecker (ab 1)." }
                minutes: { type: integer, minimum: 1, maximum: 240, nullable: true, description: "Dauer; abwesend = der 4-Stunden-Deckel. Ein größerer Wert wird geklemmt." }
                cancel: { type: boolean, description: "true nimmt eine laufende Übersteuerung zurück - gleich welcher Richtung." }
                action: { type: string, enum: [voll, pause], description: "Die Richtung; abwesend = voll." }
      responses:
        "200":
          description: Die erteilte (oder zurückgenommene) Übersteuerung, mit ihrem deutschen Satz.
          content:
            application/json:
              schema:
                type: object
                properties:
                  chargePointId: { type: string }
                  connectorId: { type: integer }
                  active: { type: boolean }
                  requestedAt: { type: string, format: date-time }
                  note: { type: string, description: "Was jetzt gilt - und was unverändert bleibt." }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Anlage, Säule oder Stecker unbekannt (deutscher Grund im Körper). }
        "400": { description: Unbekannte Art des Eingriffs (deutscher Grund im Körper). }
        "409": { description: An diesem Stecker läuft gerade kein Ladevorgang. }
        "503": { description: Die Box war nicht erreichbar - es entstand keine Freigabe. }
  /api/v1/sites/{siteId}/interventions:
    get:
      tags: [sites]
      summary: Die laufenden Handeingriffe der Anlage (Steuerung Stufe 4).
      description: >
        Der EINE Lesepfad der Jetzt-Zone: der Speicher-Eingriff („Ladestand
        halten" / „Speicher jetzt laden") und die „Automatik pausieren"-Sperre.
        Ein ABGELAUFENER Eingriff liest server-seitig als ABWESEND (§16) - hier
        kommt nie ein Geist an. RLS-gefenced wie jede `/sites/**`-Route (fremde
        Anlage 404, Admins über den `X-Tenant-Id`-Umschalter); eine leere Liste
        mit `automationPaused: false` ist der Normalzustand jeder Anlage.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Laufende Eingriffe + die Pause.
          content:
            application/json:
              schema:
                type: object
                properties:
                  automationPaused: { type: boolean }
                  pausedUntil:
                    type: string
                    format: date-time
                    nullable: true
                    description: Bis wann die Pause von Hand läuft; null = keine. Die Ruhe bis zum Start (UEMS AP-01 R0) ist kein Handeingriff und erscheint hier nicht.
                  interventions:
                    type: array
                    items:
                      type: object
                      properties:
                        kind:
                          type: string
                          enum: [speicher_halten, speicher_laden]
                        entityId: { type: string, format: uuid }
                        targetValueKw: { type: number, nullable: true }
                        endsAt: { type: string, format: date-time }
                        createdBy: { type: string, nullable: true }
                        createdAt: { type: string, format: date-time }
                        urheber:
                          allOf: [{ $ref: "#/components/schemas/ProtokollUrheber" }]
                          nullable: true
                          description: AP-03 IP-7 - wer den Eingriff gesetzt hat, im Akteur-Vokabular; null für einen Eingriff von vor dieser Fassung.
                        etikett:
                          type: string
                          nullable: true
                          description: >
                            AP-03 IP-9 (E15) - der Satz der Jetzt-Zone: „gesetzt von Murat Demirci" und, wenn
                            dessen Bedienrecht inzwischen endete, „… (Bedienrecht beendet am 14.11.2026 09:02)".
                            Der Eingriff selbst bleibt davon unberührt und wirkt bis zu seinem Ende - ein Entzug
                            schaltet nie. null ohne bekannten Urheber.
        "404": { description: Anlage nicht gefunden (auch bei fremdem Mandanten). }
  /api/v1/sites/{siteId}/battery-override:
    post:
      tags: [sites]
      summary: „Speicher jetzt laden" / „Ladestand halten" (Captain-Entscheid S1 = A).
      description: >
        Ein TTL-gebundener Handeingriff am Speicher. Es gibt KEIN neues
        Kommando: „halten" ist `setpoint_kw = 0`, „laden" ein positiver
        Sollwert - beides reist über den BESTEHENDEN `v2/desired`-Weg und wird
        auf dem Gerät durch die BESTEHENDE Guard-Kette geklemmt. Ob dabei aus
        dem NETZ geladen werden darf, entscheidet deshalb die Box gegen ihre
        Registry-Angabe `charge_from_grid_allowed` (D-8) - nicht diese Route.
        Eine Dauer oder Endzeit ist PFLICHT (400 sonst); über der 4-h-Kappe des
        Arbiters wird der Wunsch cloud-seitig ERNEUERT, nie der Vertrag gedehnt.
        Ein NEGATIVER Sollwert ist eine benannte Ablehnung, nie eine Klemmung -
        einen „jetzt entladen"-Eingriff gibt es nicht.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [kind]
              properties:
                kind:
                  type: string
                  enum: [speicher_halten, speicher_laden]
                durationMinutes: { type: integer, minimum: 15 }
                endsAt: { type: string, format: date-time }
                setpointKw:
                  type: number
                  description: >
                    Nur bei `speicher_laden`; ohne Angabe die volle
                    Ladeleistung. Auf die Nennleistung gekappt.
      responses:
        "200": { description: Der Eingriff ist notiert (und, wenn möglich, gesendet). }
        "400": { description: Ohne Dauer, mit unbekannter Art oder mit negativem Sollwert. }
        "404": { description: Anlage nicht gefunden. }
        "409": { description: Diese Anlage hat keinen steuerbaren Speicher. }
    delete:
      tags: [sites]
      summary: „Automatik fortsetzen" für den Speicher - wirkt sofort.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200": { description: Der Eingriff ist beendet. }
        "404": { description: Anlage nicht gefunden. }
  /api/v1/sites/{siteId}/automation-pause:
    post:
      tags: [sites]
      summary: „Automatik pausieren" - Fahrplan UND Regeln ruhen für die Dauer.
      description: >
        Die Sperre reist im RETAINED Registry-Push als ABSOLUTER Zeitpunkt
        (`automation_paused_until`), damit eine Box, die beim Ablauf offline
        war, sie nach ihrer EIGENEN Uhr aufheben kann. Auf dem Gerät fällt dann
        jede Komponente auf ihren Registry-Failsafe: der Speicher auf
        Eigenverbrauch, ein Gerät auf release/off. Was NICHT pausiert: Messen,
        die Guard-Kette, § 14a, die Abregelung und die Einspeise-Wache - sie
        liegen unterhalb der Arbitrierung.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                durationMinutes: { type: integer, minimum: 15 }
                endsAt: { type: string, format: date-time }
      responses:
        "200": { description: Die Pause läuft. }
        "400": { description: Ohne Dauer oder Endzeit. }
        "404": { description: Anlage nicht gefunden. }
        "409": { description: "Die Anlage ist in Ruhe, bis ihre Steuerung gestartet wird (UEMS AP-01 R0) - eine Pause mit Ende würde die Ruhe verkürzen; nichts wird geschrieben." }
    delete:
      tags: [sites]
      summary: „Automatik fortsetzen" für die ganze Anlage - wirkt sofort.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200": { description: Die Automatik läuft wieder. }
        "404": { description: Anlage nicht gefunden. }
        "409": { description: "Die Anlage ist in Ruhe, bis ihre Steuerung gestartet wird (UEMS AP-01 R0) - nur „Steuerung starten“ hebt sie auf." }
  /api/v1/sites/{siteId}/steuerung-vorschau:
    post:
      tags: [sites]
      summary: Was eine Entscheidung am Fahrplan ändert - in Euro (Steuerung Stufe 7).
      description: >
        Die KUNDEN-Tür zu derselben Rechen-Maschine wie
        `POST /api/v1/admin/sites/{siteId}/optimizer-what-if`: zwei Läufe über
        EINE frisch geholte Eingabe (die Anlage wie sie ist, und die Anlage
        unter dieser Entscheidung), und die Differenz ihres Netto-Vorteils.
        Es wird NICHTS festgeschrieben - kein Plan persistiert, kein MQTT
        veröffentlicht; das ist eine Eigenschaft des Rechendienstes (er
        importiert weder Persistenz noch einen Publisher, per Import-Graph-Test
        festgenagelt).


        Vier Unterschiede zur Admin-Tür: der Zaun ist RLS (fremde Anlage 404,
        nie 403, und die Id erreicht den Rechendienst erst danach); der Rumpf
        trägt NUR die drei Kunden-Knöpfe (Verschleiss, SoC-Band und Netzladen
        existieren hier gar nicht); ein Deckel antwortet mit einem deutschen
        Satz, bevor der teure Lauf beginnt; und die Antwort ist EINE Zahl statt
        zweier voller Pläne.


        `deltaEur` zeigt aus KUNDENSICHT - negativ = die Entscheidung kostet.
        Es ist IMMER eine Näherung (`naeherung: true`): gerechnet wird über den
        Fahrplan-Horizont, nicht über die Lebensdauer einer Regel. `null` heisst
        „keine Zahl", und `grund` sagt warum - entweder nicht berechenbar oder
        (ein ANDERER Fall) berechnet und ohne sichtbaren Unterschied.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: false
        description: >
          Jedes Feld ist optional; `null` heisst „lass es, wie die Anlage es
          hat". Ein LEERER Rumpf rechnet die Anlage zweimal gleich und ist damit
          die ehrliche Aussage „das ändert nichts". Ein Verbraucher-Fenster
          braucht Beginn, Länge und Leistung ZUSAMMEN - halb gefüllt ist 400,
          nie ergänzt.
        content:
          application/json:
            schema:
              type: object
              properties:
                socFloorNow:
                  type: boolean
                  description: >
                    „Ladestand halten" - der Speicher geht nicht unter seinen
                    jetzigen Stand. Ein BODEN, keine Einfrierung: laden und
                    wieder bis auf diesen Stand entladen bleibt erlaubt, die
                    Vorschau unterschätzt die Wirkung des Handeingriffs also
                    eher, als sie zu überschätzen.
                forcedChargeSlots:
                  type: integer
                  minimum: 1
                  maximum: 192
                  description: >
                    „Speicher jetzt laden" - so viele Viertelstunden lädt er.
                    Auf den Kopfraum des Speichers gekappt; ein bereits voller
                    Speicher ist eine benannte Ablehnung, keine unlösbare
                    Vorschau.
                verbraucherAbSlot: { type: integer, minimum: 0, maximum: 191 }
                verbraucherSlots: { type: integer, minimum: 1, maximum: 192 }
                verbraucherKw: { type: number, minimum: 0.1 }
      responses:
        "200":
          description: Die Vorschau - eine Zahl, oder ein Grund, warum es keine gibt.
          content:
            application/json:
              schema:
                type: object
                properties:
                  deltaEur:
                    type: number
                    nullable: true
                    description: variante − basis; negativ = es kostet. null = keine Zahl.
                  basisEur: { type: number, nullable: true }
                  varianteEur: { type: number, nullable: true }
                  horizonSlots: { type: integer, nullable: true }
                  naeherung: { type: boolean, description: immer true }
                  grund: { type: string, nullable: true }
        "400": { description: Ein halb gefülltes Verbraucher-Fenster oder ein krummer Wert. }
        "404": { description: Anlage nicht gefunden. }
        "409": { description: Diese Anlage hat keinen Speicher. }
        "429": { description: Zu viele Vorschauen - mit deutschem Grund, nie einer erfundenen Zahl. }
        "502": { description: Der Rechendienst war nicht erreichbar. }
  /api/v1/sites/{siteId}/suggestion-states:
    get:
      tags: [sites]
      summary: Welche Vorschläge der Kunde gerade nicht sehen will (Steuerung Stufe 6).
      description: >
        Das GEDÄCHTNIS der Vorschläge. Es gibt bewusst KEINE Route, die
        Vorschläge LIEFERT: ein Vorschlag ist eine Ableitung aus Fahrplan ×
        steuerbaren Komponenten × dem, was schon läuft, und sie lebt im Portal
        (`vorschlaege.ts`) - eine Server-Route müsste dieselbe Ableitung ein
        zweites Mal führen. Gespeichert wird nur die Gegenrichtung. Eine
        ABGELAUFENE Haltung wird gar nicht erst ausgeliefert.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Die geltenden Haltungen.
          content:
            application/json:
              schema:
                type: object
                properties:
                  states:
                    type: array
                    items:
                      type: object
                      properties:
                        key:
                          type: string
                          description: Der ABGELEITETE Vorschlags-Schlüssel (Art + Komponente).
                        state: { type: string, enum: [spaeter, abgelehnt] }
                        mutedUntil: { type: string, format: date-time }
                        updatedAt: { type: string, format: date-time }
        "404": { description: Anlage nicht gefunden. }
  /api/v1/sites/{siteId}/suggestion-states/{key}:
    put:
      tags: [sites]
      summary: „Später" (ein Tag) oder „Ablehnen" (sieben Tage).
      description: >
        Die FRIST rechnet der Server - eine vom Client gewählte Dauer wäre ein
        Weg, einen Vorschlag für immer verstummen zu lassen, ohne ihn je
        abzulehnen. Der Schlüssel wird auf seine Form geprüft, nie
        zurechtgebogen: ein still verändertes Wort träfe beim nächsten Lesen
        einen anderen Vorschlag als den weggeklickten.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: key, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [state]
              properties:
                state: { type: string, enum: [spaeter, abgelehnt] }
      responses:
        "200": { description: Die Haltung ist gemerkt; `mutedUntil` sagt bis wann. }
        "400": { description: Unbekannte Auswahl oder ungültiger Schlüssel. }
        "404": { description: Anlage nicht gefunden. }
  /api/v1/sites/{siteId}/verbraucher:
    get:
      tags: [consumer]
      summary: Die Zone "Verbraucher" der Steuerungsseite (lesend).
      description: >
        EIN Lese-Aggregat je Anlage (Konzept vp-verbrauchsmgmt-konzept-v1 §6,
        Paket P1): je steuerbarer Komponente ihre STEUERART (genau eine Quelle,
        höchstens ein Ziel), der Anlagen-Standard der Ladepunkte samt
        Ladepark-Rahmen, und die initiale Rangliste bei knapper Leistung.
        Alles ist eine PROJEKTION auf den Bestand - die aktive consumer_policy
        (§7.1), die Quellen-Wahl der Box in site_charging_config (§7.2), der
        chargers-Herzschlag und die Ansprüche der aktiven Flows; es entsteht
        kein zweites Datenformat. Seit P2 trägt jede Zeile zusätzlich ihre
        WÄHLBAREN Steuerarten (`optionen`) - aus derselben reinen Klasse, mit
        der der Schreibweg unten die Wahl ein zweites Mal prüft; die
        REIHENFOLGE ist seit P4 über PUT /rangliste setzbar (auch sie eine
        Projektion, kein neues Format). RLS-gefenced (fremde Anlage 404, Admins
        über den `X-Tenant-Id`-Umschalter); eine Anlage ohne steuerbares Gerät
        bekommt eine wohlgeformte LEERE Antwort.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Verbraucher, Ladepunkt-Abschnitt und Rangliste.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Verbraucher" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/sites/{siteId}/rangliste:
    put:
      tags: [consumer]
      summary: Die Reihenfolge bei knapper Leistung setzen.
      description: >
        Speichert die Rangliste (Konzept vp-verbrauchsmgmt-konzept-v1 §5, Paket
        P4) und antwortet mit der Zone, wie sie danach GELESEN wird - also der
        Normalform. Es entsteht KEINE Rangliste-Tabelle: geschrieben werden
        genau die vier Felder, die die Maschine schon kennt -
        consumer_profile.default_service_rank (die Position, die der Optimierer
        in D2-Stufe 1 zum Ordnen der Pflichten benutzt),
        consumer_profile.storage_relation (über dem Speicher = consumer_first,
        darunter = storage_first), site_charging_config.storage_priority und die
        Vorrang-Menge der Säulen. PFLICHTEN (Frist, feste Zeiten) gehen weiter
        IMMER vor der Rangliste - sie deckt der Optimierer, bevor der Rang
        irgendetwas ordnet. Steht keine Säule über dem Speicher, bleibt die
        Vorrang-Menge unangetastet (die Liste sagt dann nichts über sie).
        RLS-gefenced (fremde Anlage 404); jede Ablehnung ist ein deutscher Satz
        und schreibt NICHTS.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [eintraege]
              properties:
                eintraege:
                  type: array
                  minItems: 1
                  maxItems: 200
                  description: >
                    Die Liste FLACH, ein Gerät je Eintrag, von oben nach unten.
                    Eine Gruppe gleichrangiger Ladepunkte schickt der Client als
                    ihre Mitglieder hintereinander; der Server gruppiert beim
                    Lesen wieder. Nicht genannte Geräte landen unten.
                  items:
                    type: object
                    required: [art]
                    properties:
                      art: { type: string, enum: [speicher, ladepunkt, verbraucher] }
                      entityId:
                        type: string
                        format: uuid
                        nullable: true
                        description: Pflicht außer beim Speicher-Platz.
      responses:
        "200":
          description: Die Zone nach dem Speichern (die Normalform).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Verbraucher" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/sites/{siteId}/fahrzeuge:
    get:
      tags: [consumer]
      summary: Die Ladekarten dieser Anlage samt ihren Profilen.
      description: >
        Die FAHRZEUGE (Konzept vp-verbrauchsmgmt-konzept-v1 §4.5, Paket P7,
        Captain-Entscheid E4). Eine Zeile ist eine LADEKARTE, die hier schon
        einmal geladen hat - und, sobald der Kunde sie benannt hat, ihr Profil.


        DER SCHLÜSSEL IST EIN PSEUDONYM DER BOX: `tagref_` + HMAC-SHA256 mit
        ihrem eigenen Schlüssel, gemeldet im Herzschlag. Der Klartext-IdTag
        verlässt die Box nie und steht in keiner Cloud-Tabelle. Es ist
        AUSDRÜCKLICH NICHT der Bezug aus `ocpp_transaction` - den pfeffert die
        Cloud beim Ingest ein zweites Mal, die zwei Pseudonym-Räume sind
        verschiedene Werte für dieselbe Karte.


        `steuerart: null` heißt „kein Profil - die Karte lädt wie ihr
        Ladepunkt". `laedt` kommt aus dem Herzschlag, nie aus der Sichtungszeit
        geraten. RLS-gefenced wie jede /sites/**-Route (fremde Anlage 404).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Die Karten, zuletzt gesehene zuerst.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Fahrzeuge" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/sites/{siteId}/fahrzeuge/{tagRef}:
    put:
      tags: [consumer]
      summary: Eine Ladekarte benennen und/oder ihr eine Steuerart geben.
      description: >
        PATCH je Feld: ein Feld, das der Wunsch NICHT nennt, wird nicht
        angefasst - „benannt" und „gesteuert" sind zwei Schritte.


        DIE QUELLE HAT DREI ZUSTÄNDE: der Schlüssel FEHLT = nichts ändern; ein
        Wort SETZT sie; die LEERE Zeichenkette NIMMT DAS PROFIL ZURÜCK (die
        Karte lädt wieder wie ihr Ladepunkt) - ohne den dritten Zustand gäbe es
        keinen Weg zurück, ohne zugleich den Namen zu verlieren.


        EIN PROFIL TRÄGT EINE QUELLE UND NIE EIN ZIEL: ein Ziel („bis 06:00
        fertig") wird Stunden im Voraus für einen LADEPUNKT geplant, und welches
        Auto dann dort steckt, weiß niemand - `guenstig`/`feste_zeiten` werden
        deshalb BEIM NAMEN mit 400 abgelehnt.


        Eine Karte, die diese Anlage nie gesehen hat, ist 404 (ein Profil darauf
        träfe nie ein Auto); was gar kein Pseudonym dieser Box sein kann, ist
        400 - ein Klartext-IdTag darf hier nicht einmal ankommen.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: tagRef, in: path, required: true, schema: { type: string, pattern: "^tagref_[0-9a-f]{8,64}$" } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  maxLength: 200
                  description: Leer LÖSCHT den Namen; abwesend lässt ihn stehen.
                quelle:
                  type: string
                  enum: ["", sofort, ueberschuss]
                  description: >
                    Abwesend = nichts ändern, "" = Profil zurücknehmen.
                ueberschussModus: { type: string, enum: [pausieren, mindestleistung] }
                mindestleistungKw: { type: number, minimum: 0, maximum: 1000 }
      responses:
        "200":
          description: Die Karten nach dem Speichern.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Fahrzeuge" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [consumer]
      summary: Das Profil einer Ladekarte zurücknehmen.
      description: >
        Nullt Name UND Steuerart. Die SICHTUNG bleibt stehen - eine Rücknahme
        ist keine Beweisvernichtung, und die Karte erscheint danach wieder als
        unbenannte Bekannte statt beim nächsten Herzschlag als „neu".
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: tagRef, in: path, required: true, schema: { type: string, pattern: "^tagref_[0-9a-f]{8,64}$" } }
      responses:
        "200":
          description: Die Karten nach der Rücknahme.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Fahrzeuge" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/sites/{siteId}/verbraucher/{entityId}/steuerart:
    put:
      tags: [consumer]
      summary: Die Steuerart EINER Komponente setzen.
      description: >
        Der EINE Schreibweg der Steuerart (Konzept vp-verbrauchsmgmt-konzept-v1
        §3.3/§4.4, Paket P2). Er schreibt KEIN neues Format: er stellt ein
        consumer_profile sicher (auch für eine Komponente, die noch keines hat),
        legt die Requirement-Projektion (§3.3) als NEUE consumer_policy-Fassung
        ab und aktiviert sie über den BESTEHENDEN Pfad - mit dessen Kompilierung,
        V-5-Anspruchsprüfung, Flag-Toren und Audit-Spur.


        Der Rumpf ist die SPIEGELFORM der gelesenen Steuerart, damit der
        Rundlauf hält: was hier hingeht, liest die Projektion danach wieder aus.
        "Sofort" hat per §3.3 GAR KEINE Anforderung und das Schema verlangt
        `minItems: 1` - diese Quelle NIMMT deshalb die aktive Fassung zurück
        (der flag-unabhängige Stopppfad), statt eine leere zu schreiben.


        Eine OCPP-Ladesäule (`ev-charger`) ist hier 422 mit dem Weg, den es
        wirklich gibt: ihre Quelle fährt die Quellen-Bahn der Box, die
        Arbiter-Brücke dorthin entsteht in Paket P5. RLS-gefenced wie jede
        /sites/**-Route (fremde Anlage bzw. unbekannte Komponente 404, nie 403).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: entityId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SteuerartWunsch" }
      responses:
        "200":
          description: >
            Die Steuerart, wie sie JETZT gilt, plus das Ergebnis der
            Aktivierung. Eine abgelehnte Aktivierung ist KEIN Fehler, sondern
            ein benannter Ausgang (`aktiv: false` mit Grund und Satz) - der
            Entwurf bleibt gespeichert.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SteuerartErgebnis" }
        "400":
          description: >
            Die Wahl ist für diesen Typ unzulässig oder gesperrt - der Rumpf
            trägt den deutschen Grund in `message`, und es wurde NICHTS
            geschrieben.
        "422":
          description: >
            Diese Komponente lässt sich hier (noch) nicht steuern - der Satz
            nennt den Weg.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/sites/{siteId}/chargers:
    get:
      tags: [sites]
      summary: Die Ladepunkte der Anlage und das Budget, das sie sich teilen.
      description: >
        Das Standort-Ladebudget und jede OCPP-Ladesäule mit ihren Steckern, aus
        dem additiven `chargers`-Block des Geräte-Herzschlags (Lastmanagement
        Stufe 3). Jede Zahl und jeder deutsche Satz stammt aus der BOX und wird
        nur weitergereicht - dort wird entschieden, hier nur gezeigt (die
        Anschlussgrenze ist eine physische Grenze, ihr Wächter darf nicht am WAN
        hängen). READ-ONLY: es gibt bewusst keine Route, die eine Ladegrenze
        setzt. RLS-gefenced (fremde Anlage 404, Admins über den
        `X-Tenant-Id`-Umschalter); `budget: null` mit leerer Liste ist der
        ehrliche Zustand einer Anlage ohne Ladesäulen - nie ein Budget von 0.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Budget + Ladepunkte (beides leer ohne gemeldeten Block).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteCharging" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/ocpp/control:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [sites]
      summary: OCPP-Einrichtung und getrennte Bestätigungen der Boxen.
      description: Operatoren und Anlagenverwaltung lesen desired und observed; fremde Anlage 404.
      responses:
        "200":
          description: Gewünschte Konfiguration (null ohne Einstellung) und zeitgestempelte Meldungen je Box.
          content:
            application/json:
              schema:
                type: object
                properties:
                  desired: { type: object, nullable: true, additionalProperties: true }
                  observed: { type: array, items: { type: object, additionalProperties: true } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
    put:
      tags: [sites]
      summary: OCPP-Freigabe, Anschlussdaten, Karten oder zeitlich begrenzte Ladegrenzen speichern.
      description: >-
        Anlagen-, Mandanten- oder Plattformverwaltung. Payload entspricht ocpp_control
        in mqtt-charging-config.schema.json, mit der zuvor gelesenen Revision (0 initial).
        Der Server erhöht die Revision und versendet erst nach Commit. Eine Speicherung
        ist keine Bestätigung der Box oder der physischen Wirkung. Grenzen sind rein
        begrenzend, maximal 24 Stunden; beaufsichtigte Tests dauern 3 x 60 Sekunden.
      requestBody:
        required: true
        content: { application/json: { schema: { type: object, additionalProperties: true } } }
      responses:
        "200": { description: Gespeichertes desired mit neuer Revision und getrenntem observed. }
        "400": { description: Ungültige Revision, Anschlussdaten, Karten oder Zeitgrenzen. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Keine Berechtigung zur Anlagenverwaltung. }
        "404": { description: Site not found for this tenant. }
        "409": { description: Revision inzwischen geändert; neu laden. }
  /api/v1/sites/{siteId}/ocpp/stations:
    get:
      tags: [sites]
      summary: OCPP-1.6-Stationen mit Boot-, Verbindungs-, Stecker- und Fähigkeitszustand.
      description: Read-only und RLS-gesichert; keine Live-Station wird angesprochen. Plattform-Admins wählen den Mandanten wie bei allen Kundenpfaden über X-Tenant-Id.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Stationen der Anlage.
          content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/OcppStation" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/ocpp/events:
    get:
      tags: [sites]
      summary: Redigiertes OCPP-Journal mit Call, CallResult, CallError und Verbindungsereignissen.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: from, in: query, schema: { type: string, format: date-time } }
        - { name: to, in: query, schema: { type: string, format: date-time } }
        - { name: action, in: query, schema: { type: string } }
        - { name: messageType, in: query, schema: { enum: [Call, CallResult, CallError, Event] } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 1000, default: 200 } }
      responses:
        "200":
          description: Neueste Ereignisse zuerst; Geheimnisse bleiben auch im Payload redigiert.
          content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/OcppProtocolEvent" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/ocpp/gaps:
    get:
      tags: [sites]
      summary: Dauerhafte Nachweise unvollständiger OCPP-Journalbereiche.
      description: Aus action=JournalGap abgeleitet; meldet Überlauf sowie fehlgeschlagene Schreib-/Commitvorgänge mit monotonem Zähler und betroffenem Zeit-/Eventbereich. Read-only, RLS-gesichert und ohne Stationskontakt.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 1000, default: 200 } }
      responses:
        "200":
          description: Neueste Datenlücken zuerst; eine leere Liste bedeutet, dass keine Lücke gemeldet wurde, nicht dass Verlust physikalisch unmöglich war.
          content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/OcppDataGap" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/ocpp/transactions:
    get:
      tags: [sites]
      summary: OCPP-Ladevorgänge einschließlich Zählergrenzen, Stopgrund und maskierten Auth-Bezügen.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 1000, default: 200 } }
      responses:
        "200":
          description: Neueste Ladevorgänge zuerst.
          content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/OcppTransaction" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/ocpp/meter-values:
    get:
      tags: [sites]
      summary: Dimensionsgetreue OCPP MeterValues und StopTransaction.transactionData.
      description: Jede Zeile ist genau ein SampledValue; pointKey enthält measurand/context/format/phase/location/unit und verhindert dimensionsfremde Zusammenführung.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: from, in: query, schema: { type: string, format: date-time } }
        - { name: to, in: query, schema: { type: string, format: date-time } }
        - { name: pointKey, in: query, schema: { type: string } }
        - { name: transactionId, in: query, schema: { type: integer } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 5000, default: 1000 } }
      responses:
        "200":
          description: Einzelne SampledValues, neueste zuerst.
          content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/OcppMeterSample" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/ocpp/configuration:
    get:
      tags: [sites]
      summary: GetConfiguration-Inventar und SupportedFeatureProfiles.
      description: readonly, unknownKey, Standard-/Vendor-Key-Klassifikation und Redaktionsstatus. AuthorizationKey hat immer value=null.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: chargePointId, in: query, schema: { type: string } }
      responses:
        "200":
          description: Konfigurationen der Stationen.
          content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/OcppConfiguration" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/ocpp/action-permissions:
    get:
      tags: [sites]
      summary: D4-Rollenmatrix des Station-Command-Gateways.
      description: Reine Policy-Auskunft; die schreibenden Endpunkte erzwingen dieselbe Matrix serverseitig.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Aktion -> für den angemeldeten Principal erlaubt.
          content: { application/json: { schema: { $ref: "#/components/schemas/OcppActionPermissions" } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/ocpp/stations/{chargePointId}/action-intents:
    post:
      tags: [sites]
      summary: Serverseitigen Kurzzeit-Intent für Hard Reset, Full LocalList oder Firmware ausstellen.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: chargePointId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/OcppActionIntentRequest" } } }
      responses:
        "201": { description: Intent, content: { application/json: { schema: { $ref: "#/components/schemas/OcppActionIntent" } } } }
        "403": { description: Serverseitig nicht erlaubt. }
        "404": { description: Station nicht bekannt. }
  /api/v1/sites/{siteId}/ocpp/stations/{chargePointId}/actions:
    post:
      tags: [sites]
      summary: Eine OCPP-1.6-Aktion vorbereiten und one-shot an die Edge senden.
      description: >
        Idempotent über Idempotency-Key. Die Antwort beschreibt nur den
        persistierten Transportzustand; Accepted wird erst durch CallResult
        und Wirkung beobachtet erst durch ein Folgeereignis.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: chargePointId, in: path, required: true, schema: { type: string } }
        - { name: Idempotency-Key, in: header, required: true, schema: { type: string, maxLength: 200 } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/OcppActionRequest" } } }
      responses:
        "201": { description: Persistierte Aktion, content: { application/json: { schema: { $ref: "#/components/schemas/OcppAction" } } } }
        "403": { description: D4-Berechtigung fehlt. }
        "409": { description: Laufende Doppelaktion oder Intent-Konflikt. }
  /api/v1/sites/{siteId}/ocpp/actions:
    get:
      tags: [sites]
      summary: Persistierte OCPP-Aktionschoreografien.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: chargePointId, in: query, schema: { type: string } }
        - { name: limit, in: query, schema: { type: integer, default: 100 } }
      responses:
        "200": { description: Aktionen, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/OcppAction" } } } } }
  /api/v1/sites/{siteId}/ocpp/actions/{actionId}:
    get:
      tags: [sites]
      summary: Eine OCPP-Aktion mit Antwort- und Wirkungsbeleg.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: actionId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200": { description: Aktion, content: { application/json: { schema: { $ref: "#/components/schemas/OcppAction" } } } }
    delete:
      tags: [sites]
      summary: Eine noch nicht an die Edge übergebene One-shot-Aktion abbrechen.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: actionId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Abbruch protokolliert. }
        "409": { description: Bereits an die Edge übergeben; der physische Befehl kann nicht zurückgerufen werden. }
  /api/v1/sites/{siteId}/ocpp/actions/{actionId}/audit:
    get:
      tags: [sites]
      summary: Unveränderliche Zustands- und Wirkungs-Auditspur einer OCPP-Aktion.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: actionId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200": { description: Auditspur, content: { application/json: { schema: { type: array, items: { $ref: "#/components/schemas/OcppActionAudit" } } } } }
  /api/v1/sites/{siteId}/supply-price:
    get:
      tags: [sites]
      summary: The structured supply-price sheet (Bezugspreis-Komponenten).
      description: >
        One Anlage's `site_supply_price` Preisblatt (report vp-nacht-bezug-e7
        Stufe 2): the volumetric grid-import components in ct/kWh NETTO plus the
        USt rate and the Preisblatt-Stand. `present` says whether a row exists;
        `hasComponents` is the activation gate - when true, the optimizer prices
        import as `(spot + Sum Komponenten) x (1+USt)` instead of bare spot. A
        site without a row reports an empty sheet so the portal can prefill the
        researched suggestion values. RLS-scoped (foreign site 404; admins via
        the `X-Tenant-Id` switcher).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The sheet (empty when no row exists).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SupplyPrice" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
    put:
      tags: [sites]
      summary: Maintain the structured supply-price sheet.
      description: >
        Upsert the site's Preisblatt components. PATCH semantics (the
        site-update COALESCE pattern): a field ABSENT from the body keeps its
        stored value; a field present with a value is written, and an explicit
        `null` CLEARS that component to "unknown" (which suppresses the
        composition again once the last one is cleared). `ustPct` is NOT NULL - a
        present `null` there is ignored. This endpoint only makes a maintained
        sheet possible; the optimizer's behaviour changes when a component is
        actually saved (a maintained row activates the composition), never on its
        own. RLS-scoped (foreign site 404); the tenant is taken from the token,
        never the body.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: >
                Any subset of the component fields. All ct/kWh NETTO, all
                nullable (null clears to "unknown").
              properties:
                netzentgeltArbeitspreisCt: { type: number, nullable: true, minimum: 0 }
                stromsteuerCt: { type: number, nullable: true, minimum: 0 }
                konzessionsabgabeCt: { type: number, nullable: true, minimum: 0 }
                umlagenCt: { type: number, nullable: true, minimum: 0 }
                vertriebsaufschlagCt: { type: number, nullable: true, minimum: 0 }
                ustPct: { type: number, minimum: 0, maximum: 100 }
                komponentenStand: { type: string, format: date, nullable: true }
      responses:
        "200":
          description: The recomputed sheet.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SupplyPrice" }
        "400": { description: A component/USt/date value is invalid (German message). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/topology:
    get:
      tags: [topology]
      summary: Anlagen-Topologie-Read-Model (the adaptive energy-flow graph).
      description: >
        The site as an entity graph aggregated into the role-grouped hub
        topology the adaptive energy-flow diagram (AE2) renders: entities with
        each capability's resolved role (stored override else the DefaultRole
        mapping) + maßgeblich flag + latest live value (from telemetry_v2), plus
        the server-derived `topology` (role nodes PV-Erzeugung / Speicher / Netz
        / Verbraucher with directed flows, the shared Go/TS/Java twin - see
        docs/contracts/v2/topology-read-model.md). A hybrid inverter maps to
        several roles at once; several entities aggregate into one role. A fresh
        site returns empty entities + empty topology. RLS-scoped by tenant.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The site's topology read-model.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TopologyResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/topology-roles:
    put:
      tags: [topology]
      summary: Assign capabilities to roles (customer, "Rollen & Zuordnung").
      description: >
        The customer twin of the admin role-assignment (U2 "Rollen & Zuordnung",
        report §3.2): a Portal-User re-assigns a capability's role and picks the
        maßgebliche (primary) measurement for THEIR OWN site. Tenant-scoped (RLS;
        foreign site = 404, never 403); admins reach it via the `X-Tenant-Id`
        switcher. Safe by construction: a role is presentation-level and never
        widens control (guards/arbitration key on capabilities, not roles), so
        this shares the admin rule verbatim with no relaxed gate. A blank role
        reverts to the DefaultRole mapping; an unknown role is 400.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                assignments:
                  type: array
                  items:
                    type: object
                    required: [entityId, channel]
                    properties:
                      entityId: { type: string, format: uuid }
                      channel: { type: string, example: power_kw }
                      role:
                        type: string
                        description: pv | storage | grid | consumer; blank clears the override.
                        example: grid
                      primary: { type: boolean }
      responses:
        "200":
          description: The recomputed topology read-model.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TopologyResponse" }
        "400": { description: Unknown role or missing entityId/channel. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or entity not found for this tenant. }
  /api/v1/sites/{siteId}/v2-entities/adopt:
    post:
      tags: [topology]
      summary: Adopt a reported source into a component (customer, Portal v3 M6 "Zuordnen").
      description: >
        The CUSTOMER twin of the adoption bridge (Portal v3 M6 "Neues Gerät
        gefunden … jetzt zuordnen", OPEN O3): a Portal-User assigns an
        edge-reported source of THEIR OWN site to a component in one move.
        Tenant-scoped (RLS; foreign site = 404, never 403); admins reach it via
        the `X-Tenant-Id` switcher. It delegates to the SAME adoption the admin
        route uses, but is CATALOG-GUARDED to the GUIDED types only — producer,
        grid-meter and consumer-category types (the set the guided suggestion
        derives). It is never a free type picker and never takes guard-config
        input: platform-managed composed types (battery-hybrid / house-load) and
        free installer types (modbus-generic) are refused (422). Idempotent per
        `sourceId` (a re-adopt returns the existing entity).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [sourceId, entityType]
              properties:
                sourceId: { type: string, description: The edge source id from local_setup. }
                entityType: { type: string, example: wallbox }
                label: { type: string }
                maxPowerKw: { type: number, description: Consumer rated power (bounds commands). }
                capacityKwp: { type: number, description: Producer nameplate (kWp). }
                registryUnitId: { type: string, description: MaStR SEE number of the source. }
      responses:
        "200":
          description: The adopted (or already-existing) entity.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/V2Entity" }
        "400": { description: Missing sourceId/entityType or unknown type. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
        "422": { description: The type is not a guided type (battery-hybrid / house-load / free installer type). }
  /api/v1/sites/{siteId}/v2-entities/{entityId}/edge-source:
    post:
      tags: [topology]
      summary: Re-pin an entity to a currently reported edge source ("Wieder verbinden").
      description: >
        The repair lever for identity churn and crossed pins
        (vp-vier-erzeuger-p9): a source deleted + re-added on the device got a
        new id, the entity's pin went stale (orphanedPin on the entities
        surface) and the same physical device showed up as "Neues Gerät
        gefunden". Re-pinning RECONNECTS the existing component (label, kWp,
        history) instead of minting a duplicate. Customer-safe: it moves only
        the presentation-level source link of the caller's OWN site (RLS) -
        never guard config or control rights. The target source must be
        currently reported by the device and role-compatible with the entity's
        type; a source already pinned to another entity is refused (409); a
        stale de-entitied point holding the pin is released (kWp delta) so the
        pin can move. Idempotent for the same sourceId.


        swap=true (vp-bereinigung-ui-k3) turns the 409 into a guided EXCHANGE:
        both assignments move inside ONE transaction, so a customer whose plant
        is fully pinned to the WRONG components can straighten it out and can
        never strand half-swapped. The other component gets this entity's
        previous source only when that source is still reported - otherwise its
        pin is cleared (moving an orphan defect would be the worse outcome).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: entityId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [sourceId]
              properties:
                sourceId: { type: string, description: The edge source id from local_setup. }
                swap:
                  type: boolean
                  default: false
                  description: >
                    Explicit consent to take a source another component holds -
                    both assignments are exchanged atomically. Absent/false
                    keeps the 409, so an older client can never steal one.
      responses:
        "200":
          description: The re-pinned entity.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/V2Entity" }
        "400": { description: Missing sourceId. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or entity not found for this tenant. }
        "409": { description: The source is already pinned to another entity (and swap was not requested). }
        "422": { description: The source is not currently reported, its role does not fit the entity's type, or the component is platform-composed. }
  /api/v1/sites/{siteId}/v2-entities/{entityId}/label:
    put:
      tags: [topology]
      summary: Rename a component (the customer's own name / alias).
      description: >
        The customer twin of the admin label PUT (concept vp-entity-alias-k1).
        The name is what the CUSTOMER calls the thing ("Dach Süd"); since the
        Label-Hygiene migration (V20260812000000) a non-null label means
        human-given, so it wins the customer-facing name chain over the name the
        device carries on its own :8484 page.


        R1 - a name is PRESENTATION and never an intervention. Enforced by
        construction: this route accepts ONLY `label`. There is no type, role,
        pin or guard field in the request, so nothing can be smuggled through
        the rename door; assignment and control keep their own gated routes.


        EVERY component may be renamed, including the platform-composed
        battery-hybrid / grid-meter / house-load - re-pin and delete refuse
        those because they change what a component IS or whether it exists,
        while a name changes neither. Only their TYPE and ROLE stay ours.


        Semantics mirror the admin PUT so there is one label-writing rule: an
        absent/null label keeps the current name, an EMPTY one clears it - back
        to the derived name, never an empty label. Newlines/control characters
        collapse to single spaces and the value is trimmed. Duplicates are
        allowed on purpose (two arrays may both be called "Dach").
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: entityId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                label:
                  type: string
                  maxLength: 200
                  nullable: true
                  description: >
                    The customer's name. Empty string clears it (back to the
                    derived name); absent/null keeps the current one.
      responses:
        "200":
          description: The renamed entity.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/V2Entity" }
        "400": { description: The label exceeds 200 characters. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or entity not found for this tenant. }
  /api/v1/sites/{siteId}/v2-entities/{entityId}:
    delete:
      tags: [topology]
      summary: Delete an adopted component (customer cleanup lever).
      description: >
        The customer twin of the admin purgePoint delete: it removes the
        measurement point outright, releases a producer's kWp from the
        aggregate asset.pv and frees the edge-source pin, so the device
        reappears as "Neues Gerät gefunden" and can be assigned to the right
        component. The registry is re-pushed, so the device drops the component
        too. Refused (422) for the platform-composed base components
        (battery-hybrid / house-load) and for any component without a device
        assignment - a grid meter or house load composed from the plant's own
        master data is its Grundausstattung, not a mis-adoption.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: entityId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Removed. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or entity not found for this tenant. }
        "409":
          description: >
            The component is a Beleg (UEMS AP-12 E13 S2/IP-12): a released
            Berichtsstand - also a superseded one - cites a Messstelle it ever
            fed. Checked after the Grundausstattung guards, before anything is
            written; `code` `berichts_belege`, the Stände and the cited
            Messstellen. Ending the binding stays possible.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BelegeImWeg" }
        "422": { description: A platform-composed or unassigned component - not the customer's to remove. }
  /api/v1/sites/{siteId}/entity-strategies:
    get:
      tags: [topology]
      summary: Which active flows touch each entity (U2 strategy chips).
      description: >
        The entity→active-flow map the "Ihre Geräte" cards render as read-only
        strategy chips (a link into Steuerung), derived from each active flow
        document's D-13 claims. Keyed by entity id. Tenant-scoped (RLS; foreign
        site 404).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: entityId → list of {flowId, flowName}.
          content:
            application/json:
              schema:
                type: object
                additionalProperties:
                  type: array
                  items:
                    type: object
                    properties:
                      flowId: { type: string, format: uuid }
                      flowName: { type: string }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/admin/sites/{siteId}/topology-roles:
    put:
      tags: [topology, admin]
      summary: Assign capabilities to roles (platform-admin).
      description: >
        Re-assign a capability's role and pick the maßgebliche (primary)
        measurement per role. Platform-admin only, through the `X-Tenant-Id`
        switcher over the RLS path (foreign site = 404). A blank role reverts the
        capability to its DefaultRole mapping; an unknown role is 400. Returns
        the recomputed read-model.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                assignments:
                  type: array
                  items:
                    type: object
                    required: [entityId, channel]
                    properties:
                      entityId: { type: string, format: uuid }
                      channel: { type: string, example: power_kw }
                      role:
                        type: string
                        description: pv | storage | grid | consumer; blank clears the override.
                        example: grid
                      primary: { type: boolean }
      responses:
        "200":
          description: The recomputed topology read-model.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TopologyResponse" }
        "400": { description: Unknown role or missing entityId/channel. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Not a platform-admin. }
        "404": { description: Site or entity not found for the selected tenant. }
  /api/v1/sites/{siteId}/profile:
    get:
      tags: [profile]
      summary: Nutzungsprofil / EMS-Modus read-model (AE7, the second adaptation axis).
      description: >
        The site's usage profile (arbitrage | peak | private) that steers the
        adaptive portal/edge EMPHASIS, DERIVED from its strategy nodes (active
        flows) + entity mix + money master data (plant_kind, Leistungspreis),
        with an explicit override. ONE truth - not a competing concept (see
        docs/contracts/v2/usage-profile.md). Returns the effective profile, the
        derived default, the override, the emphasis map (money/peak/flow/devices)
        and the raw signals. RLS-scoped (foreign site 404; admins via the
        `X-Tenant-Id` switcher).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The usage-profile read-model.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/UsageProfile" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
    put:
      tags: [profile]
      summary: Set or clear the usage-profile override.
      description: >
        Set the explicit usage-profile override (arbitrage | peak | private) or
        clear it (null/blank -> auto-derive). Customer or admin, RLS-scoped;
        returns the recomputed read-model. Also echoed on Site.usageProfileOverride.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                override:
                  type: string
                  nullable: true
                  enum: [arbitrage, peak, private]
                  description: null/blank clears the override (auto-derive).
      responses:
        "200":
          description: The recomputed usage-profile read-model.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/UsageProfile" }
        "400": { description: Override is not one of arbitrage, peak, private. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/anwendungs-preset:
    put:
      tags: [profile]
      summary: Set the site's application PRESET (Privat | Gewerbe).
      description: >
        The PROFILE of a plant (Anwendungs-Programm Stufe 2, Captain-Entscheid
        E3): preset + tonality + reset base, never a signal of the activation
        derivation. It changes NO switch - which applications run stays the
        Regal's business (PUT .../profiles), so a later "Profil aendern" can
        never rewrite the customer's own switches. The assistant flips the
        proposed switches individually through that same route, with the same
        server effects (open the gate, seed the starter).
        An absent/blank `profil` clears it back to "none chosen"; an unknown
        word is a 400 with a German reason, never a silent fallback to one of
        the two. Deliberately a narrow route rather than a field of the site
        master-data form: the assistant writes it from a step that never loaded
        the tariff/remuneration fields, and a full-representation
        PUT /api/v1/sites/{id} from there would be a clobber risk. The path is
        deliberately NOT `/profil` - that would sit ONE character next to the
        existing `/profile` (the AE7 usage profile).
        RLS-scoped (foreign site 404; admins via the `X-Tenant-Id` switcher).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                profil:
                  type: string
                  nullable: true
                  enum: [privat, gewerbe]
                  description: The preset, or null/absent to clear it.
      responses:
        "200":
          description: The updated site (its `profil` echoes the stored value).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Site" }
        "400": { description: Unknown profil (German message). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/profiles:
    get:
      tags: [profile]
      summary: Betriebsmodell-Regal of the Anlage (Portal v3 M3 + Steuerung Stufe 0).
      description: >
        The plant's feature set, composed from the ONE application catalog
        (`anwendungen/catalog.json`) and returned as TWO lists.

        `profiles` is the SHELF the Steuerung page renders - since Steuerung
        Stufe 0 "Entwirrung" exactly the four BETRIEBSMODELLE (Marktoptimierung,
        Lastspitzenkappung, Atypische Netznutzung, Ladepark-Lastmanagement),
        server-filtered on the catalog field `regal`.

        `weitere` carries the applications that are NOT on the shelf - the two
        BASIS entries (Anlage beobachten, Speicher-Fahrplan; always on, `state`
        refused with 400) and the RULE applications (Ueberschuss nutzen,
        Verbraucher steuern, Eigene Auswertung; their switch is pure intent - no
        gate is opened, no starter seeded). Stufe 0 HIDES them, it does not
        delete them: their states are still answered here (surfaces outside the
        Steuerung read them - the cockpit gate "is Eigene Auswertung on?" and the
        intent overlay of the mode projection), and PUT still reaches them. The
        field is ADDITIVE: an older client reads only `profiles` and thereby sees
        exactly the shelf.

        Reserved catalog entries appear in NEITHER list - a switch that cannot do
        anything would be a promise nobody keeps.
        Every switchable application is a DIRECT customer toggle - there are exactly two states,
        `an` and `aus`, and no "angefragt" state anywhere. `state` is the STORED
        intent (null = no row = derived default, i.e. the pre-M3 behaviour);
        `derivedActive` is what the derivation alone says, `active` the effective
        result after the intent overlay. `blockedReason` is the honest German
        sentence for a profile that is switched on but cannot fully run yet
        (missing Leistungspreis, missing market access) - never a request prompt.
        RLS-scoped (foreign site 404; admins via the `X-Tenant-Id` switcher).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The shelf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteProfiles" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
    put:
      tags: [profile]
      summary: Switch one Anwendung on or off.
      description: >
        What switching ON does depends on the application's CLASS in the catalog:
        a BUSINESS application enables exactly ITS gated node types for this site
        and seeds its starter flow (`FlowTemplateService.autoStart`); a RULE
        application stores the INTENT only - no gate is opened and no starter is
        seeded, because its content are the rules the customer builds. Switching
        OFF deactivates the application's flows (if it has a strategy node),
        disables those node types again and persists `aus` (so a re-derived
        signal cannot silently re-enable it). The gate is opened, never faked -
        the activation path still re-checks it, and the peak-shaving
        configuration gate, the edge guard chain and the section 14a/EEG
        protections are untouched. Marktoptimierung additionally requires real
        market access on the site (a dynamic tariff and/or Direktvermarktung):
        without it the toggle still flips, but the gated node stays closed and
        nothing trades. A toggle NEVER writes money/contract master data.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [profile, state]
              properties:
                profile:
                  type: string
                  example: marktvermarktung
                  description: >
                    A visible, switchable application id from
                    `anwendungen/catalog.json`. A BASIS application is always on
                    and refuses with 400; a reserved entry is indistinguishable
                    from an unknown one (it does not exist yet).
                state: { type: string, enum: [an, aus] }
      responses:
        "200":
          description: The recomputed shelf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteProfiles" }
        "400": { description: "Unknown application, an always-on BASIS application, or an unknown state." }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/cockpit-layout:
    get:
      tags: [profile]
      summary: The stored cockpit layout of one Anlage (Anwendungs-Programm Stufe 3).
      description: >
        Returns ALL layers SEPARATELY plus the Baustein catalog - never the
        resolved surface. The resolution (catalog -> preset -> Vorgabe -> Eigen)
        is a pure function in the portal (`cockpitLayout.ts layoutResolve`); the
        server holds catalog + intent, and there is deliberately no
        `GET /surface` (BUILD.md section 4.1). They must stay separate so the UI
        can say WHAT a "Zuruecksetzen" falls back to ("the Vorgabe of your
        operator" vs "the VoltPilot standard") - Captain decision E2.
        A site with no row answers with empty layers, i.e. the deterministic
        catalog default = the behaviour before this stage.
        RLS-scoped (foreign site 404, never 403; admins via the `X-Tenant-Id`
        switcher).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The layers + the Baustein catalog.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CockpitLayout" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
    put:
      tags: [profile]
      summary: Store one layer of the cockpit layout.
      description: >
        `layer=eigen` (default) is the CUSTOMER's arrangement and WINS over any
        Vorgabe; `layer=vorgabe` is the operator's default and may only be
        written by a platform-admin (403 otherwise) - a method check, not a
        second data path, so a future operator role plugs in at one place.
        The body stores INTENT only: `order` names Bausteine in their order
        (unnamed ones keep their canonical position, which is how a newly
        activated application appears where it belongs), `hidden`/`shown` are
        explicit statements (`shown` takes back a LOWER layer's `hidden`), and
        `lead` is the highlighted block.
        Form validation: an unknown Baustein, a Baustein of another surface, a
        MANDATORY Baustein in `hidden` (Status, Zustand - a catalog property, not
        an admin's will), a duplicate in `order` and a non-lead-capable `lead`
        are each a 400 with a German reason and write nothing. A Baustein this
        plant does not currently have is deliberately NOT refused - the
        preference has to survive switching an application off and on again.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - name: layer
          in: query
          required: false
          schema: { type: string, enum: [eigen, vorgabe], default: eigen }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CockpitLayoutDocument" }
      responses:
        "200":
          description: The recomputed layers.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CockpitLayout" }
        "400": { description: "Unknown Baustein, mandatory Baustein hidden, invalid lead, or unknown layer." }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Only a platform-admin may write the `vorgabe` layer. }
        "404": { description: Site not found for this tenant. }
    delete:
      tags: [profile]
      summary: Reset one layer - it falls back to the layer beneath.
      description: >
        The reset IS a delete of exactly that row, which is why the button can
        honestly announce its target. Deleting a layer that never existed is a
        harmless no-op, not a 404.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - name: layer
          in: query
          required: false
          schema: { type: string, enum: [eigen, vorgabe], default: eigen }
      responses:
        "200":
          description: The recomputed layers.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CockpitLayout" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Only a platform-admin may reset the `vorgabe` layer. }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/eigene-auswertung:
    get:
      tags: [profile]
      summary: The VALUES of one plant's own evaluations (Anwendungs-Programm Stufe 5).
      description: >
        One narrow read for every own Baustein of a plant - current value, day
        figure and, for a `chart`, the day's buckets. No new store and no new
        query: the DEFINITIONS live in the layout document
        (`cockpit_layout.document.custom`), the VALUES come from the same entity
        history path the Messwerte explorer draws its curves from (including the
        MIG-B2 splice that keeps a migrated plant's v1 history). One query per
        COMPONENT, however many tiles sit on it.
        The four figures: `jetzt` = the newest reported value (from the SAME
        series the chart draws, so tile and curve can never disagree),
        `tagesmax` = the day's maximum, `tagesmittel` = the SAMPLE-WEIGHTED mean,
        `tagessumme` = the ADVANCE (`max - min`), never a sum of the readings -
        a kWh channel reports a METER READING here. A negative advance (meter
        swap, reset) yields null, never a fabricated figure.
        `wert` is null wherever there is none - NEVER a 0; a tile without a
        measurement says so in `hinweis`.
        Read-only and RLS-scoped like every `/sites/**` route (foreign site 404,
        never 403; admins via the `X-Tenant-Id` switcher). It takes no
        definition: what a cockpit shows is decided by the layout document, and
        answering a submitted definition would be a second way past the write
        path's validation.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - name: at
          in: query
          required: false
          description: The anchor day (Europe/Berlin); default today.
          schema: { type: string, format: date }
      responses:
        "200":
          description: One row per stored evaluation (empty when the plant has none).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EigeneAuswertungWerte" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/tenant/cockpit-layout:
    get:
      tags: [profile]
      summary: The customer-wide cockpit Vorgabe ("for all my plants", Captain decision E1).
      description: >
        Same store, `scope_kind=tenant`: an operator with 20 plants maintains ONE
        Vorgabe instead of 20 layouts. A per-plant Vorgabe BEATS it, and the
        customer beats both. The tenant comes from the validated token / the
        `X-Tenant-Id` switcher, never from the path - a platform-admin without a
        selected customer gets 404. On `surface=portfolio` the response's
        `profil` is the fleet's MAJORITY plant profile (ties and "none set"
        yield null - never a guessed audience).
      parameters:
        - name: surface
          in: query
          required: false
          schema: { type: string, enum: [cockpit, portfolio], default: cockpit }
          description: >
            Which surface the layer belongs to. `cockpit` is the per-plant
            cockpit's customer-wide Vorgabe; `portfolio` is the customer's OWN
            portfolio cockpit (Anwendungs-Programm Stufe 4) - there the `eigen`
            layer is the customer's own arrangement, not a dead end. An unknown
            word is a 400 with a German reason, never a silent fallback.
      responses:
        "200":
          description: The layers + the Baustein catalog.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CockpitLayout" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: No tenant selected. }
    put:
      tags: [profile]
      summary: Store the customer-wide Vorgabe.
      parameters:
        - name: surface
          in: query
          required: false
          schema: { type: string, enum: [cockpit, portfolio], default: cockpit }
          description: >
            Which surface the layer belongs to. `cockpit` is the per-plant
            cockpit's customer-wide Vorgabe; `portfolio` is the customer's OWN
            portfolio cockpit (Anwendungs-Programm Stufe 4) - there the `eigen`
            layer is the customer's own arrangement, not a dead end. An unknown
            word is a 400 with a German reason, never a silent fallback.
        - name: layer
          in: query
          required: false
          schema: { type: string, enum: [eigen, vorgabe], default: vorgabe }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CockpitLayoutDocument" }
      responses:
        "200":
          description: The recomputed layers.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CockpitLayout" }
        "400": { description: "Unknown Baustein, a Baustein of the OTHER surface, mandatory Baustein hidden, invalid lead, unknown layer, or unknown surface." }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Only a platform-admin may write the `vorgabe` layer. }
        "404": { description: No tenant selected. }
    delete:
      tags: [profile]
      summary: Remove the customer-wide Vorgabe.
      parameters:
        - name: surface
          in: query
          required: false
          schema: { type: string, enum: [cockpit, portfolio], default: cockpit }
          description: >
            Which surface the layer belongs to. `cockpit` is the per-plant
            cockpit's customer-wide Vorgabe; `portfolio` is the customer's OWN
            portfolio cockpit (Anwendungs-Programm Stufe 4) - there the `eigen`
            layer is the customer's own arrangement, not a dead end. An unknown
            word is a 400 with a German reason, never a silent fallback.
        - name: layer
          in: query
          required: false
          schema: { type: string, enum: [eigen, vorgabe], default: vorgabe }
      responses:
        "200":
          description: The recomputed layers.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CockpitLayout" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Only a platform-admin may reset the `vorgabe` layer. }
        "404": { description: No tenant selected. }
  /api/v1/sites/{siteId}/flows/auto-start:
    post:
      tags: [flows]
      summary: Seed this site's starter flow DRAFT (customer twin of the admin route).
      description: >
        Idempotent: a site that already has any flow is left untouched
        (`already_has_flow`), a site without a battery-hybrid entity reports
        `no_battery`. Creating a DRAFT controls nothing - the activation gate is
        unchanged, so a flow carrying a gated node still fails with
        `gated_node_not_enabled` until that node type is enabled for the site.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "201": { description: Starter flow draft created. }
        "200": { description: Nothing seeded - the outcome carries the German reason. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/admin/sites/{siteId}/flow-node-governance:
    get:
      tags: [admin]
      summary: Gated strategy-node types and their per-site enablement (AE7 governance).
      description: >
        The gated (market-/grid-near) strategy node types from the catalog
        (Arbitrage, Peak-Shaving, atyp. NN) and whether each is enabled for this
        site. A flow carrying a gated node stays inactive until it is enabled
        here. Platform-admin only, via the `X-Tenant-Id` switcher.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The gated node types + enablement.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FlowNodeGovernance" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Not a platform-admin. }
        "404": { description: Site not found for the selected tenant. }
    put:
      tags: [admin]
      summary: Enable/disable gated strategy-node types for the site.
      description: Platform-admin only. An unknown/non-gated node type is 400.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                enablements:
                  type: array
                  items:
                    type: object
                    required: [nodeType, enabled]
                    properties:
                      nodeType: { type: string, example: vp.strategy.market }
                      enabled: { type: boolean }
      responses:
        "200":
          description: The recomputed governance.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FlowNodeGovernance" }
        "400": { description: Unknown or non-gated node type. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Not a platform-admin. }
        "404": { description: Site not found for the selected tenant. }
  /api/v1/admin/sites/{siteId}/flows/auto-start:
    post:
      tags: [admin]
      summary: Seed the site's profile-appropriate starter flow draft (AE7 auto-start).
      description: >
        Seed a starter flow DRAFT matching the site's derived usage profile
        (arbitrage -> market, peak -> peak-shaving, private -> self-consumption
        on the battery), so onboarding never dead-ends. Idempotent: a site with
        any flow is left untouched (created=false, reason already_has_flow); a
        site without a battery is skipped (reason no_battery). Platform-admin only.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "201":
          description: The starter flow draft was created.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AutoStartOutcome" }
        "200":
          description: No-op (already has a flow / no battery).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AutoStartOutcome" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Not a platform-admin. }
        "404": { description: Site not found for the selected tenant. }
  /api/v1/sites/{siteId}/history:
    get:
      tags: [history]
      summary: A site's history ("Historie") for one period, money lens first.
      description: >
        Bucketed pv/load/grid/SoC series (15-min for day, hourly for week,
        Europe/Berlin-daily for month/year; served from the telemetry rollup
        tables, day live from raw telemetry) with the matching day-ahead price
        and per-bucket import cost, plus period totals: consumption / PV /
        import / export kWh, grid cost EUR (import energy x matching 15-min
        price), battery savings EUR (persisted optimizer plans, latest run per
        slot, null where none exist), Autarkiegrad and Eigenverbrauchsquote.
        For range=day the response additionally carries the Tagesprotokoll
        (notable events in plain German) and the persisted plan trajectory for
        the plan-vs-actual overlay. Period boundaries are Europe/Berlin local
        time. RLS-scoped by tenant.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - name: range
          in: query
          required: false
          schema: { type: string, enum: [day, week, month, year], default: day }
        - name: at
          in: query
          required: false
          schema: { type: string, format: date }
          description: Any date inside the wanted period (default today, Europe/Berlin).
      responses:
        "200":
          description: The site's history for the period (empty buckets if no data).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/History" }
        "400": { description: Unknown range value. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/earnings:
    get:
      tags: [history]
      summary: One site's realized money for one period (the "Erlöse" world).
      description: >
        The Anlagen-scharfe twin of GET /api/v1/earnings (Historie concept
        vp-historie-konzept-t4 §4.2 Welt B / F1, performance measure P3): the
        SAME slot math, the SAME price truth, one site and one period instead
        of the whole tenant with strip and forward market value. Beyond the
        fleet fields it exposes the COMPOSITION the money card shows -
        stromkostenEur (the metered import at the site's real supply price -
        literally the import term of actualEur), the resulting nettoErgebnisEur
        = einspeiseErloesEur + eigenverbrauchsWertEur - stromkostenEur, the
        Marktprämie already contained in the feed-in revenue, the Ø Bezugspreis
        and a per-bucket series carrying the three parts (so the chart can
        stack them and its cumulative line lands on nettoErgebnisEur).
        savedEur stays an ATTRIBUTION inside the result, never a sibling
        summand. Ranges are the Historie vocabulary (week included);
        range=all reports the site's first covered Berlin day as `from`.
        RLS-scoped - a foreign site is 404; admins use the X-Tenant-Id
        switcher.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - name: range
          in: query
          schema: { type: string, enum: [day, week, month, year, all], default: month }
        - name: at
          in: query
          description: Any date inside the wanted period (default today, Europe/Berlin).
          schema: { type: string, format: date }
      responses:
        "200":
          description: The site's realized money for the period.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteEarnings" }
        "400": { description: Unknown range value. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/forecast-quality:
    get:
      tags: [forecast-quality]
      summary: Forecast-model quality ("Prognosequalität") for a site.
      description: >
        The shadow-mode forecasting transparency view (docs/forecasting.md):
        which forecast model is LIVE per kind (activeLoadModel/activePvModel,
        resolved server-side FOR THIS SITE with the SAME precedence the
        optimizer uses - the site's own choice wins, else the platform default,
        else the env default), the
        lifecycle of every model that has run on the site (challengers report
        "collecting: day X of N" while their self-gate is unmet; trained models
        carry trainedAt/trainRows and the top feature importances with
        plain-German labels), the per-model daily error series (MAE kW, nMAE %,
        bias, skill vs the ACTIVE model - positive = better) computed by the
        daily evaluation job, and the daily plan-vs-actual economics.
        Challenger promotion is NEVER automatic; it is a deliberate act on
        /api/v1/sites/{siteId}/forecast-models - by the customer, for their own
        plant - informed by this data. The platform-wide
        /api/v1/admin/forecast-models sets only the DEFAULT for plants without
        their own choice. All rows come from RLS-scoped tables - a foreign site is a
        404; a fresh site yields empty (but well-formed) lists.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - name: days
          in: query
          required: false
          schema: { type: integer, default: 30, minimum: 1, maximum: 90 }
          description: How many trailing days of accuracy/plan series to return.
      responses:
        "200":
          description: The site's forecast-quality view.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ForecastQuality" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/forecast-models:
    get:
      tags: [forecast-quality]
      summary: Welches Prognosemodell plant DIESE Anlage (der Schalter-Zustand)?
      description: >
        Der Prognose-Schalter EINER Anlage (Captain-Auftrag 19.08.2026 - er
        revidiert die plattformweite Semantik von /api/v1/admin/forecast-models
        vom Vortag): je Prognoseart (load/pv) das Modell, das DIESE Anlage
        plant, WOHER die Wahl kommt (anlage = eine Zeile in
        site_forecast_model_choice, plattform = die Vorgabe von VoltPilot,
        env = das ausgelieferte Standardmodell), die Plattform-Vorgabe und der
        Umgebungs-Vorgabewert, wer wann umgestellt hat und die wählbare Menge -
        plus die append-only Umstellungs-Historie DIESER Anlage.
        Mandantenbezogen wie jede /sites/**-Route: KEIN platform-admin nötig,
        Authentifizierung + Postgres-RLS sind der Zaun, eine fremde Anlage ist
        404 (nie 403). Portal-Admins erreichen jede Anlage über den
        X-Tenant-Id-Umschalter auf demselben Pfad.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Der Schalter-Zustand dieser Anlage.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteForecastModels" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant (RLS). }
    post:
      tags: [forecast-quality]
      summary: Kandidat übernehmen - für DIESE Anlage.
      description: >
        Stellt das aktive Modell EINER Prognoseart FÜR DIESE ANLAGE um. Ab dem
        nächsten Planungslauf konsumiert der Optimierer die Prognosereihen
        dieses Modells - nur für sie; alle anderen Anlagen bleiben unverändert.
        Das bisherige Modell rechnet im Schatten weiter und wird weiter täglich
        bewertet, der Rückweg ist derselbe Aufruf in die Gegenrichtung. Die
        Umstellung ist append-only protokolliert (von->zu, JWT-Subject,
        Zeitpunkt). Es wird nichts trainiert und kein Gerät erreicht.
        Ablehnungen tragen einen deutschen {message}-Körper: 400 = unbekannte
        Art / unbekanntes Modell / Modell einer anderen Art; 409 = das Modell
        plant diese Anlage bereits, ODER es sammelt für sie noch Daten, ODER es
        hat für sie noch keine Tagesbewertung (die zwei Sperren, die die
        Oberfläche vor dem Klick nennt, werden hier ein zweites Mal geprüft).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [kind, model]
              properties:
                kind: { type: string, enum: [load, pv] }
                model:
                  type: string
                  enum: [load-persistence, load-xgb, pv-physical, pv-residual-xgb]
      responses:
        "200":
          description: Der neue Schalter-Zustand (inkl. der neuen Journal-Zeile).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteForecastModels" }
        "400": { description: Unbekannte Prognoseart bzw. unpassendes Modell. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant (RLS). }
        "409": { description: Plant bereits, sammelt noch, oder ohne Tagesbewertung. }
  /api/v1/sites/{siteId}/assets:
    get:
      tags: [registry]
      summary: The site's asset master data incl. registry provenance.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Asset rows (battery params for the optimizer, PV params for the forecast).
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SiteAsset" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant (RLS). }
  /api/v1/sites/{siteId}/measurement-points:
    get:
      tags: [registry]
      summary: The site's additional read-only measurement points (multi-source Anlage).
      description: >
        A site with a battery-hybrid inverter PLUS a separate AC-coupled PV
        records the second PV here as an Erzeuger source; its kWp sums into the
        aggregate `asset.pv` the forecast reads. A dedicated grid meter at the
        point of common coupling is recorded as a Netz source (`grid-meter`, at
        most one per site, no nameplate); it does not touch `asset.pv`. A
        Verbraucher (`consumer`, e.g. a go-e wallbox) is recorded here too - no
        nameplate, no count limit (a site may have several). NO second device
        claim - sources are read through the site's one connected edge, which
        folds a Netz meter into the authoritative site grid. Read-only by
        construction (control is always false).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The site's measurement points.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/MeasurementPoint" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant (RLS). }
    post:
      tags: [registry]
      summary: Record an additional read-only source (Erzeuger PV, a single Netz grid meter, or a Consumer).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                role:
                  type: string
                  description: >
                    'pv-generation' (Erzeuger, default), 'grid-meter' (Netz, at
                    most one per site; carries no nameplate) or 'consumer'
                    (Verbraucher, e.g. a wallbox; no nameplate, no count limit).
                  enum: [pv-generation, grid-meter, consumer]
                label: { type: string, maxLength: 120 }
                brand: { type: string, maxLength: 60 }
                model: { type: string, maxLength: 60 }
                capacityKwp:
                  type: number
                  description: Per-Erzeuger nameplate (kWp); sums into the aggregate asset.pv. Ignored for a Netz meter.
                registryUnitId:
                  type: string
                  description: The source's OWN MaStR SEE number (optional - never blocks adding).
      responses:
        "200":
          description: The site's measurement points after the add.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/MeasurementPoint" }
        "400": { description: An unknown role or invalid values. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant (RLS). }
        "409": { description: The site already has a Netz (grid-meter) source. }
  /api/v1/sites/{siteId}/measurement-points/{pointId}:
    delete:
      tags: [registry]
      summary: Remove an additional source; its kWp is subtracted from the aggregate site PV.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: pointId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The site's remaining measurement points.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/MeasurementPoint" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Measurement point or site not found for this tenant (RLS). }
  /api/v1/sites/{siteId}/mastr-lookup:
    post:
      tags: [registry]
      summary: Fetch one MaStR unit for confirmation (nothing persisted).
      description: >
        Looks the SEE unit number up in the Marktstammdatenregister (official
        SOAP webservice when credentials are configured, keyless public JSON
        backend otherwise) and returns the mapped preview: kWp, module count,
        orientation/tilt decoded from catalog values to degrees (nullable -
        e.g. Balkonkraftwerke carry none), commissioning date, storage kWh/kW,
        and the registry's PLZ/Ort for the customer's own plausibility check.
        Storage units are ALSO SEE-numbered; `kind` states what the registry
        says the unit is. Error bodies carry a customer-facing German
        `message`.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [einheitNummer]
              properties:
                einheitNummer:
                  type: string
                  description: SEE + 12 digits; lowercase/spaces are normalized.
                  example: SEE966831669444
      responses:
        "200":
          description: Mapped preview for confirmation.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MastrPreview" }
        "400": { description: Invalid unit number (German hint in `message`, e.g. the SES/SSE/EEG prefix mix-ups). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant, or no unit with this number in the registry. }
        "502": { description: Registry currently unreachable. }
  /api/v1/sites/{siteId}/mastr-apply:
    post:
      tags: [registry]
      summary: Persist the confirmed registry values onto the site's assets.
      description: >
        Creates or updates the site's `pv` and/or `battery` asset rows with the
        values the customer confirmed in the preview, plus provenance (registry
        id, unit number, fetch timestamp). Battery values land in the columns
        the optimizer reads; PV values feed the forecast's PlantSpec. At least
        one of `pv`/`storage` is required.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MastrApplyRequest" }
      responses:
        "200":
          description: The site's asset rows after the apply.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SiteAsset" }
        "400": { description: Neither pv nor storage present, or values out of range. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant (RLS). }
  /api/v1/sites/{siteId}/battery:
    put:
      tags: [registry]
      summary: Maintain the site's battery master data and its controlling device.
      description: >
        Create or update the site's `battery` asset from customer-entered values
        (capacity/charge/discharge power and optional round-trip efficiency) -
        the manual path for plants not in the Marktstammdatenregister. The
        battery is controlled by the inverter, which is the edge device, so the
        request also maintains the device link: pass `deviceId` to pick the
        controlling device explicitly (validated to belong to this site), or omit
        it to auto-link the site's single device (the self-maintaining rule). A
        multi-device site with no explicit choice is left unlinked and the portal
        warns that the plan cannot be executed. Manual values leave the registry
        provenance untouched (a MaStR-linked battery keeps its provenance).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [capacityKwh, maxChargeKw, maxDischargeKw]
              properties:
                capacityKwh: { type: number, description: "Usable battery capacity (kWh), > 0." }
                maxChargeKw: { type: number, description: "Max charge power (kW), > 0." }
                maxDischargeKw: { type: number, description: "Max discharge power (kW), > 0." }
                roundtripEfficiencyPct:
                  type: number
                  nullable: true
                  description: Round-trip efficiency in percent (1-100); platform default 92 when omitted.
                deviceId:
                  type: string
                  format: uuid
                  nullable: true
                  description: The controlling device; omit to auto-link the site's single device.
                speicherschonung:
                  type: string
                  enum: [aggressiv, ausgewogen, schonend]
                  nullable: true
                  description: >
                    The customer's "Umgang mit dem Speicher" preset, mapped
                    server-side onto the battery's wear cost (aggressiv 1 ct /
                    ausgewogen 4 ct / schonend 8 ct per kWh cycled) - the
                    optimizer reads it on its next 15-min cycle. Omit to keep
                    the stored value. The raw ct value stays admin-only
                    (optimizer-config); a preset pick overwrites an
                    admin-configured custom value.
      responses:
        "200":
          description: The site's asset rows after the save.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SiteAsset" }
        "400": { description: Missing/invalid battery values. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant (RLS), or the chosen device is not at this site. }
    delete:
      tags: [sites]
      summary: Batterie am Standort abmelden (asset nameplate, flow claim and battery-hybrid component together).
      description: >
        Removes the site's battery as one action; its recorded telemetry is kept.
        Idempotent - a site without a battery returns its unchanged assets.
        Refused (409 `berichts_belege`, UEMS AP-12 E13 S2/IP-12) when the
        battery-hybrid component fed a Messstelle a released Berichtsstand
        cites - checked before anything is written.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The site's remaining asset rows.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SiteAsset" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
        "409":
          description: The battery is a Beleg of released Berichtsstände - nothing written.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BelegeImWeg" }
  /api/v1/sites/{siteId}/simulation:
    post:
      tags: [simulation]
      summary: Start an Ersparnis-Simulation for this Anlage (async job).
      description: >
        Submits a simulation over the reference year (default: the last full
        calendar year). Every input defaults from the site's master data
        (tariff, plant kind, anzulegender Wert, coordinates, battery asset
        incl. Speicherschonung, PV asset) and every body field OVERRIDES its
        default - the what-if instrument. Requires backfilled day-ahead
        prices for the reference year (`voltpilot-market-data backfill`).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SimulationRequest" }
      responses:
        "202":
          description: Job accepted; poll the status route.
          content:
            application/json:
              schema:
                type: object
                properties:
                  simulationId: { type: string }
        "400": { description: Invalid inputs (German message), incl. missing coordinates/battery or a not-backfilled price year. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
        "429": { description: Too many concurrent simulations (German message). }
        "502": { description: Simulation service unreachable. }
  /api/v1/sites/{siteId}/simulation/{simulationId}:
    get:
      tags: [simulation]
      summary: Poll a running/finished simulation of this Anlage.
      description: >
        Relays the simulation service's job document
        `{status: queued|running|done|failed, progress, result?, error?}`.
        `result` grows progressively (scenarios (a)/(b) after seconds, the
        VoltPilot months as their chunks solve); jobs expire after ~1 h. Only
        jobs STARTED through this site's route are visible here.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: simulationId, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: The job document (see SimulationResult for `result`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SimulationStatus" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or simulation not found (also after expiry/api restart - simply start a new run; the input cache makes it instant). }
  /api/v1/admin/simulation:
    post:
      tags: [simulation]
      summary: Start a prospect simulation (Vertrieb "Ersparnis-Rechner").
      description: >
        Platform-admin only. All inputs explicit (plant incl. coordinates,
        consumption, battery are required by the simulation service); nothing
        about the prospect is persisted (V1 stateless). The Speicherschonung
        preset maps onto the wear cost server-side.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SimulationRequest" }
      responses:
        "202":
          description: Job accepted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  simulationId: { type: string }
        "400": { description: Invalid/missing inputs (German message). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Caller is not a platform admin. }
        "429": { description: Too many concurrent simulations. }
        "502": { description: Simulation service unreachable. }
  /api/v1/admin/simulation/{simulationId}:
    get:
      tags: [simulation]
      summary: Poll a prospect simulation.
      parameters:
        - { name: simulationId, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: The job document.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SimulationStatus" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Caller is not a platform admin. }
        "404": { description: Simulation not found (expired, or not an admin job). }
  /api/v1/sites/{siteId}/kpis:
    get:
      tags: [kpis]
      summary: Economic KPIs (self-consumption, savings) for a site.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: KPIs.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteKpis" }
  /api/v1/admin/tenants:
    get:
      tags: [admin]
      summary: List all tenants (platform-wide).
      responses:
        "200":
          description: Every tenant on the platform.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Tenant" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin]
      summary: Create a tenant.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateTenantRequest" }
      responses:
        "201":
          description: Tenant created.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Tenant" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/admin/tenants/{tenantId}:
    put:
      tags: [admin]
      summary: Update a tenant's master data (name/segment/betriebsart).
      parameters:
        - { name: tenantId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateTenantRequest" }
      responses:
        "200":
          description: Updated tenant.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Tenant" }
        "400": { description: Invalid input (blank name or bad segment). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Tenant not found. }
  /api/v1/admin/tenants/{tenantId}/delete:
    post:
      tags: [admin]
      summary: Offboard a tenant (type-to-confirm full cascade).
      description: >
        The most destructive platform action. The body must carry the tenant's
        EXACT name (`confirmName`); a mismatch is refused (400) before anything
        is touched. All tenant accounts are disabled through the shared account
        path before the database cascade runs in ONE transaction. A database
        failure retains the data and leaves accounts disabled for a retry.
        Retained MQTT topics and Keycloak users are then cleaned best-effort;
        `failedUsers` names disabled accounts still pending deletion. Repeat
        directory cleanup with POST /tenants/{tenantId}/offboarding/cleanup.
        Existing customer JWTs of a removed tenant are rejected with 401,
        including GET /api/v1/me.
      parameters:
        - { name: tenantId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DeleteTenantRequest" }
      responses:
        "200":
          description: Tenant offboarded; the report of what was removed.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TenantOffboardingReport" }
        "400": { description: confirmName does not match the tenant's name. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Tenant not found. }
        "502": { description: Accounts could not be completely listed or disabled; no database teardown. }
  /api/v1/admin/tenants/{tenantId}/offboarding/cleanup:
    post:
      tags: [admin]
      summary: Repeat directory cleanup for an already removed tenant (platform-admin only).
      description: >
        Lists accounts by their exact tenant_id attribute, disables every remaining
        account, then retries deletion. Failed deletions remain disabled and are
        returned in failedUsers and logged. An empty list is a successful retry.
        An existing tenant is refused before any directory write.
      parameters:
        - { name: tenantId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Cleanup attempted; remaining disabled accounts are named in failedUsers.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/TenantOffboardingCleanup" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { description: Tenant still exists; use the confirmed offboarding route. }
        "502": { description: Accounts could not be completely listed or disabled; no deletions attempted. }
  /api/v1/admin/tenants/{tenantId}/beenden:
    post:
      tags: [admin]
      summary: Vertragsende I - set a Kundenbereich to "beendet" (UEMS AP-20 IP-16, platform-admin only).
      description: >
        aktiv -> beendet with Auftrag, Begruendung and the tenant's EXACT name (type-to-confirm, like the
        offboarding). From then on every write route of the Kundenbereich answers 409 kundenbereich_beendet
        (customer routes and platform routes carrying its tenant, site or device); only the
        Kundenadministrator still reads (GET /api/v1/me carries kundenbereich.beendet). fristTage defaults to 90 (the contract's value is the operator's);
        loeschung_fruehestens = day of the end + fristTage, Europe/Berlin. The transition is written to the
        platform protocol kundenbereich_uebergang and happens once - a second call is 409.
        The offboarding route is unchanged by this package.
      parameters:
        - { name: tenantId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [auftrag, begruendung, confirmName]
              properties:
                auftrag: { type: string }
                begruendung: { type: string }
                confirmName: { type: string }
                fristTage: { type: integer, minimum: 1, maximum: 3650, default: 90 }
      responses:
        "200":
          description: The Kundenbereich is beendet.
          content:
            application/json:
              schema:
                type: object
                properties:
                  zustand: { type: string, enum: [beendet] }
                  beendet_am: { type: string, format: date-time }
                  frist_tage: { type: integer }
                  loeschung_fruehestens: { type: string, format: date }
        "400": { description: confirmName does not match the tenant's name, or a mandatory field is empty. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Tenant not found. }
        "409": { description: kundenbereich_schon_beendet - the Kundenbereich is already beendet. }
  /api/v1/admin/tenants/{tenantId}/wiederaufnehmen:
    post:
      tags: [admin]
      summary: Vertragsende I - resume a beendet Kundenbereich (UEMS AP-20 IP-16, platform-admin only).
      description: >
        beendet -> aktiv with Auftrag and Begruendung; the same Kundenbereich, not a new one. Written to
        kundenbereich_uebergang; a second call is 409.
      parameters:
        - { name: tenantId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [auftrag, begruendung]
              properties:
                auftrag: { type: string }
                begruendung: { type: string }
      responses:
        "200":
          description: The Kundenbereich is aktiv again.
          content:
            application/json:
              schema:
                type: object
                properties:
                  zustand: { type: string, enum: [aktiv] }
        "400": { description: A mandatory field is empty. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Tenant not found. }
        "409": { description: kundenbereich_nicht_beendet - the Kundenbereich is not beendet. }
  /api/v1/admin/tenants/{tenantId}/sites:
    get:
      tags: [admin]
      summary: List the sites of a tenant (platform-admin, cross-tenant).
      parameters:
        - { name: tenantId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The tenant's sites.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Site" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Tenant not found. }
    post:
      tags: [admin]
      summary: Create a site for a tenant (platform-admin, cross-tenant).
      description: >
        Creates a site for the tenant named in the PATH, using the BYPASSRLS admin
        datasource. The tenant is validated to exist first. The tenant is never
        taken from client-controlled body data, so this is a deliberate,
        role-gated cross-tenant write - customer site access stays RLS-scoped.
        This route does not read `standortId` (UEMS AP-02 IP-9): the site is
        created without a Standort assignment, as before.
      parameters:
        - { name: tenantId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateSiteRequest" }
      responses:
        "201":
          description: Site created for the tenant.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Site" }
        "400": { description: Invalid input (blank name, out-of-range coordinates, or bad zone). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Tenant not found. }
  /api/v1/admin/tenants/{tenantId}/users:
    get:
      tags: [admin]
      summary: List the customer users (Portal-Users) of a tenant.
      parameters:
        - { name: tenantId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Users carrying this tenant's `tenant_id` in Keycloak.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/AdminUser" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Tenant not found. }
    post:
      tags: [admin]
      summary: Ersten Kundenadministrator mit Startpasswort anlegen.
      description: >
        Nur für einen Kundenbereich ohne bestehendes Konto. Das Konto bekommt tenant_id, keine Realm-Rolle
        und die Zuweisung Kundenadministrator. Das serverseitig erzeugte Startpasswort erscheint einmalig
        in dieser Antwort (Cache-Control no-store); Keycloak verlangt UPDATE_PASSWORD. Weitere Benutzer
        legt der Kundenadministrator über /api/v1/benutzer an. Passwortregeln bestimmt der Realm.
      parameters:
        - { name: tenantId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateUserRequest" }
      responses:
        "201":
          description: Erster Kundenadministrator angelegt; einmalige Passwortanzeige.
          headers:
            Cache-Control: { schema: { type: string, enum: [no-store] } }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenutzerAngelegt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Tenant not found. }
        "409": { description: A user with that username or email already exists. }
  /api/v1/admin/control-certifications:
    get:
      tags: [admin]
      summary: The PLATFORM register of control-certified inverter models.
      description: >
        "Model X is bench-certified for battery control" - entered ONCE and then
        valid fleet-wide. It is the platform's MEMORY of a bench run: before it,
        a certification landed either in the fleet-wide env allowlist (keyed on
        the register FAMILY, so one proof leaked onto untested siblings) or in a
        First-Light grant on ONE box, so the same model had to be certified again
        for every new customer.
        The key is brand+MODEL on purpose: a family covers several product lines,
        a bench run covers exactly one.
        The register ALONE controls nothing - a plant additionally needs its
        explicit activation (see `/control-activations`).
      responses:
        "200":
          description: The register, newest certification first.
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/ControlCertification" } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Not a platform-admin. }
    post:
      tags: [admin]
      summary: Certify an inverter model (a bench run ends here).
      description: >
        Adds a model to the register and republishes it to the whole fleet, so
        every already-activated plant with that model is granted without anyone
        touching it. An already-registered model is a CONFLICT, not a silent
        update - two statements about one product could drift apart.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CertifyControlModelRequest" }
      responses:
        "201":
          description: Certified.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ControlCertification" }
        "400": { description: Missing/invalid fields (German `message`). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Not a platform-admin. }
        "409": { description: This model is already in the register (German `message`). }
    delete:
      tags: [admin]
      summary: Take a model out of the register.
      parameters:
        - { name: brand, in: query, required: true, schema: { type: string } }
        - { name: model, in: query, required: true, schema: { type: string } }
      responses:
        "204": { description: Removed; the fleet gets the smaller register. }
        "400": { description: This model is not in the register. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Not a platform-admin. }
  /api/v1/admin/control-activations:
    get:
      tags: [admin]
      summary: The plants whose battery control is armed.
      responses:
        "200":
          description: One row per armed device.
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/ControlActivation" } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Not a platform-admin. }
  /api/v1/admin/devices/{deviceId}/control-activation:
    post:
      tags: [admin]
      summary: "\"Steuerung aktivieren\" - the ONE deliberate click per plant."
      description: >
        Arms this plant so a register-certified model may actually be written.
        It stays an explicit, safety-relevant decision (it is WRITE access to a
        customer's inverter) - the register only removes the need to REPROVE the
        model. Idempotent; publishes the retained certification document to the
        device immediately.
      parameters:
        - { name: deviceId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties: { note: { type: string, maxLength: 512 } }
      responses:
        "200":
          description: Armed.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ControlActivation" }
        "400": { description: Unknown device (German `message`). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Not a platform-admin. }
    delete:
      tags: [admin]
      summary: Disarm a plant - it falls back to read-only.
      parameters:
        - { name: deviceId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Disarmed; the device is told immediately. }
        "400": { description: Unknown device, or it was not armed. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Not a platform-admin. }
  /api/v1/admin/forecast-models:
    get:
      tags: [admin]
      summary: Die PLATTFORM-VORGABE des Prognosemodells (der Schalter-Zustand).
      description: >
        Der Zustand des plattformweiten Prognose-Schalters (Captain-Auftrag
        18.08.2026): je Prognoseart (load/pv) das VORGABE-Modell, WOHER sie
        kommt (portal = eine Zeile in forecast_model_choice, env = die
        Umgebungsvariable bzw. der Registry-Default), der Umgebungs-Vorgabewert,
        wer wann umgestellt hat und die wählbare Menge - plus die append-only
        Umstellungs-Historie (von->zu). Seit dem 19.08.2026 ist das die VORGABE
        für jede Anlage OHNE eigene Wahl, nicht die Antwort auf „was plant
        Anlage X" - dafür ist /api/v1/sites/{siteId}/forecast-models da.
        Platform-admin only.
      responses:
        "200":
          description: Der Schalter-Zustand.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ForecastModelChoice" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Nur für Portal-Admins. }
    post:
      tags: [admin]
      summary: Die Plattform-Vorgabe einer Prognoseart umstellen.
      description: >
        Stellt die VORGABE einer Prognoseart um - sie gilt für jede Anlage OHNE
        eigene Wahl (eine Anlage mit eigener Zeile bleibt unberührt). Ab dem
        nächsten Planungslauf konsumiert der Optimierer dort die Prognosereihen
        dieses Modells; das bisherige Modell rechnet unverändert im Schatten weiter
        und wird weiter täglich bewertet, der Rückweg ist derselbe Aufruf in
        die Gegenrichtung. Die Umstellung ist append-only protokolliert
        (von->zu, JWT-Subject, Zeitpunkt). Es wird nichts trainiert und kein
        Gerät erreicht. Ablehnungen tragen einen deutschen {message}-Körper:
        400 = unbekannte Art / unbekanntes Modell / Modell einer anderen Art,
        409 = dieses Modell plant bereits.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [kind, model]
              properties:
                kind: { type: string, enum: [load, pv] }
                model:
                  type: string
                  enum: [load-persistence, load-xgb, pv-physical, pv-residual-xgb]
      responses:
        "200":
          description: Der neue Schalter-Zustand (inkl. der neuen Journal-Zeile).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ForecastModelChoice" }
        "400": { description: Unbekannte Prognoseart bzw. unpassendes Modell. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Nur für Portal-Admins. }
        "409": { description: Dieses Modell plant bereits. }
  /api/v1/admin/edge-releases:
    get:
      tags: [admin]
      summary: The edge release register (OTA Stufe 0).
      description: >
        Every registered edge release, NEWEST FIRST. `releaseSeq` is THE
        ordering - a monotonic integer, never a string or SHA comparison
        (decision D5). Without this register "outdated" was not well-defined:
        the fleet view compared a reported version against the fleet MAXIMUM
        (so "all equally old" read as "all current") and compared 12-char hex
        SHAs as number blocks, which is a random ordering on SHAs.
        Platform-admin only (the narrow `edge-release-publisher` role does NOT
        reach this route: the list carries every manifest and the author of
        every entry - more than registering needs). Read-only from the customer
        side, and there is NO write path towards any device here.
      responses:
        "200":
          description: The register, newest first.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/EdgeRelease" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin]
      summary: Register an edge release.
      description: >
        `version` must follow the release tag schema `edge-YYYY.MM.N`
        (edge-images already stamps `<tag>-<sha>`); a bare commit SHA is
        refused because it is exactly the uncomparable value the register
        replaces. `releaseSeq` may be omitted - it then continues the register
        (max + 1); passing it explicitly requires it to be strictly greater
        than every existing entry, so the ordering stays monotonic by
        construction. Registering an existing version with DEVIATING bytes is a
        409, never a silent no-op: the register is the paper trail. A repeat
        with BYTE-IDENTICAL `manifest`/`signature`/`notes` answers 200 with the
        stored entry instead - a re-run CI job behind a successful registration
        must not go red, and identical bytes swallow nothing.
        Reachable by `platform-admin` AND by the narrow `edge-release-publisher`
        service role used by the `edge-*` tag pipeline; that role reaches
        nothing else in the admin tree (no rollout, no wave, no device target).
        Since OTA Stufe 1 the body may additionally carry the SIGNED release
        (`manifest` + `signature`, byte for byte). The api stores those bytes
        unchanged and checks only CONSISTENCY - it deliberately does NOT verify
        the signature: the only verifier that matters is the DEVICE with its
        baked-in root, and a register that "blessed" a manifest would create
        confidence where nothing can be guaranteed. See docs/ota-signing.md.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateEdgeReleaseRequest" }
      responses:
        "201":
          description: Release registered.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EdgeRelease" }
        "200":
          description: >
            Already registered with BYTE-IDENTICAL manifest/signature/notes -
            the stored entry, unchanged (an idempotent CI re-run).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EdgeRelease" }
        "400": { description: Version does not match edge-YYYY.MM.N, or releaseSeq is not strictly increasing. }
        "409": { description: This version is already registered with DIFFERENT bytes. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/admin/edge-releases/next-seq:
    get:
      tags: [admin]
      summary: The next free release sequence number.
      description: >
        The ONE thing the release publisher must read. `release_seq` lives
        INSIDE the signed manifest and the signature covers exactly its bytes,
        so the ordering has to be fixed BEFORE signing - the api cannot hand it
        out at registration time (the manifest would already carry a different
        number) and CI must not guess it.
        Deliberately a separate, narrow route rather than publisher access to
        the full register listing.
        HONEST LIMIT: between this read and the registration lies the signing
        step, so there is a TOCTOU window. If a second release registers in
        between, the POST fails LOUDLY on the monotonicity check and the run is
        repeated with the new number - reserving instead would punch holes into
        the ordering whenever a run aborts.
        Reachable by `platform-admin` and by `edge-release-publisher`.
      responses:
        "200":
          description: The next number, plus the current head of the register.
          content:
            application/json:
              schema:
                type: object
                required: [nextSeq]
                properties:
                  nextSeq: { type: integer, format: int64, example: 13 }
                  currentSeq:
                    type: integer
                    format: int64
                    nullable: true
                    description: Highest assigned number; null on an empty register.
                  currentVersion:
                    type: string
                    nullable: true
                    example: edge-2026.08.0
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/edge/trust-set/trust-set.json:
    get:
      tags: [registry]
      summary: The current root-signed trust-set (raw bytes), for a box being INSTALLED.
      security: []
      description: >
        One of the two files that go into `<data>/ota/` on the device; its
        sibling is `trust-set.json.sig` below. Serves the install-time TOFU
        moment - see docs/ota-signing.md §6.
        RAW BYTES, verbatim - deliberately NOT wrapped in a JSON envelope: in an
        envelope the document would be an ESCAPED JSON string, and the consumer
        is a shell installer that would then have to decode it by hand. That is
        exactly where silent byte drift creeps in and the signature stops
        verifying. So the routes are NAMED like the target files and the
        installer simply writes what it downloads (`curl -o`).
        THE TRUST BOUNDARY, and it is the whole reason this route may exist:
        a box being INSTALLED trusts its installation channel by definition
        (it just pulled its images over it), so serving the CURRENT
        root-signed trust-set over the same channel adds NO new trust - the
        box still verifies the ROOT signature itself against its BAKED-IN
        root, the channel only transports public material. LATER replacement
        stays OUT-OF-BAND: a RUNNING box never fetches a trust-set over the
        network (that would let the revocation anchor travel the channel it
        revokes), so there is no downlink and no device-side fetch; the core
        does not know this route. `update.sh` may DETECT a missing set and
        say how to fix it - it never downloads one.
        UNAUTHENTICATED on purpose: a box being installed has no token yet
        (the enrollment endpoints are open for the same reason), and what is
        handed out is public key material that is readable in git anyway
        (edge-app/ota/trust-set.json) - that readability is exactly what makes
        the trusted release keys auditable.
      responses:
        "200":
          description: The trust-set document, byte for byte.
          content:
            application/json:
              schema: { type: string, description: The verbatim bytes of trust-set.json. }
        "404":
          description: >
            No trust-set uploaded yet. The installer degrades with a loud
            warning plus the manual path and the install still SUCCEEDS - a box
            without a trust-set works fully, it just cannot (yet) apply a
            release.
  /api/v1/edge/trust-set/trust-set.json.sig:
    get:
      tags: [registry]
      summary: The detached root signature over the trust-set (raw bytes).
      security: []
      description: >
        The second of the two files - the cold root's signature over exactly
        the bytes served by `trust-set.json`. Same trust boundary, same raw-byte
        rule (see the sibling route). A set without its signature is worthless
        (the device refuses fail-closed), so an installer fetches both or
        neither.
      responses:
        "200":
          description: The detached signature, byte for byte.
          content:
            application/json:
              schema: { type: string, description: The verbatim bytes of trust-set.json.sig. }
        "404": { description: No trust-set uploaded yet. }
  /api/v1/admin/edge-trust-set:
    get:
      tags: [admin]
      summary: The current trust-set with its operator metadata.
      description: >
        What is in force, who uploaded it when, which RELEASE key ids it
        trusts and which ROOT key signed it. Platform-admin only (the narrow
        `edge-release-publisher` role may upload but not inspect - uploading
        needs none of this).
      responses:
        "200":
          description: The current trust-set.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EdgeTrustSet" }
        "404": { description: No trust-set uploaded yet. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    put:
      tags: [admin]
      summary: Set or replace the current root-signed trust-set.
      description: >
        Upload the two files from the ceremony (docs/ota-signing.md §3.4).
        A rotation is exactly this: a new, freshly root-signed set (§7.1).
        PUT because there is a SINGLETON - "the current set" is a resource,
        not a history; re-uploading identical bytes is therefore idempotent
        (a re-run automation job must not go red), and the HISTORY lives where
        it belongs, as a reviewable commit in edge-app/ota/.
        Reachable by `platform-admin` AND by the narrow `edge-release-publisher`
        service role (the trust-set comes out of the same ceremony as a release
        key, and the automation should be able to keep it current without an
        admin token in CI); that role still reaches no rollout and no device.
        The api stores the bytes UNCHANGED and checks only the FORM - it
        deliberately does NOT verify the root signature: the only verifier that
        matters is the DEVICE with its baked-in root. The form checks are: both
        documents parse, `alg` is ed25519 (never a fallback - the JWT alg
        hole), the signature's `domain` is `trust-set` (else a RELEASE
        signature would pass as a trust-set signature), and the SIGNING (root)
        key must NOT appear in the set itself - the invariant `vp-ota
        trust-set` already enforces, because a set containing the root could
        EXTEND the root. Note the key ids of the two files must DIFFER, unlike
        the release case where manifest and signature name the same key.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SaveEdgeTrustSetRequest" }
      responses:
        "200":
          description: Trust-set stored; it is now what a fresh install receives.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EdgeTrustSet" }
        "400": { description: The set or its signature is malformed (German message). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/admin/edge-updates:
    get:
      tags: [admin]
      summary: Everything the platform page "Edge-Updates" shows (OTA Stufe 2).
      description: >
        ONE server-side aggregate (the /admin/fleet discipline): the release
        register, the most recent updates with their device rows, the fleet
        matrix (Anlage · Mandant · Ist · Soll · Zustand · seit · Grund) and the
        append-only audit journal.
        HONESTY RULES, enforced server-side: "unbekannt" is never "veraltet"
        (a device that reports nothing says nothing about its age), "offline" is
        never "fehlgeschlagen" (the assignment waits retained on the broker and
        is picked up on reconnect), every non-green state carries its reason,
        and the KPI percentages count only the REACHABLE set.
        `rollouts` carries the newest updates whatever their state (a LIST since
        several updates may run side by side); one that just finished must not
        vanish from the page in the very moment it completed. Platform-admin
        only; there is deliberately NO customer-facing surface for updates.
      responses:
        "200":
          description: Releases, the recent updates, the fleet matrix, the journal and the KPI.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/admin/devices:
    get:
      tags: [admin]
      summary: The device INVENTORY over the whole lifecycle (platform page "Geräte").
      description: >
        The UNION of the sticker registry and the real fleet, joined on the
        external reference - one row per device, whichever half exists.
        It closes the finding that the page called "Geräte-Registry" did not
        contain the real fleet at all: its table listed only `VP-` sticker IDs,
        while the shipped boxes connect through self-generated `edge-`
        references and appeared there with ZERO rows.
        `deviceId == null` is a printed sticker ID that no customer has
        connected yet; `provisioned == false` is a connected device whose
        reference never came from the registry (the normal case for the
        installed base). A row that is not yet a device carries NO state -
        deriving one would be a claim about a device that does not exist.
        The state fields are the SAME derivation as the fleet row
        (RolloutStates), so the two surfaces can never disagree.
        Platform-admin only.
      responses:
        "200":
          description: One row per device - registry half, fleet half, or both.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/admin/rollouts:
    post:
      tags: [admin]
      summary: Update a set of devices to ONE signed release - in one step.
      description: >
        The WHOLE flow (Captain order 2026-08-26): pick a release, pick the
        devices, done. There are no waves, no canary ring, no bake criterion and
        no second button - every chosen device gets its assignment in this call,
        verifies the signed manifest itself against its baked-in root, and
        applies it. Nobody has to touch the device.


        Only a SIGNED release can be distributed (409 otherwise): without
        manifest bytes a device has nothing to check, and the downlink would be
        an instruction without evidence. That is the ONLY precondition left -
        and it is a property of the RELEASE, not of the device.


        Several updates may run SIDE BY SIDE - the former "at most one live
        rollout" bar was a gate that blocked a legitimate second order. What
        stays unique is what matters: exactly one assignment per DEVICE.


        Assigning publishes the signed manifest RETAINED on
        `ems/{t}/{s}/{d}/v2/update` (contract mqtt-ota-target.schema.json), so a
        device that is offline right now picks it up on its next connect.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [releaseSeq, devices]
              properties:
                releaseSeq: { type: integer, format: int64 }
                devices:
                  type: array
                  minItems: 1
                  items: { type: string, format: uuid }
      responses:
        "201":
          description: >
            Started; every chosen device has its assignment - except devices of
            an ENDED customer area (UEMS AP-20, E10 = A "alles gesperrt"): they
            get no row and no retained instruction, and `ausgelassen` names them
            (additive; empty otherwise).
          content:
            application/json:
              schema:
                type: object
                required: [rolloutId, ausgelassen]
                properties:
                  rolloutId: { type: string, format: uuid }
                  ausgelassen:
                    type: array
                    items:
                      type: object
                      required: [deviceId, label, kundenbereich, grund]
                      properties:
                        deviceId: { type: string, format: uuid }
                        label: { type: string }
                        kundenbereich: { type: string, description: Name of the ended customer area. }
                        grund: { type: string, enum: [kundenbereich_beendet] }
        "400": { description: No devices, or an unknown device. }
        "404": { description: The release is not registered. }
        "409": { description: The release is not signed - or every chosen device belongs to an ended customer area (kundenbereich_beendet). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/devices/{deviceId}/update-target:
    post:
      tags: [admin]
      summary: Assign ONE device a release.
      description: >
        The single-device path (the device page). Only a signed release is
        assignable (409 otherwise); the device does the rest by itself.
      parameters:
        - name: deviceId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [releaseSeq]
              properties:
                releaseSeq: { type: integer, format: int64 }
      responses:
        "204": { description: Assigned; the signed manifest went out retained. }
        "404": { description: Device or release not found. }
        "409": { description: The release is not signed. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/devices/{deviceId}/update-target/revert:
    post:
      tags: [admin]
      summary: Withdraw a device's assignment.
      description: >
        The row goes AND the retained message is cleared - otherwise an
        instruction nobody owns any more would wait on the broker for the
        device to return.
      parameters:
        - name: deviceId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "204": { description: Withdrawn. }
        "404": { description: Device not found. }
        "409": { description: This device has no assignment. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/admin/provisioned-devices:
    get:
      tags: [admin]
      summary: List the manufacturing registry of sticker Geräte-IDs.
      description: >
        Platform-wide view of every provisioned (manufactured) device ID and its
        claim state - which tenant, if any, has connected it.
      responses:
        "200":
          description: All provisioned devices.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ProvisionedDevice" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin]
      summary: Register a manufactured sticker Geräte-ID.
      description: >
        Registers a device ID (canonicalized like the claim path - trimmed,
        uppercased) so a customer can claim it. Only sticker-format IDs
        (prefix `VP-`) are accepted. Idempotent - re-registering an existing ID
        returns 200 with the unchanged entry.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProvisionDeviceRequest" }
      responses:
        "200":
          description: Already registered - the existing, unchanged entry.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ProvisionedDevice" }
        "201":
          description: Geräte-ID registered.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ProvisionedDevice" }
        "400": { description: Blank or non-sticker-format (missing VP- prefix) Geräte-ID. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/admin/provisioned-devices/{externalRef}:
    delete:
      tags: [admin]
      summary: Remove a wrongly registered sticker Geräte-ID from the registry.
      description: >
        Refused (409) while a customer's claim references the ID - the device
        must be unclaimed first. The path ref is canonicalized like the claim
        path (trimmed, uppercased).
      parameters:
        - { name: externalRef, in: path, required: true, schema: { type: string }, example: VP-1234-ABCD }
      responses:
        "204": { description: Registry entry removed; the ID can no longer be claimed. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: No such registry entry. }
        "409": { description: The ID is claimed by a customer; unclaim the device first. }
  /api/v1/admin/enrollments/pending:
    get:
      tags: [admin]
      summary: List enrolled-but-unclaimed devices.
      description: >
        Operator view onto devices that uploaded an enrollment CSR but have no
        matching claimed device row (the ref was never claimed - e.g. a customer
        typed a different reference - or was unclaimed after issuance). Surfaces
        the otherwise-invisible half of a mistyped-reference dead-end. Read-only;
        the device-facing enrollment poll stays opaque (pending == unknown).
      responses:
        "200":
          description: Pending (enrolled-but-unclaimed) enrollments, newest CSR first.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/PendingEnrollment" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/admin/fleet:
    get:
      tags: [admin]
      summary: The ONE fleet aggregate behind the platform overview (Flotten-Puls).
      description: >
        One row per site across ALL tenants, server-side: the overview core
        fields (device liveness from each device's newest telemetry ARRIVAL,
        5-min window; worstStatus stale > waiting > online), the age of the
        newest optimizer run, the control/curtailment evidence, the reported
        edge version, the reported source health, and the SERVER-derived
        maintenance flags (B4: tariff, battery without device, kWp plausibility,
        fleet-wide forecast outliers). It replaces the platform overview's
        client-side loop over tenants; the surface stays the same.
        Platform-admin only (a customer token gets 403), read-only, and the
        cross-tenant read is confined to the dedicated BYPASSRLS admin
        datasource exactly like the other /api/v1/admin/** reads - customer
        endpoints stay on the RLS path untouched.
        Honesty is part of the contract: a block nobody measured is null and
        carries its reason - never a fabricated zero, never a guessed verdict.
      responses:
        "200":
          description: One row per site, tenant then site name.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminFleet" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/admin/sites/{siteId}/v2-entities:
    get:
      tags: [admin]
      summary: List a site's v2 entity registry rows (E1a pilot).
      description: >
        Platform-admin, through the RLS-scoped path via the X-Tenant-Id
        switcher (no or wrong tenant selected => 404). Entities are
        measurement_point rows with a non-null entityType; capabilities and
        guards carry the docs/contracts/v2/edge-entity.schema.json objects
        verbatim.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The site's v2 entities.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/V2Entity" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not found (or no tenant selected). }
  /api/v1/admin/sites/{siteId}/v2-entities/adopt:
    post:
      tags: [admin]
      summary: Adopt an edge-reported source into a v2 entity (U2 "Vom Gerät gemeldet").
      description: >
        U2 adoption bridge (report §3.3, admin-only first increment): the
        customer's :8484 device reports a source (go-e wallbox, AC-coupled PV, a
        grid meter) in its heartbeat local_setup; this creates the matching
        entity in ONE step, prefilled from the report + the type catalog, and
        PINS it to the reporting source (`sourceId`) so the "Vom Gerät gemeldet"
        matcher is deterministic and re-adoption is idempotent (a re-adopt
        returns the existing entity). A non-composed consumer type is created
        like a v2-native entity; a composed producer/grid-meter is recorded as a
        read-only source measurement point (its kWp / MaStR SEE # are the
        customer-only master data captured here - the ErzeugerSourcesPanel job,
        §3.4). Platform-admin, through the RLS-scoped X-Tenant-Id switcher.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [sourceId, entityType]
              properties:
                sourceId: { type: string, description: The edge source id from local_setup. }
                entityType: { type: string, example: wallbox }
                label: { type: string }
                maxPowerKw: { type: number, description: Consumer rated power (bounds commands). }
                capacityKwp: { type: number, description: Producer nameplate (kWp). }
                registryUnitId: { type: string, description: MaStR SEE number of the source. }
      responses:
        "200":
          description: The adopted (or already-existing) entity.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/V2Entity" }
        "400": { description: Missing sourceId/entityType or unknown type. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not found (or no tenant selected). }
        "422": { description: The type cannot be adopted as a source (e.g. battery-hybrid). }
  /api/v1/admin/sites/{siteId}/v2-entities/preview:
    get:
      tags: [admin]
      summary: Dry-run the v1->v2 conversion (MIG migration step 1) - writes nothing.
      description: >
        The migration runbook review step: reports EXACTLY what
        /v2-entities/bootstrap would create/refresh from the site's current v1
        master data - the entities, their derived topology roles, capabilities
        and guards, and the resolved gateway device - WITHOUT touching any
        table. `action` is `create` for a row that would newly become a v2
        entity and `refresh` for one already stamped (re-run). Idempotent + safe
        to call repeatedly while reviewing.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The conversion plan (nothing written).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/V2ConversionPreview" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not found (or no tenant selected). }
  /api/v1/admin/sites/{siteId}/v2-entities/history-cutover:
    get:
      tags: [admin]
      summary: The site's v1->v2 history bridge cutover instant (null = un-migrated).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The current cutover (cutoverAt null when un-migrated).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/V2HistoryCutover" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not found (or no tenant selected). }
    put:
      tags: [admin]
      summary: Set the history cutover (portal Historie splices v1 before / v2 after).
      description: >
        MIG runbook: called when the site goes live on v2 so the portal Historie
        switches its data source at this instant (v1 5-channel telemetry before,
        v2 per-entity reconstruction after). Body `at` optional - defaults to now.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties: { at: { type: string, format: date-time } }
      responses:
        "200":
          description: The applied cutover instant.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/V2HistoryCutover" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not found (or no tenant selected). }
    delete:
      tags: [admin]
      summary: Clear the history cutover (MIG rollback - Historie reverts to pure v1).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Cleared. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not found (or no tenant selected). }
  /api/v1/admin/sites/{siteId}/v2-entities/bootstrap:
    post:
      tags: [admin]
      summary: Create/refresh the three pilot entities from the site's v1 master data.
      description: >
        The E1a pilot-mapping utility (used by the E13a cutover; nothing
        converts automatically): battery asset -> battery-hybrid entity (creates
        the site's control measurement_point row when absent), pv-generation
        points -> producer entities, the grid-meter point -> grid-meter entity.
        Idempotent - re-running refreshes capabilities/guard config from the
        current master data. Afterwards the registry is pushed BEST-EFFORT as
        one retained message on ems/{t}/{s}/{d}/v2/entities (the response's
        push block reports whether it went out).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Resulting entities + skipped notes + push outcome.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/V2EntityBootstrapResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not found (or no tenant selected). }
  /api/v1/admin/sites/{siteId}/v2-entities/adopt-from-device:
    post:
      tags: [admin]
      summary: Bestands-Übernahme jetzt versuchen (Einheitsmodell Stufe 2).
      description: >
        Die Übernahme läuft ohnehin AUTOMATISCH (getakteter Abgleich); diese
        Route stößt sie ohne Wartezeit an und - vor allem - nennt den GRUND,
        wenn eine Anlage noch box-verwaltet ist ("meldet noch nicht, wie ihre
        Geräte angebunden sind", "steht noch nicht in unserem
        Geräte-Verzeichnis", …). Sie ist ALLES ODER NICHTS: ein unvollständig
        gemeldetes Ist übernimmt gar nichts.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Ausgang der Prüfung (übernommen oder der Grund dagegen).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComponentAdoptionOutcome" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not found (or no tenant selected). }
  /api/v1/admin/sites/{siteId}/v2-entities/revert-to-device:
    post:
      tags: [admin]
      summary: Der RÜCKWEG - die Anlage wieder am Gerät verwalten lassen.
      description: >
        Nimmt AUSSCHLIESSLICH die Autorität zurück (component_authority ->
        box). Die gespeicherten Definitionen bleiben stehen - sie sind der
        Beleg, was übernommen wurde, und der Weg zurück nach vorn. Der folgende
        Registry-Push trägt das Autoritäts-Feld dann nicht mehr, womit der
        Applier auf der Box strukturell nichts mehr anwendet und :8484 wieder
        bedient. Der Übernahme-Stempel wird gelöscht, damit der getaktete
        Abgleich die Anlage erneut betrachtet.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Ausgang.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComponentAdoptionOutcome" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not found (or no tenant selected). }
  /api/v1/admin/sites/{siteId}/v2-entities/push:
    post:
      tags: [admin]
      summary: Re-push the stored v2 entity registry to the gateway device.
      description: >
        Composes the current registry push payload and publishes it retained
        (best-effort; e.g. after a broker outage or a device re-claim).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Current entities + push outcome.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/V2EntityBootstrapResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not found (or no tenant selected). }
    post:
      tags: [admin]
      summary: Create a v2-native entity of an open catalog type (E1b).
      description: >
        Create an entity of an open catalog type (wallbox / heating-rod /
        generic-load / ...; the type catalog at GET
        /api/v1/admin/entity-type-catalog is the truth). capabilities and
        guards default from the catalog (bounded by maxPowerKw); explicit
        JSON overrides are validated against the contract shapes. A COMPOSED
        pilot type (battery-hybrid/producer/grid-meter) is refused (422) -
        those are maintained through their v1 master data + the bootstrap.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SaveV2EntityRequest" }
      responses:
        "200": { description: The created entity., content: { application/json: { schema: { $ref: "#/components/schemas/V2Entity" } } } }
        "400": { description: Unknown type / invalid capabilities or guards. }
        "422": { description: A composed pilot type cannot be created directly. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not found (or no tenant selected). }
  /api/v1/admin/sites/{siteId}/v2-entities/{pointId}:
    put:
      tags: [admin]
      summary: Edit a v2 entity (label; config only for non-composed types).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: pointId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SaveV2EntityRequest" }
      responses:
        "200": { description: The updated entity., content: { application/json: { schema: { $ref: "#/components/schemas/V2Entity" } } } }
        "400": { description: Invalid capabilities or guards. }
        "422": { description: A composed pilot type's config is not editable here. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Entity/site not found (or no tenant selected). }
    delete:
      tags: [admin]
      summary: Remove a v2 entity (v1-backed rows only lose their entity config).
      description: >
        purgePoint=true additionally deletes the measurement point outright -
        its kWp is released from the aggregate asset.pv and its edge-source pin
        is freed. The duplicate-cleanup lever (vp-vier-erzeuger-p9): without it
        a wrongly adopted duplicate keeps its pinned point and reappears as
        "Neues Gerät gefunden" forever.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: pointId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: purgePoint, in: query, required: false, schema: { type: boolean, default: false } }
      responses:
        "204": { description: Removed. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Entity/site not found (or no tenant selected). }
  /api/v1/admin/entity-type-catalog:
    get:
      tags: [admin]
      summary: The data-driven entity-type catalog the admin entity editor uses.
      description: >
        The one truth for known entity types (entitytypes/catalog.json):
        category (steers edge guard semantics), controllable, composed, and
        the default capabilities/guards/failsafe seeded on create. Adding a
        type is catalog data + an optional edge driver, never a schema release.
      responses:
        "200": { description: The catalog document., content: { application/json: { schema: { type: object } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/sites/{siteId}/entities:
    get:
      tags: [sites]
      summary: The site's v2 entities with Soll/Ist drift + the edge-local commissioning view.
      description: >
        The customer "Geräte & Entitäten" surface: the composed registry Soll
        (revision) reconciled against the edge-reported observed Ist (health,
        last telemetry, applied revision) - drift is SURFACED via syncStatus
        (in_sync | pending | missing_on_device | unreported | never_pushed),
        never silently resolved. localSetup is the edge-authoritative
        commissioning view (:8484 inverter/sources); staleOnDevice lists
        entities the edge still reports that the registry no longer contains.
        Tenant-scoped (foreign site 404); admins read via the X-Tenant-Id
        switcher, WRITES stay under /admin/.../v2-entities.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200": { description: The site's entity surface., content: { application/json: { schema: { $ref: "#/components/schemas/SiteEntities" } } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found. }
  /api/v1/sites/{siteId}/entities/{entityId}/history:
    get:
      tags: [sites]
      summary: Per-entity channel history over the v2 telemetry rollups.
      description: >
        Channel-keyed bucket series of one entity: day = live 15-min buckets
        from raw telemetry_v2, week = hourly rollup, month/year = Europe/Berlin
        daily rollup (the v1 history discipline). RLS-scoped.
        For a COMPOSED entity (battery-hybrid / grid-meter / house-load) the
        part of the window BEFORE the v2 era is reconstructed read-side from the
        site's v1 telemetry through the same channel map the writer's
        ComposedEntityFanout writes forward with, spliced at
        max(site.v2_history_cutover_at, the entity's first v2 sample in the
        window) - v1 owns buckets starting before it, v2 those at/after it, so
        the series is gap-free and overlap-free across the migration. Nothing is
        copied. A v2-native entity (wallbox, generic Modbus) is pure v2.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: entityId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: range, in: query, required: false, schema: { type: string, enum: [day, week, month, year], default: day } }
        - { name: at, in: query, required: false, schema: { type: string, format: date } }
      responses:
        "200": { description: The channel series., content: { application/json: { schema: { $ref: "#/components/schemas/EntityHistory" } } } }
        "400": { description: Bad range. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Entity/site not found. }
  /api/v1/soc-curve-templates:
    get:
      tags: [components]
      summary: The OCV curve templates of the SoC derivation (P5b/P5d).
      description: >
        Fills the `curve_charge`/`curve_discharge` fields of a `soc_derivation`
        in the battery assistant. It exists so the portal does NOT rebuild the
        support points: a curve in the frontend would be a twin of
        `soccurves/catalog.json` and free to drift from it - and a drifted curve
        is a wrong state of charge with decimal places.


        Authenticated, NOT plant-scoped - a curve is a statement about a CELL
        CHEMISTRY, not about a plant. Every template carries its `chemistry` and
        its cell window visibly, so the surface can NAME them: the same voltage
        means a completely different state of charge on a LiFePO4 cell than on
        an NMC one.


        The list grows only with MEASURED tables; an invented "LiFePO4 generic"
        would not be a template but a trap with a seal of approval.
      responses:
        "200":
          description: All templates in catalog order.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SocCurveTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/component-templates:
    get:
      tags: [templates]
      summary: The component templates the later add-component assistant renders from.
      description: >
        Einheitsmodell Stufe 0a. Newest version per template_ref, sorted by
        brand then model. ONLY the public origins (`builtin` + `certified`) are
        served - a `custom` template (private per Anlage, Stufe 3) has no tenant
        fence on this global table and is therefore never returned.
        Authenticated, not admin-gated (a template is the device choice the
        customer sees, not internals). An EMPTY list means the register is empty
        (the startup mirror did not run), never "there are no devices".
      responses:
        "200":
          description: The public templates.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ComponentTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/component-templates/{templateRef}:
    get:
      tags: [templates]
      summary: One component template (newest version).
      description: >
        Unknown AND non-public answer identically with 404, so the route never
        reveals whether a key exists (the enrollment "pending == unknown"
        discipline).
      parameters:
        - name: templateRef
          in: path
          required: true
          description: >
            The stable, OPAQUE template key (built-in templates use
            `builtin:<brand>:<model>`). Never parse it - brand/model ride as
            their own fields.
          schema: { type: string, pattern: "^[a-z0-9][a-z0-9._:-]{0,127}$" }
      responses:
        "200":
          description: The template.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComponentTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Unknown or non-public template. }
  /api/v1/sites/{siteId}/components:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [components]
      summary: The plant's components, with the authority + Soll/Ist state.
      description: >
        Einheitsmodell Stufe 1. `componentAuthority` says WHO owns this plant's
        device configuration (`box` | `portal`) - the portal offers the add
        assistant ONLY on a portal-managed plant, because a Soll saved on a
        box-managed one would never take effect. `appliedRevision` is what the
        BOX reported it really applied; `null` means UNKNOWN (older edge, no
        heartbeat yet), never "did not arrive". A refusal rides NEXT TO the
        applied revision, never in its place - what runs is still the last
        configuration that really worked.
      responses:
        "200":
          description: The components.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteComponents" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site (or component) not found for this tenant. }
    post:
      tags: [components]
      summary: Add a component (assistant step 4).
      description: >
        Brand/model/family/communication come from the TEMPLATE, never from the
        body - a client cannot save an inconsistent connection. Requires a
        PASSED connection test for exactly this plant + template + connection
        (422 otherwise): since this stage the stored Soll IS the plant's read
        path, and a typo in the IP makes it blind.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SaveComponentRequest" }
      responses:
        "200":
          description: The plant's components after the write.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteComponents" }
        "400": { description: Unknown template or role. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site (or component) not found for this tenant. }
        "409":
          description: >
            Box-managed plant, or the plant already has this component
            (at most one grid meter / one inverter). German `message`.
        "422": { description: No passed connection test for this connection. }
  /api/v1/sites/{siteId}/components/{entityId}:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - name: entityId
        in: path
        required: true
        schema: { type: string, format: uuid }
    put:
      tags: [components]
      summary: Edit an existing component in place - a NEW version.
      description: >
        The entity id and previous versions stay intact. `expectedRevision`
        prevents lost updates. A connection test is required only when the
        template or effective connection changes, and then for exactly that new
        pair. Secrets are returned masked and absent/masked values retain the
        old server-side secret. Compatible role/family changes are versioned;
        a family change records an effective marker and never reinterprets old samples.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SaveComponentRequest" }
      responses:
        "200":
          description: The plant's components after the write.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteComponents" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site (or component) not found for this tenant. }
        "409": { description: Box-managed plant, incompatible topology, or stale expectedRevision. }
        "422": { description: No passed connection test for this connection. }
  /api/v1/sites/{siteId}/components/{entityId}/versions:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - name: entityId
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [components]
      summary: The component's definition versions, newest first.
      responses:
        "200":
          description: The versions.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ComponentDefinition" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site (or component) not found for this tenant. }
  /api/v1/sites/{siteId}/components/{entityId}/events:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - name: entityId
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [components]
      summary: Effective edit, family-change, rollback and device-replacement markers for a stable component id.
      responses:
        "200":
          description: Append-only change markers, newest first.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ComponentChangeEvent" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site (or component) not found for this tenant. }
  /api/v1/sites/{siteId}/components/{entityId}/versions/{version}/rollback:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - name: entityId
        in: path
        required: true
        schema: { type: string, format: uuid }
      - name: version
        in: path
        required: true
        schema: { type: integer, minimum: 1 }
    post:
      tags: [components]
      summary: Roll a component back to an earlier version.
      description: >
        WRITES a new version carrying the old content instead of deleting one -
        "what ran last week" stays answerable, and the way back is the same way
        as any other change. No connection test is required: this connection was
        stored before and passed its test then; demanding a new one would block
        the way out of a mistake exactly when the device is not answering.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [expectedRevision]
              properties:
                expectedRevision: { type: integer, minimum: 1 }
      responses:
        "200":
          description: The plant's components after the rollback.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteComponents" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Unknown component or version. }
        "409": { description: Box-managed plant. }
  /api/v1/sites/{siteId}/component-test:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [components]
      summary: Connection test - the box reads the not-yet-saved device once.
      description: >
        Runs the assistant's connection test over the Probe-Kanal
        (docs/contracts/mqtt-probe.schema.json, op `test_connection`) - the SAME
        machinery the device's own `:8484` button has always used, generalized
        to every transport. A SUCCESS also records the receipt that unlocks
        saving (for exactly this plant + template + connection); a failure
        records nothing. A timeout is an honest OUTCOME (HTTP 200 with
        `errorCode: timeout`), not an error. Nothing is persisted.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [templateRef, connection]
              properties:
                templateRef: { type: string }
                templateVersion:
                  type: integer
                  minimum: 1
                  description: Exact template definition tested; omitted only by legacy clients.
                role: { type: string, enum: [inverter, pv-generation, grid-meter, consumer] }
                connection:
                  type: object
                  additionalProperties: true
                  description: The template's transport_schema fields, passed through.
                deviceId:
                  type: string
                  format: uuid
                  description: >
                    Optional - the box to ask. Absent means "the plant's single
                    device"; a plant with several must name one.
                entityId:
                  type: string
                  format: uuid
                  description: Optional edited component whose stored secrets are merged server-side for this exact test.
      responses:
        "200":
          description: The device's answer (honest outcome, including a timeout).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ProbeResult" }
        "400": { description: Unknown template / malformed request. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site (or component) not found for this tenant. }
        "409": { description: The plant has no device, or several and none was named. }
        "503": { description: The plant is not reachable right now. }
  /api/v1/sites/{siteId}/component-match:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [components]
      summary: "Does this device already have a component? (the suggestion BEFORE the click)"
      description: >
        Answers ONE question: would saving this device TAKE OVER an existing,
        orphaned component - and what is it called? It runs the very same rule
        `POST /components` runs afterwards (`ComponentTakeover`), so the
        assistant can say "this is probably your existing 'Fronius Anlage WR1'"
        instead of silently minting a nameless parallel component (Alias
        continuity, live case Herzogau 20.08.2026). It changes NOTHING - no
        write, no receipt, no message to the box. NO match is 204, never an
        error: "a new component is created" is a perfectly normal outcome.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [templateRef, role, connection]
              properties:
                templateRef: { type: string }
                role: { type: string, enum: [inverter, pv-generation, grid-meter, consumer] }
                connection:
                  type: object
                  additionalProperties: true
      responses:
        "200":
          description: The component that would be taken over.
          content:
            application/json:
              schema:
                type: object
                required: [entityId, role, orphaned]
                properties:
                  entityId: { type: string, format: uuid }
                  label:
                    type: string
                    nullable: true
                    description: >
                      The customer's own name, or null - then the row carries no
                      human-given name and the surface derives one from brand +
                      model instead of inventing one.
                  role: { type: string }
                  brand: { type: string, nullable: true }
                  model: { type: string, nullable: true }
                  orphaned:
                    type: boolean
                    description: >
                      true = it HAD a binding that broke; false = it was never
                      assigned to a device.
        "204": { description: No match - saving would create a new component. }
        "400": { description: Unknown template / role. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/components/custom/read:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [components]
      summary: "\u201eJetzt lesen\u201c - read ONE channel of a self-built Modbus device, once."
      description: >
        The Einheitsmodell Stufe 3 live preview: the box reads the register over
        the Probe-Kanal and answers with the RAW and the SCALED value side by
        side - the moment a scaling mistake becomes visible. It is also the
        connection test of step 1: a SUCCESS records the receipt that unlocks
        saving (keyed on the CONNECTION, not the channel, so a changed scale
        needs no new read while a changed address does). Nothing is persisted;
        a timeout is an honest OUTCOME (HTTP 200 with `errorCode`), not an
        error. LAN-only is enforced here AND independently on the box.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SelfBuildReadRequest" }
      responses:
        "200":
          description: The reading, or an honest classified failure.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SelfBuildReadResult" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
        "409": { description: The plant has no device, or several and none was named. }
  /api/v1/sites/{siteId}/components/custom:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [components]
      summary: Create a SELF-BUILT Modbus device (Einheitsmodell Stufe 3).
      description: >
        Creates a normal v2 entity (`modbus-generic`, measure-only) with the
        customer's plain-text channels, stores the definition as version 1 and
        compiles + rolls out its generated READ flow through the existing
        flowc / `v2/flows` machinery. Requires a receipt from a successful
        `components/custom/read` for this plant + connection (422 otherwise).
        Only on a PORTAL-managed plant (409 on a box-managed one). This stage
        creates measuring devices only - switching is a later stage.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SaveSelfBuildRequest" }
      responses:
        "200":
          description: The plant's components, including the new one.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteComponents" }
        "400": { description: Channel/poll-budget/LAN rule violated - the reason is German. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
        "409": { description: The plant's devices are managed on the box. }
        "422": { description: No connection test on record for this connection. }
        "503": { description: The read plan could not be built or distributed. }
  /api/v1/sites/{siteId}/components/custom/{entityId}:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: entityId, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [components]
      summary: Change a self-built device - a NEW version, the old stays retrievable.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SaveSelfBuildRequest" }
      responses:
        "200":
          description: The plant's components.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteComponents" }
        "400": { description: Channel/poll-budget/LAN rule violated. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or component not found for this tenant. }
        "422": { description: Not a self-built component, or no connection test on record. }
    delete:
      tags: [components]
      summary: Remove a self-built device - together with its generated read flow.
      description: >
        Recorded measurements stay in the history - they WERE measured, and
        removing a device does not un-measure its past.
      responses:
        "200":
          description: The plant's remaining components.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteComponents" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or component not found for this tenant. }
        "409":
          description: >
            The component is a Beleg of released Berichtsstände (UEMS AP-12 E13 S2/IP-12)
            - `berichts_belege`, nothing written, the read flow stays. The plain 409
            of a box-managed plant keeps its `message`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BelegeImWeg" }
        "422": { description: Not a self-built component. }
  /api/v1/sites/{siteId}/components/battery:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [components]
      summary: Connect a SELF-BUILT battery - over a local MQTT broker or its HTTP/JSON endpoint (P5 Ebene 1).
      description: >
        Creates a normal v2 entity (`user-defined-battery`, measure-only) whose
        capabilities are exactly the MAPPED standard channels, stores the
        definition as version 1 and compiles + rolls out its generated READ flow
        through the existing flowc / `v2/flows` machinery - the same path a
        self-built Modbus device takes, one transport further. Only on a
        PORTAL-managed plant (409 on a box-managed one).


        `transport` picks the level-1 READ TYPE, and it is the only thing that
        differs: `mqtt_local` compiles ONE `vp.mqtt.read` under origin
        `mqtt-device`, `http_local` ONE `vp.http.read` under origin
        `http-device`. Everything behind the mapping is identical - the same
        standard channels, the same aggregates, the same SoC derivation (P5b) and
        the same feeder binding (P6).


        Unlike the Modbus Baukasten this path requires NO connection receipt: the
        preview is an OFFER, not a duty - a box that cannot run it yet would turn
        the duty into a dead end, and a duty without a door is not a safeguard.
        The LAN-only rule applies unchanged to both transports.


        ⚠ Rollout order: a box that does not yet know the `mqtt_local` /
        `http_local` communication refuses the pushed driver and drops the WHOLE
        registry push. The edge release has to reach a plant BEFORE the first
        such battery is created there.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SaveUserDefinedBatteryRequest" }
      responses:
        "200":
          description: The plant's components, including the new battery.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteComponents" }
        "400": { description: Mapping/topic/LAN rule violated - the reason is German. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
        "409": { description: The plant's devices are managed on the box, or it has no connected device. }
        "503": { description: The read plan could not be built or distributed. }
  /api/v1/sites/{siteId}/components/battery/preview:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - name: entityId
        in: query
        required: false
        schema: { type: string, format: uuid }
        description: >
          When EDITING, the component - only then can the server re-insert a
          stored auth secret, so the preview does exactly what a save would do.
          Without it the customer would have to retype the BMS key for every
          preview, or it would have to come back into the browser so he does not
          have to. Both are the wrong answer.
    post:
      tags: [components]
      summary: Preview a battery field mapping - the box reads once and reports what it got (P5d).
      description: >
        The mapping assistant's live preview: the SAME checked definition a save
        would write travels to the box as the `connection` block of a
        `test_connection` op. For `mqtt_local` it carries a short listening
        window (`listen_s`) - MQTT is not polled, it arrives; for `http_local`
        there is NO window, because the box fetches exactly once. The answer
        carries one `samples` row per mapping with the RAW and the scaled value
        side by side - the moment a wrong topic, a wrong value path or a wrong
        scale becomes visible.


        It writes NOTHING: no entity, no version, no flow, no connection
        receipt. And unlike `POST /component-test` it is deliberately NOT a
        precondition of saving - a box that cannot listen yet would turn the
        duty into a dead end, and a duty without a door is not a safeguard.


        Every outcome is therefore a 200 with a NAMED class: `not_supported` is
        a statement about the BOX ("cannot listen yet"), `timeout` one about the
        connection. Neither is a verdict about the mapping, and the surface says
        exactly that.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SaveUserDefinedBatteryRequest" }
      responses:
        "200":
          description: The box's answer - `samples` per mapping, or a named class.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ProbeResult" }
        "400": { description: Mapping/topic/LAN rule violated - the reason is German. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant, or it has no connected device. }
        "503": { description: The plant is not reachable right now. }
  /api/v1/sites/{siteId}/components/battery/{entityId}:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: entityId, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [components]
      summary: Change a self-connected battery - a NEW version, the old stays retrievable.
      description: >
        The mapping list IS the capability list: a removed target channel also
        disappears as a measurement, or the battery would promise a value nobody
        reads any more.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SaveUserDefinedBatteryRequest" }
      responses:
        "200":
          description: The plant's components.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteComponents" }
        "400": { description: Mapping/topic/LAN rule violated. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or component not found for this tenant. }
        "422": { description: Not a self-connected battery. }
    delete:
      tags: [components]
      summary: Remove a self-connected battery - together with its generated read flow.
      description: >
        Recorded measurements stay in the history - they WERE measured, and
        removing a device does not un-measure its past.
      responses:
        "200":
          description: The plant's remaining components.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteComponents" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or component not found for this tenant. }
        "409":
          description: >
            The battery is a Beleg of released Berichtsstände (UEMS AP-12 E13 S2/IP-12)
            - `berichts_belege`, nothing written, the read flow stays. The plain 409
            of a box-managed plant keeps its `message`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BelegeImWeg" }
        "422": { description: Not a self-connected battery. }
  /api/v1/sites/{siteId}/components/custom/{entityId}/switch-test:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: entityId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [components]
      summary: The guided switch test - writes ONCE, bounded by an on-device auto-off.
      description: >
        Einheitsmodell Stufe 4. The box ARMS its automatic off BEFORE it writes,
        so a lost answer can never leave the device on. This is not a release
        yet: it records the receipt the release then requires, keyed to THIS
        exact switch definition - a changed address is a different register and
        invalidates it. A timeout is an honest OUTCOME (200 with errorCode),
        never an error: the write may have landed and only its answer got lost.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SwitchTestRequest" }
      responses:
        "200":
          description: The outcome of the test - passed, or a named refusal.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SwitchTestResult" }
        "400": { description: The switch definition is impossible (German reason). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or component not found for this tenant. }
        "422": { description: Test value outside the released clamp, or not a self-built device. }
  /api/v1/sites/{siteId}/components/custom/{entityId}/switch-test/cancel:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: entityId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [components]
      summary: Abort the running test - writes the off/safe value IMMEDIATELY.
      description: >
        Restates the register in full instead of relying on a remembered state
        on the box, so an abort works even after a restart.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SwitchTestRequest" }
      responses:
        "200":
          description: The outcome of the abort.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SwitchTestResult" }
        "400": { description: The switch definition is impossible (German reason). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or component not found for this tenant. }
  /api/v1/sites/{siteId}/components/custom/{entityId}/switch-release:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: entityId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [components]
      summary: Release switching - only with a PASSED test AND the confirmed effect.
      description: >
        The test proves the register is reachable; the confirmation proves the
        RIGHT device reacted - neither alone carries both statements. On
        success the component becomes a switchable device (type, capability,
        consumer clamp) and the switch is compiled into its device flow. The
        device release REPLACES the type certification, which is structurally
        impossible for arbitrary self-built devices; it never replaces the
        operator emergency stop.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SwitchReleaseRequest" }
      responses:
        "200":
          description: The plant's components, with this one now switchable.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteComponents" }
        "400": { description: Switch or consumer data invalid (German reason). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or component not found for this tenant. }
        "422": { description: No passed test on THIS definition, or the effect was not confirmed. }
    delete:
      tags: [components]
      summary: Revoke the release - the device is a sensor again, immediately.
      description: >
        The switch node leaves the flow, the capability leaves the component,
        and the rules pointing at it stop. The DEFINITION stays stored - a
        revoke is not evidence destruction - but the test receipt falls, because
        it belonged to a state that no longer exists.
      responses:
        "200":
          description: The plant's components, with this one measuring only.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SiteComponents" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or component not found for this tenant. }
        "422": { description: Not a self-built component. }
  /api/v1/sites/{siteId}/components/custom/{entityId}/duplicate:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: entityId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [components]
      summary: "\u201eDuplizieren\u201c - turn a device into a PRIVATE template of this plant."
      description: >
        Captain scope 4 - templates are per plant and private, no catalog, no
        sharing. The ADDRESS deliberately does not travel: a template describes
        a device TYPE, not a specimen.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                label: { type: string, maxLength: 200 }
      responses:
        "200":
          description: The plant's private templates.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SiteComponentTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or component not found for this tenant. }
  /api/v1/sites/{siteId}/component-templates:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [components]
      summary: The PRIVATE device templates of this plant.
      description: >
        Tenant-scoped by RLS (own table `site_component_template`) - a private
        template is customer data and never appears on the public
        `/api/v1/component-templates` route, which serves product statements
        (builtin/certified) only.
      responses:
        "200":
          description: The plant's private templates.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SiteComponentTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/component-templates/{templateRef}:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: templateRef, in: path, required: true, schema: { type: string } }
    put:
      tags: [components]
      summary: Rename a private template (Einheitsmodell Stufe 6).
      description: >
        FULL representation of name and note. A blank name is refused (a
        nameless template cannot be told apart in the picker); an ABSENT note
        CLEARS it - the semantics of the component alias. No new version is
        created: a template IS its read plan, and a name changes nothing about
        it.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SiteComponentTemplateRequest" }
      responses:
        "200":
          description: The plant's private templates after the rename.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SiteComponentTemplate" }
        "400": { description: Blank name. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or template not found for this tenant. }
    delete:
      tags: [components]
      summary: Remove a private template. Devices created from it stay.
      responses:
        "200":
          description: The plant's remaining private templates.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/SiteComponentTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or template not found for this tenant. }
  /api/v1/admin/component-templates:
    get:
      tags: [admin]
      summary: EVERY template version, incl. the withdrawn ones (Stufe 6).
      description: >
        Einheitsmodell Stufe 6, platform-admin only. Unlike the customer-facing
        `/api/v1/component-templates` (newest SELECTABLE version per key) this
        answers the operator question: which versions exist, who entered them,
        and how many components of the fleet stand on that key
        (`usedByComponents`). Read through the BYPASSRLS admin role - the
        register is global (no tenant_id, no RLS), the endpoint is the fence.
      responses:
        "200":
          description: All versions, sorted by brand/model, newest version first.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/AdminComponentTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Not a Portal-Admin. }
    post:
      tags: [admin]
      summary: Enter a NEW checked template (version 1) - as DATA, not a release.
      description: >
        The key is DERIVED from brand + model (`certified:<brand>:<model>`) and
        deliberately NOT a request field - it is opaque by contract. `channels`
        and `writes` are absent = "this template does not explain it here"; an
        EMPTY array is REFUSED instead of silently nulled (it would claim
        "delivers no measurements" / "cannot switch anything"). A template that
        carries a write definition must be `certified`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SaveComponentTemplateRequest" }
      responses:
        "201":
          description: The stored version.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminComponentTemplate" }
        "400": { description: Rejected, with a German reason naming every fault. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Not a Portal-Admin. }
        "409": { description: The key already exists - add a version instead. }
  /api/v1/admin/component-templates/{templateRef}/versions:
    parameters:
      - { name: templateRef, in: path, required: true, schema: { type: string } }
    post:
      tags: [admin]
      summary: Add a new VERSION of an existing checked template.
      description: >
        A version is never overwritten - "what ran last week" must stay
        answerable. Brand and model belong to the key and cannot change (409).
        BUILT-IN templates are refused (409): they belong to the startup mirror
        of the Go catalog and a hand edit would be gone at the next restart.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SaveComponentTemplateRequest" }
      responses:
        "201":
          description: The new version.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminComponentTemplate" }
        "400": { description: Rejected, with a German reason. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Not a Portal-Admin. }
        "404": { description: Unknown template. }
        "409": { description: Built-in, or brand/model changed. }
  /api/v1/admin/component-templates/{templateRef}/versions/{version}/withdraw:
    parameters:
      - { name: templateRef, in: path, required: true, schema: { type: string } }
      - { name: version, in: path, required: true, schema: { type: integer } }
    post:
      tags: [admin]
      summary: Take a version out of the assistant's choice (NOT a delete).
      description: >
        Withdrawing NEVER breaks a component: the version snapshot on
        `measurement_point` carries no foreign key, so a running component keeps
        its version and history. Only the CHOICE changes - and because the
        filter sits INSIDE the version pick, withdrawing version 2 falls BACK to
        version 1. Reversible via `/restore`. There is deliberately no delete.
      responses:
        "200":
          description: The withdrawn version.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminComponentTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Not a Portal-Admin. }
        "404": { description: Unknown version. }
        "409": { description: Built-in templates are not editable here. }
  /api/v1/admin/component-templates/{templateRef}/versions/{version}/restore:
    parameters:
      - { name: templateRef, in: path, required: true, schema: { type: string } }
      - { name: version, in: path, required: true, schema: { type: integer } }
    post:
      tags: [admin]
      summary: Put a withdrawn version back into the choice.
      responses:
        "200":
          description: The restored version.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminComponentTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Not a Portal-Admin. }
        "404": { description: Unknown version. }
        "409": { description: Built-in templates are not editable here. }
  /api/v1/admin/component-fleet:
    get:
      tags: [admin]
      summary: The component world of the WHOLE fleet, one row per plant (Stufe 6).
      description: >
        Einheitsmodell Stufe 6, platform-admin only, READ-ONLY - it creates no
        new write path. It answers the three operating questions that were only
        answerable per plant before: where devices are maintained
        (`componentAuthority`), where the components come from (`sources`), and
        where Soll differs from Ist (`syncStatus`, the SAME derivation the
        customer surface uses). Where the box reported nothing the field is
        ABSENT - "unknown", never 0 and never "not arrived".
      responses:
        "200":
          description: One row per plant across every tenant.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminComponentFleet" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Not a Portal-Admin. }
  /api/v1/flow-catalog:
    get:
      tags: [flows]
      summary: The node catalog for the CUSTOMER flow editor (E3b).
      description: >
        E3b customer flow release. The SAME catalog served to the admin editor
        (services/api flowcatalog/catalog.json), but on an authenticated,
        non-admin-gated route so a Portal-User's editor palettes/validates
        against it. Tenant-agnostic (the catalog carries no tenant data); the
        `gated` flag per node lets the customer editor render a market-/grid-near
        strategy node "locked" (needs VoltPilot enablement).
      responses:
        "200":
          description: The catalog document ({catalog_version, types[]}).
          content: { application/json: { schema: { type: object } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/sites/{siteId}/flow-node-governance:
    get:
      tags: [flows]
      summary: READ-ONLY gated-node enablement for the customer editor (E3b).
      description: >
        Which gated strategy node types are enabled for THIS site, so the
        customer editor can render a not-enabled gated node "locked" with the
        Beratung hint. Read-only for customers - enabling a gated node stays a
        Portal-Admin action (PUT /api/v1/admin/sites/{siteId}/flow-node-governance).
        Tenant-scoped like every /sites route (foreign site => 404).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The gated node types + per-site enablement.
          content: { application/json: { schema: { $ref: "#/components/schemas/FlowNodeGovernance" } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found (foreign tenant). }
  /api/v1/sites/{siteId}/flows/{flowId}/layout:
    get:
      tags: [flows]
      summary: The editor canvas layout of a flow (Portal v3 M5).
      description: >
        The saved node positions of a flow. Deliberately its OWN resource: the
        flow-graph schema carries no positions and flowc assigns the compiled
        artifact's x/y itself INSIDE content_hash, so a dragged position must
        never reach the document (a drag would otherwise re-deploy the device).
        Keyed on the flow, so a forked draft inherits the arrangement. Empty
        positions = never arranged (the client renders the auto-layout).
        Mirror on /api/v1/admin/sites/{siteId}/flows/{flowId}/layout.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The saved positions.
          content: { application/json: { schema: { $ref: "#/components/schemas/FlowLayout" } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or flow not found (foreign tenant). }
    put:
      tags: [flows]
      summary: Persist the editor canvas layout (Portal v3 M5).
      description: >
        Replace the flow's node positions. Unknown node ids are DROPPED
        server-side; nothing here reaches the flow document, so content_hash
        cannot move.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/FlowLayout" } } }
      responses:
        "200":
          description: The stored positions (unknown ids dropped).
          content: { application/json: { schema: { $ref: "#/components/schemas/FlowLayout" } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or flow not found (foreign tenant). }
  /api/v1/sites/{siteId}/flow-node-status:
    get:
      tags: [flows]
      summary: What the DEVICE reports about this site's flows (Portal v3 M5).
      description: >
        The device's own view of the flows it runs: the deployment acks (which
        version really runs - the "Läuft auf dem Gerät" badge) plus, only when
        the edge sends the feature-flagged block, the per-node live states. An
        empty nodes list means the device does not report node states, and the
        editor then shows channel values only - never a guessed one. Read-only.
        Mirror on /api/v1/admin/sites/{siteId}/flow-node-status.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The device-reported acks + node states.
          content: { application/json: { schema: { $ref: "#/components/schemas/FlowLiveStatus" } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found (foreign tenant). }
  /api/v1/sites/{siteId}/flows:
    get:
      tags: [flows]
      summary: List a customer's own site flows (E3b).
      description: >
        A Portal-User reads the flows of THEIR OWN site (tenant from the JWT,
        RLS-scoped; a Portal-Admin reaches any site via the X-Tenant-Id switcher).
        One summary per flow with its version spine; documents follow
        docs/contracts/v2/flow-graph.schema.json. Mirrors the admin list, minus
        the platform-admin gate.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The site's flows.
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/FlowSummary" } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found (foreign tenant). }
    post:
      tags: [flows]
      summary: Create a flow (version 1, draft) on one's own site.
      description: >
        The server stamps ALL identity (flow_id, flow_version, site_id,
        tenant_id) into the document; the client only supplies the name and an
        optional starting document.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SaveFlowRequest" }
      responses:
        "201":
          description: The created draft version.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FlowVersion" }
        "400": { description: Missing/over-long name. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found (foreign tenant). }
  /api/v1/sites/{siteId}/flows/{flowId}:
    delete:
      tags: [flows]
      summary: Delete a whole flow (all versions; draft cleanup).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Deleted. }
        "409": { description: A version is active - deactivate first. }
        "404": { description: Flow/site not found. }
  /api/v1/sites/{siteId}/flows/{flowId}/deactivate:
    post:
      tags: [flows]
      summary: Stop a running flow (retire its active version + re-publish).
      description: >
        Retires the flow's active version and re-publishes the site's (now
        smaller) COMPLETE deployment set so the edge drops the retired flow.
        Never gated by the activation flag - stopping a flow is always allowed.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Deactivation outcome ({deactivated, published, lifecycle}).
          content: { application/json: { schema: { type: object } } }
        "409": { description: The flow has no active version. }
        "404": { description: Flow/site not found. }
  /api/v1/sites/{siteId}/flows/{flowId}/versions/{version}:
    get:
      tags: [flows]
      summary: One flow version (document + lifecycle + recorded dry-run).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: version, in: path, required: true, schema: { type: integer } }
      responses:
        "200":
          description: The flow version.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FlowVersion" }
        "404": { description: Flow/version/site not found. }
    put:
      tags: [flows]
      summary: Save a flow version (a simulated/active version yields a new draft).
      description: >
        A draft is updated in place; editing a simulated/active version creates a
        NEW draft version (flow-graph contract paragraph 5) - the response carries
        the resulting version.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: version, in: path, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SaveFlowRequest" }
      responses:
        "200":
          description: The saved (possibly new) version.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FlowVersion" }
        "409": { description: The version is retired (read-only). }
        "404": { description: Flow/version/site not found. }
  /api/v1/sites/{siteId}/flows/{flowId}/versions/{version}/validate:
    post:
      tags: [flows]
      summary: Validate a flow version (V-1..V-8).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: version, in: path, required: true, schema: { type: integer } }
      responses:
        "200":
          description: Findings + the blocking verdict.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FlowValidationResult" }
  /api/v1/sites/{siteId}/flows/{flowId}/versions/{version}/simulate:
    post:
      tags: [flows]
      summary: Start the flow dry-run (Ersparnis-Simulation mapping).
      description: >
        Compiles the flow to its deterministic simulation mapping and submits a
        job over the site's real master data. 422 for an invalid flow or one
        without a simulierbar battery strategy.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: version, in: path, required: true, schema: { type: integer } }
      responses:
        "202":
          description: The dry-run was submitted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  simulationId: { type: string }
                  flowScenario: { type: string, enum: [voltpilot, standardSpeicher] }
        "422": { description: Invalid flow / no simulierbar strategy (German message). }
  /api/v1/sites/{siteId}/flows/{flowId}/versions/{version}/simulation/{simulationId}:
    get:
      tags: [flows]
      summary: Poll the flow dry-run; completion flips draft -> simulated.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: version, in: path, required: true, schema: { type: integer } }
        - { name: simulationId, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: The job status (+ flowScenario).
          content: { application/json: { schema: { type: object } } }
        "404": { description: Job/version not found. }
  /api/v1/sites/{siteId}/flows/{flowId}/versions/{version}/activate:
    post:
      tags: [flows]
      summary: Activate a simulated version (governance-gated; E3b customer path).
      description: >
        The SAME lifecycle + gates as the admin activate, never relaxed for a
        customer: strict validation, the D1 safety gate (simulate first), the AE7
        node-governance gate (activated=false, reason=gated_node_not_enabled when
        a gated strategy node is not yet enabled by a Portal-Admin for the site),
        the peak-shaving precondition, then compile + rollout - gated by
        VOLTPILOT_FLOWS_ACTIVATION_ENABLED (default OFF -> reason=activation_disabled).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: version, in: path, required: true, schema: { type: integer } }
      responses:
        "200":
          description: Activation outcome (activated, reason, message, published).
          content: { application/json: { schema: { type: object } } }
        "409": { description: Already active / draft not simulated yet. }
        "422": { description: Validation errors (message + findings). }
        "404": { description: Not found. }
  /api/v1/admin/flow-catalog:
    get:
      tags: [admin]
      summary: The VoltPilot node catalog the flow editor palettes/validates against.
      description: >
        E3a flow editor. ONE catalog truth (services/api flowcatalog/catalog.json,
        mirrored as the portal's synced copy): typed port signatures, parameter
        specs, runtime support and D-13 claim templates per node type. Adding a
        node type is additive.
      responses:
        "200":
          description: The catalog document ({catalog_version, types[]}).
          content: { application/json: { schema: { type: object } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/admin/sites/{siteId}/flows:
    get:
      tags: [admin]
      summary: List a site's flows (E3a flow editor).
      description: >
        Platform-admin via the X-Tenant-Id switcher over the RLS path (no or
        wrong tenant => 404). One summary per flow with its version spine;
        documents follow docs/contracts/v2/flow-graph.schema.json.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The site's flows.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/FlowSummary" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not found (or no tenant selected). }
    post:
      tags: [admin]
      summary: Create a flow (version 1, draft).
      description: >
        The server stamps ALL identity (flow_id, flow_version, site_id,
        tenant_id) into the stored document - clients never pick ids. Without a
        document a skeleton with one slot-boundary trigger is created.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SaveFlowRequest" }
      responses:
        "201":
          description: The created draft version.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FlowVersion" }
        "400": { description: Missing/overlong name. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not found (or no tenant selected). }
  /api/v1/admin/sites/{siteId}/flows/{flowId}:
    delete:
      tags: [admin]
      summary: Delete a whole flow (all versions; draft cleanup).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Deleted. }
        "409": { description: A version is active - retire first. }
        "404": { description: Flow/site not found. }
  /api/v1/admin/sites/{siteId}/flows/{flowId}/versions/{version}:
    get:
      tags: [admin]
      summary: One flow version (document + lifecycle + recorded dry-run).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: version, in: path, required: true, schema: { type: integer } }
      responses:
        "200":
          description: The version.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FlowVersion" }
        "404": { description: Not found. }
    put:
      tags: [admin]
      summary: Save a flow version.
      description: >
        A DRAFT is updated in place; saving onto a simulated/active version
        creates a NEW draft version (flow-graph contract paragraph 5 - the
        response carries the resulting version). Retired versions are
        read-only (409).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: version, in: path, required: true, schema: { type: integer } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SaveFlowRequest" }
      responses:
        "200":
          description: The saved (possibly NEW) version.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FlowVersion" }
        "409": { description: Version is retired. }
        "404": { description: Not found. }
  /api/v1/admin/sites/{siteId}/flows/{flowId}/versions/{version}/validate:
    post:
      tags: [admin]
      summary: Validate a version against the platform validator (V-1..V-8).
      description: >
        Machine-readable findings per docs/contracts/v2/flow-graph.md paragraph
        4 - rule id, offending node/edge ids, German message. Includes the
        cross-flow exclusive-resource check against the site's ACTIVE flows and
        the capability match against the v2 entity registry.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: version, in: path, required: true, schema: { type: integer } }
      responses:
        "200":
          description: Findings + blocking verdict.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FlowValidationResult" }
        "404": { description: Not found. }
  /api/v1/admin/sites/{siteId}/flows/{flowId}/versions/{version}/simulate:
    post:
      tags: [admin]
      summary: Start the flow dry-run (Ersparnis-Simulation mapping).
      description: >
        Compiles the flow to its deterministic simulation mapping (a market or
        peak-shaving strategy -> the voltpilot scenario incl. its
        Speicherschonung) and submits an
        Ersparnis-Simulation job over the site's real master data. 422 with a
        German reason for an invalid flow or one without a battery strategy.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: version, in: path, required: true, schema: { type: integer } }
      responses:
        "202":
          description: Job accepted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  simulationId: { type: string }
                  flowScenario: { type: string, enum: [voltpilot, standardSpeicher] }
        "422": { description: Invalid or not simulierbar (German message). }
        "404": { description: Not found. }
  /api/v1/admin/sites/{siteId}/flows/{flowId}/versions/{version}/simulation/{simulationId}:
    get:
      tags: [admin]
      summary: Poll the flow dry-run; completion flips draft -> simulated.
      description: >
        Relays the simulation job document plus flowScenario. When the job
        reports done, the version's lifecycle flips draft -> simulated and the
        summary (scenario, headline, price year) is recorded on the version.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: version, in: path, required: true, schema: { type: integer } }
        - { name: simulationId, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: The job status (+ flowScenario).
          content: { application/json: { schema: { type: object } } }
        "404": { description: Job/version not found (api restart forgets jobs). }
  /api/v1/admin/sites/{siteId}/flows/{flowId}/versions/{version}/activate:
    post:
      tags: [admin]
      summary: Activate a simulated version (compile + D-11 rollout).
      description: >
        Strict validation, the D1 safety gate (a draft must be simulated
        first), then compile + retire-previous + mark-active + publish the
        COMPLETE retained deployment set on ems/{t}/{s}/{d}/v2/flows
        (docs/contracts/v2/flow-artifact.md). Until the E2 compiler module
        lands the activation answers HONESTLY with activated=false,
        reason=compiler_missing ("Compiler folgt") and publishes nothing; with
        a compiler present the feature flag
        VOLTPILOT_FLOWS_ACTIVATION_ENABLED (default OFF, simulator-rig only)
        still gates the rollout.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: flowId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: version, in: path, required: true, schema: { type: integer } }
      responses:
        "200":
          description: Activation outcome (activated, reason, message, published).
          content: { application/json: { schema: { type: object } } }
        "409": { description: Already active / draft not simulated yet. }
        "422": { description: Validation errors (message + findings). }
        "404": { description: Not found. }
  /api/v1/admin/tenants/{tenantId}/users/{userId}:
    put:
      tags: [admin]
      summary: Update a customer user's profile (email/name).
      description: The username is the login identity and stays immutable.
      parameters:
        - { name: tenantId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: userId, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateUserRequest" }
      responses:
        "200":
          description: Updated user.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminUser" }
        "400": { description: Invalid email or over-long name. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Tenant or user not found (including a user outside this tenant). }
        "409": { description: A user with that email already exists. }
    delete:
      tags: [admin]
      summary: Permanently delete a customer user's login.
      description: >
        Admins can never delete their OWN account (409) - the guard fires
        before any tenant check.
      parameters:
        - { name: tenantId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: userId, in: path, required: true, schema: { type: string } }
      responses:
        "204": { description: User deleted; their login stops working immediately. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Tenant or user not found (including a user outside this tenant). }
        "409": { description: Attempt to delete one's own account. }
  /api/v1/admin/tenants/{tenantId}/users/{userId}/disable:
    post:
      tags: [admin]
      summary: Disable a customer user (blocks their logins).
      description: >
        Admins can never disable their OWN account (409) - the guard fires
        before any tenant check.
      parameters:
        - { name: tenantId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: userId, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Updated user.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminUser" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Tenant or user not found (including a user outside this tenant). }
        "409": { description: Attempt to disable one's own account. }
  /api/v1/admin/tenants/{tenantId}/users/{userId}/enable:
    post:
      tags: [admin]
      summary: Re-enable a disabled customer user.
      parameters:
        - { name: tenantId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: userId, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Updated user.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminUser" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Tenant or user not found (including a user outside this tenant). }
  /api/v1/admin/tenants/{tenantId}/users/{userId}/reset-password:
    post:
      tags: [admin]
      summary: Ehemaliger Support-Weg; Startpasswörter vergibt der Kundenadministrator.
      deprecated: true
      description: >
        Antwortet für ein Konto im Kundenbereich mit 403 recht_fehlt, für ein fremdes Konto mit 404.
        Der Kundenadministrator verwendet POST /api/v1/benutzer/{sub}/startpasswort.
      parameters:
        - { name: tenantId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: userId, in: path, required: true, schema: { type: string } }
      responses:
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Tenant or user not found (including a user outside this tenant). }
  /api/v1/admin/sites/{siteId}/optimizer-diagnostics:
    get:
      tags: [admin]
      summary: Per-slot "why" view of one persisted optimizer run (Portal-Admin).
      description: >
        The plan's slots PLUS the per-slot euro decomposition the P1-P5 solver
        redesign actually optimized against: the bare spot price
        (`solverPriceCtKwh`), the real import price per the site's tariff
        (spot+Aufschlag / flat / spot), the real export value (spot + dynamic
        Marktprämie for Direktvermarktung incl. the §51 negative-price
        suspension; feste EEG-Einspeisevergütung incl. §51a for Eigenverbrauch;
        bare spot in merchant mode - mirrors the optimizer's pricing.py), the
        PERSISTED per-slot wear spend and its derived ct/kWh rate, an
        explicitly-APPROXIMATE stored-energy value (forward best-use price
        discounted by efficiency and wear - NOT a MILP shadow price), a
        decision label (the portal chargeKind twin) and one composed German
        rationale per slot. Fields that are honestly not computable for a slot
        (no persisted spot, pre-P2 rows without wear, battery-less site) are
        null, never a fabricated 0.

        Requires the `platform-admin` role AND the `X-Tenant-Id` tenant
        switcher: the read runs RLS-scoped over the selected tenant's own data
        (no BYPASSRLS); no tenant selected or the wrong tenant = 404. The §14a
        limit a run used is not persisted and therefore not returned;
        forecast-model lifecycle comes from GET /sites/{siteId}/forecast-quality.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - name: generatedAt
          in: query
          required: false
          schema: { type: string, format: date-time }
          description: >
            Select a historical run by its exact generated_at; omitted = the
            latest run (or, with `date`, that day's newest run). An unknown
            timestamp is a 404 (never silently the latest); `availableRuns`
            in the response lists the day's choices.
        - name: date
          in: query
          required: false
          schema: { type: string, format: date }
          description: >
            A Europe/Berlin calendar day: scopes `availableRuns` to that day's
            runs (the schedule hypertable keeps every run, so navigation is
            per day) and, without an explicit `generatedAt`, shows the day's
            newest run. A day without runs returns an empty well-formed body
            (not 404 - gaps inside the covered range are possible). Omitted =
            the shown run's day. `firstRunDate`/`lastRunDate` in the response
            bound the picker.
      responses:
        "200":
          description: >
            The run's diagnostics; a site without any plan yet returns an
            empty well-formed body (generatedAt null, no slots).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OptimizerDiagnostics" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not visible for the selected tenant, or unknown generatedAt. }
  /api/v1/admin/sites/{siteId}/optimizer-config:
    get:
      tags: [admin]
      summary: The site's optimizer knobs - platform defaults, overrides, effective values.
      description: >
        Platform defaults mirror the optimizer's `OPTIMIZER_*` environment
        (the api receives the same variables - keep them in sync in compose);
        overrides are the nullable per-site/per-asset columns
        (`asset.wear_cost_ct_per_kwh`, `asset.soc_min_pct`/`soc_max_pct`,
        `site.backup_reserve_soc_pct`); effective = override ?? default. The
        pre-existing site levers (netzladen_erlaubt, plant_kind, tariff,
        anzulegender Wert) are echoed READ-ONLY - they stay editable via
        PUT /api/v1/sites/{siteId} (admins: with the X-Tenant-Id switcher).
        Terminal-value settings are env-level and read-only here. Same
        auth/tenancy as optimizer-diagnostics.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The merged config view.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OptimizerConfig" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not visible for the selected tenant. }
    put:
      tags: [admin]
      summary: Write the per-site / per-asset optimizer overrides.
      description: >
        FULL-REPRESENTATION for the four override fields: the body IS the
        override state, a null/absent field CLEARS that override back to the
        platform default (matching the columns' NULL semantics). Battery
        fields (wear cost, SoC band) require a battery asset (409 otherwise);
        the EFFECTIVE SoC band (override ?? default per side) must stay a real
        window (400). Changes apply on the optimizer's next 15-min cycle - no
        re-solve is triggered here; use POST optimizer-what-if (design §4.3)
        to preview a knob change before saving it.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateOptimizerConfigRequest" }
      responses:
        "200":
          description: The updated merged config view.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/OptimizerConfig" }
        "400": { description: Invalid value range or inconsistent effective SoC band. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not visible for the selected tenant. }
        "409": { description: Battery override on a site without a battery asset. }

  /api/v1/admin/sites/{siteId}/optimizer-what-if:
    post:
      tags: [admin]
      summary: Preview a knob change - solve this site twice, persist nothing.
      description: >
        The on-demand what-if re-optimize (design vp-admin-optimizer-ui-design
        §4.3). The api resolves the site through the RLS-scoped datasource
        (X-Tenant-Id switcher; a site the selected tenant cannot see is 404 and
        is never forwarded) and then asks the optimization service's internal
        synchronous solve surface to gather ONE fresh set of inputs
        (prices/forecasts/SoC/§14a/tariff of the real plant) and solve it
        TWICE: `baseline` as the site is configured, `variant` with the posted
        knobs. The delta is variant minus baseline.


        EPHEMERAL BY CONSTRUCTION: no plan is persisted to `schedule`, no MQTT
        schedule is published, the site's stored settings are untouched and the
        run currently in force stays in force. The comparison deliberately does
        NOT use the site's persisted run - that was generated at another time
        over another horizon with another price vintage, so a delta against it
        would attribute the passage of time to the knob.


        Every override field is OPTIONAL; absent means "leave as configured"
        (never zero), so an empty body simply re-solves the site as it stands.
        `backupReserveSocPct: 0` IS "no reserve" - a 0 % floor and no floor are
        the same constraint. Solves take well under a second, hence a plain
        synchronous response and no job id; concurrent runs are bounded, so a
        saturated service answers 429.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WhatIfRequest" }
      responses:
        "200":
          description: Both freshly solved plans plus their delta. Nothing was committed.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/WhatIfResult" }
        "400":
          description: >
            Invalid knobs (range, inverted SoC band) or a site that honestly
            cannot be planned right now (no price coverage). Body carries a
            German `message`.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Site not visible for the selected tenant. }
        "409": { description: The site has no battery asset - nothing to re-plan. }
        "429": { description: Too many concurrent re-optimizes; retry shortly. }
        "502": { description: The solve service is unreachable; the stored plan is unchanged. }
  /api/v1/sites/{siteId}/consumer-options:
    get:
      tags: [consumer]
      summary: The Anlagen-Modell assistant's capability-/context-filtered inputs.
      description: >
        The consumer types the site may create (catalog category=consumer,
        controllable, non-composed - each with its allowed control kinds, default
        failsafe and per-type intent suggestions), the condition SIGNAL catalog
        (name/label/class/valueType; cloud signals are compiled to windows and
        carry no hysteresis, D1), the four intent cards, whether the site has a
        storage (steers the storage questions, E3), the edge-reported sources
        that can back a consumer, and the master-data defaults. Tenant-scoped (RLS
        foreign site = 404).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The assistant inputs.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ConsumerOptions" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/consumers:
    get:
      tags: [consumer]
      summary: List the site's controllable consumers.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The consumers with their master data + control-activation state.
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/Consumer" } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
    post:
      tags: [consumer]
      summary: Create a consumer (connected to a reported source, or as a draft).
      description: >
        Creates the consumer as a v2 entity + its consumer_profile. `edgeSourceId`
        binds a reported source ("verbunden"); omitting it creates an unconnected
        DRAFT ("Noch nicht verbunden"). The requested `controlKind` is refused
        (422) when the device type cannot execute it (§16 - never widened by a
        customer value). No real command is issued in Increment 1.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CreateConsumerRequest" }
      responses:
        "201":
          description: The created consumer.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Consumer" }
        "400": { description: Unknown type / invalid master data. Body carries a German `message`. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
        "409": { description: The chosen reported source is already assigned to a consumer. }
        "422": { description: The device type cannot execute the requested control kind. }
  /api/v1/sites/{siteId}/consumers/{id}:
    get:
      tags: [consumer]
      summary: Read one consumer's master data + state.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The consumer.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Consumer" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Consumer/site not found for this tenant. }
    patch:
      tags: [consumer]
      summary: Edit a consumer's master data (optimistic via version).
      description: >
        Optimistic concurrency: send `expectedVersion`; a stale value is refused
        with 409. The control kind is again mapped onto the device capabilities
        (§16). `name` updates the entity label.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PatchConsumerRequest" }
      responses:
        "200":
          description: The updated consumer.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Consumer" }
        "400": { description: Invalid master data. Body carries a German `message`. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Consumer/site not found for this tenant. }
        "409": { description: Version conflict (someone else changed it first). }
        "422": { description: The device type cannot execute the requested control kind. }
    delete:
      tags: [consumer]
      summary: Fachlich stilllegen (409 while still connected).
      description: >
        Removes an UNCONNECTED consumer (returning the site to its pre-consumer
        state - Increment 1 holds no audit/fulfilment data). A consumer still
        bound to a device/source is refused (409): disconnect first (physische
        Registry-Bereinigung erst nach Deaktivierung, §11).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Removed. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Consumer/site not found for this tenant. }
        "409":
          description: >
            Still connected to a device/source; disconnect first (plain `message`).
            Or the unconnected consumer is a Beleg of released Berichtsstände
            (UEMS AP-12 E13 S2/IP-12): it fed a cited Messstelle - e.g. read by a
            box that was abgemeldet since. `berichts_belege`, nothing written.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BelegeImWeg" }
  /api/v1/sites/{siteId}/consumers/{id}/policy:
    get:
      tags: [consumer]
      summary: Read the consumer's latest policy (204 when none).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The latest policy version (draft in Increment 1).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ConsumerPolicyVersion" }
        "204": { description: No policy authored yet. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Consumer/site not found for this tenant. }
    put:
      tags: [consumer]
      summary: Save a new DRAFT policy version.
      description: >
        Validates the policy document against consumer-policy.schema.json + the
        semantic ConsumerPolicyValidator (kind/enforcement coherence, must_run⇒grid
        allow, condition-tree limits, cloud-vs-local hysteresis, catalog signals,
        D4 power ranges, 24:00 normalisation) and stores it as the next DRAFT
        version. The document's `entity_id` is stamped from the path (a body can
        never target another entity). content_hash is derived from canonical JSON.
        Activation does not exist yet (Increment 1).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [document]
              properties:
                document:
                  type: object
                  description: A consumer-policy document (consumer-policy.schema.json).
      responses:
        "200":
          description: The stored draft version.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ConsumerPolicyVersion" }
        "400": { description: The policy is invalid. Body carries a German `message`. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Consumer/site not found for this tenant. }
  /api/v1/sites/{siteId}/consumers/{id}/policy/activate:
    post:
      tags: [consumer]
      summary: Validate, compile and atomically activate the latest policy version (Inkrement 4).
      description: >
        Verbrauchssteuerung §11/§13.2 (D-19): validates the LATEST policy
        version, compiles its reactive-with-local requirements into at most ONE
        generated flow artifact (precomputed UTC price windows per D1, local
        signals with freshness + hysteresis, must_run → the bounded D-5
        override) and activates atomically - previous active version retired,
        the artifact rolled out over the EXISTING retained flow-deployment set.
        Flag-gated (VOLTPILOT_CONSUMER_CONTROL_ENABLED +
        VOLTPILOT_CONSUMER_POLICY_COMPILER_ENABLED, both default OFF): with a
        flag off the response is 200 with activated:false and a reason
        (activation_disabled / compiler_disabled) - the rule stays a draft. A
        compiler or rollout failure is 503 and the previously active version
        stays UNCHANGED. An entity already claimed by another active automation
        is 409 naming it (V-5).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Outcome envelope (activated true/false + reason + published).
          content:
            application/json:
              schema:
                type: object
                properties:
                  activated: { type: boolean }
                  reason: { type: string, nullable: true, description: "activation_disabled | compiler_disabled | null" }
                  message: { type: string }
                  published: { type: boolean }
                  policyVersion: { type: integer, nullable: true }
        "400": { description: No policy yet / the stored policy is invalid. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Consumer/site not found for this tenant. }
        "409": { description: The entity is already controlled by another active automation (named in `message`). }
        "422": { description: The consumer is not connected yet (E11). }
        "503": { description: Compiler/rollout unavailable - the previously active version is unchanged. }
  /api/v1/sites/{siteId}/consumers/{id}/policy/deactivate:
    post:
      tags: [consumer]
      summary: The real stop path - retire the active policy AND retract the retained artifact.
      description: >
        §16: works INDEPENDENTLY of the activation feature flags (a disabled
        flag must never leave a deployed rule looking stopped). Retires the
        active policy version and re-publishes the site's retained deployment
        set WITHOUT the generated artifact.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Stopped.
          content:
            application/json:
              schema:
                type: object
                properties:
                  published: { type: boolean }
                  message: { type: string }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Consumer/site not found for this tenant. }
        "409": { description: No active policy. }
  /api/v1/sites/{siteId}/consumers/{id}/pause:
    post:
      tags: [consumer]
      summary: Pause the consumer (Gesamtschalter off, device failsafe applies).
      description: >
        §11/§16: flips the consumer's enable switch off, retracts the generated
        artifact from the retained deployment set and lets the desired TTLs
        expire - the entity falls to its registry failsafe. Works with the
        activation flags OFF (stop paths are never flag-gated).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Paused.
          content:
            application/json:
              schema:
                type: object
                properties:
                  published: { type: boolean }
                  message: { type: string }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Consumer/site not found for this tenant. }
  /api/v1/sites/{siteId}/consumers/{id}/resume:
    post:
      tags: [consumer]
      summary: Resume after a pause (re-enable + re-deploy the stored artifact, no compile).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Outcome envelope (the activate shape).
          content:
            application/json:
              schema:
                type: object
                properties:
                  activated: { type: boolean }
                  reason: { type: string, nullable: true }
                  message: { type: string }
                  published: { type: boolean }
                  policyVersion: { type: integer, nullable: true }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Consumer/site not found for this tenant. }
        "409": { description: No active policy to resume. }
        "503": { description: Rollout unavailable. }
  /api/v1/sites/{siteId}/consumer-schedule:
    get:
      tags: [consumer]
      summary: Consumer slots of the newest co-optimizer run (Fahrplan view).
      description: >
        Verbrauchssteuerung Inkrement 2 (§14.11): the planned consumer slots of
        the site's newest co-optimized run, one entity per consumer with its
        FULL slot grid (command on_off|setpoint_kw, target value, §15
        reason_code, served requirement). SHADOW semantics: rows exist only for
        sites the optimizer co-plans (VOLTPILOT_V2_PLAN_SITES) with active
        policies - nothing controls a device. Without a stored run the body is
        well-formed and empty (planId/generatedAt null, entities []); the
        Fahrplan then renders byte-identical to the pre-consumer view.
        Tenant-scoped via RLS (foreign site = 404).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The newest run's consumer slots (possibly empty).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ConsumerSchedule" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/modbus-probe:
    post:
      tags: [probe]
      summary: Read registers on this plant's device ONCE and show what came back.
      description: >
        Einheitsmodell Stufe 0b. The api publishes a NON-retained probe on
        ems/{tenant}/{site}/{device}/v2/probe (contract
        docs/contracts/mqtt-probe.schema.json) and waits ~5 s for the device's
        answer; nothing is stored on either side.


        Honesty rules the response carries: a successful line has raw AND value
        side by side (the pair that makes a scaling mistake visible) plus the
        raw 16-bit register words; a failed line carries a NAMED German class
        and no value at all - never a fabricated 0. A device that does not
        answer in time yields errorCode `timeout` with HTTP 200: that is an
        honest outcome the assistant renders, not an error.


        Which device is asked is never guessed - `deviceId` is optional only
        while the plant has exactly one device; several devices without a
        choice is a named 409.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProbeRequest" }
      responses:
        "200":
          description: >
            The device's answer, or the honest timeout/rate-limit outcome.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ProbeResult" }
        "400": { description: The request form is not readable (validation). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site or device not found for this tenant. }
        "409": { description: The plant has no device, or several and none was chosen. }
        "503": { description: The plant could not be reached to ask at all. }
  /api/v1/sites/{siteId}/consumer-status:
    get:
      tags: [consumer]
      summary: Edge-reported live states of the site's consumers.
      description: >
        Verbrauchssteuerung Inkrement 3 (D9/§15.1, a §11 status excerpt): the
        device heartbeat's additive consumers block, ingested by its own
        sibling listener into consumer_runtime_status (one row per entity,
        replaced wholesale per heartbeat; unknown state/reason words are
        discarded at ingest, never stored). An EMPTY list is the honest
        no-evidence state ("Zustand nicht bestätigt") - the portal then
        renders byte-identical to before. Tenant-scoped via RLS (foreign
        site = 404).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Reported consumer states (possibly empty).
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ConsumerRuntimeStatus" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/consumers/{id}/status:
    get:
      tags: [consumer]
      summary: One consumer's edge-reported live state.
      description: >
        The single-entity form of consumer-status. 204 without evidence -
        never a fabricated live state.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The reported state.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ConsumerRuntimeStatus" }
        "204": { description: No reported state yet ("Zustand nicht bestätigt"). }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/rule-events:
    get:
      tags: [rules]
      summary: Das Regel-Protokoll der Anlage (Zähler, Verlauf je Regel, Gesamt-Protokoll).
      description: >
        Einheitsmodell Stufe 5b (Konzept vp-komponenten-einheit-h2 Teil 5b.6):
        der Verlaufsspeicher, den die Momentaufnahmen-Tabellen
        (consumer_runtime_status, flow_device_ack) je Herzschlag wegwerfen.
        Geschrieben wird AUSSCHLIESSLICH aus WECHSELN im bestehenden
        Herzschlag-Strom - keine Edge-Änderung, kein neues Topic.
        Ehrlichkeitsregeln, die jede Fläche kennen muss: `switchedToday` ist
        NULLABLE (null = der Speicher hat den heutigen Tag nicht ganz gesehen,
        die Fläche sagt dann "seit HH:MM aufgezeichnet" statt einer 0),
        `recordingSince` null = für diese Anlage wurde noch gar nicht
        aufgezeichnet, und `accuracySeconds` nennt die Genauigkeit (der
        Herzschlag-Takt; ein Wechsel-und-zurück dazwischen ist unsichtbar).
        Mandantenbezogen über RLS (fremde Anlage = 404).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Das Protokoll (leer, solange nichts aufgezeichnet wurde).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RuleEvents" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Site not found for this tenant. }
  /api/v1/sites/{siteId}/command-history:
    get:
      tags: [commands]
      summary: Der Kommando-Verlauf einer Anlage bzw. EINER Komponente.
      description: >
        Kommando-Transparenz V1 (Konzept vp-kommando-transparenz-k3 §6.3): die
        Halteperioden und Punkt-Ereignisse EINES Zeitraums, aelteste zuerst,
        plus der Live-Zustand (control/curtailment - woertlich die DTOs der
        beiden bestehenden Momentaufnahme-Routen, damit keine zweite Wahrheit
        ueber dieselbe Sekunde entsteht).
        Ehrlichkeitsregeln, die jede Flaeche kennen muss: `recordingSince` null
        = fuer diese Anlage wurde noch gar nicht aufgezeichnet (ein leerer
        Verlauf ist dann KEINE entlastende Aussage), `writes` false = an diese
        Komponente geht kein Befehl, sie wird nur gelesen (die F4-Antwort),
        die vier `cycles*` sind in V1 IMMER null (aus 15-Sekunden-Momentaufnahmen
        laesst sich die Zahl der 10-Sekunden-Schreibvorgaenge nicht ableiten),
        `accuracySeconds` nennt die Genauigkeit, und `truncated` sagt, dass der
        Deckel gegriffen hat. Mandantenbezogen ueber RLS (fremde Anlage = 404).
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - name: entity
          in: query
          required: false
          description: >
            Die Komponente. Fehlt sie, umfasst die Antwort die ganze Anlage.
            Mit ihr kommen IHRE Zeilen plus die geraetebezogenen Zeilen ihres
            Geraets (entityId null) - die betreffen den Schreibweg, ueber den
            sie gesteuert wird, und waeren sonst nirgends sichtbar.
          schema: { type: string, format: uuid }
        - name: device
          in: query
          required: false
          description: >
            Das GERAET (Anlagen-Zentrale Stufe 1): die Referenz der VoltPilot-Box,
            die vom Edge gemeldete Quellen-Kennung eines Geraets dahinter
            (`inverter`, `src-...`) oder `cp-<ChargePointId>` einer Ladesaeule.
            Die BOX bekommt jede Zeile ihres Geraets - auch die geraetebezogenen
            ohne Komponente (die Abregelung), denn sie IST der Schreibweg der
            Anlage. Ein Geraet DAHINTER bekommt nur die Zeilen SEINER
            Komponenten: eine anlagenweite Abregelung einem von drei
            Wechselrichtern zuzuschreiben waere eine erfundene Zuordnung.
            Unbekannte Adresse = 404; zusammen mit `entity` = 400 (zwei
            verschiedene Fragen).
          schema: { type: string }
        - name: range
          in: query
          required: false
          description: >
            Tag, Woche oder Monat (Europe/Berlin-Kalender). Vorgabe `day`.
            Ein JAHR gibt es bewusst nicht - es ueberschritte die Aufbewahrung
            und waere damit ein Fenster, das per Konstruktion unvollstaendig
            antwortet.
          schema: { type: string, enum: [day, week, month], default: day }
        - name: at
          in: query
          required: false
          description: Anker-Datum des Zeitraums (Vorgabe heute).
          schema: { type: string, format: date }
        - name: from
          in: query
          required: false
          description: >
            Ein eigener Zeitraum, erster Kalendertag (nur zusammen mit `to`;
            schliesst `range`/`at` aus). Weiter zurueck als die Aufbewahrung
            (90 Tage) wird ABGELEHNT statt still gekappt: dort laege nichts
            mehr, und ein leeres Fenster laese sich als "damals wurde nichts
            geschickt" - eine entlastende Aussage ueber eine geloeschte Zeit.
          schema: { type: string, format: date }
        - name: to
          in: query
          required: false
          description: >
            Letzter Kalendertag des eigenen Zeitraums, EINSCHLIESSLICH - ein
            Fenster, das um 00:00 dieses Tages endete, liesse den ganzen
            gewaehlten Tag aussen vor.
          schema: { type: string, format: date }
        - name: streams
          in: query
          required: false
          description: >
            Befehlsart(en), komma-getrennt: `batterie` `abregelung`
            `verbraucher` `ladepunkt` `register`. Fehlt der Parameter, gilt
            keine Einschraenkung. Ein Wort ausserhalb des Vokabulars ist ein
            BENANNTER 400 (nie ein stiller Rueckfall auf "alles" - der zeigte
            MEHR Zeilen als verlangt).
          schema: { type: string }
        - name: sources
          in: query
          required: false
          description: >
            Herkunft/Herkuenfte, komma-getrennt: `cloud_abgeleitet` `geraet`
            `portal`.
          schema: { type: string }
        - name: verdicts
          in: query
          required: false
          description: >
            Ergebnis(se), komma-getrennt. Halteperioden: `bestaetigt`
            `abweichend` `keine_antwort` `prueft` `unbestaetigt` plus
            `fremdeinfluss` und `notaus` (beides KEINE Ruecklese-Urteile - sie
            leben in eigenen Spalten, weil sie andere Fragen beantworten).
            Register-Vorgaenge: `uebernommen` `nicht_uebernommen`
            `keine_quittung`. Ein Filter, der nur Woerter EINES Vokabulars
            nennt, schliesst den anderen Speicher aus - eine Zeile ohne dieses
            Ergebnis ist kein Treffer.
          schema: { type: string }
        - name: limit
          in: query
          required: false
          description: Zeilen je Speicher (1..2000). Vorgabe 500.
          schema: { type: integer, minimum: 1, maximum: 2000, default: 500 }
        - name: before
          in: query
          required: false
          description: >
            Der Seiten-Cursor nach hinten (`nextBefore` der vorigen Antwort):
            nur Zeilen, die BIS zu diesem Zeitpunkt begannen.
            Verglichen wird `<=`, nicht `<` - zwei Zeilen duerfen denselben
            Beginn tragen, und ein striktes Kleiner verloere die zweite lautlos
            an der Seitengrenze. Die Grenzzeile kommt deshalb ZWEIMAL; die
            Flaeche mischt die Seiten ueber die `id`.
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: Der Verlauf (leer, solange nichts aufgezeichnet wurde).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CommandHistory" }
        "400": { description: Unbekannter Zeitraum oder unbekanntes Filter-Wort. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Anlage oder Komponente nicht gefunden. }
  /api/v1/sites/{siteId}/register-write/preview:
    post:
      tags: [register-write]
      summary: Schritt 1 - den Ist-Wert eines Registers lesen (Vorschau).
      description: >
        Liest das Register EINMAL ueber die Schreib-Lane und liefert Roh- UND
        (wo eine Skala bekannt ist) skalierten Wert plus die Warnklasse. Es wird
        NICHTS geschrieben und NICHTS protokolliert - eine Lesung aendert nichts,
        und ein Protokoll der Lesungen wuerde die Schreibvorgaenge begraben, fuer
        die das Journal existiert.
        Weil die Vorschau ueber DIESELBE Lane laeuft, beweist sie mit, dass der
        Schreibweg offen ist: eine Ablehnung ("die Steuerung besitzt dieses
        Register gerade") erscheint hier, nie erst nach dem Klick.
        Ein Ausbleiben der Antwort ist ein ehrlicher AUSGANG (200 mit
        `errorCode: timeout`), kein Fehler.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RegisterWriteRequest" }
      responses:
        "200":
          description: Das Ergebnis der Vorschau (auch ein benannter Fehlschlag ist 200).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RegisterWriteOutcome" }
        "400": { description: Adresse oder Registerart nicht lesbar. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Anlage oder Geraet nicht gefunden. }
        "409": { description: Die Anlage hat kein oder mehr als ein Geraet - das Ziel wird nie geraten. }
        "503": { description: "Die Anlage ist gerade nicht erreichbar (nichts angefordert) - ODER das Register-Schreiben ist plattformweit abgeschaltet (voltpilot.register-write.enabled=false); der deutsche Grund unterscheidet die beiden." }
  /api/v1/sites/{siteId}/register-write:
    post:
      tags: [register-write]
      summary: Schritt 2 - der EINE Schreibvorgang, mit Beleg und Papier-Spur.
      description: >
        Schreibt das Register GENAU EINMAL (kein Retry, kein Auffrischen - es
        liegt im EEPROM) und liest ~2 s spaeter zurueck.
        `expectedBefore` reist als Waechter mit: hat sich der Ist-Wert seit der
        Vorschau geaendert, verweigert die Box, statt blind zu ueberschreiben.
        Die Bestaetigung des Downlink-Kontrakts baut der SERVER aus Adresse und
        Wert (Protokoll-Sicherheit, kein Tipp-Zwang).
        `note` ist PFLICHT bei Registern der Klasse `netz_compliance` (in Stufe 1
        also bei 0x00E7) und sonst freiwillig; sie wandert VERBATIM ins Journal.
        Reihenfolge: erst veroeffentlichen, dann protokollieren - scheitert schon
        das Veroeffentlichen (503), entsteht KEINE Zeile, die eine Anforderung
        behauptet, die es nie gab.
        Bleibt die Quittung aus, ist der Ausgang `unbekannt` - ausdruecklich NICHT
        "nicht geschrieben"; die Quittung landet im Journal, sobald sie eintrifft.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RegisterWriteRequest" }
      responses:
        "200":
          description: Der Beleg (auch eine Ablehnung der Box ist 200 mit ihrem deutschen Grund).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/RegisterWriteOutcome" }
        "400": { description: Wert/Adresse nicht lesbar, oder die Pflicht-Notiz fehlt. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Anlage oder Geraet nicht gefunden. }
        "409": { description: Die Anlage hat kein oder mehr als ein Geraet. }
        "503": { description: "Die Anlage ist gerade nicht erreichbar (es wurde NICHTS geschrieben) - ODER das Register-Schreiben ist plattformweit abgeschaltet; der deutsche Grund unterscheidet die beiden." }
  /api/v1/sites/{siteId}/register-write/targets:
    get:
      tags: [register-write]
      summary: Der Geraete-Picker - welche Geraete als Ziel in Frage kommen.
      description: >
        Es entsteht KEINE neue Wahrheit: die Liste kommt aus dem, was die BOX
        ueber ihre Einrichtung meldet (local_setup) und aus den
        portal-verwalteten Komponenten. Ein Geraet OHNE Schreibweg wird
        GENANNT (`writable: false` + `reason`), nie verschwiegen - es
        wegzulassen erzeugte die Frage "warum fehlt mein Geraet?" und
        beantwortete sie nirgends. `writable` ist eine ANZEIGE-Hilfe, kein
        Tor: was wirklich geht, entscheidet das Geraet.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Die Ziele dieser Anlage.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/RegisterWriteTarget" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Anlage nicht gefunden. }
  /api/v1/sites/{siteId}/register-write/register-knowledge:
    get:
      tags: [register-write]
      summary: Das kuratierte Register-Wissen (reine Anzeige).
      description: >
        Der Nachschlage-Teil des Experten-Modus: je Register-FAMILIE die
        bekannten Register mit Klartext-Namen, Warnklasse und - wo bekannt -
        Skala und Einheit. Es entscheidet NICHTS und sperrt nichts; ein
        Register, das hier fehlt, ist schreibbar und heisst ehrlich
        "unbekannt".
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Das Verzeichnis, Familie fuer Familie.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/RegisterKnowledgeFamily" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Anlage nicht gefunden. }
  /api/v1/sites/{siteId}/register-write/history:
    get:
      tags: [register-write]
      summary: Der Verlauf der Register-Schreibvorgaenge dieser Anlage.
      description: >
        Dieselben Journal-Zeilen, die die Befehle-Seite als vierten Strom
        `register` einmischt - je Vorgang zu EINER Zeile gefaltet (Anforderung +
        Ergebnis, verbunden ueber `requestId`). Neueste zuerst.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - name: deviceId
          in: query
          required: false
          description: Nur die Vorgaenge EINES Geraets.
          schema: { type: string, format: uuid }
        - name: limit
          in: query
          required: false
          schema: { type: integer, minimum: 1, maximum: 200, default: 50 }
      responses:
        "200":
          description: Der Verlauf (leer, solange nie geschrieben wurde).
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/RegisterWriteEvent" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Anlage nicht gefunden. }
  /api/v1/messstellen:
    get:
      tags: [messstellen]
      summary: Das Messstellen-Register - je Messstelle Ort, Stellung, Quelle und Zustand zum Stichtag (AP-04 IP-4).
      description: >
        Recht `messstelle.ansehen`. `messstellen` trägt weiter die Vertrags-Form jeder
        Messstelle (IP-3), `register` die Zeile zum Stichtag: Ort mit abgeleitetem
        Standort (dieselbe Ableitung wie `GET /api/v1/messstellen/{id}/standort?am=`),
        elektrische Stellung, Quelle der Hauptgröße (Gerät, Messwert, seit; bei einem
        Wechsel `davor`) und der Lebenszyklus. Beide Listen nennen DIESELBEN Messstellen
        in derselben Reihenfolge (nach Kennzeichen); jeder Filter gilt für beide.
        `stichtag`: ein Tag (2026-11-20, dann gilt sein Beginn in Europe/Berlin) oder
        ein Zeitpunkt mit Versatz (das „+“ URL-kodiert); fehlend = jetzt. Ort und
        Stellung gelten an dessen Tag, die Quelle genau zu ihm. Ein Standort, Ort oder
        eine Anlage, die es im Kundenbereich nicht gibt (auch eine fremde), findet
        nichts - leer, nie 403. `beobachtung` und `letzter_wert` (IP-15) sagen je Größe,
        was ihre führende Quelle liefert - `aggregat` zählt „x von y Messstellen liefern
        Daten“ über genau die gezeigten Zeilen; `teilansicht` bleibt bis AP-03 `false`.
      parameters:
        - { name: standort, in: query, required: false, description: "Kurzzeichen oder ID des Standorts; die Zeile zählt, wenn ihr abgeleiteter Standort dieser ist.", schema: { type: string } }
        - { name: ort, in: query, required: false, description: "Kurzzeichen oder ID eines Standorts, Gebäudes oder Bereichs - einschließlich seines Teilbaums am Stichtag; U = direkt am Unternehmen.", schema: { type: string } }
        - { name: anlage, in: query, required: false, description: "ID der Anlage der elektrischen Stellung am Stichtag.", schema: { type: string, format: uuid } }
        - { name: zustand, in: query, required: false, description: "Ein Wort des Lebenszyklus.", schema: { type: string, enum: [entwurf, eingerichtet, aktiv, angehalten, archiviert] } }
        - { name: ohneQuelle, in: query, required: false, description: "true = nur gemessene ohne führende Quelle zum Zeitpunkt (berechnete sind nicht „ohne Quelle“, sie haben keine); fehlend oder false = kein Filter.", schema: { type: boolean } }
        - { name: geplantFuerEinsatz, in: query, required: false, description: "true = nur Messstellen, die einen eingelösten Messbedarf tragen („geplant für EE-…“).", schema: { type: boolean } }
        - { name: stichtag, in: query, required: false, schema: { type: string } }
      responses:
        "200":
          description: Die Messstellen und ihre Register-Zeilen (leer ohne gewählten Kundenbereich).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleListe" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [messstellen]
      summary: Legt eine Messstelle an; ohne Kennzeichen das nächste automatische (MS-0001 …).
      description: >
        Recht `messstelle.bearbeiten`. Geprüft wird erst jede Form (400:
        `kennzeichen_format`, `anfrage_ungueltig`, `groesse_ungueltig` mit dem ersten
        verletzten Merkmal nach dem Größen-Katalog), dann die Belegung (409
        `kennzeichen_belegt` - belegt ist, was eine Messstelle trägt, ein
        archiviertes UND das frühere Kennzeichen einer umbenannten; `bestehend`
        nennt den Träger). Das automatische Kennzeichen vergibt der Server unter
        der Zeilensperre des Zählers in derselben Transaktion wie Nebengrößen und
        Protokolleintrag. Das Medium ist das volle Vokabular des Vertrags, geprüft
        über den Größen-Katalog: Gas/Volumen (MS-21) ist anlegbar, Wasser, Wärme,
        Kälte und Druckluft haben noch keine Größe (400 `groesse_ungueltig`,
        `grund: medium`). Dass der Dialog im ersten Umfang nur „Strom“ anbietet
        (AP-00 E11) und keine berechnete Messstelle (E9), ist eine Regel der
        Fläche, nicht der Schnittstelle. Ein Feld, das es hier nicht gibt, ist 400
        `anfrage_ungueltig` - nie still verworfen.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MessstelleAnlegen" }
            example:
              name: Spritzguss SG01–SG06
              art: gemessen
              medium: Strom
              hauptgroesse: { groesse: Wirkenergie, richtung: Bezug, einheit: kWh, wertart: Zählerstand }
              nebengroessen:
                - { groesse: Wirkleistung, richtung: Bezug, einheit: kW, wertart: Momentanwert }
      responses:
        "201":
          description: Angelegt; `Location` zeigt auf die neue Messstelle.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Messstelle" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Kein Kundenbereich gewählt (Plattform-Admin ohne X-Tenant-Id). }
        "409": { $ref: "#/components/responses/MessstelleAbgelehnt" }
  /api/v1/messstellen/kennzeichen-vorschlag:
    get:
      tags: [messstellen]
      summary: Das nächste automatische Kennzeichen für den Anlege-Dialog.
      description: >
        Recht `messstelle.bearbeiten`. Die kleinste Nummer über dem Zähler, deren
        Kennzeichen niemand trägt oder trug (E7); der Zähler bewegt sich erst, wenn
        eine Messstelle ohne Kennzeichen gespeichert wird.
      responses:
        "200":
          description: Der Vorschlag.
          content:
            application/json:
              schema:
                type: object
                required: [kennzeichen]
                properties:
                  kennzeichen: { type: string, pattern: "^[A-Z0-9./-]{2,16}$", example: MS-0022 }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/messstellen/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [messstellen]
      summary: Eine Messstelle.
      description: Recht `messwerte.ansehen`.
      responses:
        "200":
          description: Die Messstelle.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Messstelle" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [messstellen]
      summary: Schreibt Kennzeichen, Name und Notiz (die ganze Menge).
      description: >
        Recht `messstelle.bearbeiten`. Art, Medium und Hauptgröße sind nie änderbar
        (eine andere Größe ist eine andere Messstelle): wer sie mitschickt, bekommt
        400 `anfrage_ungueltig`. Ein fehlendes Feld ist leer - ohne `kennzeichen` ist
        die Anfrage 400 `kennzeichen_format`, ohne `name` fehlt der Name danach
        (Entwurf). Das bisherige Kennzeichen bleibt der Messstelle belegt; sie darf
        zu ihm zurück. Ändert sich nichts, wird nichts geschrieben und nichts
        protokolliert. Eine archivierte Messstelle bleibt, wie sie ist (409
        `zustand_passt_nicht`).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MessstelleBearbeiten" }
            example: { kennzeichen: MS-06, name: Spritzguss SG01–SG06, notiz: Zählerplatz UV-3 }
      responses:
        "200":
          description: Die Messstelle nach dem Bearbeiten.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Messstelle" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/MessstelleAbgelehnt" }
  /api/v1/messstellen/{id}/anhalten:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [messstellen]
      summary: Hält die Messstelle an (Umbau, Zähler ausgebaut) - Quellen und Zuordnungen bleiben.
      description: >
        Recht `messstelle.bearbeiten`, rückwirkend zusätzlich `aenderung.rueckwirkend`.
        Nur, wenn sie weder angehalten noch archiviert ist (sonst 409
        `zustand_passt_nicht`). Zeitpunkt auf die Minute (sonst 400), Vorgabe jetzt;
        die Vergangenheit ist erlaubt und steht als „rückwirkend“ im Protokoll, die
        Zukunft nicht (422 `zeitpunkt_in_zukunft`); er liegt NACH dem letzten
        Fortsetzen (422 `zeitpunkt_vor_vorgaenger`, `vorgaenger` nennt es) und nie vor
        dem Beginn der Messstelle (Mitternacht des ersten Tages ihres ersten Orts; 422
        `zeitpunkt_vor_vorgaenger` mit `beginn`).
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MessstelleUebergang" }
            example: { zeitpunkt: "2026-11-18T10:40:00+01:00", grund: Umbau }
      responses:
        "200":
          description: Die angehaltene Messstelle.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Messstelle" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "422": { $ref: "#/components/responses/MessstelleAbgelehnt" }
  /api/v1/messstellen/{id}/fortsetzen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [messstellen]
      summary: Setzt die angehaltene Messstelle fort; die Lücke bleibt sichtbar, nie aufgefüllt.
      description: >
        Recht `messstelle.bearbeiten`, rückwirkend zusätzlich `aenderung.rueckwirkend`.
        Nur, wenn sie angehalten und nicht archiviert ist (sonst 409
        `zustand_passt_nicht`). Der Zeitpunkt liegt NACH dem Anhalten (422
        `zeitpunkt_vor_vorgaenger` - dieselbe Regel und derselbe Code wie der Wechsel
        einer Quelle, A15), nie in der Zukunft (422 `zeitpunkt_in_zukunft`).
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MessstelleUebergang" }
      responses:
        "200":
          description: Die fortgesetzte Messstelle.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Messstelle" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "422": { $ref: "#/components/responses/MessstelleAbgelehnt" }
  /api/v1/messstellen/{id}/archivieren:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [messstellen]
      summary: Archiviert die Messstelle - das Kennzeichen bleibt belegt, kein Wiederbeleben.
      description: >
        Recht `messstelle.bearbeiten`, rückwirkend zusätzlich `aenderung.rueckwirkend`.
        Zeitpunkt jetzt oder Vergangenheit (AP-04 §4.5; Zukunft 422
        `zeitpunkt_in_zukunft`), nach dem letzten Anhalten/Fortsetzen (422
        `zeitpunkt_vor_vorgaenger`). Auch eine angehaltene Messstelle lässt sich
        archivieren; eine archivierte nicht noch einmal (409 `zustand_passt_nicht`).
        Ihre Nebengrößen gelten mit ihr als archiviert. Ihre Zuordnungen (Ort,
        Stellung) enden am Vortag des Archivtags, eine erst an ihm oder später
        beginnende wird aufgehoben (A13); der Protokolleintrag nennt den letzten Tag
        (`zuordnungen_bis`). Jede offene Quelle endet zum Archivzeitpunkt (IP-13,
        `quellen_beendet` im Protokoll); eine angekündigte oder mit späterem Ende ließe
        sich dort nur umschreiben - dann 409 `zustand_passt_nicht`.
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MessstelleUebergang" }
      responses:
        "200":
          description: Die archivierte Messstelle.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Messstelle" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "422": { $ref: "#/components/responses/MessstelleAbgelehnt" }
  /api/v1/messstellen/{id}/ort:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [messstellen]
      summary: Setzt den Ort der Messstelle ab einem Tag (AP-04 IP-7).
      description: >
        Recht `messstelle.bearbeiten`, mit `gueltig_ab` vor heute zusätzlich
        `aenderung.rueckwirkend`. Die Tages-Mechanik des Ortsbaums
        (docs/contracts/v2/ortsbaum-vectors.json): `gueltig_ab` ist ein Tag, das
        laufende Intervall endet am Vortag, das neue erbt dessen Ende (bis zu einer
        geplanten Zuordnung); `korrektur: true` ersetzt das Intervall, das an
        `gueltig_ab` beginnt (das alte bleibt aufgehoben lesbar). Rückwirkend ist
        erlaubt und steht im Protokoll (`rueckwirkend`), die Zukunft ist geplant.
        Der Ort ist das Kurzzeichen eines Standorts, Gebäudes oder Bereichs - oder `U`,
        das Unternehmen (nur eine berechnete Messstelle). Abgelehnt: den Ort gibt es
        nicht, gab es an einem Tag noch nicht oder er war archiviert, `U` für eine
        gemessene (422 `ort_ungueltig` mit `grund`); an dem Tag beginnt schon ein Ort
        (409 `zuordnung_ueberlappt` - der Weg ist die Korrektur); schon genau dieser Ort
        (400 `zuordnung_unveraendert`); vor dem ersten Ort, kein Beginn am Tag der
        Korrektur (422 `zuordnung_ungueltig` mit `grund`); archivierte Messstelle (409
        `zustand_passt_nicht`). Status wie AP-02 §5.10, `ort_ungueltig` wie der
        Messstellen-Vertrag §7. Je Schreibvorgang genau ein Protokolleintrag
        (`ort_zugeordnet` bzw. `ort_korrigiert`).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MessstelleOrtAendern" }
            example: { kennzeichen: B-3, gueltig_ab: "2027-03-01", grund: Kaltwassersatz zieht in Halle 2 }
      responses:
        "200":
          description: Die Messstelle mit ihren Orten.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Messstelle" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "422": { $ref: "#/components/responses/MessstelleAbgelehnt" }
  /api/v1/messstellen/{id}/stellung:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [messstellen]
      summary: Setzt die elektrische Stellung der Messstelle ab einem Tag (AP-04 IP-7).
      description: >
        Recht `messstelle.bearbeiten`, mit `gueltig_ab` vor heute zusätzlich
        `aenderung.rueckwirkend`. Dieselbe Tages-Mechanik wie beim Ort (409
        `zuordnung_ueberlappt`, 400 `zuordnung_unveraendert`, 422 `zuordnung_ungueltig`). Regel 8 (Vertrag
        docs/contracts/v2/messstelle.md §6, E12) an JEDEM Tag des neuen Intervalls, an
        dem sich etwas ändert (`tag` nennt den ersten verletzten): 422
        `stellung_ungueltig` mit `grund` nicht_elektrisch · bezug_nur_bei_unterzaehler
        · bezug_fehlt · selbst · fremde_anlage (`bestehend`) · zyklus (`kette`); 409
        `hauptzaehler_vorhanden` mit `bestehend` - je Anlage und Richtung einer, alle
        am selben Zähler. Bis IP-13 die Quellen bindet, ist der Zähler einer Messstelle
        unbekannt: dann ist auch ein zweiter Hauptzähler in anderer Richtung 409. Eine
        Messstelle, die an einem Tag Unterzähler der geänderten ist, darf dadurch nicht
        in eine fremde Anlage zeigen (422 `stellung_ungueltig` mit `betroffen`). Die
        Anlage ist die ID einer Anlage des Kundenbereichs (sonst 400
        `anfrage_ungueltig`, `feld: anlage`), `unterzaehler_von` das Kennzeichen der
        Bezug-Messstelle. Je Schreibvorgang genau ein Protokolleintrag
        (`stellung_zugeordnet` bzw. `stellung_korrigiert`).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MessstelleStellungAendern" }
            example: { anlage: 5b0c0c6e-8f3a-4d7e-9a61-2f4f1c9d7b10, stellung: Unterzähler, unterzaehler_von: MS-10, gueltig_ab: "2027-03-01" }
      responses:
        "200":
          description: Die Messstelle mit ihrer elektrischen Stellung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Messstelle" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "422": { $ref: "#/components/responses/MessstelleAbgelehnt" }
  /api/v1/messstellen/{id}/standort:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: am, in: query, required: false, schema: { type: string, format: date }, description: "Der Tag; fehlend = heute in der Zeitzone des Unternehmens." }
    get:
      tags: [messstellen]
      summary: Der Stand der Zuordnungen an einem Tag - Ort, Pfad, abgeleiteter Standort, Stellung.
      description: >
        Recht `messstelle.ansehen`, ein Tag in der Vergangenheit ("Stand am")
        zusätzlich `aenderungsprotokoll.lesen`. Die Verortung des Ortsbaum-Vertrags
        (Familie `messstelle_standort`): der Standort ist die Wurzel des Ortsknotens
        AN DIESEM TAG - zieht ein Gebäude um, wechselt der Standort ohne eigene
        Zuordnung. `grund`: verortet · am_unternehmen (Ort `U`, Standort null) ·
        nicht_verortet (an dem Tag kein Ort) · ort_nicht_im_baum. Dazu die Stellung,
        die an dem Tag gilt (null ohne). Eine fremde Messstelle ist 404, nie 403.
      responses:
        "200":
          description: Der Stand am Tag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleStandortAm" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/messstellen/{id}/aenderungen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - $ref: "#/components/parameters/ProtokollVon"
      - $ref: "#/components/parameters/ProtokollBis"
      - $ref: "#/components/parameters/ProtokollAchse"
      - $ref: "#/components/parameters/ProtokollLimit"
      - $ref: "#/components/parameters/ProtokollNach"
    get:
      tags: [messstellen]
      summary: Das Aenderungsprotokoll EINER Messstelle, juengster Eintrag zuerst (AP-04 IP-21).
      description: >
        Recht `aenderungsprotokoll.lesen`. Jede Aenderung der Messstelle: anlegen,
        bearbeiten, anhalten, fortsetzen, archivieren, Ort, elektrische Stellung,
        Quellenbindung, Einstellungs-Fassung, Zaehlerwechsel. Nur lesend - ein
        Eintrag entsteht allein im jeweiligen Fachweg. Jede Zeile nennt BEIDE
        Zeitpunkte: `gilt_ab` (wann die Aenderung gilt) und `eingetragen_am` (wann
        sie eingetragen wurde); `zeitform` ist das Urteil dazu. `von`/`bis` filtern
        auf der Achse, die `achse` nennt - Vorgabe `wirkung` ("gilt ab"). Eine
        fremde Messstelle ist 404, nie 403.
      responses:
        "200":
          description: Das Protokoll der Messstelle.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Protokoll" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/messstellen/{id}/quellen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [messstellen]
      summary: Die Quellen der Messstelle - je Größe die führende zum Stichtag, Vergleichsquellen, Zeitstrahl, Historie (AP-04 IP-13).
      description: >
        Recht `messstelle.ansehen`. `stichtag`: ein Zeitpunkt mit Versatz
        (2026-11-18T10:40:00+01:00, das „+“ URL-kodiert) oder ein Tag (dann dessen
        Beginn in Europe/Berlin); fehlend = jetzt. Je Größe (Hauptgröße zuerst) die
        führende Quelle zum Stichtag - `null` heißt keine Quelle, nie 0 -, die
        laufenden Vergleichsquellen und der Zeitstrahl der führenden Quellen ab Beginn
        der Messstelle (Mitternacht ihres ersten Orts; ohne Ort ab der ersten Quelle)
        mit jeder Lücke als eigenem Abschnitt. Eine fremde Messstelle ist 404, nie 403.
      parameters:
        - { name: stichtag, in: query, required: false, schema: { type: string } }
      responses:
        "200":
          description: Die Quellen der Messstelle.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleQuellenListe" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      tags: [messstellen]
      summary: Bindet eine Größe der Messstelle an einen Messkanal - führend oder zum Vergleich, zeitgültig auf die Minute.
      description: >
        Recht `messstelle.quelle`; mit einem „gültig ab“ vor jetzt zusätzlich
        `aenderung.rueckwirkend`. Der Messkanal ist Komponente + Gerät + Kanalname; das
        Gerät ist der Einbau, der die Komponente zu `gueltig_ab` speist, und die Quelle
        liegt ganz in dieser Speisung (sonst 422 `kein_geraet_zum_zeitpunkt`). Geprüft
        wird in der Reihenfolge des Vertrags (docs/contracts/v2/messstelle.md §5):
        Medium (422 `medium_ohne_quelle`) → Zweck (400 `vergleich_ohne_zweck`) →
        Passung Regel 7 (422 `quelle_passt_nicht` mit `grund` wertart · groesse ·
        einheit · richtung · anteil; ein Vorzeichen-Wert wie die Wirkleistung am
        Zweirichtungszähler hat keine Richtung — er bindet nur mit `anteil`: positiv an
        Bezug, negativ an Abgabe, AP-08 IP-7) → Zeitraum (400
        `zeitraum_ungueltig`) → Gerät zum Zeitpunkt → Messwert führt schon eine andere
        Messstelle (409 `kanal_bereits_fuehrend`, je Anteil: der positive und der negative
        Anteil führen je eine Messstelle, der ganze Wert schließt jeden aus) → vor dem Beginn der Messstelle (422
        `zeitpunkt_vor_vorgaenger`) → Überlappung derselben Größe und Rolle, beim
        Vergleich desselben Messwerts (409 `bindung_ueberlappt`). Vergleichsquellen
        stehen überlappend nebeneinander; Lücken sind erlaubt. Eine neue offene
        führende Quelle beendet die laufende genau zu ihrem Beginn (Regel 2) - in
        derselben Transaktion, mit `endstand_vorgaenger`, und EINEM Protokolleintrag
        `quelle_gebunden`. Eine archivierte Messstelle oder Nebengröße bekommt keine
        Quelle (409 `zustand_passt_nicht`); eine fremde Messstelle oder Komponente ist
        404, nie 403.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MessstelleQuelleBinden" }
      responses:
        "201":
          description: Gebunden; `Location` nennt die neue Quelle.
          headers:
            Location: { schema: { type: string } }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleQuelleVorgang" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "422": { $ref: "#/components/responses/MessstelleAbgelehnt" }
  /api/v1/messstellen/{id}/quellen/wechsel:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [messstellen]
      summary: Zähler wechseln - derselbe Vorgang wie POST /api/v1/geraete/{id}/austausch, aus der Sicht der Messstelle.
      description: >
        Recht `messstelle.quelle` („Führende Quelle binden · Zählerwechsel ·
        Wandlerfaktoren"); mit einem Zeitpunkt vor jetzt zusätzlich
        `aenderung.rueckwirkend`. Das Gerät wählt nie der Kunde: es ist das, aus dem die
        Messstelle zum Wechselzeitpunkt liest. Liest sie dann aus keinem (409
        `zustand_passt_nicht`: erst eine Quelle binden) oder aus mehreren (409
        `zustand_passt_nicht`: dann auf der Geräteseite wechseln), lehnt der Vorgang ab.
        Alles Weitere - Wirkungen, Prüfreihenfolge, Ablehnungen - steht bei
        `POST /api/v1/geraete/{id}/austausch`; es ist derselbe Vorgang in derselben
        Transaktion.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/Zaehlerwechsel" }
      responses:
        "201":
          description: Gewechselt - beide Einbauten, die umgezogenen Bindungen, die übernommenen Einstellungen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ZaehlerwechselVorgang" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "422": { $ref: "#/components/responses/MessstelleAbgelehnt" }
  /api/v1/messstellen/{id}/quellen/{quelleId}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: quelleId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [messstellen]
      summary: Eine Quelle der Messstelle.
      description: Recht `messstelle.ansehen`. Eine Quelle einer anderen oder fremden Messstelle ist 404.
      responses:
        "200":
          description: Die Quelle.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleQuelle" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/messstellen/{id}/quellen/{quelleId}/beenden:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: quelleId, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [messstellen]
      summary: Beendet eine Quelle - genau einmal, nie gelöscht, nie überschrieben.
      description: >
        Recht `messstelle.quelle`; mit einem Ende vor jetzt zusätzlich
        `aenderung.rueckwirkend`. `gueltig_bis` auf die Minute (fehlend = jetzt; die
        Zukunft ist angekündigt), optional der Endstand (ohne Einheit: Hinweis
        `ablesestand_pruefen`). Das Ende liegt nach dem Beginn (400
        `zeitraum_ungueltig`); eine beendete Quelle wird nie erneut beendet (409
        `bindung_bereits_beendet`). Bis eine neue Quelle beginnt, bleibt eine sichtbare
        Lücke. Ein Protokolleintrag `quelle_beendet`.
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MessstelleQuelleBeenden" }
      responses:
        "200":
          description: Die beendete Quelle.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleQuelleVorgang" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/MessstelleAbgelehnt" }
  /api/v1/messstellen/{id}/quellen/{quelleId}/kadenz:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: quelleId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [messstellen]
      summary: Die erwartete Kadenz der Quellenbindung zum Stichtag, mit Herkunft und Historie (AP-07 IP-10).
      description: >
        Recht `messstelle.ansehen`. Die Kadenz ist seit AP-07 IP-10 (Entscheid E9) ein
        zeitgültiges Feld der Quellenbindung: `erwartet_s` ist, was zum `stichtag`
        erwartet wird, `herkunft` sagt woher die Zahl kommt (`fassung` · `auswahl` ·
        `katalog` · `vorgabe`), `vorgabe_s` was ohne eingetragene Fassung gälte.
        `stichtag` ist ein Tag (`2027-03-03`, dann gilt sein Beginn) oder ein Zeitpunkt
        mit Versatz; fehlend = jetzt. Eine Quelle einer anderen oder fremden Messstelle
        ist 404.
      parameters:
        - { name: stichtag, in: query, required: false, schema: { type: string } }
      responses:
        "200":
          description: Die Kadenz zum Stichtag und die Historie ihrer Fassungen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Kadenz" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      tags: [messstellen]
      summary: Trägt eine neue Kadenz-Fassung ein - sie gilt ab ihrem Zeitpunkt, nie rückwärts.
      description: >
        Recht `messstelle.quelle`; mit einem `gueltig_ab` vor jetzt zusätzlich
        `aenderung.rueckwirkend`. `erwartet_s` 1 … 86 400 s - genau die Schranken des
        Drahtvertrags `mqtt-measurement-config` (400 `kadenz_ungueltig`);
        `gueltig_ab` auf die volle Minute (400 `zeitpunkt_ungueltig`), fehlend = jetzt.
        Die Fassung liegt in ihrer Bindung (422 `vor_beginn` · `nach_ende`), zu einem
        schon belegten Beginn gibt es keine zweite (409 `beginn_belegt`), und dieselbe
        Zahl noch einmal ist 400 `unveraendert`. Die dort geltende Fassung endet GENAU
        zu diesem Zeitpunkt; alles davor behält seine alte Erwartung. Ein
        Protokolleintrag `kadenz_geaendert` an der Messstelle. ⚠ Am Draht ändert sich
        nichts: die Box bekommt dieselbe Nachricht `…/v2/measurement-config` in
        derselben Form - nur mit dieser Zahl in `cadence_s`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/KadenzEintragen" }
      responses:
        "201":
          description: Die neue Fassung und die, die sie beendet hat.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KadenzVorgang" }
        "400": { $ref: "#/components/responses/KadenzAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/KadenzAbgelehnt" }
        "422": { $ref: "#/components/responses/KadenzAbgelehnt" }
  /api/v1/messstellen/{id}/formel:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [messstellen]
      summary: Die Formel einer berechneten Messstelle an einem Tag - die Terme der Fassung, die an ihm gilt (AP-10 IP-3).
      description: >
        Recht `messstelle.ansehen`. Die Formel ist tagesgenau zeitgültig (Fassungen,
        Vertrag docs/contracts/v2/messstelle-formel.md §6): `terme` sind die Terme der
        Fassung, die am Tag `am` gilt, `formel_vorhanden`/`eingaenge_eingerichtet` ihr
        Stand. OHNE `am` gilt heute (Zeitzone des Standorts) und die Antwort ist
        Zeichen für Zeichen die von vor AP-10 IP-3 - ohne den Block `fassung_am`; die
        Portal-Fläche „Gesamtwert“ (PR #689) liest sie unverändert. MIT `am` steht
        zusätzlich `fassung_am` in der Antwort: der Tag und die Fassung, die an ihm gilt
        (`null`, wenn an dem Tag keine gilt - dann ohne Terme). Felder in snake_case.
        Keine oder eine fremde berechnete Messstelle ist 404, nie 403.
      parameters:
        - { name: am, in: query, required: false, schema: { type: string, format: date }, description: "Der Tag (JJJJ-MM-TT); fehlend = heute, dann ohne `fassung_am`." }
      responses:
        "200":
          description: Die Formel am Tag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleFormel" }
        "400": { $ref: "#/components/responses/MessstelleFormelAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/messstellen/{id}/formel/fassungen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [messstellen]
      summary: Trägt eine neue Fassung der Formel ab einem Tag ein - die laufende endet am Vortag, nie rückwärts (AP-10 IP-3).
      description: >
        Recht `messstelle.formel` (AP-10 E15); mit einem `gueltig_ab` vor heute zusätzlich
        `aenderung.rueckwirkend`. Die Terme werden geprüft wie beim Anlegen
        (`anfrage_ungueltig` 400, `groessen_gemischt` 422, `formel_zyklus` 422); die
        abgeleitete Hauptgröße muss die der Messstelle bleiben (`groessen_gemischt` 422
        mit `grund`). Die Regel der Tage (uems/MessstelleFormelRegeln.fassungEintrag):
        Fassung n + 1 beendet Fassung n am VORTAG, nichts wird überschrieben; beginnt die
        neue nicht NACH dem Beginn der jüngsten Fassung, 422
        `formel_fassung_ueberlappt` („Ab diesem Tag gilt schon Fassung 2.“, `fassung`,
        `gueltig_ab`). Eine Fassung vor dem Eintragstag ist erlaubt und trägt
        `rueckwirkend` samt `abzeichen` („rückwirkend (5 Tage)“). Eine archivierte
        Messstelle bekommt keine Fassung (409 `zustand_passt_nicht`). In einer
        Transaktion mit GENAU EINEM Protokolleintrag `formel_geaendert`. Die Anfrage wird
        streng gelesen: ein unbekanntes Feld (auch camelCase) ist 400.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MessstelleFormelFassungEintragen" }
      responses:
        "201":
          description: Die Formel am ersten Tag der neuen Fassung (mit `fassung_am`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleFormel" }
        "400": { $ref: "#/components/responses/MessstelleFormelAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "422": { $ref: "#/components/responses/MessstelleFormelAbgelehnt" }
  /api/v1/sites/{siteId}/data-sources:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [datenquellen]
      summary: Die Datenquellen der Anlage, archivierte eingeschlossen, in der Reihenfolge des Anlegens.
      description: >
        Recht: lesend (AP-06 §4.8). Je Quelle ihre Zeiträume und die Box, die JETZT
        liest (`zustaendige_box`, null ohne laufenden Zeitraum). „Liefert Daten“ ist
        eine Beobachtung aus dem Herzschlag (IP-14) und steht hier nicht.
      responses:
        "200":
          description: Die Quellen (ein Objekt, kein nacktes Array - additiv erweiterbar).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DatenquelleListe" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      tags: [datenquellen]
      summary: Legt eine Datenquelle als Entwurf an; das Kennzeichen DQ-n vergibt der Server.
      description: >
        Recht `datenquelle.bearbeiten`. Die Adresse wird normalisiert (Vertrag §3
        Nr. 5: Host klein, ohne Leerzeichen, Modbus-Port fehlend = 502, HTTP-Port
        ausgeschrieben; Solarman-Datenlogger `host:port/seriennummer`, Port fehlend = 8899). Mit `device_id` (der gewählten Box) laufen vorher die Regeln
        des Antrags `anlegen` bis vor die Prüfung - ein Grund davor
        (`adresse_an_box_vergeben`, `netzlage_fehlt`, `nur_ein_leser`,
        `vergleich_bestaetigen`) lehnt ab, BEVOR ein Entwurf entsteht; eine Box
        außerhalb des Kundenbereichs ist 404. `vergleich_bestaetigt` beantwortet
        die Rückfrage „als Vergleichsquelle anlegen?“. Ein Protokoll außerhalb des
        Vokabulars ist 400 `protokoll_unbekannt`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DatenquelleAnlegen" }
            example:
              name: WAGO-Steuerung C-2
              protokoll: modbus_tcp
              adresse: 192.168.30.10
              geraete_ids: [1]
              netz: 192.168.30.0/24
              kadenz_s: 60
              device_id: 6f1c2a9e-0000-4000-8000-0000000000e3
      responses:
        "201":
          description: Angelegt (Entwurf, noch liest keine Box); `Location` zeigt auf die Quelle.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Datenquelle" }
        "400": { $ref: "#/components/responses/DatenquelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/DatenquelleAbgelehnt" }
        "422": { $ref: "#/components/responses/DatenquelleAbgelehnt" }
  /api/v1/sites/{siteId}/data-sources/{id}:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [datenquellen]
      summary: Eine Datenquelle der Anlage.
      description: Recht lesend. Eine Quelle einer anderen Anlage oder eines anderen Kundenbereichs ist 404.
      responses:
        "200":
          description: Die Quelle.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Datenquelle" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [datenquellen]
      summary: Bearbeitet die Quelle (volle Darstellung); protokolliert wird nur, was sich ändert.
      description: >
        Recht `datenquelle.bearbeiten`. Name, Netzlage, Geräte-IDs und Lesetakt
        jederzeit; Protokoll, Adresse, Ein-Leser-Eigenschaft und Steuerquelle - die
        Eingänge der Antrags-Regeln - nur, solange die Quelle keinen Zeitraum hat
        (sonst 409 `weg_fest` mit `felder`). Eine archivierte Quelle ist 409
        `quelle_archiviert`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DatenquelleBearbeiten" }
      responses:
        "200":
          description: Die Quelle danach.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Datenquelle" }
        "400": { $ref: "#/components/responses/DatenquelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/DatenquelleAbgelehnt" }
  /api/v1/sites/{siteId}/data-sources/{id}/reachability-check:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [datenquellen]
      summary: Erreichbarkeitsprüfung von GENAU der gewählten Box (E11).
      description: >
        Recht `datenquelle.bearbeiten`. Die Box `device_id` - auch eine mit anderer
        Heimat-Anlage (E7), nie eine außerhalb des Kundenbereichs (404) - liest
        einmal über den Probe-Kanal (docs/contracts/mqtt-probe.schema.json, op
        `read`, auf ihrem EIGENEN Topic) die gespeicherte Adresse der Quelle; Host
        und Port kommen nie aus der Anfrage. Heute für Modbus TCP und
        SunSpec-Modbus (`register` bei SunSpec mit 40000 „SunS“ vorbelegt); für
        MQTT, HTTP und OCPP 422 `pruefung_nicht_moeglich`. Die Antwort ist ein
        ehrlicher AUSGANG (200), auch wenn die Prüfung scheitert. `gewertet`: „ok“
        oder eine Fehlerklasse der BOX (Vertrag §7) - nur dann steht die Prüfung im
        Protokoll und zählt für eine Zuständigkeit. Schweigt die Box
        (`box_meldet_sich_nicht`) oder lehnt ihr Prüf-Kanal ab (`rate_limited`,
        `not_supported`, `invalid_request`), zählt nichts.
        `op: wago_kopf` (AP-05, nur Modbus TCP): statt `read` liest die Box den Kopf
        eines VoltPilot-Registerbilds WAGO v1 an der Basisadresse `register` (op
        `wago_kopf`, ohne `data_type`); nur hinter einem erkannten v1-Kopf danach je
        gemeldeter Karte Steckplatz, Kartentyp und Variante (u16-Lesungen, zwei Karten
        je Probe, höchstens 32 Karten). Gewertet wird allein der Kopf; die Antwort trägt
        dann zusätzlich `wago`. Eine Lese-Prüfung antwortet unverändert.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DatenquellePruefen" }
            example: { device_id: 6f1c2a9e-0000-4000-8000-0000000000e2, unit_id: 1, register: 0 }
      responses:
        "200":
          description: Das Ergebnis der Prüfung (auch gescheitert oder ohne Antwort der Box).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DatenquellePruefergebnis" }
        "400": { $ref: "#/components/responses/DatenquelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/DatenquelleAbgelehnt" }
        "422": { $ref: "#/components/responses/DatenquelleAbgelehnt" }
        "503": { description: Die Frage konnte nicht an die Box gehen (Broker nicht erreichbar). }
  /api/v1/sites/{siteId}/data-sources/{id}/assignments:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [datenquellen]
      summary: Ab `effective_from` liest `device_id` die Quelle - erste Box oder Wechsel.
      description: >
        Recht `datenquelle.zustaendigkeit`. Vor allen Regeln (AP-15 T6): ist die Quelle
        Netzzähler, Messpunkt oder Steuerquelle eines Mitglieds einer Gemeinsamen
        Steuerung in `anteile_aktiv` oder angehalten - und jede Steuerquelle in einer Anlage
        mit eingerichteter Gemeinsamer Steuerung, außer vor dem Scharfschalten (S0-S2) zu
        einem Mitglied (IP-26: dann ein Wechsel ohne den Grund `steuerquelle`) -, 409
        `gemeinsame_steuerung_aendern` mit Fakt `anlage` - ihre Box wechselt nur als Änderung
        der Gemeinsamen Steuerung (`…/sites/{anlage}/gemeinsame-steuerung`). Die erste Box
        einer neuen Quelle ist kein Wechsel. Ohne Gemeinsame Steuerung greift das nie. Die erste Box einer Quelle ist ein Antrag
        `anlegen`, jede weitere ein `wechsel`; geprüft von DatenquelleRegeln in der
        festen Reihenfolge des Vertrags (§5): protokoll_unbekannt → keine_volle_minute
        → rueckwirkend → steuerquelle → spaeterer_wechsel_geplant → schon_zustaendig →
        adresse_an_box_vergeben → netzlage_fehlt → nur_ein_leser →
        vergleich_bestaetigen → pruefung_fehlt → pruefung_gescheitert; der erste
        Grund entscheidet (Status siehe DatenquelleFehler). Das Prüfergebnis ist die
        jüngste gewertete Prüfung dieser Quelle von GENAU dieser Box für ihre heutige
        Adresse. `effective_from` auf die volle Minute (mit Sekunden 422, nie
        gerundet), frühestens jetzt; fehlend = jetzt auf die Minute abgerundet.
        Erlaubt: der laufende Zeitraum wird bei `effective_from` beendet und der neue
        eingetragen - in EINER Transaktion unter der Zeilensperre der Quelle; die
        Speicher-Regel (§4, `leerer_zeitraum`, `ueberschneidung`) prüft jeden
        geänderten Zeitraum; greift trotzdem ein Constraint (gleichzeitiger
        Schreibvorgang), 409 `gleichzeitig_geaendert` mit `grund`. Die Steuerquelle
        wechselt ihre Box nicht (409 `steuerquelle`, A13). Eine bestätigte
        Vergleichsquelle wird gekennzeichnet (`vergleichsquelle`).
        Unmittelbar vor dem Schreiben wird das physische Lesebudget der Ziel-Box aus den
        Kanälen und Blockanfragen aller dort zum Zeitpunkt gelesenen Datenquellen vorgerechnet.
        Eine Überschreitung ist 422 `budget_ueberschritten`; der Körper trägt Quelle, Summe,
        Grenzen, freie Kapazität aller Boxen am Standort und die Auswege `Takt … s` und
        `Box …`. Kein bestehender Zeitraum wird dabei verändert.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DatenquelleZuweisen" }
            example: { device_id: 6f1c2a9e-0000-4000-8000-0000000000e2, effective_from: "2027-04-10T07:30:00+02:00" }
      responses:
        "201":
          description: Erlaubt und eingetragen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DatenquelleZugewiesen" }
        "400": { $ref: "#/components/responses/DatenquelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/DatenquelleAbgelehnt" }
        "422": { $ref: "#/components/responses/DatenquelleAbgelehnt" }
  /api/v1/sites/{siteId}/data-sources/{id}/assignments/{assignmentId}:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: assignmentId, in: path, required: true, schema: { type: string, format: uuid } }
    delete:
      tags: [datenquellen]
      summary: Einen noch nicht wirksamen Zuständigkeitswechsel zurücknehmen.
      description: >
        Recht `datenquelle.zustaendigkeit`. Die geplante Anweisung bleibt historisch als
        zurückgenommen erhalten; der Vorgängerzeitraum wird wieder geöffnet und das Protokoll
        ergänzt. Fremde Quelle oder Zeitraum 404. Bereits wirksam oder bereits zurückgenommen
        ist 409 mit `grund` und dem Kundensatz in `satz`.
      responses:
        "200":
          description: Die Datenquelle ohne den zurückgenommenen Plan.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Datenquelle" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/DatenquelleAbgelehnt" }
  /api/v1/sites/{siteId}/data-sources/{id}/history:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [datenquellen]
      summary: Das Protokoll der Quelle, jüngster Eintrag zuerst.
      description: >
        Recht lesend (`aenderungsprotokoll.lesen`). Append-only (data_source_aenderung):
        angelegt · bearbeitet (nur die geänderten Felder) · erreichbarkeit_geprueft
        (Box, Ergebnis) · zustaendigkeit_begonnen · zustaendigkeit_gewechselt (alt:
        die bisherige Box) · zustaendigkeit_zurueckgenommen · aus_bestand_uebernommen
        (IP-4: neu = Kennzeichen, Weg,
        Takt, Box, Beginn, Komponenten; `gilt_ab` = Reihenbeginn), jeder Eintrag mit Urheber (Kundenbenutzer =
        kundenadministrator/kunde, Plattform-Admin = voltpilot_betrieb/voltpilot).
      responses:
        "200":
          description: Die Einträge.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DatenquelleProtokoll" }
  /api/v1/sites/{siteId}/data-sources/vorschlag:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [datenquellen]
      summary: Die Vorschlagsliste der Bestands-Übernahme - vorhandene Komponenten, gruppiert zu Datenquellen je Box.
      description: >
        Recht `datenquelle.ansehen` (AP-06 IP-4, Vertrag §8, Abnahme A12). Liest nur -
        bis zur Bestätigung ändert sich nichts: kein Push, kein Fahrplan, keine Fläche.
        Gruppiert werden die Komponenten der Anlage OHNE Quelle nach Box + Protokoll +
        Adresse (DatenquelleRegeln.vorschlagsliste, Vektoren Familie `bestand`); ein
        komponiertes Geschwister landet bei seinem Wechselrichter; was keinen lesbaren
        Weg hat, steht benannt unter `ausgelassen` (keine_box · keine_adresse ·
        protokoll_unbekannt · anker_ohne_vorschlag) - nie geraten. Die Box einer
        Komponente ist die, die sie heute liest: die eigene einer komponierten Zeile, die
        Box der Ladestation, sonst die führende Box (LeadDeviceService, `fuehrung`).
        `ab` ist der Reihenbeginn (erste Speisung der Komponente, frühestens seit die Box
        in der Anlage ist); `kennzeichen` ab der nächsten freien Nummer des Kundenbereichs.
      responses:
        "200":
          description: Die Vorschläge und die ausgelassenen Komponenten.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DatenquelleVorschlagsliste" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/sites/{siteId}/data-sources/vorschlag/uebernehmen:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [datenquellen]
      summary: Übernimmt bestätigte Vorschläge - Quelle, Zuständigkeit ab Reihenbeginn, Verweise, Protokoll.
      description: >
        Recht `datenquelle.bearbeiten` (AP-06 §4.8 anlegen; die Zuständigkeit ist die der
        HEUTIGEN Box, kein Wechsel). Je Vorschlag, in EINER Transaktion: die Quelle
        (Kennzeichen DQ-n vergibt der Server, ohne Name, Netzlage leer, Ein-Leser), ihre
        Zuständigkeit `[ab, offen)` auf die Minute - der EINZIGE Weg zu einem Beginn in der
        Vergangenheit (Vertrag §4) -, `measurement_point.data_source_id` der Komponenten,
        `geraet.data_source_id` des Geräts ihrer laufenden Speisung und EIN Protokoll-Eintrag
        `aus_bestand_uebernommen`. Bestätigt wird nur, was gezeigt wurde (Box, Protokoll,
        Adresse, Komponenten): sonst 409 `vorschlag_geaendert`. Hat eine Komponente
        inzwischen auf anderem Weg eine Quelle: 409 `komponente_hat_quelle`. Ist die
        Adresse an der Box schon vergeben: 409 `adresse_an_box_vergeben`. Ein schon
        übernommener Vorschlag zählt als `unveraendert` - ein zweiter Aufruf legt nichts an.
        Registry-Push, Flow-Aktivierung und Herzschlag bleiben unberührt (Push je Box: IP-6).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DatenquelleUebernehmen" }
            example:
              vorschlaege:
                - device_id: 6f1c2a9e-0000-4000-8000-0000000000e1
                  protokoll: modbus_tcp
                  adresse: "192.168.10.31:502"
                  komponenten: [2b7e0f1a-0000-4000-8000-0000000000a4, 2b7e0f1a-0000-4000-8000-0000000000a5]
      responses:
        "200":
          description: Übernommen (auch, wenn schon alles übernommen war - dann `neu` 0).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DatenquelleUebernommen" }
        "400": { $ref: "#/components/responses/DatenquelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/DatenquelleAbgelehnt" }
  /api/v1/sites/{siteId}/komponenten/{entityId}/messkanaele:
    get:
      tags: [messstellen]
      summary: Die Messkanäle einer Komponente mit Größe, Richtung, Einheit und Wertart (AP-04 IP-9).
      description: >
        Recht `messwerte.ansehen`. Je Zeile der Mess-Selektion der Komponente
        (Katalogpunkt oder Selbstbau, aktiv oder abgewählt) ein Kanal, sortiert nach
        Kanalname. `groesse`, `richtung`, `einheit` und `wertart` sind in den Wörtern
        des Messstellen-Vertrags genau die Eingänge der Passung (Regel 7) einer
        späteren Quellenbindung; `quantity`/`direction` sind die Katalogwörter, aus
        denen sie abgebildet sind (Tabelle in catalog/measurement-points/README.md).
        Ein Katalogwort ohne Vertragswort (etwa `import_export`, der Vorzeichen-Wert
        eines Zweirichtungszählers) ergibt `null` - nie ein geratenes Nachbarwort.
        Selbstbau trägt Name und Einheit, aber (noch) keine Wertart, Größe oder
        Richtung. `geraet` ist das Gerät, das die Komponente JETZT speist (AP-04
        IP-10: die Speisung, deren halboffener Zeitraum jetzt enthält) - für jeden
        Kanal dasselbe, `null` ohne laufende Speisung, nie geraten. `speist` nennt je
        Kanal die Quellenbindungen, die zum `stichtag` laufen (AP-04 IP-13: Messstelle,
        Größe, Rolle, Zweck) - leer ohne laufende Bindung. Mandanten-Zaun ist die RLS:
        eine fremde Komponente, eine Komponente einer anderen Anlage und eine unbekannte
        sind 404, nie 403; Plattform-Admins nutzen X-Tenant-Id.
      parameters:
        - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: entityId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: stichtag, in: query, required: false, schema: { type: string }, description: "Wann `speist` gilt: ein Zeitpunkt mit Versatz oder ein Tag (dessen Beginn in Europe/Berlin); fehlend = jetzt." }
      responses:
        "200":
          description: Die Kanäle der Komponente (leer ohne Mess-Selektion).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MesskanalListe" }
              example:
                site_id: 5b0e1c52-2f7c-4d0e-9a61-0c3d4b7e2a10
                komponente: 9f4a6d2e-8c1b-4e37-b5a0-3d2c1e0f7a64
                inhaltsstand: "2026.09.11.1"
                messkanaele:
                  - kanal: sunspec.model_203.totwhimp
                    anzeigename: Total Watt-hours Imported
                    einheit: Wh
                    wertart: counter
                    groesse: Wirkenergie
                    richtung: Bezug
                    quantity: active_energy
                    direction: import
                    kadenz_s: 10
                    aktiv: true
                    lesende_box: 1c9e3a7b-5d2f-4b60-8e14-7a0b2c3d4e5f
                    geraet:
                      id: 7d2c9e14-3a5b-4f60-8b17-2e0c4d6a8f31
                      geraet: GR-2
                      einbau: GR-2
                      seriennummer: null
                    speist:
                      - messstelle_id: 3e8a1c52-6d0f-4b27-9a13-5c2e7d4f8b90
                        messstelle: MS-01
                        groesse: Wirkenergie
                        richtung: Bezug
                        rolle: fuehrend
                        zweck: null
                        gueltig_ab: "2024-03-12T00:00:00+01:00"
                        gueltig_bis: null
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/funktionen:
    get:
      tags: [funktionen]
      summary: Je nicht archiviertem Standort beide Funktionen mit Zustand, „es fehlt“ und der Teilnahme je Anlage - dazu „läuft an x von y Standorten“.
      description: >
        Recht: heute lesend - keine eigene Kennung. Steuern & Optimieren am Standort ist der höchste
        Zustand seiner Teilnahmen; eine Anlage ohne Teilnahme ist `kein_objekt` („Diese Anlage misst
        nur“). Die Prüfliste steht nur in entwurf, eingerichtet und angehalten; `wege` nennt je roter
        Zeile, was zu tun ist; `aktionen` sind die Übergänge, die jetzt erlaubt wären. Liest nur.
      responses:
        "200":
          description: Die Funktionen des Unternehmens.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Funktionen" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/sites/{siteId}/gemeinsame-steuerung:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [funktionen]
      summary: Gemeinsame Steuerung der Anlage lesen (UEMS AP-15 IP-5).
      description: >
        Recht: heute lesend — keine eigene Kennung; eine Anlage außerhalb des Zugriffs ist dieselbe 404 wie eine
        unbekannte. Ohne Gemeinsame Steuerung `eingerichtet: false`, `zustand: nicht_eingerichtet` und sonst nichts
        (I6) — dazu nur die Warnung Z1 `warnung_fuehrung`, wenn führende Box und Speicher-Box auseinanderfallen (W2).
        Mit: Stufe, Epoche, wirksame Mitglieder und je fehlender Bedingung zum `naechster_schritt` ein Wort des
        Ablehnungs-Vokabulars (`fehlt`). Lesen legt nichts an.
      responses:
        "200":
          description: Der Zustand.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerung" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
    put:
      tags: [funktionen]
      summary: Gemeinsame Steuerung einrichten oder ändern (Kundenadministrator, I5).
      description: >
        Recht `funktion.steuern_einrichten` an der Anlage. Der Körper ist der gewünschte Stand der Mitglieder;
        unveränderte bleiben, geänderte und fehlende enden mit der laufenden Minute, neue beginnen mit ihr. Danach
        S0 `erklaert` oder — bei vollständiger Struktur — S1 `beobachtet` (I3). An den Boxen ändert sich nichts.
        Mandant und Anlage kommen aus Anmeldung und Pfad: jedes andere Feld ist 400. 409 mit dem ersten Wort des
        Ablehnungs-Vokabulars (`box_nicht_in_anlage` T1/R20, `fuehrende_box_misst_nicht`,
        `mitsteuernde_box_misst_nicht`) oder `erst_anhalten` (Anteile aktiv).
        **Erklärung (§5.2 Fragen 4/5, Vertrag §6a):** steht eines der Felder `geraete`/`ungeregelt` (je Mitglied),
        `ungesteuerte_erzeuger` oder `vorbehalt`, ist die Erklärung vollständig: jedes Mitglied nennt `geraete`, jede
        Komponente mit Schreibfreigabe an seiner Box steht darin, `ungesteuerte_erzeuger` ist `"keine"` oder eine
        Liste — sonst 422 `erklaerung_unvollstaendig` mit je Lücke der Kennung. Die Schreibfreigabe kommt aus dem
        Bestand (400, wenn sie erklärt wird). Ein kleinerer erklärter Vorbehalt als ein gemessener ist 409
        `vorbehalt_gemessen`. Jede Änderung ist eine Strukturänderung (Stufe zurück, Protokoll `geraete` ·
        `erzeuger` · `vorbehalt`). Ohne diese Felder bleibt die Erklärung stehen.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GemeinsameSteuerungEinrichten" }
      responses:
        "200":
          description: Eingerichtet bzw. geändert; der Zustand danach.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerung" }
        "400": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Das Recht `funktion.steuern_einrichten` fehlt. }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "409": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "422": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/sites/{siteId}/gemeinsame-steuerung/einrichten:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [funktionen]
      summary: Einrichten in sechs Fragen — Vorschlag, Erklärung und Ergebnis (UEMS AP-15, §5.2).
      description: >
        Recht: heute lesend — keine eigene Kennung; der Zaun liest an der Anlage, eine Anlage außerhalb ist dieselbe
        404 wie eine unbekannte. Je Box, was sie heute liest und steuern darf (Schreibfreigabe aus dem Bestand,
        Nennleistung soweit bekannt), die Box am Netzzähler, die wirksamen Grenzen; mit Gemeinsamer Steuerung dazu
        die Erklärung (Geräte je Box mit Herkunft des Rückfalls `am_geraet` · `katalog` · `ohne_angabe`,
        ungesteuerte Erzeuger, Vorbehalt mit Herkunft und dem Wert aus den Messwerten) und das Ergebnis (Frage 6:
        Anteile je Box und Richtung, Urteil der Auslegung). Ohne Gemeinsame Steuerung nur der Vorschlag; gelesen
        wird, geschrieben nie (I6).
      responses:
        "200":
          description: Vorschlag, Erklärung und Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerungEinrichtenAuskunft" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/sites/{siteId}/gemeinsame-steuerung/einrichten/vorschau:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [funktionen]
      summary: Frage 6 für einen Entwurf — Ergebnis vor dem Absenden, schreibt nichts (UEMS AP-15, §5.2 Nr. 6/7).
      description: >
        Recht `funktion.steuern_einrichten` an der Anlage (wie `PUT …/gemeinsame-steuerung`). Derselbe Körper wie
        das `PUT`, dieselben Prüfungen und Antworten (400, 409 mit dem ersten Wort des Ablehnungs-Vokabulars,
        422 `erklaerung_unvollstaendig` mit je Lücke der Kennung). Die Antwort ist das Bild von
        `GET …/gemeinsame-steuerung/einrichten`, als wäre der Entwurf gespeichert — das Ergebnis (Frage 6) aus
        derselben Ableitung. Geschrieben wird NICHTS: keine Zeile, kein Protokoll, keine Stufe; eine Anlage ohne
        Gemeinsame Steuerung bleibt ohne (I6). Erst „Absenden“ (`PUT`) schreibt. Dazu der Zustand, den das `PUT`
        antworten würde (`fehlt` trägt die Sätze von Frage 6, etwa `faehigkeit_fehlt`).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GemeinsameSteuerungEinrichten" }
      responses:
        "200":
          description: Vorschlag, Erklärung, Ergebnis und Zustand für den Entwurf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerungVorschau" }
        "400": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Das Recht `funktion.steuern_einrichten` fehlt. }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "409": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "422": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/sites/{siteId}/gemeinsame-steuerung/komponenten/{komponenteId}/rueckfall:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: komponenteId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [funktionen]
      summary: Rückfall-Angaben einer Komponente lesen (UEMS AP-15 IP-6).
      description: >
        Recht: heute lesend — keine eigene Kennung; der Zaun liest an der Anlage. Alle Angaben, neueste zuerst, mit
        wer/wann; `aufgehoben_am` gesetzt = nicht mehr wirksam. Eine Komponente einer anderen Anlage ist 404.
      responses:
        "200":
          description: Der Verlauf.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/GemeinsameSteuerungRueckfallAngabe" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
    put:
      tags: [funktionen]
      summary: Sicheren Rückfallwert am Gerät hinterlegen (UEMS AP-15 IP-6, G3).
      description: >
        Recht `funktion.steuern_einrichten` an der Anlage (wie einrichten). Hinterlegt, was am Gerät eingestellt ist
        (wer/wann), und hebt die bisherige Angabe dieser Richtung auf; dieselbe Angabe noch einmal schreibt nichts.
        Nur `faellt_auf_wert` trägt kW (dann Pflicht); eine Bestätigung am Prüfstand trägt nur der Betreiber ein
        (NW-7). Liefert den Verlauf.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GemeinsameSteuerungRueckfallHinterlegen" }
      responses:
        "200":
          description: Hinterlegt; der Verlauf danach.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/GemeinsameSteuerungRueckfallAngabe" }
        "400": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Das Recht `funktion.steuern_einrichten` fehlt. }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "409": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/sites/{siteId}/gemeinsame-steuerung/anhalten:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [funktionen]
      summary: Gemeinsame Steuerung anhalten (Kundenadministrator, I5).
      description: >
        Recht `steuerung.starten_beenden` an der Anlage. Nur aus `anteile_aktiv` (sonst 409 `nicht_aktiv`); danach
        `angehalten`: nur die führende Box bekommt Pläne, die Anteile bleiben in Kraft. Kein Körper (mit Feldern 400).
      responses:
        "200":
          description: Angehalten.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerung" }
        "400": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Das Recht `steuerung.starten_beenden` fehlt. }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "409": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/sites/{siteId}/gemeinsame-steuerung/fortsetzen:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [funktionen]
      summary: Gemeinsame Steuerung nach dem Anhalten fortsetzen (Kundenadministrator).
      description: >
        Recht `steuerung.starten_beenden` an der Anlage. Nur aus `angehalten` (sonst 409 `nicht_angehalten`); ohne
        neue Probe und in derselben Epoche, weil jede Änderung der Struktur die Stufe zurücksetzt — aber alle
        Bedingungen aus I1 werden in derselben Transaktion erneut geprüft (409 mit dem ersten Wort und `fehlt`).
        Kam das jüngste Anhalten nicht von einem Kundenkonto (der Betreiber, W9/I5), ist ein Kundenkonto 409
        `vom_betreiber_angehalten` — dann setzt nur der Betreiber fort (`POST /api/v1/admin/…/fortsetzen`).
      responses:
        "200":
          description: Fortgesetzt, `anteile_aktiv`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerung" }
        "400": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Das Recht `steuerung.starten_beenden` fehlt. }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "409": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/sites/{siteId}/gemeinsame-steuerung/aufloesen:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [funktionen]
      summary: Gemeinsame Steuerung auflösen (Kundenadministrator).
      description: >
        Recht `steuerung.starten_beenden` an der Anlage. Nie scharf (`epoche = 0`): sofort `aufgeloest`.
        Nach S3, auch in S1/S2 oder angehalten: alle mitsteuernden Boxen scheiden gemeinsam aus. `zustand` ist
        `wird_aufgeloest`, `aufloesen` zählt die bestätigten und die wartenden Boxen. Übergang auf Rückfall/0,
        führende Box auf min(alt, neu); Ziel erst nach jeder erforderlichen Quittung oder Betreiber-Bestätigung.
        Die führende Box hält als letztes Dokument Grenze minus Vorbehalt einschließlich reservierter Rückfälle.
        Dann enden alle Mitgliedschaften zeitgültig; Zustand `aufgeloest`, Historie bleibt erhalten.
        409 `zweischritt_laeuft` bei laufender Änderung oder Einzel-Ausscheiden; 409 `rueckgespielt` nach Rückspielen.
        Wieder einrichten öffnet denselben Verbund, Scharfschalten braucht eine neue Epoche.
      responses:
        "200":
          description: Aufgelöst oder gemeinsames Auflösen begonnen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerung" }
        "400": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Das Recht `steuerung.starten_beenden` fehlt. }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "409": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/sites/{siteId}/gemeinsame-steuerung/mitglieder/{boxId}/ausscheiden:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: boxId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [funktionen]
      summary: Eine Box scheidet aus der Gemeinsamen Steuerung aus (Kundenadministrator, AP-15 §5.5).
      description: >
        Recht `steuerung.starten_beenden` an der Anlage. Zweischritt (G5): im Übergang fällt der Anteil der Box in
        beiden Richtungen auf den Rückfall ihrer Geräte (G3; ohne Geräte 0), die anderen behalten ihren; erst nach
        IHRER Quittung bekommen sie den Rest nach G4 — der Rückfall ihrer Geräte bleibt im Vorbehalt reserviert. Bleibt
        die Quittung aus, steht der Übergang (R12), bis VoltPilot bestätigt, dass ihre Geräte vom Netz sind
        (`…/admin/…/ausscheiden-bestaetigen`). Dann endet die Mitgliedschaft (zeitgültig); die Box hält ihr letztes
        Dokument (V5). Nie scharf (kein Anteils-Dokument): die Mitgliedschaft endet sofort. Das Abmelden der Box
        (`DELETE /api/v1/devices/{id}`) führt denselben Weg und wartet auf VoltPilot. 409 `kein_mitglied`,
        `fuehrende_box_bleibt` (die führende Box, solange eine andere mitsteuert — erst die Rolle wechseln),
        `scheidet_schon_aus`, `zweischritt_laeuft` (eine Änderung wartet noch auf Quittungen), `rueckgespielt` (A18),
        `ausscheiden_passt_nicht` (ohne die Box passt die Auslegung nicht).
      responses:
        "200":
          description: Das Ausscheiden läuft; der Zustand danach (`mitglieder[].ausscheiden`).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GemeinsameSteuerung" }
        "400": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Das Recht `steuerung.starten_beenden` fehlt. }
        "404": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
        "409": { $ref: "#/components/responses/GemeinsameSteuerungAbgelehnt" }
  /api/v1/sites/{siteId}/funktionen/steuern:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [funktionen]
      summary: Anlage aufnehmen oder Steuern & Optimieren starten, anhalten, fortsetzen oder beenden.
      description: >
        Recht `funktion.steuern_einrichten` (aufnehmen), `steuerung.starten_beenden` (starten, beenden) bzw.
        `steuerung.anhalten_fortsetzen` (anhalten, fortsetzen). Aufnehmen legt nur Teilnahme und Ruhe an.
        Starten und Fortsetzen prüfen die Liste IN der Transaktion erneut (R1/R2);
        ist sie rot, antwortet 409 `pruefliste_offen` mit `fehlt` und `wege`, und nichts ist geschrieben.
        Starten/Fortsetzen heben die Ruhe auf, Anhalten/Beenden setzen sie (ohne Ende); der Registry-Push
        folgt nach dem Commit. Die Funktion des Standorts zieht nach.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FunktionSteuernAnfrage" }
      responses:
        "200":
          description: Übergang ausgeführt; der Standort, wie er danach steht.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FunktionSteuernErgebnis" }
        "400": { $ref: "#/components/responses/FunktionAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/FunktionAbgelehnt" }
        "409": { $ref: "#/components/responses/FunktionAbgelehnt" }
  /api/v1/sites/{siteId}/funktionen/steuern/pruefung:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [funktionen]
      summary: Frische Startprüfung für Steuern & Optimieren an einer Anlage.
      description: >
        Recht `steuerung.starten_beenden`, mit RechtPruefung an der Anlage durchgesetzt. Liefert für Box,
        Freigabe, Verbindungstest, Grenze, Hauptzähler und Betriebsweise den Fakt; rote Zeilen zusätzlich mit
        Grund und Weg. `freigaben` ergänzt komponentengenau die bestehenden Wege Selbstbau, OCPP und
        Wechselrichter samt Zählung. Fremde Anlage = 404. Liest nur; der Start prüft dieselben Fakten in seiner
        Transaktion erneut.
      responses:
        "200":
          description: Prüfliste und Folgen des Starts.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FunktionSteuernPruefung" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/FunktionAbgelehnt" }
  /api/v1/standorte/{standortId}/funktionen/steuern:
    parameters:
      - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [funktionen]
      summary: Steuern & Optimieren für den ganzen Standort anhalten, fortsetzen oder beenden - alle Teilnahmen, für die der Übergang erlaubt ist.
      description: >
        Recht `steuerung.anhalten_fortsetzen` (anhalten, fortsetzen) bzw. `steuerung.starten_beenden`
        (beenden). Gestartet wird je Anlage (`starten` hier = 400). Fortsetzen ist ganz oder gar nicht:
        ist eine Prüfliste rot, wird keine Teilnahme fortgesetzt (409 `pruefliste_offen`).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FunktionSteuernAnfrage" }
      responses:
        "200":
          description: Übergang ausgeführt; `betroffen` sind die Anlagen, deren Teilnahme sich geändert hat.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FunktionSteuernErgebnis" }
        "400": { $ref: "#/components/responses/FunktionAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/FunktionAbgelehnt" }
        "409": { $ref: "#/components/responses/FunktionAbgelehnt" }
  /api/v1/standorte/{standortId}/funktionen/messen:
    parameters:
      - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [funktionen]
      summary: Messen & Auswerten für einen Standort einrichten - legt die Funktion im Entwurf an (Schritt 1 des Assistenten).
      description: >
        Recht `funktion.messen_einrichten` (AP-01 IP-9a). Legt NUR das Funktionsobjekt des Standorts im
        Entwurf an; Box, Messstellen und Prüfung folgen in den weiteren Schritten, und ob der Entwurf
        eingerichtet ist, entscheidet jedes Lesen neu. Der Übergang ist der des Vertrags funktion-zustand:
        ein zweites Einrichten ist 409 `bereits_angelegt` (nie eine zweite Funktion), ein archivierter
        Standort 409 `standort_archiviert`. Einzige Aktion ist `einrichten` - Messen startet mit der
        Einrichtung von selbst.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FunktionMessenAnfrage" }
      responses:
        "200":
          description: Eingerichtet; der Standort, wie er danach steht.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FunktionMessenErgebnis" }
        "400": { $ref: "#/components/responses/FunktionAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/FunktionAbgelehnt" }
        "409": { $ref: "#/components/responses/FunktionAbgelehnt" }
  /api/v1/sites/{siteId}/bilanz:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [bilanz]
      summary: Die Bilanz einer Anlage je Hauptzähler - Zufluss, Abfluss, zugeordnet, Rest aus der Stellung, Live und der Vorschlag „Rest anlegen“.
      description: >
        Recht `messstelle.ansehen`. Die Periode `tag` · `monat` (Vorgabe) · `jahr`, die `am`
        enthält (Vorgabe heute, Zeitzone des Standorts). Je Hauptzähler Bezug der Anlage leitet
        BilanzAbleitung.restAusStellung die Terme JE TAG ab; gleiche Terme hintereinander sind ein
        Abschnitt. Gelten in der Periode überall dieselben Terme, rechnet EIN Abschnitt mit den
        Periodenwerten der Terme (Lese-Modell „Werte je Messstelle“); wechseln sie
        (`stellung_geaendert`), rechnet jeder Abschnitt Tag für Tag - eine Zahl über die ganze
        Periode gibt es dann nicht, auch nicht als gespeicherter Periodenwert der Rest-Messstelle
        (AP-10 IP-10, `terme_wechseln`). Zufluss/Abfluss/zugeordnet aus BilanzAbleitung.summe
        („mindestens …“, `mit_werten` von `gesamt`), der Rest aus MessstelleFormelRegeln.periodenwert
        (`rest`): ein nicht vollständiger Eingang macht ihn zu „keine Werte“ (menge null, nie 0).
        `live` (F18): der Rest JETZT in kW über den PR-688-Weg - null, sobald ein Term fehlt oder
        veraltet ist, mit `fehlende`. `vorschlag` nur, wenn der Hauptzähler heute Hauptzähler der
        Anlage ist und noch keine Rest-Messstelle hat. Liest nur.
      parameters:
        - { name: periode, in: query, required: false, schema: { type: string, enum: [tag, monat, jahr] } }
        - { name: am, in: query, required: false, schema: { type: string, format: date } }
      responses:
        "200":
          description: Die Bilanz.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Bilanz" }
        "400": { $ref: "#/components/responses/BilanzAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/sites/{siteId}/bilanz/rest:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [bilanz]
      summary: Vorschlag „Rest anlegen“ bestätigen - legt die Rest-Messstelle eines Hauptzählers an (Typ rest), nie zweimal.
      description: >
        Recht `messstelle.formel`. Nur für einen Hauptzähler, der HEUTE Hauptzähler Bezug dieser
        Anlage ist (sonst 422 `rest_ohne_hauptzaehler`). Legt in EINER Transaktion eine berechnete
        Messstelle an: Kennzeichen automatisch (MS-xxxx), Name wie angegeben oder vorbelegt
        „<Anlage> nicht zugeordnet“, Hauptgröße fest Wirkenergie · Bezug · kWh · Intervallmenge
        (E1), Formel-Fassung 1 vom Typ `rest` OHNE Terme (sie kommen je Tag aus der Stellung), und
        GENAU EIN Protokolleintrag `angelegt` mit dem Urheber. Hat der Hauptzähler schon eine
        Rest-Messstelle, entsteht nichts: 200 mit `neu` = false und der vorhandenen - auch wenn zwei
        Menschen gleichzeitig klicken (Sperre je Hauptzähler, eindeutiger Index dahinter).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BilanzRestAnlegen" }
      responses:
        "201":
          description: Die Rest-Messstelle ist entstanden.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BilanzRestAngelegt" }
        "200":
          description: Der Hauptzähler hatte schon eine Rest-Messstelle - nichts ist entstanden.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BilanzRestAngelegt" }
        "400": { $ref: "#/components/responses/BilanzAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/BilanzAbgelehnt" }
  /api/v1/sites/{siteId}/geraete:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [geraete]
      summary: Alle Geräte der Anlage, ausgebaute eingeschlossen.
      description: >
        Recht `messwerte.ansehen`. Je Einbau eine Zeile, nach Gerät (GR-2 vor
        GR-10) und darin nach Einbau; jede mit ihren Komponenten-Zeiträumen,
        Karten und Vorgängern. Ein Objekt, kein nacktes Array - erweiterbar,
        ohne die Form zu brechen.
      responses:
        "200":
          description: Die Geräte der Anlage (leer, wenn es keine gibt).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GeraetListe" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/geraete/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [geraete]
      summary: Ein Gerät (ein Einbau) mit Komponenten, Karten und Vorgängern.
      description: >
        Recht `messwerte.ansehen`. `vorgaenger` sind die früheren Einbauten
        desselben Geräts (gleiches `kennzeichen`, früher eingebaut), der jüngste
        zuerst - Z-5b nennt Z-5a mit `ausgebaut_am`.
      responses:
        "200":
          description: Das Gerät.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Geraet" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/geraete/{id}/aenderungen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - $ref: "#/components/parameters/ProtokollVon"
      - $ref: "#/components/parameters/ProtokollBis"
      - $ref: "#/components/parameters/ProtokollAchse"
      - $ref: "#/components/parameters/ProtokollLimit"
      - $ref: "#/components/parameters/ProtokollNach"
    get:
      tags: [geraete]
      summary: Das Aenderungsprotokoll EINES Geraets - Bindungen, Einstellungen, Ein- und Ausbau (AP-04 IP-21).
      description: >
        Recht `aenderungsprotokoll.lesen`. Ein Geraet hat kein eigenes Journal: seine
        Eintraege haengen an den Messstellen, die es speist, und nennen es ueber sein
        Einbau-Kennzeichen. Ein Zaehlerwechsel steht deshalb in BEIDEN
        Geraete-Protokollen - beim ausgebauten und beim eingebauten. Zeitachsen,
        Zeitraum und Seitenweise wie bei der Messstelle. Ein fremdes Geraet ist 404,
        nie 403.
      responses:
        "200":
          description: Das Protokoll des Geraets.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Protokoll" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/geraete/{id}/einstellungen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [geraete]
      summary: Die Einstellungs-Fassungen eines Geräts (Einbaus) - Historie und gültig zum Stichtag.
      description: >
        Recht `messwerte.ansehen`; ein Stichtag in der Vergangenheit („Stand
        am") zusätzlich `aenderungsprotokoll.lesen`. Wandlerverhältnis,
        Spannungswandler, Skalierung, Offset, Vorzeichen, Impulswertigkeit und
        Zählerkonstante als zeitgültige Fassungen AN DER QUELLE (UEMS AP-04
        IP-11, E4): am Einbau selbst, an einer Komponente, die er speist, oder
        an einem Kanal dieser Komponente (Vertrag
        docs/contracts/v2/quelle-einstellung.md). `gueltig` nennt je Quelle und
        Art die zum Stichtag gültige Fassung, `historie` alle - nach Quelle,
        Art und Beginn. Die Fassung 1 der Bestands-Komponenten ist aus ihrer
        heutigen Verbindung abgeleitet (`herkunft: bestand`, „gilt seit
        Beginn" der Speisung).
      parameters:
        - name: stichtag
          in: query
          required: false
          description: >
            Zeitpunkt nach ISO 8601 mit Versatz; ohne = jetzt. Das `+` eines
            Versatzes ist in der Adresse als `%2B` zu schreiben.
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: Die Einstellungen des Einbaus (leer, wenn keine erfasst ist).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Einstellungen" }
        "400": { $ref: "#/components/responses/EinstellungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      tags: [geraete]
      summary: Eine neue Einstellungs-Fassung eintragen - Wirkung nur ab „gültig ab".
      description: >
        Recht `messstelle.quelle` („Führende Quelle binden · Zählerwechsel ·
        Wandlerfaktoren"); mit „gültig ab" vor jetzt zusätzlich
        `aenderung.rueckwirkend`. Die neue Fassung beendet die zu ihrem Beginn
        gültige derselben Quelle und Art genau dort und gilt bis zum Beginn der
        nächsten späteren (höchstens bis zum Ende der Quelle). KEIN
        gespeicherter Wert ändert sich (E5): keine Messreihe, keine Verbindung,
        keine Komponenten-Fassung, nichts an der Box - eine angewendete Fassung
        steht als „Zustellung ausstehend", bis AP-06 zustellt; einen falsch
        erfassten Zeitraum berichtigt eine Korrektur (AP-08). Protokoll an der
        Quelle (die Fassung mit Urheber, Zeit, rückwirkend, Begründung) und an
        jeder Messstelle, deren Quellenbindung (führend oder Vergleich) zum
        Beginn der Fassung aus der Quelle liest - je ein Eintrag
        `einstellung_geaendert` in ihrem Protokoll; `messstellen` nennt sie.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EinstellungNeu" }
      responses:
        "201":
          description: Eingetragen - die neue Fassung, die beendete, die Folgen-Sätze.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EinstellungEingetragen" }
        "400": { $ref: "#/components/responses/EinstellungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/EinstellungAbgelehnt" }
        "422": { $ref: "#/components/responses/EinstellungAbgelehnt" }
  /api/v1/sites/{siteId}/wago/karten:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [components]
      summary: Die Energiekarten einer WAGO-Steuerung samt Controller anlegen (WAGO-Assistent, B05).
      description: >
        Recht `geraet.einrichten` an der Anlage. Legt je Karte die Komponente an (wie `POST
        /api/v1/sites/{siteId}/components`, mit der WAGO-Vorlage und bestandenem Verbindungstest) und
        in DERSELBEN Transaktion EINEN Controller (`geraet`, Geräteart `controller`) mit je Karte einem
        `geraet_teil` (Steckplatz, Kartentyp aus der Kartenlesung - ungelesen leer) und der Speisung
        der Komponente über diese Karte. Der Anlege-Trigger legt dann kein eigenes Gerät je Komponente
        an. Alle Karten brauchen dieselbe vollständige Verbindung (Adresse, Port, Geräte-ID,
        Basisadresse, Funktionscode, Wortfolge); ein `slot` der Verbindung muss dem Steckplatz
        gleichen. `index` der Antwort ist Karte n im Registerbild (n-te nach Steckplatz) - der Index,
        den eine Kartenpunkt-Auswahl `wago.pm49x.karte[n]` trägt.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WagoKartenAnlegen" }
      responses:
        "200":
          description: Der Controller und seine Karten.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/WagoKartenAnlage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Die Anlage wird noch auf der Box verwaltet, oder eine Karte wurde nicht als eigene Komponente angelegt.
        "422":
          description: Die Verbindung einer Karte ist nicht geprüft.
  /api/v1/sites/{siteId}/wago/karten/nachtragen:
    parameters:
      - { name: siteId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [components]
      summary: Bestand - bestehenden WAGO-Komponenten ohne Karte ihren Controller und ihre Karten nachtragen (B05).
      description: >
        Recht `geraet.einrichten` an der Anlage. Die genannten WAGO-Komponenten stecken in EINER
        Steuerung: es entsteht ein Controller mit je Komponente einer Karte, gültig ab der NÄCHSTEN
        vollen Minute. Die bisherige, abgeleitete Speisung endet dort (nichts wird gelöscht oder
        umgeschrieben); ihr Gerät wird ausgebaut, wenn es danach nichts mehr speist und keine Karte
        trägt. Eine Komponente, die schon in einer Karte steckt, bleibt unberührt (409 - ein Tausch
        ist ein Kartenwechsel).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WagoKartenNachtragen" }
      responses:
        "200":
          description: Der Controller und seine Karten.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/WagoKartenAnlage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Eine Komponente steckt schon in einer Energiekarte.
  /api/v1/geraete/{id}/wago/soll-lesen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [geraete]
      summary: Das Soll eines WAGO-Registerbilds aus der Steuerung lesen und speichern (AP-05, Folgepaket der Verdrahtung).
      description: >
        Recht `geraet.einrichten`; ein Gerät außerhalb des Zugriffs oder ohne Hersteller WAGO ist 404.
        Die Box `deviceId` liest über die gemeinsame Verbindung der heute zugeordneten Energiekarten
        den Kopf (`wago_kopf`, IP-7) und je Karte Steckplatz, Kartentyp und Variante (Vertrag
        `wago-registerbild.md` §4, Offset 0-2). Gespeichert wird AUSSCHLIESSLICH aus dieser Lesung und
        nur in leere Stellen: `geraet.controller_kennung`, je Karte `geraet_teil.variante` und - wenn
        leer - der Kartentyp. Weicht eine Lesung von einem gespeicherten Soll ab (oder passen
        Steckplatz/Kartentyp nicht zur Karte im Bestand), wird nichts überschrieben: `ergebnis`
        `abweichung` und ein Eintrag `wago_soll_abweichung` im Protokoll des Geräts. Schweigt die Box
        oder steht kein v1-Registerbild dort, ist die Antwort ein ehrlicher Ausgang (200,
        `nicht_gelesen`). Fehlendes Soll heißt an der Box „nicht geprüft“, nie 0.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WagoSollLesenEingabe" }
      responses:
        "200":
          description: Der Ausgang der Lesung und das gespeicherte Soll danach.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/WagoSollLesung" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Keine Energiekarte mit Steckplatz zugeordnet oder keine gemeinsame Verbindung der Karten.
  /api/v1/geraete/{id}/messmittel:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [geraete]
      summary: Messmittel-Angaben eines Einbaus und getrennte Herstellerangaben (AP-16 IP-15/IP-16).
      description: >
        Recht `messwerte.ansehen`; ein Gerät außerhalb des Zugriffs ist 404.
        Die Angaben hängen am EINBAU (G1): ein Zählerwechsel legt einen neuen
        Einbau ohne Angaben an. Ohne Angabe steht `nicht_erhoben` (G3) - nie
        ein Vorgabewert, nie eine gerechnete Genauigkeit der Messkette. Die
        Katalog-Angaben stehen in `laut_hersteller` getrennt daneben (IP-16), ersetzen die
        Einbau-Angabe nie und werden nicht zu einer Genauigkeit der Messkette verrechnet.
      responses:
        "200":
          description: Die Angaben des Einbaus.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessmittelAngaben" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [geraete]
      summary: Messmittel-Angaben eines Einbaus eintragen - die ganze Angabe, Beleg als Verweis mit SHA-256.
      description: >
        Recht `messmittel.angaben` (Kundenadministrator, Energiemanager,
        Bearbeiter am Standort, Unterstützer mit „Einrichten"); der Standort
        ist der des Einbaus (die Anlage des Geräts), außerhalb 404. Was fehlt,
        ist nicht erhoben. Der Beleg (G2) ist Bezeichnung, Ablage beim Kunden
        und die SHA-256 der Datei - im Portal gebildet, die Datei wird nie
        übertragen; ohne gültige Prüfsumme 422. Derselbe Verweis behält Person
        und Zeitpunkt seines Eintragens. `wandler` nennt nur die Fassungen,
        deren Klasse sich ändert; eine Klasse steht nur an `wandler_strom` und
        `wandler_spannung` dieses Einbaus. Jede Änderung steht als
        `messmittel_angabe` mit alt/neu und Akteur im Protokoll des Geräts; ein
        unveränderter PUT schreibt nichts.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MessmittelEintrag" }
      responses:
        "200":
          description: Die Angaben nach dem Eintragen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessmittelAngaben" }
        "400":
          description: anfrage_ungueltig - kein JSON-Objekt, unbekanntes Feld oder falsche Form.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessmittelFehler" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422":
          description: >
            pruefsumme_ungueltig, beleg_unvollstaendig, pruefungsart_unbekannt,
            zeitraum_ungueltig, text_zu_lang, fassung_unbekannt oder
            klasse_nur_am_wandler; `feld` nennt die Stelle.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessmittelFehler" }
  /api/v1/messstellen/{kennzeichen}/vergleich:
    parameters:
      - { name: kennzeichen, in: path, required: true, schema: { type: string } }
    get:
      tags: [messstellen]
      summary: Monatsvergleich führend ↔ Vergleich je Vergleichsquelle mit Toleranz und Befund (AP-16 IP-17, G5).
      description: >
        Recht `messwerte.ansehen`; eine Messstelle außerhalb des Zugriffs ist 404.
        Je Vergleichsquelle die Monate `von`–`bis` (JJJJ-MM, beide eingeschlossen,
        höchstens 24; ohne Angabe der letzte volle Monat in der Zeitzone der
        Messstelle): die führende Monatsmenge (dieselbe Zahl wie an `…/werte`),
        die gespeicherte Monatsmenge des Vergleichskanals, die Abweichung
        `|führend − Vergleich| ÷ führend` auf eine Stelle und die Toleranz-Fassung
        des Monats. Über der Toleranz (streng größer, ungerundet) steht ein Befund
        `abweichung_vergleichsquelle` - ohne Ursache, ohne Ersatz, ohne Änderung
        eines Werts (AP-04 E3, AP-08 E7). Eine Lücke, ein Ersatzwert oder ein
        angebrochener Monat der Vergleichsquelle ist `nicht_vergleichbar` mit
        `grund`, nie „passt“. Nur Vergleichsquellen der Hauptgröße mit einer
        Monatsmenge werden verglichen; die übrigen stehen mit
        `monatsvergleich: ohne_monatsmenge` und ohne Monate. Ohne Vergleichsquelle
        leere Listen - nichts wird gelesen.
      parameters:
        - { name: von, in: query, schema: { type: string, pattern: "^[0-9]{4}-[0-9]{2}$" } }
        - { name: bis, in: query, schema: { type: string, pattern: "^[0-9]{4}-[0-9]{2}$" } }
      responses:
        "200":
          description: Vergleichsquellen, Monate und Befunde.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/VergleichToleranzVergleich" }
        "400":
          description: anfrage_ungueltig - kein Monat JJJJ-MM, `bis` vor `von`, mehr als 24 Monate oder nur eine Grenze.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/VergleichToleranzFehler" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/messstellen/{id}/quellen/{quelleId}/toleranz:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: quelleId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [messstellen]
      summary: Toleranz einer Vergleichsquelle als neue Fassung mit Begründung (AP-16 IP-17, E10 = A).
      description: >
        Recht `messmittel.angaben` (Kundenadministrator, Energiemanager,
        Bearbeiter am Standort, Unterstützer mit „Einrichten“); Standort-Zaun über
        die Messstelle, außerhalb 404. Fassung 1 ist der Startwert 2 % je Monat
        des Vertrags und wird nie gespeichert. Eine neue Fassung n + 1 gilt ab dem
        laufenden Monat in der Zeitzone der Messstelle (frühestens ab dem Monat
        des Beginns der Vergleichsquelle) - nie rückwirkend, ein abgeschlossener
        Monat wird nie anders beurteilt. Dieselbe Toleranz wie die wirksame
        schreibt nichts (200). Die Toleranz ändert keinen Wert.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/VergleichToleranzEintrag" }
      responses:
        "201":
          description: Die neue Fassung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/VergleichToleranzFassung" }
        "200":
          description: Unverändert - die wirksame Fassung, nichts geschrieben.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/VergleichToleranzFassung" }
        "400":
          description: anfrage_ungueltig - kein JSON-Objekt oder unbekanntes Feld.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/VergleichToleranzFehler" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: nicht_gefunden - die Messstelle oder ihre Quelle gibt es nicht (oder außerhalb des Zugriffs).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/VergleichToleranzFehler" }
        "422":
          description: >
            toleranz_ungueltig (über 0, höchstens 100, höchstens zwei
            Nachkommastellen), begruendung_fehlt, text_zu_lang (über 500 Zeichen)
            oder keine_vergleichsquelle (die Quelle ist führend); `feld` nennt die Stelle.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/VergleichToleranzFehler" }
  /api/v1/geraete/{id}/austausch/vorschau:
    get:
      summary: Folgen des Controllerwechsels zum Zeitpunkt lesen (AP-04 E10)
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: zeitpunkt, in: query, schema: { type: string, format: date-time } }
      responses:
        "200":
          description: Betroffene Karten und Bindungen zum Zeitpunkt; ausschließlich gelesen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ControllerwechselVorschau" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/geraete/{id}/austausch:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [geraete]
      summary: Gerät austauschen - der Zählerwechsel als EIN Vorgang (AP-04 IP-17).
      description: >
        Recht `geraet.einrichten`; die Quellenbindungen, die dabei umziehen, tragen
        zusätzlich `messstelle.quelle`, ein Zeitpunkt vor jetzt außerdem
        `aenderung.rueckwirkend`. DERSELBE Vorgang wie
        `POST /api/v1/messstellen/{id}/quellen/wechsel`, nur vom anderen Einstieg aus:
        entweder alle Wirkungen landen oder keine. In EINER Transaktion bekommt das alte
        Gerät seinen Ausbau, das neue seinen Einbau (dasselbe Gerät-Kennzeichen GR-4, ein
        neues Einbau-Kennzeichen), jede laufende Speisung wandert mit, jede laufende
        Quellenbindung endet GENAU zum Wechselzeitpunkt und beginnt dort neu am neuen
        Einbau, die Ablesestände landen als Endstand des Vorgängers und Anfangsstand des
        Nachfolgers, die Einstellungs-Fassungen werden übernommen, und je betroffener
        Messstelle hält EIN Protokolleintrag `zaehler_gewechselt` den Vorgang mit Urheber
        und Rückwirkend-Marker fest. Additiv steht die Marke „Zähler gewechselt" im
        Komponenten-Verlauf (`component_change_event`, `device_replaced`) - ein Zusatz, der
        den Wechsel nie scheitern lässt. Geprüft wird in dieser Reihenfolge: Zeitpunkt im
        laufenden Einbau des Vorgängers (422 `zeitpunkt_vor_vorgaenger` davor, 422
        `kein_geraet_zum_zeitpunkt` an einem schon ausgebauten) → Energiekarten (409
        `zustand_passt_nicht`, falls die Kartenentscheidung fehlt; IP-19 übernimmt oder ersetzt jede Karte) → speist
        er überhaupt etwas (422 `kein_geraet_zum_zeitpunkt`) → angekündigte Quelle am alten
        Einbau (409 `zustand_passt_nicht`) → eine Bindung, die zum Zeitpunkt schon beendet
        ist (409 `bindung_bereits_beendet`) → freies Einbau-Kennzeichen (409
        `kennzeichen_belegt`) → je Bindung die Regeln der Quellenbindung (Vorgang
        `wechsel`). Eine archivierte Messstelle lehnt ab (409 `zustand_passt_nicht`); ein
        fremdes Gerät ist 404, nie 403. Der Wechsel ändert KEINEN gespeicherten Messwert:
        die Werte vor dem Zeitpunkt bleiben dem alten Einbau zugeordnet, die Lücke bis zum
        ersten Wert des neuen bleibt sichtbar und wird nie aufgefüllt.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/Zaehlerwechsel" }
      responses:
        "201":
          description: Gewechselt - beide Einbauten, die umgezogenen Bindungen, die übernommenen Einstellungen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ZaehlerwechselVorgang" }
        "400": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/MessstelleAbgelehnt" }
        "422": { $ref: "#/components/responses/MessstelleAbgelehnt" }
  /api/v1/bezugsgroessen:
    get:
      tags: [bezugsgroessen]
      summary: Die Bezugsgrößen des Kundenbereichs, archivierte eingeschlossen, nach Kennzeichen.
      description: Recht `messwerte.ansehen` (AP-09 §4.11).
      responses:
        "200":
          description: Die Bezugsgrößen (ein Objekt, kein nacktes Array - additiv erweiterbar).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseListe" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [bezugsgroessen]
      summary: Legt eine Bezugsgröße an; ohne Kennzeichen vergibt der Server BZ-0001 … (M2).
      description: >
        Recht `bezugsgroesse.verwalten` (AP-09 §4.11, W8). Geprüft in der Reihenfolge
        `verwalten.pruefreihenfolge.anlegen` des Vertrags, danach ob das Objekt des
        Geltungsbereichs im Kundenbereich da ist (`geltung_unbekannt`, ein fremdes
        antwortet gleich). Ohne Kennzeichen folgt die Nummer der HÖCHSTEN je belegten
        (auch einer gelöschten Bezugsgröße) - ein Kennzeichen wird nie weitergegeben
        (409 `kennzeichen_belegt`). Prozess und Kostenstelle sind seit AP-10 IP-7 wählbar
        (eine Art ohne Objekt wäre 422 `geltung_nicht_waehlbar`), eine Fläche wird am Gebäude gepflegt (422
        `flaeche_aus_struktur`). Die Anfrage wird streng gelesen: ein unbekanntes Feld
        (auch camelCase), ein Feld, das kein Text ist, eine `geltung_id`, die keine ID
        ist → 400 `anfrage_ungueltig` mit `feld`. GENAU EIN Protokolleintrag
        `angelegt`; eine Ablehnung schreibt nichts.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BezugsgroesseAnfrage" }
            example:
              name: Produktionsmenge Spritzguss
              wertart: periodenwert
              einheit: kg
              periode_art: monat
              geltung_art: standort
              geltung_id: 5b0d6f0e-8a3c-4f5e-9d2a-2f6f0c1b7a11
      responses:
        "201":
          description: Angelegt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Bezugsgroesse" }
        "400": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "422": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
  /api/v1/bezugsgroessen/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [bezugsgroessen]
      summary: Eine Bezugsgröße.
      description: Recht `messwerte.ansehen`. Fremd oder nicht da = 404 `nicht_gefunden`.
      responses:
        "200":
          description: Die Bezugsgröße.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Bezugsgroesse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
    put:
      tags: [bezugsgroessen]
      summary: Ändert die ganze Bezugsgröße - nach dem ersten Wert nur noch Name und Kennzeichen (M1).
      description: >
        Recht `bezugsgroesse.verwalten`. Der Körper ist die GANZE Bezugsgröße mit
        Kennzeichen. Nach dem ersten Wert bleiben Wertart, Einheit, Periodenart und
        Geltungsbereich fest (422 `bedeutung_fest` mit `felder`); eine archivierte wird
        nicht mehr geändert (409 `archiviert`); ein neues Kennzeichen, das eine andere
        trägt oder trug, ist 409 `kennzeichen_belegt` - die eigene darf zu ihrem
        früheren zurück. GENAU EIN Protokolleintrag `bearbeitet` mit den geänderten
        Feldern in `alt`/`neu`; ein unverändertes PUT schreibt nichts.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BezugsgroesseAnfrage" }
      responses:
        "200":
          description: Geändert (oder unverändert).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Bezugsgroesse" }
        "400": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "409": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "422": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
    delete:
      tags: [bezugsgroessen]
      summary: Löscht eine Bezugsgröße OHNE einen einzigen Wert (M6); ihr Kennzeichen bleibt belegt.
      description: >
        Recht `bezugsgroesse.verwalten`. Trägt sie eine Wert-Zeile - auch eine
        Rücknahme ist eine Fassung -, ist die Antwort 409 `hat_werte` (`werte`) und der
        Weg ist das Archivieren. Die Datenbank hält dieselbe Regel (Trigger
        `bezugsgroesse_nur_ohne_wert_loeschen`, V20260913120000); das Kennzeichen
        bleibt als Grabstein im Kennzeichen-Verlauf belegt. Protokolleintrag
        `geloescht` mit dem letzten Stand in `alt`. Verweise aus Energieeinsätzen (auch aufgehobene)
        ergeben 409 `bezugsgroesse_in_verwendung` mit `energieeinsaetze` als Liste der Kennzeichen,
        Variablen einer Bezugsbasis-Fassung (AP-17 IP-7) dasselbe mit `bezugsbasen`; zitiert ein freigegebener
        Berichtsstand die Bezugsgröße, 409 `berichts_belege` in der Form `BelegeImWeg` (AP-17 IP-21b, S4);
        die Bezugsgröße kann archiviert werden.
      responses:
        "204": { description: Gelöscht. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "409":
          description: Werte oder Energieeinsätze verhindern das Löschen.
          content:
            application/json:
              schema:
                oneOf:
                  - { $ref: "#/components/schemas/BezugsgroesseFehler" }
                  - { $ref: "#/components/schemas/EnergieeinsatzFehler" }
  /api/v1/bezugsgroessen/{id}/kanalbindung:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [bezugsgroessen]
      summary: Liest die zeitgültigen Kanalbindungen einer Bezugsgröße.
      description: Recht `messwerte.ansehen`; fremde Bezugsgröße ist 404.
      responses:
        "200":
          description: Bindungen in zeitlicher Reihenfolge.
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/BezugsgroesseKanalbindung" } }
        "404": { description: Bezugsgröße nicht gefunden. }
    post:
      tags: [bezugsgroessen]
      summary: Bindet einen Zähler-, Zustands-, Temperatur- oder Leistungskanal an Periodenwerte (K1–K6, E9).
      description: >
        Recht `bezugsgroesse.verwalten`. Minutenweise, halboffen; frühestens ab dem ersten
        gespeicherten Rohwert. Der Kanal muss Daten liefern. Ein Zustandskanal braucht einen
        gewählten Zustand; ein Zähler eine passende Einheit. Temperatur benötigt quantity=temperature, °C und Standortgeltung in Kd; Raumtemperatur und Heizgrenze sind je Bindung gespeichert (Vorgabe G20/15). Im gebundenen Zeitraum sind
        Eingabe, Berichtigung und Import mit 422 `kanal_gebunden` gesperrt. Archivieren erhält
        die Historie; eine Bezugsgröße mit Bindung kann nicht gelöscht werden.
        Betriebszeit aus Leistung benötigt eine Messstelle mit führendem Wirkleistungs-Gauge (W/kW),
        schwelle_kw und eine Begründung. Die nächste Fassung beendet die vorige; jede Zahl trägt
        „aus Leistung über x kW (Annahme)“. Akteur und unveränderliche Parameter bleiben gespeichert.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [von]
              anyOf:
                - required: [entity_id, kanal]
                - required: [messstelle_id, schwelle_kw, begruendung]
              properties:
                entity_id: { type: string, format: uuid }
                kanal: { type: string }
                messstelle_id: { type: string, format: uuid, description: "Nur Betriebszeit aus Leistung; entity_id und kanal werden daraus gelesen oder müssen dazu passen." }
                schwelle_kw: { type: number, minimum: 0, description: "Nur Betriebszeit aus Leistung; striktes Leistung > Schwelle." }
                begruendung: { type: string, minLength: 10, maxLength: 2000, description: "Pflicht für jede Schwellenfassung." }
                zustand: { type: string, nullable: true }
                von: { type: string, format: date-time }
                raumtemperatur: { type: number, default: 20, description: "Nur Temperatur; größer als Heizgrenze." }
                heizgrenze: { type: number, default: 15, description: "Nur Temperatur; darunter zählt Raumtemperatur minus Tagesmittel." }
      responses:
        "201":
          description: Bindung und Protokolleintrag angelegt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseKanalbindung" }
        "404": { description: Bezugsgröße oder Kanal nicht gefunden. }
        "409":
          description: "`wetterbezug_vorhanden` (AP-17 IP-12c) — die Gradtagzahl bezieht Wetter aus dem Archiv; erst lösen."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseKanalfehler" }
        "422":
          description: Bindung abgelehnt; Code und Kundensatz nennen den Grund.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseKanalfehler" }
  /api/v1/bezugsgroessen/{id}/kanalbindung/kanaele:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [bezugsgroessen]
      summary: Passende Messkanäle für den Bindungsdialog.
      description: Recht `bezugsgroesse.verwalten`; nur sichtbare Anlagen, passende Wertart und Einheit; fremde Bezugsgröße ist 404.
      responses:
        "200":
          description: Kanäle mit erstem Rohwert, Lieferzustand und gemessenen Zustandsbezeichnungen.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required: [entity_id, komponente, kanal, name, wertart, einheit, erste_messung, liefert, zustaende]
                  properties:
                    entity_id: { type: string, format: uuid }
                    komponente: { type: string, nullable: true }
                    kanal: { type: string }
                    name: { type: string, nullable: true }
                    wertart: { type: string, enum: [counter, state, gauge] }
                    einheit: { type: string, nullable: true }
                    erste_messung: { type: string, format: date-time, nullable: true }
                    liefert: { type: boolean }
                    zustaende: { type: array, items: { type: string } }
        "404": { description: Bezugsgröße nicht gefunden. }
  /api/v1/bezugsgroessen/{id}/kanalbindung/{bindung}/beenden:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: bindung, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [bezugsgroessen]
      summary: Beendet eine Kanalbindung einmalig ab jetzt oder später.
      description: Recht `bezugsgroesse.verwalten`. `bis` ist minutengenau und ausschließlich.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [bis]
              properties:
                bis: { type: string, format: date-time }
      responses:
        "200":
          description: Beendet und protokolliert.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseKanalbindung" }
        "404": { description: Bezugsgröße oder Bindung nicht gefunden. }
        "422":
          description: Ende ungültig oder Bindung bereits beendet.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseKanalfehler" }
  /api/v1/bezugsgroessen/{id}/wetterbezug:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [bezugsgroessen]
      summary: Liest die Bindung einer Gradtagzahl an das Wetter-Archiv mit letztem Abruf und Stand (AP-17 IP-12c).
      description: Recht `messwerte.ansehen`; fremde Bezugsgröße ist 404. `bindung` ist null, solange nichts gebunden ist.
      responses:
        "200":
          description: Wetter-Sicht der Bezugsgröße.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseWetterbezug" }
        "404": { description: Bezugsgröße nicht gefunden. }
    put:
      tags: [bezugsgroessen]
      summary: Bindet eine Gradtagzahl an das Wetter-Archiv (Herkunft `bezogen`, E9 = C).
      description: >
        Recht `bezugsgroesse.verwalten`. Nur Art `gradtagzahl`, Geltung Standort, Periodenart Tag oder Monat,
        am Standort mit Koordinaten. Kein sofortiger Abruf — der tägliche `WetterArchivLaeufer` holt die Tage ab
        `von` (höchstens gestern; bei Monatswerten der Erste eines Monats). Dieselbe Bindung noch einmal ist 200 ohne
        Änderung. Eine Bezugsgröße hat eine Quelle: mit Kanalbindung, mit Werten anderer Herkunft oder mit einer
        anderen Wetter-Bindung 409. Protokoll `wetter_gebunden`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [von]
              properties:
                von: { type: string, format: date }
                raumtemperatur: { type: number, default: 20, description: "Größer als die Heizgrenze." }
                heizgrenze: { type: number, default: 15 }
      responses:
        "200":
          description: Gebunden (oder unverändert gebunden).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseWetterbezug" }
        "404": { description: Bezugsgröße nicht gefunden oder außerhalb des Zugriffs. }
        "409":
          description: "`kanalbindung_vorhanden`, `werte_vorhanden` oder `wetterbezug_vorhanden`."
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseKanalfehler" }
        "422":
          description: >
            `art_passt_nicht`, `grenzen_ungueltig`, `von_ungueltig` oder `koordinaten_fehlen` — dieser mit dem Satz
            aus AP-17 §5.8: „Für den Standort Lindach kann VoltPilot kein Wetter beziehen: die Koordinaten fehlen. …“
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseKanalfehler" }
    delete:
      tags: [bezugsgroessen]
      summary: Löst die Bindung an das Wetter-Archiv; die bezogenen Werte bleiben.
      description: Recht `bezugsgroesse.verwalten`. Ohne Bindung ändert sich nichts. Protokoll `wetter_geloest`.
      responses:
        "204": { description: Gelöst (oder war nicht gebunden). }
        "404": { description: Bezugsgröße nicht gefunden oder außerhalb des Zugriffs. }
  /api/v1/standorte/{standortId}/wetter:
    parameters:
      - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [bezugsgroessen]
      summary: Die Zeile „Wetter“ am Standort (AP-17 IP-12c).
      description: >
        Recht `messwerte.ansehen`; ein Standort außerhalb des Zugriffs ist 404. Koordinaten ja/nein (ohne: der Satz
        aus §5.8), die an das Wetter-Archiv gebundenen Gradtagzahlen im Zugriff und der späteste Abruf.
      responses:
        "200":
          description: Zeile „Wetter“.
          content:
            application/json:
              schema:
                type: object
                required: [koordinaten, gradtagzahlen]
                properties:
                  koordinaten: { type: boolean }
                  satz: { type: string }
                  quelle: { type: string }
                  letzter_abruf: { type: string, format: date-time }
                  gradtagzahlen:
                    type: array
                    items:
                      type: object
                      required: [id, kennzeichen, name]
                      properties:
                        id: { type: string, format: uuid }
                        kennzeichen: { type: string }
                        name: { type: string }
                        quelle: { type: string }
                        letzter_abruf: { type: string, format: date-time }
        "404": { description: Standort nicht gefunden. }
  /api/v1/bezugsgroessen/{id}/archivieren:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [bezugsgroessen]
      summary: Archiviert eine Bezugsgröße - ihre Werte bleiben lesbar, ihr Kennzeichen belegt (M6).
      description: Recht `bezugsgroesse.verwalten`. Einmal; ein zweites Mal ist 409 `archiviert`. Protokolleintrag `archiviert`.
      responses:
        "200":
          description: Archiviert.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Bezugsgroesse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "409": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
  /api/v1/bezugsgroessen/{id}/werte:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [bezugsgroessen]
      summary: Die Werte im Zeitraum mit ihren Fassungen und der Herkunft je Fassung.
      parameters:
        - { name: von, in: query, required: false, schema: { type: string, format: date }, description: "Erster Tag (einschließlich)." }
        - { name: bis, in: query, required: false, schema: { type: string, format: date }, description: "Letzter Tag (EINSCHLIESSLICH)." }
        - { name: fassungen, in: query, required: false, schema: { type: string, enum: [wirksam, alle], default: wirksam } }
      description: >
        Recht `messwerte.ansehen` („Werte, Herkunft, Fassungen ansehen“). Ein Wert ist
        ein Schlüssel - eine Periode (`periode_von`/`periode_bis`, der letzte Tag
        einschließlich) oder ein Zeitpunkt (Stand) - mit seinen Fassungen, älteste
        zuerst. Ein Periodenwert gehört dazu, wenn seine Periode den Zeitraum berührt;
        ein Stand, wenn sein Tag in SEINER Zone (`zeitzone`) darin liegt. `fassungen=wirksam`
        (Vorgabe) zeigt je Schlüssel die Fassung, die den wirksamen Betrag trägt, mit
        ihrer Herkunft; `alle` die ganze Kette. Den Stand jeder Fassung („wirksam bis
        Fassung 2“) und den wirksamen Betrag rechnet die Regel `fassung` des Vertrags
        (BezugsdatenRegeln.fassungen). Nach einer Rücknahme ist `wirksamer_betrag` null,
        nie 0. Eine freigegebene Berichtigung ist EINE wirksame Fassung mit Urheber und
        Freigeber (AP-09 IP-7, Vertrag B5); ein offener Vorschlag ist noch keine Fassung und
        steht als `vorschlag` am Wert. Trägt eine Kette dennoch eine gespeicherte Vorschlags-
        oder Ablehnungs-Fassung, ist `stand_offen` true, `stand` und `wirksamer_betrag` null
        und alle Fassungen werden gezeigt. `von` nach `bis` = 400 `zeitraum_ungueltig`; ein unbekanntes Wort in
        `fassungen` = 400 `wort_unbekannt`.
      responses:
        "200":
          description: Die Werte.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseWerte" }
        "400": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
    post:
      tags: [bezugsgroessen]
      summary: Einen Wert für eine abgeschlossene Periode eingeben (AP-09 IP-7).
      description: >
        Recht `bezugsgroesse.eingeben`. Prüfreihenfolge `verwalten.pruefreihenfolge.eingeben`:
        archiviert (409 `archiviert`) → nur ein Periodenwert (422 `kein_periodenwert`) → die
        Periode (422 `periode_passt_nicht`, `periode_nicht_zu_ende` - Z4) → die Zahl (422
        `zahl_unlesbar`, bei Stück, Personen und Schichten mit `hinweis` „Stück sind ganze
        Zahlen.“ - U5) → nicht negativ (422 `wert_negativ` - U6) → ein ANDERER wirksamer Wert
        (409 `konflikt_anderer_wert` mit `wirksamer_betrag` - der Weg ist die Berichtigung, F5).
        Ein Erstwert ist Fassung 1 ohne Begründung und ohne Freigabe, auch bei Vier-Augen an
        (F1): 201 `neu`, kein Ereignis. Derselbe Betrag noch einmal ist eine Wiederholung: 200
        `wiederholung`, nichts geschrieben. Ein unplausibler, gültiger Wert (eine Betriebszeit
        über der Stundenzahl der Periode, bei einer Messstelle als Bezug) geht durch und trägt
        nur den Hinweis `wert_unplausibel` (U6). Eine Ablehnung schreibt nichts.
        Eine an das Wetter-Archiv gebundene Gradtagzahl (Zeile in `bezugsgroesse_wetterbezug`) hat
        eine Quelle: 409 `wetterbezug_vorhanden` mit dem Satz „Diese Gradtagzahl bezieht ihr Wetter
        von VoltPilot. Werte werden hier nicht eingegeben oder importiert; lösen Sie zuerst den
        Wetterbezug.“ (AP-17 Nachlese 1, wie `kanal_gebunden` an einer Kanalbindung).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BezugsgroesseWertAnfrage" }
            example: { periode: "2026-10", wert: "48.200" }
      responses:
        "200":
          description: Wiederholung - derselbe Betrag ist schon gespeichert, nichts geschrieben.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseEingabe" }
        "201":
          description: Gespeichert als Erstwert.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseEingabe" }
        "400": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "409": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "422": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
  /api/v1/bezugsgroessen/{id}/werte/{periode}/berichtigung:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: periode, in: path, required: true, schema: { type: string }, description: "Die Periode des Werts, z. B. `2026-10` (wie `BezugsgroesseWertAnfrage.periode`)." }
    post:
      tags: [bezugsgroessen]
      summary: Einen gespeicherten Wert berichtigen - Fassung n + 1 oder, bei Vier-Augen an, ein Vorschlag (AP-09 IP-7).
      description: >
        Recht `bezugsgroesse.eingeben`. Prüfreihenfolge `verwalten.pruefreihenfolge.berichtigen`:
        dieselben Prüfungen des Werts wie beim Eingeben → Begründung mit 10 bis 500 Zeichen (422
        `begruendung_zu_kurz`, `begruendung_zu_lang` - F2) → es gibt einen wirksamen Wert (409
        `kein_wert`) → kein offener Vorschlag (409 `vorschlag_offen` - F3). Derselbe Betrag wie
        der wirksame ist eine Wiederholung (200, nichts geschrieben). Jede Berichtigung ist ein
        Vorgang `BK-<Jahr>-<lfd. Nr.>`. Vier-Augen aus (Vorgabe): 201 `berichtigung` - Fassung
        n + 1 ist sofort wirksam, Fassung n bleibt lesbar („wirksam bis Fassung n + 1“), und es
        entsteht das Ereignis `correction` mit Bezug `bezugsgroesse` (F4). Vier-Augen an: 201
        `vorschlag` - der Wert bleibt, wie er ist, der Vorschlag steht als `vorschlag` am Wert;
        freigegeben wird über `POST /api/v1/korrekturen/{kennung}/freigeben`, nie von der
        Erstellerin (403 `zweite_person_noetig`). Erst die Freigabe schreibt die Fassung - mit
        Urheber UND Freigeber - und das Ereignis. Eine Ablehnung schreibt nichts. An einer
        wetter-gebundenen Gradtagzahl 409 `wetterbezug_vorhanden` wie beim Eingeben; ebenso die
        Freigabe eines älteren Vorschlags nach dem Binden.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BezugsgroesseBerichtigungAnfrage" }
            example:
              wert: "48.200"
              begruendung: "Tippfehler — eine Null fehlte (Montagebericht Oktober)"
      responses:
        "200":
          description: Wiederholung - derselbe Betrag ist schon wirksam, nichts geschrieben.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseEingabe" }
        "201":
          description: Berichtigt (Fassung n + 1) oder als Vorschlag gespeichert.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseEingabe" }
        "400": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "409": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "422": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
  /api/v1/bezugsgroessen/{id}/stammdatum:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [bezugsgroessen]
      summary: Die Intervalle eines Stammdatums (E15) - und auf Wunsch der Wert je Periode am Stichtag (E17, S3).
      description: >
        Recht `messwerte.ansehen`. Ein Stammdatum, das AP-09 selbst hält (Mitarbeitende u. a.),
        hat Gültigkeiten statt Fassungen: `intervalle` zeigt jedes, aufgehobene
        (Korrekturen) eingeschlossen, mit dem Abzeichen „rückwirkend (n Tage)“. Mit
        `periode_art`, `von` und `bis` (alle drei) kommt je Periode, die den Zeitraum
        berührt, der Wert am Stichtag = dem LETZTEN Tag der Periode; ändert sich der Wert
        nach dem ersten Tag der Periode, nennt `kennzeichen` jeden Übergang (Sätze
        `stammdatum_saetze` des Bezugsdaten-Vertrags). Ohne gültiges Intervall am Stichtag ist
        `betrag` null, nie 0. Eine Bezugsgröße, die kein Stammdatum ist = 422
        `kein_stammdatum`.
      parameters:
        - { name: periode_art, in: query, required: false, schema: { type: string, enum: [tag, woche, monat, jahr] } }
        - { name: von, in: query, required: false, schema: { type: string, format: date }, description: "Erster Tag (einschließlich)." }
        - { name: bis, in: query, required: false, schema: { type: string, format: date }, description: "Letzter Tag (EINSCHLIESSLICH)." }
      responses:
        "200":
          description: Das Stammdatum.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseStammdatum" }
        "400": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "422": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
    put:
      tags: [bezugsgroessen]
      summary: Ein Wert eines Stammdatums ab einem Tag (E15/S4) - die Mechanik der Bezugsfläche.
      description: >
        Recht `bezugsgroesse.eingeben`. Das laufende Intervall endet am VORTAG, das neue erbt
        dessen Ende; ein Wert am Tag, an dem schon eines beginnt, ist eine Korrektur (das
        alte wird aufgehoben und bleibt lesbar); gilt am Tag schon genau dieser Wert, wird
        nichts geschrieben (Regel `stammdatum_eintrag`). Genau ein Protokolleintrag
        `stammdatum_eingetragen` mit „gilt ab“ und „rückwirkend“. Prüfreihenfolge
        `verwalten.pruefreihenfolge.stammdatum`: 409 `archiviert` → 422 `kein_stammdatum` →
        422 `flaeche_aus_struktur` → 400 `wert_ungueltig`. Eine Bezugsfläche wird hier NIE
        geschrieben - sie steht in der Ortsstruktur (`PUT /api/v1/orte/{id}/flaeche`).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BezugsgroesseStammdatumAnfrage" }
      responses:
        "200":
          description: Das Stammdatum nach dem Eintrag (ohne Perioden).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsgroesseStammdatum" }
        "400": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "409": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "422": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
  /api/v1/bericht-vorlagen:
    get:
      tags: [berichte]
      summary: Die Berichtsvorlagen - vier VoltPilot-Vorlagen mit Fassung und festen Abschnitten (V2).
      description: >
        Keine eigene Rechte-Kennung: der Katalog ist für jede angemeldete Person derselbe und nennt
        keine Daten eines Kundenbereichs. UEMS AP-12 IP-5. Die Antwort ist die Datei
        docs/contracts/v2/bericht-vorlagen.json Byte für Byte (API-Ressource berichte/bericht-vorlagen.json,
        Portal-Kopie frontend/portal/src/berichte/); ihre Form ist
        docs/contracts/v2/bericht.schema.json $defs/vorlagen_datei. Eine neue Fassung ändert keinen
        Berichtsstand (V3).
      responses:
        "200":
          description: Der Vorlagen-Katalog.
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/berichte:
    get:
      tags: [berichte]
      summary: Die Berichte, die die Person lesen darf - mit Vermerk (R5); archivierte nicht (V4).
      description: >
        Recht je Bericht: `bericht.standort_abrufen` am Standort-Bericht, `bericht.unternehmen_abrufen` am
        Unternehmens-Bericht, `bewertung.ansehen` an der energetischen Bewertung (Lesen getrennt vom Freigeben,
        AP-19 IP-11) (E12 G1, durchgesetzt über uems/BerichtRechte); fremder Kundenbereich oder
        fremder Standort 404 `nicht_gefunden`, fehlendes Recht 403 `recht_fehlt` - die Unterstützung
        bekommt nie Entwurf oder Stand. Die Liste enthält nur, was die Person lesen darf; darf sie
        nirgends einen Bericht lesen, ist das 403. Neueste zuerst. UEMS AP-12 IP-7.
      responses:
        "200":
          description: Die Berichte.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BerichtListe" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BerichtAbgelehnt" }
    post:
      tags: [berichte]
      summary: Legt einen Bericht an (Vorlage × Geltung × Zeitraum) und bildet seinen Entwurf.
      description: >
        Recht `bericht.standort_freigeben` am Standort-Bericht, `bericht.unternehmen` am
        Unternehmens-Bericht (E12 G1, durchgesetzt über uems/BerichtRechte); fremder Kundenbereich oder
        fremder Standort 404 `nicht_gefunden`, fehlendes Recht 403 `recht_fehlt`. Anlegen folgt dem
        Freigabe-Recht. Reihenfolge: Felder (400 `anfrage_ungueltig`) → Vorlage (422
        `vorlage_unbekannt`) → Zeitraum-Schlüssel passend zur Vorlage (400) → Geltung des Kundenbereichs
        (404 `geltung_unbekannt`, fremd ebenso) → Recht → Vorlage × Geltung × Zeitraum gibt es schon
        (409 `bericht_gibt_es_schon` mit `kennung`, auch archiviert) → Messstellen der Geltung im Zeitraum
        (422 `keine_quellen`; am Unternehmen die Netzbezugs-Zähler der Standorte, die Unternehmens- und die
        Prozess-Messstellen - AP-12 IP-6). Dann in EINER Transaktion: Kennung `BR-<Jahr>-<Nr.>` (Jahr des Anlegens),
        Entwurf mit Datenstand = jetzt (D1, `gebildet_von` anlegen, uems/BerichtAbzugBildung), Protokoll
        `anlegen`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BerichtAnlegen" }
            example:
              vorlage: monatsbericht_standort
              geltung_id: "5f0c9a1e-3b2d-4c6e-8a7f-1d2e3f4a5b6c"
              zeitraum: "2026-10"
      responses:
        "201":
          description: Angelegt - mit Entwurf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Bericht" }
        "400": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "404": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "409": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "422": { $ref: "#/components/responses/BerichtAbgelehnt" }
  /api/v1/berichte/betroffen:
    get:
      tags: [berichte]
      summary: Die Zeile „Freigegebene Berichte …“ einer Folgen-Karte - wen eine Strukturänderung ab gilt_ab träfe.
      description: >
        Recht je Bericht: `bericht.standort_abrufen` am Standort-Bericht, `bericht.unternehmen_abrufen` am
        Unternehmens-Bericht, `bewertung.ansehen` an der energetischen Bewertung (Lesen getrennt vom Freigeben,
        AP-19 IP-11); genannt werden nur Stände, die die Person lesen darf; darf sie nirgends einen
        Bericht lesen, 403 `recht_fehlt`. Für die Folgen-Karten „Fläche ändern“ (anlass flaeche_rueckwirkend,
        objekt = Gebäude, Bereich oder Standort), „Anlage zuordnen“ (anlage_umzug_rueckwirkend, objekt = Anlage)
        und „Archivieren“ (zuordnung_rueckwirkend, objekt = Ort oder Standort, gilt_ab = Archivtag). Dieselbe
        Auflösung wie der Strukturänderungs-Läufer (uems/StrukturAufloesung, bericht.md §7 B3) - ohne
        Protokollzeile und ohne Datenstand-Schranke, weil die Änderung noch nicht geschrieben ist. `betroffen`:
        die gültigen Stände, die den Vermerk „Revision nötig“ bekämen; `zitieren`: jeder Stand (auch ersetzte),
        der eine Quelle des Objekts zitiert (B12); `berichte_vorhanden`: die Person liest hier mindestens einen
        Bericht (sonst beide Listen leer). Reihenfolge: Felder (400 `anfrage_ungueltig` mit `feld`) → Recht (403)
        → Objekt des Kundenbereichs, passend zur Art (404 `nicht_gefunden`). Schreibt nichts. UEMS AP-12 IP-9.
      parameters:
        - { name: objekt, in: query, required: true, schema: { type: string, format: uuid } }
        - { name: gilt_ab, in: query, required: true, schema: { type: string, format: date } }
        - name: anlass
          in: query
          required: true
          schema: { type: string, enum: [zuordnung_rueckwirkend, anlage_umzug_rueckwirkend, flaeche_rueckwirkend, verteilung_rueckwirkend] }
      responses:
        "200":
          description: Die betroffenen und die zitierenden Stände.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BerichteBetroffen" }
        "400": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "404": { $ref: "#/components/responses/BerichtAbgelehnt" }
  /api/v1/berichte/{kennung}:
    parameters:
      - { name: kennung, in: path, required: true, schema: { type: string, pattern: "^BR-[0-9]{4}-[0-9]{4,}$" }, description: "Die Kennung des Berichts (BR-<Jahr>-<Nr.>); eine, die keine ist, ist 404 `nicht_gefunden`." }
    get:
      tags: [berichte]
      summary: Kopf, Berichtsstände (Nr. 1 zuerst) und Revisions-Anstöße eines Berichts.
      description: >
        Recht `bericht.standort_abrufen` am Standort-Bericht, `bericht.unternehmen_abrufen` am
        Unternehmens-Bericht, `bewertung.ansehen` an der energetischen Bewertung (Lesen getrennt vom Freigeben,
        AP-19 IP-11) (E12 G1, durchgesetzt über uems/BerichtRechte); fremder Kundenbereich oder
        fremder Standort 404 `nicht_gefunden`, fehlendes Recht 403 `recht_fehlt` - die Unterstützung
        bekommt nie Entwurf oder Stand. Ein archivierter Bericht bleibt lesbar.
      responses:
        "200":
          description: Der Bericht.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BerichtDetail" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "404": { $ref: "#/components/responses/BerichtAbgelehnt" }
  /api/v1/berichte/{kennung}/wiedervorlage:
    parameters:
      - { name: kennung, in: path, required: true, schema: { type: string, pattern: "^BR-[0-9]{4}-[0-9]{4,}$" } }
    put:
      tags: [berichte]
      summary: Wiedervorlage der energetischen Bewertung ändern.
      description: >
        Nur für die Vorlage `energetische_bewertung`, Recht `bericht.unternehmen`. Die Änderung braucht eine Begründung
        und leitet noch keine Fälligkeit ab (AP-16 IP-21; die Fälligkeitsableitung folgt in IP-24).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BerichtWiedervorlage" }
      responses:
        "200":
          description: Der Bericht mit der geänderten Wiedervorlage.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Bericht" }
        "400": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "404": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "422": { $ref: "#/components/responses/BerichtAbgelehnt" }
  /api/v1/berichte/{kennung}/entwurf:
    parameters:
      - { name: kennung, in: path, required: true, schema: { type: string, pattern: "^BR-[0-9]{4}-[0-9]{4,}$" }, description: "Die Kennung des Berichts (BR-<Jahr>-<Nr.>); eine, die keine ist, ist 404 `nicht_gefunden`." }
    get:
      tags: [berichte]
      summary: Der Entwurf - auf Aktualität geprüft (D4), nötigenfalls neu gebildet.
      description: >
        Recht `bericht.standort_abrufen` am Standort-Bericht, `bericht.unternehmen_abrufen` am
        Unternehmens-Bericht, `bewertung.ansehen` an der energetischen Bewertung (Lesen getrennt vom Freigeben,
        AP-19 IP-11) (E12 G1, durchgesetzt über uems/BerichtRechte); fremder Kundenbereich oder
        fremder Standort 404 `nicht_gefunden`, fehlendes Recht 403 `recht_fehlt` - die Unterstützung
        bekommt nie Entwurf oder Stand. D4: hat eine Quelle des Entwurfs seit seinem Datenstand eine
        neue Version, Fassung, Periodenrechnung oder ein rückwirkendes Protokoll, oder lief seitdem das
        Ende des Zeitraums oder ein „endgültig ab“ ab, wird der Entwurf neu gebildet (`gebildet_von`
        abruf, `neu_gebildet` true, kein Ereignis - B6). Der Abzug steht als der gespeicherte kanonische
        Text in der Antwort. Ein Entwurf wird nie als Datei ausgegeben und nie als Abruf protokolliert
        (EW4). Standort- und Unternehmensbericht gleich (seit AP-12 IP-6).
      responses:
        "200":
          description: Der aktuelle Entwurf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BerichtEntwurf" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "404": { $ref: "#/components/responses/BerichtAbgelehnt" }
  /api/v1/berichte/{kennung}/entwurf/vergleich:
    parameters:
      - { name: kennung, in: path, required: true, schema: { type: string, pattern: "^BR-[0-9]{4}-[0-9]{4,}$" }, description: "Die Kennung des Berichts (BR-<Jahr>-<Nr.>); eine, die keine ist, ist 404 `nicht_gefunden`." }
      - { name: gegen, in: query, required: true, schema: { type: integer, minimum: 1 }, description: "Die Nr. des Berichtsstands, gegen den verglichen wird; fehlt sie, 400 `anfrage_ungueltig`." }
    get:
      tags: [berichte]
      summary: Vergleicht den aktuellen Entwurf mit einem Berichtsstand - jede Abweichung (R1).
      description: >
        Recht `bericht.standort_abrufen` am Standort-Bericht, `bericht.unternehmen_abrufen` am
        Unternehmens-Bericht, `bewertung.ansehen` an der energetischen Bewertung (Lesen getrennt vom Freigeben,
        AP-19 IP-11) (E12 G1, durchgesetzt über uems/BerichtRechte); fremder Kundenbereich oder
        fremder Standort 404 `nicht_gefunden`, fehlendes Recht 403 `recht_fehlt` - die Unterstützung
        bekommt nie Entwurf oder Stand. Erst der Stand (404 `stand_gibt_es_nicht`, Prüfsumme geprüft -
        500 `abzug_beschaedigt`), dann der Entwurf nach D4 wie `…/entwurf`; die Abweichungen spricht die
        Regel `abweichungen` (bericht.md §8). Zahlen als Dezimaltext, ungerundet.
      responses:
        "200":
          description: Die Abweichungen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BerichtVergleich" }
        "400": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "404": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "500": { $ref: "#/components/responses/BerichtAbgelehnt" }
  /api/v1/berichte/{kennung}/freigeben:
    parameters:
      - { name: kennung, in: path, required: true, schema: { type: string, pattern: "^BR-[0-9]{4}-[0-9]{4,}$" }, description: "Die Kennung des Berichts (BR-<Jahr>-<Nr.>); eine, die keine ist, ist 404 `nicht_gefunden`." }
    post:
      tags: [berichte]
      summary: Gibt den gesehenen Entwurf als Berichtsstand frei - eine byte-gleiche Kopie mit Prüfsumme (F1–F5).
      description: >
        Recht `bericht.standort_freigeben` am Standort-Bericht, `bericht.unternehmen` am
        Unternehmens-Bericht (E12 G1, durchgesetzt über uems/BerichtRechte); fremder Kundenbereich oder
        fremder Standort 404 `nicht_gefunden`, fehlendes Recht 403 `recht_fehlt`. Das Recht prüft VOR
        den Voraussetzungen. In EINER Transaktion: Bericht gesperrt, Entwurf `FOR SHARE`; dieselbe
        Freigabe (Bericht, Datenstand) noch einmal ist 200 mit demselben Stand (F5). F1 in dieser
        Reihenfolge: Zeitraum zu Ende (422 `zeitraum_nicht_zu_ende`, `moeglich_ab`) → jeder Wert
        endgültig (422 `werte_vorlaeufig`) → der gesehene Entwurf ist der gespeicherte und seit seinem
        Datenstand hat sich nichts geändert (D2, D3; 409 `entwurf_veraltet`). Dann wird nichts neu
        gerechnet: der Abzug wird Byte für Byte nach `bericht_stand` kopiert (Nr. = letzte + 1), die
        Quellen eingefroren, am Vorgänger „ersetzt durch“ gesetzt, offene Anstöße erledigt (der erste
        ist der Anlass), Ereignis `bericht_freigegeben` und Protokoll geschrieben. Kein
        Vier-Augen-Prinzip (F4); eine Freigabe wird nie zurückgenommen (F6).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BerichtFreigeben" }
            example:
              entwurf_datenstand: "2026-11-10T07:55:00Z"
      responses:
        "201":
          description: Berichtsstand Nr. n - neu.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BerichtStand" }
        "200":
          description: Dieselbe Freigabe noch einmal (F5) - derselbe Stand, nichts geschrieben.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BerichtStand" }
        "400": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "404": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "409": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "422": { $ref: "#/components/responses/BerichtAbgelehnt" }
  /api/v1/berichte/{kennung}/staende/{nr}:
    parameters:
      - { name: kennung, in: path, required: true, schema: { type: string, pattern: "^BR-[0-9]{4}-[0-9]{4,}$" }, description: "Die Kennung des Berichts (BR-<Jahr>-<Nr.>); eine, die keine ist, ist 404 `nicht_gefunden`." }
      - { name: nr, in: path, required: true, schema: { type: integer, minimum: 1 }, description: "Die Nr. des Berichtsstands." }
    get:
      tags: [berichte]
      summary: Ein freigegebener Berichtsstand - seine Prüfsumme wird geprüft, nicht nur mitgeliefert.
      description: >
        Recht `bericht.standort_abrufen` am Standort-Bericht, `bericht.unternehmen_abrufen` am
        Unternehmens-Bericht, `bewertung.ansehen` an der energetischen Bewertung (Lesen getrennt vom Freigeben,
        AP-19 IP-11) (E12 G1, durchgesetzt über uems/BerichtRechte); fremder Kundenbereich oder
        fremder Standort 404 `nicht_gefunden`, fehlendes Recht 403 `recht_fehlt` - die Unterstützung
        bekommt nie Entwurf oder Stand. Stimmt die Prüfsumme nicht mehr zum gespeicherten Abzug,
        verlässt er den Server nie: 500 `abzug_beschaedigt`. Ein ersetzter Stand bleibt lesbar
        (`ersetzt_durch_nr`). Das Lesen ist kein Abruf einer Datei (PDF/CSV: AP-12 IP-10/IP-11).
      responses:
        "200":
          description: Der Berichtsstand.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BerichtStand" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "404": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "500": { $ref: "#/components/responses/BerichtAbgelehnt" }
  /api/v1/berichte/{kennung}/staende/{nr}/csv:
    parameters:
      - { name: kennung, in: path, required: true, schema: { type: string, pattern: "^BR-[0-9]{4}-[0-9]{4,}$" }, description: "Die Kennung des Berichts (BR-<Jahr>-<Nr.>); eine, die keine ist, ist 404 `nicht_gefunden`." }
      - { name: nr, in: path, required: true, schema: { type: integer, minimum: 1 }, description: "Die Nr. des Berichtsstands." }
    get:
      tags: [berichte]
      summary: Der Berichts-CSV eines Stands in Kundenform - nur aus dem Abzug, jeder Abruf protokolliert.
      description: >
        Recht `export.standort` am Standort-Bericht, `export.unternehmen` am Unternehmens-Bericht
        (E12 G1, durchgesetzt über uems/BerichtRechte); fremder Kundenbereich oder fremder Standort
        404 `nicht_gefunden`, fehlendes Recht 403 `recht_fehlt` - die Unterstützung bekommt nie eine
        Datei. Form (E11 DA3): UTF-8 mit BOM, Zeilenende CRLF, 16 Kopfzeilen `# schlüssel=wert`
        (Bericht, Vorlage, Geltung, Zeitraum, Stand, Datenstand, Freigabe am/von, Zeitzone, Dezimal,
        Trenner, Zahlen ungerundet, Prüfsumme, erzeugt am/von, Teilansicht), bei einem ersetzten Stand
        direkt dahinter `# wasserzeichen=ersetzt durch Nr. n (Datum)`, dann die 13 Spalten und je
        Abschnitt `# abschnitt=<Schlüssel der Vorlage>` eine Zeile je Wert (Standort
        verbrauch_je_messstelle, kennzahlen; Unternehmen standorte, kostenstellen, kennzahlen).
        Die Datei entsteht NUR aus dem Abzug (A1); jeder Abruf schreibt `bericht_abruf` und die
        Meldung `bericht_abgerufen` (DA5) - scheitert das, geht keine Datei hinaus. Ein Entwurf hat
        keine Datei (EW4); stimmt die Prüfsumme nicht, 500 `abzug_beschaedigt`.
      responses:
        "200":
          description: Der Berichts-CSV (Content-Disposition attachment, filename bericht-<Kennung>-nr<Nr.>.csv).
          content:
            "text/csv":
              schema: { type: string }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "404": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "500": { $ref: "#/components/responses/BerichtAbgelehnt" }
  /api/v1/berichte/{kennung}/staende/{nr}/pdf:
    parameters:
      - { name: kennung, in: path, required: true, schema: { type: string, pattern: "^BR-[0-9]{4}-[0-9]{4,}$" }, description: "Die Kennung des Berichts (BR-<Jahr>-<Nr.>); eine, die keine ist, ist 404 `nicht_gefunden`." }
      - { name: nr, in: path, required: true, schema: { type: integer, minimum: 1 }, description: "Die Nr. des Berichtsstands." }
    get:
      tags: [berichte]
      summary: Das PDF eines Stands - server-seitig nur aus dem Abzug, byte-gleich bei jedem Abruf, jeder Abruf protokolliert.
      description: >
        Recht `bericht.standort_abrufen` am Standort-Bericht, `bericht.unternehmen_abrufen` am
        Unternehmens-Bericht, `bewertung.ansehen` an der energetischen Bewertung (Lesen getrennt vom Freigeben,
        AP-19 IP-11) (E12 G1: das PDF folgt dem Abrufen, durchgesetzt über uems/BerichtRechte);
        fremder Kundenbereich oder fremder Standort 404 `nicht_gefunden`, fehlendes Recht 403
        `recht_fehlt` - die Unterstützung bekommt nie eine Datei. Inhalt (E11 DA2): Kopf,
        Zusammenfassung, Tabellen, Kennzahlen, Qualität, Quellenverzeichnis, auf jeder Seite der Fuß
        mit Bericht, Datenstand, Berichtsstand Nr., Freigabe und Prüfsumme; ein ersetzter Stand trägt
        auf jeder Seite das Wasserzeichen „ersetzt durch Nr. n (Datum)“. Deterministisch: Datum im
        Dokument = Freigabe, Dokument-Kennung aus der Prüfsumme, eingebettete Schrift - zwei Abrufe
        desselben Stands sind byte-gleich, auch durch verschiedene Personen (Abrufer und Teilansicht
        stehen nur im Protokoll). Die Datei entsteht NUR aus dem Abzug (A1); jeder Abruf schreibt
        `bericht_abruf` und die Meldung `bericht_abgerufen` mit Format `pdf` (DA5) - scheitert das,
        geht keine Datei hinaus. Ein Entwurf hat keine Datei (EW4); stimmt die Prüfsumme nicht, 500
        `abzug_beschaedigt`.
      responses:
        "200":
          description: Das PDF (Content-Disposition attachment, filename bericht-<Kennung>-nr<Nr.>.pdf).
          content:
            "application/pdf":
              schema: { type: string, format: binary }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "404": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "500": { $ref: "#/components/responses/BerichtAbgelehnt" }
  /api/v1/berichte/{kennung}/anstoesse/{id}/verwerfen:
    parameters:
      - { name: kennung, in: path, required: true, schema: { type: string, pattern: "^BR-[0-9]{4}-[0-9]{4,}$" }, description: "Die Kennung des Berichts (BR-<Jahr>-<Nr.>); eine, die keine ist, ist 404 `nicht_gefunden`." }
      - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: "Der Revisions-Anstoß an einem Stand dieses Berichts." }
    post:
      tags: [berichte]
      summary: Verwirft einen offenen Revisions-Anstoß mit Begründung - der Stand bleibt der gültige (R4).
      description: >
        Recht `bericht.standort_freigeben` am Standort-Bericht, `bericht.unternehmen` am
        Unternehmens-Bericht (E12 G1, durchgesetzt über uems/BerichtRechte); fremder Kundenbereich oder
        fremder Standort 404 `nicht_gefunden`, fehlendes Recht 403 `recht_fehlt`. Reihenfolge: Anstoß
        dieses Berichts (404) → Begründung 10 bis 500 Zeichen (422 `begruendung_fehlt`) → offen (409
        `anstoss_nicht_offen`). Verwerfen löscht nichts; der Vermerk wird „Anstoß verworfen (…)“.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BerichtAnstossVerwerfen" }
            example:
              begruendung: "Korrektur betrifft nur den 31.10. nach Betriebsschluss, Bericht bleibt"
      responses:
        "200":
          description: Verworfen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BerichtAnstoss" }
        "400": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "404": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "409": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "422": { $ref: "#/components/responses/BerichtAbgelehnt" }
  /api/v1/berichte/{kennung}/archivieren:
    parameters:
      - { name: kennung, in: path, required: true, schema: { type: string, pattern: "^BR-[0-9]{4}-[0-9]{4,}$" }, description: "Die Kennung des Berichts (BR-<Jahr>-<Nr.>); eine, die keine ist, ist 404 `nicht_gefunden`." }
    post:
      tags: [berichte]
      summary: Archiviert einen Bericht - er verschwindet aus der Liste, seine Stände bleiben lesbar (V4).
      description: >
        Recht `bericht.standort_freigeben` am Standort-Bericht, `bericht.unternehmen` am
        Unternehmens-Bericht (E12 G1, durchgesetzt über uems/BerichtRechte); fremder Kundenbereich oder
        fremder Standort 404 `nicht_gefunden`, fehlendes Recht 403 `recht_fehlt`. Archivieren folgt dem
        Freigabe-Recht. Kein Inhalt oder `{}`; jedes Feld ist 400. Ein zweites Mal ändert nichts
        (dasselbe `archiviert_am`, kein Protokoll).
      responses:
        "200":
          description: Der archivierte Bericht.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Bericht" }
        "400": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BerichtAbgelehnt" }
        "404": { $ref: "#/components/responses/BerichtAbgelehnt" }
  /api/v1/bezugsflaechen:
    get:
      tags: [bezugsgroessen]
      summary: Die Bezugsflächen aus der Ortsstruktur als Bezugsgrößen - Wert je Periode am Stichtag (E17, S3).
      description: >
        Recht `messwerte.ansehen`. GELESEN aus `flaeche_gueltigkeit` (AP-02), nie kopiert: jedes
        Objekt mit eigener Fläche und jeder Standort, an dem ein Gebäude mit Fläche hing (dann
        „aus Gebäuden summiert“). Je Periode, die den Zeitraum berührt, der Wert am Stichtag =
        dem LETZTEN Tag der Periode; ein Übergang nach dem ersten Tag der Periode steht als
        Kennzeichen („Fläche geändert am 01.01.2027 (3.100 → 3.400 m²)“), gelesen wird nur der
        Stichtag. Ohne gültige Fläche am Stichtag ist `betrag` null, nie 0. Diese Route hat
        KEINEN Schreibweg (405): geändert wird eine Fläche über `PUT /api/v1/orte/{id}/flaeche`.
      parameters:
        - { name: periode_art, in: query, required: true, schema: { type: string, enum: [tag, woche, monat, jahr] } }
        - { name: von, in: query, required: true, schema: { type: string, format: date }, description: "Erster Tag (einschließlich)." }
        - { name: bis, in: query, required: true, schema: { type: string, format: date }, description: "Letzter Tag (EINSCHLIESSLICH)." }
      responses:
        "200":
          description: Die Bezugsflächen mit ihren Werten.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Bezugsflaechen" }
        "400": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/bezugsbasis-methoden:
    get:
      tags: [kennzahlen]
      summary: Der Methoden-Katalog der Bezugsbasis - vier Methoden der Bereinigung mit Datenbedarf, Grenze und Startwerten.
      description: >
        Keine eigene Rechte-Kennung (Muster Kennzahl-Vorlagen, AP-11 E9 - kein Kundenobjekt, kein Mandant):
        jede angemeldete Person liest denselben Katalog. UEMS AP-17 IP-5 (§4.5, E4 = A). Die Antwort ist die
        Datei docs/contracts/v2/bezugsbasis-methoden.json Byte für Byte (API-Ressource
        bezugsbasis/bezugsbasis-methoden.json, Portal-Kopie frontend/portal/src/bezugsbasis/); ihre Form ist
        docs/contracts/v2/bezugsbasis-methoden.schema.json. Vier Methoden - verhaeltnis,
        regression_eine_variable, regression_zwei_variablen, gradtage - mit Formel, Variablen, Datenbedarf,
        Mindestumfang, Grenze und Kennzeichen, dazu die Startwerte ohne Norm-Herleitung (G6). Gerechnet wird
        hier nichts; die Methode einer Fassung ist eingefroren (M4).
      responses:
        "200":
          description: Der Methoden-Katalog.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsbasisMethoden" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/kennzahlen/{id}/bezugsbasen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [kennzahlen]
      summary: Alle Bezugsbasen der Kennzahl in der Form der Einzel-Route, die laufende zuerst (AP-17 IP-8).
      description: >
        Recht `bezugsbasis.ansehen`; Sichtbarkeit über die Kennzahl (außerhalb 404). Das Kennzeichen der laufenden Basis
        mit Fassung und Freigabe-Zustand steht zusätzlich am Register-Eintrag (`Kennzahl.bezugsbasis`, B3). Vertrag
        bezugsbasis.md §13.
      responses:
        "200":
          description: Die Bezugsbasen der Kennzahl.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsbasisListe" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
    post:
      tags: [kennzahlen]
      summary: Legt die Bezugsbasis BB-… einer Kennzahl an (AP-17 IP-7, B1, B2, B4).
      description: >
        Recht `bezugsbasis.verwalten` an der Geltung der Kennzahl (Standort-Zaun über die Kennzahl). Höchstens eine
        laufende Bezugsbasis je Kennzahl; der Verantwortliche ist der der Kennzahl. Vertrag bezugsbasis.md §13.
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BezugsbasisAnlegen" }
      responses:
        "201":
          description: Die angelegte Bezugsbasis (noch ohne Fassung).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Bezugsbasis" }
        "400": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "404": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "409": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "422": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
  /api/v1/kennzahlen/{id}/bezugsbasen/{bid}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: bid, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [kennzahlen]
      summary: Die Bezugsbasis mit ihren Fassungen.
      description: Recht `bezugsbasis.ansehen`; Sichtbarkeit über die Kennzahl (außerhalb 404).
      responses:
        "200":
          description: Die Bezugsbasis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Bezugsbasis" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
  /api/v1/kennzahlen/{id}/bezugsbasen/{bid}/fassungen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: bid, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Bildet eine Fassung als Entwurf mit Vorschau - Grundlage als Kopie mit Prüfsumme, Basiswert Σ ÷ Σ oder Modell.
      description: >
        Recht `bezugsbasis.verwalten` an der Geltung der Kennzahl. Referenzperiode JJJJ-MM/JJJJ-MM aus abgeschlossenen
        Monaten (P1/P3). Methoden `verhaeltnis`, `regression_eine_variable`, `regression_zwei_variablen`, `gradtage`
        (IP-10): ein Modell friert Koeffizienten, R², Streuung und Spannweite je Variable ein; 422 `zu_wenig_perioden`
        (unter 12 Monaten, G1), `variable_fehlt` (G2), `variable_keine_gradtagzahl`, `zweite_variable_fehlt`,
        `variable_unbekannt`, `modell_ohne_nenner`; eine abhängige zweite Variable (G4) wird nicht aufgenommen und steht
        in `abgelehnte_variablen` und im Protokoll `variable_abgelehnt`. Ein offener
        Entwurf wird neu gebildet. Die Antwort ist die gespeicherte Fassung (freigabe_status `entwurf`), byte-gleich
        zu GET …/fassungen/{n}. Statische Faktoren `faktoren` nur aus dem Faktoren-Vorschlag am
        Bildungstag (sonst 422 `faktor_unbekannt`), Wert als Kopie (§17). Vertrag bezugsbasis.md §13, §17.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BezugsbasisEntwurf" }
      responses:
        "200":
          description: Die Fassung als Vorschau.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsbasisFassung" }
        "400": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "404": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "409": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "422": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
  /api/v1/kennzahlen/{id}/bezugsbasen/{bid}/fassungen/{n}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: bid, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: n, in: path, required: true, schema: { type: integer, minimum: 1 } }
    get:
      tags: [kennzahlen]
      summary: Die gespeicherte Fassung - byte-gleich zur Vorschau ihres Entwurfs.
      description: Recht `bezugsbasis.ansehen`; Sichtbarkeit über die Kennzahl (außerhalb 404).
      responses:
        "200":
          description: Die Fassung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsbasisFassung" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
  /api/v1/kennzahlen/{id}/bezugsbasen/{bid}/bleibt:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: bid, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Geprüft, bleibt - bestätigt die laufende Fassung einer Bezugsbasis mit Begründung und setzt die Wiedervorlage neu.
      description: >
        Recht `bezugsbasis.verwalten` am Geltungsbereich der Kennzahl (UEMS AP-17 IP-17, F5, R13; Vertrag
        docs/contracts/v2/bezugsbasis.md §15). Ein Protokolleintrag
        `gueltig_bleibt` mit `begruendung` (10–500 Zeichen, sonst 422 `begruendung_fehlt`); die Fassung bleibt
        byte-gleich, die Frist beginnt heute neu (heute + `wiedervorlage_monate`). Offene Anstöße der laufenden
        Fassung gelten als beantwortet (`bleibt`). Kennzahl nicht sichtbar 404; Recht fehlt 403 `recht_fehlt`;
        Basis beendet 409 `bezugsbasis_beendet`; ohne freigegebene Fassung 409 `keine_freigegebene_fassung`;
        unbekanntes Feld 400 `anfrage_ungueltig`. Kein Läufer, keine Nachricht.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [begruendung]
              properties:
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        "200":
          description: Bestätigt - der Zustand mit der neuen Frist.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsbasisZustand" }
        "400": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "404": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "409": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "422": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
  /api/v1/kennzahlen/{id}/bezugsbasen/{bid}/beenden:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: bid, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Beendet eine Bezugsbasis mit Tag, Grund und Begründung - nie gelöscht.
      description: >
        Recht `bezugsbasis.verwalten` am Geltungsbereich der Kennzahl (UEMS AP-17 IP-17, F4). `tag` ist der
        letzte eingeschlossene Tag (JJJJ-MM-TT, sonst 400); ein Tag vor heute nur mit `rueckwirkend: true`
        (sonst 422 `rueckwirkend_fehlt`), nie vor dem ersten Tag der laufenden Fassung (422 `tag_vor_fassung`).
        `grund` aus dem Vokabular `anpassungsgrund` (A1; sonst 422 `grund_unbekannt`), `begruendung` 10–500
        Zeichen (422 `begruendung_fehlt`). Die laufende Fassung endet mit (`gilt_bis`), offene Anstöße gelten
        als beantwortet (`beendet`), ein Protokolleintrag `bezugsbasis_beendet`. Schon beendet 409
        `bezugsbasis_beendet`. Danach darf die Kennzahl eine neue Bezugsbasis bekommen (B1). Die Archivierung
        der Kennzahl beendet die laufende Basis am selben Tag mit Grund `nicht_mehr_anwendbar`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [tag, grund, begruendung]
              properties:
                tag: { type: string, format: date }
                grund:
                  type: string
                  enum: [referenzperiode_vervollstaendigt, grundlage_korrigiert, struktur_geaendert, variable_geaendert, methode_geaendert, nicht_mehr_anwendbar, sonstiger]
                begruendung: { type: string, minLength: 10, maxLength: 500 }
                rueckwirkend: { type: boolean, default: false }
      responses:
        "200":
          description: Beendet.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsbasisZustand" }
        "400": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "404": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "409": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "422": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
  /api/v1/energiemanagement/personen:
    get:
      tags: [energiemanagement]
      summary: Personen im Energiemanagement - aktive zuerst, beendete danach.
      description: >
        Recht `energiemanagement.ansehen` (UEMS AP-19 IP-6, PA1). Personen tragen nur den Mandanten-Zaun: auch ein
        Standort-Leser braucht den Namen hinter „entschieden von“. `konto` ist `null` für eine Person ohne Konto (die
        Leitung ohne Login, eine externe Auditorin).
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementPersonen" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
    post:
      tags: [energiemanagement]
      summary: Person erfassen, auch ohne Konto (PA1).
      description: >
        Recht `energiemanagement.verwalten` am Unternehmen (403 `recht_fehlt`). Name und Funktion Pflicht (422
        `name_fehlt`, `funktion_fehlt`, `angabe_ungueltig` über 200 Zeichen bzw. ein Kürzel über 10); das Konto ist
        eines des Kundenbereichs (422 `konto_unbekannt`) und gehört höchstens einer Person (409 `konto_vergeben`),
        ebenso das Kürzel (409 `kuerzel_vergeben`). Protokoll `person_erfasst`. Eine Löschroute gibt es nicht (PA5).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergiemanagementPersonAnlegen" }
      responses:
        "201":
          description: Die Person mit ihrem Verlauf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementPersonMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/personen/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [energiemanagement]
      summary: Eine Person mit ihrem Verlauf.
      description: >
        Recht `energiemanagement.ansehen`. Der Verlauf nennt jeden Übergang (`person_erfasst`, `person_geaendert`,
        `person_beendet`) mit alt, neu, Begründung, Akteur und Zeit - so steht auch jede Konto-Verknüpfung darin.
        Unbekannt 404.
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementPersonMitVerlauf" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
    put:
      tags: [energiemanagement]
      summary: Person ändern, mit einem Konto verknüpfen oder mit „bis“ beenden.
      description: >
        Recht `energiemanagement.verwalten` am Unternehmen. Der ganze änderbare Stand (Name, Funktion, Kürzel,
        Organisation, Konto, seit) - Protokoll `person_geaendert` mit alt und neu. Ein anderes Konto (verknüpfen,
        wechseln, lösen) braucht eine Begründung mit 10 bis 500 Zeichen (422 `begruendung_fehlt`). `bis` beendet die
        Person endgültig - Begründung Pflicht, `bis` nicht vor `seit` (422 `zeitraum_ungueltig`), und nur, wenn sie
        danach keine Aufgabe mehr trägt, auch nicht als Vertretung (409 `aufgaben_laufen` mit den `zuordnungen`);
        Protokoll `person_beendet`. Eine beendete Person 409 `person_beendet`. Gelöscht wird eine Person nie (PA5).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergiemanagementPersonAendern" }
      responses:
        "200":
          description: Die Person mit ihrem Verlauf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementPersonMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/aufgaben:
    get:
      tags: [energiemanagement]
      summary: Aufgaben im Energiemanagement am Tag, mit der Leitung (PA2, PA3).
      description: >
        Recht `energiemanagement.ansehen`, nur unternehmensweit - wer nur Standorte liest, bekommt keine Zeile,
        ohne Hinweis und nie den Satz. `tag` (Vorgabe heute in der Zeitzone des Unternehmens, sonst 400): je Wort
        des Vokabulars `aufgabe` die laufenden Zuordnungen (gilt ab ≤ tag ≤ gilt bis, der letzte Tag zählt mit);
        ohne Person der Satz „… — keine Person festgelegt.“ aus dem Vertrag (nie für `weitere`), ein Satz, keine
        Warnung. `leitung` sind die Personen mit laufender Aufgabe `unternehmensleitung` - leer heißt, Freigaben mit
        Leitungs-Pflicht sind gesperrt. `zuordnungen` nennt alle, auch beendete und künftige.
      parameters:
        - in: query
          name: tag
          schema: { type: string, format: date }
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementAufgaben" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
    post:
      tags: [energiemanagement]
      summary: Aufgabe zuordnen - Aufgabe × Person × gilt ab, mit „entschieden von“ (PA2).
      description: >
        Recht `energiemanagement.verwalten` am Unternehmen. `aufgabe` aus dem Vokabular (422 `aufgabe_unbekannt`),
        `weitere` mit Wortlaut (422 `wortlaut_fehlt`, ein Wortlaut an einer anderen Aufgabe `angabe_ungueltig`);
        Person (422 `person_fehlt`, `person_unbekannt`), gilt ab (422 `gilt_ab_fehlt`), Begründung 10 bis 500
        Zeichen (422 `begruendung_fehlt`). „entschieden von“ ist eine Person im Energiemanagement und Pflicht außer
        bei `unternehmensleitung` (422 `entschieden_von_fehlt`). Wahlfrei Vertretung (eine andere Person, 422
        `vertretung_gleich_person`), Beleg als Verweis mit Ablage (422 `beleg_ungueltig`) und Beschluss
        `BR-JJJJ-nnnn/Bn` (422 `beschluss_ungueltig`) einer freigegebenen Managementbewertung (MG6: unbekannt 422
        `beschluss_unbekannt`, ohne Stand 422 `managementbewertung_nicht_freigegeben`). Eine Person, deren `bis` vor gilt ab liegt, 422
        `person_beendet`; dieselbe Aufgabe für dieselbe Person im selben Zeitraum 409 `zuordnung_laeuft_bereits`.
        Nur anhängen - Protokoll `aufgabe_zugeordnet`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergiemanagementAufgabeZuordnen" }
      responses:
        "201":
          description: Die Zuordnung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementZuordnung" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/aufgaben/{id}/beenden:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    post:
      tags: [energiemanagement]
      summary: Zuordnung beenden - einmal, der letzte Tag zählt mit.
      description: >
        Recht `energiemanagement.verwalten` am Unternehmen. `gilt_bis` Pflicht (422 `gilt_bis_fehlt`), nicht vor
        gilt ab (422 `zeitraum_ungueltig`), Begründung 10 bis 500 Zeichen (422 `begruendung_fehlt`); schon beendet 409
        `aufgabe_beendet`. Eine Übergabe ist danach eine neue Zuordnung ab dem Folgetag. Protokoll `aufgabe_beendet`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergiemanagementAufgabeBeenden" }
      responses:
        "200":
          description: Die beendete Zuordnung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementZuordnung" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/verantwortung:
    get:
      tags: [energiemanagement]
      summary: Wer ist wofür verantwortlich - Aufgaben, Verantwortliche der Objekte, Freigaben der Bezugsbasen (PA2, PA4).
      description: >
        Recht `energiemanagement.ansehen`. Ein Leser, kein Speicher (UEMS AP-19 IP-10). `aufgaben`, `leitung` und
        `tag` wie `GET …/aufgaben` (nur unternehmensweit; wer nur Standorte liest, bekommt keine Aufgabe und nie den
        Satz), `ohne_person` die Aufgaben des Vokabulars ohne laufende Zuordnung am Tag. `objekte` nennt die
        Verantwortlichen so, wie die Register der Objekte sie heute zeigen - jede Quelle in ihrem eigenen Zaun und mit
        ihren Rechten gelesen, nichts kopiert: laufende Kennzahlen, Energieeinsätze und Bezugsbasen (der
        Verantwortliche der Bezugsbasis ist die Vorgabe aus der Kennzahl, AP-17 B4), Energieziele, Maßnahmen und
        Abweichungen in jedem Zustand, interne Audits (IP-18) in jedem Zustand mit ihrem Verantwortlichen (Konto).
        `bezugsbasen_freigaben` nennt jede freigegebene Fassung mit der Person, die sie
        freigegeben hat, auch abgelöste. Der Leser urteilt nicht, ob die Verteilung genügt; Verantwortung verleiht
        kein Recht.
      parameters:
        - in: query
          name: tag
          schema: { type: string, format: date }
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementVerantwortung" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/energiemanagement/verzeichnis:
    get:
      tags: [energiemanagement]
      summary: Verzeichnis - jede Entscheidung und jeder Nachweis mit Person, Tag, Fassung oder Nr., Prüfsumme und Ort; auch als CSV (VZ1-VZ4, KS2).
      description: >
        Recht `energiemanagement.ansehen` (UEMS AP-19 IP-8). Ein Leser, kein Speicher: er sammelt die Zeilen aller
        Verzeichnis-Quellen - Dokument-Fassungen und Bekanntmachungen (IP-7), laufende Aufgaben (IP-10) und die Stände,
        Fassungen und Belege von AP-11/12/16/17/18 - jede über ihren Dienst, im Zaun und mit den Rechten des Aufrufers
        gelesen, nichts kopiert; was der Aufrufer dort nicht sieht, steht hier nicht, ohne Hinweis. Stichtag ist der
        Abruf; eine Zeile mit späterem Tag ist noch nicht festgehalten. `gruppe` zeigt nur diese Gruppe, `von`/`bis`
        filtern den Tag (beide eingeschlossen; eine Zeile ohne Tag fällt dann heraus), `person` (ID einer Person im
        Energiemanagement) zeigt die Zeilen in ihrem Namen - sie ist „entschieden von“ oder, wo niemand anderes
        entschieden hat, „eingetragen von“. Eine Gruppe ohne Zeile trägt `satz` „Hier ist noch nichts festgehalten.“
        und ihre Zeilen des Zuschnitts - nie „fehlt“, keine Zahl über das Ganze. `format=csv` liefert dieselben Zeilen
        als Datei (UTF-8 mit BOM, CRLF, Trenner `;`): Kopfzeilen `# Stichtag TT.MM.JJJJ HH:MM`, `# <Verantwortungs-Satz>`
        und die wirksamen Filter, dann die Spalten `Gruppe;Art;Kennzeichen;Titel;Fassung oder Nr.;entschieden von;eingetragen
        von;Tag;Prüfsumme;Ort` und eine Zeile je Nachweis. Dasselbe Recht wie die Seite, nicht eigens protokolliert.
      parameters:
        - in: query
          name: gruppe
          schema: { type: string, enum: [grundlagen, verantwortung, risiken_chancen, kompetenz_kommunikation, betrieb_auslegung_beschaffung, bewertung_messplanung, kennzahlen_bezugsbasen, ziele_massnahmen_abweichungen, audits_feststellungen, managementbewertung, berichte] }
        - in: query
          name: von
          schema: { type: string, format: date }
        - in: query
          name: bis
          schema: { type: string, format: date }
        - in: query
          name: person
          schema: { type: string, format: uuid }
        - in: query
          name: format
          schema: { type: string, enum: [json, csv] }
      responses:
        "200":
          description: Ergebnis - JSON oder mit `format=csv` die Datei.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementVerzeichnis" }
            text/csv:
              schema: { type: string }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/wiedervorlage:
    get:
      tags: [energiemanagement]
      summary: Wiedervorlage - was fällig ist und was in den nächsten Tagen fällig wird, über alle Objekte; auch als Kalender-Abzug (WV1-WV4, E10).
      description: >
        Recht `energiemanagement.ansehen` (UEMS AP-19 IP-21). Ein Leser, kein Speicher, kein Läufer, keine Nachricht: er
        sammelt die Fristen aller Wiedervorlage-Quellen - Dokument-Überprüfung (DK5), nächstes internes Audit (IA4),
        offene Feststellungen (FS1), die Überprüfung der energetischen Bewertung (AP-16 S5), der laufenden Bezugsbasen
        (AP-17 F5), offene Revisions-Anstöße der Berichte (AP-12 E7), die fälligen Vorgänge des Übersichts-Lesers
        „Ziele und Maßnahmen“ (AP-18) samt der noch nicht fälligen offenen Vorgänge und die Fristen offener Messbedarfe -
        jede über ihren Dienst, im Zaun und mit den Rechten des Aufrufers gelesen; keine Frist wird nachgerechnet (WV2).
        Abruf ist der Tag in der Zeitzone des Unternehmens. `faellig` (Abruf ≥ fällig am, 0 Tage = „heute fällig“) am
        längsten fällig zuerst, bei gleichem Tag nach Kennzeichen; `vorschau` die Fristen in den nächsten
        `vorschau_tage` Tagen (Einstellung, Startwert 30; genau 30 ist Vorschau); `nicht_in_liste` die Kennzeichen mit
        späterer Frist; `naechste_managementbewertung` die Frist von MG7 mit Herkunft, auch außerhalb des Fensters.
        `format=ics` liefert dieselben Zeilen (fällig und Vorschau) als Kalender-Abzug nach RFC 5545 -
        je Zeile ein ganztägiger Termin am Tag, an dem sie fällig ist bzw. wird, Titel „Kennzeichen: Titel“,
        Beschreibung und Kalender mit dem Vermerk „Stand vom TT.MM.JJJJ aus VoltPilot; maßgeblich ist die Wiedervorlage
        im Portal.“ Dasselbe Recht wie die Seite, nicht eigens protokolliert; nichts wird verschickt.
      parameters:
        - in: query
          name: format
          schema: { type: string, enum: [json, ics] }
      responses:
        "200":
          description: Ergebnis - JSON oder mit `format=ics` der Kalender-Abzug.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementWiedervorlage" }
            text/calendar:
              schema: { type: string }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/energiemanagement/energieeinsaetze/{id}/nachweise:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [energiemanagement]
      summary: Der Abschnitt „Nachweise“ am Energieeinsatz - Dokumente mit Ort und Überprüfung (R7).
      description: >
        Recht `energiemanagement.ansehen` (UEMS AP-19 IP-14, DK1, KS1). Die Dokumente, deren Bezug dieser Einsatz ist
        (Betrieb und Instandhaltung, Auslegung, Beschaffung ...), je mit dem Ort der gültigen Fassung - beim Verweis
        „Geführt in Ihrem System: <Ablage>“, Kennung, Fassungsangabe und die im Browser gebildete Prüfsumme; der Inhalt
        liegt beim Kunden, VoltPilot öffnet, prüft und lädt nichts -, der Überprüfung beim Abruf und den
        Bekanntmachungen. Ein Leser über `EnergiemanagementDokumentService`: der Zaun folgt dem Standort des Einsatzes;
        ein Einsatz, den die Anfrage nicht sieht, ist 404.
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementNachweiseAmEinsatz" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/personen/{id}/nachweise:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [energiemanagement]
      summary: Die Nachweise an einer Person und an ihren Aufgaben (R8).
      description: >
        Recht `energiemanagement.ansehen` (UEMS AP-19 IP-14, DK1, DK5). Die Dokumente am Bezug `person` und an den
        Aufgaben (Zuordnungen) dieser Person - Kompetenz als Verweis ins Personal- oder Schulungssystem, ohne
        Überprüfung. Dokumente an Person und Aufgabe haben keinen Standort: wer nur Standorte liest, bekommt die Person
        mit leerer Liste. Unbekannte Person 404.
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementNachweiseDerPerson" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/bekanntmachungen:
    get:
      tags: [energiemanagement]
      summary: Die Bekanntmachungen als Liste der Kommunikationsnachweise (DK6, R8).
      description: >
        Recht `energiemanagement.ansehen` (UEMS AP-19 IP-14). Jede Bekanntmachung eines Dokuments im Zaun, nach Tag und
        Kennzeichen. Die Einträge desselben Dokuments mit derselben Fassung, demselben Tag, demselben Kreis und
        derselben Person sind EINE Mitteilung mit allen Wegen („über Aushang und Intranet“). VoltPilot verschickt
        nichts und fragt kein „gelesen“ ab.
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementKommunikationsnachweise" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/energiemanagement/dokumente:
    get:
      tags: [energiemanagement]
      summary: Dokumente im Energiemanagement - nach Kennzeichen, je mit der Überprüfung beim Abruf.
      description: >
        Recht `energiemanagement.ansehen` (UEMS AP-19 IP-7, DK1). Der Zaun folgt dem Standort des Bezugs: ein Dokument
        am Unternehmen sieht nur, wer unternehmensweit liest; ein Standort-Konto sieht die Dokumente seiner Standorte -
        ohne Hinweis auf die übrigen. `ueberpruefung` ist `null` an einem aufgehobenen Dokument.
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementDokumente" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [energiemanagement]
      summary: Dokument anlegen - Art, Titel, Bezug (DK1).
      description: >
        Recht `energiemanagement.verwalten` am Standort des Bezugs bzw. am Unternehmen (403 `recht_fehlt`). `art` aus
        der geschlossenen Liste (422 `art_unbekannt`), Titel Pflicht (422 `titel_fehlt`, über 200 Zeichen
        `angabe_ungueltig`); Bezug `unternehmen` (ohne Kennung), `standort` (ein Standort des Kundenbereichs im
        eigenen Zaun, sonst 422 `standort_unbekannt`), `energieeinsatz` (ein Einsatz, den die Anfrage sieht, sonst 422
        `energieeinsatz_unbekannt`), `person` (422 `person_unbekannt`) oder `aufgabe` (eine Zuordnung, 422
        `aufgabe_unbekannt`), sonst 422 `bezug_unbekannt`; eine Kennung einer anderen Art 422 `angabe_ungueltig`
        (AP-19 IP-14). Der Zaun des Einsatzes ist sein Standort: der eine Standort, an dem am Tag des Anlegens die
        Messstellen seines Prozesses hängen - an mehreren oder keinem gilt das Dokument am Unternehmen; Person und
        Aufgabe haben keinen Standort (nur unternehmensweit sichtbar). `ueberpruefung_monate` 1-60 nur an Vorgabe-Arten (422
        `angabe_ungueltig`), ohne Angabe die Einstellung (Startwert 12). Das Original als Verweis mit Ablage (422
        `beleg_ungueltig`). Das Dokument entsteht im Entwurf, D-nnnn vergibt der Zähler. Protokoll `dokument_angelegt`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergiemanagementDokumentAnlegen" }
      responses:
        "201":
          description: Das Dokument.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementDokument" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/dokumente/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [energiemanagement]
      summary: Ein Dokument mit Fassungen, Einträgen, Verlauf und der Überprüfung beim Abruf.
      description: >
        Recht `energiemanagement.ansehen`; außerhalb des Zauns 404. Gültig ist die jüngste freigegebene Fassung, die
        früheren tragen `status: abgeloest` (DK4, gelesen, nie gespeichert). `ueberpruefung` nach dem Vertrag
        `ueberpruefung` (DK5) am Abruf-Tag: Basis ist der Tag der Entscheidung der gültigen Fassung bzw. ihr letztes
        „geprüft, bleibt“; Nachweis-Arten ohne Frist (`grund: nachweis`). `saetze` wörtlich aus dem Vertrag:
        Kopf, „Überprüfung fällig seit n Tagen.“ und - bei einer Leitungs-Art ohne Leitung - der Satz, warum die
        Freigabe gesperrt ist.
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementDokument" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/dokumente/{id}/vergleich:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [energiemanagement]
      summary: Anwendungsbereich neben dem Betrachtungsumfang der energetischen Bewertung (DK7, W5).
      description: >
        Recht `energiemanagement.ansehen`; nur am Anwendungsbereich (sonst 404). Liest die gültige Fassung und den am
        Abruf-Tag laufenden Betrachtungsumfang (AP-16 U1, `bewertung_umfang`) und vergleicht Standorte und Träger
        (Vertrag `anwendungsbereich_vergleich`) - ohne Urteil, der Vergleich ändert nichts. `saetze`: deckungsgleich
        oder je Unterschied „… gehört zum Anwendungsbereich, aber nicht zum Betrachtungsumfang …“; die Gegenrichtung
        steht nur in den Feldern. Ohne gültige Fassung oder ohne Betrachtungsumfang `vergleich: null`; den
        Betrachtungsumfang (am Unternehmen) bekommt nur, wer unternehmensweit liest.
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementVergleich" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/dokumente/{id}/fassungen:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    post:
      tags: [energiemanagement]
      summary: Fassung entwerfen - Wortlaut oder Verweis (DK2, G3).
      description: >
        Recht `energiemanagement.verwalten` am Standort des Bezugs bzw. am Unternehmen. `form` `wortlaut` (Pflicht,
        höchstens 20 000 Zeichen, 422 `wortlaut_fehlt`) oder `verweis` (Ablage Pflicht, 422 `ablage_fehlt`; Prüfsumme
        aus dem Browser 64 Zeichen 0-9/a-f) - ganz oder gar nicht (422 `form_unbekannt`, `angabe_ungueltig`); nie eine
        Datei. Der Anwendungsbereich nennt Standorte des Kundenbereichs (422 `standort_unbekannt`), Energieträger aus
        dem Vokabular des Betrachtungsumfangs (422 `traeger_unbekannt`) und Ausschlüsse mit Begründung (422
        `ausschluss_ungueltig`); fehlen sie, 422 `anwendungsbereich_fehlt`, an anderen Arten 422 `angabe_ungueltig`.
        Begründung ab Fassung 2 Pflicht (422 `begruendung_fehlt`), Beschluss `BR-JJJJ-nnnn/Bn` (422
        `beschluss_ungueltig`) einer freigegebenen Managementbewertung (MG6: unbekannt 422 `beschluss_unbekannt`,
        ohne Stand 422 `managementbewertung_nicht_freigegeben`). Ein offener Entwurf wird überschrieben (200, dieselbe Nr.), sonst entsteht Fassung n
        + 1 (201); ein offener Antrag 409 `fassung_beantragt`, ein aufgehobenes Dokument 409 `dokument_aufgehoben`.
        Protokoll `fassung_entworfen`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergiemanagementFassungEntwerfen" }
      responses:
        "200":
          description: Der Entwurf wurde überschrieben.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementDokument" }
        "201":
          description: Eine neue Fassung im Entwurf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementDokument" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/dokumente/{id}/fassungen/{nr}/beantragen:
    parameters:
      - { in: path, name: id, required: true, schema: { type: string, format: uuid } }
      - { in: path, name: nr, required: true, schema: { type: integer, minimum: 1 } }
    post:
      tags: [energiemanagement]
      summary: Freigabe beantragen - nur mit Vier-Augen (DK3).
      description: >
        Recht `energiemanagement.freigeben` am Standort des Bezugs bzw. am Unternehmen. Nur ein Entwurf (sonst 409
        `fassung_<status>`), nur mit Vier-Augen (409 `vieraugen_aus`). „entschieden von“ ist eine Person im
        Energiemanagement (422 `entschieden_von_fehlt`, `person_unbekannt`), `entschieden_am` ein Tag bis heute
        (Vorgabe heute, 422 `tag_in_der_zukunft`), Begründung 10-500 Zeichen (422 `begruendung_fehlt`). Bei
        Energiepolitik, Anwendungsbereich und Bestellung ist es die Person mit der am Tag laufenden Aufgabe „Leitung
        des Unternehmens“ (422 `leitung_fehlt`). Kopie und Prüfsumme entstehen mit dem Antrag. Protokoll
        `fassung_beantragt`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergiemanagementEntscheid" }
      responses:
        "200":
          description: Das Dokument.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementDokument" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/dokumente/{id}/fassungen/{nr}/freigeben:
    parameters:
      - { in: path, name: id, required: true, schema: { type: string, format: uuid } }
      - { in: path, name: nr, required: true, schema: { type: integer, minimum: 1 } }
    post:
      tags: [energiemanagement]
      summary: Fassung freigeben - mit „entschieden von“, bei Vier-Augen durch eine zweite Person (DK3, DK4).
      description: >
        Recht `energiemanagement.freigeben` am Standort des Bezugs bzw. am Unternehmen. Ohne Vier-Augen der Entwurf
        mit „entschieden von“, Tag und Begründung wie beim Antrag (Leitungs-Pflicht 422 `leitung_fehlt`); mit
        Vier-Augen ist ein Entwurf 409 `vieraugen_beantragen`, und den Antrag bestätigt eine zweite Person - nie, wer
        die Fassung entworfen oder beantragt hat (422 `vieraugen_urheber`), mit Rolle Kundenadministrator oder
        Energiemanager (403 `vieraugen_rolle`); sie schickt höchstens eine Begründung (ein „entschieden von“ 422
        `angabe_ungueltig`). Schon entschieden 409 `fassung_<status>`. Die erste Freigabe macht das Dokument gültig
        (`dokument_gueltig`), jede weitere löst die vorige ab (`fassung_abgeloest`). Protokoll `fassung_freigegeben`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergiemanagementEntscheid" }
      responses:
        "200":
          description: Das Dokument.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementDokument" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/dokumente/{id}/fassungen/{nr}/ablehnen:
    parameters:
      - { in: path, name: id, required: true, schema: { type: string, format: uuid } }
      - { in: path, name: nr, required: true, schema: { type: integer, minimum: 1 } }
    post:
      tags: [energiemanagement]
      summary: Antrag ablehnen - die zweite Person, mit Begründung (DK3).
      description: >
        Recht `energiemanagement.freigeben` am Standort des Bezugs bzw. am Unternehmen. Nur ein Antrag (409
        `fassung_<status>`), nur die zweite Person (422 `vieraugen_urheber`, 403 `vieraugen_rolle`), Begründung 10-500
        Zeichen (422 `begruendung_fehlt`). Danach ist ein neuer Entwurf möglich. Protokoll `fassung_abgelehnt`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergiemanagementAblehnen" }
      responses:
        "200":
          description: Das Dokument.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementDokument" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/dokumente/{id}/geprueft:
    parameters:
      - { in: path, name: id, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [energiemanagement]
      summary: „Geprüft, bleibt“ - Überprüfung ohne neue Fassung (DK5).
      description: >
        Recht `energiemanagement.freigeben` am Standort des Bezugs bzw. am Unternehmen. Nur an einem gültigen Dokument
        (409 `dokument_nicht_gueltig`, aufgehoben 409 `dokument_aufgehoben`) einer Vorgabe-Art (422
        `keine_ueberpruefung`); „entschieden von“, Tag bis heute und nicht vor der Freigabe der gültigen Fassung (422
        `tag_in_der_zukunft`, `angabe_ungueltig`), Begründung 10-500 Zeichen, wahlfrei Beschluss `BR-JJJJ-nnnn/Bn`
        (422 `beschluss_ungueltig`) einer freigegebenen Managementbewertung (MG6: unbekannt 422
        `beschluss_unbekannt`, ohne Stand 422 `managementbewertung_nicht_freigegeben`). Die Überprüfung
        rechnet ab diesem Tag neu. Protokoll `geprueft_bleibt`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergiemanagementGeprueft" }
      responses:
        "200":
          description: Das Dokument.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementDokument" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/dokumente/{id}/bekanntmachungen:
    parameters:
      - { in: path, name: id, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [energiemanagement]
      summary: Bekannt gemacht - an wen, am, über welchen Weg (DK6).
      description: >
        Recht `energiemanagement.verwalten` am Standort des Bezugs bzw. am Unternehmen. Nur an der gültigen Fassung
        (409 `dokument_nicht_gueltig`); Kreis Pflicht (422 `kreis_fehlt`), Weg aus dem Vokabular (422
        `weg_unbekannt`), `weiterer` mit Wortlaut (422 `wortlaut_fehlt`), Tag bis heute; die Person ist `person_id`
        oder die Person des eigenen Kontos (422 `person_fehlt`, `person_unbekannt`). Ein Eintrag je Weg. VoltPilot
        verschickt nichts und fragt kein „gelesen“ ab. Protokoll `bekannt_gemacht`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergiemanagementBekanntmachen" }
      responses:
        "201":
          description: Das Dokument mit dem neuen Eintrag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementDokument" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/dokumente/{id}/aufheben:
    parameters:
      - { in: path, name: id, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [energiemanagement]
      summary: Dokument aufheben - Tag und Begründung; es bleibt lesbar (DK8).
      description: >
        Recht `energiemanagement.freigeben` am Standort des Bezugs bzw. am Unternehmen. „entschieden von“, Tag bis
        heute, Begründung 10-500 Zeichen, wahlfrei Beschluss; schon aufgehoben 409 `dokument_aufgehoben`. Das
        Dokument bleibt mit allen Fassungen und Prüfsummen lesbar, eine Löschroute gibt es nicht. Protokoll
        `dokument_aufgehoben`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergiemanagementAufheben" }
      responses:
        "200":
          description: Das Dokument.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiemanagementDokument" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/feststellungen:
    get:
      tags: [energiemanagement]
      summary: Die Feststellungen - offene zuerst, am längsten überfällig oben, dann abgeschlossene (FS1).
      description: >
        Recht `energiemanagement.ansehen` (UEMS AP-19 IP-19). Sichtbar sind die Feststellungen im Zaun (RLS aus IP-16:
        eine Feststellung ohne Standort nur unternehmensweit, eine am Standort, wer ihn sieht). `lage` ist beim Abruf
        abgeleitet, nie gespeichert: die Operation `ueberpruefung` (Art `feststellung`) des Vertrags
        `energiemanagement.md` - Frist, sonst festgestellt am + Frist der Einstellung (Startwert 90 Tage); abgeschlossen
        ohne Frist (`grund` = `abgeschlossen`). `ergebnis` ist das des schließenden Stands. `tag` Vorgabe heute.
      parameters:
        - in: query
          name: tag
          schema: { type: string, format: date }
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FeststellungListe" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [energiemanagement]
      summary: Eine Feststellung erfassen (FS1).
      description: >
        Recht `energiemanagement.verwalten` am Standort des Bezugs, ohne Standort am Unternehmen (403 `recht_fehlt`,
        auch für „Einsicht“). `quelle.art` aus `internes_audit` (mit `audit_id`, nur am durchgeführten Audit: 422
        `audit_unbekannt`, 409 `audit_nicht_durchgefuehrt`), `eigene`, `extern` (mit `wortlaut`, höchstens 500 Zeichen),
        `managementbewertung` mit `kennung` BR-…/Bn eines Beschlusses einer freigegebenen Managementbewertung (AP-19 IP-23; sonst 422 `quelle_unbekannt`); sonst
        422 `quelle_ungueltig`, ein fremder Verweis 422 `angabe_ungueltig`. Pflicht: `wortlaut` (höchstens 2 000
        Zeichen), eine `vorgabe` - Dokument mit Fassung und/oder Wortlaut (422 `vorgabe_fehlt`, `vorgabe_unbekannt`),
        `festgestellt_von` eine Person im Energiemanagement, auch ohne Konto (422 `festgestellt_von_fehlt`,
        `person_unbekannt`), `verantwortlich` ein aktives Konto (422 `verantwortlich_fehlt`, `konto_unbekannt`).
        `festgestellt_am` Vorgabe heute, nie in der Zukunft (422 `tag_in_zukunft`), beim Audit nicht vor seiner
        Durchführung (422 `tag_vor_durchfuehrung`); `frist` wahlfrei, nicht davor (422 `frist_ungueltig`). `bezug`
        wahlfrei: `standort_id` (422 `standort_unbekannt`), `aufgabe` (422 `aufgabe_unbekannt`), `dokument_id` (422
        `dokument_unbekannt`), `objekte` (Kennzeichen, je einmal). F-JJJJ-nnnn trägt das Jahr des Anlegens. Protokoll
        `feststellung_erfasst`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FeststellungErfassen" }
      responses:
        "201":
          description: Die Feststellung mit Verlauf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FeststellungMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/feststellungen/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [energiemanagement]
      summary: Eine Feststellung mit Einträgen, Maßnahmen, Wirksamkeit, Vier-Augen und Verlauf.
      description: >
        Recht `energiemanagement.ansehen`. Eine Feststellung außerhalb des Zauns oder eines anderen Kundenbereichs gibt
        es nicht (404 `nicht_gefunden`). `massnahmen` sind die AP-18-Maßnahmen mit Herkunft `nichtkonformitaet` und
        dieser Kennung (IP-17) mit ihrem Zustand; `wirksamkeit` die Stände Nr. n mit Kopie und Prüfsumme. `vieraugen`
        ist das Antwortfeld von FS6/W15: bei Vier-Augen nach Einstellung die Berechtigten (KA/EM am Unternehmen), wer
        jetzt zweite Person sein kann - nie die Urheberin (die eines offenen Antrags, sonst die anfragende Person),
        nie der Verantwortliche - und ohne jemanden `erfuellbar` = false mit dem Satz „Vier-Augen nicht erfüllbar: …“.
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FeststellungMitVerlauf" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/feststellungen/{id}/eintraege:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    post:
      tags: [energiemanagement]
      summary: Einen Eintrag anhängen - Kommentar, sofortige Behebung, Ursache (Aussage), ähnliche Fälle (FS2).
      description: >
        Recht `energiemanagement.verwalten` am Standort des Bezugs. `art` aus dem Vokabular `feststellung_eintrag`
        (422 `eintrag_art_ungueltig`); nach dem Abschluss nur `kommentar` (409 `feststellung_abgeschlossen`).
        `wortlaut` 1 bis 2 000 Zeichen; `person_id` Pflicht - jeder Eintrag, auch die Ursache, ist die Aussage einer
        Person, nie ein Satz des Systems (422 `person_fehlt`, `person_unbekannt`); `am` Vorgabe heute, nie in der
        Zukunft, nicht vor dem Tag der Feststellung (422 `tag_vor_feststellung`). Nur anhängen. Protokoll `eintrag`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FeststellungEintragFesthalten" }
      responses:
        "201":
          description: Die Feststellung mit Verlauf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FeststellungMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/feststellungen/{id}/frist:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    put:
      tags: [energiemanagement]
      summary: Die Frist einer offenen Feststellung ändern.
      description: >
        Recht `energiemanagement.verwalten` am Standort des Bezugs. Nur offen (409 `feststellung_abgeschlossen`);
        `frist` Pflicht, nicht vor dem Tag der Feststellung (422 `frist_ungueltig`); Begründung 10 bis 500 Zeichen
        (422 `begruendung_fehlt`). Protokoll `feststellung_geaendert` mit alt und neu; dieselbe Frist schreibt nichts.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FeststellungFristAendern" }
      responses:
        "200":
          description: Die Feststellung mit Verlauf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FeststellungMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/feststellungen/{id}/verantwortlicher:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    put:
      tags: [energiemanagement]
      summary: Den Verantwortlichen einer offenen Feststellung ändern.
      description: >
        Recht `energiemanagement.verwalten` am Standort des Bezugs. Nur offen (409 `feststellung_abgeschlossen`);
        `verantwortlich` ein aktives Konto (422 `verantwortlich_fehlt`, `konto_unbekannt`); Begründung 10 bis 500
        Zeichen (422 `begruendung_fehlt`). Protokoll `feststellung_geaendert`; dasselbe Konto schreibt nichts.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FeststellungVerantwortlichAendern" }
      responses:
        "200":
          description: Die Feststellung mit Verlauf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FeststellungMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/feststellungen/{id}/wirksamkeit:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    post:
      tags: [energiemanagement]
      summary: Die Wirksamkeit als Stand Nr. n festhalten - ohne Vier-Augen (FS4).
      description: >
        Recht `energiemanagement.freigeben` am Unternehmen. Nur offen (409 `feststellung_abgeschlossen`), kein offener
        Antrag (409 `wirksamkeit_beantragt`); mit Vier-Augen nach Einstellung 409 `vieraugen_beantragen`. `ergebnis`
        `wirksam` (schließt ab) oder `nicht_wirksam` (hält offen) - sonst 422 `ergebnis_ungueltig`; erlaubt, wenn jede
        Maßnahme mit Herkunft dieser Feststellung umgesetzt, bewertet oder verworfen ist und mindestens eine umgesetzt
        oder bewertet (409 `wirksamkeit_noch_nicht`). Begründung 10 bis 500 Zeichen (422 `begruendung_fehlt`),
        `entschieden_von` eine Person (422 `entschieden_von_fehlt`, `person_unbekannt`), `am` Vorgabe heute. Die Kopie
        (Feststellung, Wortlaut, Anzahl der Einträge, Maßnahmen mit Zustand, laufende Aufgabe des Bezugs, Tag) mit
        Prüfsumme in der kanonischen Form des Vertrags. Protokoll `wirksamkeit_geprueft` bzw. `feststellung_abgeschlossen`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FeststellungStandFesthalten" }
      responses:
        "201":
          description: Die Feststellung mit dem neuen Stand.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FeststellungMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/feststellungen/{id}/wirksamkeit/beantragen:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    post:
      tags: [energiemanagement]
      summary: Vier-Augen - einen Stand beantragen (FS6).
      description: >
        Recht `energiemanagement.freigeben` am Unternehmen. Nur mit Vier-Augen nach Einstellung (409 `vieraugen_aus`);
        `ergebnis` eines der vier Wörter von `wirksamkeit_ergebnis`, sonst dieselben Prüfungen wie `…/wirksamkeit` und
        `…/abschliessen`. Kann niemand zweite Person sein - nie die Urheberin, nie der Verantwortliche -, 409
        `vieraugen_nicht_erfuellbar` mit `satz` („Vier-Augen nicht erfüllbar: …“) und `berechtigte`: kein Antrag, über
        den niemand entscheiden könnte. Protokoll `wirksamkeit_beantragt`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FeststellungStandFesthalten" }
      responses:
        "201":
          description: Die Feststellung mit dem Antrag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FeststellungMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/feststellungen/{id}/wirksamkeit/freigeben:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    post:
      tags: [energiemanagement]
      summary: Vier-Augen - die zweite Person bestätigt den offenen Antrag.
      description: >
        Recht `energiemanagement.freigeben` am Unternehmen. Kein offener Antrag 409 `kein_antrag`; die zweite Person ist
        nie die Urheberin (422 `vieraugen_urheber`) und nie der Verantwortliche der Feststellung (422
        `vieraugen_verantwortlich`) und hat Rolle KA oder EM (403 `vieraugen_rolle`). Der Körper ist leer. Ein
        schließendes Ergebnis schließt die Feststellung. Protokoll `wirksamkeit_geprueft` bzw.
        `feststellung_abgeschlossen`.
      requestBody:
        required: false
        content:
          application/json:
            schema: { type: object, additionalProperties: false }
      responses:
        "200":
          description: Die Feststellung mit dem bestätigten Stand.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FeststellungMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/feststellungen/{id}/wirksamkeit/ablehnen:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    post:
      tags: [energiemanagement]
      summary: Vier-Augen - die zweite Person lehnt den offenen Antrag ab.
      description: >
        Recht `energiemanagement.freigeben` am Unternehmen. Wie `…/freigeben`; Begründung 10 bis 500 Zeichen (422
        `begruendung_fehlt`). Der Stand behält seine Nr.; danach ist ein neuer Stand möglich. Protokoll
        `wirksamkeit_abgelehnt`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FeststellungAblehnen" }
      responses:
        "200":
          description: Die Feststellung mit dem abgelehnten Stand.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FeststellungMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/feststellungen/{id}/abschliessen:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    post:
      tags: [energiemanagement]
      summary: Ohne Maßnahme abschließen oder zurücknehmen - ein Stand mit Kopie (FS5).
      description: >
        Recht `energiemanagement.freigeben` am Unternehmen. `ergebnis` `ohne_massnahme` (die sofortige Behebung genügt)
        oder `zurueckgenommen` (keine Nichterfüllung) - sonst 422 `ergebnis_ungueltig`; Begründung Pflicht; sonst wie
        `…/wirksamkeit` (ohne die Voraussetzung an den Maßnahmen); mit Vier-Augen 409 `vieraugen_beantragen`. Endgültig;
        eine neue Nichterfüllung ist eine neue Feststellung (FS7). Protokoll `feststellung_abgeschlossen`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FeststellungStandFesthalten" }
      responses:
        "201":
          description: Die abgeschlossene Feststellung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FeststellungMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/audits:
    get:
      tags: [energiemanagement]
      summary: Das Auditprogramm - alle internen Audits und das nächste fällige (IA4).
      description: >
        Recht `energiemanagement.ansehen` (UEMS AP-19 IP-18). Sichtbar sind die Audits im Zaun (RLS aus IP-16: ein Audit
        ohne Standort nur unternehmensweit, eines mit Standorten nur, wer alle sieht), Termin absteigend, jeder Zustand.
        `naechstes` ist beim Abruf abgeleitet, nie gespeichert: letzter Durchführungstag bis `tag` + Rhythmus der
        Einstellung (Startwert 12 Monate) - die Operation `ueberpruefung` des Vertrags `energiemanagement.md`; ohne
        durchgeführtes Audit keine Frist (`grund` = `kein_audit`, kein erfundener Beginn). `tag` Vorgabe heute.
      parameters:
        - in: query
          name: tag
          schema: { type: string, format: date }
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InternesAuditprogramm" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [energiemanagement]
      summary: Internes Audit planen (IA1).
      description: >
        Recht `energiemanagement.verwalten` am Unternehmen (403 `recht_fehlt`, auch für „Einsicht“). Pflicht: `titel`,
        `termin`, `was`, `woran` (422 `angabe_fehlt` mit `feld`), `auditor_ids` - Personen im Energiemanagement, auch
        ohne Konto, jede einmal (422 `auditor_fehlt`, `person_unbekannt`, `person_beendet` wenn sie am Termin nicht mehr
        dabei ist), `unabhaengigkeit` als Wortlaut (422 `unabhaengigkeit_fehlt`), `verantwortlich` ein aktives Konto
        (422 `verantwortlich_fehlt`, `konto_unbekannt`); wahlfrei `standort_ids` (422 `standort_unbekannt`). Wortlaute
        höchstens 2 000 Zeichen, Titel 200 (422 `angabe_ungueltig`). Das Kennzeichen AU-JJJJ-nnnn trägt das Jahr des
        Anlegens. Protokoll `audit_geplant`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/InternesAuditStand" }
      responses:
        "201":
          description: Das Audit mit Verlauf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InternesAuditMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/audits/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [energiemanagement]
      summary: Ein internes Audit mit Hinweisen, Abschluss und Verlauf.
      description: >
        Recht `energiemanagement.ansehen`. Ein Audit außerhalb des Zauns oder eines anderen Kundenbereichs gibt es
        nicht (404 `nicht_gefunden`). Hinweise mit „festgestellt von“ (Person) und „eingetragen von“ (Konto); der
        Abschluss mit Kopie und Prüfsumme.
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InternesAuditMitVerlauf" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
    put:
      tags: [energiemanagement]
      summary: Ein geplantes Audit ändern - der ganze Stand.
      description: >
        Recht `energiemanagement.verwalten` am Unternehmen. Nur geplant (409 `audit_nicht_geplant`); dieselben
        Prüfungen wie beim Planen; ein anderer Termin braucht eine Begründung mit 10 bis 500 Zeichen (422
        `begruendung_fehlt`). Protokoll `audit_geaendert` mit alt und neu; ein unveränderter Stand schreibt nichts.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/InternesAuditStand" }
      responses:
        "200":
          description: Das Audit mit Verlauf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InternesAuditMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/audits/{id}/durchgefuehrt:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    post:
      tags: [energiemanagement]
      summary: Durchgeführt melden - geplant → durchgeführt.
      description: >
        Recht `energiemanagement.verwalten` am Unternehmen. Nur geplant (409 `audit_nicht_geplant`); `am` Pflicht
        (422 `angabe_fehlt`), nie in der Zukunft (422 `tag_in_zukunft`). Protokoll `audit_durchgefuehrt`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/InternesAuditDurchgefuehrt" }
      responses:
        "200":
          description: Das Audit mit Verlauf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InternesAuditMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/audits/{id}/hinweise:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    post:
      tags: [energiemanagement]
      summary: Einen Hinweis festhalten (IA2, IA5).
      description: >
        Recht `energiemanagement.verwalten` am Unternehmen. Nur am durchgeführten Audit (409
        `audit_nicht_durchgefuehrt`). `wortlaut` (höchstens 2 000 Zeichen) und `festgestellt_von` - die Person, die
        prüft; sie braucht kein Konto und kein Schreibrecht - sind Pflicht (422 `angabe_fehlt`, `person_unbekannt`);
        „eingetragen von“ ist das Konto, das festhält. `am` Vorgabe der Durchführungstag, nicht davor (422
        `tag_vor_durchfuehrung`) und nie in der Zukunft. Die Nummer vergibt die Datenbank lückenlos. Protokoll `hinweis`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/InternesAuditHinweisFesthalten" }
      responses:
        "201":
          description: Das Audit mit dem neuen Hinweis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InternesAuditMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/audits/{id}/abschliessen:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    post:
      tags: [energiemanagement]
      summary: Ein durchgeführtes Audit abschließen - Kopie mit Prüfsumme (IA3).
      description: >
        Recht `energiemanagement.freigeben` am Unternehmen. Nur am durchgeführten Audit (409
        `audit_nicht_durchgefuehrt`, ein zweiter Abschluss ebenso). `entschieden_von` Pflicht, eine Person (422
        `entschieden_von_fehlt`, `person_unbekannt`); `am` Vorgabe heute, nicht vor der Durchführung, nie in der Zukunft;
        der Bericht als Verweis (`bericht.ablage`, die Prüfsumme bildet der Browser) oder eine `zusammenfassung` (422
        `bericht_oder_zusammenfassung`, `bericht_ungueltig`). `massnahmen` nennt wahlfrei je Hinweis die Maßnahme, die
        aus ihm wurde - sie muss dieses Audit als Herkunft `audit` tragen (422 `hinweis_unbekannt`,
        `massnahme_unbekannt`). Die Kopie hält `kennzeichen`, `hinweise`, `feststellungen` (mit Quelle dieses Audit) und
        `bericht` fest; `pruefsumme` in der kanonischen Form des Vertrags. Danach ist das Audit unveränderlich.
        Protokoll `audit_abgeschlossen`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/InternesAuditAbschliessen" }
      responses:
        "200":
          description: Das abgeschlossene Audit.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InternesAuditMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/audits/{id}/absagen:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    post:
      tags: [energiemanagement]
      summary: Ein geplantes Audit absagen.
      description: >
        Recht `energiemanagement.verwalten` am Unternehmen. Nur geplant (409 `audit_nicht_geplant`); Begründung 10 bis
        500 Zeichen (422 `begruendung_fehlt`); endgültig. Protokoll `audit_abgesagt`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/InternesAuditAbsagen" }
      responses:
        "200":
          description: Das abgesagte Audit.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InternesAuditMitVerlauf" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/managementbewertungen/{kennung}:
    parameters:
      - in: path
        name: kennung
        required: true
        schema: { type: string, pattern: "^BR-[0-9]{4}-[0-9]{4,}$" }
    get:
      tags: [energiemanagement]
      summary: Sitzung, Beschlüsse und Folgen einer Managementbewertung (MG4–MG6).
      description: >
        Recht `energiemanagement.ansehen` (UEMS AP-19 IP-23); Sichtbarkeit aus RLS und dem Zaun des Berichts. Ein
        Bericht einer anderen Vorlage oder eine unbekannte Kennung ist 404. Die Folgen jedes Beschlusses mit dem Zustand
        von heute - von Hand verknüpft (`von_hand`) und die Objekte, die den Beschluss selbst nennen: Maßnahme mit
        Herkunft `managementbewertung` (`herkunft`), Aufgabe (`zuordnung`), Dokument-Fassung (`fassung`), „geprüft,
        bleibt“ (`geprueft_bleibt`). Nach der Freigabe trägt ein Beschluss ohne Folge den Satz `beschluss_ohne_folge`.
      responses:
        "200":
          description: Die Managementbewertung mit Sitzung und Beschlüssen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Managementbewertung" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/managementbewertungen/{kennung}/sitzung:
    parameters:
      - in: path
        name: kennung
        required: true
        schema: { type: string, pattern: "^BR-[0-9]{4}-[0-9]{4,}$" }
    put:
      tags: [energiemanagement]
      summary: Die Sitzung festhalten - Tag, Leitung, Teilnehmende (MG4).
      description: >
        Recht `energiemanagement.verwalten` am Unternehmen. Bis zur Freigabe (409 `managementbewertung_freigegeben`);
        ein zweites Festhalten ersetzt die erste Angabe. `tag` nie in der Zukunft (422 `tag_in_der_zukunft`); `leitung`
        eine Person mit der laufenden Aufgabe „Leitung des Unternehmens“ am Tag der Sitzung (PA3, 422 `leitung_fehlt`);
        `teilnehmende` Personen (422 `person_unbekannt`); `ort` wahlfrei, höchstens 200 Zeichen. Bildet den Entwurf in
        derselben Transaktion neu - die Freigabe friert genau diese Sitzung ein.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ManagementbewertungSitzungFesthalten" }
      responses:
        "200":
          description: Die Managementbewertung mit der Sitzung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Managementbewertung" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/managementbewertungen/{kennung}/beschluesse:
    parameters:
      - in: path
        name: kennung
        required: true
        schema: { type: string, pattern: "^BR-[0-9]{4}-[0-9]{4,}$" }
    post:
      tags: [energiemanagement]
      summary: Einen Beschluss der Leitung festhalten (MG5).
      description: >
        Recht `energiemanagement.verwalten` am Unternehmen. Nach der Sitzung (422 `sitzung_fehlt`), bis zur Freigabe
        (409 `managementbewertung_freigegeben`); die nächste Nr. `art` aus `beschluss_art`, `wortlaut` 1 bis 2 000
        Zeichen; `entschieden_von` die Leitung (Vorgabe: die Leitung der Sitzung, sonst 422 `leitung_fehlt`); wahlfrei
        `zustaendig` und `termin`. Bildet den Entwurf in derselben Transaktion neu.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ManagementbewertungBeschlussFesthalten" }
      responses:
        "201":
          description: Die Managementbewertung mit dem neuen Beschluss.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Managementbewertung" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/managementbewertungen/{kennung}/beschluesse/{nr}:
    parameters:
      - in: path
        name: kennung
        required: true
        schema: { type: string, pattern: "^BR-[0-9]{4}-[0-9]{4,}$" }
      - in: path
        name: nr
        required: true
        schema: { type: integer, minimum: 1 }
    put:
      tags: [energiemanagement]
      summary: Einen Beschluss ändern - bis zur Freigabe (MG5).
      description: >
        Recht `energiemanagement.verwalten` am Unternehmen. Wie das Festhalten; die Nr. bleibt, eine unbekannte ist 404.
        Nach der Freigabe 409 `managementbewertung_freigegeben` - eine Berichtigung ist eine neue Freigabe mit Grund.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ManagementbewertungBeschlussFesthalten" }
      responses:
        "200":
          description: Die Managementbewertung mit dem geänderten Beschluss.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Managementbewertung" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energiemanagement/managementbewertungen/{kennung}/beschluesse/{nr}/folgen:
    parameters:
      - in: path
        name: kennung
        required: true
        schema: { type: string, pattern: "^BR-[0-9]{4}-[0-9]{4,}$" }
      - in: path
        name: nr
        required: true
        schema: { type: integer, minimum: 1 }
    post:
      tags: [energiemanagement]
      summary: Eine Folge mit einem Beschluss verknüpfen (MG6).
      description: >
        Recht `energiemanagement.verwalten` am Unternehmen. Nach der Freigabe (409
        `managementbewertung_nicht_freigegeben`); nur anhängen, dasselbe Objekt ein zweites Mal ändert nichts; der Stand
        ändert sich nie. `art` `energieziel` (EZ-…), `dokument` (die Fassung D-…/n), `aufgabe` (ID der Zuordnung) oder
        `audit` (AU-…); ein unbekanntes Objekt 422 `objekt_unbekannt`. Eine Maßnahme verknüpft sich selbst über ihre
        Herkunft `managementbewertung` (422 `folge_art`).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ManagementbewertungFolgeVerknuepfen" }
      responses:
        "201":
          description: Die Managementbewertung mit der Folge.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Managementbewertung" }
        "400": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergiemanagementAbgelehnt" }
  /api/v1/energieziele:
    get:
      tags: [kennzahlen]
      summary: Das Register der Energieziele - wahlweise einer Kennzahl oder eines Zustands.
      description: >
        Recht `verbesserung.ansehen` (UEMS AP-18 IP-6); sichtbar sind die Ziele im Zaun (RLS über `standort_id`,
        ein Ziel am Unternehmen nur unternehmensweit). `kennzahl` ist die ID einer sichtbaren Kennzahl (sonst 404),
        `zustand` aus `offen · bewertet · beendet`; ein anderer Parameter 400.
      parameters:
        - { name: kennzahl, in: query, required: false, schema: { type: string, format: uuid } }
        - { name: zustand, in: query, required: false, schema: { type: string, enum: [offen, bewertet, beendet] } }
      responses:
        "200":
          description: Die sichtbaren Energieziele, nach Kennzeichen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiezielListe" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
    post:
      tags: [kennzahlen]
      summary: Legt ein Energieziel EZ-JJJJ-nnnn an einer Kennzahl mit freigegebener Bezugsbasis an.
      description: >
        Recht `verbesserung.verwalten` am Geltungsbereich der Kennzahl (Z1, Z2, E3 = A). Das Ziel zitiert die
        freigegebene, heute geltende Bezugsbasis-Fassung; ohne sie 422 `kennzahl_ohne_bezugsbasis`. `zielwert_prozent`
        ist Prozent gegenüber dem Erwarteten (eine Stelle, weniger Energie negativ, −100 < x < 100; sonst 400) - kein
        absoluter Wert. `zielperiode` fest aus ganzen Monaten `JJJJ-MM/JJJJ-MM` (höchstens 120), nie rollierend (400
        `zielperiode_ungueltig`), Beginn frühestens im Monat nach dem Anlegen (400 `zielperiode_rueckwirkend`) und nicht
        vor der Geltung der Fassung (422 `zielperiode_vor_fassung`). `wortlaut` Pflicht (422 `wortlaut_fehlt`),
        `begruendung` 10–500 Zeichen (422 `begruendung_fehlt`); `verantwortlich` ein aktiver Benutzer (`sub`, 422
        `benutzer_unbekannt`), ohne ihn der der Kennzahl bzw. die anlegende Person. Je Kennzahl höchstens ein offenes
        Ziel mit überschneidender Zielperiode (409 `energieziel_laeuft`). Das Kennzeichen trägt das erste Jahr der
        Zielperiode; Protokoll `energieziel_angelegt`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [kennzahl, zielwert_prozent, zielperiode, wortlaut, begruendung]
              properties:
                kennzahl: { type: string, format: uuid }
                zielwert_prozent: { type: number, exclusiveMinimum: -100, exclusiveMaximum: 100 }
                zielperiode: { type: string, pattern: "^[0-9]{4}-(0[1-9]|1[0-2])/[0-9]{4}-(0[1-9]|1[0-2])$" }
                wortlaut: { type: string, minLength: 1 }
                begruendung: { type: string, minLength: 10, maxLength: 500 }
                verantwortlich: { type: string, description: "sub eines aktiven Benutzers" }
      responses:
        "201":
          description: Angelegt, offen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Energieziel" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/energieziele/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [kennzahlen]
      summary: Das Energieziel mit Verlauf.
      description: Recht `verbesserung.ansehen`; Zaun über die Kennzahl und `standort_id` - außerhalb 404.
      responses:
        "200":
          description: Das Energieziel.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Energieziel" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
    put:
      tags: [kennzahlen]
      summary: Ändert Wortlaut und Ende der Zielperiode, solange das Ziel offen ist - mit Begründung.
      description: >
        Recht `verbesserung.verwalten` am Geltungsbereich der Kennzahl (§5.7). Die Zielperiode verschiebt nur ihr
        Ende nach hinten (422 `zielperiode_nur_nach_hinten`); `begruendung` 10–500 Zeichen Pflicht; nicht offen 409
        `energieziel_nicht_offen`. Zielwert, Kennzahl und Fassung bleiben. Protokoll `energieziel_geaendert` (alt/neu).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [begruendung]
              properties:
                wortlaut: { type: string, minLength: 1 }
                zielperiode: { type: string, pattern: "^[0-9]{4}-(0[1-9]|1[0-2])/[0-9]{4}-(0[1-9]|1[0-2])$" }
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        "200":
          description: Geändert.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Energieziel" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/energieziele/{id}/verantwortlicher:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [kennzahlen]
      summary: Setzt den Verantwortlichen des Energieziels (Benutzer + Schnappschuss) mit Begründung.
      description: >
        Recht `verbesserung.verwalten` am Geltungsbereich der Kennzahl; ein aktiver Benutzer des Kundenbereichs (422
        `benutzer_unbekannt`), solange offen. Verantwortung verleiht kein Recht (RE3). Protokoll
        `verantwortlicher_geaendert`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [benutzer, begruendung]
              properties:
                benutzer: { type: string }
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        "200":
          description: Umbesetzt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Energieziel" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/energieziele/{id}/anstoesse/{aid}/antwort:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: aid, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Antwortet auf einen Anstoß am Vorgang (IP-17) - einmalig; die Kopie bleibt, bis eine Person anders antwortet.
      description: >
        Recht `verbesserung.verwalten` (`bleibt`) bzw. `verbesserung.abschliessen` (`neu_bewertet`) an der Geltung der
        Kennzahl (Z5, §5.7). `bleibt` mit Begründung (Pflicht); `neu_bewertet` nur an `messgrundlage_beendet`,
        `messgrundlage_neu_gefasst` - die Bewertung des offenen Ziels wie `POST …/bewerten` (mit Vier-Augen als Antrag);
        an `bewertung_korrigiert` nur `bleibt` - eine gestellte Bewertung wird nie zurückgenommen (422
        `antwort_passt_nicht`). Schon beantwortet 409 `anstoss_beantwortet`; ein Anstoß eines anderen Ziels 404.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [antwort]
              properties:
                antwort: { type: string, enum: [bleibt, neu_bewertet] }
                begruendung: { type: string, minLength: 10, maxLength: 500, description: "Pflicht bei bleibt" }
                ergebnis: { type: string, description: "nur bei neu_bewertet: das Ergebnis der Bewertung" }
      responses:
        "200":
          description: Beantwortet.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Energieziel" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/energieziele/{id}/beenden:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Beendet ein offenes Energieziel vorzeitig (Tag, Begründung) - endgültig, nie gelöscht.
      description: >
        Recht `verbesserung.verwalten` am Geltungsbereich der Kennzahl. `zum` ohne Angabe heute, nie in der Zukunft
        oder vor dem Anlegen (422 `tag_ungueltig`); `begruendung` 10–500 Zeichen. Protokoll `energieziel_beendet`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [begruendung]
              properties:
                zum: { type: string, format: date }
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        "200":
          description: Beendet.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Energieziel" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/energieziele/{id}/bewerten:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Bewertet ein Energieziel nach dem Ende der Zielperiode - Ergebnis mit Begründung, Kopie des Stands mit Prüfsumme.
      description: >
        Recht `verbesserung.abschliessen` am Geltungsbereich der Kennzahl (Z4, Z5). Nur offen und nach dem Ende der
        Zielperiode, wenn ihr letzter Monat endgültig ist (sonst 409 `bewertung_nicht_faellig`). Das Ergebnis setzt die
        Person; `bewertung.vorschlag` zeigt daneben den Vorschlag des Lesers (null bei unvollständiger Periode). Die
        Bewertung ist eine Kopie des Ziel-Stands zum Bewertungstag als kanonischer Text (`bewertung.kopie`) mit
        `bewertung.pruefsumme` (sha256) - nie zurückgenommen. Mit Vier-Augen (`unternehmen.vieraugen_freigabe`) 409
        `vieraugen_beantragen`; ein zweites Mal 409 `energieziel_nicht_offen`; ein offener Antrag 409
        `bewertung_beantragt`. Protokoll `energieziel_bewertet`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [ergebnis, begruendung]
              properties:
                ergebnis: { type: string, enum: [erreicht, verfehlt, nicht_bewertbar] }
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        "200":
          description: Bewertet.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Energieziel" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/energieziele/{id}/bewertung/beantragen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Beantragt die Bewertung eines Energieziels bei Vier-Augen-Freigabe.
      description: >
        Recht `verbesserung.abschliessen`. Wie `bewerten`, aber das Ziel bleibt offen mit `bewertung.status =
        beantragt`, bis eine zweite Person entscheidet; ohne Vier-Augen 409 `vieraugen_aus`. Nach einer Ablehnung darf
        ein neuer Antrag kommen. Protokoll `bewertung_beantragt`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [ergebnis, begruendung]
              properties:
                ergebnis: { type: string, enum: [erreicht, verfehlt, nicht_bewertbar] }
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        "200":
          description: Beantragt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Energieziel" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/energieziele/{id}/bewertung/freigeben:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Bestätigt die beantragte Bewertung als zweite Person.
      description: >
        Recht `verbesserung.abschliessen`; die zweite Person hat die Rolle Kundenadministrator oder Energiemanager
        (403 `vieraugen_rolle`) und ist nie, wer beantragt hat (422 `vieraugen_urheber`). Ohne Antrag 409
        `bewertung_nicht_beantragt`. Ergebnis, Kopie und Prüfsumme bleiben die des Antrags; das Ziel ist `bewertet`.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        "200":
          description: Bewertet.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Energieziel" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/energieziele/{id}/bewertung/ablehnen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Lehnt die beantragte Bewertung als zweite Person mit Begründung ab.
      description: >
        Recht `verbesserung.abschliessen`; zweite Person wie bei `freigeben`; Begründung 10–500 Zeichen. Das Ziel
        bleibt offen, `bewertung.status = abgelehnt`; ein neuer Antrag ist möglich. Protokoll `bewertung_abgelehnt`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [begruendung]
              properties:
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        "200":
          description: Abgelehnt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Energieziel" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/energieziele/{id}/stand:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [kennzahlen]
      summary: Der Ziel-Stand - je Monat der Vergleich mit der Bezugsbasis, Summe durch Summe, x von y.
      description: >
        Recht `verbesserung.ansehen` (Z3, Z4; Vertrag verbesserung.md §4). Je Monat der Zielperiode die Zeile des
        Vergleich-Lesers der zitierten Bezugsbasis (Fassung am letzten Tag des Monats, P4) und ob der Monat endgültig
        ist; über die endgültigen Monate die Operation `zielstand`: Σ gemessen ÷ Σ erwartet über die bewertbaren
        (nie ein Mittel), `monate_text` „x von y“, `nicht_gezaehlt` mit Grund, `vorschlag` (`erreicht ·
        nicht_erreicht`) nur bei vollständiger Periode, sonst null. `satz` und `vorschlag_satz` sind Kundensätze des
        Vertrags (§6), ohne bewertbaren Monat null.
      responses:
        "200":
          description: Der Stand zum Abruf-Tag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergiezielStand" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/massnahmen:
    get:
      tags: [kennzahlen]
      summary: Das Register der Maßnahmen - Filter Zustand, überfällig, Kennzahl, Einsatz.
      description: >
        Recht `verbesserung.ansehen` (UEMS AP-18 IP-10); sichtbar sind die Maßnahmen im Zaun (RLS über
        `standort_id`, eine Maßnahme am Unternehmen nur unternehmensweit; mit Messgrundlage zusätzlich über die
        Kennzahl). `ueberfaellig` filtert über die Operation `frist` beim Abruf (Uhr der Kennzahlen, kein Läufer,
        kein Zustand); `kennzahl` ist die ID einer sichtbaren Kennzahl (sonst 404); ein anderer Parameter 400.
      parameters:
        - { name: zustand, in: query, required: false, schema: { type: string, enum: [geplant, umgesetzt, bewertet, verworfen] } }
        - { name: ueberfaellig, in: query, required: false, schema: { type: string, enum: ["true", "false"] } }
        - { name: kennzahl, in: query, required: false, schema: { type: string, format: uuid } }
        - { name: einsatz, in: query, required: false, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Die sichtbaren Maßnahmen, nach Kennzeichen, mit dem Abruf-Tag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MassnahmeListe" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
    post:
      tags: [kennzahlen]
      summary: Legt eine Maßnahme M-JJJJ-nnnn an - mit oder ohne Messgrundlage (E2 = A).
      description: >
        Recht `verbesserung.verwalten` an der Geltung der Kennzahl bzw. am gewählten `standort` (außerhalb der Sicht 404
        `standort_unbekannt`; ohne Standort = am
        Unternehmen; M1, RE1, RE2). Pflicht: `titel` (422 `titel_fehlt`), `verantwortlich` als aktives Konto des
        Kundenbereichs (422 `verantwortlich_fehlt` bzw. `benutzer_unbekannt`), `termin` (400),
        `erwartete_wirkung_wortlaut` (422 `wortlaut_fehlt`). Herkunft `abweichung · energieziel · einsatz · von_hand ·
        nichtkonformitaet · audit · managementbewertung` (ohne: `von_hand`); bei `abweichung` die Kennung AW-… in
        `herkunft_kennung`, bei `energieziel`/`einsatz` folgt sie dem Verweis (422 `herkunft_ohne_verweis`). Aus dem
        Energiemanagement (AP-19 IP-17): `nichtkonformitaet` mit F-… einer offenen Feststellung (422
        `feststellung_nicht_offen`), `audit` mit AU-… eines durchgeführten oder abgeschlossenen internen Audits (422
        `audit_nicht_durchgefuehrt`), `managementbewertung` mit BR-…/Bn eines Beschlusses einer freigegebenen
        Managementbewertung (AP-19 IP-23, noch ohne Stand 422 `managementbewertung_nicht_freigegeben`; die Maßnahme ist
        dann selbst die Folge des Beschlusses, MG6); eine unbekannte oder nicht sichtbare Kennung ist 422
        `herkunft_kennung`, ein falsches Muster 400. Mit `kennzahl` (M2): die freigegebene, heute geltende
        Bezugsbasis-Fassung (sonst 422 `kennzahl_ohne_bezugsbasis`) und die Ausgangslage als KOPIE des Vergleich-Lesers
        über `monate` (`JJJJ-MM` oder `JJJJ-MM/JJJJ-MM`, höchstens 12, abgeschlossen - 422
        `monate_nicht_abgeschlossen`; ohne: der letzte abgeschlossene Monat), kanonischer Text mit Prüfsumme; der
        Standort ist der der Kennzahl (422 `standort_der_kennzahl`). Ohne `kennzahl` (M4) ist
        `erwartete_wirkung_prozent` 422 `ohne_messgrundlage` mit `kennzeichen` und `hinweis`, welche Kennzahl fehlt.
        Protokoll `massnahme_angelegt`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [titel, verantwortlich, termin, erwartete_wirkung_wortlaut]
              properties:
                titel: { type: string, minLength: 1 }
                verantwortlich: { type: string, description: "sub eines aktiven Benutzers" }
                termin: { type: string, format: date }
                herkunft:
                  type: string
                  enum: [abweichung, energieziel, einsatz, von_hand, nichtkonformitaet, audit, managementbewertung]
                herkunft_kennung:
                  type: string
                  pattern: "^(AW-[0-9]{4}-[0-9]{4,9}|F-[0-9]{4}-[0-9]{4,9}|AU-[0-9]{4}-[0-9]{4,9}|BR-[0-9]{4}-[0-9]{4,}/B[0-9]{1,3})$"
                kennzahl: { type: string, format: uuid }
                monate: { type: string, pattern: "^[0-9]{4}-(0[1-9]|1[0-2])(/[0-9]{4}-(0[1-9]|1[0-2]))?$" }
                einsatz: { type: string, format: uuid }
                einstufung_fassung: { type: integer, minimum: 1 }
                energieziel: { type: string, format: uuid }
                standort: { type: string, format: uuid }
                erwartete_wirkung_prozent: { type: number, exclusiveMinimum: -100, exclusiveMaximum: 100 }
                erwartete_wirkung_wortlaut: { type: string, minLength: 1 }
      responses:
        "201":
          description: Angelegt, geplant.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Massnahme" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/massnahmen/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [kennzahlen]
      summary: Die Maßnahme mit Verlauf.
      description: Recht `verbesserung.ansehen`; Zaun über `standort_id` und die Kennzahl - außerhalb 404.
      responses:
        "200":
          description: Die Maßnahme.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Massnahme" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
    put:
      tags: [kennzahlen]
      summary: Ändert Titel, Termin und erwartete Wirkung, solange die Maßnahme geplant ist - mit Begründung.
      description: >
        Recht `verbesserung.verwalten` (M6, §5.7). Nicht geplant 409 `massnahme_nicht_geplant`; `begruendung` 10–500
        Zeichen (422 `begruendung_fehlt`); eine Zahl der erwarteten Wirkung ohne Messgrundlage 422
        `ohne_messgrundlage`. Die Messgrundlage und ihre Ausgangslage bleiben. Protokoll `massnahme_geaendert` (alt/neu).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [begruendung]
              properties:
                titel: { type: string, minLength: 1 }
                termin: { type: string, format: date }
                erwartete_wirkung_prozent: { type: number, exclusiveMinimum: -100, exclusiveMaximum: 100 }
                erwartete_wirkung_wortlaut: { type: string, minLength: 1 }
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        "200":
          description: Geändert.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Massnahme" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/massnahmen/{id}/wirkung:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [kennzahlen]
      summary: Die Wirkung der Maßnahme - Nachher-Monate gegen die Bezugsbasis, Summe durch Summe, x von N.
      description: >
        Recht `verbesserung.ansehen` (UEMS AP-18 IP-11, WK1–WK5; Vertrag verbesserung.md §3). Ein Leser, kein
        gespeicherter Wert: je Monat vom Umsetzungsmonat bis `monate` Monate danach die Zeile des Vergleich-Lesers
        (Fassung am letzten Tag des Monats, P4) und ob der Monat endgültig ist; über den Umsetzungsmonat und die
        endgültigen Nachher-Monate die Operation `wirkung`: der Umsetzungsmonat „nicht gezählt“, eine Fassung mit
        Referenzperiode bis in den Umsetzungsmonat oder danach `basis_nach_umsetzung`, Σ gemessen ÷ Σ erwartet über die
        bewertbaren (nie ein Mittel), `monate_text` „x von N“, `vorlaeufig` solange weniger als N Nachher-Monate
        endgültig sind, `nicht_gezaehlt` mit Grund. Erwartete Wirkung und Ausgangslage (Kopie, byte-gleich) stehen in
        `massnahme`; kein Satz nennt eine Ursache. Ohne Messgrundlage `grund` `ohne_messgrundlage` und nur `satz`,
        keine Zahl; vor der Umsetzung `grund` `nicht_umgesetzt` ohne Monate und ohne Satz. Zaun wie die Maßnahme -
        außerhalb 404; ein anderer Parameter 400.
      parameters:
        - { name: monate, in: query, required: false, schema: { type: integer, minimum: 12, maximum: 36, default: 12 }, description: "Länge des Nachher-Zeitraums (WK2); außerhalb 12 … 36 400" }
      responses:
        "200":
          description: Die Wirkung zum Abruf-Tag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MassnahmeWirkung" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/massnahmen/{id}/anstoesse/{aid}/antwort:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: aid, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Antwortet auf einen Anstoß am Vorgang (IP-17) - einmalig; die Kopie bleibt, bis eine Person anders antwortet.
      description: >
        Recht `verbesserung.verwalten` (`bleibt`, `neu_kopiert`) bzw. `verbesserung.abschliessen` (`neu_bewertet`) an
        der Geltung der Kennzahl oder am Standort (M5, §5.7). `bleibt` mit Begründung (Pflicht); `neu_kopiert` nur an
        `ausgangslage_korrigiert` - die Ausgangslage neu aus dem Leser (dieselbe Kennzahl, Fassung und Monate), neue
        Prüfsumme, die alte Kopie steht in der Protokollzeile `anstoss_beantwortet` (`alt`); `neu_bewertet` nur an
        `bewertung_korrigiert`, `messgrundlage_beendet`, `messgrundlage_neu_gefasst` - der Stand Nr. n + 1 wie
        `POST …/bewertungen` (mit Vier-Augen als Antrag), `ergebnis` und `begruendung` wie dort. Was nicht zur Art passt,
        422 `antwort_passt_nicht`; schon beantwortet 409 `anstoss_beantwortet`; ein Anstoß eines anderen Vorgangs 404.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [antwort]
              properties:
                antwort: { type: string, enum: [bleibt, neu_kopiert, neu_bewertet] }
                begruendung: { type: string, minLength: 10, maxLength: 500, description: "Pflicht bei bleibt, sonst wahlfrei" }
                ergebnis: { type: string, description: "nur bei neu_bewertet: das Ergebnis der Bewertung" }
      responses:
        "200":
          description: Beantwortet.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Massnahme" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/massnahmen/{id}/bewertungen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [kennzahlen]
      summary: Alle Stände der Bewertung - Stand Nr. 1 … n, auch beantragte und abgelehnte.
      description: >
        Recht `verbesserung.ansehen` (UEMS AP-18 IP-12, WK6). Jeder Stand mit Ergebnis, Begründung, Person, Tag,
        Vier-Augen-Entscheid, Kopie der Wirkung (kanonischer Text) und Prüfsumme; nie zurückgenommen. Zaun wie die
        Maßnahme - außerhalb 404.
      responses:
        "200":
          description: Die Stände nach Nr.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MassnahmeBewertungen" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
    post:
      tags: [kennzahlen]
      summary: Bewertet die umgesetzte Maßnahme - Stand Nr. n mit Kopie der Wirkung und Prüfsumme.
      description: >
        Recht `verbesserung.abschliessen` (WK6, E6 = A). Nur umgesetzt oder bewertet (sonst 409
        `massnahme_nicht_umgesetzt`); `begruendung` 10–500 Zeichen (422 `begruendung_fehlt`). Die Kopie ist die Wirkung
        (`GET …/wirkung`, 12 Monate) zum Bewertungstag in der Form der Referenzdatei 1.9 (`bewertungen[].kopie`),
        kanonisch mit `sha256:`-Prüfsumme; ohne Messgrundlage keine Kopie und nur `nicht_messbar` (422
        `ohne_messgrundlage`). Mit Vier-Augen-Freigabe des Unternehmens 409 `vieraugen_beantragen`; ein offener Antrag
        409 `bewertung_beantragt`. Ein weiterer Stand ist Nr. n + 1, der alte bleibt. Zustand `bewertet`; Protokoll
        `massnahme_bewertet`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [ergebnis, begruendung]
              properties:
                ergebnis: { type: string, enum: [belegt, nicht_belegt, nicht_messbar] }
                begruendung: { type: string, minLength: 10, maxLength: 500, description: "bei `belegt`: was die Person weiß und die Zahl nicht zeigt (WK6)" }
      responses:
        "201":
          description: Bewertet; die Maßnahme mit dem Stand in `bewertung`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Massnahme" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/massnahmen/{id}/bewertungen/beantragen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Beantragt die Bewertung (Vier-Augen) - eine zweite Person bestätigt oder lehnt ab.
      description: >
        Recht `verbesserung.abschliessen` (WK6). Wie `POST …/bewertungen`, aber als Antrag Nr. n (`bewertung_antrag`);
        ohne Vier-Augen-Freigabe 409 `vieraugen_aus`, ein offener Antrag 409 `bewertung_beantragt`. Protokoll
        `bewertung_beantragt`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [ergebnis, begruendung]
              properties:
                ergebnis: { type: string, enum: [belegt, nicht_belegt, nicht_messbar] }
                begruendung: { type: string, minLength: 10, maxLength: 500, description: "bei `belegt`: was die Person weiß und die Zahl nicht zeigt (WK6)" }
      responses:
        "201":
          description: Beantragt; die Maßnahme mit dem Antrag in `bewertung_antrag`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Massnahme" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/massnahmen/{id}/bewertungen/freigeben:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Die zweite Person bestätigt den Antrag - die Maßnahme ist bewertet.
      description: >
        Recht `verbesserung.abschliessen` (WK6, §5.7). Wer beantragt hat, 422 `vieraugen_urheber`; der Verantwortliche
        der Maßnahme 422 `vieraugen_verantwortlich`; nicht Kundenadministrator oder Energiemanager 403
        `vieraugen_rolle`; ohne Antrag 409 `bewertung_nicht_beantragt`. Ergebnis, Kopie und Prüfsumme bleiben die des
        Antrags. Protokoll `massnahme_bewertet`.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        "200":
          description: Bestätigt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Massnahme" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/massnahmen/{id}/bewertungen/ablehnen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Die zweite Person lehnt den Antrag mit Begründung ab - er behält seine Nr.
      description: >
        Recht `verbesserung.abschliessen` (WK6). Dieselben Prüfungen wie `freigeben`; `begruendung` Pflicht. Danach
        darf ein neuer Antrag kommen (Nr. n + 1). Protokoll `bewertung_abgelehnt`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [begruendung]
              properties:
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        "200":
          description: Abgelehnt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Massnahme" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/massnahmen/{id}/verantwortlicher:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [kennzahlen]
      summary: Setzt den Verantwortlichen der Maßnahme (Benutzer + Schnappschuss) mit Begründung.
      description: >
        Recht `verbesserung.verwalten`, solange geplant (409 `massnahme_nicht_geplant`); `benutzer` ist ein aktives
        Konto des Kundenbereichs (422 `benutzer_unbekannt`) - Verantwortung verleiht kein Recht (RE3). Protokoll
        `verantwortlicher_geaendert`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [benutzer, begruendung]
              properties:
                benutzer: { type: string, description: "sub eines aktiven Benutzers" }
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        "200":
          description: Gesetzt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Massnahme" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/massnahmen/{id}/umgesetzt:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Meldet die Maßnahme als umgesetzt (Tag, Begründung) - einmalig.
      description: >
        Recht `verbesserung.verwalten` (M6). Nur geplant, einmalig (409 `massnahme_nicht_geplant`); `am` nie nach
        heute in der Zeitzone des Unternehmens (422 `umgesetzt_in_der_zukunft`); `begruendung` 10–500 Zeichen.
        Protokoll `massnahme_umgesetzt`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [am, begruendung]
              properties:
                am: { type: string, format: date }
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        "200":
          description: Umgesetzt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Massnahme" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/massnahmen/{id}/verwerfen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Verwirft die geplante Maßnahme mit Begründung - endgültig, nie gelöscht.
      description: >
        Recht `verbesserung.verwalten` (M6). Nur geplant (409 `massnahme_nicht_geplant`); `begruendung` 10–500 Zeichen
        Pflicht. Protokoll `massnahme_verworfen`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [begruendung]
              properties:
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        "200":
          description: Verworfen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Massnahme" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/massnahmen/{id}/eintraege:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Ein Kommentar im Verlauf der Maßnahme.
      description: >
        Recht `verbesserung.verwalten` (M7, §5.7). An einer geplanten oder umgesetzten Maßnahme (sonst 409
        `massnahme_nicht_geplant`); `text` 1–2 000 Zeichen (422 `text_ungueltig`); `art` nur `kommentar`. Eine
        Protokollzeile mit Person, Zeit und Text - nichts wird geändert, nichts gelöscht.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [text]
              properties:
                art: { type: string, enum: [kommentar] }
                text: { type: string, minLength: 1, maxLength: 2000 }
      responses:
        "201":
          description: Eingetragen; die Maßnahme mit Verlauf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Massnahme" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/kennzahlen/{id}/auffaelligkeiten:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [kennzahlen]
      summary: Die Auffälligkeiten (Vermerke) einer Kennzahl - offen und beantwortet.
      description: >
        Recht `verbesserung.ansehen` (UEMS AP-18 IP-16, A1, A2); Zaun über die Kennzahl (außerhalb 404) und über
        `standort_id` des Vermerks (RLS). Je Vermerk die Kopie des Vergleichsergebnisses mit Prüfsumme, die geerbten
        Vorbehalte („vorläufig“) und die Antwort; `zustand` (`offen · beantwortet`) filtert, `offen` zählt immer alle
        offenen; ein anderer Parameter 400. Die Vermerke schreibt die Naht (IP-15).
      parameters:
        - { name: zustand, in: query, required: false, schema: { type: string, enum: [offen, beantwortet] } }
      responses:
        "200":
          description: Die Vermerke nach Monat, mit dem Abruf-Tag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuffaelligkeitListe" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/kennzahlen/{id}/auffaelligkeiten/{aid}/antwort:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: aid, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Die einmalige Antwort auf eine Auffälligkeit - Abweichung eröffnen oder zur Kenntnis nehmen.
      description: >
        Recht `verbesserung.verwalten` an der Geltung der Kennzahl (A2, RE1). Einmalig (409
        `auffaelligkeit_beantwortet`). `abweichung` eröffnet AW-JJJJ-nnnn (Jahr des Eröffnens) mit ALLEN offenen
        Vermerken derselben Kennzahl × Bezugsbasis-Fassung - Anlass = Kopie des Vermerks (byte-gleich; mehrere als
        `{"vermerke": […]}` kanonisch) mit Prüfsumme; `verantwortlich` ist ein aktives Konto (422
        `verantwortlich_fehlt`/`benutzer_unbekannt`), `frist` ohne Angabe Eröffnungstag + 30, nie davor (422
        `frist_vor_eroeffnung`); 201 mit Vermerk und Abweichung. `zur_kenntnis` verlangt `begruendung` 10–500 (422
        `begruendung_fehlt`) und kennt weder Frist noch Verantwortlichen (400); 200 mit dem Vermerk. Keine Antwort
        ändert eine Zahl (A5).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [antwort]
              properties:
                antwort: { type: string, enum: [abweichung, zur_kenntnis] }
                begruendung: { type: string, minLength: 10, maxLength: 500 }
                frist: { type: string, format: date }
                verantwortlich: { type: string, description: "sub eines aktiven Benutzers" }
      responses:
        "200":
          description: Zur Kenntnis genommen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuffaelligkeitBeantwortet" }
        "201":
          description: Abweichung eröffnet.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuffaelligkeitBeantwortet" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/abweichungen:
    get:
      tags: [kennzahlen]
      summary: Das Register der Abweichungen - Filter Zustand, überfällig, Kennzahl.
      description: >
        Recht `verbesserung.ansehen` (UEMS AP-18 IP-16); sichtbar sind die Abweichungen im Zaun (RLS über
        `standort_id`, eine am Unternehmen nur unternehmensweit, zusätzlich über die Kennzahl). `ueberfaellig` filtert
        über die Operation `frist` beim Abruf (Uhr der Kennzahlen, kein Läufer); `kennzahl` ist die ID einer sichtbaren
        Kennzahl (sonst 404); ein anderer Parameter 400.
      parameters:
        - { name: zustand, in: query, required: false, schema: { type: string, enum: [offen, abgeschlossen] } }
        - { name: ueberfaellig, in: query, required: false, schema: { type: string, enum: ["true", "false"] } }
        - { name: kennzahl, in: query, required: false, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Die sichtbaren Abweichungen, nach Kennzeichen, mit dem Abruf-Tag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AbweichungListe" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
    post:
      tags: [kennzahlen]
      summary: Eröffnet eine Abweichung AW-JJJJ-nnnn von Hand an Vergleichszeilen (A3).
      description: >
        Recht `verbesserung.verwalten` an der Geltung der Kennzahl (RE1, RE2). Anlass = KOPIE des Vergleich-Lesers über
        `monate` (`JJJJ-MM` oder `JJJJ-MM/JJJJ-MM`, höchstens 12, abgeschlossen - 422 `monate_nicht_abgeschlossen`),
        kanonischer Text mit Prüfsumme; alle Monate gegen EINE freigegebene Fassung (422 `monat_ohne_fassung`,
        `monate_verschiedene_fassungen`, ohne Bezugsbasis `kennzahl_ohne_bezugsbasis`); `bezugsbasis` (BB-…) wahlfrei.
        `wortlaut` 10–500, warum (422 `wortlaut_fehlt`) - auch an `im_rahmen`. `verantwortlich` aktives Konto,
        `frist` wie bei der Antwort. Protokoll `abweichung_eroeffnet`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [kennzahl, monate, wortlaut, verantwortlich]
              properties:
                kennzahl: { type: string, format: uuid }
                bezugsbasis: { type: string, pattern: "^BB-[0-9]{4,}$" }
                monate: { type: string, pattern: "^[0-9]{4}-(0[1-9]|1[0-2])(/[0-9]{4}-(0[1-9]|1[0-2]))?$" }
                wortlaut: { type: string, minLength: 10, maxLength: 500 }
                verantwortlich: { type: string, description: "sub eines aktiven Benutzers" }
                frist: { type: string, format: date }
      responses:
        "201":
          description: Eröffnet, offen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Abweichung" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/abweichungen/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [kennzahlen]
      summary: Die Abweichung mit Vermerken und Verlauf.
      description: Recht `verbesserung.ansehen`; Zaun über `standort_id` und die Kennzahl - außerhalb 404.
      responses:
        "200":
          description: Die Abweichung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Abweichung" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/abweichungen/{id}/eintraege:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Ein Kommentar oder eine Ursache-Aussage im Verlauf der Abweichung.
      description: >
        Recht `verbesserung.verwalten` (A4, U1–U3). Nur an einer offenen Abweichung (409 `abweichung_abgeschlossen`).
        `kommentar`: `text` 1–2 000 (422 `text_ungueltig`). `ursache_aussage`: `wortlaut` 10–500 (422
        `wortlaut_fehlt`), die aussagende Person als `aussage_sub` (aktives Konto, Name als Schnappschuss) und/oder
        `aussage_name` (422 `aussage_ohne_person`), `aussage_am` (nie in der Zukunft - 422 `aussage_in_der_zukunft`),
        wahlfrei `beleg_kennung`. Wer einträgt, muss nicht wer aussagt sein (R2). Eine Protokollzeile - nichts wird
        geändert, nichts gelöscht.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                art: { type: string, enum: [kommentar, ursache_aussage] }
                text: { type: string, minLength: 1, maxLength: 2000 }
                wortlaut: { type: string, minLength: 10, maxLength: 500 }
                aussage_sub: { type: string }
                aussage_name: { type: string }
                aussage_am: { type: string, format: date }
                beleg_kennung: { type: string, maxLength: 200 }
      responses:
        "201":
          description: Eingetragen; die Abweichung mit Verlauf.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Abweichung" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/abweichungen/{id}/frist:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [kennzahlen]
      summary: Setzt die Frist der offenen Abweichung mit Begründung.
      description: >
        Recht `verbesserung.verwalten` (A4). Nie vor dem Eröffnungstag (422 `frist_vor_eroeffnung`, Uhr der Kennzahlen,
        Zeitzone des Unternehmens); Begründung 10–500 (422 `begruendung_fehlt`); nur offen (409). Protokoll
        `abweichung_geaendert` mit alt/neu.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [frist, begruendung]
              properties:
                frist: { type: string, format: date }
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        "200":
          description: Geändert.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Abweichung" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/abweichungen/{id}/verantwortlicher:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [kennzahlen]
      summary: Setzt den Verantwortlichen der offenen Abweichung mit Begründung.
      description: >
        Recht `verbesserung.verwalten` (A4, RE3). Ein aktives Konto des Kundenbereichs (422 `benutzer_unbekannt`), sein
        Name als Schnappschuss; Begründung 10–500; nur offen (409). Protokoll `verantwortlicher_geaendert`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [benutzer, begruendung]
              properties:
                benutzer: { type: string }
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        "200":
          description: Geändert.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Abweichung" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/abweichungen/{id}/abschliessen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Schließt die Abweichung ab - einmalig, mit Ergebnis und Begründung.
      description: >
        Recht `verbesserung.abschliessen` an der Geltung der Kennzahl (A6; Bearbeiter 403 `recht_fehlt`). Einmalig (409
        `abweichung_abgeschlossen`). `ergebnis` `massnahme · erklaert · keine_abweichung · nicht_bewertbar`; bei
        `massnahme` ist `massnahme` Pflicht (422 `massnahme_fehlt`) und eine sichtbare Maßnahme (422
        `massnahme_unbekannt`), sonst verboten (400); Begründung 10–500 (422 `begruendung_fehlt`). Protokoll
        `abweichung_abgeschlossen`; nichts an der Kennzahl ändert sich (A5).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [ergebnis, begruendung]
              properties:
                ergebnis: { type: string, enum: [massnahme, erklaert, keine_abweichung, nicht_bewertbar] }
                begruendung: { type: string, minLength: 10, maxLength: 500 }
                massnahme: { type: string, format: uuid }
      responses:
        "200":
          description: Abgeschlossen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Abweichung" }
        "400": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "404": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerbesserungAbgelehnt" }
  /api/v1/bezugsbasen/uebersicht:
    get:
      tags: [kennzahlen]
      summary: Übersicht am Unternehmen - laufende Bezugsbasen nach Zustand und die fälligen Überprüfungen.
      description: >
        Recht `bezugsbasis.ansehen`, durchgesetzt über die Sichtbarkeit der Kennzahl (eine Basis an einer
        Kennzahl, die der Aufrufer nicht sieht, zählt nicht). UEMS AP-17 IP-17 (R13, §5.5): Zähler
        freigegeben · vorläufig · Anstoß liegt vor · Überprüfung fällig, dazu die fälligen Basen (am längsten
        fällig zuerst). Die Frist wird beim Abruf abgeleitet (Freigabetag bzw. jüngstes „bleibt“ +
        Wiedervorlage) - kein Läufer, kein Ereignis, keine Nachricht.
      responses:
        "200":
          description: Die Übersicht.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsbasisUebersicht" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/verbesserung/uebersicht:
    get:
      tags: [kennzahlen]
      summary: Übersicht „Ziele und Maßnahmen“ am Unternehmen - Zähler je Art und je fälligem Vorgang eine Zeile.
      description: >
        Recht `verbesserung.ansehen` (UEMS AP-18 IP-19, F1–F3, W7); gezählt wird nur, was der Aufrufer sieht (RLS
        über `standort_id` und die Kennzahl; ein Anstoß nur mit sichtbarem Vorgang, ein Messbedarf im Zaun seines
        Energieeinsatzes). Abruf-Tag ist die Uhr der Kennzahlen in der Zeitzone des Unternehmens; „überfällig seit n
        Tagen“ bzw. „Bewertung fällig seit n Tagen“ leitet die Operation `frist` beim Abruf ab - kein Läufer, kein
        Ereignis, keine Nachricht (E5 = A). Die Messbedarf-Frist (AP-16) wird nur gelesen.
      responses:
        "200":
          description: Die Übersicht.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/VerbesserungUebersicht" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/kennzahlen/{id}/bezugsbasen/{bid}/fassungen/{n}/beantragen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: bid, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: n, in: path, required: true, schema: { type: integer, minimum: 1 } }
    post:
      tags: [kennzahlen]
      summary: Beantragt die Freigabe eines Entwurfs bei Vier-Augen (AP-17 IP-8, F2).
      description: >
        Recht `bezugsbasis.freigeben` an der Geltung der Kennzahl (wer beantragt, ist die Freigabe-Person). Nur mit
        `unternehmen.vieraugen_freigabe`, sonst 409 `vieraugen_aus`; Begründung 10–500 Zeichen (Körper oder die des
        Entwurfs, sonst 422 `begruendung_fehlt`). Nachlese 2: die Grundlage wird neu gebildet und ihre Prüfsumme
        verglichen (abweichend 409 `entwurf_veraltet`); die statischen Faktoren werden am Antragstag neu bewertet und bei
        Änderung neu kopiert (Objekt nicht mehr gültig 422 `faktor_ungueltig`). Vertrag bezugsbasis.md §13, §17.
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BezugsbasisEntscheid" }
      responses:
        "200":
          description: Die beantragte Fassung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsbasisFassung" }
        "400": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "404": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "409": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "422": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
  /api/v1/kennzahlen/{id}/bezugsbasen/{bid}/fassungen/{n}/freigeben:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: bid, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: n, in: path, required: true, schema: { type: integer, minimum: 1 } }
    post:
      tags: [kennzahlen]
      summary: Gibt eine Fassung frei - direkt oder als zweite Person (AP-17 IP-8, F1, F2, F4).
      description: >
        Recht `bezugsbasis.freigeben`. Ohne Vier-Augen gibt die Person den Entwurf mit Begründung frei (mit Vier-Augen
        409 `vieraugen_beantragen`); mit Vier-Augen bestätigt eine zweite Person mit Rolle Kundenadministrator oder
        Energiemanager den Antrag (Urheber 422 `vieraugen_urheber`). `freigegeben_am` beginnt die Wiedervorlage; die
        laufende Vorgängerin endet am Vortag des `gilt_ab` (F4). Nachlese 2 (F3, E5/E6 = A): vor der Freigabe wird die
        Grundlage neu gebildet; weicht die Prüfsumme ab, ist der Entwurf bzw. Antrag veraltet (409 `entwurf_veraltet`
        mit `grund`, nichts wird freigegeben). Die statischen Faktoren werden am Freigabetag neu bewertet: geändert im
        Entwurf → neu kopiert (Grundlage und Prüfsumme neu), geändert nach dem Antrag → 409 `entwurf_veraltet`; ein
        verwiesenes Objekt, das am Freigabetag nicht mehr gilt → 422 `faktor_ungueltig`. Vertrag bezugsbasis.md §13, §17.
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BezugsbasisEntscheid" }
      responses:
        "200":
          description: Die freigegebene Fassung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsbasisFassung" }
        "400": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "404": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "409": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "422": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
  /api/v1/kennzahlen/{id}/bezugsbasen/{bid}/fassungen/{n}/ablehnen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: bid, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: n, in: path, required: true, schema: { type: integer, minimum: 1 } }
    post:
      tags: [kennzahlen]
      summary: Lehnt einen Antrag mit Begründung ab (AP-17 IP-8, F2).
      description: >
        Recht `bezugsbasis.freigeben`; nur ein Antrag, nur eine zweite Person (Urheber 422 `vieraugen_urheber`),
        Begründung Pflicht (422 `begruendung_fehlt`). Danach ist ein neuer Entwurf möglich. Vertrag bezugsbasis.md §13.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BezugsbasisEntscheid" }
      responses:
        "200":
          description: Die abgelehnte Fassung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsbasisFassung" }
        "400": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "404": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "409": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "422": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
  /api/v1/kennzahlen/{id}/bezugsbasen/{bid}/verantwortlicher:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: bid, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [kennzahlen]
      summary: Setzt den Verantwortlichen der Bezugsbasis (AP-17 IP-8, B4).
      description: >
        Recht `bezugsbasis.verwalten` an der Geltung der Kennzahl. Ein aktiver Benutzer des Kundenbereichs (sonst 422
        `benutzer_unbekannt`); sein Name wird als Schnappschuss gespeichert, Protokoll `verantwortlicher_geaendert`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BezugsbasisVerantwortlicher" }
      responses:
        "200":
          description: Die Bezugsbasis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Bezugsbasis" }
        "400": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "404": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "409": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
        "422": { $ref: "#/components/responses/BezugsbasisAbgelehnt" }
  /api/v1/kennzahl-vorlagen:
    get:
      tags: [kennzahlen]
      summary: Der Vorlagen-Katalog der Kennzahlen - acht VoltPilot-Vorlagen, die den Assistenten „Kennzahl anlegen“ vorbelegen.
      description: >
        Keine eigene Kennung (E9 - kein Kundenobjekt, kein Mandant, kein Recht für Vorlagen): jeder
        angemeldete Benutzer liest denselben Katalog. Die Antwort ist Knoten für Knoten die Datei
        `services/api/src/main/resources/kennzahlen/kennzahl-vorlagen.json` ohne ihre Entwickler-Felder
        (`_comment`, `$schema_datei`, `herkunft`); das Portal trägt eine byte-gleiche Kopie, die Form hält
        `docs/contracts/v2/kennzahl-vorlagen.schema.json` fest. Eine Vorlage ist nie eine Kennzahl: sie
        belegt Rechenform, Name (Platzhalter `{Geltungsbereich}`), Zweck und beim Anteil das Komplement
        vor und nennt die Erwartung an Menge und Bezugsgröße. Die Eingänge bindet der Kunde ausdrücklich;
        `POST /api/v1/kennzahlen/vorschau` prüft die vorbelegte Anfrage mit ihnen und schreibt nichts (K20).
      responses:
        "200":
          description: Der Katalog.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KennzahlVorlagen" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/kennzahlen:
    get:
      tags: [kennzahlen]
      summary: Die Kennzahlen des Kundenbereichs, archivierte eingeschlossen, nach Kennzeichen.
      description: >
        Recht `messwerte.ansehen` (AP-11 R2). Die Sichtbarkeit R-A1 und R-A6 je Person setzt
        der Geltungsbereich-Prüfpunkt durch (AP-03 IP-11).
      responses:
        "200":
          description: Die Kennzahlen (ein Objekt, kein nacktes Array - additiv erweiterbar).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KennzahlListe" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [kennzahlen]
      summary: Legt eine Kennzahl mit Fassung 1 „gilt seit Beginn“ an; ohne Kennzeichen vergibt der Server KZ-0001 …
      description: >
        Recht `kennzahl.standort_definieren` (Geltungsbereich Standort, Gebäude, Bereich, Messstelle - am Standort des Objekts) bzw. `kennzahl.unternehmen_definieren` (Unternehmen, Prozess, Kostenstelle) nach G1, durchgesetzt über die Rechte-Ableitung: 403 `recht_fehlt` mit `rolle_noetig` und dem Satz der Rechte-Ableitung; ein Standort außerhalb des Zugriffs ist 404 `nicht_gefunden` (fremd ist nicht da). Prüfreihenfolge: die Anfrage (400 `anfrage_ungueltig` mit `feld`,
        `kennzeichen_format`) → der Geltungsbereich (422 `geltung_unbekannt`) → das Recht (403/404)
        → das Kennzeichen (409 `kennzeichen_belegt`; auch das einer gelöschten Kennzahl bleibt
        belegt) → die Berechnung (422 `rechenform_unbekannt`, `eingang_unbekannt`,
        `einheit_unpassend`, `groesse_unbekannt`, `periode_passt_nicht`, `formel_zyklus`,
        `eingang_ausserhalb_geltung`) mit dem Satz der Regel. Eingänge werden über das
        Kennzeichen ihres Objekts genannt. `periode_art` ist ein geprüfter Wunsch, kein
        gespeichertes Feld. Die Anfrage wird streng gelesen (auch camelCase ist ein unbekanntes
        Feld). GENAU EIN Protokolleintrag `kennzahl_fassung_eingetragen`; eine Ablehnung schreibt
        nichts.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/KennzahlAnfrage" }
            example:
              name: Stromeinsatz Montage je Stück — Halle 2
              rechenform: quotient
              geltung_art: gebaeude
              geltung_id: 5b0d6f0e-8a3c-4f5e-9d2a-2f6f0c1b7a11
              zweck: Spezifischer Stromeinsatz der Montagelinie M1 je Gutteil.
              eingaenge:
                - { rolle: zaehler, art: messstelle, kennzeichen: MS-12 }
                - { rolle: nenner, art: bezugsgroesse, kennzeichen: BZ-6 }
      responses:
        "201":
          description: Angelegt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Kennzahl" }
        "400": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "404": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "409": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "422": { $ref: "#/components/responses/KennzahlAbgelehnt" }
  /api/v1/kennzahlen/vorschau:
    post:
      tags: [kennzahlen]
      summary: Prüft eine Kennzahl wie das Anlegen und rechnet die letzten drei abgeschlossenen Perioden - schreibt nichts.
      description: >
        Recht `kennzahl.standort_definieren` (Geltungsbereich Standort, Gebäude, Bereich, Messstelle - am Standort des Objekts) bzw. `kennzahl.unternehmen_definieren` (Unternehmen, Prozess, Kostenstelle) nach G1, durchgesetzt über die Rechte-Ableitung: 403 `recht_fehlt` mit `rolle_noetig` und dem Satz der Rechte-Ableitung; ein Standort außerhalb des Zugriffs ist 404 `nicht_gefunden` (fremd ist nicht da). Nur-Lese-Transaktion (Muster Import-Vorschau): kein Kennzeichen, keine
        Fassung, kein Protokoll. Dieselbe Anfrage wie das Anlegen (Name darf fehlen). Eine
        fachliche Ablehnung (409, 422 und eine Regel über die Eingänge) steht als Befund in
        `befunde`, die Perioden nur ohne Befund; die Form der Anfrage (400) und das Recht
        (403/404) antworten wie beim Anlegen. Werte der Eingänge werden aus ihren Lesemodellen
        gelesen, nie nachgerechnet; eine Kennzahl je Woche hat noch keine Vorschau-Perioden (IP-12).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/KennzahlAnfrage" }
      responses:
        "200":
          description: Die Befunde und die Perioden.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KennzahlVorschau" }
        "400": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "404": { $ref: "#/components/responses/KennzahlAbgelehnt" }
  /api/v1/kennzahlen/paare:
    parameters:
      - { name: rechenform, in: query, required: false, schema: { type: string, enum: [quotient, anteil, zusammenfassung] }, description: "Nur die Gruppe dieser Rechenform." }
      - { name: einheit, in: query, required: false, schema: { type: string, example: "kWh/Stück" }, description: "Nur die Gruppe dieser Einheit - wie `Kennzahl.einheit`, ungekürzt." }
      - { name: standort_id, in: query, required: false, schema: { type: string, format: uuid }, description: "Nur Kennzahlen dieses Standorts (`Kennzahl.standort_id`) - die Eingänge einer Standort-Kennzahl liegen im Standort." }
    get:
      tags: [kennzahlen]
      summary: Die möglichen Paare einer Zusammenfassung - gruppiert nach Rechenform und Einheit.
      description: >
        Recht `messwerte.ansehen` (AP-11 R2, keine Durchsetzung bis AP-03 IP-11). UEMS AP-11 IP-11,
        für den Assistenten „Kennzahl anlegen“ (Schritt „Kennzahlen“). Jede nicht archivierte Kennzahl
        mit heute geltender Berechnung steht in der Gruppe ihrer Rechenform und Einheit; eine
        Zusammenfassung nimmt mindestens zwei aus EINER Gruppe (R4 - sonst 422 `einheit_unpassend`
        beim Anlegen). Gruppen nach Rechenform (quotient, anteil, zusammenfassung), dann Einheit;
        Kennzahlen nach Kennzeichen; auch eine Gruppe mit nur einer Kennzahl steht da. Ein unbekannter
        oder leerer Parameter ist 400. Die Route rechnet nichts und schreibt nichts.
      responses:
        "200":
          description: Die Gruppen (ein Objekt, kein nacktes Array - additiv erweiterbar).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KennzahlPaare" }
        "400": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/kennzahlen/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [kennzahlen]
      summary: Eine Kennzahl mit der heute geltenden Berechnung.
      description: Recht `messwerte.ansehen`.
      responses:
        "200":
          description: Die Kennzahl.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Kennzahl" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KennzahlAbgelehnt" }
    put:
      tags: [kennzahlen]
      summary: Ändert die GANZEN Stammdaten (Kennzeichen, Name, Verantwortlich, Zweck) ohne Fassung.
      description: >
        Recht `kennzahl.standort_definieren` (Geltungsbereich Standort, Gebäude, Bereich, Messstelle - am Standort des Objekts) bzw. `kennzahl.unternehmen_definieren` (Unternehmen, Prozess, Kostenstelle) nach G1, durchgesetzt über die Rechte-Ableitung: 403 `recht_fehlt` mit `rolle_noetig` und dem Satz der Rechte-Ableitung; ein Standort außerhalb des Zugriffs ist 404 `nicht_gefunden` (fremd ist nicht da). V4: Geltungsbereich und Rechenform sind nach der ersten Fassung fest (ein Feld
        dafür ist 400). Eine archivierte Kennzahl ist 409 `archiviert`. Ein unverändertes PUT
        schreibt nichts, sonst genau ein Protokolleintrag `kennzahl_geaendert`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/KennzahlStammdatenAnfrage" }
      responses:
        "200":
          description: Geändert.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Kennzahl" }
        "400": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "404": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "409": { $ref: "#/components/responses/KennzahlAbgelehnt" }
    delete:
      tags: [kennzahlen]
      summary: Löscht eine Kennzahl ohne einen einzigen Wert, die keine andere Kennzahl liest.
      description: >
        Recht `kennzahl.standort_definieren` (Geltungsbereich Standort, Gebäude, Bereich, Messstelle - am Standort des Objekts) bzw. `kennzahl.unternehmen_definieren` (Unternehmen, Prozess, Kostenstelle) nach G1, durchgesetzt über die Rechte-Ableitung: 403 `recht_fehlt` mit `rolle_noetig` und dem Satz der Rechte-Ableitung; ein Standort außerhalb des Zugriffs ist 404 `nicht_gefunden` (fremd ist nicht da). V5: sonst 409 `hat_werte` (`werte`) bzw. `wird_gelesen` (`leser`) - archivieren
        Sie sie stattdessen. Das Kennzeichen bleibt belegt (Grabstein); das Protokoll behält einen
        Eintrag `kennzahl_geaendert` mit dem letzten Stand und leerem `neu`.
      responses:
        "204": { description: Gelöscht. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "404": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "409": { $ref: "#/components/responses/KennzahlAbgelehnt" }
  /api/v1/kennzahlen/{id}/archivieren:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [kennzahlen]
      summary: Archiviert eine Kennzahl - die Werte bleiben lesbar, kein Rechenlauf mehr.
      description: >
        Recht `kennzahl.standort_definieren` (Geltungsbereich Standort, Gebäude, Bereich, Messstelle - am Standort des Objekts) bzw. `kennzahl.unternehmen_definieren` (Unternehmen, Prozess, Kostenstelle) nach G1, durchgesetzt über die Rechte-Ableitung: 403 `recht_fehlt` mit `rolle_noetig` und dem Satz der Rechte-Ableitung; ein Standort außerhalb des Zugriffs ist 404 `nicht_gefunden` (fremd ist nicht da). V5; schon archiviert ist 409 `archiviert`. Zitiert ein freigegebener Leistungsvergleichs-Stand die Kennzahl (AP-17 IP-21b, S4), ist es 409 `berichts_belege` in der Form `BelegeImWeg` (`berichtsstaende` nennt jeden Stand) - nichts ist geschrieben. Genau ein Protokolleintrag `kennzahl_archiviert`.
      responses:
        "200":
          description: Archiviert.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Kennzahl" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "404": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "409": { $ref: "#/components/responses/KennzahlAbgelehnt" }
  /api/v1/kennzahlen/{id}/fassungen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [kennzahlen]
      summary: Jede Fassung der Berechnung, nach Nummer - Fassung 1 bleibt lesbar.
      description: Recht `messwerte.ansehen`.
      responses:
        "200":
          description: Die Fassungen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KennzahlFassungen" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KennzahlAbgelehnt" }
    post:
      tags: [kennzahlen]
      summary: Trägt die Berechnung ab einem Tag als Fassung n + 1 ein - mit Begründung, auch rückwirkend.
      description: >
        Recht `kennzahl.standort_definieren` (Geltungsbereich Standort, Gebäude, Bereich, Messstelle - am Standort des Objekts) bzw. `kennzahl.unternehmen_definieren` (Unternehmen, Prozess, Kostenstelle) nach G1, durchgesetzt über die Rechte-Ableitung: 403 `recht_fehlt` mit `rolle_noetig` und dem Satz der Rechte-Ableitung; ein Standort außerhalb des Zugriffs ist 404 `nicht_gefunden` (fremd ist nicht da). V1 (Regel `fassungEintrag` der Formel-Fassung): Fassung n + 1 beginnt an
        `gueltig_ab` und beendet die laufende am Vortag; ein Tag vor dem Eintragstag trägt
        „rückwirkend (n Tage)“; ein Tag an oder vor dem Beginn der jüngsten ist 422
        `fassung_ueberlappt`. Rechenform und Geltungsbereich bleiben; der Kreis wird am Tag
        `gueltig_ab` geprüft (`formel_zyklus` mit `kette`), G3 ebenso. Eine archivierte Kennzahl
        ist 409. Genau ein Protokolleintrag `kennzahl_fassung_eingetragen` mit „gilt ab“,
        rückwirkend und Begründung.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/KennzahlFassungAnfrage" }
            example:
              gueltig_ab: "2027-03-01"
              begruendung: Seit dem Umzug des Kaltwassersatzes gehört die Prozesskühlung zum Stromeinsatz Spritzguss.
              eingaenge:
                - { rolle: zaehler, art: messstelle, kennzeichen: MS-24 }
                - { rolle: nenner, art: bezugsgroesse, kennzeichen: BZ-1 }
      responses:
        "200":
          description: Eingetragen - alle Fassungen danach.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KennzahlFassungen" }
        "400": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "404": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "409": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "422": { $ref: "#/components/responses/KennzahlAbgelehnt" }
  /api/v1/kennzahlen/{id}/berechnung:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [kennzahlen]
      summary: Die Fassung der Berechnung, die an einem Tag galt.
      description: Recht `messwerte.ansehen`. Ohne `am` gilt heute in der Zeitzone des Standorts (sonst des Unternehmens).
      parameters:
        - { name: am, in: query, required: false, schema: { type: string, format: date }, description: "Der Tag (JJJJ-MM-TT)." }
      responses:
        "200":
          description: Die Fassung am Tag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KennzahlBerechnung" }
        "400": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KennzahlAbgelehnt" }
  /api/v1/kennzahlen/{id}/faktoren-vorschlag:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: stichtag, in: query, required: false, schema: { type: string, format: date, example: "2026-10-01" }, description: "Der Tag (JJJJ-MM-TT); ohne ihn heute in der Zeitzone der Kennzahl." }
    get:
      tags: [kennzahlen]
      summary: Die statischen Faktoren, die die Struktur der Geltung einer Kennzahl am Stichtag vorschlägt - nichts wird gespeichert.
      description: >
        Recht `messwerte.ansehen`, Zaun über die Kennzahl (wer sie nicht ganz sieht, bekommt 404, nie 403).
        UEMS AP-17 IP-16a (V3, E6 = A), Vertrag docs/contracts/v2/bezugsbasis.md §14. Aus der Struktur der
        Geltung am Stichtag: die Fläche je Ort (mit Zahl und Einheit m²) und als Summe, Standorte, Anlagen,
        Prozesse (eine Ebene) und Kostenstellen (je als Verweis ohne Zahl) - jeder Kandidat mit Gültigkeit
        und Kundensatz. Ein Objekt ohne Gültigkeit am Stichtag entfällt; fehlt einem Ort der Geltung die
        Fläche, ist die Summe null mit `ohne_flaeche`, nie 0. Ein Lese-Weg: keine Zeile wird geschrieben;
        die Liste an der Fassung mit der Kopie zum Freigabetag baut IP-16b. Ein unbekannter Parameter ist 400.
      responses:
        "200":
          description: Der Vorschlag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FaktorenVorschlag" }
        "400": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KennzahlAbgelehnt" }
  /api/v1/kennzahlen/{id}/werte:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: periode, in: query, required: true, schema: { type: string, enum: [tag, woche, monat, jahr] } }
      - { name: von, in: query, required: true, schema: { type: string, format: date, example: "2026-10-01" }, description: "Der ERSTE Tag einer Periode (JJJJ-MM-TT)." }
      - { name: bis, in: query, required: true, schema: { type: string, format: date, example: "2026-10-31" }, description: "Der LETZTE Tag einer Periode, einschließlich - nicht vor `von`, höchstens 2 200 Perioden." }
      - { name: version, in: query, required: false, schema: { type: integer, minimum: 1 }, description: "Ohne Angabe zeigt jeder Schritt seine NEUESTE Version; mit `version=n` genau Version n - die damalige Zahl aus ihrer Zeile. Fehlt n an einem Schritt, sagt er `version_nicht_gespeichert` und nennt in `versionen` seine neueste; gibt es n an KEINEM Schritt, ist die Anfrage 404 `version_gibt_es_nicht`." }
    get:
      tags: [kennzahlen]
      summary: Die Werte einer Kennzahl je Periode - ungerundet, nie ohne Zustand, Kennzeichen, Version und Herkunft.
      description: >
        Recht `messwerte.ansehen` (AP-11 R2, keine Durchsetzung bis AP-03 IP-11). UEMS AP-11 IP-7.
        Gelesen wird `kennzahl_wert` (append-only): je Periode die neueste Zeile - höchste Version,
        dann jüngstes `berechnet_am`. Die Trägerform ist die des Messstellen-Werts
        (`MessstelleWerteWert`) mit Wert, Zähler und Nenner als DEZIMALTEXT - ungerundet, gerundet wird
        nur im Portal (U4) -, dazu `richtung`, `definition_fassung` (V2) und die Einheit der gelesenen
        Fassung. Jedes Feld steht immer da; `null` heißt unbekannt oder nicht gebildet, nie 0. Eine
        Periode ohne Zeile sagt `grund` `noch_nicht_gebildet`. `herkunft` ist die Hülle `{satz, fehlt}`
        nach docs/contracts/v2/kennzahlwert-herkunft.schema.json, gebaut aus dem, was die Zeile beim
        Bilden las - an jeder Version, auch ohne Zahl (K19); ohne Version null. Eine Zeit-Periode, die
        der Rechenlauf über ihre eigenen Teilperioden bildet, nennt bei einer Zusammenfassung ihre Paare
        in derselben Periode (IP-11, K14); jede andere hat keine gespeicherten Eingänge: `satz`
        null, `fehlt` [eingaenge]. Ein unbekannter Parameter ist 400. Eine fremde Kennzahl ist 404, nie 403.
      responses:
        "200":
          description: Die Werte.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KennzahlWerte" }
        "400": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404":
          description: Die Kennzahl gibt es nicht (`nicht_gefunden`) - oder die angefragte Version an keinem Schritt.
          content:
            application/json:
              schema:
                oneOf:
                  - { $ref: "#/components/schemas/MessstelleWerteVersionGibtEsNicht" }
                  - { $ref: "#/components/schemas/KennzahlFehler" }
  /api/v1/kennzahlen/{id}/werte/versionen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: periode, in: query, required: true, schema: { type: string, enum: [tag, woche, monat, jahr] } }
      - { name: von, in: query, required: true, schema: { type: string, format: date, example: "2026-10-01" }, description: "Der ERSTE Tag der Periode - genau eine Periode." }
    get:
      tags: [kennzahlen]
      summary: Die Versions-Historie einer Periode - was vorher dastand, was jetzt, wer es wann warum geändert hat.
      description: >
        Recht `messwerte.ansehen` (AP-11 V6). Muster AP-08 IP-18. Je Version der Wert, genau wie
        `…/werte?version=n` ihn zeigt, der Wert davor, `gebildet_am` (die erste Zeile der Version) und
        `nachgezogen_am` (eine vorläufige Version zieht als weitere Zeile derselben Nummer nach), der
        Anlass wie gespeichert und die Entscheidungen: nennt der Beleg eine Korrektur (`K-…`) oder einen
        Ersatzwert (`EW-…`), deren Fassung bis zum Bilden der Version - gesprochen wie an der Messstelle;
        ist der Anlass `definition`, die Fassung der Berechnung mit ihrem Urheber und ihrer Begründung.
        Ein Beleg ohne lesbaren Vorgang (etwa ein Import `I-…`) hat keine Entscheidung - sein Text steht im
        Anlass. `grund` nur ohne Version: `noch_nicht_gebildet` oder der gespeicherte Grund der Zeile.
      responses:
        "200":
          description: Die Versionen, Version 1 zuerst.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KennzahlWerteHistorie" }
        "400": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KennzahlAbgelehnt" }
  /api/v1/kennzahlen/{id}/variablen-vorschlag:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: referenzperiode, in: query, required: false, schema: { type: string, pattern: "^[0-9]{4}-[0-9]{2}/[0-9]{4}-[0-9]{2}$", example: "2026-11/2027-10" }, description: "Die Monate der Abhängigkeits-Prüfung (G4), beide eingeschlossen, höchstens 120. Ohne Angabe die zwölf abgeschlossenen Monate vor dem laufenden (Zeitzone der Geltung)." }
    get:
      tags: [kennzahlen]
      summary: Die Einflussgrößen des Energieeinsatzes als Variablen-Vorschlag - Verweise als Kandidaten, Wortlaute ohne Zahl; übernimmt nichts.
      description: >
        Recht `messwerte.ansehen` wie die Kennzahl selbst (Zaun über die Kennzahl; fremd oder verborgen ist 404,
        nie 403). UEMS AP-17 IP-11a (V4, E3 = A, W1); Vertrag docs/contracts/v2/kennzahl-variablen-vorschlag.md.
        Gelesen werden die Einflussgrößen der laufenden Energieeinsätze, deren Prozess die Geltung der Kennzahl ist
        (`bezug` prozess) - sonst derer, deren Prozess eine Zähler-Messstelle der heute geltenden Fassung
        zugeordnet ist (`bezug` zaehler_messstellen); ohne beides `bezug` keiner mit `satz` und leeren Listen.
        Verweise (BZ-…) werden `kandidaten` mit ihrer Bezugsgröße: der Nenner ist `variable_1`, ein Stammdatum
        ein `statischer_faktor`, jede andere eine `variable` mit dem Hinweis `abhaengigkeit` gegen Variable 1
        (Pearson-r über die Monatspaare der Referenzperiode, |r| ≥ 0,9 → `variablen_abhaengig`, unter drei
        Paaren `nicht_pruefbar`; docs/contracts/v2/variablen-vorschlag-vectors.json). Wortlaute werden
        `ohne_zahl`-Zeilen „ohne Zahl — erst als Bezugsgröße erfassen“. Nichts wird übernommen, nichts am
        Einsatz, an der Kennzahl oder an einer Bezugsgröße verändert (AP-16 B5). Ein unbekannter Parameter ist 400.
      responses:
        "200":
          description: Der Vorschlag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KennzahlVariablenVorschlag" }
        "400": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KennzahlAbgelehnt" }
  /api/v1/kennzahlen/{id}/vergleich:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: basis, in: query, required: false, schema: { type: string, pattern: "^BB-[0-9]{4,}$", example: "BB-0001" }, description: "Die Bezugsbasis dieser Kennzahl; ohne Angabe die laufende, sonst die zuletzt beendete. Eine Basis einer anderen Kennzahl ist 404." }
      - { name: von, in: query, required: false, schema: { type: string, pattern: "^[0-9]{4}-(0[1-9]|1[0-2])$", example: "2027-11" }, description: "Erster Monat (JJJJ-MM). Ohne von/bis die zwölf abgeschlossenen Monate vor dem laufenden (Zeitzone der Geltung); höchstens 120 Monate." }
      - { name: bis, in: query, required: false, schema: { type: string, pattern: "^[0-9]{4}-(0[1-9]|1[0-2])$", example: "2028-02" }, description: "Letzter Monat (JJJJ-MM), eingeschlossen." }
    get:
      tags: [kennzahlen]
      summary: Der Vergleich mit der Bezugsbasis - je Monat gemessen, Bedingung, erwartet, Δ, Urteil mit Band; roh daneben ohne Urteil; Zeitraum Σ ÷ Σ.
      description: >
        Recht `bezugsbasis.ansehen` (Zaun über die Kennzahl; fremd oder verborgen ist 404, nie 403). UEMS AP-17
        IP-19 (U1–U6, E7 = A, E8 = A); Vertrag docs/contracts/v2/bezugsbasis.md §16. Je Monat `roh` (Veränderung
        zum Vormonat, Operation `roh`, immer `ohne_urteil`) und `bereinigt` (Operation `vergleich` gegen die
        freigegebene Fassung, die am letzten Tag des Monats gilt, P4): gemessen = gespeicherter Zähler der Kennzahl
        mit Version, `bedingung` = jede Variable mit Wert und Fassung, erwartet, Δ, Band = max(Toleranz, Streuung),
        Urteil oder Grund, Kennzeichen nach G5. `zeitraum` = Operation `zeitraum` gegen die Fassung am letzten Tag
        von `bis` - Σ gemessen ÷ Σ erwartet, nie ein Mittel (U5). Gerechnet wird in BezugsbasisRegeln; nichts wird
        gespeichert. Ein unbekannter oder falsch geformter Parameter ist 400 `anfrage_ungueltig` mit `feld`.
      responses:
        "200":
          description: Der Vergleich.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsbasisVergleich" }
        "400": { $ref: "#/components/responses/KennzahlAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KennzahlAbgelehnt" }
  /api/v1/bezugsdaten/vorlagen:
    get:
      tags: [bezugsgroessen]
      summary: Die aktuellen Fassungen der Zuordnungs-Vorlagen des Kundenbereichs (AP-09 IP-14).
      description: Lesend - keine eigene Kennung. Die Mandanten-RLS begrenzt die Liste auf den Kundenbereich.
      responses:
        "200":
          description: Je Vorlage die aktuelle Fassung mit Zuordnung, Urheber und Zeitpunkt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsdatenVorlageListe" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [bezugsgroessen]
      summary: Legt eine Vorlage oder die nächste Fassung einer Vorlage an.
      description: >
        Recht `bezugsgroesse.importieren`. Ohne `vorlage_id` entsteht eine neue Vorlage mit Fassung 1;
        mit `vorlage_id` die nächste Fassung. Keine Fassung wird überschrieben. Ein Import behält den
        Verweis auf die beim Import benutzte Fassung.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BezugsdatenVorlageAnfrage" }
      responses:
        "201":
          description: Die angelegte Fassung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsdatenVorlage" }
        "400": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
  /api/v1/bezugsdaten/importe:
    get:
      tags: [bezugsgroessen]
      summary: Import-Protokoll des Kundenbereichs lesen.
      description: Recht `bezugsgroesse.importieren`; jeder Eintrag wird zusätzlich an seinen Bezugsgrößen-Zielen geprüft.
      responses:
        "200":
          description: Jüngster Status und unveränderlicher Dateibeleg aller sichtbaren Importe.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsdatenImportProtokoll" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [bezugsgroessen]
      summary: Einen geprüften CSV-Import atomar übernehmen (AP-09 IP-13).
      description: >
        Recht `bezugsgroesse.importieren`, zusätzlich je Bezugsgrößen-Ziel geprüft.
        Datei und Zuordnung werden unter der Mandantensperre erneut geprüft. Die Vorschau-Kennung
        muss noch gelten. Konflikte bleiben ohne Entscheidung erhalten; ersetzen braucht eine
        Begründung. Teilübernahme braucht den exakten Bestätigungssatz. Bei Vier-Augen entstehen
        Berichtigungs-Vorschläge; Erstwerte wirken sofort. Abbruch schreibt weder Werte noch Import-Zeilen.
        Eine übernommene Zeile auf eine an das Wetter-Archiv gebundene Gradtagzahl bricht mit 409
        `wetterbezug_vorhanden` ab (AP-17 Nachlese 1; eine Bezugsgröße, eine Quelle).
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [datei, bestaetigung]
              properties:
                datei: { type: string, format: binary }
                zuordnung: { type: string, description: JSON der Form BezugsdatenZuordnung; genau eines aus zuordnung und vorlage_id. }
                vorlage_id: { type: string, format: uuid, description: Verwendet die aktuelle unveränderliche Vorlagenfassung. }
                bestaetigung: { type: string, description: JSON der Form BezugsdatenImportBestaetigung. }
            encoding:
              zuordnung: { contentType: application/json }
              bestaetigung: { contentType: application/json }
      responses:
        "200":
          description: Übernahme, Wiederholung oder protokollierter verworfener Import ohne gültige Zeile.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsdatenImportErgebnis" }
        "400": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { description: "Offener Vorschlag, gleichzeitig veränderter Wert oder `wetterbezug_vorhanden` (Gradtagzahl mit Wetterbezug)." }
        "422": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
  /api/v1/bezugsdaten/importe/{kennung}:
    get:
      tags: [bezugsgroessen]
      summary: Status und offene Vorschläge eines Imports.
      description: Lesend keine eigene Kennung; der Dienst prüft jedes Ziel, ein fremder Import ist 404.
      parameters:
        - { name: kennung, in: path, required: true, schema: { type: string, pattern: '^I-[0-9]{4}-[0-9]{4,}$' } }
      responses:
        "200":
          description: Der jüngste Import-Status, wirksame Änderungen und offene Vorschläge.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsdatenImportProtokollEintrag" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/bezugsdaten/importe/{kennung}/ruecknahme:
    post:
      tags: [bezugsgroessen]
      summary: Einen Import durch Folgefassungen zurücknehmen.
      description: >
        Recht `bezugsgroesse.importieren`. Neue Erstwerte werden ohne Betrag zurückgenommen,
        Berichtigungen stellen den vorherigen Betrag wieder her. Eine spätere Änderung sperrt
        die gesamte Rücknahme mit 409. Bei Vier-Augen wird ein gemeinsamer Vorschlag angelegt.
      parameters:
        - { name: kennung, in: path, required: true, schema: { type: string, pattern: '^I-[0-9]{4}-[0-9]{4,}$' } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BezugsdatenImportGrund" }
      responses:
        "200":
          description: Zurückgenommen oder zur Freigabe vorgeschlagen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsdatenImportErgebnis" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { description: Der betroffene Wert wurde inzwischen geändert oder ein Vorschlag ist offen. }
        "422": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
  /api/v1/bezugsdaten/importe/{kennung}/ruecknahme/vorschau:
    get:
      tags: [bezugsgroessen]
      summary: Folgen einer Import-Rücknahme zeigen, ohne zu schreiben.
      description: Recht `bezugsgroesse.importieren`; verwendet dieselbe Planung wie die schreibende Rücknahme und schreibt nichts.
      parameters:
        - { name: kennung, in: path, required: true, schema: { type: string, pattern: '^I-[0-9]{4}-[0-9]{4,}$' } }
      responses:
        "200":
          description: Jeder Wert und die Folgefassung, welche die Rücknahme erzeugen würde.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsdatenRuecknahmeVorschau" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { description: Der betroffene Wert wurde inzwischen geändert oder ein Vorschlag ist offen. }
  /api/v1/bezugsdaten/importe/{kennung}/freigeben:
    post:
      tags: [bezugsgroessen]
      summary: Alle Berichtigungen oder die Rücknahme eines Imports gemeinsam freigeben.
      description: >
        Recht `korrektur.freigeben`; dieselbe Freigabeprüfung wie Einzelberichtigungen.
        Bei Vier-Augen darf der Ersteller nicht selbst freigeben, ein Unterstützer nie.
        Alle erwarteten Wert-Fassungen müssen noch gelten, sonst schreibt die Freigabe nichts.
      parameters:
        - { name: kennung, in: path, required: true, schema: { type: string, pattern: '^I-[0-9]{4}-[0-9]{4,}$' } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BezugsdatenImportGrund" }
      responses:
        "200":
          description: Freigegeben; der aktuelle Import-Status.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsdatenImportErgebnis" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { description: Kein offener Vorschlag oder gleichzeitig veränderter Wert. }
        "422": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
  /api/v1/bezugsdaten/importe/vorschau:
    post:
      tags: [bezugsgroessen]
      summary: Die Vorschau eines CSV-Imports - sie zeigt jedes Urteil und schreibt nichts (AP-09 IP-12).
      description: >
        Recht `bezugsgroesse.importieren` (AP-09 §4.11). Liest die Datei (C1: ≤ 5 242 880 Bytes,
        ≤ 100 000 Datenzeilen, UTF-8 oder Windows-1252), wendet die Zuordnung an (C3) und urteilt je
        Datenzeile in der Prüfreihenfolge Zahl → Einheit → Periode/Zeit → Bezug → Schlüssel (C5, Befunde
        C8 mit Kundensatz). SCHREIBT NICHTS - keinen Wert, keinen Import, keine Vorlage, und nicht die
        Datei (E14); zweimal dieselbe Anfrage gegen denselben Bestand ist dieselbe Antwort. Fingerabdruck
        der Datei = SHA-256 der Bytes; Fingerabdruck der Zeile = SHA-256 von Bezugsgröße + Periode bzw.
        Zeitpunkt + Betrag in der Einheit der Bezugsgröße (C2/E8), nie der Zeilentext. `datei_bekannt` ist
        ein HINWEIS, keine Abweisung. Die `vorschau.kennung` gehört 30 Minuten lang zu genau diesem
        Ergebnis; sie reserviert nichts - die Übernahme (IP-13) bringt die Datei noch einmal mit, rechnet
        neu und nennt eine abgelaufene oder veraltete Kennung. Was an der Zuordnung nicht stimmt, ist 400
        `anfrage_ungueltig` mit `feld`; was an der DATEI nicht stimmt, ist ein Befund in `datei.befund`
        (200), eine Datei über der Upload-Grenze 413 mit demselben Befund.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [datei]
              oneOf:
                - required: [zuordnung]
                - required: [vorlage_id]
              properties:
                datei: { type: string, format: binary, description: "Die CSV-Datei (C1)." }
                zuordnung:
                  type: string
                  description: "JSON der Form BezugsdatenZuordnung (C3)."
                vorlage_id:
                  type: string
                  format: uuid
                  description: "Statt `zuordnung`: verwendet die aktuelle Fassung dieser Vorlage und nennt sie in der Antwort."
            encoding:
              zuordnung: { contentType: application/json }
      responses:
        "200":
          description: Die Vorschau.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BezugsdatenImportVorschau" }
        "400": { $ref: "#/components/responses/BezugsgroesseAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "413":
          description: Die Datei ist größer als 5 242 880 Bytes - Befund `datei_zu_gross`, Zusatz `zu_viele_bytes`.
  /api/v1/unternehmen/kostenstellen:
    get:
      tags: [kostenstellen-prozesse]
      summary: Die Kostenstelle-Objekte des Unternehmens; mit `stichtag` nur die an dem Tag bestehenden.
      description: >
        Heute lesend - keine eigene Kennung (wie das Unternehmen). Ohne Stichtag alle, beendete eingeschlossen, nach Kennzeichen.
        Der Auswahl-Katalog (Standort-Zaun, Entscheid 21.09.2026, Lesart B): eine unternehmensweite Rolle, ein Aufruf
        ohne Zugriff-Kontext und der Umschalter sehen jede Zeile ganz. Ein Konto, das `messstelle.bearbeiten` oder
        `messstelle.verteilung` im Unternehmen oder an einem seiner Standorte hat, sieht jede Zeile nur mit ihren
        Stammdaten - Kennung, Kennzeichen, Name, Gültigkeit; `angelegt_am` fehlt dann. Jedes andere Konto bekommt
        eine leere Liste, ohne Hinweis und ohne Anzahl. Detail und Bilanz bleiben den unternehmensweiten Rollen.
      parameters:
        - { name: stichtag, in: query, required: false, schema: { type: string, format: date } }
      responses:
        "200":
          description: Die Liste (ein Objekt, kein nacktes Array - additiv erweiterbar).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KostenstelleListe" }
        "400": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [kostenstellen-prozesse]
      summary: Legt an (Kennzeichen, Name, gültig ab, wahlweise gültig bis).
      description: >
        Recht `kostenstelle.verwalten` (AP-10 §4.10, E15). Reihenfolge: Form (400) → Kennzeichen-Form
        (422 `kennzeichen_format`) → Zeitraum (422 `zeitraum_ungueltig`) → Unternehmen da
        (409 `unternehmen_nicht_angelegt`) → Kennzeichen frei (409 `kennzeichen_belegt`,
        auch das eines beendeten Objekts).
        Die Kostenstelle ist flach: `eltern_id` gibt es an ihr nicht (400 `anfrage_ungueltig`).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/KostenstelleProzessAnlegen" }
            example:
              kennzeichen: "9010"
              name: Druckluft
              gueltig_ab: "2026-10-01"
      responses:
        "201":
          description: Angelegt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Kostenstelle" }
        "400": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "422": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
  /api/v1/unternehmen/kostenstellen/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [kostenstellen-prozesse]
      summary: Ein Objekt.
      description: Heute lesend - keine eigene Kennung. Fremd oder unbekannt = 404 `nicht_gefunden`.
      responses:
        "200":
          description: Das Objekt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Kostenstelle" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
    put:
      tags: [kostenstellen-prozesse]
      summary: Benennt um - Kennzeichen, Beginn und Elternteil bleiben.
      description: Recht `kostenstelle.verwalten`. Nur `name`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/KostenstelleProzessUmbenennen" }
      responses:
        "200":
          description: Umbenannt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Kostenstelle" }
        "400": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
  /api/v1/unternehmen/kostenstellen/{id}/beenden:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [kostenstellen-prozesse]
      summary: Beendet (der letzte Tag) - beenden statt löschen.
      description: >
        Recht `kostenstelle.verwalten`. Ein Ende wird nur vorgezogen: ein späteres oder gleiches Ende eines
        beendeten Objekts ist 409 `bereits_beendet` (Muster „nur verkürzt“). Ein Tag
        vor dem Beginn ist 422 `zeitraum_ungueltig`. Gelten Zuordnungen über das Ende hinaus
        (Messstellen, Unterprozesse - und seit IP-8 die Anteile einer Verteilung, `art` verteilung), antwortet
        409 `zuordnung_besteht` mit `zuordnungen` - gekürzt oder gelöscht wird nichts.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/KostenstelleProzessBeenden" }
            example:
              gueltig_bis: "2026-12-31"
      responses:
        "200":
          description: Beendet.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Kostenstelle" }
        "400": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "409": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "422": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
  /api/v1/unternehmen/kostenstellen/{id}/energie:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [kostenstellen-prozesse]
      summary: Was eine Kostenstelle verbraucht hat - gemessen, verteilt, berechnet - und daneben, was niemandem gehört (nicht verteilt).
      description: >
        Recht `messstelle.ansehen` (AP-10 §4.10, eingetragen, nicht durchgesetzt). Die Periode `tag` ·
        `monat` (Vorgabe) · `jahr`, die `am` enthält (Vorgabe heute, Zeitzone des Unternehmens).
        Vier Herkünfte nebeneinander (KostenstelleEnergieRegeln, Regel `kostenstelle` in
        verteilung-vectors.json): `gemessen` = eine gemessene Messstelle geht an dem Tag zu 100 % an
        die Kostenstelle, `verteilt` = zu einem Anteil unter 100 %, `berechnet` = eine berechnete
        Messstelle (gleich zu welchem Anteil), `nicht_verteilt` = ein Tageswert ohne Verteilungszeile.
        `nicht_verteilt` gehört KEINER Kostenstelle: es steht in jeder Kostenstellen-Sicht daneben,
        wird nie aufgeteilt, nie weggelassen und zählt in `summe` nie mit. Jeder Tag wird mit SEINEM
        Anteil verteilt (E12, `tage`); die Periode ist die Summe der Tage. Ohne Zuordnung KEINE Menge
        (`menge` null, `grund` `keine_zuordnung`) - nie 0; verschiedene Größen, Richtungen oder
        Einheiten werden nie zu einer Zahl (`groessen_gemischt`, je Größe eine `summen`-Zeile).
        `version`: je Tag die höchste Version bis dahin (Korrektur-Kaskade, `messreihe_periode_version`);
        ohne Angabe die neueste - mit `version=1` stehen die Zahlen, wie sie vor jeder Korrektur galten.
        Ein Posten trägt die höchste Version seiner Tage mit EINMAL „korrigiert (Version n)“ und seine
        Herkunft nach bilanzwert-herkunft (Art `verteilt`). Nichts wird gespeichert; `berechnet_am` ist
        der Zeitpunkt dieser Sicht. Liest nur. `doppelzaehlung` (Captain-Entscheid 14.09.2026) warnt,
        welcher Posten in welchem bereits enthalten ist (Regel `doppelzaehlung`, Formeln je Tag, rekursiv,
        Kreis benannt) - und ändert keine Zahl: kein Posten fehlt, keine Summe wird bereinigt.
      parameters:
        - { name: periode, in: query, required: false, schema: { type: string, enum: [tag, monat, jahr] } }
        - { name: am, in: query, required: false, schema: { type: string, format: date } }
        - { name: version, in: query, required: false, schema: { type: integer, minimum: 1 } }
      responses:
        "200":
          description: Die Kostenstellen-Sicht.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KostenstelleEnergie" }
        "400": { $ref: "#/components/responses/BilanzAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/unternehmen/bewertung/rangliste:
    get:
      tags: [energieeinsaetze]
      summary: Mengen, Kriterien-Urteile und Vorschläge der Energieeinsätze.
      description: >
        Recht `energieeinsatz.ansehen`. Laufende Einsätze und heutige Umfangsfassung, Anlagen und direkte Prozesszuordnungen
        am letzten Tag der Datengrundlage. Ganze Kalendermonate, letzter Tag eingeschlossen.
        Standortrollen erhalten teilansicht=true mit ausschließlich sichtbaren Anlagen und Messstellen;
        deren Nenner und x von y gelten nur für diese Teilansicht. Fehlt darin eine Bilanz, sind der
        Nenner null und alle Stromanteile unvollständig. Ohne Hauptzähler Bezug heißt die Anlage
        ohne Bilanz. Mengenlose Einsätze tragen keine Werte, keine Null. Weitere Träger behalten
        ihre Einheit und haben keinen Anteil. Die wirksame Kriterien-Fassung erzeugt je Einsatz nur
        Urteil und Vorschlag, niemals eine Einstufung. Der Herkunftsentwurf nennt jede gelesene
        Monatszahl mit Version sowie die Bilanzwerte des Nenners; eine Teilansicht enthält nur Sichtbares.
      parameters:
        - { in: query, name: von, required: true, schema: { type: string, format: date }, description: Erster Tag des ersten Monats. }
        - { in: query, name: bis, required: true, schema: { type: string, format: date }, description: Letzter Tag des letzten Monats. }
      responses:
        "200":
          description: Mengen, Herkunft in Monatswerten, Nenner und nicht zugeordneter Rest je Anlage.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BewertungRangliste" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/unternehmen/bewertung/messabdeckung:
    get:
      tags: [energieeinsaetze]
      summary: Messabdeckung je Energieeinsatz und je Ort aus Monatswerten und Anlagenbilanzen.
      description: >
        Recht `energieeinsatz.ansehen`; keine Schreibrecht-Annotation. Derselbe Standort-Zaun,
        Betrachtungsumfang, Nenner, Messstellenmengen und Anlagenrest wie in der Rangliste.
        Ganze Kalendermonate, letzter Tag eingeschlossen. Gemessen sind ausschließlich direkte,
        nicht archivierte Messstellen mit Werten; berechnete Messstellen zählen nie. Geplant sind
        Messstellen ohne Datenquelle und — ab IP-19 über die benannte Naht — offene Messbedarfe;
        eine geplante Menge ist null, nie 0. Ersatz ist eine Teilmenge von gemessen. Ungemessen ist
        ausschließlich der Rest der jeweiligen Anlagenbilanz. K8 stammt aus der wirksamen
        Kriterien-Fassung. Standortrollen erhalten teilansicht=true und keine fremden Kennungen.
      parameters:
        - { in: query, name: von, required: true, schema: { type: string, format: date }, description: Erster Tag des ersten Monats. }
        - { in: query, name: bis, required: true, schema: { type: string, format: date }, description: Letzter Tag des letzten Monats. }
      responses:
        "200":
          description: Summe sowie vier Spalten je Einsatz und je Ort.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BewertungMessabdeckung" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/unternehmen/bewertung/kriterien:
    get:
      tags: [energieeinsaetze]
      summary: Wirksame Kriterien-Fassung oder Vertragsvorgabe lesen.
      description: >
        Recht energieeinsatz.ansehen; Unternehmens- oder sichtbarer Standortumfang.
        Ohne gespeicherte Fassung 1 mit Herkunft Vorgabe und gueltig_ab null, ohne Schreibwirkung.
        Beantragte und abgelehnte Fassungen sind nur in der Historie, nie wirksam.
      responses:
        "200":
          description: Wirksame Fassung; werte entspricht bei Fassung 1 startwerte aus bewertung-vectors.json.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BewertungKriterienFassung" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [energieeinsaetze]
      summary: Kriterien als nächste Fassung mit Begründung speichern.
      description: >
        Recht bewertung.kriterien (KA U, EM U). Vollständige Werte im Vertragsformat.
        Sichert beim ersten Ändern Fassung 1, danach stets nächste Nummer; alle Fassungen bleiben lesbar.
        Ohne Vier-Augen sofort ab heute in der Unternehmenszeitzone wirksam, sonst beantragt ohne gueltig_ab.
        Höchstens ein offener Antrag. Fassung und Protokoll atomar. Kein Anstoß (IP-23).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BewertungKriterienSpeichern" }
      responses:
        "200":
          description: Neue wirksame oder beantragte Fassung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BewertungKriterienFassung" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: freigabe_offen — zuerst den vorhandenen Antrag entscheiden.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BewertungKriterienFehler" }
        "422":
          description: begruendung_fehlt oder kriterien_ungueltig.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BewertungKriterienFehler" }
  /api/v1/unternehmen/bewertung/kriterien/fassungen:
    get:
      tags: [energieeinsaetze]
      summary: Alle Kriterien-Fassungen, jüngste zuerst, einschließlich Anträge und Ablehnungen.
      description: Recht energieeinsatz.ansehen; ohne Speicherung nur Fassung 1 als Vorgabe.
      responses:
        "200":
          description: Unverlierbare Historie.
          content:
            application/json:
              schema:
                type: object
                required: [fassungen]
                properties:
                  fassungen: { type: array, items: { $ref: "#/components/schemas/BewertungKriterienFassung" } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/unternehmen/bewertung/kriterien/{nummer}/freigeben:
    post:
      tags: [energieeinsaetze]
      summary: Beantragte Kriterien-Fassung durch eine zweite Person freigeben.
      description: >
        Recht bewertung.kriterien (KA U, EM U). Urheber ausgeschlossen, auch nach Abschalten der Vier-Augen-Einstellung.
        Freigabe wird ab dem Bestätigungstag in der Unternehmenszeitzone wirksam; Ablehnung erhält die bisher wirksame Fassung.
      parameters:
        - in: path
          name: nummer
          required: true
          schema: { type: integer, minimum: 2 }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              description: Optionale Begründung der Freigabe.
              properties:
                begruendung: { type: string, minLength: 1 }
      responses:
        "200":
          description: Entschiedene Fassung mit beiden Akteuren und Entscheidungszeitpunkt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BewertungKriterienFassung" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: Fehlendes Recht oder zweite_person_noetig.
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: bereits_entschieden — kein offener Antrag.
        "422":
          description: begruendung_fehlt bei Ablehnung.
  /api/v1/unternehmen/bewertung/kriterien/{nummer}/ablehnen:
    post:
      tags: [energieeinsaetze]
      summary: Beantragte Kriterien-Fassung durch eine zweite Person ablehnen.
      description: >
        Recht bewertung.kriterien (KA U, EM U). Urheber ausgeschlossen, auch nach Abschalten der Vier-Augen-Einstellung.
        Freigabe wird ab dem Bestätigungstag in der Unternehmenszeitzone wirksam; Ablehnung erhält die bisher wirksame Fassung.
      parameters:
        - in: path
          name: nummer
          required: true
          schema: { type: integer, minimum: 2 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [begruendung]
              properties:
                begruendung: { type: string, minLength: 1 }
      responses:
        "200":
          description: Entschiedene Fassung mit beiden Akteuren und Entscheidungszeitpunkt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BewertungKriterienFassung" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: Fehlendes Recht oder zweite_person_noetig.
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: bereits_entschieden — kein offener Antrag.
        "422":
          description: begruendung_fehlt bei Ablehnung.
  /api/v1/unternehmen/bewertung/umfang:
    get:
      tags: [energieeinsaetze]
      summary: Betrachtungsumfang am Stichtag oder ungespeicherte Vorgabe.
      description: >
        Recht `energieeinsatz.ansehen`. Sichtbare Standorte und deren zeitgültig gebundene Anlagen.
        Ohne gültige Fassung 200 mit fassung null, allen sichtbaren Standorten und Strom als Vorschlag.
        Standortrollen erhalten eine Teilansicht ohne fremde Namen oder Anlagenzahlen.
        Keine Bilanzrechnung; die Anlagenzahl ist ausschließlich y, kein erfundenes x von y.
      parameters:
        - in: query
          name: am
          description: Tag; Vorgabe heute in der Zeitzone des Unternehmens.
          schema: { type: string, format: date }
      responses:
        "200":
          description: Umfang oder Vorgabe; ein Standort ohne Anlage hat eine leere Liste und Anzahl 0.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BewertungUmfang" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [energieeinsaetze]
      summary: Umfang als neue Fassung speichern.
      description: >
        Recht `energieeinsatz.verwalten` (Kundenadministrator oder Energiemanager im Unternehmen).
        Unternehmenssperre, Fassung und Protokoll atomar. Identischer Inhalt einschließlich Gültigkeitsbeginn
        und Begründung liefert 200 ohne neue Fassung; Reihenfolge und Wiederholung von Standorten/Trägern zählen nicht.
        Der Beginn darf nicht vor dem Beginn der letzten Fassung liegen; derselbe Tag korrigiert ab diesem Tag.
        Die Antwort zeigt die neue Fassung an ihrem Gültigkeitsbeginn. Strom ist optional.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BewertungUmfangSpeichern" }
      responses:
        "200":
          description: Gespeicherte oder unveränderte Fassung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BewertungUmfang" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422":
          description: Begründung fehlt oder Angaben zum Umfang sind ungültig.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BewertungUmfangFehler" }
  /api/v1/unternehmen/bewertung/umfang/fassungen:
    get:
      tags: [energieeinsaetze]
      summary: Alle lesbaren Fassungen des Betrachtungsumfangs, jüngste zuerst.
      description: >
        Recht `energieeinsatz.ansehen`. Einschließlich aufgehobener Fassungen;
        Anlagen jeweils am Gültigkeitsbeginn der Fassung und unter dem aktuellen Standort-Zaun.
      responses:
        "200":
          description: Fassungen mit Akteur; ohne Fassung eine leere Liste.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BewertungUmfangHistorie" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/unternehmen/energieeinsaetze:
    get:
      tags: [energieeinsaetze]
      summary: Energieeinsätze — laufende zuerst, beendete danach.
      description: >
        Recht `energieeinsatz.ansehen`. Standort-Zaun über die Messstellen des Prozesses; außerhalb dieselbe 404 wie unbekannt.
      parameters:
        - in: query
          name: prozess
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergieeinsatzListe" }
        "400": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
    post:
      tags: [energieeinsaetze]
      summary: Energieeinsatz anlegen; je Prozess und Träger höchstens einer laufend.
      description: >
        Recht `energieeinsatz.verwalten`. Schreiben mit Akteur im Protokoll; beendete Einsätze bleiben lesbar.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergieeinsatzAnlegen" }
      responses:
        "201":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Energieeinsatz" }
        "400": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
  /api/v1/unternehmen/energieeinsaetze/vorschlaege:
    get:
      tags: [energieeinsaetze]
      summary: Prozesse ohne laufenden Einsatz für Strom.
      description: >
        Recht `energieeinsatz.ansehen`. Standort-Zaun über die Messstellen des Prozesses; außerhalb dieselbe 404 wie unbekannt.
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergieeinsatzVorschlaege" }
        "400": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
  /api/v1/unternehmen/energieeinsaetze/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [energieeinsaetze]
      summary: Energieeinsatz lesen.
      description: >
        Recht `energieeinsatz.ansehen`. Standort-Zaun über die Messstellen des Prozesses; außerhalb dieselbe 404 wie unbekannt.
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Energieeinsatz" }
        "400": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
    put:
      tags: [energieeinsaetze]
      summary: Name, Wortlaut und Verbraucher ändern.
      description: >
        Recht `energieeinsatz.verwalten`. Schreiben mit Akteur im Protokoll; beendete Einsätze bleiben lesbar.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergieeinsatzBearbeiten" }
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Energieeinsatz" }
        "400": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
  /api/v1/unternehmen/energieeinsaetze/{id}/beenden:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    post:
      tags: [energieeinsaetze]
      summary: Mit Grund beenden; der letzte Tag zählt mit.
      description: >
        Recht `energieeinsatz.verwalten`. Schreiben mit Akteur im Protokoll; beendete Einsätze bleiben lesbar.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergieeinsatzBeenden" }
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Energieeinsatz" }
        "400": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
  /api/v1/unternehmen/energieeinsaetze/{id}/einstufung:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    put:
      tags: [energieeinsaetze]
      summary: Begründete Einstufungs-Fassung einer Person setzen.
      description: >
        Recht `energieeinsatz.einstufen`. Der vollständige Herkunftssatz aus der Rangliste wird eingefroren;
        der Vorschlag ist keine Einstufung. Bei Vier-Augen bleibt die Fassung bis zur Bestätigung beantragt.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergieeinsatzEinstufungSpeichern" }
      responses:
        "200":
          description: Angelegte Fassung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergieeinsatzEinstufungFassung" }
        "400": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
  /api/v1/unternehmen/energieeinsaetze/{id}/einstufung/bestaetigen:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    post:
      tags: [energieeinsaetze]
      summary: Beantragte Einstufung durch eine zweite Person bestätigen.
      description: Recht `energieeinsatz.einstufen`; der Urheber darf nicht selbst bestätigen.
      responses:
        "200":
          description: Wirksame Fassung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergieeinsatzEinstufungFassung" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
  /api/v1/unternehmen/energieeinsaetze/{id}/einstufungen:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [energieeinsaetze]
      summary: Alle Einstufungs-Fassungen lesen.
      description: Recht `energieeinsatz.ansehen`; Rückstufungen entfernen keine frühere Fassung.
      responses:
        "200":
          description: Historie, jüngste Fassung zuerst.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergieeinsatzEinstufungHistorie" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
  /api/v1/unternehmen/energieeinsaetze/{id}/verantwortlicher:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    put:
      tags: [energieeinsaetze]
      summary: Verantwortlichen aus den Benutzern setzen oder aufheben.
      description: >
        Recht `energieeinsatz.verwalten`. Schreiben mit Akteur im Protokoll; beendete Einsätze bleiben lesbar.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergieeinsatzVerantwortlicherSetzen" }
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Energieeinsatz" }
        "400": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
  /api/v1/unternehmen/energieeinsaetze/{id}/einflussgroessen:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    put:
      tags: [energieeinsaetze]
      summary: Einflussgrößen ersetzen; Vorgänger bleiben erhalten.
      description: >
        Recht `energieeinsatz.verwalten`. Schreiben mit Akteur im Protokoll; beendete Einsätze bleiben lesbar.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EnergieeinsatzEinfluesseSetzen" }
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Energieeinsatz" }
        "400": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
  /api/v1/unternehmen/energieeinsaetze/{id}/protokoll:
    parameters:
      - in: path
        name: id
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [energieeinsaetze]
      summary: Änderungsprotokoll mit Akteur.
      description: >
        Recht `energieeinsatz.ansehen`. Standort-Zaun über die Messstellen des Prozesses; außerhalb dieselbe 404 wie unbekannt.
      responses:
        "200":
          description: Ergebnis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnergieeinsatzProtokoll" }
        "400": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
  /api/v1/unternehmen/messbedarf:
    get:
      tags: [energieeinsaetze]
      summary: Messbedarfe aller Energieeinsätze, wahlweise nur die an einem Standort.
      description: >-
        Recht `energieeinsatz.ansehen`; je Bedarf derselbe Standort-Zaun wie an seinem Energieeinsatz. Mit `standort`
        nur Bedarfe, deren strukturierter Ort (`ort_ziel`) heute an diesem Standort hängt; ein Bedarf nur mit
        Ort-Wortlaut steht an keinem Standort. Ein unbekannter oder nicht lesbarer Standort ist 404.
      parameters:
        - { name: standort, in: query, required: false, schema: { type: string, format: uuid } }
      responses:
        "200": { description: Messbedarfe, content: { application/json: { schema: { $ref: "#/components/schemas/MessbedarfListe" } } } }
        "400": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
  /api/v1/unternehmen/energieeinsaetze/{id}/messbedarf:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [energieeinsaetze]
      summary: Alle Messbedarfe des Einsatzes; auch eingelöste und verworfene bleiben lesbar.
      description: Recht `energieeinsatz.ansehen`; derselbe Standort-Zaun wie am Energieeinsatz.
      responses:
        "200": { description: Messbedarfe, content: { application/json: { schema: { $ref: "#/components/schemas/MessbedarfListe" } } } }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
    post:
      tags: [energieeinsaetze]
      summary: Einen offenen Messbedarf erfassen.
      description: Recht `energieeinsatz.verwalten`; schreibt Protokoll und `messbedarf_erfasst` atomar.
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/MessbedarfAnlegen" } } } }
      responses:
        "201": { description: Erfasst, content: { application/json: { schema: { $ref: "#/components/schemas/Messbedarf" } } } }
        "400": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "403": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
  /api/v1/unternehmen/energieeinsaetze/{id}/messbedarf/{messbedarfId}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: messbedarfId, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [energieeinsaetze]
      summary: Einen offenen Messbedarf bearbeiten.
      description: >-
        Recht `energieeinsatz.verwalten`; ersetzt Wortlaut, Ort, Größe und Frist vollständig. Ein abgeschlossener Bedarf
        bleibt unverändert (409 `messbedarf_abgeschlossen`); zitiert ihn ein freigegebener Berichtsstand, 409
        `berichts_belege`.
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/MessbedarfAnlegen" } } } }
      responses:
        "200": { description: Bearbeitet, content: { application/json: { schema: { $ref: "#/components/schemas/Messbedarf" } } } }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
  /api/v1/unternehmen/energieeinsaetze/{id}/messbedarf/{messbedarfId}/einloesen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: messbedarfId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [energieeinsaetze]
      summary: Bedarf durch eine eingerichtete Messstelle im selben Mandanten und Standort-Zaun einlösen.
      description: Recht `energieeinsatz.verwalten`; schreibt Protokoll und `messbedarf_eingeloest` atomar.
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/MessbedarfEinloesen" } } } }
      responses:
        "200": { description: Eingelöst, content: { application/json: { schema: { $ref: "#/components/schemas/Messbedarf" } } } }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "409": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
  /api/v1/unternehmen/energieeinsaetze/{id}/messbedarf/{messbedarfId}/verwerfen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: messbedarfId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [energieeinsaetze]
      summary: Einen offenen Messbedarf mit Begründung verwerfen.
      description: Recht `energieeinsatz.verwalten`; der verworfene Eintrag bleibt lesbar.
      requestBody: { required: true, content: { application/json: { schema: { $ref: "#/components/schemas/MessbedarfVerwerfen" } } } }
      responses:
        "200": { description: Verworfen, content: { application/json: { schema: { $ref: "#/components/schemas/Messbedarf" } } } }
        "409": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
        "422": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
  /api/v1/unternehmen/energieeinsaetze/{id}/messbedarf/{messbedarfId}/protokoll:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: messbedarfId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [energieeinsaetze]
      summary: Das unveränderliche Messbedarf-Protokoll lesen.
      description: Recht `energieeinsatz.ansehen`; derselbe Standort-Zaun wie am Energieeinsatz.
      responses:
        "200": { description: Protokoll, content: { application/json: { schema: { $ref: "#/components/schemas/MessbedarfProtokoll" } } } }
        "404": { $ref: "#/components/responses/EnergieeinsatzAbgelehnt" }
  /api/v1/unternehmen/prozesse:
    get:
      tags: [kostenstellen-prozesse]
      summary: Die Prozess-Objekte des Unternehmens; mit `stichtag` nur die an dem Tag bestehenden.
      description: >
        Heute lesend - keine eigene Kennung (wie das Unternehmen). Ohne Stichtag alle, beendete eingeschlossen, nach Kennzeichen.
        Der Auswahl-Katalog (Standort-Zaun, Entscheid 21.09.2026, Lesart B): eine unternehmensweite Rolle, ein Aufruf
        ohne Zugriff-Kontext und der Umschalter sehen jede Zeile ganz. Ein Konto, das `messstelle.bearbeiten` oder
        `messstelle.verteilung` im Unternehmen oder an einem seiner Standorte hat, sieht jede Zeile nur mit ihren
        Stammdaten - Kennung, Kennzeichen, Name, Gültigkeit, Eltern; `angelegt_am` fehlt dann. Jedes andere Konto bekommt
        eine leere Liste, ohne Hinweis und ohne Anzahl. Detail und Bilanz bleiben den unternehmensweiten Rollen.
      parameters:
        - { name: stichtag, in: query, required: false, schema: { type: string, format: date } }
      responses:
        "200":
          description: Die Liste (ein Objekt, kein nacktes Array - additiv erweiterbar).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ProzessListe" }
        "400": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [kostenstellen-prozesse]
      summary: Legt an (Kennzeichen, Name, gültig ab, wahlweise gültig bis).
      description: >
        Recht `prozess.verwalten` (AP-10 §4.10, E15). Reihenfolge: Form (400) → Kennzeichen-Form
        (422 `kennzeichen_format`) → Zeitraum (422 `zeitraum_ungueltig`) → Unternehmen da
        (409 `unternehmen_nicht_angelegt`) → Kennzeichen frei (409 `kennzeichen_belegt`,
        auch das eines beendeten Objekts).
        Beim Prozess wählt `eltern_id` den übergeordneten Prozess: gibt es ihn nicht,
        422 `eltern_unbekannt`; hat er selbst ein Elternteil, 422 `eine_ebene`; besteht er
        nicht an jedem Tag des neuen Prozesses, 422 `ziel_besteht_nicht`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/KostenstelleProzessAnlegen" }
            example:
              kennzeichen: "P-1"
              name: Spritzguss
              gueltig_ab: "2026-10-01"
      responses:
        "201":
          description: Angelegt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Prozess" }
        "400": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "422": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
  /api/v1/unternehmen/prozesse/{id}:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [kostenstellen-prozesse]
      summary: Ein Objekt.
      description: Heute lesend - keine eigene Kennung. Fremd oder unbekannt = 404 `nicht_gefunden`.
      responses:
        "200":
          description: Das Objekt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Prozess" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
    put:
      tags: [kostenstellen-prozesse]
      summary: Benennt um - Kennzeichen, Beginn und Elternteil bleiben.
      description: Recht `prozess.verwalten`. Nur `name`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/KostenstelleProzessUmbenennen" }
      responses:
        "200":
          description: Umbenannt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Prozess" }
        "400": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
  /api/v1/unternehmen/prozesse/{id}/beenden:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    put:
      tags: [kostenstellen-prozesse]
      summary: Beendet (der letzte Tag) - beenden statt löschen.
      description: >
        Recht `prozess.verwalten`. Ein Ende wird nur vorgezogen: ein späteres oder gleiches Ende eines
        beendeten Objekts ist 409 `bereits_beendet` (Muster „nur verkürzt“). Ein Tag
        vor dem Beginn ist 422 `zeitraum_ungueltig`. Gelten Zuordnungen über das Ende hinaus
        (Messstellen, Unterprozesse - und mit IP-8 die Anteile einer Verteilung), antwortet
        409 `zuordnung_besteht` mit `zuordnungen` - gekürzt oder gelöscht wird nichts.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/KostenstelleProzessBeenden" }
            example:
              gueltig_bis: "2026-12-31"
      responses:
        "200":
          description: Beendet.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Prozess" }
        "400": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "409": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "422": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
  /api/v1/unternehmen/prozesse/{id}/messstellen:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: am, in: query, required: true, schema: { type: string, format: date } }
    get:
      tags: [kostenstellen-prozesse, bewertung]
      summary: Gemessene und berechnete Messstellen eines Prozesses an einem Tag.
      description: >
        Recht `energieeinsatz.ansehen`; reine Lese-Route ohne `@Recht`. Der Standort-Zaun entspricht dem
        Energieeinsatz: sichtbar über mindestens eine Messstelle des Prozesses, außerhalb dieselbe 404 wie unbekannt;
        in der Antwort stehen nur lesbare Messstellen. `prozess_summe_passt` prüft die Quell-Messstellen der
        berechneten Messstellen gegen `gemessen`. Ein äußerer Term ist ein Hinweis, niemals ein Fehler oder eine
        Umrechnung; Verteilungs-Terme nennen `ueber_verteilung`, Kostenstelle und Tagesanteil.
      responses:
        "200":
          description: Messstellen und P4-Hinweise am Stichtag.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ProzessMessstellen" }
        "400": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
  /api/v1/standorte/{standortId}/netzanschluesse:
    parameters:
      - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [netzanschluesse]
      summary: Die Netzanschlüsse des Standorts; mit `stichtag` nur die an dem Tag bestehenden.
      description: >
        Heute lesend - keine eigene Kennung (wie das Standort-Lesemodell). Ohne Stichtag alle,
        beendete eingeschlossen, nach Kennzeichen; mit Stichtag je Anschluss nur die Bindungen
        des Tages. `kennzeichen_vorschlag` ist das nächste automatische Kennzeichen (vergeben
        erst beim Anlegen). Fremder Standort = 404.
      parameters:
        - { name: stichtag, in: query, required: false, schema: { type: string, format: date } }
      responses:
        "200":
          description: Die Liste (ein Objekt, kein nacktes Array - additiv erweiterbar).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NetzanschlussListe" }
        "400": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
    post:
      tags: [netzanschluesse]
      summary: Legt einen Netzanschluss am Standort an.
      description: >
        Recht `netzanschluss.verwalten` (AP-10 §4.10, E15). Reihenfolge: Standort da (404) →
        Form (400) → Felder des Vertrags (400 `anfrage_ungueltig` mit `feld`; Marktlokation 422
        `malo_form`) → Zeitraum (422 `zeitraum_ungueltig`) → Kennzeichen (422
        `kennzeichen_format`, 409 `kennzeichen_belegt`). Ohne `kennzeichen` vergibt die Regel das
        nächste freie (`NA-0001`). Vereinbart über Anschluss ist ein HINWEIS (`hinweise`), keine
        Ablehnung.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/NetzanschlussAnschluss" }
            example:
              kennzeichen: NA-1
              name: Hauptanschluss Halle 1
              malo: "47110000001"
              netzbetreiber: Netzgesellschaft Ahrental (fiktiv)
              anschluss_kva: 630
              vereinbart_kw: 550
              messung: RLM
      responses:
        "201":
          description: Angelegt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Netzanschluss" }
        "400": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
        "409": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
        "422": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
  /api/v1/standorte/{standortId}/netzanschluesse/vorschlaege:
    parameters:
      - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [netzanschluesse]
      summary: Vorverknüpfte Anlegevorschläge für vorhandene Anlagen.
      description: >
        Recht `netzanschluss.verwalten`. Nur aktive Messfunktion und aktuell zugeordnete Anlagen ohne
        bestehende oder geplante Bindung und ohne frühere Entscheidung. Liest ausschließlich, reserviert
        kein Kennzeichen. Name und Bindungsbeginn sind Vorschläge; Vertragsangaben ergänzt der Kunde.
        Ein Kennzeichen kann vor Bestätigung anderweitig vergeben werden.
      responses:
        "200":
          description: Vorschläge; bei reinem Betriebskunden leer.
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/NetzanschlussVorschlag" } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Recht fehlt. }
        "404": { description: Standort nicht gefunden oder nicht sichtbar. }
  /api/v1/standorte/{standortId}/netzanschluesse/vorschlaege/{anlageId}/uebernehmen:
    parameters:
      - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: anlageId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [netzanschluesse]
      summary: Nach Bestätigung Anschluss und Bindung gemeinsam anlegen.
      description: >
        Recht `netzanschluss.verwalten`, rückwirkend zusätzlich `aenderung.rueckwirkend` und Begründung.
        Bestehende Feld-, Kennzeichen- und Bindungsregeln. Anschluss, Bindung, Protokoll und Entscheidung
        in einer Transaktion. Keine Änderung von site oder Preisblatt. Zweite Übernahme oder inzwischen
        gebundene/verschobene Anlage = 409. Vertragsangaben werden nicht aus Anlagenpreisen abgeleitet.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/NetzanschlussUebernehmen" }
      responses:
        "201":
          description: Angelegt und gebunden.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Netzanschluss" }
        "400": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Recht fehlt. }
        "404": { description: Standort oder Anlage nicht gefunden oder nicht sichtbar. }
        "409": { description: Vorschlag nicht mehr verfügbar oder Kennzeichen/Bindung belegt. }
        "422": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
  /api/v1/standorte/{standortId}/netzanschluesse/vorschlaege/{anlageId}/verwerfen:
    parameters:
      - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: anlageId, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [netzanschluesse]
      summary: Entscheidung je Anlage dauerhaft merken.
      description: Recht `netzanschluss.verwalten`. Kein Anschluss entsteht; manuelles Anlegen bleibt möglich.
      responses:
        "204": { description: Verworfen. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { description: Recht fehlt. }
        "404": { description: Standort oder Anlage nicht gefunden oder nicht sichtbar. }
        "409": { description: Vorschlag nicht mehr verfügbar. }
  /api/v1/standorte/{standortId}/netzanschluesse/{id}:
    parameters:
      - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [netzanschluesse]
      summary: Ein Netzanschluss mit allen wirksamen Bindungen.
      description: Heute lesend - keine eigene Kennung. Fremd, unbekannt oder an einem anderen Standort = 404 `nicht_gefunden`.
      responses:
        "200":
          description: Der Netzanschluss.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Netzanschluss" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
    put:
      tags: [netzanschluesse]
      summary: Bearbeitet - die ganze Menge; ein fehlendes Feld ist leer.
      description: >
        Recht `netzanschluss.verwalten`. Ein neues Kennzeichen ist frei, wenn kein ANDERER
        Anschluss es trägt oder trug (409 `kennzeichen_belegt`); ein Ende wird nur vorgezogen
        (409 `bereits_beendet`); Tage, die eine Bindung abschnitten, sind 409 `bindung_besteht`
        mit `bindungen`. Beenden = `gueltig_bis` setzen. Ohne Änderung: kein Protokolleintrag.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/NetzanschlussAnschluss" }
      responses:
        "200":
          description: Bearbeitet.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Netzanschluss" }
        "400": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
        "409": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
        "422": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
  /api/v1/standorte/{standortId}/netzanschluesse/{id}/anlagen:
    parameters:
      - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    post:
      tags: [netzanschluesse]
      summary: Bindet eine Anlage ab einem Tag an diesen Netzanschluss.
      description: >
        Recht `netzanschluss.verwalten`; mit „gültig ab“ vor heute zusätzlich
        `aenderung.rueckwirkend`. Urteil aus NetzanschlussRegeln.bindung über alle wirksamen
        Bindungen der Anlage und des Anschlusses: eine zweite Bindung derselben Anlage am selben
        Tag → 409 `bindung_ueberlappt`; ein Anschluss, der an einem der Tage an einer anderen
        Anlage hängt → 409 `anschluss_belegt`; beginnt die neue nach der laufenden, endet die
        laufende am VORTAG. Die neue Bindung endet mit ihrem Anschluss (422
        `netzanschluss_besteht_nicht`, wenn er am Tag nicht besteht). Die Anlage kommt aus dem
        Kundenbereich (422 `anlage_unbekannt`). Genau ein Protokolleintrag `gebunden`.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/NetzanschlussBinden" }
            example:
              anlage_id: 7d0e9c7e-2f6b-4a3a-9d61-0a9d7c3e5b11
              gueltig_ab: "2024-03-12"
      responses:
        "201":
          description: Gebunden - der Netzanschluss mit seinen Bindungen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Netzanschluss" }
        "400": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
        "409": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
        "422": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
  /api/v1/standorte/{standortId}/netzanschluesse/{id}/grenzen:
    parameters:
      - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [netzanschluesse]
      summary: Das Grenzblatt des Netzanschlusses (UEMS AP-15 IP-3).
      description: >
        Heute lesend - keine eigene Kennung; der Zaun fragt `RechtPruefung#pruefenLesen` am
        Standort. Fremd, unbekannt, an einem anderen Standort oder außerhalb des Zugriffs = 404
        `nicht_gefunden`. Alle wirksamen Fassungen und die am `stichtag` (ohne: heute am Standort)
        gültige; `gilt` ist null, wenn keine gilt. Was an der Anlage WIRKT, ist der engere Wert aus
        Anlage und dieser Fassung (netzanschluss.md §5).
      parameters:
        - { name: stichtag, in: query, required: false, schema: { type: string, format: date } }
      responses:
        "200":
          description: Das Grenzblatt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NetzanschlussGrenzblatt" }
        "400": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
    post:
      tags: [netzanschluesse]
      summary: Ab einem Tag gilt diese Fassung des Grenzblatts.
      description: >
        Recht `netzanschluss.verwalten`; mit „gültig ab“ vor heute zusätzlich
        `aenderung.rueckwirkend`. Einspeise- und Bezugsgrenze in kW, je unbekannt oder größer 0; alternativ ausdrücklich `einspeisegrenze_keine: true`
        (400 `anfrage_ungueltig` mit `feld`); der Anschluss besteht am Tag (422
        `netzanschluss_besteht_nicht`); Plausibilität gegen den Anschluss: Bezugsgrenze über der
        vereinbarten Leistung → 422 `grenze_ueber_vereinbart`, eine Grenze über der
        Anschlussleistung → 422 `grenze_ueber_anschluss`. Eine zweite Fassung am selben Tag hebt
        die erste auf; dieselben Werte noch einmal schreiben nichts. Genau ein Protokolleintrag
        `grenze`. Keine Preise (W1). Der Mandant kommt aus der Anmeldung, nie aus dem Körper.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/NetzanschlussGrenzeSetzen" }
            example:
              gueltig_ab: "2027-05-03"
              einspeisegrenze_kw: 100
              bezugsgrenze_kw: 550
      responses:
        "201":
          description: Eingetragen - das Grenzblatt heute.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NetzanschlussGrenzblatt" }
        "400": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
        "422": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
  /api/v1/standorte/{standortId}/netzanschluesse/{id}/grenznachweis:
    parameters:
      - { name: standortId, in: path, required: true, schema: { type: string, format: uuid } }
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [netzanschluesse]
      summary: Der Grenz-Nachweis des Netzanschlusses für einen Monat oder freien Zeitraum (UEMS AP-15 IP-31/NW-8).
      description: >
        Heute lesend - keine eigene Kennung; der Zaun fragt `RechtPruefung#pruefenLesen` am
        Standort. Fremd, unbekannt, an einem anderen Standort oder außerhalb des Zugriffs = 404
        `nicht_gefunden`; ein Hauptzähler außerhalb des Zugriffs nimmt seiner Richtung die Zahlen
        (`ausserhalb_zugriff`). Gerechnet, nie gespeichert, über die abgeschlossenen Tage des
        Monats (ohne Parameter: der laufende am Standort; heute gehört nicht dazu) oder über den freien Zeitraum
        `[von,bis)` (beide Zeitpunkte erforderlich, statt `monat`, höchstens 31 Tage). Je Richtung
        (Bezug = Hauptzähler Bezug, Einspeisung = Hauptzähler Abgabe) das höchste
        Viertelstunden-Mittel gegen die an seinem Tag wirksame Grenze (der engere Wert aus Anlage
        und Grenzblatt), die Viertelstunden und Minuten darüber und die Unterbrechungen
        (zusammenhängende Viertelstunden darüber) mit Höchstwert. Nur eine vollständige
        Viertelstunde belegt etwas: eine Lücke heißt `nicht_belegt`, nie `eingehalten` (B5).
        `grenze_geprueft` ist wahr, wo Grenze UND Hauptzähler vorliegen; sonst nennt `grund`
        `keine_grenze`, `kein_hauptzaehler` oder `kein_abgeschlossener_tag`. M-2 (Augenblick) ist
        `nicht_gemessen` - die Mess-Welt hat keine Dauer unter der Viertelstunde
        (netzanschluss.md §6).
      parameters:
        - { name: monat, in: query, required: false, schema: { type: string, pattern: "^[0-9]{4}-[0-9]{2}$", example: "2026-09" } }
        - { name: von, in: query, required: false, schema: { type: string, format: date-time } }
        - { name: bis, in: query, required: false, schema: { type: string, format: date-time } }
      responses:
        "200":
          description: Der Nachweis.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/NetzanschlussGrenzNachweis" }
        "400": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NetzanschlussAbgelehnt" }
  /api/v1/messstellen/{kennzeichen}/ablesungen:
    parameters:
      - { name: kennzeichen, in: path, required: true, schema: { type: string, example: MS-21 } }
    get:
      tags: [messstellen]
      summary: Ablesungen mit allen Fassungen und Herkunft lesen.
      description: Recht `messwerte.ansehen`, einschließlich Standortumfang. Erhalten auch nach Rohwertaufbewahrung.
      responses:
        '200':
          description: Nach Messzeit und Fassung geordnet.
          content:
            application/json:
              schema: { type: array, items: { $ref: '#/components/schemas/Ablesung' } }
        '403': { description: Recht fehlt. }
        '404': { description: Messstelle fehlt oder liegt außerhalb des Kundenbereichs bzw. Umfangs. }
    post:
      tags: [messstellen]
      summary: Gemessenen Zählerstand an einer manuell abgelesenen Messstelle erfassen.
      description: >
        Recht `ablesung.erfassen` (KA/EM unternehmensweit, Bearbeiter im Standort; kein Unterstützer).
        Zeitpunkt mit Versatz und voller Minute, nicht in der Zukunft. Keine Box/Komponente.
        Gleicher Stand und gleiche Zuordnung sind eine Wiederholung ohne Schreibwirkung.
        Monatsvorgabe = größter Zeitanteil, nur bei höchstens zwei berührten Kalendermonaten.
        Ein zugeordneter Monat summiert ganze Ablesezeiträume, Tageswerte werden nie interpoliert.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [zeitpunkt, stand]
              properties:
                zeitpunkt: { type: string, format: date-time, example: '2026-11-02T07:40:00+01:00' }
                stand: { type: string, example: '49.451', description: Nicht negativer deutscher Zahltext. }
                zuordnung_monat: { type: [string, 'null'], pattern: '^[0-9]{4}-[0-9]{2}$', description: 'Weggelassen = Vorgabe; null = keinem Monat zuordnen.' }
      responses:
        '200':
          description: Eingetragen oder unveränderte Wiederholung.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AblesungAntwort' }
        '400': { description: Unlesbare Anfrage oder unbekanntes Feld. }
        '403': { description: Recht fehlt. }
        '404': { description: Messstelle fehlt oder ist nicht sichtbar. }
        '409': { description: Anderer Stand bereits vorhanden; Berichtigung verwenden. }
        '422': { description: Quelle passt nicht, Zeitpunkt oder Zählerstand ungültig, Rücksprung. }
  /api/v1/messstellen/{kennzeichen}/ablesungen/{zeitpunkt}/berichtigung:
    parameters:
      - { name: kennzeichen, in: path, required: true, schema: { type: string, example: MS-21 } }
      - { name: zeitpunkt, in: path, required: true, schema: { type: string, format: date-time } }
    post:
      tags: [messstellen]
      summary: Stand oder Monatszuordnung versioniert berichtigen.
      description: >
        Recht `ablesung.erfassen`. Die gemeinsame Vier-Augen-Einstellung des Unternehmens gilt.
        Bei aktiver Einstellung entsteht ein K-Vorschlag; Freigabe über die vorhandene Korrekturroute
        durch eine zweite Person. Sonst sofort neue Fassung. Alte Rohwerte und Periodenversionen bleiben.
        Rücknahme einer Berichtigung über die vorhandene Korrekturroute stellt den vorherigen Stand
        als weitere Fassung wieder her. Erstwerte werden nicht zurückgenommen.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [stand, begruendung]
              properties:
                stand: { type: string, example: '49.451' }
                zuordnung_monat: { type: [string, 'null'], pattern: '^[0-9]{4}-[0-9]{2}$', description: Weggelassen bewahrt die bisherige Zuordnung. }
                begruendung: { type: string, minLength: 10, maxLength: 500 }
      responses:
        '200':
          description: Berichtigt, Vorschlag oder unveränderte Wiederholung.
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AblesungAntwort' }
        '400': { description: Unlesbare Anfrage oder unbekanntes Feld. }
        '403': { description: Recht fehlt. }
        '404': { description: Messstelle oder Ablesung fehlt oder ist nicht sichtbar. }
        '409': { description: Vorschlag offen oder Fassung inzwischen geändert. }
        '422': { description: Ungültiger Stand oder Begründung; Rücksprung. }
  /api/v1/messstellen/{kennzeichen}/werte:
    parameters:
      - { name: kennzeichen, in: path, required: true, schema: { type: string, example: MS-10 }, description: "Das Kennzeichen, das die Messstelle HEUTE trägt." }
      - { name: raster, in: query, required: true, schema: { type: string, enum: [viertelstunde, stunde, tag, monat, jahr] } }
      - { name: von, in: query, required: true, schema: { type: string, example: "2026-11-03" }, description: "Ein Tag (JJJJ-MM-TT, sein Beginn in der Zeitzone des Standorts) oder ein Zeitpunkt mit Versatz - auf einer Grenze des Rasters." }
      - { name: bis, in: query, required: true, schema: { type: string, example: "2026-11-03" }, description: "Ein Tag = der LETZTE Tag einschließlich; ein Zeitpunkt mit Versatz = ausschließlich. Auf einer Grenze des Rasters." }
      - { name: version, in: query, required: false, schema: { type: integer, minimum: 1 }, description: "AP-08 IP-18: ohne Angabe zeigt jeder Schritt seine NEUESTE Version; mit `version=n` genau Version n - die damalige Zahl (Version 1 = die Zeile der Verdichtung). Fehlt n an einem Schritt, sagt er `version_nicht_gespeichert` und nennt in `versionen` seine neueste; gibt es n an KEINEM Schritt, ist die Anfrage 404 `version_gibt_es_nicht`." }
    get:
      tags: [messstellen]
      summary: Die Werte je Messstelle - Menge bzw. Mittel/Min/Max je Schritt, nie ohne Zustand, Abdeckung und Kennzeichen.
      description: >
        Recht `messwerte.ansehen` (AP-08 §4.8, keine Durchsetzung bis AP-03). UEMS AP-08 IP-9.
        Nur die FÜHRENDEN Bindungen der Hauptgröße liefern (eine Vergleichsquelle oder eine
        Nebengröße nie). Gelesen wird aus den Speicherklassen: Viertelstunde
        `messreihe_viertelstunde`, Tag `messreihe_tag`, Monat und Jahr `messreihe_periode` -
        Tag, Monat und Jahr also aus den Periodenständen, nie als Summe der Viertelstunden. Die
        Stunde ist je Schritt der freie Zeitraum der Verbrauchsregel über ihre Viertelstunden,
        ihre Abdeckung die Summe ihrer Viertelstunden. Jedes Feld steht immer da; `null` heißt
        unbekannt oder nicht gebildet, nie 0. `zustand` ist das Wort des Ergebnis-Zustands
        (docs/contracts/v2/ergebnis-zustand.md), `fassung` vorläufig/endgültig - zwei Aussagen.
        Eine Periode ohne gespeicherte Zeile hat `keine Werte` mit 0 von den erwarteten Werten
        der Kadenz-Kette; liegen darunter schon Rohwerte oder Viertelstunden, sagt `grund`
        `noch_nicht_gebildet`. Ein Schritt ohne Zustand nennt immer `grund`. Eine BERECHNETE
        Messstelle (AP-10 IP-10) liest Viertelstunde, Tag, Monat und Jahr aus ihrer gespeicherten
        Spur `berechnet` (ohne Rohwerte: `erhalten`/`erwartet`/`quelle` null); ohne Zeile
        `noch_nicht_gebildet`, die Stunde und eine Formel mit Momentanwert weiter `berechnet`.
        Versionen (AP-08 IP-18): Versionen ab 2 stehen in `messreihe_viertelstunde_version` bzw.
        `messreihe_periode_version`; ohne `version` zeigt jeder Schritt die neueste, `versionen`
        zählt sie (die Naht der Portal-Fläche „Versionen“ am Wert). Die Stunde hat keine eigenen
        Versionen: trägt eine ihrer Viertelstunden eine spätere, sagt sie `version_nicht_gebildet`
        (mit `version=1` die damalige Stunde). Eine fremde Messstelle ist 404, nie 403.
        Nach den Fristen (AP-12 IP-16, Bericht-Vertrag S4/B16): fragt die Anfrage GENAU EINE Periode (Monat
        oder Jahr) einer gemessenen Reihe in Version 1 - angefragt oder als neueste -, liegt ihr Beginn 3 653
        Tage oder länger zurück und sind Zeile und Viertelstunden weg, ist die Antwort 404
        `wert_nicht_mehr_gespeichert` mit dem Satz des Bericht-Vertrags und dem Berichtsstand, der sie
        festhält. Versionen ab 2 haben keine Frist; innerhalb der Aufbewahrung bleibt es `keine Werte`.
      responses:
        "200":
          description: Die Werte.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleWerte" }
        "400": { $ref: "#/components/responses/MessstelleWerteAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404":
          description: Die Messstelle gibt es nicht (`{message}`) - oder die angefragte Version an keinem Schritt - oder die EINE Periode ist nach den Fristen nicht mehr gespeichert (`wert_nicht_mehr_gespeichert`).
          content:
            application/json:
              schema:
                oneOf:
                  - { $ref: "#/components/schemas/MessstelleWerteVersionGibtEsNicht" }
                  - { $ref: "#/components/schemas/MessstelleWerteNichtMehrGespeichert" }
                  - { type: object, required: [message], properties: { message: { type: string } } }
  /api/v1/messstellen/{kennzeichen}/werte/versionen:
    parameters:
      - { name: kennzeichen, in: path, required: true, schema: { type: string, example: MS-10 }, description: "Das Kennzeichen, das die Messstelle HEUTE trägt." }
      - { name: raster, in: query, required: true, schema: { type: string, enum: [viertelstunde, tag, monat, jahr] }, description: "Die Stunde hat keine eigenen Versionen (400 `raster_ohne_versionen`)." }
      - { name: von, in: query, required: true, schema: { type: string, example: "2026-11-03" }, description: "Wie an `…/werte`." }
      - { name: bis, in: query, required: true, schema: { type: string, example: "2026-11-03" }, description: "Wie an `…/werte`; `von` und `bis` umfassen GENAU EINEN Schritt (sonst 400 `nicht_genau_eine_periode`)." }
    get:
      tags: [messstellen]
      summary: Die Versions-Historie einer Periode - was vorher dastand, was jetzt, wer es wann warum geändert hat.
      description: >
        Recht `messwerte.ansehen` (AP-08 §4.8, keine Durchsetzung bis AP-03). UEMS AP-08 IP-18.
        Je Version `wert_alt` (Version n − 1) und `wert_neu` (Version n), beide genau so, wie
        `…/werte?version=` sie zeigt; Version 1 ist die Zahl der Verdichtung ohne Entscheidung.
        `entscheidungen` sind die Fassungen der Ersatzwerte (`EW-…`) und Korrekturen (`K-…`), die
        Version n gegenüber n − 1 ausmachen, mit Urheber (`wer`), Zeitpunkt (`wann`) und dem Text,
        den der Mensch DAZU geschrieben hat (`warum`: an der anlegenden Fassung die Begründung, an
        Freigabe oder Rücknahme ihr Grund). Fehlt er, ist `warum` null und `fehlt` nennt „warum“ -
        nie ein erfundener Grund; die anlegende Fassung steht an einer späteren unter `angelegt`.
        Eine Periode ohne Korrektur hat genau eine Version. Nichts wird gerechnet oder geschrieben.
        Eine fremde Messstelle ist 404, nie 403.
      responses:
        "200":
          description: Die Historie.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleWerteHistorie" }
        "400": { $ref: "#/components/responses/MessstelleWerteAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { description: Die Messstelle gibt es nicht. }
        "404": { $ref: "#/components/responses/NotFound" }
  /api/v1/messstellen/{id}/prozesse:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [kostenstellen-prozesse]
      summary: Die Prozesse einer Messstelle - alle wirksamen Intervalle, mit `am` die an dem Tag geltenden.
      description: Recht `messstelle.ansehen`.
      parameters:
        - { name: am, in: query, required: false, schema: { type: string, format: date } }
      responses:
        "200":
          description: Die Intervalle.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleProzesse" }
        "400": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
    put:
      tags: [kostenstellen-prozesse]
      summary: Ab einem Tag gehört die Messstelle zu GENAU diesen Prozessen (leer = zu keinem).
      description: >
        Recht `messstelle.bearbeiten` („Ort, Prozess, Kostenstelle zuordnen“); mit
        `gueltig_ab` vor heute zusätzlich `aenderung.rueckwirkend`. Was am Tag läuft und
        bleibt, läuft weiter; was am Tag läuft und nicht mehr gilt, endet am VORTAG; was an
        oder nach dem Tag beginnt und nicht passt, wird aufgehoben (lesbar). Ein neues
        Intervall endet mit seinem Prozess (`endet_mit_prozess`). Unbekannter Prozess 422
        `prozess_unbekannt`, ein Prozess, der am Tag nicht besteht, 422 `ziel_besteht_nicht`,
        archivierte Messstelle 409 `messstelle_archiviert`. GENAU EIN Protokolleintrag
        `prozesse_zugeordnet`, wenn sich etwas ändert; eine Ablehnung schreibt nichts.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MessstelleProzesseSetzen" }
            example:
              gueltig_ab: "2026-10-01"
              prozesse: ["5b0d6f0e-8a3c-4f5e-9d2a-2f6f0c1b7a11"]
      responses:
        "200":
          description: Die Intervalle danach.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleProzesse" }
        "400": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "409": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
        "422": { $ref: "#/components/responses/KostenstelleProzessAbgelehnt" }
  /api/v1/messstellen/{id}/verteilung:
    parameters:
      - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
    get:
      tags: [verteilung]
      summary: Die Verteilung einer Messstelle - alle wirksamen Anteile, mit `am` die an dem Tag geltenden und der Zustand.
      description: >
        Recht `messstelle.ansehen`. Mit `am`: `zustand` = `verteilt` oder ausdrücklich
        `nicht verteilt` (keine Zeile an dem Tag - nie „zu 0 % verteilt“); ohne `am` ist
        `zustand` null. Ein Anteil, dessen Kostenstelle endet, endet mit ihr
        (`endet_mit_kostenstelle`).
      parameters:
        - { name: am, in: query, required: false, schema: { type: string, format: date } }
      responses:
        "200":
          description: Die Anteile.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleVerteilung" }
        "400": { $ref: "#/components/responses/VerteilungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/VerteilungAbgelehnt" }
    put:
      tags: [verteilung]
      summary: Ab einem Tag gilt GENAU dieser Satz (alle Ziele eines Tages in einer Anfrage; leer = ab dem Tag nicht verteilt).
      description: >
        Recht `messstelle.verteilung`; mit `gueltig_ab` vor heute zusätzlich
        `aenderung.rueckwirkend`. Die Regel ist `VerteilungRegeln.satzAbTag` (Vertrag
        verteilung-vectors.json, Regel `satz_ab_tag`): jeder Anteil in (0, 100] mit höchstens
        einer Nachkommastelle (422 `anteil_ungueltig`), jede Kostenstelle besteht am Tag (422
        `ziel_besteht_nicht`), die Summe ist genau 100 % - auch an jedem Tag nach dem Ende einer
        der Kostenstellen (422 `verteilung_summe` mit `summe` und `tag`); ein neuer Anteil endet mit
        seiner Kostenstelle. Die laufende Verteilung endet am VORTAG; beginnt am Tag oder danach
        schon eine, ist das 422 `formel_fassung_ueberlappt` - außer `korrektur` = true: dann wird
        die Verteilung, die GENAU an dem Tag beginnt, aufgehoben (lesbar) und ersetzt. Derselbe
        Satz noch einmal ändert nichts. Unbekannte Kostenstelle 422 `kostenstelle_unbekannt`,
        archivierte Messstelle 409 `messstelle_archiviert`. Alles in EINER Transaktion; die
        Datenbank prüft die 100 % zur Commit-Zeit. GENAU EIN Protokolleintrag
        `verteilung_geaendert` und GENAU EIN Ereignis `verteilung_geaendert`, wenn sich etwas
        ändert; eine Ablehnung schreibt nichts. Doppelte Zählung wird gemeldet, nie abgelehnt: die
        Antwort trägt `doppelzaehlung` - je Ziel-Kostenstelle, deren Sicht der Aufrufer lesen darf,
        welcher Posten dort am Tag `gueltig_ab` bereits in welchem enthalten ist (dieselbe Regel wie
        `GET /api/v1/unternehmen/kostenstellen/{id}/energie`, nur die Paare mit dieser Messstelle).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MessstelleVerteilungSetzen" }
            example:
              gueltig_ab: "2027-01-15"
              zeilen:
                - { kostenstelle_id: "5b0d6f0e-8a3c-4f5e-9d2a-2f6f0c1b7a11", anteil_prozent: "60" }
                - { kostenstelle_id: "7c1e2a3b-4d5f-4a6b-8c9d-0e1f2a3b4c5d", anteil_prozent: "40" }
              korrektur: false
      responses:
        "200":
          description: Alle wirksamen Anteile danach, mit dem Hinweis `doppelzaehlung`.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessstelleVerteilung" }
        "400": { $ref: "#/components/responses/VerteilungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/VerteilungAbgelehnt" }
        "409": { $ref: "#/components/responses/VerteilungAbgelehnt" }
        "422": { $ref: "#/components/responses/VerteilungAbgelehnt" }
  /api/v1/benutzer:
    post:
      tags: [zugriff]
      summary: Benutzer mit Startpasswort, Rolle und Standorten anlegen.
      description: >
        Recht benutzer.verwalten. Nur der Kundenadministrator des eigenen Kundenbereichs; der Mandant
        stammt aus dem JWT. Konto und Zuweisung entstehen gemeinsam. Keycloak prüft seine Realm-Policy
        und erzwingt UPDATE_PASSWORD. Kein Passwort in weiteren Antworten, Protokollen oder E-Mails.
        AP-19 Folge IP-13: `gueltig_bis` befristet `einsicht` gleich beim Anlegen - dieselben Regeln wie
        `POST /api/v1/zugriff`, geprüft vor dem Keycloak-Konto; protokolliert wie jede Zuweisung.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BenutzerAnlage" }
      responses:
        "201":
          description: Konto angelegt, Startpasswort genau einmal.
          headers:
            Cache-Control: { schema: { type: string, enum: [no-store] } }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenutzerAngelegt" }
        "400": { description: "Ungültige Angaben oder Passwortregeln des Realms; `gueltig_bis` bei einer anderen Rolle als `einsicht` oder kein Tag." }
        "403": { description: recht_fehlt. }
        "404": { description: Standort außerhalb des Kundenbereichs. }
        "409": { description: Benutzername oder E-Mail-Adresse ist im Realm bereits vergeben. }
        "422": { description: "standort_fehlt bei einer standortbezogenen Rolle; gueltig_bis_vergangen, wenn der letzte Tag vor heute liegt (Kalender des Kundenbereichs)." }
  /api/v1/benutzer/{sub}/zugriff:
    put:
      tags: [zugriff]
      summary: Zuweisungen einer Person ersetzen (Rolle ändern).
      description: >
        Recht benutzer.verwalten. Entzieht die Zuweisungen `bisher` und weist `rolle` zu - atomar, jede Ablehnung
        rollt den ganzen Wechsel zurück; andere Rollen der Person bleiben. `bisher` leer = weitere Rolle.
        AP-19 Folge IP-13: `gueltig_bis` befristet `einsicht` (letzter Tag, einschließlich) - dieselben Regeln wie
        `POST /api/v1/zugriff`, geprüft vor dem Entzug; protokolliert wie jede Zuweisung.
      parameters:
        - { name: sub, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [bisher, rolle, standorte]
              properties:
                bisher: { type: array, items: { type: string, format: uuid } }
                rolle: { type: string, enum: [kundenadministrator, energiemanager, bearbeiter, bedienberechtigt, leser, einsicht] }
                standorte:
                  type: array
                  description: Bei standortbezogener Rolle mindestens ein Standort; sonst leer.
                  items: { type: string, format: uuid }
                gueltig_bis: { type: string, format: date, nullable: true, description: "Nur bei `einsicht`: letzter Tag, einschließlich (AP-19 Folge IP-13)." }
      responses:
        "204": { description: Gewechselt. }
        "400": { description: "anfrage_ungueltig - keine Kundenrolle, `gueltig_bis` bei einer anderen Rolle als `einsicht` oder kein Tag." }
        "403": { description: recht_fehlt. }
        "404": { description: Benutzer oder Zuweisung nicht im Kundenbereich. }
        "409": { description: zuweisung_vorhanden, eigene_zuweisung oder letzter_kundenadministrator. }
        "422": { description: "Standort fehlt bei einer standortbezogenen Rolle; gueltig_bis_vergangen, wenn der letzte Tag vor heute liegt." }
  /api/v1/benutzer/{sub}/startpasswort:
    post:
      tags: [zugriff]
      summary: Startpasswort neu vergeben.
      description: >
        Recht benutzer.verwalten. Nur Kundenkonten des eigenen Kundenbereichs. Setzt temporary=true,
        hebt eine Anmeldesperre auf und protokolliert startpasswort_neu ohne Passwort. Rollen, Standorte
        und der bereits erreichte Konto-Zustand bleiben erhalten.
      parameters:
        - { name: sub, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Neues Startpasswort genau einmal.
          headers:
            Cache-Control: { schema: { type: string, enum: [no-store] } }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BenutzerAngelegt" }
        "403": { description: recht_fehlt. }
        "404": { description: Benutzer nicht im eigenen Kundenbereich. }
  /api/v1/me:
    get:
      tags: [zugriff]
      summary: Die Selbstauskunft - wer fragt und was er darf.
      description: >
        UEMS AP-03 IP-4, Recht `konto.eigenes` - jede Person liest nur sich selbst; die Route setzt nichts durch und
        schreibt nichts. Die EINE Quelle des Portals: Name, Kontoart, Rollen, sichtbare Standorte mit den Rechten je
        Aktion der Rechte-Matrix (`docs/contracts/v2/rechte-matrix.json`, Ableitung `darf`), künftige Zuweisungen,
        Teilansicht, Unterstützungen (eigene und gewährte) mit ihren Banner-Sätzen und die Kundenadministratoren für
        „Grund und Weg“. `standorte`, `kuenftig`, `text` und `teilansicht` sind die Ableitung `sichtbare_standorte`
        aus `rechte-vectors.json`. Der Kundenbereich des Kundenkontos kommt aus dem Token; Partner- und
        Plattform-Konten wählen ihn mit `X-Kundenbereich`, der nur gegen eine wirksame Unterstützung gilt (Partner:
        Installateur, Plattform: VoltPilot oder Notfall-Zugriff). Ohne sie antwortet diese Route mit dem eigenen Konto
        allein (`kundenbereich` null), jede andere Kundenroute mit 404. Die Plattform erreicht Kundenbereiche
        weiterhin über `X-Tenant-Id` (`zugang` = `umschalter`), solange
        `voltpilot.uems.unterstuetzung.umschalter-enabled` an ist (nur lokale Kompatibilität; Produktionsvorgabe AUS seit IP-15); ist er aus,
        gilt für sie dasselbe wie für einen Partner ohne Gewährung.
      parameters:
        - $ref: "#/components/parameters/Kundenbereich"
      responses:
        "200":
          description: Die Selbstauskunft.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Selbstauskunft" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/zugriff:
    get:
      tags: [zugriff]
      summary: Die Zuweisungen EINER Person - wirksame, künftige und beendete.
      description: >
        UEMS AP-03 IP-9, Recht `zuweisung.verwalten` (Matrix-Zelle U - nur der Kundenadministrator, E2).
        `benutzer` ist Pflicht (das Subject aus Keycloak); eine Zuweisung wird nie umgeschrieben, darum steht
        eine beendete weiter in der Liste und trägt ihr `beendet_am`.
      parameters:
        - { name: benutzer, in: query, required: true, schema: { type: string } }
      responses:
        "200":
          description: Die Zuweisungen, älteste zuerst.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Zugriff" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [zugriff]
      summary: Eine Rolle zuweisen (ab jetzt).
      description: >
        UEMS AP-03 IP-9 (§5.2), Recht `zuweisung.verwalten`. Eine Rolle je Standort braucht einen Standort
        (422 `standort_fehlt`); niemand erweitert die EIGENE Zuweisung (409 `eigene_zuweisung`). Das Konto muss
        im Kundenbereich schon gespiegelt sein - Konten legt `benutzer.verwalten` an (IP-13/IP-14). Der
        Unterstützer entsteht allein über `POST /api/v1/unterstuetzung` (400 sonst). AP-19 IP-12: `einsicht`
        (nur lesen, unternehmensweit, ohne Standort) lässt sich mit `gueltig_bis` befristen - letzter Tag,
        einschließlich; jede andere Rolle mit `gueltig_bis` und ein Ende in der Vergangenheit sind 400.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [benutzer_sub, rolle]
              properties:
                benutzer_sub: { type: string }
                rolle:
                  type: string
                  enum: [kundenadministrator, energiemanager, bearbeiter, bedienberechtigt, leser, einsicht]
                standort_id: { type: string, format: uuid, nullable: true }
                gueltig_bis: { type: string, format: date, nullable: true, description: "Nur bei `einsicht`: letzter Tag, einschließlich (AP-19 IP-12)." }
                grund: { type: string, nullable: true }
      responses:
        "201":
          description: Die neue Zuweisung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Zugriff" }
        "400": { description: Rumpf unvollständig, Rolle unbekannt oder nicht über diesen Weg zuweisbar. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Benutzer oder Standort nicht gefunden. }
        "409": { $ref: "#/components/responses/ZugriffAbgelehnt" }
        "422": { $ref: "#/components/responses/ZugriffAbgelehnt" }
  /api/v1/zugriff/{id}:
    delete:
      tags: [zugriff]
      summary: Eine Zuweisung entziehen - sofort.
      description: >
        UEMS AP-03 IP-9 (§4.7, A6/A8), Recht `zuweisung.verwalten`. Der Entzug wirkt mit der NÄCHSTEN Anfrage
        des Betroffenen, nicht mit seiner nächsten Anmeldung: sie bekommt 404/403 `zugriff_beendet`. Gesetzte
        Handeingriffe bleiben und laufen bis zu ihrem Ende (E15) - ein Entzug ist keine Steuerung; die Jetzt-Zone
        trägt sie danach mit „(Bedienrecht beendet am …)". Zwei Regeln lehnen mit 409 ab: die eigene Zuweisung
        ist unveränderlich (`eigene_zuweisung`) und der letzte Kundenadministrator ist geschützt
        (`letzter_kundenadministrator`). Eine UNTERSTÜTZUNG endet über `DELETE /api/v1/unterstuetzung/{id}`
        (hier 404). Protokolliert wird zweimal: im Zugriffsprotokoll des Kundenbereichs und als Zeile im
        Änderungsprotokoll des Standorts (`zugriff_entzogen`, AP-02 §4.4).
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                grund: { type: string, nullable: true }
      responses:
        "204": { description: Entzogen. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Zuweisung nicht gefunden, schon beendet oder eine Unterstützung. }
        "409": { $ref: "#/components/responses/ZugriffAbgelehnt" }
  /api/v1/unterstuetzung:
    get:
      tags: [zugriff]
      summary: Die Unterstützungen des Kundenbereichs - gewährte, künftige und archivierte.
      description: >
        UEMS AP-03 IP-8, Recht `unterstuetzung.verwalten` (Matrix-Zelle U - nur der Kundenadministrator, E2).
        Eine Gewährung wird je Standort als eigene Zuweisung eingetragen; diese Route fasst sie wieder zusammen.
        Ihre `id` ist der Griff der Gewährung - die kleinste Kennung ihrer Zeilen - und bleibt, auch nachdem sie
        endete. Den BANNER sieht jeder Benutzer des Kundenbereichs, er steht in `GET /api/v1/me`.
      responses:
        "200":
          description: Die Unterstützungen, jüngste zuerst.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/Unterstuetzung" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [zugriff]
      summary: Eine Unterstützung gewähren.
      description: >
        UEMS AP-03 IP-8 (§4.6, E5/E6/E7/E9), Recht `unterstuetzung.verwalten`. Enddatum Pflicht, Vorgabe 30 Tage,
        höchstens 12 Monate; mindestens ein Standort; ohne `umfang` gilt die Vorgabe je Art (Installateur
        „Einrichten und Bedienen“, VoltPilot „Ansehen“). Ist die `email` im Realm unbekannt, entsteht ein
        Partner-Konto, und `startpasswort` steht GENAU EINMAL in dieser Antwort - nie in einer Liste, einem
        Protokoll, einem Hinweis oder einer E-Mail (E14). Eine Unterstützung für VoltPilot gewährt der
        Kundenadministrator nur mit `anfrage_id` (E8): VoltPilot fragt an, gewährt sich aber nichts selbst.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UnterstuetzungGewaehren" }
      responses:
        "201":
          description: Die gewährte Unterstützung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Unterstuetzung" }
        "400": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
        "422": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
        "502": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
  /api/v1/unterstuetzung/anfragen:
    get:
      tags: [zugriff]
      summary: Die Anfragen von VoltPilot.
      description: >
        UEMS AP-03 IP-8 (E8, A5), Recht `unterstuetzung.verwalten`. Ohne `offen=false` nur die unentschiedenen.
        Eine Anfrage gewährt NICHTS; bestätigt wird sie über `POST /api/v1/unterstuetzung` mit `anfrage_id`
        (auch mit geändertem Umfang oder anderer Dauer).
      parameters:
        - name: offen
          in: query
          required: false
          schema: { type: boolean, default: true }
      responses:
        "200":
          description: Die Anfragen, jüngste zuerst.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/UnterstuetzungAnfrage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
  /api/v1/unterstuetzung/anfragen/{id}/ablehnen:
    post:
      tags: [zugriff]
      summary: Eine Anfrage ablehnen.
      description: >
        UEMS AP-03 IP-8, Recht `unterstuetzung.verwalten`. Die Anfrage bleibt sichtbar und gewährt nie etwas;
        eine schon entschiedene ist 409 `anfrage_entschieden`.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "204": { description: Abgelehnt. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
        "409": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
  /api/v1/unterstuetzung/hinweise:
    get:
      tags: [zugriff]
      summary: Das eigene Hinweis-Postfach.
      description: >
        UEMS AP-03 IP-8 (E8), Recht `konto.eigenes` - der Aufrufer liest nur seine eigenen Hinweise, die Route
        nennt kein fremdes Subject. Solange kein SMTP steht (`vp-login-smtp-reset-d6`), ist dies der Weg, auf dem
        ein Kundenadministrator von einer Anfrage, einem Notfall-Zugriff, einer Erinnerung oder einem Ablauf
        erfährt; mit SMTP geht dieselbe Zeile zusätzlich als E-Mail hinaus (`email_versandt_am`).
      parameters:
        - name: offen
          in: query
          required: false
          schema: { type: boolean, default: true }
      responses:
        "200":
          description: Die Hinweise, jüngster zuerst.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/UnterstuetzungHinweis" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/unterstuetzung/hinweise/{id}/gelesen:
    post:
      tags: [zugriff]
      summary: Einen eigenen Hinweis schließen.
      description: UEMS AP-03 IP-8, Recht `konto.eigenes` - ein fremder Hinweis ist 404, nie 403.
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "204": { description: Gelesen. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
  /api/v1/unterstuetzung/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
        description: Der Griff der Gewährung (die kleinste Kennung ihrer Zeilen).
    get:
      tags: [zugriff]
      summary: Eine Unterstützung.
      description: UEMS AP-03 IP-8, Recht `unterstuetzung.verwalten`.
      responses:
        "200":
          description: Die Unterstützung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Unterstuetzung" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
    put:
      tags: [zugriff]
      summary: Eine Unterstützung verlängern.
      description: >
        UEMS AP-03 IP-8 (§4.6, E6), Recht `unterstuetzung.verwalten`. Verlängern ist ein NEUES Enddatum: die
        laufende Gewährung endet jetzt und eine neue beginnt lückenlos (eine Zuweisung wird nie umgeschrieben,
        AP-00 Invariante 4) - die Antwort trägt darum einen neuen Griff. Ein früheres Enddatum ist 422
        `ende_nicht_spaeter`; ein Notfall-Zugriff wird nicht verlängert (409).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UnterstuetzungVerlaengern" }
      responses:
        "200":
          description: Die verlängerte Unterstützung (neuer Griff).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Unterstuetzung" }
        "400": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
        "409": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
        "422": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
    delete:
      tags: [zugriff]
      summary: Eine Unterstützung beenden.
      description: >
        UEMS AP-03 IP-8 (§4.7), Recht `unterstuetzung.verwalten`. Wirkt SOFORT: die nächste Anfrage des
        Unterstützers findet keine wirksame Zuweisung mehr und bekommt 404 auf jeder Kundenroute - kein Abwarten
        eines Token-Ablaufs. Eine schon beendete oder abgelaufene Gewährung ist 409 `bereits_beendet`.
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UnterstuetzungBeenden" }
      responses:
        "204": { description: Beendet. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
        "409": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
  /api/v1/admin/tenants/{tenantId}/unterstuetzung/anfrage:
    post:
      tags: [admin, zugriff]
      summary: VoltPilot fragt eine Unterstützung an.
      description: >
        UEMS AP-03 IP-8 (E8, A5), Plattform-Betrieb (`platform-admin`). Die Anfrage gewährt NICHTS: sie legt
        einen Wunsch an und einen Hinweis für jeden Kundenadministrator. Erst dessen Bestätigung über
        `POST /api/v1/unterstuetzung` mit `anfrage_id` macht daraus einen Zugang.
      parameters:
        - name: tenantId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UnterstuetzungPlattformAntrag" }
      responses:
        "201":
          description: Die Anfrage.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/UnterstuetzungAnfrage" }
        "400": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Den Kundenbereich gibt es nicht. }
        "422": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
  /api/v1/admin/tenants/{tenantId}/unterstuetzung/notfall:
    post:
      tags: [admin, zugriff]
      summary: Notfall-Zugriff - 24 Stunden, Grund Pflicht, der Kunde erfährt davon.
      description: >
        UEMS AP-03 IP-8 (E8, A14), Plattform-Betrieb (`platform-admin`). VoltPilot gewährt ihn sich selbst, und
        genau deshalb ist er eng und laut: genau 24 Stunden ab jetzt (`gueltig_bis` gibt es hier nicht), der
        `grund` ist PFLICHT - ohne ihn 422 `grund_fehlt`, und es wird nichts angelegt -, jeder Benutzer des
        Kundenbereichs sieht den Banner „VoltPilot-Support hat Notfall-Zugriff auf … bis … - Grund: …“, und jeder
        Kundenadministrator bekommt einen Hinweis (E-Mail, sobald SMTP steht). Der Kundenadministrator kann ihn
        jederzeit beenden wie jede Unterstützung.
      parameters:
        - name: tenantId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UnterstuetzungPlattformAntrag" }
      responses:
        "201":
          description: Der Notfall-Zugriff.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Unterstuetzung" }
        "400": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { description: Den Kundenbereich gibt es nicht. }
        "422": { $ref: "#/components/responses/UnterstuetzungAbgelehnt" }
  /api/v1/unternehmen/vieraugen:
    get:
      tags: [korrekturen]
      summary: Die Vier-Augen-Einstellung des Unternehmens.
      description: >
        Lesend, ohne eigene Kennung (wie das Unternehmen-Lesemodell). `vorgabe` true = nie eingestellt -
        dann gilt die Vorgabe aus (`vieraugen` false), auch ohne Unternehmen-Zeile.
      responses:
        "200":
          description: Die Einstellung.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/VierAugen" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/KorrekturFreigabeAbgelehnt" }
    put:
      tags: [korrekturen]
      summary: Schaltet „Freigabe durch eine zweite Person“ an oder aus.
      description: >
        Recht `vieraugen.einstellen` - nur der Kundenadministrator; ein Unterstützer (Plattform-Admin im
        Kundenbereich) bekommt 403 `recht_fehlt`. Jede Änderung steht im Änderungsprotokoll des
        Unternehmens (`bearbeitet`, Feld `vieraugen_freigabe`); ändert sich nichts, wird nichts geschrieben.
        Die Einstellung wirkt ab der nächsten Freigabe - eine gespeicherte Freigabe bleibt, wie sie ist.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/VierAugenSetzen" }
            example:
              vieraugen: true
      responses:
        "200":
          description: Die Einstellung danach.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/VierAugen" }
        "400": { $ref: "#/components/responses/KorrekturFreigabeAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/KorrekturFreigabeAbgelehnt" }
        "404": { $ref: "#/components/responses/KorrekturFreigabeAbgelehnt" }
        "409": { $ref: "#/components/responses/KorrekturFreigabeAbgelehnt" }
  /api/v1/korrekturen/{kennung}/freigeben:
    parameters:
      - { name: kennung, in: path, required: true, schema: { type: string, pattern: "^(K|BK)-[0-9]{4}-[0-9]{4,}$" }, description: "K-… eine Korrektur einer Reihe (AP-08), BK-… die Berichtigung eines Bezugsgrößen-Werts (AP-09 IP-7)." }
    post:
      tags: [korrekturen]
      summary: Gibt einen Korrektur-Vorschlag frei - unter der Vier-Augen-Einstellung dieses Augenblicks.
      description: >
        Recht `korrektur.freigeben`. Reihenfolge: Korrektur des eigenen Kundenbereichs (sonst 404
        `nicht_gefunden`, auch fremd) → Recht mit Einstellung und Ersteller (403 `recht_fehlt` mit
        `rolle_noetig`; 403 `zweite_person_noetig`, wenn bei Vier-Augen an der Ersteller freigibt - auch mit
        Recht) → Begründung 10 bis 500 Zeichen (422 `begruendung_fehlt`) → Stand `vorschlag` (409
        `status_passt_nicht`). Die Freigabe protokolliert Ersteller, Freigeber, Zeitpunkt und Begründung und
        hält die geprüfte Einstellung fest; die Neuberechnung übernimmt die Kaskade (AP-08 IP-17). Seit AP-09
        IP-7 auch die Berichtigung eines Bezugsgrößen-Werts (`BK-…`): dieselbe Reihenfolge und dieselben
        Ablehnungen; die Freigabe schreibt die Wert-Fassung (Urheber = Ersteller, Freigeber = wer freigibt) und
        das Ereignis `correction` mit Bezug `bezugsgroesse`. Hat sich der Wert seit dem Vorschlag geändert, ist
        das 409 `gleichzeitig`. Ein `BK-…` wird nicht zurückgenommen (404 an `…/zuruecknehmen`).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/KorrekturFreigeben" }
            example:
              begruendung: "Nachlieferung nach Endgültigkeit (Box Halle 2 repariert)"
      responses:
        "200":
          description: Freigegeben - mit dem Protokoll der vier Angaben.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KorrekturEntscheidung" }
        "400": { $ref: "#/components/responses/KorrekturFreigabeAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/KorrekturFreigabeAbgelehnt" }
        "404": { $ref: "#/components/responses/KorrekturFreigabeAbgelehnt" }
        "409": { $ref: "#/components/responses/KorrekturFreigabeAbgelehnt" }
        "422": { $ref: "#/components/responses/KorrekturFreigabeAbgelehnt" }
  /api/v1/korrekturen/{kennung}/zuruecknehmen:
    parameters:
      - { name: kennung, in: path, required: true, schema: { type: string, pattern: "^(K|BK)-[0-9]{4}-[0-9]{4,}$" }, description: "K-… eine Korrektur einer Reihe (AP-08), BK-… die Berichtigung eines Bezugsgrößen-Werts (AP-09 IP-7)." }
    post:
      tags: [korrekturen]
      summary: Nimmt eine freigegebene Korrektur zurück - sie bleibt sichtbar.
      description: >
        Recht `korrektur.zuruecknehmen` (AP-08 §5): Energiemanager und Kundenadministrator, der Bearbeiter nur
        die eigene und nur bei Vier-Augen aus; nie der Unterstützer. Grund 10 bis 500 Zeichen (422
        `begruendung_fehlt`), Stand `freigegeben` (409 `status_passt_nicht`). Fremd ist 404.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/KorrekturZuruecknehmen" }
            example:
              grund: "Die Box hat den Puffer doppelt geschickt."
      responses:
        "200":
          description: Zurückgenommen.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/KorrekturEntscheidung" }
        "400": { $ref: "#/components/responses/KorrekturFreigabeAbgelehnt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/KorrekturFreigabeAbgelehnt" }
        "404": { $ref: "#/components/responses/KorrekturFreigabeAbgelehnt" }
        "409": { $ref: "#/components/responses/KorrekturFreigabeAbgelehnt" }
        "422": { $ref: "#/components/responses/KorrekturFreigabeAbgelehnt" }
  /api/v1/standorte/{standortId}/korrekturen:
    get:
      tags: [UEMS]
      summary: "Korrekturen im sichtbaren Standort"
      description: >-
        Mandant aus dem Anmeldekontext; alle betroffenen Standorte müssen sichtbar sein, sonst 404.
        Schreibwege prüfen die Rechte aus AP-08 IP-15. Vorschau und Job teilen denselben Rechenplan.
        Vorschläge verändern keine Werte. Bei Vier-Augen darf der Ersteller nicht selbst freigeben.
      parameters:
        - name: standortId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: "Korrekturen im sichtbaren Standort"
          content:
            application/json:
              schema: { type: array, items: { $ref: '#/components/schemas/KorrekturPortalDetail' } }
        '400': { description: Unlesbare Anfrage oder unbekanntes Feld }
        '403': { description: Recht fehlt oder zweite Person nötig }
        '404': { description: Nicht sichtbar oder nicht vorhanden }
        '409': { description: Bereits entschieden oder gleichzeitig geändert }
        '422': { description: Methode, Zeitraum, Begründung oder Beleg passen nicht }
  /api/v1/korrekturen/{kennung}:
    get:
      tags: [UEMS]
      summary: "Korrektur prüfen: gespeicherte Vorschau und Auswirkungen"
      description: >-
        Mandant aus dem Anmeldekontext; alle betroffenen Standorte müssen sichtbar sein, sonst 404.
        Schreibwege prüfen die Rechte aus AP-08 IP-15. Vorschau und Job teilen denselben Rechenplan.
        Vorschläge verändern keine Werte. Bei Vier-Augen darf der Ersteller nicht selbst freigeben.
      parameters:
        - name: kennung
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: "Korrektur prüfen: gespeicherte Vorschau und Auswirkungen"
          content:
            application/json:
              schema: { $ref: '#/components/schemas/KorrekturPortalDetail' }
        '400': { description: Unlesbare Anfrage oder unbekanntes Feld }
        '403': { description: Recht fehlt oder zweite Person nötig }
        '404': { description: Nicht sichtbar oder nicht vorhanden }
        '409': { description: Bereits entschieden oder gleichzeitig geändert }
        '422': { description: Methode, Zeitraum, Begründung oder Beleg passen nicht }
  /api/v1/messstellen/{kennzeichen}/ersatzwerte/luecken:
    get:
      tags: [UEMS]
      summary: Lücken und Zählerbrüche für die Ersatzwert-Methodenwahl
      description: Lesend, neueste Ereignisfortschreibung je Kennung. Sichtbare Quelle und Standort erforderlich, sonst 404. Zeitraum höchstens 366 Tage.
      parameters:
        - { name: kennzeichen, in: path, required: true, schema: { type: string } }
        - { name: quelle_id, in: query, required: true, schema: { type: string, format: uuid } }
        - { name: von, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: bis, in: query, required: true, schema: { type: string, format: date-time } }
      responses:
        '200':
          description: Tatsächliche Lücken ohne geschätzten Zuwachs
          content:
            application/json:
              schema: { type: array, items: { $ref: '#/components/schemas/ErsatzwertLuecke' } }
        '404': { description: Nicht sichtbar oder nicht vorhanden }
        '422': { description: Ungültiger Zeitraum }
  /api/v1/messstellen/{kennzeichen}/ersatzwerte/vorschau:
    post:
      tags: [UEMS]
      summary: "Ersatzwert ohne Schreibvorgang berechnen"
      description: >-
        Mandant aus dem Anmeldekontext; alle betroffenen Standorte müssen sichtbar sein, sonst 404.
        Schreibwege prüfen die Rechte aus AP-08 IP-15. Vorschau und Job teilen denselben Rechenplan.
        Vorschläge verändern keine Werte. Bei Vier-Augen darf der Ersteller nicht selbst freigeben.
      parameters:
        - name: kennzeichen
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ErsatzwertEingabe' }
      responses:
        '200':
          description: "Ersatzwert ohne Schreibvorgang berechnen"
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErsatzwertVorschau' }
        '400': { description: Unlesbare Anfrage oder unbekanntes Feld }
        '403': { description: Recht fehlt oder zweite Person nötig }
        '404': { description: Nicht sichtbar oder nicht vorhanden }
        '409': { description: Bereits entschieden oder gleichzeitig geändert }
        '422': { description: Methode, Zeitraum, Begründung oder Beleg passen nicht }
  /api/v1/messstellen/{kennzeichen}/ersatzwerte:
    post:
      tags: [UEMS]
      summary: "Ersatzwert erfassen; bei Vier-Augen als Vorschlag"
      description: >-
        Mandant aus dem Anmeldekontext; alle betroffenen Standorte müssen sichtbar sein, sonst 404.
        Schreibwege prüfen die Rechte aus AP-08 IP-15. Vorschau und Job teilen denselben Rechenplan.
        Vorschläge verändern keine Werte. Bei Vier-Augen darf der Ersteller nicht selbst freigeben.
      parameters:
        - name: kennzeichen
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ErsatzwertEingabe' }
      responses:
        '200':
          description: "Ersatzwert erfassen; bei Vier-Augen als Vorschlag"
          content:
            application/json:
              schema: { $ref: '#/components/schemas/KorrekturPortalDetail' }
        '400': { description: Unlesbare Anfrage oder unbekanntes Feld }
        '403': { description: Recht fehlt oder zweite Person nötig }
        '404': { description: Nicht sichtbar oder nicht vorhanden }
        '409': { description: Bereits entschieden oder gleichzeitig geändert }
        '422': { description: Methode, Zeitraum, Begründung oder Beleg passen nicht }
  /api/v1/korrekturen/{kennung}/ablehnen:
    post:
      tags: [UEMS]
      summary: "Vorschlag mit Begründung ablehnen"
      description: >-
        Mandant aus dem Anmeldekontext; alle betroffenen Standorte müssen sichtbar sein, sonst 404.
        Schreibwege prüfen die Rechte aus AP-08 IP-15. Vorschau und Job teilen denselben Rechenplan.
        Vorschläge verändern keine Werte. Bei Vier-Augen darf der Ersteller nicht selbst freigeben.
      parameters:
        - name: kennung
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/KorrekturZuruecknehmen' }
      responses:
        '200':
          description: "Vorschlag mit Begründung ablehnen"
          content:
            application/json:
              schema: { $ref: '#/components/schemas/KorrekturPortalDetail' }
        '400': { description: Unlesbare Anfrage oder unbekanntes Feld }
        '403': { description: Recht fehlt oder zweite Person nötig }
        '404': { description: Nicht sichtbar oder nicht vorhanden }
        '409': { description: Bereits entschieden oder gleichzeitig geändert }
        '422': { description: Methode, Zeitraum, Begründung oder Beleg passen nicht }
  /api/v1/ersatzwerte/{kennung}/zuruecknehmen:
    post:
      tags: [UEMS]
      summary: "Ersatzwert append-only zurücknehmen"
      description: >-
        Mandant aus dem Anmeldekontext; alle betroffenen Standorte müssen sichtbar sein, sonst 404.
        Schreibwege prüfen die Rechte aus AP-08 IP-15. Vorschau und Job teilen denselben Rechenplan.
        Vorschläge verändern keine Werte. Bei Vier-Augen darf der Ersteller nicht selbst freigeben.
      parameters:
        - name: kennung
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/KorrekturZuruecknehmen' }
      responses:
        '200':
          description: "Ersatzwert append-only zurücknehmen"
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ErsatzwertStand' }
        '400': { description: Unlesbare Anfrage oder unbekanntes Feld }
        '403': { description: Recht fehlt oder zweite Person nötig }
        '404': { description: Nicht sichtbar oder nicht vorhanden }
        '409': { description: Bereits entschieden oder gleichzeitig geändert }
        '422': { description: Methode, Zeitraum, Begründung oder Beleg passen nicht }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: >
        Keycloak-issued OIDC access token (realm `voltpilot`). Must carry a
        `tenant_id` claim; the API sets it as the Postgres `app.tenant_id`
        session variable to drive Row-Level-Security.

        Tenant switcher (Portal-Admins only): a token with the `platform-admin`
        realm role (which carries NO tenant_id) may select a tenant per request
        via the `X-Tenant-Id` header (UUID). The customer endpoints then serve
        that tenant's data through the same RLS scoping the customer gets. The
        header is IGNORED for every non-admin token, so it can never widen a
        customer's access; an admin without the header sees zero rows
        (default-deny).
  parameters:
    Kundenbereich:
      name: X-Kundenbereich
      in: header
      required: false
      description: >
        UEMS AP-03 IP-4 - der Kundenbereich (Mandanten-Kennung) eines Partner- oder Plattform-Kontos. Angenommen nur
        gegen eine wirksame Unterstützung des Kontos in genau diesem Kundenbereich; sonst 404 auf jeder Kundenroute
        (nie 403, nie eine leere Liste) und `/api/v1/me` ohne Kundenbereich. Ein Kundenkonto ignoriert ihn;
        `X-Tenant-Id` bleibt der Umschalter der Plattform (bis IP-8) und allein für `/api/v1/admin/**` bestimmt.
      schema: { type: string, format: uuid }
    ProtokollVon:
      name: von
      in: query
      required: false
      schema: { type: string }
      description: >
        Der Beginn des Zeitraums auf der gewaehlten Achse - ein Zeitpunkt mit
        Versatz (2026-11-18T10:00:00+01:00, das "+" URL-kodiert) oder ein Tag
        (2026-11-18, dann dessen Beginn in Europe/Berlin). Fehlend = ohne Grenze.
        Der Zeitraum ist halboffen: "von" zaehlt mit. Mit `achse=gueltigkeit` NUR
        ein Tag (2027-02-01).
    ProtokollBis:
      name: bis
      in: query
      required: false
      schema: { type: string }
      description: >
        Das Ende des Zeitraums, AUSSCHLIESSLICH ("bis" zaehlt nicht mehr mit);
        dieselbe Form wie "von". Fehlend = ohne Grenze. Mit `achse=gueltigkeit`
        NUR ein Tag, und er zaehlt MIT (2027-02-28 = der ganze Februar).
    ProtokollAchse:
      name: achse
      in: query
      required: false
      schema: { type: string, enum: [wirkung, eintrag, gueltigkeit], default: wirkung }
      description: >
        WELCHE Zeitachse "von"/"bis" filtert und wonach sortiert wird.
        `wirkung` (Vorgabe) ist "gilt ab" - wann die Aenderung GILT; `eintrag` ist
        "eingetragen am" - wann sie EINGETRAGEN wurde. Ein rueckwirkender
        Zaehlerwechsel gilt am 18.11. um 10:40 und wurde um 11:05 eingetragen: mit
        `wirkung` steht er im Zeitraum 10:00-11:00, mit `eintrag` im Zeitraum
        11:00-12:00 - beide Male genau EINMAL. `gueltigkeit` (AP-02 IP-14) sortiert
        wie `wirkung` und liefert, was in einen Zeitraum aus TAGEN reicht: "gilt ab"
        <= letzter Tag und ("gilt bis" fehlt oder >= erster Tag) - die Regel
        `reicht_in_zeitraum` des Ortsbaum-Vertrags. Eintraege der Messstellen und
        Datenquellen haben kein "gilt bis" und reichen nur in ihren eigenen Tag.
    ProtokollLimit:
      name: limit
      in: query
      required: false
      schema: { type: integer, minimum: 1, maximum: 500, default: 100 }
      description: Wie viele Eintraege diese Seite hoechstens traegt.
    ProtokollNach:
      name: nach
      in: query
      required: false
      schema: { type: string }
      description: >
        Der Fortsetzungszeiger: der Wert `weiter` der vorigen Seite. Er traegt das
        Sortier-Tripel (Zeit auf der Achse, Herkunft, laufende Nummer), damit die
        naechste Seite auch bei gleichen Zeitstempeln genau hinter dem letzten
        gelieferten Eintrag ansetzt.
  responses:
    EnergieeinsatzAbgelehnt:
      description: Lesbare Ablehnung mit stabiler Kennung; kein Schreibvorgang bei Ablehnung.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/EnergieeinsatzFehler" }
    Unauthorized:
      description: Missing, expired or invalid Bearer token.
    Forbidden:
      description: Authenticated, but lacking the required role (e.g. a customer calling an admin endpoint).
    NotFound:
      description: >
        Not visible for the caller's tenant (RLS) or unknown. A foreign site is
        404, never 403.
    BadRequest:
      description: >
        Rejected before anything was written; the body carries a German
        `message`.
    UnterstuetzungAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben - kein Zugriff, kein Protokoll, kein Hinweis. Der Körper trägt `code`,
        einen deutschen `message`-Satz und die Fakten des Urteils (siehe UnterstuetzungFehler).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/UnterstuetzungFehler" }
    ZugriffAbgelehnt:
      description: >
        UEMS AP-03 IP-9 - abgelehnt, nichts geschrieben. `code` ist der Grund des Rechte-Vertrags
        (`eigene_zuweisung`, `letzter_kundenadministrator`, `standort_fehlt`), `message` sein Kundensatz.
      content:
        application/json:
          schema:
            type: object
            required: [code, message]
            properties:
              code:
                type: string
                enum: [eigene_zuweisung, letzter_kundenadministrator, standort_fehlt]
              message: { type: string }
    ZugriffBeendet:
      description: >
        UEMS AP-03 IP-9 (§4.7, §5.9) - der Aufrufer HATTE diesen Zugang, und er ist vorbei: 404 mit `code`
        `zugriff_beendet` und dem Satz „Ihr Zugriff auf … wurde beendet." bzw. „Ihre Unterstützung für … ist
        beendet.". Wer den Zugang NIE hatte, bekommt weiter die stumme 404 - die Existenz wird nicht bestätigt.
      content:
        application/json:
          schema:
            type: object
            required: [code, message]
            properties:
              code: { type: string, enum: [zugriff_beendet] }
              message: { type: string }
              standort: { type: string, nullable: true }
              kundenbereich: { type: string, nullable: true }
    MessstelleAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben. Der Körper trägt `code`, einen deutschen
        `message`-Satz und die Fakten des Urteils (siehe MessstelleFehler).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/MessstelleFehler" }
    MessstelleWerteAbgelehnt:
      description: >
        Die Anfrage an das Lese-Modell ist abgelehnt, nichts ist gelesen: `code`
        `anfrage_ungueltig`, ein deutscher `message`-Satz, `feld` und `grund`.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/MessstelleWerteFehler" }
    BezugsbasisAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben. `{code, message, …Fakten}`; die Codes stehen in bezugsbasis.md §13
        (403/404 der Kennzahl in deren Form).
      content:
        application/json:
          schema: { type: object, additionalProperties: true, required: [code, message],
                    properties: { code: { type: string }, message: { type: string } } }
    VerbesserungAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben. `{code, message, …Fakten}` (UEMS AP-18: Energieziel, Maßnahme, Abweichung;
        403/404 der Kennzahl in deren Form).
      content:
        application/json:
          schema: { type: object, additionalProperties: true, required: [code, message],
                    properties: { code: { type: string }, message: { type: string } } }
    EnergiemanagementAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben - auch kein Protokolleintrag. `{code, message, …Fakten}` (UEMS AP-19); 403
        `recht_fehlt` in der Form des Rechte-Interceptors.
      content:
        application/json:
          schema: { type: object, additionalProperties: true, required: [code, message],
                    properties: { code: { type: string }, message: { type: string } } }
    BezugsgroesseAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben - auch kein Protokolleintrag. Code, Status und der
        deutsche `message`-Satz kommen aus dem geschlossenen Satz des Vertrags
        (bezugsdaten-vectors.json → verwalten.ablehnungen); dazu die Fakten.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/BezugsgroesseFehler" }
    KennzahlAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben - auch kein Protokolleintrag. Code und Status kommen aus dem
        geschlossenen Satz des Vertrags (kennzahl-vectors.json → schnittstelle.ablehnungen); der
        deutsche `message`-Satz aus der Schnittstelle, der Regel oder der Rechte-Ableitung; dazu die Fakten.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/KennzahlFehler" }
    KostenstelleProzessAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben - auch kein Protokolleintrag. Code, Status und der
        deutsche `message`-Satz kommen aus dem geschlossenen Satz
        (uems/KostenstelleProzessAbgelehnt); dazu die Fakten.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/KostenstelleProzessFehler" }
    BerichtAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben - kein Bericht, kein Stand, kein Protokoll, kein Ereignis. Code
        und Status aus dem geschlossenen Satz (uems/BerichtAbgelehnt): die Codes des Vertrags
        (bericht.md, Report §5.8) mit dem Kundensatz ihrer Regel, ein 403 mit dem Satz der
        Rechte-Ableitung; dazu die Fakten.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/BerichtFehler" }
    KorrekturFreigabeAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben. Code, Status und der deutsche `message`-Satz kommen aus dem
        geschlossenen Satz (uems/KorrekturFreigabeAbgelehnt); ein 403 trägt den Satz der
        Rechte-Ableitung. Dazu die Fakten.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/KorrekturFreigabeFehler" }
    VerteilungAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben - auch kein Protokolleintrag und kein Ereignis. Code,
        Status und der deutsche `message`-Satz kommen aus dem geschlossenen Satz
        (uems/VerteilungAbgelehnt); dazu die Fakten.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/VerteilungFehler" }
    NetzanschlussAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben - auch kein Protokolleintrag. Code, Status und der
        deutsche `message`-Satz kommen aus dem geschlossenen Satz
        (uems/NetzanschlussAbgelehnt); dazu die Fakten.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/NetzanschlussFehler" }
    FunktionAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben. 400 `anfrage_ungueltig`, 404 `nicht_gefunden` (fremd, nie 403),
        409 mit dem Grund des Vertrags funktion-zustand und seinem Satz (Steuern & Optimieren, dazu
        `bereits_angelegt`/`standort_archiviert` beim Einrichten von Messen & Auswerten); bei `pruefliste_offen` die roten
        Zeilen als `fehlt` und `wege` (uems/FunktionAbgelehnt).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/FunktionFehler" }
    GemeinsameSteuerungAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben (uems/GemeinsameSteuerungAbgelehnt). 400 `anfrage_ungueltig` (auch Mandant
        oder Anlage im Körper), 404 `nicht_gefunden` (fremd, nie 403), 409 mit dem ersten Wort des
        Ablehnungs-Vokabulars und allen Befunden in `fehlt` — oder einem Übergangs-Grund: `nicht_eingerichtet` ·
        `nicht_aktiv` · `nicht_angehalten` · `vom_betreiber_angehalten` · `bereits_aktiv` · `erst_anhalten` · `anteile_in_kraft` ·
        `kein_mitglied` · `bereits_bestaetigt` · `kein_vorschlag` · `vorbehalt_gemessen` · `nicht_beobachtet` ·
        `sprungprobe_nicht_gemeldet` · `sprungprobe_laeuft` · `netzpunkt_nicht_frisch` · `nicht_zugestellt`. 422
        `erklaerung_unvollstaendig`: die Erklärung ist unvollständig, `fehlt` nennt je Lücke `wort` (`geraete` ·
        `komponente` · `ungesteuerte_erzeuger`) mit `box_id`/`komponente_id`.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/GemeinsameSteuerungFehler" }
    BilanzAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben. Code, Status und der deutsche `message`-Satz kommen aus
        uems/BilanzAbgelehnt; dazu die Fakten.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/BilanzFehler" }
    MessstelleFormelAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben - auch kein Protokolleintrag. Der Körper trägt
        `code`, einen deutschen `message`-Satz und die Fakten des Urteils (siehe
        MessstelleFormelFehler).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/MessstelleFormelFehler" }
    KadenzAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben - auch kein Protokolleintrag. Der Körper trägt
        `code`, einen deutschen `message`-Satz und die Fakten des Urteils (siehe
        KadenzFehler).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/KadenzFehler" }
    DatenquelleAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben. Der Körper trägt `code`, einen deutschen
        `message`-Satz (bei den Gründen des Vertrags genau dessen Satz) und die Fakten
        des Urteils (siehe DatenquelleFehler).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/DatenquelleFehler" }
    OrtAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben (auch kein Protokolleintrag). Der Körper
        trägt `code`, einen deutschen `message`-Satz mit Grund und Weg (AP-02
        §5.10) und die Fakten des Urteils (siehe OrtFehler).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/OrtFehler" }
    EinstellungAbgelehnt:
      description: >
        Abgelehnt, nichts geschrieben (auch kein Protokolleintrag). Der Körper
        trägt `code`, einen deutschen `message`-Satz und die Fakten des Urteils
        (siehe EinstellungFehler).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/EinstellungFehler" }
  schemas:
    BewertungKriterienWerte:
      type: object
      additionalProperties: false
      required: [K1, K2, K3, K5, K6, K7, K8, mindest_monate]
      description: >
        JSON-Typen und Startwerte aus bewertung-vectors.json#/startwerte, K4 hat keine Zahl.
        Prozent 0 bis 100, Menge nichtnegativ; mindest_monate höchstens K7.
      properties:
        K1: { type: string, pattern: '^(0|[1-9][0-9]*)(\.[0-9]+)?$', description: Anteil in Prozent. }
        K2: { type: string, pattern: '^(0|[1-9][0-9]*)(\.[0-9]+)?$', description: Kumulierter Block in Prozent. }
        K3: { type: string, pattern: '^(0|[1-9][0-9]*)(\.[0-9]+)?$', description: Jahresmenge in kWh. }
        K5: { type: string, pattern: '^(0|[1-9][0-9]*)(\.[0-9]+)?$', description: Datenlage in Prozent. }
        K6: { type: string, pattern: '^(0|[1-9][0-9]*)(\.[0-9]+)?$', description: Ersatzwert-Anteil in Prozent. }
        K7: { type: integer, minimum: 1, maximum: 2147483647, description: Volle Monate. }
        K8: { type: string, pattern: '^(0|[1-9][0-9]*)(\.[0-9]+)?$', description: Messabdeckung in Prozent. }
        mindest_monate: { type: integer, minimum: 1, maximum: 2147483647 }
    BewertungKriterienSpeichern:
      type: object
      additionalProperties: false
      required: [werte, begruendung]
      properties:
        werte: { $ref: "#/components/schemas/BewertungKriterienWerte" }
        begruendung: { type: string, minLength: 1 }
    BewertungKriterienAkteur:
      type: object
      nullable: true
      required: [sub, name, rolle, art]
      properties:
        sub: { type: string, nullable: true }
        name: { type: string }
        rolle: { type: string, nullable: true }
        art: { type: string, enum: [kunde, unterstuetzung, voltpilot, notfall] }
    BewertungKriterienFassung:
      type: object
      required: [fassung, werte, kriterien, herkunft, gueltig_ab, begruendung, akteur, vieraugen, freigabe_status, entschieden_von, entschieden_am, entscheidungs_begruendung, created_at, aufgehoben_am]
      properties:
        fassung: { type: integer, minimum: 1 }
        werte: { $ref: "#/components/schemas/BewertungKriterienWerte" }
        kriterien:
          type: array
          minItems: 8
          maxItems: 8
          description: K1 bis K8 in Reihenfolge; K4 ohne Schwelle, Einheit und Vergleich.
          items:
            type: object
            required: [kennung, schwelle, einheit, vergleich]
            properties:
              kennung: { type: string, enum: [K1, K2, K3, K4, K5, K6, K7, K8] }
              schwelle:
                oneOf:
                  - { type: string, nullable: true }
                  - { type: integer }
              einheit: { type: string, nullable: true, enum: ['%', kWh, Monate, null] }
              vergleich: { type: string, nullable: true, enum: ['>=', '<=', '=', kumuliert bis, null] }
        herkunft: { type: string, enum: [Vorgabe, Unternehmen] }
        gueltig_ab: { type: string, format: date, nullable: true, description: Null bei ungespeicherter Vorgabe und unwirksamen Anträgen. }
        begruendung: { type: string, nullable: true }
        akteur: { $ref: "#/components/schemas/BewertungKriterienAkteur" }
        vieraugen: { type: boolean, description: Einstellung beim Beantragen; spätere Änderungen umgehen die Freigabe nicht. }
        freigabe_status: { type: string, enum: [beantragt, freigegeben, abgelehnt] }
        entschieden_von:
          allOf: [{ $ref: "#/components/schemas/BewertungKriterienAkteur" }]
          nullable: true
        entschieden_am: { type: string, format: date-time, nullable: true }
        entscheidungs_begruendung: { type: string, nullable: true }
        created_at: { type: string, format: date-time, nullable: true }
        aufgehoben_am: { type: string, format: date-time, nullable: true }
    BewertungKriterienFehler:
      type: object
      required: [code, message]
      properties:
        code: { type: string, enum: [anfrage_ungueltig, nicht_gefunden, kriterien_ungueltig, begruendung_fehlt, freigabe_offen, bereits_entschieden, zweite_person_noetig] }
        message: { type: string }
    BewertungUmfangSpeichern:
      type: object
      additionalProperties: false
      required: [gueltig_ab, standort_ids, traeger, ausschluesse]
      properties:
        gueltig_ab: { type: string, format: date }
        standort_ids: { type: array, items: { type: string, format: uuid } }
        traeger:
          type: array
          minItems: 1
          items: { type: string, enum: [Strom, Gas, Wärme, Kälte, Wasser, Druckluft] }
        ausschluesse: { type: array, items: { $ref: "#/components/schemas/BewertungUmfangAusschluss" } }
        begruendung: { type: string, nullable: true }
    BewertungUmfangAusschluss:
      type: object
      additionalProperties: false
      required: [art, verweis, begruendung]
      properties:
        art: { type: string, enum: [standort, anlage, prozess] }
        verweis: { type: string, format: uuid }
        begruendung: { type: string, minLength: 1 }
    BewertungUmfangTraeger:
      type: object
      required: [name, mit_anteil]
      properties:
        name: { type: string, enum: [Strom, Gas, Wärme, Kälte, Wasser, Druckluft] }
        mit_anteil: { type: boolean, description: Nur Strom; alle anderen Träger sind im Umfang ohne Anteil. }
    BewertungMessabdeckung:
      type: object
      required: [von, bis, umfang_id, umfang_fassung, teilansicht, summe, je_einsatz, je_ort]
      properties:
        von: { type: string, format: date }
        bis: { type: string, format: date }
        umfang_id: { type: string, format: uuid, nullable: true }
        umfang_fassung: { type: integer, nullable: true }
        teilansicht: { type: boolean }
        summe: { $ref: "#/components/schemas/BewertungMessabdeckungSumme" }
        je_einsatz: { type: array, items: { $ref: "#/components/schemas/BewertungMessabdeckungEinsatz" } }
        je_ort: { type: array, items: { $ref: "#/components/schemas/BewertungMessabdeckungOrt" } }
    BewertungMessabdeckungSumme:
      type: object
      required: [nenner, gemessen_zugeordnet, abdeckung_prozent, k8, ersatz, ungemessen, ungemessen_prozent]
      properties:
        nenner: { $ref: "#/components/schemas/BewertungRanglisteNenner" }
        gemessen_zugeordnet: { type: string, nullable: true }
        abdeckung_prozent: { type: string, nullable: true }
        k8: { type: string, enum: [ueber_schwelle, unter_schwelle, nicht_anwendbar] }
        ersatz: { type: string, nullable: true, description: Teil der gemessenen Menge, nie zusätzlich addiert. }
        ungemessen: { type: string, nullable: true }
        ungemessen_prozent: { type: string, nullable: true }
    BewertungMessabdeckungEinsatz:
      type: object
      required: [id, kennzeichen, name, prozess_id, traeger, einheit, menge, gemessen, geplant, ersatz, ungemessen]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        name: { type: string }
        prozess_id: { type: string, format: uuid }
        traeger: { type: string, enum: [Strom, Gas, Wärme, Kälte, Wasser, Druckluft] }
        einheit: { type: string, nullable: true }
        menge: { type: string, nullable: true }
        gemessen: { type: array, items: { $ref: "#/components/schemas/BewertungMessabdeckungMesswert" } }
        geplant: { type: array, items: { $ref: "#/components/schemas/BewertungMessabdeckungPlan" } }
        ersatz: { type: array, items: { $ref: "#/components/schemas/BewertungMessabdeckungMesswert" } }
        ungemessen: { type: array, items: { $ref: "#/components/schemas/BewertungMessabdeckungRest" } }
    BewertungMessabdeckungOrt:
      type: object
      required: [art, id, kennzeichen, name, traeger, einheit, gemessen, geplant, ersatz, ungemessen]
      properties:
        art: { type: string, enum: [anlage, standort, gebaeude, bereich, unternehmen, ort] }
        id: { type: string, format: uuid, nullable: true }
        kennzeichen: { type: string, nullable: true }
        name: { type: string, nullable: true }
        traeger: { type: string, enum: [Strom, Gas, Wärme, Kälte, Wasser, Druckluft] }
        einheit: { type: string, nullable: true }
        gemessen: { type: array, items: { $ref: "#/components/schemas/BewertungMessabdeckungMesswert" } }
        geplant: { type: array, items: { $ref: "#/components/schemas/BewertungMessabdeckungPlan" } }
        ersatz: { type: array, items: { $ref: "#/components/schemas/BewertungMessabdeckungMesswert" } }
        ungemessen: { $ref: "#/components/schemas/BewertungMessabdeckungRest" }
    BewertungMessabdeckungMesswert:
      type: object
      required: [id, kennzeichen, ort, menge, einheit]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        ort: { type: string, nullable: true }
        menge: { type: string, nullable: true }
        einheit: { type: string, nullable: true }
    BewertungMessabdeckungPlan:
      type: object
      required: [messstelle_id, kennzeichen, ort, keine_datenquelle_seit, messbedarf]
      properties:
        messstelle_id: { type: string, format: uuid, nullable: true }
        kennzeichen: { type: string, nullable: true }
        ort: { type: string, nullable: true }
        keine_datenquelle_seit: { type: string, format: date, nullable: true }
        messbedarf: { type: string, nullable: true, description: Erst ab IP-19 belegt. }
    BewertungMessabdeckungRest:
      type: object
      nullable: true
      required: [anlage_id, anlage, menge, anteil_prozent]
      properties:
        anlage_id: { type: string, format: uuid }
        anlage: { type: string }
        menge: { type: string, nullable: true }
        anteil_prozent: { type: string, nullable: true }
    BewertungRangliste:
      type: object
      required: [von, bis, umfang_id, umfang_fassung, teilansicht, monate, kriterien, urteil, nenner, zugeordnet, rest, abdeckung_prozent, zustand, anlagen, einsaetze, weitere_traeger]
      properties:
        von: { type: string, format: date }
        bis: { type: string, format: date }
        umfang_id: { type: string, format: uuid, nullable: true }
        umfang_fassung: { type: integer, nullable: true }
        teilansicht: { type: boolean }
        monate: { type: integer, minimum: 1 }
        kriterien: { $ref: "#/components/schemas/BewertungKriterienGrundlage" }
        urteil: { $ref: "#/components/schemas/BewertungStandUrteil" }
        nenner: { $ref: "#/components/schemas/BewertungRanglisteNenner" }
        zugeordnet: { type: string, nullable: true, description: Bekannte Strommenge in kWh als ungerundeter Dezimaltext. }
        rest: { type: string, nullable: true }
        abdeckung_prozent: { type: string, nullable: true, description: Anzeige mit einer Nachkommastelle. }
        zustand: { type: string, enum: [vollständig, unvollständig, ohne Anteil] }
        anlagen: { type: array, items: { $ref: "#/components/schemas/BewertungRanglisteAnlage" } }
        einsaetze: { type: array, items: { $ref: "#/components/schemas/BewertungRanglisteEinsatz" } }
        weitere_traeger: { type: array, items: { $ref: "#/components/schemas/BewertungRanglisteEinsatz" } }
    BewertungRanglisteNenner:
      type: object
      required: [wert, einheit, vorhanden, gesamt, anlagen, zustand]
      properties:
        wert: { type: string, nullable: true }
        einheit: { type: string, nullable: true, enum: [kWh, null] }
        vorhanden: { type: integer, minimum: 0 }
        gesamt: { type: integer, minimum: 0 }
        anlagen: { type: string, example: 3 von 3 }
        zustand: { type: string, enum: [vollständig, unvollständig, ohne Anteil] }
    BewertungRanglisteAnlage:
      type: object
      required: [id, name, ab, nenner, zugeordnet, rest, rest_anteil_prozent, zustand]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        ab: { type: string, format: date, nullable: true, description: Beginn des ersten Bilanzabschnitts im Zeitraum. }
        nenner: { type: string, nullable: true }
        zugeordnet: { type: string, nullable: true }
        rest: { type: string, nullable: true }
        rest_anteil_prozent: { type: string, nullable: true }
        zustand: { type: string, enum: [vollständig, unvollständig, mit Ersatzwert, ohne Bilanz] }
    BewertungRanglisteEinsatz:
      type: object
      required: [id, kennzeichen, name, prozess_id, traeger, einheit, menge, zustand, ersatz, ersatz_prozent, datenlage_prozent, anteil_prozent, kumuliert_zugeordnet_prozent, anteil_zustand, rang, urteil, vorschlag, herkunft, messstellen, prozess_summe_hinweise]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        name: { type: string }
        prozess_id: { type: string, format: uuid }
        traeger: { type: string, enum: [Strom, Gas, Wärme, Kälte, Wasser, Druckluft] }
        einheit: { type: string, nullable: true }
        menge: { type: string, nullable: true }
        zustand: { type: string, enum: [vollständig, unvollständig, mit Ersatzwert, keine Werte] }
        ersatz: { type: string, nullable: true, description: Teil der Menge; null wenn nicht nachweisbar. }
        ersatz_prozent: { type: string, nullable: true }
        datenlage_prozent: { type: string, nullable: true, description: Anteil vollständiger Tage, Anzeige mit einer Nachkommastelle. }
        anteil_prozent: { type: string, nullable: true }
        kumuliert_zugeordnet_prozent: { type: string, nullable: true }
        anteil_zustand: { type: string, enum: [vollständig, unvollständig, mit Ersatzwert, keine Werte, ohne Anteil] }
        rang: { type: integer, nullable: true }
        urteil: { $ref: "#/components/schemas/BewertungEinsatzUrteil" }
        vorschlag: { type: string, enum: [ueber_schwelle, unter_schwelle], description: Keine Einstufung. }
        herkunft: { $ref: "#/components/schemas/BewertungHerkunftEntwurf" }
        messstellen: { type: array, items: { $ref: "#/components/schemas/BewertungRanglisteMessstelle" } }
        prozess_summe_hinweise: { type: array, items: { $ref: "#/components/schemas/ProzessSummeHinweis" }, description: "P4-Hinweise; ändern keine Menge, Formel, Kennzahl oder Einstufung." }
    BewertungKriterienGrundlage:
      type: object
      required: [fassung, werte]
      properties:
        fassung: { type: integer, minimum: 1 }
        werte: { $ref: "#/components/schemas/BewertungKriterienWerte" }
    BewertungStandUrteil:
      type: object
      required: [K7, K8]
      properties:
        K7: { type: string, enum: [erfuellt, unter_zwoelf, vorlaeufig] }
        K8: { type: string, enum: [ueber_schwelle, unter_schwelle, nicht_anwendbar] }
    BewertungEinsatzUrteil:
      type: object
      required: [K1, K2, K3, K5, K6]
      properties:
        K1: { type: string, enum: [ueber_schwelle, unter_schwelle, nicht_anwendbar] }
        K2: { type: string, enum: [ueber_schwelle, unter_schwelle, nicht_anwendbar, nicht_belastbar] }
        K3: { type: string, enum: [ueber_schwelle, unter_schwelle, nicht_anwendbar] }
        K5: { type: string, enum: [erfuellt, vorbehalt_datenlage] }
        K6: { type: string, enum: [erfuellt, vorbehalt_ersatzwerte] }
    BewertungHerkunftEntwurf:
      type: object
      required: [zeitraum, kriterien_fassung, eingaenge, nenner, urteil, vorschlag]
      properties:
        zeitraum: { type: string, example: 2026-10 }
        kriterien_fassung: { type: integer, minimum: 1 }
        eingaenge: { type: array, items: { $ref: "#/components/schemas/BewertungHerkunftEingang" } }
        nenner: { allOf: [{ $ref: "#/components/schemas/BewertungHerkunftNenner" }], nullable: true }
        urteil: { $ref: "#/components/schemas/BewertungEinsatzUrteil" }
        vorschlag: { type: string, enum: [ueber_schwelle, unter_schwelle] }
    BewertungHerkunftEingang:
      type: object
      required: [objekt, von, bis, wert, version, zustand]
      properties:
        objekt: { type: string }
        von: { type: string }
        bis: { type: string }
        wert: { type: string, nullable: true }
        version: { type: integer, nullable: true }
        zustand: { type: string, nullable: true }
    BewertungHerkunftNenner:
      type: object
      required: [wert, anlagen, bilanzwerte]
      properties:
        wert: { type: string, nullable: true }
        anlagen: { type: string }
        bilanzwerte: { type: array, items: { $ref: "#/components/schemas/BewertungBilanzwert" } }
    BewertungBilanzwert:
      type: object
      required: [anlage, von, bis, wert, version, zustand, eingaenge]
      properties:
        anlage: { type: string }
        von: { type: string, format: date }
        bis: { type: string, format: date }
        wert: { type: string, nullable: true }
        version: { type: integer, minimum: 1 }
        zustand: { type: string }
        eingaenge: { type: array, items: { $ref: "#/components/schemas/BewertungBilanzEingang" } }
    BewertungBilanzEingang:
      type: object
      required: [objekt, wert, version, zustand]
      properties:
        objekt: { type: string }
        wert: { type: string, nullable: true }
        version: { type: integer, minimum: 1 }
        zustand: { type: string, nullable: true }
    BewertungRanglisteMessstelle:
      type: object
      required: [id, kennzeichen, anlage_id, einheit, menge, zustand, ersatz, ersatz_prozent, monatswerte]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        anlage_id: { type: string, format: uuid, nullable: true }
        einheit: { type: string }
        menge: { type: string, nullable: true }
        zustand: { type: string, enum: [vollständig, unvollständig, mit Ersatzwert, keine Werte] }
        ersatz: { type: string, nullable: true }
        ersatz_prozent: { type: string, nullable: true }
        monatswerte: { $ref: "#/components/schemas/MessstelleWerte" }
    BewertungUmfangAnlage:
      type: object
      required: [id, name]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
    BewertungUmfangStandort:
      type: object
      required: [id, name, anlagen_im_umfang, anzahl_anlagen_im_umfang]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        anlagen_im_umfang: { type: array, items: { $ref: "#/components/schemas/BewertungUmfangAnlage" } }
        anzahl_anlagen_im_umfang: { type: integer, minimum: 0, description: Ohne Anlage 0; später als 0 von 0 Anlagen darstellbar. }
    BewertungUmfang:
      type: object
      required: [id, fassung, gueltig_ab, am, standorte, traeger, ausschluesse, anlagen_im_umfang, anzahl_anlagen_im_umfang, nenner_traeger, begruendung, akteur, created_at, aufgehoben_am, teilansicht]
      properties:
        id: { type: string, format: uuid, nullable: true }
        fassung: { type: integer, minimum: 1, nullable: true, description: null kennzeichnet den ungespeicherten Vorschlag. }
        gueltig_ab: { type: string, format: date, nullable: true }
        am: { type: string, format: date }
        standorte: { type: array, items: { $ref: "#/components/schemas/BewertungUmfangStandort" } }
        traeger: { type: array, items: { $ref: "#/components/schemas/BewertungUmfangTraeger" } }
        ausschluesse: { type: array, items: { $ref: "#/components/schemas/BewertungUmfangAusschluss" } }
        anlagen_im_umfang: { type: array, items: { $ref: "#/components/schemas/BewertungUmfangAnlage" } }
        anzahl_anlagen_im_umfang: { type: integer, minimum: 0, description: Nur Anlagen im Umfang (y); sagt nichts über Hauptzähler oder Bilanzabdeckung (x). }
        nenner_traeger: { type: string, nullable: true, enum: [Strom, null], description: null ohne Strom; es wird noch kein Nenner berechnet. }
        begruendung: { type: string, nullable: true }
        akteur:
          type: object
          nullable: true
          required: [sub, name, rolle, art]
          properties:
            sub: { type: string, nullable: true }
            name: { type: string }
            rolle: { type: string, nullable: true }
            art: { type: string, enum: [kunde, unterstuetzung, voltpilot, notfall] }
        created_at: { type: string, format: date-time, nullable: true }
        aufgehoben_am: { type: string, format: date-time, nullable: true, description: Ablösung protokolliert; ältere Gültigkeit bleibt am früheren Stichtag lesbar. }
        teilansicht: { type: boolean, description: Liste und Anzahl gelten nur für den sichtbaren Standortumfang. }
    BewertungUmfangHistorie:
      type: object
      required: [fassungen]
      properties:
        fassungen: { type: array, items: { $ref: "#/components/schemas/BewertungUmfang" } }
    BewertungUmfangFehler:
      type: object
      required: [code, message]
      properties:
        code: { type: string, enum: [anfrage_ungueltig, nicht_gefunden, traeger_unbekannt, standort_unbekannt, ausschluss_ungueltig, begruendung_fehlt, gueltig_ab_ungueltig] }
        message: { type: string }
    EnergieeinsatzEinstufungSpeichern:
      type: object
      additionalProperties: false
      required: [einstufung, begruendung, grund, herkunft]
      properties:
        einstufung: { type: string, enum: [wesentlich, nicht_wesentlich] }
        begruendung: { type: string, minLength: 1 }
        grund:
          type: array
          uniqueItems: true
          items: { type: string, enum: [K1, K2, K3, K4] }
        gueltig_ab: { type: string, format: date, description: Vorgabe heute; ein früherer Tag wird als rückwirkend gekennzeichnet. }
        herkunft:
          allOf: [{ $ref: "#/components/schemas/BewertungHerkunftEntwurf" }]
          description: Vollständige Kopie aus der Rangliste; bei Strom ist der Nenner Pflicht und jede Zahl trägt eine Version.
    EnergieeinsatzEinstufungFassung:
      type: object
      required: [fassung, einstufung, begruendung, grund, herkunft, vorgeschlagen_ab, gueltig_ab, gueltig_bis, rueckwirkend, akteur, vieraugen, freigabe_status, entschieden_von, entschieden_am, created_at]
      properties:
        fassung: { type: integer, minimum: 1 }
        einstufung: { type: string, enum: [wesentlich, nicht_wesentlich] }
        begruendung: { type: string }
        grund: { type: array, uniqueItems: true, items: { type: string, enum: [K1, K2, K3, K4] } }
        herkunft: { $ref: "#/components/schemas/BewertungHerkunftEntwurf" }
        vorgeschlagen_ab: { type: string, format: date }
        gueltig_ab: { type: string, format: date, nullable: true }
        gueltig_bis: { type: string, format: date, nullable: true, description: Letzter eingeschlossener Tag. }
        rueckwirkend: { type: boolean }
        akteur: { $ref: "#/components/schemas/BewertungKriterienAkteur" }
        vieraugen: { type: boolean }
        freigabe_status: { type: string, enum: [beantragt, freigegeben] }
        entschieden_von: { $ref: "#/components/schemas/BewertungKriterienAkteur" }
        entschieden_am: { type: string, format: date-time, nullable: true }
        created_at: { type: string, format: date-time }
    EnergieeinsatzEinstufungHistorie:
      type: object
      required: [fassungen]
      properties:
        fassungen: { type: array, items: { $ref: "#/components/schemas/EnergieeinsatzEinstufungFassung" } }
    EnergieeinsatzFehler:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
          enum: [anfrage_ungueltig, nicht_gefunden, prozess_unbekannt, traeger_unbekannt, einsatz_laeuft_bereits, verantwortlicher_unbekannt, einflussgroesse_ungueltig, name_fehlt, grund_fehlt, zeitraum_ungueltig, einsatz_beendet, bezugsgroesse_in_verwendung, recht_fehlt, begruendung_fehlt, einstufung_ungueltig, grund_ungueltig, herkunft_unvollstaendig, gueltig_ab_ungueltig, freigabe_offen, keine_freigabe_offen, zweite_person_noetig, wortlaut_fehlt, messstelle_fehlt, messstelle_nicht_eingerichtet, messbedarf_abgeschlossen]
        message: { type: string }
        energieeinsaetze:
          type: array
          description: Kennzeichen aller referenzierenden Einsätze, auch bei aufgehobenen Einflussgrößen.
          items: { type: string }
    EnergieeinsatzAnlegen:
      type: object
      additionalProperties: false
      required: [prozess_id, traeger, name]
      properties:
        prozess_id: { type: string, format: uuid }
        traeger: { type: string, enum: [Strom, Gas, Wärme, Kälte, Wasser, Druckluft] }
        name: { type: string }
        wortlaut: { type: string, nullable: true }
        verbraucher_wortlaut: { type: string, nullable: true }
        verantwortlich_sub: { type: string, nullable: true, description: Benutzer des Kundenbereichs; kein Recht folgt daraus. }
        gueltig_ab: { type: string, format: date, description: Vorgabe heute in der Zeitzone des Unternehmens. }
        einflussgroessen: { type: array, items: { $ref: "#/components/schemas/EnergieeinsatzEinfluss" } }
    EnergieeinsatzBearbeiten:
      type: object
      additionalProperties: false
      required: [name]
      properties:
        name: { type: string }
        wortlaut: { type: string, nullable: true }
        verbraucher_wortlaut: { type: string, nullable: true }
    EnergieeinsatzBeenden:
      type: object
      additionalProperties: false
      required: [grund]
      properties:
        grund: { type: string }
        gueltig_bis: { type: string, format: date, description: Letzter eingeschlossener Tag; Vorgabe heute. }
    EnergieeinsatzVerantwortlicherSetzen:
      type: object
      additionalProperties: false
      properties:
        verantwortlich_sub: { type: string, nullable: true }
    EnergieeinsatzEinfluss:
      type: object
      additionalProperties: false
      required: [art]
      description: Genau ein Bezugsgrößen-Verweis oder ein nicht leerer Wortlaut.
      properties:
        bezugsgroesse_id: { type: string, format: uuid, nullable: true }
        bezugsgroesse:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/EnergieeinsatzVerweis" }]
          description: Kennzeichen und Name, wenn der Aufrufer die Bezugsgröße sehen darf; sonst nicht enthalten.
        wortlaut: { type: string, nullable: true }
        art: { type: string, enum: [produktion, betriebszeit, wetter, sonstige] }
    EnergieeinsatzEinfluesseSetzen:
      type: object
      additionalProperties: false
      required: [einflussgroessen]
      properties:
        einflussgroessen: { type: array, items: { $ref: "#/components/schemas/EnergieeinsatzEinfluss" } }
    EnergieeinsatzVerweis:
      type: object
      required: [id, kennzeichen, name]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        name: { type: string }
    EnergieeinsatzVerantwortlicher:
      type: object
      properties:
        sub: { type: string, nullable: true }
        name: { type: string, nullable: true, description: Schnappschuss beim Setzen. }
        konto: { type: string, nullable: true, description: Schnappschuss beim Setzen. }
        zustand: { type: string, nullable: true, description: Aktueller Zustand des Kontos; entfernt bei Konto-Ende. }
        ohne_konto_seit: { type: string, format: date-time, nullable: true }
    EnergieeinsatzMessstelle:
      type: object
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        name: { type: string }
        art: { type: string, enum: [gemessen, berechnet] }
        traeger: { type: string }
        orte: { type: array, items: { $ref: "#/components/schemas/MessstelleOrtZuordnung" } }
        zustand: { type: string, description: Lebenszyklus aus dem Messstellen-Register. }
        letzter_monat:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/MessstelleWerte" }]
          description: Letzter voller Monat über den bestehenden Messwerte-Leseweg; berechnete Messstellen ohne Werteübersicht.
    Energieeinsatz:
      type: object
      required: [id, kennzeichen, prozess, traeger, name, verantwortlich, einflussgroessen, messstellen, keine_werte, gueltig_ab]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, pattern: '^EE-[1-9][0-9]{0,12}$' }
        prozess: { $ref: "#/components/schemas/EnergieeinsatzVerweis" }
        traeger: { type: string }
        name: { type: string }
        wortlaut: { type: string, nullable: true }
        verbraucher_wortlaut: { type: string, nullable: true }
        verantwortlich: { $ref: "#/components/schemas/EnergieeinsatzVerantwortlicher" }
        einflussgroessen: { type: array, items: { $ref: "#/components/schemas/EnergieeinsatzEinfluss" } }
        messstellen: { type: array, items: { $ref: "#/components/schemas/EnergieeinsatzMessstelle" } }
        keine_werte: { type: boolean, description: Keine direkte gemessene Messstelle dieses Trägers mit einer Menge im letzten vollen Monat; nie als Nullmenge darstellen. }
        gueltig_ab: { type: string, format: date }
        gueltig_bis: { type: string, format: date, nullable: true }
        beendet_am: { type: string, format: date-time, nullable: true }
        beendet_grund: { type: string, nullable: true }
    EnergieeinsatzListe:
      type: object
      required: [energieeinsaetze]
      properties:
        energieeinsaetze: { type: array, items: { $ref: "#/components/schemas/Energieeinsatz" } }
    EnergieeinsatzVorschlaege:
      type: object
      required: [vorschlaege]
      properties:
        vorschlaege:
          type: array
          items:
            type: object
            properties:
              prozess: { $ref: "#/components/schemas/EnergieeinsatzVerweis" }
              traeger: { type: string, enum: [Strom] }
    EnergieeinsatzProtokoll:
      type: object
      required: [aenderungen]
      properties:
        aenderungen:
          type: array
          items:
            type: object
            required: [id, art, alt, neu, akteur, zeit]
            properties:
              id: { type: integer, format: int64 }
              art: { type: string, enum: [angelegt, bearbeitet, verantwortlicher, einflussgroessen, beendet] }
              alt: { nullable: true, description: Vorheriger Schnappschuss als Objekt oder Einflussgrößen-Liste. }
              neu: { nullable: true, description: Neuer Schnappschuss als Objekt oder Einflussgrößen-Liste. }
              akteur:
                type: object
                properties:
                  sub: { type: string, nullable: true }
                  name: { type: string }
                  rolle: { type: string, nullable: true }
                  art: { type: string, enum: [kunde, unterstuetzung, voltpilot, notfall] }
              zeit: { type: string, format: date-time }
    EnergiemanagementPersonAnlegen:
      type: object
      additionalProperties: false
      required: [name, funktion]
      properties:
        name: { type: string, maxLength: 200 }
        funktion: { type: string, maxLength: 200 }
        kuerzel: { type: string, nullable: true, maxLength: 10, description: So nennen die Kopien die Person („entschieden_von“ „RF“). }
        organisation: { type: string, nullable: true, maxLength: 200 }
        konto_sub: { type: string, nullable: true, description: Ein Konto des Kundenbereichs; ohne Konto ist die Person trotzdem eine Person. }
        seit: { type: string, format: date, nullable: true }
    EnergiemanagementPersonAendern:
      type: object
      additionalProperties: false
      required: [name, funktion]
      description: >
        Der ganze änderbare Stand; ein fehlendes wahlfreies Feld heißt „keins“. Ein anderes `konto_sub` und `bis`
        brauchen `begruendung`.
      properties:
        name: { type: string, maxLength: 200 }
        funktion: { type: string, maxLength: 200 }
        kuerzel: { type: string, nullable: true, maxLength: 10 }
        organisation: { type: string, nullable: true, maxLength: 200 }
        konto_sub: { type: string, nullable: true }
        seit: { type: string, format: date, nullable: true }
        bis: { type: string, format: date, nullable: true, description: Beendet die Person endgültig. }
        begruendung: { type: string, nullable: true, minLength: 10, maxLength: 500 }
    EnergiemanagementPerson:
      type: object
      required: [id, name, funktion, kuerzel, organisation, konto, seit, bis, zustand, beendet_begruendung, eingetragen]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        funktion: { type: string }
        kuerzel: { type: string, nullable: true }
        organisation: { type: string, nullable: true }
        konto:
          type: object
          nullable: true
          description: "`null`: eine Person ohne Konto."
          properties:
            sub: { type: string }
            name: { type: string, nullable: true, description: Anzeigename aus der Benutzerverwaltung. }
            zustand: { type: string, nullable: true }
        seit: { type: string, format: date, nullable: true }
        bis: { type: string, format: date, nullable: true }
        zustand: { type: string, enum: [aktiv, beendet] }
        beendet_begruendung: { type: string, nullable: true }
        eingetragen: { $ref: "#/components/schemas/EnergiemanagementEingetragen" }
    EnergiemanagementEingetragen:
      type: object
      required: [akteur, am]
      properties:
        akteur:
          type: object
          properties:
            sub: { type: string, nullable: true }
            name: { type: string }
            rolle: { type: string, nullable: true }
            art: { type: string, enum: [kunde, unterstuetzung, voltpilot, notfall] }
        am: { type: string, format: date-time }
    EnergiemanagementPersonen:
      type: object
      required: [personen]
      properties:
        personen:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementPerson" }
    EnergiemanagementAenderung:
      type: object
      required: [id, art, alt, neu, begruendung, akteur, zeit]
      properties:
        id: { type: integer, format: int64 }
        art: { type: string, enum: [person_erfasst, person_geaendert, person_beendet, aufgabe_zugeordnet, aufgabe_beendet] }
        alt: { nullable: true, description: Schnappschuss vorher. }
        neu: { nullable: true, description: Schnappschuss nachher. }
        begruendung: { type: string, nullable: true }
        akteur:
          type: object
          properties:
            sub: { type: string, nullable: true }
            name: { type: string }
            rolle: { type: string, nullable: true }
            art: { type: string, enum: [kunde, unterstuetzung, voltpilot, notfall] }
        zeit: { type: string, format: date-time }
    EnergiemanagementPersonMitVerlauf:
      type: object
      required: [person, verlauf]
      properties:
        person: { $ref: "#/components/schemas/EnergiemanagementPerson" }
        verlauf:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementAenderung" }
    EnergiemanagementPersonKurz:
      type: object
      required: [id, name, funktion, kuerzel, mit_konto]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        funktion: { type: string }
        kuerzel: { type: string, nullable: true }
        mit_konto: { type: boolean }
    EnergiemanagementBeleg:
      type: object
      additionalProperties: false
      description: Ein Verweis (G3) - ohne Ablage keiner seiner Teile.
      properties:
        bezeichnung: { type: string, nullable: true, maxLength: 200 }
        ablage: { type: string, nullable: true, maxLength: 200 }
        kennung: { type: string, nullable: true, maxLength: 200 }
        adresse: { type: string, nullable: true, maxLength: 2000 }
        sha256: { type: string, nullable: true, pattern: "^[0-9a-f]{64}$" }
    EnergiemanagementAufgabeZuordnen:
      type: object
      additionalProperties: false
      required: [aufgabe, person_id, gilt_ab, begruendung]
      properties:
        aufgabe: { type: string, enum: [unternehmensleitung, energiemanagement_leiten, energieteam, bezugsbasen, energieziele_massnahmen, bewertung_messplanung, interne_audits, managementbewertung, dokumente, weitere] }
        aufgabe_wortlaut: { type: string, nullable: true, maxLength: 200, description: Nur und Pflicht bei `weitere`. }
        person_id: { type: string, format: uuid }
        gilt_ab: { type: string, format: date }
        vertretung_person_id: { type: string, format: uuid, nullable: true }
        entschieden_von: { type: string, format: uuid, nullable: true, description: Eine Person im Energiemanagement; Pflicht außer bei `unternehmensleitung`. }
        begruendung: { type: string, minLength: 10, maxLength: 500 }
        beleg: { $ref: "#/components/schemas/EnergiemanagementBeleg" }
        beschluss_kennung: { type: string, nullable: true, pattern: "^BR-[0-9]{4}-[0-9]{4,}/B[0-9]{1,3}$" }
    EnergiemanagementAufgabeBeenden:
      type: object
      additionalProperties: false
      required: [gilt_bis, begruendung]
      properties:
        gilt_bis: { type: string, format: date, description: Der letzte Tag, eingeschlossen. }
        begruendung: { type: string, minLength: 10, maxLength: 500 }
    EnergiemanagementZuordnung:
      type: object
      required: [id, aufgabe, wort, aufgabe_wortlaut, person, vertretung, gilt_ab, gilt_bis, zustand, entschieden_von, begruendung, beleg, beschluss_kennung, beendet_begruendung, eingetragen]
      properties:
        id: { type: string, format: uuid }
        aufgabe: { type: string }
        wort: { type: string, description: Das Kundenwort der Aufgabe (Vertrag `woerter.aufgabe`), bei `weitere` der Wortlaut. }
        aufgabe_wortlaut: { type: string, nullable: true }
        person: { $ref: "#/components/schemas/EnergiemanagementPersonKurz" }
        vertretung: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementPersonKurz" }], nullable: true }
        gilt_ab: { type: string, format: date }
        gilt_bis: { type: string, format: date, nullable: true }
        zustand: { type: string, enum: [laufend, beendet] }
        entschieden_von: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementPersonKurz" }], nullable: true }
        begruendung: { type: string }
        beleg: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementBeleg" }], nullable: true }
        beschluss_kennung: { type: string, nullable: true }
        beendet_begruendung: { type: string, nullable: true }
        eingetragen: { $ref: "#/components/schemas/EnergiemanagementEingetragen" }
    EnergiemanagementAufgabe:
      type: object
      required: [aufgabe, wort, laufend, satz]
      properties:
        aufgabe: { type: string }
        wort: { type: string }
        laufend:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementZuordnung" }
        satz: { type: string, nullable: true, description: „… — keine Person festgelegt.“ genau dann, wenn keine Zuordnung läuft (nie bei `weitere`). }
    EnergiemanagementAufgaben:
      type: object
      required: [tag, leitung, aufgaben, zuordnungen]
      properties:
        tag: { type: string, format: date }
        leitung:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementPersonKurz" }
        aufgaben:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementAufgabe" }
        zuordnungen:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementZuordnung" }
    EnergiemanagementVerantwortlich:
      type: object
      required: [sub, name]
      properties:
        sub: { type: string, nullable: true, description: Nur wo das Objekt ein Konto führt; Kennzahl und Bezugsbasis tragen nur den Namen. }
        name: { type: string }
    EnergiemanagementVerantwortungObjekt:
      type: object
      required: [art, id, kennzeichen, titel, verantwortlich, zustand]
      properties:
        art: { type: string, enum: [kennzahl, energieeinsatz, bezugsbasis, energieziel, massnahme, abweichung, internes_audit, feststellung], description: Die Arten des Bestands, dann die angedockten Quellen (`VerantwortungQuelle`) - internes Audit seit IP-18, Feststellung seit IP-19. }
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        titel: { type: string }
        verantwortlich:
          nullable: true
          description: "`null`: am Objekt ist niemand eingetragen."
          allOf: [{ $ref: "#/components/schemas/EnergiemanagementVerantwortlich" }]
        zustand: { type: string, nullable: true, description: Der Zustand des Objekts, wo es selbst einen führt (Energieziel, Maßnahme, Abweichung, internes Audit, Feststellung). }
    EnergiemanagementBezugsbasisFreigabe:
      type: object
      required: [bezugsbasis_id, bezugsbasis, kennzahl_id, kennzahl, fassung, freigegeben_von, freigegeben_am, vieraugen, zweite_person]
      properties:
        bezugsbasis_id: { type: string, format: uuid }
        bezugsbasis: { type: string }
        kennzahl_id: { type: string, format: uuid }
        kennzahl: { type: string }
        fassung: { type: integer }
        freigegeben_von: { type: string, nullable: true }
        freigegeben_am: { type: string, format: date-time, nullable: true }
        vieraugen: { type: boolean }
        zweite_person: { type: string, nullable: true, description: Bei Vier-Augen die Person, die entschieden hat. }
    EnergiemanagementVerantwortung:
      type: object
      required: [tag, leitung, aufgaben, ohne_person, objekte, bezugsbasen_freigaben]
      properties:
        tag: { type: string, format: date }
        leitung:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementPersonKurz" }
        aufgaben:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementAufgabe" }
        ohne_person:
          type: array
          items: { type: string }
          description: Die Aufgaben des Vokabulars ohne laufende Zuordnung am Tag (nie `weitere`) - dieselben, deren `satz` gesetzt ist.
        objekte:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementVerantwortungObjekt" }
        bezugsbasen_freigaben:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementBezugsbasisFreigabe" }
    EnergiemanagementVerzeichnisZeile:
      type: object
      required: [gruppe, art, kennzeichen, titel, nr, entschieden_von, eingetragen_von, tag, pruefsumme, ort, gruppe_wort, ort_satz]
      description: >
        Die Ausgabe der Operation `verzeichnis_zeile` (energiemanagement.md §5). `nr` ist Fassung oder Nr.; was die
        Quelle nicht trägt, bleibt `null` - nie „fehlt“.
      properties:
        gruppe: { type: string }
        art: { type: string }
        kennzeichen: { type: string }
        titel: { type: string }
        nr: { type: integer, nullable: true }
        entschieden_von: { type: string, nullable: true }
        eingetragen_von: { type: string, nullable: true }
        tag: { type: string, format: date, nullable: true }
        pruefsumme: { type: string, nullable: true }
        ort: { type: string, enum: [in_voltpilot, wortlaut_original_beim_kunden, verweis] }
        gruppe_wort: { type: string }
        ort_satz: { type: string, description: "„in VoltPilot“, „Wortlaut in VoltPilot, Original bei Ihnen: <Ablage>“ oder „Geführt in Ihrem System: <Ablage>“ (G1)." }
    EnergiemanagementVerzeichnisZuschnitt:
      type: object
      required: [teil, stufe]
      properties:
        teil: { type: string }
        stufe: { type: string, description: Die Stufe des Teils als Wort (G1). }
    EnergiemanagementVerzeichnisGruppe:
      type: object
      required: [gruppe, gruppe_wort, zuschnitt, satz, zeilen]
      properties:
        gruppe: { type: string }
        gruppe_wort: { type: string }
        zuschnitt:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementVerzeichnisZuschnitt" }
        satz: { type: string, nullable: true, description: "„Hier ist noch nichts festgehalten.“ - nur ohne Zeile." }
        zeilen:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementVerzeichnisZeile" }
    EnergiemanagementVerzeichnisFilter:
      type: object
      required: [gruppe, von, bis, person, person_name]
      properties:
        gruppe: { type: string, nullable: true }
        von: { type: string, format: date, nullable: true }
        bis: { type: string, format: date, nullable: true }
        person: { type: string, format: uuid, nullable: true }
        person_name: { type: string, nullable: true }
    EnergiemanagementVerzeichnis:
      type: object
      required: [stichtag, verantwortung, filter, gruppen]
      properties:
        stichtag: { type: string, format: date-time, description: Der Abruf in der Zeitzone des Unternehmens, auf die Minute. }
        verantwortung: { type: string, description: Der Verantwortungs-Satz (SP4). }
        filter: { $ref: "#/components/schemas/EnergiemanagementVerzeichnisFilter" }
        gruppen:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementVerzeichnisGruppe" }
    EnergiemanagementWiedervorlageZeile:
      type: object
      required: [art, kennzeichen, titel, faellig_am, tage, satz, verantwortlich, id, kennzahl_id]
      description: >
        Die Ausgabe der Operation `wiedervorlage` (energiemanagement.md §3). `faellig_am` kommt aus der Regel des
        Objekts; `id` und `kennzahl_id` tragen den Sprung auf die Seite des Objekts - wo es keine gibt, `null`.
      properties:
        art: { type: string, enum: [dokument_ueberpruefung, internes_audit, managementbewertung, feststellung, bewertung_ueberpruefung, bezugsbasis_ueberpruefung, energieziel_bewertung, massnahme_termin, abweichung_frist, messbedarf_frist, bericht_anstoss] }
        kennzeichen: { type: string }
        titel: { type: string }
        faellig_am: { type: string, format: date }
        tage: { type: integer, description: "Abruf − fällig am in Kalendertagen: positiv = fällig, 0 = heute, negativ = Vorschau." }
        satz: { type: string, description: "„seit n Tagen fällig“, „heute fällig“ oder „fällig in n Tagen“." }
        verantwortlich: { type: string, nullable: true }
        id: { type: string, format: uuid, nullable: true }
        kennzahl_id: { type: string, format: uuid, nullable: true }
    EnergiemanagementWiedervorlage:
      type: object
      required: [stichtag, vorschau_tage, faellig, vorschau, anzahl_faellig, anzahl_vorschau, nicht_in_liste, verantwortung, naechste_managementbewertung]
      properties:
        stichtag: { type: string, format: date-time, description: Der Abruf in der Zeitzone des Unternehmens, auf die Minute. }
        vorschau_tage: { type: integer, description: Das Vorschau-Fenster der Einstellung (Startwert 30). }
        faellig:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementWiedervorlageZeile" }
        vorschau:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementWiedervorlageZeile" }
        anzahl_faellig: { type: integer }
        anzahl_vorschau: { type: integer, description: "Mit `anzahl_faellig` der Baustein „Energiemanagement — n fällig · m in den nächsten 30 Tagen.“ (WV5)." }
        nicht_in_liste:
          type: array
          items: { type: string }
        verantwortung: { type: string, description: Der Verantwortungs-Satz (SP4). }
        naechste_managementbewertung:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/EnergiemanagementWiedervorlageNaechsteManagementbewertung" }]
          description: >
            Additiv (Folge AP-19 IP-24): die Frist von MG7 mit ihrer Herkunft - auch außerhalb des Vorschau-Fensters, aus
            derselben Rechnung wie die Zeile `managementbewertung`; `null` ohne freigegebene Managementbewertung mit Sitzung.
    EnergiemanagementWiedervorlageNaechsteManagementbewertung:
      type: object
      required: [faellig_am, kennzeichen, sitzung_am, rhythmus_monate]
      description: >
        MG7: fällig am = Tag der letzten Sitzung einer freigegebenen Managementbewertung + Rhythmus der Einstellung
        (`managementbewertung_rhythmus_monate`, Startwert 12) - beim Abruf, nie gespeichert.
      properties:
        faellig_am: { type: string, format: date }
        kennzeichen: { type: string, description: "Die Managementbewertung (BR-JJJJ-nnnn), an deren Sitzung die Frist hängt." }
        sitzung_am: { type: string, format: date }
        rhythmus_monate: { type: integer }
    FeststellungQuelle:
      type: object
      additionalProperties: false
      description: Die Quelle (FS1) mit genau ihrem Verweis - `internes_audit` mit `audit_id`, `eigene` ohne, `extern` mit `wortlaut`, `managementbewertung` mit `kennung` (BR-JJJJ-nnnn/Bn).
      properties:
        art: { type: string, enum: [internes_audit, eigene, extern, managementbewertung] }
        audit_id: { type: string, format: uuid, nullable: true }
        kennung: { type: string, nullable: true }
        wortlaut: { type: string, nullable: true, maxLength: 500 }
    FeststellungVorgabe:
      type: object
      additionalProperties: false
      description: Was nicht erfüllt ist - eine Dokument-Fassung (beide Felder) und/oder ein Wortlaut.
      properties:
        dokument_id: { type: string, format: uuid, nullable: true }
        fassung: { type: integer, nullable: true }
        wortlaut: { type: string, nullable: true, maxLength: 2000 }
    FeststellungBezug:
      type: object
      additionalProperties: false
      description: Der Bezug - ohne Standort das Unternehmen (der Zaun, RE1); wahlfrei eine Aufgabe (Vokabular `aufgabe`), ein Dokument, Objekt-Kennzeichen.
      properties:
        standort_id: { type: string, format: uuid, nullable: true }
        aufgabe: { type: string, nullable: true }
        dokument_id: { type: string, format: uuid, nullable: true }
        objekte: { type: array, nullable: true, items: { type: string, maxLength: 60 } }
    FeststellungErfassen:
      type: object
      additionalProperties: false
      description: Erfassen (FS1).
      properties:
        quelle: { $ref: "#/components/schemas/FeststellungQuelle" }
        wortlaut: { type: string, maxLength: 2000 }
        vorgabe: { $ref: "#/components/schemas/FeststellungVorgabe" }
        bezug:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/FeststellungBezug" }]
        festgestellt_von: { type: string, format: uuid, description: Eine Person im Energiemanagement, auch ohne Konto. }
        festgestellt_am: { type: string, format: date, nullable: true, description: Vorgabe heute. }
        verantwortlich: { type: string, description: Das Konto (`sub`). }
        frist: { type: string, format: date, nullable: true, description: Ohne Frist festgestellt am + Frist der Einstellung (Startwert 90 Tage). }
    FeststellungEintragFesthalten:
      type: object
      additionalProperties: false
      description: Ein Eintrag (FS2) - immer die Aussage einer Person an einem Tag.
      properties:
        art: { type: string, enum: [kommentar, behebung, ursache_aussage, aehnliche_faelle] }
        wortlaut: { type: string, maxLength: 2000 }
        person_id: { type: string, format: uuid }
        am: { type: string, format: date, nullable: true, description: Vorgabe heute. }
    FeststellungFristAendern:
      type: object
      additionalProperties: false
      properties:
        frist: { type: string, format: date }
        begruendung: { type: string, description: 10–500 Zeichen. }
    FeststellungVerantwortlichAendern:
      type: object
      additionalProperties: false
      properties:
        verantwortlich: { type: string, description: Das Konto (`sub`). }
        begruendung: { type: string, description: 10–500 Zeichen. }
    FeststellungStandFesthalten:
      type: object
      additionalProperties: false
      description: Ein Stand (FS4, FS5) - `…/wirksamkeit` nimmt `wirksam · nicht_wirksam`, `…/abschliessen` `ohne_massnahme · zurueckgenommen`, `…/wirksamkeit/beantragen` alle vier.
      properties:
        ergebnis: { type: string, enum: [wirksam, nicht_wirksam, ohne_massnahme, zurueckgenommen] }
        begruendung: { type: string, description: 10–500 Zeichen, bei jedem Ergebnis. }
        entschieden_von: { type: string, format: uuid, description: Die Person, die geprüft hat. }
        am: { type: string, format: date, nullable: true, description: Vorgabe heute. }
    FeststellungAblehnen:
      type: object
      additionalProperties: false
      properties:
        begruendung: { type: string, description: 10–500 Zeichen. }
    FeststellungQuelleAus:
      type: object
      required: [art, audit_id, kennung, wortlaut]
      properties:
        art: { type: string, enum: [internes_audit, eigene, extern, managementbewertung] }
        audit_id: { type: string, format: uuid, nullable: true }
        kennung: { type: string, nullable: true, description: AU-… beim Audit, BR-…/Bn bei der Managementbewertung. }
        wortlaut: { type: string, nullable: true }
    FeststellungVorgabeAus:
      type: object
      required: [dokument_id, dokument, fassung, wortlaut]
      properties:
        dokument_id: { type: string, format: uuid, nullable: true }
        dokument: { type: string, nullable: true, description: Das Kennzeichen D-nnnn. }
        fassung: { type: integer, nullable: true }
        wortlaut: { type: string, nullable: true }
    FeststellungBezugAus:
      type: object
      required: [standort_id, aufgabe, dokument_id, dokument, objekte]
      properties:
        standort_id: { type: string, format: uuid, nullable: true }
        aufgabe: { type: string, nullable: true }
        dokument_id: { type: string, format: uuid, nullable: true }
        dokument: { type: string, nullable: true }
        objekte: { type: array, items: { type: string } }
    FeststellungFrist:
      type: object
      required: [abruf, faellig_am, tage, satz, grund]
      description: Vertrag `ueberpruefung` (Art `feststellung`) am Abruf-Tag; abgeschlossen ohne Frist.
      properties:
        abruf: { type: string, format: date }
        faellig_am: { type: string, format: date, nullable: true }
        tage: { type: integer, nullable: true, description: Abruf minus Frist - positiv = überfällig. }
        satz: { type: string, nullable: true, description: „seit n Tagen fällig“, „heute fällig“, „fällig in n Tagen“. }
        grund: { type: string, nullable: true, enum: [abgeschlossen, null] }
    Feststellung:
      type: object
      required: [id, kennzeichen, quelle, wortlaut, vorgabe, bezug, festgestellt_von, festgestellt_am, verantwortlich, frist, zustand, lage, ergebnis, eintraege, massnahmen, eingetragen]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, pattern: "^F-[0-9]{4}-[0-9]{4,9}$" }
        quelle: { $ref: "#/components/schemas/FeststellungQuelleAus" }
        wortlaut: { type: string }
        vorgabe: { $ref: "#/components/schemas/FeststellungVorgabeAus" }
        bezug: { $ref: "#/components/schemas/FeststellungBezugAus" }
        festgestellt_von: { $ref: "#/components/schemas/EnergiemanagementPersonKurz" }
        festgestellt_am: { type: string, format: date }
        verantwortlich: { $ref: "#/components/schemas/EnergiemanagementVerantwortlich" }
        frist: { type: string, format: date }
        zustand: { type: string, enum: [offen, abgeschlossen] }
        lage: { $ref: "#/components/schemas/FeststellungFrist" }
        ergebnis: { type: string, nullable: true, enum: [wirksam, ohne_massnahme, zurueckgenommen, null], description: Das Ergebnis des schließenden Stands. }
        eintraege: { type: integer, description: Anzahl der Einträge. }
        massnahmen: { type: array, items: { type: string }, description: Die Kennzeichen der Maßnahmen mit Herkunft dieser Feststellung. }
        eingetragen: { $ref: "#/components/schemas/EnergiemanagementEingetragen" }
    FeststellungEintrag:
      type: object
      required: [id, art, am, person, wortlaut, eingetragen]
      properties:
        id: { type: integer, format: int64 }
        art: { type: string, enum: [kommentar, behebung, ursache_aussage, aehnliche_faelle] }
        am: { type: string, format: date }
        person: { $ref: "#/components/schemas/EnergiemanagementPersonKurz" }
        wortlaut: { type: string }
        eingetragen: { $ref: "#/components/schemas/EnergiemanagementEingetragen" }
    FeststellungMassnahme:
      type: object
      required: [id, kennzeichen, titel, zustand, termin, umgesetzt_am, verantwortlich]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        titel: { type: string }
        zustand: { type: string, enum: [geplant, umgesetzt, bewertet, verworfen] }
        termin: { type: string, format: date }
        umgesetzt_am: { type: string, format: date, nullable: true }
        verantwortlich: { $ref: "#/components/schemas/EnergiemanagementVerantwortlich" }
    FeststellungStand:
      type: object
      required: [nr, ergebnis, begruendung, am, entschieden_von, kopie, pruefsumme, vieraugen, status, eingetragen, zweite_person, ablehnung_begruendung]
      properties:
        nr: { type: integer }
        ergebnis: { type: string, enum: [wirksam, nicht_wirksam, ohne_massnahme, zurueckgenommen] }
        begruendung: { type: string }
        am: { type: string, format: date }
        entschieden_von: { $ref: "#/components/schemas/EnergiemanagementPersonKurz" }
        kopie: { type: object, description: "`feststellung`, `wortlaut`, `eintraege` (Anzahl), `massnahmen` (kennzeichen, zustand, umgesetzt_am), `aufgabe` (aufgabe, person, vertretung, gilt_ab, entschieden_von - ohne Aufgabe im Bezug null), `am`." }
        pruefsumme: { type: string, pattern: "^sha256:[0-9a-f]{64}$" }
        vieraugen: { type: boolean }
        status: { type: string, enum: [beantragt, freigegeben, abgelehnt] }
        eingetragen: { $ref: "#/components/schemas/EnergiemanagementEingetragen" }
        zweite_person:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/EnergiemanagementEingetragen" }]
        ablehnung_begruendung: { type: string, nullable: true }
    FeststellungVierAugen:
      type: object
      required: [an, erfuellbar, berechtigte, zweite_person, satz]
      description: >
        FS6/W15 als Antwortfeld: ohne Vier-Augen `an` = false. Sonst die Berechtigten (KA/EM am Unternehmen), wer jetzt
        zweite Person sein kann (nie die Urheberin, nie der Verantwortliche) und ohne jemanden `erfuellbar` = false mit
        `satz` („Vier-Augen nicht erfüllbar: außer … darf niemand freigeben, und … sind hier beteiligt.“).
      properties:
        an: { type: boolean }
        erfuellbar: { type: boolean }
        berechtigte:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementVerantwortlich" }
        zweite_person:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementVerantwortlich" }
        satz: { type: string, nullable: true }
    FeststellungAenderung:
      type: object
      required: [id, art, alt, neu, begruendung, akteur, zeit]
      properties:
        id: { type: integer, format: int64 }
        art: { type: string, enum: [feststellung_erfasst, eintrag, feststellung_geaendert, wirksamkeit_beantragt, wirksamkeit_geprueft, wirksamkeit_abgelehnt, feststellung_abgeschlossen] }
        alt: { nullable: true, description: Schnappschuss vorher. }
        neu: { nullable: true, description: Schnappschuss nachher (beim Eintrag der Eintrag, beim Stand der Stand ohne Kopie). }
        begruendung: { type: string, nullable: true }
        akteur:
          type: object
          properties:
            sub: { type: string, nullable: true }
            name: { type: string }
            rolle: { type: string, nullable: true }
            art: { type: string, enum: [kunde, unterstuetzung, voltpilot, notfall] }
        zeit: { type: string, format: date-time }
    FeststellungMitVerlauf:
      type: object
      required: [feststellung, eintraege, massnahmen, wirksamkeit, vieraugen, verlauf]
      properties:
        feststellung: { $ref: "#/components/schemas/Feststellung" }
        eintraege:
          type: array
          items: { $ref: "#/components/schemas/FeststellungEintrag" }
        massnahmen:
          type: array
          items: { $ref: "#/components/schemas/FeststellungMassnahme" }
        wirksamkeit:
          type: array
          items: { $ref: "#/components/schemas/FeststellungStand" }
        vieraugen: { $ref: "#/components/schemas/FeststellungVierAugen" }
        verlauf:
          type: array
          items: { $ref: "#/components/schemas/FeststellungAenderung" }
    FeststellungListe:
      type: object
      required: [tag, feststellungen]
      properties:
        tag: { type: string, format: date }
        feststellungen:
          type: array
          items: { $ref: "#/components/schemas/Feststellung" }
    ManagementbewertungPerson:
      type: object
      required: [id, name]
      properties:
        id: { type: string, format: uuid }
        name: { type: [string, "null"] }
    ManagementbewertungSitzung:
      type: object
      required: [tag, leitung, leitung_gilt, teilnehmende]
      properties:
        tag: { type: string, format: date }
        leitung: { $ref: "#/components/schemas/ManagementbewertungPerson" }
        leitung_gilt: { type: boolean, description: "PA3 - die Person hat am Tag der Sitzung die laufende Aufgabe „Leitung des Unternehmens“." }
        teilnehmende: { type: array, items: { $ref: "#/components/schemas/ManagementbewertungPerson" } }
        ort: { type: [string, "null"] }
        eingetragen_von: { type: string }
        eingetragen_am: { type: string, format: date }
    ManagementbewertungFolge:
      type: object
      required: [art, objekt, wie, zustand]
      properties:
        art: { type: string, enum: [energieziel, massnahme, dokument, aufgabe, audit] }
        objekt: { type: string, description: "Kennzeichen (EZ-…, M-…, D-…/n, D-…, AU-…) oder das Aufgaben-Wort." }
        objekt_id: { type: string, format: uuid }
        wie: { type: string, enum: [von_hand, herkunft, zuordnung, fassung, geprueft_bleibt] }
        zustand: { type: string, description: "Das Zustandswort des Objekts von heute." }
        tag: { type: [string, "null"], format: date }
        angabe: { type: [string, "null"], description: "Zielperiode des Energieziels, Person der Aufgabe." }
        verknuepft_am: { type: string, format: date }
        eingetragen_von: { type: string }
    ManagementbewertungBeschluss:
      type: object
      required: [nr, kennung, art, wortlaut, entschieden_von, folgen]
      properties:
        nr: { type: integer }
        kennung: { type: string, description: "BR-JJJJ-nnnn/Bn" }
        art: { type: string, enum: [energieziel, massnahme, dokument, aufgabe, ressourcen, audit, keine_aenderung, weitere] }
        wortlaut: { type: string }
        entschieden_von: { $ref: "#/components/schemas/ManagementbewertungPerson" }
        zustaendig: { oneOf: [{ $ref: "#/components/schemas/ManagementbewertungPerson" }, { type: "null" }] }
        termin: { type: [string, "null"], format: date }
        eingetragen_von: { type: string }
        eingetragen_am: { type: string, format: date }
        folgen: { type: array, items: { $ref: "#/components/schemas/ManagementbewertungFolge" } }
        satz: { type: [string, "null"], description: "Nach der Freigabe ohne Folge: „Keine Folge in VoltPilot - der Beschluss steht im Stand vom …“." }
    Managementbewertung:
      type: object
      required: [kennung, freigegeben, beschluesse]
      properties:
        kennung: { type: string }
        freigegeben: { type: boolean }
        stand_nr: { type: [integer, "null"] }
        sitzung: { oneOf: [{ $ref: "#/components/schemas/ManagementbewertungSitzung" }, { type: "null" }] }
        beschluesse: { type: array, items: { $ref: "#/components/schemas/ManagementbewertungBeschluss" } }
    ManagementbewertungSitzungFesthalten:
      type: object
      additionalProperties: false
      required: [tag, leitung]
      properties:
        tag: { type: string, format: date }
        leitung: { type: string, format: uuid }
        teilnehmende: { type: array, items: { type: string, format: uuid } }
        ort: { type: string, maxLength: 200 }
    ManagementbewertungBeschlussFesthalten:
      type: object
      additionalProperties: false
      required: [art, wortlaut]
      properties:
        art: { type: string, enum: [energieziel, massnahme, dokument, aufgabe, ressourcen, audit, keine_aenderung, weitere] }
        wortlaut: { type: string, minLength: 1, maxLength: 2000 }
        entschieden_von: { type: string, format: uuid }
        zustaendig: { type: string, format: uuid }
        termin: { type: string, format: date }
    ManagementbewertungFolgeVerknuepfen:
      type: object
      additionalProperties: false
      required: [art, objekt]
      properties:
        art: { type: string, enum: [energieziel, dokument, aufgabe, audit] }
        objekt: { type: string }
    InternesAuditStand:
      type: object
      additionalProperties: false
      description: Planen und ändern (IA1) - beim Ändern der ganze Stand.
      properties:
        titel: { type: string, maxLength: 200 }
        termin: { type: string, format: date }
        auditor_ids: { type: array, items: { type: string, format: uuid }, minItems: 1 }
        unabhaengigkeit: { type: string, maxLength: 2000 }
        was: { type: string, maxLength: 2000 }
        woran: { type: string, maxLength: 2000 }
        verantwortlich: { type: string, description: Das Konto (`sub`). }
        standort_ids: { type: array, nullable: true, items: { type: string, format: uuid } }
        begruendung: { type: string, nullable: true, description: Beim Ändern Pflicht, wenn sich der Termin ändert (10–500). }
    InternesAuditDurchgefuehrt:
      type: object
      additionalProperties: false
      properties:
        am: { type: string, format: date }
    InternesAuditHinweisFesthalten:
      type: object
      additionalProperties: false
      properties:
        wortlaut: { type: string, maxLength: 2000 }
        festgestellt_von: { type: string, format: uuid }
        am: { type: string, format: date, nullable: true }
    InternesAuditHinweisMassnahme:
      type: object
      additionalProperties: false
      properties:
        hinweis: { type: integer }
        massnahme: { type: string, pattern: "^M-[0-9]{4}-[0-9]{4,9}$" }
    InternesAuditBericht:
      type: object
      additionalProperties: false
      description: Der unterschriebene Bericht als Verweis (G3) - ohne Ablage keiner seiner Teile.
      properties:
        bezeichnung: { type: string, nullable: true, maxLength: 200 }
        ablage: { type: string, nullable: true, maxLength: 200 }
        kennung: { type: string, nullable: true, maxLength: 200 }
        adresse: { type: string, nullable: true, maxLength: 2000 }
        sha256: { type: string, nullable: true, pattern: "^[0-9a-f]{64}$" }
    InternesAuditAbschliessen:
      type: object
      additionalProperties: false
      properties:
        entschieden_von: { type: string, format: uuid }
        am: { type: string, format: date, nullable: true }
        zusammenfassung: { type: string, nullable: true, maxLength: 2000 }
        bericht:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/InternesAuditBericht" }]
        massnahmen:
          type: array
          nullable: true
          items: { $ref: "#/components/schemas/InternesAuditHinweisMassnahme" }
    InternesAuditAbsagen:
      type: object
      additionalProperties: false
      properties:
        begruendung: { type: string, minLength: 10, maxLength: 500 }
    InternesAuditHinweis:
      type: object
      required: [nr, am, festgestellt_von, wortlaut, eingetragen]
      properties:
        nr: { type: integer }
        am: { type: string, format: date }
        festgestellt_von: { $ref: "#/components/schemas/EnergiemanagementPersonKurz" }
        wortlaut: { type: string }
        eingetragen: { $ref: "#/components/schemas/EnergiemanagementEingetragen" }
    InternesAuditAbschluss:
      type: object
      required: [am, entschieden_von, zusammenfassung, bericht, kopie, pruefsumme, eingetragen]
      properties:
        am: { type: string, format: date }
        entschieden_von: { $ref: "#/components/schemas/EnergiemanagementPersonKurz" }
        zusammenfassung: { type: string, nullable: true }
        bericht:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/InternesAuditBericht" }]
        kopie: { type: object, description: "`kennzeichen`, `hinweise` (nr, am, festgestellt_von, eingetragen_von, wortlaut, massnahme), `feststellungen`, `bericht`." }
        pruefsumme: { type: string, pattern: "^sha256:[0-9a-f]{64}$" }
        eingetragen: { $ref: "#/components/schemas/EnergiemanagementEingetragen" }
    InternesAudit:
      type: object
      required: [id, kennzeichen, titel, termin, auditoren, unabhaengigkeit, was, woran, verantwortlich, standort_ids, zustand, durchgefuehrt_am, abgesagt_begruendung, hinweise, feststellungen, abschluss, eingetragen]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, pattern: "^AU-[0-9]{4}-[0-9]{4,9}$" }
        titel: { type: string }
        termin: { type: string, format: date }
        auditoren:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementPersonKurz" }
        unabhaengigkeit: { type: string }
        was: { type: string }
        woran: { type: string }
        verantwortlich: { $ref: "#/components/schemas/EnergiemanagementVerantwortlich" }
        standort_ids: { type: array, items: { type: string, format: uuid } }
        zustand: { type: string, enum: [geplant, durchgefuehrt, abgeschlossen, abgesagt] }
        durchgefuehrt_am: { type: string, format: date, nullable: true }
        abgesagt_begruendung: { type: string, nullable: true }
        hinweise: { type: integer, description: Anzahl der Hinweise. }
        feststellungen: { type: array, items: { type: string }, description: Die Kennzeichen der Feststellungen mit Quelle dieses Audit. }
        abschluss:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/InternesAuditAbschluss" }]
        eingetragen: { $ref: "#/components/schemas/EnergiemanagementEingetragen" }
    InternesAuditAenderung:
      type: object
      required: [id, art, alt, neu, begruendung, akteur, zeit]
      properties:
        id: { type: integer, format: int64 }
        art: { type: string, enum: [audit_geplant, audit_geaendert, audit_durchgefuehrt, hinweis, audit_abgeschlossen, audit_abgesagt] }
        alt: { nullable: true, description: Schnappschuss vorher. }
        neu: { nullable: true, description: Schnappschuss nachher (beim Hinweis der Eintrag). }
        begruendung: { type: string, nullable: true }
        akteur:
          type: object
          properties:
            sub: { type: string, nullable: true }
            name: { type: string }
            rolle: { type: string, nullable: true }
            art: { type: string, enum: [kunde, unterstuetzung, voltpilot, notfall] }
        zeit: { type: string, format: date-time }
    InternesAuditMitVerlauf:
      type: object
      required: [audit, hinweise, verlauf]
      properties:
        audit: { $ref: "#/components/schemas/InternesAudit" }
        hinweise:
          type: array
          items: { $ref: "#/components/schemas/InternesAuditHinweis" }
        verlauf:
          type: array
          items: { $ref: "#/components/schemas/InternesAuditAenderung" }
    InternesAuditNaechstes:
      type: object
      required: [rhythmus_monate, faellig_am, basis, tage, satz, grund]
      description: Die Felder der Operation `ueberpruefung` (Art `internes_audit`) des Vertrags `energiemanagement.md`.
      properties:
        rhythmus_monate: { type: integer }
        faellig_am: { type: string, format: date, nullable: true }
        basis: { type: string, format: date, nullable: true, description: Der letzte Durchführungstag bis zum Abruf. }
        tage: { type: integer, nullable: true, description: Abruf − fällig am (positiv = fällig, 0 am Fälligkeitstag). }
        satz: { type: string, nullable: true }
        grund: { type: string, nullable: true, description: "`kein_audit` - ohne durchgeführtes Audit bis zum Abruf keine Frist." }
    InternesAuditprogramm:
      type: object
      required: [tag, audits, naechstes]
      properties:
        tag: { type: string, format: date }
        audits:
          type: array
          items: { $ref: "#/components/schemas/InternesAudit" }
        naechstes: { $ref: "#/components/schemas/InternesAuditNaechstes" }
    EnergiemanagementDokumentBezug:
      type: object
      additionalProperties: false
      required: [art]
      description: >
        Das Unternehmen, ein Standort, ein Energieeinsatz, eine Person oder eine Aufgabe (AP-19 IP-14) - genau die
        Kennung der Art ist gesetzt; den Standort eines Einsatzes leitet der Dienst ab.
      properties:
        art: { type: string, enum: [unternehmen, standort, energieeinsatz, person, aufgabe] }
        standort_id: { type: string, format: uuid, nullable: true, description: Nur und Pflicht bei `standort`. }
        energieeinsatz_id: { type: string, format: uuid, nullable: true, description: Nur und Pflicht bei `energieeinsatz`. }
        person_id: { type: string, format: uuid, nullable: true, description: Nur und Pflicht bei `person`. }
        aufgabe_id: { type: string, format: uuid, nullable: true, description: Nur und Pflicht bei `aufgabe` - eine Zuordnung. }
    EnergiemanagementDokumentAnlegen:
      type: object
      additionalProperties: false
      required: [art, titel, bezug]
      properties:
        art: { type: string, enum: [energiepolitik, anwendungsbereich, kontext, rechtliche_anforderungen, risiken_chancen, bestellung, verfahren, betrieb, beschaffung, kommunikation, auslegung, kompetenz] }
        titel: { type: string, maxLength: 200 }
        bezug: { $ref: "#/components/schemas/EnergiemanagementDokumentBezug" }
        ueberpruefung_monate: { type: integer, nullable: true, minimum: 1, maximum: 60, description: Nur an Vorgabe-Arten; ohne Angabe die Einstellung (Startwert 12). }
        beleg: { $ref: "#/components/schemas/EnergiemanagementBeleg" }
    EnergiemanagementVerweis:
      type: object
      additionalProperties: false
      description: Wo das Original liegt (G3) - ohne Ablage keiner seiner Teile; die Prüfsumme bildet der Browser.
      properties:
        bezeichnung: { type: string, nullable: true, maxLength: 200 }
        ablage: { type: string, nullable: true, maxLength: 200 }
        kennung: { type: string, nullable: true, maxLength: 200 }
        adresse: { type: string, nullable: true, maxLength: 2000 }
        fassungsangabe: { type: string, nullable: true, maxLength: 200 }
        datum: { type: string, format: date, nullable: true }
        sha256: { type: string, nullable: true, pattern: "^[0-9a-f]{64}$" }
    EnergiemanagementAusschluss:
      type: object
      additionalProperties: false
      required: [art, verweis, begruendung]
      properties:
        art: { type: string, enum: [standort, anlage, prozess] }
        verweis: { type: string, format: uuid }
        begruendung: { type: string, minLength: 10, maxLength: 500 }
    EnergiemanagementAnwendungsbereichEingang:
      type: object
      additionalProperties: false
      required: [standort_ids, traeger]
      properties:
        standort_ids: { type: array, minItems: 1, items: { type: string, format: uuid } }
        traeger: { type: array, minItems: 1, items: { type: string, enum: [Strom, Gas, Wärme, Kälte, Wasser, Druckluft] } }
        ausschluesse: { type: array, items: { $ref: "#/components/schemas/EnergiemanagementAusschluss" } }
    EnergiemanagementFassungEntwerfen:
      type: object
      additionalProperties: false
      required: [form]
      properties:
        form: { type: string, enum: [wortlaut, verweis] }
        wortlaut: { type: string, nullable: true, maxLength: 20000 }
        verweis: { $ref: "#/components/schemas/EnergiemanagementVerweis" }
        anwendungsbereich: { $ref: "#/components/schemas/EnergiemanagementAnwendungsbereichEingang" }
        begruendung: { type: string, nullable: true, minLength: 10, maxLength: 500, description: Ab Fassung 2 Pflicht. }
        beschluss_kennung: { type: string, nullable: true, pattern: "^BR-[0-9]{4}-[0-9]{4,}/B[0-9]{1,3}$" }
    EnergiemanagementEntscheid:
      type: object
      additionalProperties: false
      description: >
        Antrag und Freigabe ohne Vier-Augen - `entschieden_von` und `begruendung` Pflicht, `entschieden_am` Vorgabe
        heute. Die zweite Person bei Vier-Augen schickt höchstens `begruendung`.
      properties:
        entschieden_von: { type: string, format: uuid, nullable: true, description: Eine Person im Energiemanagement, auch ohne Konto. }
        entschieden_am: { type: string, format: date, nullable: true }
        begruendung: { type: string, nullable: true, minLength: 10, maxLength: 500 }
    EnergiemanagementAblehnen:
      type: object
      additionalProperties: false
      required: [begruendung]
      properties:
        begruendung: { type: string, minLength: 10, maxLength: 500 }
    EnergiemanagementGeprueft:
      type: object
      additionalProperties: false
      required: [entschieden_von, begruendung]
      properties:
        entschieden_von: { type: string, format: uuid }
        am: { type: string, format: date, nullable: true, description: Vorgabe heute. }
        begruendung: { type: string, minLength: 10, maxLength: 500 }
        beschluss_kennung: { type: string, nullable: true, pattern: "^BR-[0-9]{4}-[0-9]{4,}/B[0-9]{1,3}$" }
    EnergiemanagementBekanntmachen:
      type: object
      additionalProperties: false
      required: [kreis, weg]
      properties:
        kreis: { type: string, maxLength: 200 }
        am: { type: string, format: date, nullable: true, description: Vorgabe heute. }
        weg: { type: string, enum: [aushang, intranet, unterweisung, besprechung, e_mail, weiterer] }
        weg_wortlaut: { type: string, nullable: true, maxLength: 200, description: Nur und Pflicht bei `weiterer`. }
        person_id: { type: string, format: uuid, nullable: true, description: Ohne Angabe die Person des eigenen Kontos. }
    EnergiemanagementAufheben:
      type: object
      additionalProperties: false
      required: [entschieden_von, begruendung]
      properties:
        entschieden_von: { type: string, format: uuid }
        am: { type: string, format: date, nullable: true, description: Vorgabe heute. }
        begruendung: { type: string, minLength: 10, maxLength: 500 }
        beschluss_kennung: { type: string, nullable: true, pattern: "^BR-[0-9]{4}-[0-9]{4,}/B[0-9]{1,3}$" }
    EnergiemanagementStandortKurz:
      type: object
      required: [id, kurzzeichen, name]
      properties:
        id: { type: string, format: uuid }
        kurzzeichen: { type: string, nullable: true }
        name: { type: string, nullable: true }
    EnergiemanagementEinsatzKurz:
      type: object
      required: [id, kennzeichen, name]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, nullable: true }
        name: { type: string, nullable: true }
    EnergiemanagementAufgabeKurz:
      type: object
      required: [id, aufgabe, wort, person]
      description: Eine Aufgabe als Bezug - die Zuordnung mit dem Wort der Aufgabe (bei „weitere“ ihr Wortlaut) und ihrer Person.
      properties:
        id: { type: string, format: uuid }
        aufgabe: { type: string, nullable: true }
        wort: { type: string, nullable: true }
        person: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementPersonKurz" }], nullable: true }
    EnergiemanagementDokumentBezugAus:
      type: object
      required: [art, standort, energieeinsatz, person, aufgabe]
      description: >
        `standort` ist der Zaun (beim Energieeinsatz abgeleitet, an Unternehmen, Person und Aufgabe `null`); je Art ist
        höchstens einer von `energieeinsatz`, `person`, `aufgabe` gesetzt.
      properties:
        art: { type: string }
        standort: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementStandortKurz" }], nullable: true }
        energieeinsatz: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementEinsatzKurz" }], nullable: true }
        person: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementPersonKurz" }], nullable: true }
        aufgabe: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementAufgabeKurz" }], nullable: true }
    EnergiemanagementAnwendungsbereich:
      type: object
      required: [standorte, traeger, ausschluesse]
      properties:
        standorte:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementStandortKurz" }
        traeger: { type: array, items: { type: string } }
        ausschluesse:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementAusschluss" }
    EnergiemanagementFassung:
      type: object
      required: [nr, form, wortlaut, verweis, anwendungsbereich, status, begruendung, beschluss_kennung, pruefsumme, vieraugen, entschieden_von, entschieden_am, freigabe_begruendung, freigabe, zweite_person, ablehnung_begruendung, freigegeben_am, eingetragen]
      properties:
        nr: { type: integer }
        form: { type: string, enum: [wortlaut, verweis] }
        wortlaut: { type: string, nullable: true }
        verweis: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementVerweis" }], nullable: true }
        anwendungsbereich: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementAnwendungsbereich" }], nullable: true }
        status: { type: string, enum: [entwurf, beantragt, freigegeben, abgelehnt, abgeloest], description: „abgeloest“ wird gelesen, nie gespeichert (DK4). }
        begruendung: { type: string, nullable: true }
        beschluss_kennung: { type: string, nullable: true }
        pruefsumme: { type: string, nullable: true, description: "`sha256:` über die kanonische Kopie (Vertrag energiemanagement.md §6); ab dem Antrag." }
        vieraugen: { type: boolean }
        entschieden_von: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementPersonKurz" }], nullable: true }
        entschieden_am: { type: string, format: date, nullable: true, description: Der Tag der Entscheidung - Basis der Überprüfung. }
        freigabe_begruendung: { type: string, nullable: true }
        freigabe: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementEingetragen" }], nullable: true, description: Das Konto, das die Entscheidung eingetragen (bei Vier-Augen beantragt) hat. }
        zweite_person: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementEingetragen" }], nullable: true }
        ablehnung_begruendung: { type: string, nullable: true }
        freigegeben_am: { type: string, format: date-time, nullable: true }
        eingetragen: { $ref: "#/components/schemas/EnergiemanagementEingetragen" }
    EnergiemanagementDokumentEintrag:
      type: object
      required: [id, art, fassung, am, person, entschieden_von, kreis, weg, weg_wortlaut, begruendung, beschluss_kennung, kommentar, satz, eingetragen]
      properties:
        id: { type: integer, format: int64 }
        art: { type: string, enum: [bekannt_gemacht, geprueft_bleibt, aufgehoben, kommentar] }
        fassung: { type: integer, nullable: true }
        am: { type: string, format: date, nullable: true }
        person: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementPersonKurz" }], nullable: true }
        entschieden_von: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementPersonKurz" }], nullable: true }
        kreis: { type: string, nullable: true }
        weg: { type: string, nullable: true }
        weg_wortlaut: { type: string, nullable: true }
        begruendung: { type: string, nullable: true }
        beschluss_kennung: { type: string, nullable: true }
        kommentar: { type: string, nullable: true }
        satz: { type: string, nullable: true, description: Bei „geprüft, bleibt“ der Satz des Vertrags. }
        eingetragen: { $ref: "#/components/schemas/EnergiemanagementEingetragen" }
    EnergiemanagementUeberpruefung:
      type: object
      required: [abruf, faellig_am, basis, fassung, tage, satz, grund]
      description: Vertrag `ueberpruefung` (DK5) am Abruf-Tag; ohne Frist sind fällig am, Basis, Fassung, Tage und Satz `null`.
      properties:
        abruf: { type: string, format: date }
        faellig_am: { type: string, format: date, nullable: true }
        basis: { type: string, format: date, nullable: true }
        fassung: { type: integer, nullable: true }
        tage: { type: integer, nullable: true, description: Abruf minus fällig am - positiv = fällig, 0 = heute fällig. }
        satz: { type: string, nullable: true }
        grund: { type: string, nullable: true, enum: [nachweis, keine_fassung, null] }
    EnergiemanagementDokumentSaetze:
      type: object
      required: [kopf, ueberpruefung, freigabe_gesperrt]
      properties:
        kopf: { type: string, nullable: true }
        ueberpruefung: { type: string, nullable: true }
        freigabe_gesperrt: { type: string, nullable: true }
    EnergiemanagementDokumentKurz:
      type: object
      required: [id, kennzeichen, art, art_wort, klasse, titel, bezug, zustand, gueltige_fassung, ueberpruefung, eingetragen]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, pattern: "^D-[0-9]{4,13}$" }
        art: { type: string }
        art_wort: { type: string }
        klasse: { type: string, enum: [vorgabe, nachweis] }
        titel: { type: string }
        bezug: { $ref: "#/components/schemas/EnergiemanagementDokumentBezugAus" }
        zustand: { type: string, enum: [entwurf, gueltig, aufgehoben] }
        gueltige_fassung: { type: integer, nullable: true }
        ueberpruefung: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementUeberpruefung" }], nullable: true }
        eingetragen: { $ref: "#/components/schemas/EnergiemanagementEingetragen" }
    EnergiemanagementDokumente:
      type: object
      required: [dokumente]
      properties:
        dokumente:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementDokumentKurz" }
    EnergiemanagementDokumentAenderung:
      type: object
      required: [id, art, alt, neu, begruendung, akteur, zeit]
      properties:
        id: { type: integer, format: int64 }
        art: { type: string, enum: [dokument_angelegt, fassung_entworfen, fassung_beantragt, fassung_freigegeben, fassung_abgelehnt, dokument_gueltig, fassung_abgeloest, geprueft_bleibt, bekannt_gemacht, dokument_aufgehoben] }
        alt: { nullable: true, description: Schnappschuss vorher. }
        neu: { nullable: true, description: Schnappschuss nachher. }
        begruendung: { type: string, nullable: true }
        akteur:
          type: object
          properties:
            sub: { type: string, nullable: true }
            name: { type: string }
            rolle: { type: string, nullable: true }
            art: { type: string, enum: [kunde, unterstuetzung, voltpilot, notfall] }
        zeit: { type: string, format: date-time }
    EnergiemanagementDokument:
      type: object
      required: [id, kennzeichen, art, art_wort, klasse, titel, bezug, zustand, ueberpruefung_monate, beleg, gueltige_fassung, fassungen, eintraege, ueberpruefung, saetze, eingetragen, verlauf]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, pattern: "^D-[0-9]{4,13}$" }
        art: { type: string }
        art_wort: { type: string, description: Das Kundenwort der Art (Vertrag `woerter.dokument_art`). }
        klasse: { type: string, enum: [vorgabe, nachweis] }
        titel: { type: string }
        bezug: { $ref: "#/components/schemas/EnergiemanagementDokumentBezugAus" }
        zustand: { type: string, enum: [entwurf, gueltig, aufgehoben] }
        ueberpruefung_monate: { type: integer, nullable: true }
        beleg: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementBeleg" }], nullable: true, description: Das Original beim Kunden als Verweis. }
        gueltige_fassung: { type: integer, nullable: true }
        fassungen:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementFassung" }
        eintraege:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementDokumentEintrag" }
        ueberpruefung: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementUeberpruefung" }], nullable: true }
        saetze: { $ref: "#/components/schemas/EnergiemanagementDokumentSaetze" }
        eingetragen: { $ref: "#/components/schemas/EnergiemanagementEingetragen" }
        verlauf:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementDokumentAenderung" }
    EnergiemanagementBetrachtungsumfang:
      type: object
      required: [fassung, gueltig_ab, standorte, traeger, begruendung]
      properties:
        fassung: { type: integer }
        gueltig_ab: { type: string, format: date }
        standorte:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementStandortKurz" }
        traeger: { type: array, items: { type: string } }
        begruendung: { type: string, nullable: true }
    EnergiemanagementVergleichErgebnis:
      type: object
      required: [standorte_nur_im_anwendungsbereich, standorte_nur_im_betrachtungsumfang, traeger_nur_im_anwendungsbereich, traeger_nur_im_betrachtungsumfang, deckungsgleich]
      properties:
        standorte_nur_im_anwendungsbereich: { type: array, items: { $ref: "#/components/schemas/EnergiemanagementStandortKurz" } }
        standorte_nur_im_betrachtungsumfang: { type: array, items: { $ref: "#/components/schemas/EnergiemanagementStandortKurz" } }
        traeger_nur_im_anwendungsbereich: { type: array, items: { type: string } }
        traeger_nur_im_betrachtungsumfang: { type: array, items: { type: string } }
        deckungsgleich: { type: boolean }
    EnergiemanagementVergleich:
      type: object
      required: [abruf, fassung, anwendungsbereich, betrachtungsumfang, vergleich, saetze]
      properties:
        abruf: { type: string, format: date }
        fassung: { type: integer, nullable: true, description: Die gültige Fassung des Anwendungsbereichs. }
        anwendungsbereich: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementAnwendungsbereich" }], nullable: true }
        betrachtungsumfang: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementBetrachtungsumfang" }], nullable: true }
        vergleich: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementVergleichErgebnis" }], nullable: true }
        saetze: { type: array, items: { type: string } }
    EnergiemanagementNachweisOrt:
      type: object
      required: [ort, ort_satz, satz, inhalt_in_voltpilot, fassung, festgehalten_am, ablage, kennung, adresse, adresse_als_verweis, fassungsangabe, datum, sha256]
      description: >
        KS1, G1 (AP-19 IP-14): wo der Inhalt der gültigen Fassung liegt. `ort_satz` ist die Zeile des Verzeichnisses
        (Operation `verzeichnis_zeile`), `satz` der Kundensatz `ort_verweis` bzw. `ort_wortlaut` (in VoltPilot `null`).
        Eine Adresse wird nur mit `https:` als Verweis gezeigt; VoltPilot öffnet, prüft und lädt nichts.
      properties:
        ort: { type: string, enum: [in_voltpilot, wortlaut_original_beim_kunden, verweis] }
        ort_satz: { type: string }
        satz: { type: string, nullable: true }
        inhalt_in_voltpilot: { type: boolean }
        fassung: { type: integer }
        festgehalten_am: { type: string, format: date, nullable: true, description: Der Tag der Entscheidung der Fassung. }
        ablage: { type: string, nullable: true }
        kennung: { type: string, nullable: true }
        adresse: { type: string, nullable: true }
        adresse_als_verweis: { type: boolean }
        fassungsangabe: { type: string, nullable: true }
        datum: { type: string, format: date, nullable: true }
        sha256: { type: string, nullable: true, description: Die im Browser gebildete Prüfsumme der Datei bzw. des Originals. }
    EnergiemanagementKommunikationsnachweis:
      type: object
      required: [dokument_id, kennzeichen, titel, art, fassung, am, kreis, wege, wege_wort, person, satz]
      description: DK6 - eine Mitteilung an einen Kreis an einem Tag durch eine Person, über einen oder mehrere Wege.
      properties:
        dokument_id: { type: string, format: uuid }
        kennzeichen: { type: string, pattern: "^D-[0-9]{4,13}$" }
        titel: { type: string }
        art: { type: string }
        fassung: { type: integer }
        am: { type: string, format: date }
        kreis: { type: string }
        wege: { type: array, items: { type: string, enum: [aushang, intranet, unterweisung, besprechung, e_mail, weiterer] } }
        wege_wort: { type: string, description: „Aushang und Intranet“; ein weiterer Weg mit seinem Wortlaut. }
        person: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementPersonKurz" }], nullable: true }
        satz: { type: string }
    EnergiemanagementKommunikationsnachweise:
      type: object
      required: [abruf, bekanntmachungen]
      properties:
        abruf: { type: string, format: date }
        bekanntmachungen:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementKommunikationsnachweis" }
    EnergiemanagementNachweis:
      type: object
      required: [id, kennzeichen, art, art_wort, klasse, titel, bezug, zustand, gueltige_fassung, ort, ueberpruefung, bekanntmachungen]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, pattern: "^D-[0-9]{4,13}$" }
        art: { type: string }
        art_wort: { type: string }
        klasse: { type: string, enum: [vorgabe, nachweis] }
        titel: { type: string }
        bezug: { $ref: "#/components/schemas/EnergiemanagementDokumentBezugAus" }
        zustand: { type: string, enum: [entwurf, gueltig, aufgehoben] }
        gueltige_fassung: { type: integer, nullable: true }
        ort: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementNachweisOrt" }], nullable: true, description: Ohne gültige Fassung `null`. }
        ueberpruefung: { allOf: [{ $ref: "#/components/schemas/EnergiemanagementUeberpruefung" }], nullable: true, description: Kompetenz und Auslegung haben keine (Grund `nachweis`). }
        bekanntmachungen:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementKommunikationsnachweis" }
    EnergiemanagementNachweiseAmEinsatz:
      type: object
      required: [energieeinsatz, abruf, nachweise]
      properties:
        energieeinsatz: { $ref: "#/components/schemas/EnergiemanagementEinsatzKurz" }
        abruf: { type: string, format: date }
        nachweise:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementNachweis" }
    EnergiemanagementNachweiseDerPerson:
      type: object
      required: [person, abruf, nachweise]
      properties:
        person: { $ref: "#/components/schemas/EnergiemanagementPersonKurz" }
        abruf: { type: string, format: date }
        nachweise:
          type: array
          items: { $ref: "#/components/schemas/EnergiemanagementNachweis" }
    MessbedarfAnlegen:
      type: object
      additionalProperties: false
      description: >-
        Ort und Größe gibt es als Wortlaut (`ort`, `groesse`) und optional als Struktur. `ort_id` ist ein Standort,
        Gebäude oder Bereich des Mandanten (sonst 422 `ort_unbekannt`); `messgroesse`/`richtung` stammen aus dem
        Größen-Katalog der Messstelle (sonst 422 `groesse_ungueltig`, `richtung` nur mit `messgroesse`). Fehlt der
        Wortlaut zu einer Struktur, entsteht er daraus — Kurzzeichen des Orts, Größe als „Wirkenergie · Bezug“.
      required: [wortlaut]
      properties:
        wortlaut: { type: string, minLength: 1 }
        ort: { type: string, nullable: true }
        groesse: { type: string, nullable: true }
        frist: { type: string, format: date, nullable: true }
        ort_id: { type: string, format: uuid, nullable: true }
        messgroesse: { type: string, nullable: true, example: Wirkenergie }
        richtung: { type: string, nullable: true, example: Bezug }
    MessbedarfEinloesen:
      type: object
      additionalProperties: false
      required: [messstelle_id]
      properties:
        messstelle_id: { type: string, format: uuid }
    MessbedarfVerwerfen:
      type: object
      additionalProperties: false
      required: [begruendung]
      properties:
        begruendung: { type: string, minLength: 1 }
    MessbedarfOrtZiel:
      type: object
      description: Der strukturierte Ort; `standort_*` ist der Standort, an dem er heute hängt (`null`, wenn keiner).
      required: [id, art, kurzzeichen, name, standort_id, standort_name]
      properties:
        id: { type: string, format: uuid }
        art: { type: string, enum: [standort, gebaeude, bereich] }
        kurzzeichen: { type: string }
        name: { type: string, nullable: true }
        standort_id: { type: string, format: uuid, nullable: true }
        standort_name: { type: string, nullable: true }
    Messbedarf:
      type: object
      description: >-
        Ohne Struktur (`ort_ziel`, `messgroesse` null) bleibt der Wortlaut führend für die Anzeige — so liest sich jeder
        Bedarf aus der Fassung vor der Struktur unverändert.
      required: [id, kennzeichen, energieeinsatz_id, wortlaut, ort, groesse, frist, zustand, messstelle,
        begruendung, akteur, angelegt_am, geaendert_am, ort_ziel, messgroesse, richtung]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, pattern: '^MB-[1-9][0-9]*$' }
        energieeinsatz_id: { type: string, format: uuid }
        wortlaut: { type: string }
        ort: { type: string, nullable: true }
        groesse: { type: string, nullable: true }
        frist: { type: string, format: date, nullable: true }
        zustand: { type: string, enum: [offen, eingeloest, verworfen] }
        messstelle:
          type: object
          nullable: true
          required: [id, kennzeichen, name]
          properties:
            id: { type: string, format: uuid }
            kennzeichen: { type: string }
            name: { type: string, nullable: true }
        begruendung: { type: string, nullable: true }
        akteur:
          type: object
          required: [sub, name, rolle, art]
          properties:
            sub: { type: string, nullable: true }
            name: { type: string }
            rolle: { type: string, nullable: true }
            art: { type: string, enum: [kunde, unterstuetzung, voltpilot, notfall] }
        angelegt_am: { type: string, format: date-time }
        geaendert_am: { type: string, format: date-time }
        ort_ziel:
          allOf: [{ $ref: "#/components/schemas/MessbedarfOrtZiel" }]
          nullable: true
        messgroesse: { type: string, nullable: true }
        richtung: { type: string, nullable: true }
    MessbedarfListe:
      type: object
      required: [messbedarfe]
      properties:
        messbedarfe: { type: array, items: { $ref: "#/components/schemas/Messbedarf" } }
    MessbedarfProtokoll:
      type: object
      required: [aenderungen]
      properties:
        aenderungen:
          type: array
          items:
            type: object
            required: [id, art, alt, neu, akteur, zeit]
            properties:
              id: { type: integer, format: int64 }
              art: { type: string, enum: [erfasst, bearbeitet, eingeloest, verworfen] }
              alt: { nullable: true, description: Vorheriger Schnappschuss. }
              neu: { nullable: true, description: Neuer Schnappschuss. }
              akteur:
                type: object
                required: [sub, name, rolle, art]
                properties:
                  sub: { type: string, nullable: true }
                  name: { type: string }
                  rolle: { type: string, nullable: true }
                  art: { type: string, enum: [kunde, unterstuetzung, voltpilot, notfall] }
              zeit: { type: string, format: date-time }
    GemeinsameSteuerung:
      type: object
      required: [eingerichtet, zustand, mitglieder, fehlt]
      properties:
        eingerichtet: { type: boolean }
        zustand:
          type: string
          enum: [nicht_eingerichtet, erklaert, beobachtet, geprueft, anteile_aktiv, angehalten, aufgeloest, wird_aufgeloest]
        stufe: { type: string, nullable: true, enum: [S0, S1, S2, S3], description: "null bei angehalten, aufgeloest, nicht_eingerichtet" }
        epoche: { type: integer, format: int64, nullable: true }
        netzanschluss_id: { type: string, format: uuid, nullable: true }
        mitglieder:
          type: array
          items: { $ref: "#/components/schemas/GemeinsameSteuerungMitglied" }
        naechster_schritt:
          type: string
          nullable: true
          enum: [beobachtet, anteile_aktiv, vom_betreiber_angehalten]
          description: "vom_betreiber_angehalten: der Betreiber hat angehalten — fortsetzen kann nur er (W9/I5)."
        fehlt:
          type: array
          items: { $ref: "#/components/schemas/GemeinsameSteuerungBefund" }
        warnung_fuehrung:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/GemeinsameSteuerungWarnungFuehrung" }]
        bilanz:
          nullable: true
          description: "Verbund-Bilanz (AP-15 IP-12) — null, solange kein Tag gerechnet ist."
          allOf: [{ $ref: "#/components/schemas/GemeinsameSteuerungBilanz" }]
        vorbehalt:
          nullable: true
          description: "Vorbehalt je Richtung mit Herkunft und offenem Vorschlag (AP-15 IP-13) — null ohne Gemeinsame Steuerung."
          allOf: [{ $ref: "#/components/schemas/GemeinsameSteuerungVorbehalt" }]
        aufloesen:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/GemeinsameSteuerungAufloesen" }]
    GemeinsameSteuerungAufloesen:
      type: object
      description: Nur beim gemeinsamen Auflösen; Quittung oder Betreiber-Bestätigung, kein Zeitablauf.
      required: [bestaetigt, gesamt, wartet_auf]
      properties:
        bestaetigt: { type: integer, minimum: 0 }
        gesamt: { type: integer, minimum: 0 }
        wartet_auf:
          type: array
          items: { type: string, format: uuid }
    GemeinsameSteuerungBilanz:
      type: object
      required: [zustand, tag, seit, gerechnet_am]
      properties:
        zustand: { type: string, enum: [plausibel, unplausibel, unbekannt] }
        tag: { type: string, format: date, description: "der jüngste gerechnete Tag (Europe/Berlin)" }
        seit: { type: string, format: date, description: "erster Tag der ununterbrochenen Reihe mit diesem Zustand" }
        grund:
          type: string
          nullable: true
          enum: [struktur_geaendert, netzpunkt_ohne_messstelle, box_ohne_messstelle, komponente_ohne_messstelle,
                 richtung_nicht_eindeutig, luecke, einzelne_abweichung]
          description: "nur bei unbekannt"
        gerechnet_am: { type: string, format: date-time }
        komponente:
          type: object
          nullable: true
          required: [entity_id]
          description: "nur bei komponente_ohne_messstelle: die erklärte Komponente einer mitsteuernden Box ohne Abgangszähler, an der eine Messstelle mit Wirkleistung oder Wirkenergie fehlt"
          properties:
            entity_id: { type: string, format: uuid }
            name: { type: string, nullable: true }
    GemeinsameSteuerungVorbehalt:
      type: object
      required: [einspeisung, bezug]
      properties:
        einspeisung: { $ref: "#/components/schemas/GemeinsameSteuerungVorbehaltRichtung" }
        bezug: { $ref: "#/components/schemas/GemeinsameSteuerungVorbehaltRichtung" }
        vorschlag:
          nullable: true
          description: "offener Vorschlag zum Senken — wirksam erst durch POST …/admin/…/vorbehalt/freigeben"
          allOf: [{ $ref: "#/components/schemas/GemeinsameSteuerungVorbehaltVorschlag" }]
    GemeinsameSteuerungVorbehaltRichtung:
      type: object
      required: [herkunft]
      properties:
        kw: { type: number, nullable: true, description: "null = unbekannt, keine Null" }
        herkunft:
          type: string
          enum: [erklaert, gemessen]
          description: "gemessen: eine selbsttätige Erhöhung oder ein freigegebener Vorschlag hat den Wert gesetzt; die Einspeiseseite ist immer erklaert"
        seit: { type: string, format: date-time, nullable: true }
        zweischritt:
          type: string
          nullable: true
          enum: [veroeffentlicht, kein_verbund, stufe_zu_frueh, noch_nicht_scharf, auslegung_passt_nicht,
                 zweischritt_laeuft, rueckgespielt, wirksame_anteile_unbekannt, unveraendert]
          description: "nur bei gemessen: was der Zweischritt danach tat; auslegung_passt_nicht = nichts erweitert, die Boxen halten ihr letztes Dokument"
    GemeinsameSteuerungVorbehaltVorschlag:
      type: object
      required: [richtung, neu_kw, hoechstwert_kw, zeitraum_von, zeitraum_bis, messtage, erstellt_am]
      properties:
        richtung: { type: string, enum: [einspeisung, bezug] }
        alt_kw: { type: number, nullable: true }
        neu_kw: { type: number }
        hoechstwert_kw: { type: number, description: "höchster belegter Viertelstundenwert des Ungeregelten" }
        hoechstwert_von: { type: string, format: date-time, nullable: true }
        zeitraum_von: { type: string, format: date }
        zeitraum_bis: { type: string, format: date }
        messtage: { type: integer }
        erstellt_am: { type: string, format: date-time }
    GemeinsameSteuerungMitglied:
      type: object
      required: [box_id, rolle, gueltig_ab, vorgabe_signal, verbraucher14a]
      properties:
        box_id: { type: string, format: uuid }
        rolle: { type: string, enum: [fuehrt, steuert_mit] }
        messpunkt_id: { type: string, format: uuid, nullable: true }
        gueltig_ab: { type: string, format: date-time }
        bestaetigt_am: { type: string, format: date-time, nullable: true }
        vorgabe_signal:
          type: string
          enum: [ja, nein, unbekannt]
          description: >-
            G6: liegt das Signal des Netzbetreibers (§ 14a u. ä.) an dieser Box an — vom Kundenadministrator
            erklärt; `unbekannt` (Vorgabe) zählt als „liegt nicht an“.
        vorgabe_signal_am:
          type: string
          format: date-time
          nullable: true
          description: Wann das Signal zuletzt erklärt wurde; null = nie.
        verbraucher14a:
          type: string
          enum: [ja, nein, unbekannt]
          description: >-
            G6: hängen steuerbare Verbraucher nach § 14a hinter dieser Box — abgeleitet aus den Geräte-Angaben der
            Box (Richtung bezug mit Komponente und Schreibfreigabe); `unbekannt` (keine Angabe) zählt als „ja“.
        anteil_verlust:
          nullable: true
          description: >-
            AP-15 IP-22 (E1 = A, R2): was der feste Einspeise-Anteil dieser Box zurückhielt, gemeldet von der Box im
            Herzschlag. `null`, solange sie an dieser Anlage keinen Tag gemeldet hat.
          allOf: [{ $ref: '#/components/schemas/GemeinsameSteuerungAnteilVerlust' }]
        sprungprobe:
          nullable: true
          description: >-
            AP-15 IP-21 (E3 = A, T5): die jüngste Sprungprobe dieser Box. `null` ohne Probe.
          allOf: [{ $ref: '#/components/schemas/GemeinsameSteuerungSprungprobe' }]
        wirksame_anteile:
          nullable: true
          description: >-
            AP-15 IP-23-Folge (G4): die Anteile, die dieser Box zugestellt UND von ihr quittiert sind — der Stand
            ihres jüngsten quittierten Anteils-Dokuments (im Übergangsstand der Übergangswert; weicht der Betreiber
            beim Scharfschalten von der Auslegung ab, dessen Zahl). Gespeichert, nicht aus dem Herzschlag. `null`,
            solange nichts quittiert ist — unbekannt, keine Null.
          allOf: [{ $ref: '#/components/schemas/GemeinsameSteuerungWirksameAnteile' }]
        zuletzt_gehoert:
          type: string
          format: date-time
          nullable: true
          description: >-
            AP-15 IP-23-Folge: der letzte Status-Herzschlag dieser Box (Cloud-Ankunft); null = nie. Die Ausfall-Sätze
            A1/A2/A4 der Kundenfläche lesen hier.
        ausscheiden:
          allOf: [{ $ref: '#/components/schemas/GemeinsameSteuerungAusscheiden' }]
          nullable: true
          description: AP-15 §5.5 — das Mitglied scheidet aus; null, solange nicht.
    GemeinsameSteuerungAusscheiden:
      type: object
      required: [seit, wartet_auf]
      description: >-
        AP-15 §5.5 (V20260922160000): die Box scheidet aus — ihr Anteil fällt im Zweischritt auf den Rückfall ihrer
        Geräte; die anderen bekommen den Rest erst mit dem Ende ihrer Mitgliedschaft.
      properties:
        seit: { type: string, format: date-time }
        wartet_auf:
          type: string
          enum: [box, voltpilot]
          description: >-
            `box` = auf die Quittung der Box (Route `ausscheiden`); `voltpilot` = die Box ist abgemeldet, VoltPilot
            bestätigt, dass ihre Geräte vom Netz sind.
        vom_netz_bestaetigt_am: { type: string, format: date-time, nullable: true }
    GemeinsameSteuerungVorschau:
      type: object
      description: >-
        Antwort von `POST …/gemeinsame-steuerung/einrichten/vorschau` — als wäre der Entwurf gespeichert; geschrieben
        ist nichts.
      required: [einrichten, zustand]
      properties:
        einrichten: { $ref: "#/components/schemas/GemeinsameSteuerungEinrichtenAuskunft" }
        zustand: { $ref: "#/components/schemas/GemeinsameSteuerung" }
    GemeinsameSteuerungWirksameAnteile:
      type: object
      description: Wirksame Anteile einer Box je Richtung in kW; eine leere Richtung ist unbekannt, keine Null.
      properties:
        einspeisung_kw: { type: number, nullable: true }
        bezug_kw: { type: number, nullable: true }
    GemeinsameSteuerungSprungprobe:
      type: object
      required: [probe_id, box_id, art, sprung_kw, dauer_s, wiederholungen, ausgeloest_am, urteil, gilt]
      properties:
        probe_id: { type: string, format: uuid }
        box_id: { type: string, format: uuid }
        art: { type: string, enum: [erzeugung_senken, verbrauch_senken] }
        sprung_kw: { type: number }
        dauer_s: { type: integer }
        wiederholungen: { type: integer }
        ausgeloest_am: { type: string, format: date-time }
        urteil:
          type: string
          enum: [ausgeloest, bestanden, nicht_bestanden, abgebrochen, nicht_auswertbar]
          description: >-
            `ausgeloest` = der Bericht der Box steht aus. `nicht_auswertbar` = unbekannt (kein Bestanden).
        grund:
          type: string
          nullable: true
          enum: [nicht_gesehen, falsche_richtung, zu_klein, zu_gross, netzpunkt_nicht_frisch,
            eigene_wirkung_unbekannt, sprung_zu_klein, bericht_unvollstaendig, einspeisewaechter, bezugswaechter,
            eingefroren, geraeteschutz, regelung_aus, keine_stellgroesse, abgelaufen, neustart, null]
        ausgewertet_am: { type: string, format: date-time, nullable: true }
        entwertet_am:
          type: string
          format: date-time
          nullable: true
          description: I3 — eine Strukturänderung nach der Probe; sie zählt nicht mehr.
        gilt:
          type: boolean
          description: >-
            Trägt die Naht `sprungprobe` für die HEUTIGE Struktur: bestanden, nicht entwertet, dieselbe führende Box.
    GemeinsameSteuerungBetreiberblatt:
      type: object
      required: [boxen, sprungproben, steckerproben]
      properties:
        boxen: { type: array, items: { $ref: '#/components/schemas/GemeinsameSteuerungBoxStand' } }
        zweischritt: { allOf: [{ $ref: '#/components/schemas/GemeinsameSteuerungZweischritt' }], nullable: true }
        sprungproben: { type: array, items: { $ref: '#/components/schemas/GemeinsameSteuerungSprungprobeProtokoll' } }
        steckerproben: { type: array, items: { $ref: '#/components/schemas/GemeinsameSteuerungSteckerprobe' } }
    GemeinsameSteuerungSteckerprobe:
      type: object
      required: [id, von, bis, box_id, bemerkung, eingetragen_am, grund, nachweis]
      properties:
        id: { type: string, format: uuid }
        von: { type: string, format: date-time }
        bis: { type: string, format: date-time }
        box_id: { type: string, format: uuid }
        bemerkung: { type: string, nullable: true }
        eingetragen_am: { type: string, format: date-time }
        grund: { type: string, enum: [kein_netzanschluss], nullable: true }
        nachweis:
          allOf: [{ $ref: '#/components/schemas/NetzanschlussGrenzNachweis' }]
          nullable: true
    GemeinsameSteuerungBoxStand:
      type: object
      required: [box_id, rolle, faehigkeit, plan, anteile]
      properties:
        box_id: { type: string, format: uuid }
        rolle: { type: string, enum: [fuehrt, steuert_mit] }
        zuletzt_gesehen: { type: string, format: date-time, nullable: true, description: letzter Herzschlag der Box. }
        faehigkeit: { $ref: '#/components/schemas/GemeinsameSteuerungFaehigkeit' }
        messpunkt: { allOf: [{ $ref: '#/components/schemas/GemeinsameSteuerungMesspunkt' }], nullable: true }
        waechter:
          allOf: [{ $ref: '#/components/schemas/GemeinsameSteuerungWaechter' }]
          nullable: true
          description: null = kein Herzschlag-Block seit dem Start der api (alte Box) — nicht gemeldet, keine Stufe.
        plan: { $ref: '#/components/schemas/GemeinsameSteuerungPlanStand' }
        anteile: { $ref: '#/components/schemas/GemeinsameSteuerungAnteilStand' }
        verlust_gestern:
          allOf: [{ $ref: '#/components/schemas/GemeinsameSteuerungVerlustTag' }]
          nullable: true
          description: Folgepaket zu IP-22 — null = die Box hat für gestern keinen Anteils-Verlust gemeldet.
        ausscheiden:
          allOf: [{ $ref: '#/components/schemas/GemeinsameSteuerungAusscheiden' }]
          nullable: true
          description: AP-15 §5.5 — das Mitglied scheidet aus; null, solange nicht.
    GemeinsameSteuerungVerlustTag:
      type: object
      required: [tag, verlust_kwh, gebunden_s]
      description: >-
        Der gestrige Tag einer Box: `verlust_kwh` die UNTERGRENZE der Box (gemessen), `gebunden_s` exakt,
        `schaetzung_kwh` die Schätzung der Cloud mit `schaetzung_grundlage` (`prognose` · `nowcast` · `keine`;
        null = noch nicht gerechnet). Bei `keine` gibt es keinen Wert — unbekannt ist keine Null.
      properties:
        tag: { type: string, format: date }
        verlust_kwh: { type: number }
        gebunden_s: { type: integer }
        schaetzung_kwh: { type: number, nullable: true }
        schaetzung_grundlage: { type: string, enum: [prognose, nowcast, keine], nullable: true }
    GemeinsameSteuerungPilotBericht:
      type: object
      required: [site_id, von, bis, boxen]
      properties:
        site_id: { type: string, format: uuid }
        von: { type: string, format: date }
        bis: { type: string, format: date }
        boxen: { type: array, items: { $ref: '#/components/schemas/GemeinsameSteuerungPilotZeile' } }
        summe:
          allOf: [{ $ref: '#/components/schemas/GemeinsameSteuerungPilotZeile' }]
          nullable: true
          description: über alle Boxen (`box_id` null); null ohne gemeldeten Tag.
    GemeinsameSteuerungPilotZeile:
      type: object
      required: [tage, gebunden_s, verlust_kwh, tage_geschaetzt, tage_ohne_prognose, tage_nicht_gerechnet]
      properties:
        box_id: { type: string, format: uuid, nullable: true }
        tage: { type: integer }
        gebunden_s: { type: integer, format: int64 }
        verlust_kwh: { type: number, description: Summe der Untergrenze der Box. }
        schaetzung_kwh:
          type: number
          nullable: true
          description: Summe der Schätzung über `tage_geschaetzt` Tage; null, wenn keiner eine hat.
        tage_geschaetzt: { type: integer }
        tage_ohne_prognose: { type: integer }
        tage_nicht_gerechnet: { type: integer }
    GemeinsameSteuerungFaehigkeit:
      type: object
      properties:
        steuerungsverbund_anteil: { type: string, enum: [gemeldet, versions_tabelle, fehlt] }
        sprungprobe: { type: string, enum: [gemeldet, versions_tabelle, fehlt] }
    GemeinsameSteuerungMesspunkt:
      type: object
      properties:
        data_source_id: { type: string, format: uuid }
        zustand: { type: string, enum: [ok, stale, never, nicht_gemeldet] }
        gelesen_am: { type: string, format: date-time, nullable: true }
    GemeinsameSteuerungWaechter:
      type: object
      properties:
        einspeisung: { type: string, nullable: true }
        bezug: { type: string, nullable: true }
    GemeinsameSteuerungPlanStand:
      type: object
      description: Veröffentlicht gegen angenommen (R11); ein Teil ist null, wenn es ihn nicht gibt.
      properties:
        veroeffentlicht: { allOf: [{ $ref: '#/components/schemas/GemeinsameSteuerungPlanZeile' }], nullable: true }
        angenommen: { allOf: [{ $ref: '#/components/schemas/GemeinsameSteuerungPlanZeile' }], nullable: true }
    GemeinsameSteuerungPlanZeile:
      type: object
      properties:
        plan_id: { type: string, format: uuid }
        erzeugt_am: { type: string, format: date-time, nullable: true }
        am: { type: string, format: date-time, nullable: true }
        urteil: { type: string, nullable: true }
        grund: { type: string, nullable: true }
    GemeinsameSteuerungAnteilStand:
      type: object
      properties:
        gesendet: { allOf: [{ $ref: '#/components/schemas/GemeinsameSteuerungRevision' }], nullable: true }
        quittiert: { allOf: [{ $ref: '#/components/schemas/GemeinsameSteuerungRevision' }], nullable: true }
        wirksam_kw:
          type: object
          nullable: true
          description: Die wirksamen Anteile je Richtung aus dem Herzschlag der Box; null = nicht gemeldet.
          properties:
            einspeisung: { type: number }
            bezug: { type: number }
        reserve_verbraucher_kw:
          type: number
          nullable: true
          description: >-
            Reserve der anderen steuerbaren Verbraucher (Schalter/Relais, SG-Ready, Wärmepumpen, Wallbox außerhalb des
            Ladeparks) aus dem Herzschlag der Box - um sie senkt die Box das Ladebudget ihres Bezugs-Anteils; null = nicht
            gemeldet, dann gilt keine.
        ungeregelt_hinter_abgang_kw:
          type: number
          nullable: true
          description: >-
            Erklärtes Ungeregeltes hinter dem Abgangszähler dieser Box (Erklärung ungeregelt und Geräte ohne
            Schreibfreigabe, Richtung Bezug, aufgerundet auf 0,1 kW) - dieselbe Zahl, die ihr Anteils-Dokument als
            ungeregelt_hinter_abgang.bezug trägt; blind zieht die mitsteuernde Box sie von ihrem Anteil ab. null = keins
            erklärt (oder die Box führt).
    GemeinsameSteuerungRevision:
      type: object
      required: [epoche, revision]
      properties:
        epoche: { type: integer, format: int64 }
        revision: { type: integer, format: int64 }
        am: { type: string, format: date-time, nullable: true }
    GemeinsameSteuerungZweischritt:
      type: object
      required: [schritt, epoche, revision, bestaetigt, wartet_auf]
      description: >-
        Das jüngste Anteils-Dokument. Der Zielstand gilt erst mit `schritt = ziel` und leerem `wartet_auf`.
      properties:
        schritt: { type: string, enum: [uebergang, ziel] }
        epoche: { type: integer, format: int64 }
        revision: { type: integer, format: int64 }
        am: { type: string, format: date-time, nullable: true }
        bestaetigt: { type: array, items: { type: string, format: uuid } }
        wartet_auf: { type: array, items: { type: string, format: uuid } }
    GemeinsameSteuerungSprungprobeProtokoll:
      type: object
      required: [probe, spruenge]
      properties:
        probe: { $ref: '#/components/schemas/GemeinsameSteuerungSprungprobe' }
        spruenge: { type: array, items: { $ref: '#/components/schemas/GemeinsameSteuerungSprungMessung' } }
    GemeinsameSteuerungSprungMessung:
      type: object
      properties:
        eigene_kw: { type: number, nullable: true }
        erwartet_kw: { type: number, nullable: true }
        gesehen_kw: { type: number, nullable: true }
        toleranz_kw: { type: number, nullable: true }
        abweichung_kw: { type: number, nullable: true, description: gesehen − erwartet. }
        urteil: { type: string, nullable: true }
        grund: { type: string, nullable: true }
    GemeinsameSteuerungAnteilVerlust:
      type: object
      properties:
        heute: { allOf: [{ $ref: '#/components/schemas/GemeinsameSteuerungVerlustSumme' }], nullable: true }
        monat: { allOf: [{ $ref: '#/components/schemas/GemeinsameSteuerungVerlustSumme' }], nullable: true }
    GemeinsameSteuerungVerlustSumme:
      type: object
      required: [kwh, gebunden_s, tage]
      description: >-
        Tage der Anlage (Europe/Berlin). `kwh` ist eine UNTERGRENZE — die Box kennt die verfügbare Erzeugung einer
        abgeregelten PV nur aus gemessenen Werten; `gebunden_s` (wie lange der Anteil die Erzeuger hielt) ist exakt.
      properties:
        kwh: { type: number }
        gebunden_s: { type: integer, format: int64 }
        tage: { type: integer }
        schaetzung_kwh:
          type: number
          nullable: true
          description: >-
            Folgepaket zu IP-22: die SCHÄTZUNG der Cloud aus der PV-Prognose über `tage_geschaetzt` Tage (gerechnet
            nach dem Tag, heute also nie); null, solange keiner eine hat.
        tage_geschaetzt: { type: integer }
    GemeinsameSteuerungBefund:
      type: object
      required: [wort]
      properties:
        wort:
          type: string
          enum: [box_nicht_in_anlage, kein_netzanschluss, grenze_fehlt, faehigkeit_fehlt, nachweis_fehlt,
                 auslegung_passt_nicht, fuehrende_box_misst_nicht, vorgabe_signal_nicht_an_jeder_box,
                 mitsteuernde_box_misst_nicht]
        box_id: { type: string, format: uuid, nullable: true }
        richtung: { type: string, nullable: true, enum: [einspeisung, bezug] }
    GemeinsameSteuerungWarnungFuehrung:
      type: object
      required: [wort, fuehrende_box_id, speicher_box_id]
      properties:
        wort: { type: string, enum: [fuehrende_box_ist_nicht_speicher_box] }
        fuehrende_box_id: { type: string, format: uuid }
        speicher_box_id: { type: string, format: uuid }
    GemeinsameSteuerungEinrichten:
      type: object
      additionalProperties: false
      required: [mitglieder]
      properties:
        mitglieder:
          type: array
          minItems: 1
          items:
            type: object
            additionalProperties: false
            required: [box_id, rolle]
            properties:
              box_id: { type: string, format: uuid }
              rolle: { type: string, enum: [fuehrt, steuert_mit] }
              messpunkt_id: { type: string, format: uuid, nullable: true }
              vorgabe_signal:
                type: string
                nullable: true
                enum: [ja, nein, unbekannt, null]
                description: >-
                  G6, wahlfrei: fehlt es, bleibt das erklärte Signal der Box (eine neue Box beginnt mit
                  `unbekannt`). Eine Änderung ist eine Strukturänderung (Stufe zurück, Protokoll `vorgabe_signal`).
              geraete:
                type: array
                description: >-
                  Erklärung: ALLE Geräte hinter dem Abgang der Box — jede Komponente mit Schreibfreigabe an ihr muss
                  darin stehen (sonst 422). Die Schreibfreigabe kommt aus dem Bestand; eine Komponente ohne zählt als
                  ungeregelt mit Nennleistung (I1).
                items:
                  type: object
                  additionalProperties: false
                  required: [komponente_id, richtung, nenn_kw]
                  properties:
                    komponente_id: { type: string, format: uuid }
                    richtung: { type: string, enum: [einspeisung, bezug] }
                    nenn_kw: { type: number, exclusiveMinimum: 0 }
              ungeregelt:
                type: array
                description: "Wahlfrei, nur mit `geraete`: das Ungeregelte hinter dem Abgang (B3), je Richtung einmal."
                items:
                  type: object
                  additionalProperties: false
                  required: [richtung, hoechstwert_kw]
                  properties:
                    richtung: { type: string, enum: [einspeisung, bezug] }
                    hoechstwert_kw: { type: number, exclusiveMinimum: 0 }
        ungesteuerte_erzeuger:
          description: >-
            Erklärung, Pflicht (Frage 4): `"keine"` oder eine nicht leere Liste der Erzeuger hinter dem Anschluss, die
            keine Box steuert — unbekannt ist keine Null. Ihre Summe ist der Vorbehalt der Einspeiseseite.
          oneOf:
            - { type: string, enum: [keine] }
            - type: array
              minItems: 1
              items: { $ref: "#/components/schemas/GemeinsameSteuerungErzeuger" }
        vorbehalt:
          type: object
          additionalProperties: false
          required: [bezug_kw]
          description: >-
            Erklärung, wahlfrei (B4): der ERKLÄRTE Vorbehalt der Bezugsseite. Trägt eine Messung einen höheren, ist
            ein kleinerer 409 `vorbehalt_gemessen` (senken = Vorschlag mit Freigabe des Betreibers).
          properties:
            bezug_kw: { type: number, minimum: 0 }
    GemeinsameSteuerungErzeuger:
      type: object
      additionalProperties: false
      required: [bezeichnung, nenn_kw]
      properties:
        bezeichnung: { type: string }
        nenn_kw: { type: number, exclusiveMinimum: 0 }
    GemeinsameSteuerungFehler:
      type: object
      required: [code, message]
      properties:
        code: { type: string }
        message: { type: string }
        fehlt:
          type: array
          description: Befunde des Ablehnungs-Vokabulars (409) oder Lücken der Erklärung (422).
          items:
            oneOf:
              - { $ref: "#/components/schemas/GemeinsameSteuerungBefund" }
              - { $ref: "#/components/schemas/GemeinsameSteuerungLuecke" }
    GemeinsameSteuerungLuecke:
      type: object
      properties:
        wort: { type: string, enum: [geraete, komponente, ungesteuerte_erzeuger] }
        box_id: { type: string, format: uuid, nullable: true }
        komponente_id: { type: string, format: uuid, nullable: true }
    GemeinsameSteuerungRueckfallHinterlegen:
      type: object
      additionalProperties: false
      required: [richtung, rueckfall]
      properties:
        richtung: { type: string, enum: [einspeisung, bezug] }
        rueckfall: { type: string, enum: [haelt_letzten_wert, faellt_auf_wert, laeuft_frei, unbekannt] }
        rueckfall_kw: { type: number, minimum: 0, nullable: true, description: "Nur und genau bei `faellt_auf_wert`." }
        nach_s: { type: integer, minimum: 0, nullable: true }
        hinweis: { type: string, nullable: true }
    GemeinsameSteuerungRueckfallAngabe:
      type: object
      properties:
        id: { type: string, format: uuid }
        richtung: { type: string, enum: [einspeisung, bezug] }
        rueckfall: { type: string, enum: [haelt_letzten_wert, faellt_auf_wert, laeuft_frei, unbekannt] }
        rueckfall_kw: { type: number, nullable: true }
        nach_s: { type: integer, nullable: true }
        hinweis: { type: string, nullable: true }
        eingetragen_von: { type: string }
        eingetragen_am: { type: string, format: date-time }
        aufgehoben_am: { type: string, format: date-time, nullable: true }
    GemeinsameSteuerungEinrichtenAuskunft:
      type: object
      description: Antwort von `GET …/gemeinsame-steuerung/einrichten` (Vertrag steuerungsverbund.md §6a).
      properties:
        eingerichtet: { type: boolean }
        netzzaehler_box_id: { type: string, format: uuid, nullable: true, description: "Frage 2: die Box am Netzzähler (Rolle grid-meter)." }
        grenzen:
          type: object
          description: Frage 3 — die wirksamen Grenzen heute; leer = unbekannt.
          properties:
            einspeisung_kw: { type: number, nullable: true }
            bezug_kw: { type: number, nullable: true }
        boxen:
          type: array
          items: { $ref: "#/components/schemas/GemeinsameSteuerungEinrichtenBox" }
        ungesteuerte_erzeuger:
          nullable: true
          description: "`keine`, die Liste — oder leer: nicht erklärt."
          oneOf:
            - { type: string, enum: [keine] }
            - type: array
              items: { $ref: "#/components/schemas/GemeinsameSteuerungErzeuger" }
        vorbehalt:
          type: object
          nullable: true
          properties:
            einspeisung_kw: { type: number, nullable: true }
            bezug_kw: { type: number, nullable: true }
            bezug_herkunft: { type: string, nullable: true, enum: [erklaert, gemessen, null] }
            von: { type: string, nullable: true }
            am: { type: string, format: date-time, nullable: true }
            aus_messwerten:
              type: object
              nullable: true
              description: Was IP-13 heute aus den Messwerten rechnet (Höchstwert × 1,1); leer ohne Messtag.
              properties:
                kw: { type: number }
                hoechstwert_kw: { type: number }
                hoechstwert_von: { type: string, format: date-time, nullable: true }
                messtage: { type: integer }
                zeitraum_von: { type: string, format: date }
                zeitraum_bis: { type: string, format: date }
        ergebnis:
          type: object
          nullable: true
          description: Frage 6 — je Richtung die Auslegung; eine Richtung ist leer, solange sie nicht rechenbar ist.
          properties:
            einspeisung: { $ref: "#/components/schemas/GemeinsameSteuerungAuslegung" }
            bezug: { $ref: "#/components/schemas/GemeinsameSteuerungAuslegung" }
        hinweise:
          type: array
          items:
            type: object
            properties:
              wort:
                type: string
                enum: [nennleistung_weicht_ab, ohne_schreibfreigabe, geraete_nicht_erklaert, geraet_nicht_erklaert,
                  erzeuger_nicht_erklaert, vorbehalt_nicht_erklaert, netzzaehler_nicht_gelesen]
              box_id: { type: string, format: uuid, nullable: true }
              komponente_id: { type: string, format: uuid, nullable: true }
              richtung: { type: string, nullable: true }
              bestand_kw: { type: number, nullable: true }
              erklaert_kw: { type: number, nullable: true }
    GemeinsameSteuerungEinrichtenBox:
      type: object
      properties:
        box_id: { type: string, format: uuid }
        name: { type: string }
        rolle: { type: string, nullable: true, enum: [fuehrt, steuert_mit, null] }
        messpunkt_id: { type: string, format: uuid, nullable: true }
        liest_netzzaehler: { type: boolean }
        komponenten:
          type: array
          items:
            type: object
            properties:
              komponente_id: { type: string, format: uuid }
              name: { type: string, nullable: true }
              typ: { type: string }
              schreibfreigabe: { type: boolean }
              richtungen: { type: array, items: { type: string, enum: [einspeisung, bezug] } }
              nenn_kw: { type: number, nullable: true, description: "Soweit der Bestand sie kennt (PV, Verbraucher-Profil)." }
        geraete_erklaert: { type: boolean }
        geraete:
          type: array
          items:
            type: object
            properties:
              komponente_id: { type: string, format: uuid }
              richtung: { type: string, enum: [einspeisung, bezug] }
              nenn_kw: { type: number }
              schreibfreigabe: { type: boolean }
              rueckfall: { type: string, nullable: true }
              rueckfall_kw: { type: number, nullable: true }
              rueckfall_herkunft: { type: string, nullable: true, enum: [am_geraet, katalog, ohne_angabe, null] }
        ungeregelt:
          type: array
          items:
            type: object
            properties:
              richtung: { type: string, enum: [einspeisung, bezug] }
              hoechstwert_kw: { type: number }
    GemeinsameSteuerungAuslegung:
      type: object
      nullable: true
      properties:
        urteil: { type: string, enum: [passt, auslegung_passt_nicht, vorbehalt_ueber_grenze] }
        grenze_kw: { type: number }
        vorbehalt_kw: { type: number }
        verteilbar_kw: { type: number }
        summe_rueckfall_kw: { type: number }
        anteile:
          type: array
          items:
            type: object
            properties:
              box_id: { type: string, format: uuid }
              kw: { type: number }
        ungenutzt_kw: { type: number }
        uebergangszuschlag_kw:
          type: number
          description: Puffer für den Ausfall der führenden Box (Rückfallzeit / 900 s × Lade- bzw. Entladeleistung ihrer Speicher, aufgerundet); 0 ohne Speicher dort.
        uebergangszuschlag_fehlt_kw:
          type: number
          nullable: true
          description: Teil des Puffers, der über den Rückfällen keinen Platz fand (nur bei passt). Führt nie zur Ablehnung.
    BezugsdatenImportBestaetigung:
      type: object
      additionalProperties: false
      required: [vorschau]
      properties:
        vorschau: { type: string, description: Kennung der höchstens 30 Minuten alten unveränderten Vorschau. }
        entscheidungen:
          type: object
          description: Datenzeilennummer als Schlüssel; fehlende Konfliktentscheidung bedeutet behalten.
          additionalProperties: { type: string, enum: [behalten, ersetzen] }
        begruendung: { type: string, nullable: true, minLength: 10, maxLength: 500 }
        teiluebernahme: { type: string, nullable: true, description: Exakter Bestätigungssatz der Import-Regel. }
    BezugsdatenImportGrund:
      type: object
      additionalProperties: false
      required: [begruendung]
      properties:
        begruendung: { type: string, minLength: 10, maxLength: 500 }
    BezugsdatenImportErgebnis:
      type: object
      required: [kennung, status, aenderungen, vorschlaege, zaehler, vorlage]
      properties:
        kennung: { type: string, pattern: '^I-[0-9]{4}-[0-9]{4,}$' }
        status: { type: string, enum: [uebernommen, teilweise_uebernommen, wiederholt, verworfen, zurueckgenommen] }
        aenderungen: { type: integer, minimum: 0 }
        vorschlaege: { type: integer, minimum: 0, description: Offene Wertvorschläge. Eine vorgeschlagene Rücknahme ändert den Import-Status erst nach Freigabe. }
        zaehler: { type: object, nullable: true, description: Import-Zähler der gemeinsamen Bezugsdaten-Regel. }
        vorlage:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/BezugsdatenVorlageVerweis" }]
          description: Die beim Import tatsächlich verwendete Vorlage und Fassung; spätere Vorlagenänderungen ändern diesen Beleg nicht.
    BezugsdatenImportProtokoll:
      type: object
      required: [importe]
      properties:
        importe:
          type: array
          items: { $ref: "#/components/schemas/BezugsdatenImportProtokollEintrag" }
    BezugsdatenImportProtokollEintrag:
      type: object
      required: [kennung, status, datei_name, datei_bytes, erstellt_am, geaendert_am, aenderungen, vorschlaege, zaehler, vorlage, urheber, zeilen]
      properties:
        kennung: { type: string, pattern: '^I-[0-9]{4}-[0-9]{4,}$' }
        status: { type: string, enum: [uebernommen, teilweise_uebernommen, wiederholt, verworfen, zurueckgenommen] }
        datei_name: { type: string }
        datei_bytes: { type: integer, minimum: 0 }
        erstellt_am: { type: string, format: date-time }
        geaendert_am: { type: string, format: date-time }
        aenderungen: { type: integer, minimum: 0 }
        vorschlaege: { type: integer, minimum: 0 }
        zaehler: { type: object }
        vorlage:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/BezugsdatenVorlageVerweis" }]
        begruendung: { type: [string, 'null'] }
        urheber: { type: object }
        zeilen:
          type: array
          items:
            type: object
            required: [nr, urteil, befunde]
            properties:
              nr: { type: integer }
              urteil: { type: string }
              befunde:
                type: array
                items: { $ref: "#/components/schemas/BezugsdatenBefund" }
    BezugsdatenRuecknahmeVorschau:
      type: object
      required: [kennung, aenderungen, vieraugen, werte]
      properties:
        kennung: { type: string }
        aenderungen: { type: integer, minimum: 0 }
        vieraugen: { type: boolean }
        werte:
          type: array
          items:
            type: object
            required: [bezugsgroesse_id, kennzeichen, name, einheit, vorgang]
            properties:
              bezugsgroesse_id: { type: string, format: uuid }
              kennzeichen: { type: string }
              name: { type: string }
              periode_von: { type: [string, 'null'], format: date }
              periode_bis: { type: [string, 'null'], format: date }
              zeitpunkt: { type: [string, 'null'], format: date-time }
              bisheriger_betrag: { type: [string, 'null'] }
              neuer_betrag: { type: [string, 'null'] }
              einheit: { type: string }
              vorgang: { type: string, enum: [zurueckgenommen, vorfassung_wiederhergestellt] }
    Ablesung:
      type: object
      required: [quelle, zeitpunkt, fassung, stand, monat, woher, urheber, korrektur, eingetragen_am]
      properties:
        quelle: { type: string, format: uuid }
        zeitpunkt: { type: string, format: date-time }
        fassung: { type: integer, minimum: 1 }
        stand: { type: number }
        monat: { type: [string, 'null'], format: date }
        woher: { type: string, enum: [eingabe, import] }
        urheber:
          type: object
          required: [sub, name, rolle, art]
          properties:
            sub: { type: string }
            name: { type: string }
            rolle: { type: string }
            art: { type: string }
        korrektur: { type: [string, 'null'] }
        eingetragen_am: { type: string, format: date-time }
    AblesungAntwort:
      type: object
      required: [urteil, korrektur, ablesung, ablesezeitraum]
      properties:
        urteil: { type: string, enum: [eingetragen, wiederholung, berichtigung, vorschlag] }
        korrektur: { type: [string, 'null'] }
        ablesung: { $ref: '#/components/schemas/Ablesung' }
        ablesezeitraum:
          type: [object, 'null']
          description: Menge, Zustand, Ablesezeitraum-Kennzeichen und Zuordnungshilfe mit Zeitanteilen; keine Interpolation.
    Selbstauskunft:
      type: object
      description: UEMS AP-03 IP-4 - die Selbstauskunft (`GET /api/v1/me`).
      required: [kennung, name, konto, zustand, kundenbereich, zugang, rollen, unternehmensweit, standorte,
        unternehmen_rechte, kuenftig, text, teilansicht, unterstuetzungen, kundenadministratoren, kundenbereiche]
      properties:
        kennung: { type: string, nullable: true, description: Das Subject des Kontos. }
        name: { type: string, nullable: true, description: "Anzeigename aus dem Benutzer-Spiegel, sonst aus dem Token." }
        konto: { type: string, nullable: true, enum: [benutzer, partner, plattform] }
        zustand: { type: string, enum: [angelegt, aktiv, gesperrt, entfernt] }
        kundenbereich:
          nullable: true
          description: null ohne angenommenen Kundenbereich (Partner ohne wirksame Unterstützung).
          allOf: [{ $ref: "#/components/schemas/SelbstauskunftKundenbereich" }]
        zugang:
          type: string
          nullable: true
          enum: [konto, unterstuetzung, umschalter]
          description: "`umschalter` = Plattform über `X-Tenant-Id` (bis AP-03 IP-8); null ohne Kundenbereich."
        rollen:
          type: array
          items: { type: string }
          description: Die wirksamen Rollen (Kennungen der Matrix) in ihrer Folge.
        unternehmensweit: { type: boolean }
        standorte:
          type: array
          items: { $ref: "#/components/schemas/SelbstauskunftStandort" }
        unternehmen_rechte:
          type: array
          items: { type: string }
          description: Die Aktionen (Kennungen der Matrix), die der Aufrufer auf Unternehmensebene darf.
        kuenftig:
          type: array
          items: { $ref: "#/components/schemas/SelbstauskunftKuenftig" }
        text:
          type: string
          nullable: true
          description: Der Satz ohne Standort („Ihnen ist derzeit kein Standort zugewiesen. …“), sonst null.
        teilansicht:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/SelbstauskunftTeilansicht" }]
        unterstuetzungen: { $ref: "#/components/schemas/SelbstauskunftUnterstuetzungen" }
        kundenadministratoren:
          type: array
          items: { $ref: "#/components/schemas/SelbstauskunftPerson" }
        kundenbereiche:
          type: array
          description: Nur eigene aktuell gültige Unterstützungen des verifizierten Partner-/Plattformkontos; Kundenkonten erhalten eine leere Liste. Ende exklusiv.
          items:
            type: object
            required: [id, name, umfang, endet]
            properties:
              id: { type: string, format: uuid }
              name: { type: string }
              umfang: { type: string, enum: [ansehen, einrichten, einrichten_und_bedienen] }
              endet: { type: string, format: date-time }
    SelbstauskunftKundenbereich:
      type: object
      required: [id, name, beendet]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        beendet:
          description: >
            UEMS AP-20 IP-16 - null while the Kundenbereich is aktiv. Once beendet, every other customer
            route answers this person 409 kundenbereich_beendet {code, message, beendet_am,
            loeschung_fruehestens} unless liest is true (the Kundenadministrator), and every write route
            answers it to everyone.
          nullable: true
          allOf: [{ $ref: "#/components/schemas/SelbstauskunftKundenbereichBeendet" }]
    SelbstauskunftKundenbereichBeendet:
      type: object
      required: [beendet_am, loeschung_fruehestens, liest, text]
      properties:
        beendet_am: { type: string, format: date }
        loeschung_fruehestens: { type: string, format: date, description: day of the end + Frist, computed on request }
        liest: { type: boolean, description: true only for the Kundenadministrator }
        text: { type: string, description: the sentence for THIS person, built by the API }
    SelbstauskunftStandort:
      type: object
      required: [id, kennzeichen, name, rollen, umfang, ocpp_stufe, rechte]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, example: ST-1 }
        name: { type: string }
        rollen: { type: array, items: { type: string } }
        umfang:
          type: string
          nullable: true
          enum: [ansehen, einrichten, einrichten_und_bedienen]
          description: Der höchste Umfang einer Unterstützung an diesem Standort, sonst null.
        ocpp_stufe: { type: string, enum: [keine, CUSTOMER, SITE_ADMIN, PLATFORM] }
        rechte:
          type: array
          items: { type: string }
          description: Die Aktionen (Kennungen der Matrix), die der Aufrufer an diesem Standort darf.
    SelbstauskunftKuenftig:
      type: object
      required: [standort, ab, text]
      properties:
        standort: { type: string }
        ab: { type: string, format: date-time }
        text: { type: string }
    SelbstauskunftTeilansicht:
      type: object
      required: [sichtbar, gesamt, unternehmensebene, teilansicht, kopfzeile, export_kopfzeile,
        unternehmensweite_objekte]
      properties:
        sichtbar: { type: integer }
        gesamt: { type: integer }
        unternehmensebene: { type: boolean }
        teilansicht: { type: boolean }
        kopfzeile: { type: string, nullable: true }
        export_kopfzeile: { type: string, nullable: true }
        unternehmensweite_objekte: { type: boolean }
    SelbstauskunftUnterstuetzungen:
      type: object
      required: [eigene, gewaehrte]
      properties:
        eigene:
          type: array
          items: { $ref: "#/components/schemas/SelbstauskunftUnterstuetzung" }
          description: Die wirksamen Unterstützungen des Aufrufers in diesem Kundenbereich (Partner, Plattform).
        gewaehrte:
          type: array
          items: { $ref: "#/components/schemas/SelbstauskunftUnterstuetzung" }
          description: Die wirksamen Unterstützungen an den sichtbaren Standorten des Kundenkontos.
    SelbstauskunftUnterstuetzung:
      type: object
      required: [art, umfang, standorte, gueltig_ab, gueltig_bis, endet, zustand, erinnerung, unterstuetzer, banner]
      properties:
        art: { type: string, enum: [installateur, voltpilot, notfall] }
        umfang: { type: string, nullable: true, enum: [ansehen, einrichten, einrichten_und_bedienen] }
        standorte: { type: array, items: { type: string } }
        gueltig_ab: { type: string, format: date-time }
        gueltig_bis:
          type: string
          format: date
          nullable: true
          description: Das Enddatum (einschließlich); der Notfall-Zugriff hat keins, nur `endet`.
        endet: { type: string, format: date-time, nullable: true }
        zustand: { type: string, enum: [entwurf, eingerichtet, aktiv, archiviert] }
        erinnerung: { type: boolean, description: true in den letzten sieben Tagen (nie beim Notfall-Zugriff). }
        unterstuetzer: { $ref: "#/components/schemas/SelbstauskunftPerson" }
        banner:
          type: string
          nullable: true
          description: Der Banner-Satz - beim Kunden „… hat Zugriff auf …“, beim Unterstützer „Sie arbeiten im Kundenbereich …“.
    Zugriff:
      type: object
      description: >
        UEMS AP-03 IP-9 - eine Zuweisung: Person x Rolle x Geltungsbereich x Zeit. `standort_id` null heißt
        unternehmensweit. Eine Zuweisung wird nie umgeschrieben; `beendet_am` ist ihr Ende (der Entzug wirkt ab
        diesem Zeitpunkt mit der nächsten Anfrage).
      required: [id, benutzer_sub, rolle, gueltig_ab]
      properties:
        id: { type: string, format: uuid }
        benutzer_sub: { type: string }
        benutzer_name: { type: string, nullable: true }
        rolle:
          type: string
          enum: [kundenadministrator, energiemanager, bearbeiter, bedienberechtigt, leser, unterstuetzer, einsicht]
        standort_id: { type: string, format: uuid, nullable: true }
        standort_kurzzeichen: { type: string, nullable: true }
        standort_name: { type: string, nullable: true }
        gueltig_ab: { type: string, format: date-time }
        gueltig_bis: { type: string, format: date, nullable: true, description: "Enddatum, letzter Tag einschließlich; null = unbefristet (AP-19 IP-12: Einsicht, Unterstützung)." }
        beendet_am: { type: string, format: date-time, nullable: true }
    Unterstuetzung:
      type: object
      description: >
        Eine gewährte Unterstützung (UEMS AP-03 IP-8) - die Zeilen einer Gewährung zusammengefasst. Die Wörter
        `art`, `umfang` und `zustand` sind die des Rechte-Vertrags (docs/contracts/v2/rechte-vectors.json).
      required: [id, art, umfang, standorte, standort_kennzeichen, unterstuetzer, gueltig_ab, gueltig_bis, endet,
        zustand, erinnerung, grund, banner, text, startpasswort]
      properties:
        id:
          type: string
          format: uuid
          description: Der Griff der Gewährung - die kleinste Kennung ihrer Zeilen; er bleibt, auch nachdem sie endete.
        art: { type: string, enum: [installateur, voltpilot, notfall] }
        umfang: { type: string, nullable: true, enum: [ansehen, einrichten, einrichten_und_bedienen] }
        standorte: { type: array, items: { type: string, format: uuid } }
        standort_kennzeichen:
          type: array
          items: { type: string }
          description: Dieselben Standorte als Kennzeichen des Vertrags (ST-1 …).
        unterstuetzer: { $ref: "#/components/schemas/UnterstuetzungPerson" }
        gueltig_ab: { type: string, format: date-time }
        gueltig_bis:
          type: string
          format: date
          nullable: true
          description: Das Enddatum (einschließlich); der Notfall-Zugriff hat keins, nur `endet`.
        endet: { type: string, format: date-time, nullable: true }
        zustand: { type: string, enum: [entwurf, eingerichtet, aktiv, archiviert] }
        erinnerung: { type: boolean, description: true in den letzten sieben Tagen (nie beim Notfall-Zugriff). }
        grund: { type: string, nullable: true }
        banner:
          type: string
          nullable: true
          description: Der Banner-Satz, solange sie wirkt; sonst null.
        text:
          type: string
          nullable: true
          description: Der Satz danach („Endete am … durch Zeitablauf“, „Beendet am … durch …“, „Wirkt ab …“).
        startpasswort:
          type: string
          nullable: true
          description: >
            GENAU EINMAL in der Antwort des Gewährens, wenn dafür ein Partner-Konto entstand (E14) - nie in
            einer Liste, einem Protokoll, einem Hinweis oder einer E-Mail.
    UnterstuetzungGewaehren:
      type: object
      description: Streng gelesen - ein unbekanntes Feld (auch camelCase) ist 400 `anfrage_ungueltig` mit `feld`.
      properties:
        art:
          type: string
          enum: [installateur, voltpilot]
          description: Ohne `anfrage_id` Pflicht; mit ihr gilt `voltpilot`.
        email:
          type: string
          nullable: true
          description: >
            Die Adresse des Partner-Kontos (Installateur, E7). Unbekannt im Realm → ein neues Konto mit
            Startpasswort; bekannt → dasselbe Konto, nie eine zweite Identität für dieselbe Person.
        standorte:
          type: array
          items: { type: string, format: uuid }
          description: Mindestens einer (E5); leer ist 422 `standort_fehlt`. Mit `anfrage_id` sonst die der Anfrage.
        umfang: { type: string, nullable: true, enum: [ansehen, einrichten, einrichten_und_bedienen] }
        gueltig_ab: { type: string, format: date-time, nullable: true, description: Vorgabe jetzt. }
        gueltig_bis:
          type: string
          format: date
          nullable: true
          description: Enddatum einschließlich; Vorgabe 30 Tage, höchstens 12 Monate (422 `hoechstens_12_monate`).
        grund: { type: string, nullable: true, description: Freitext; erscheint im Banner-Detail und im Protokoll. }
        anfrage_id:
          type: string
          format: uuid
          nullable: true
          description: Bestätigt eine Anfrage von VoltPilot (E8) - Umfang und Dauer dürfen dabei abweichen.
    UnterstuetzungVerlaengern:
      type: object
      required: [gueltig_bis]
      properties:
        gueltig_bis: { type: string, format: date, description: Das NEUE Enddatum (einschließlich), später als das alte. }
    UnterstuetzungBeenden:
      type: object
      properties:
        grund: { type: string, nullable: true }
    UnterstuetzungAnfrage:
      type: object
      description: Ein Wunsch von VoltPilot (E8, A5) - er gewährt nichts.
      required: [id, art, umfang, standorte, angefragt_von, gueltig_ab, gueltig_bis, grund, zustand,
        entschieden_am, unterstuetzung, angefragt_am]
      properties:
        id: { type: string, format: uuid }
        art: { type: string, enum: [voltpilot] }
        umfang: { type: string, enum: [ansehen, einrichten, einrichten_und_bedienen] }
        standorte: { type: array, items: { type: string, format: uuid } }
        angefragt_von: { $ref: "#/components/schemas/UnterstuetzungPerson" }
        gueltig_ab: { type: string, format: date-time }
        gueltig_bis: { type: string, format: date }
        grund: { type: string, nullable: true }
        zustand:
          type: string
          enum: [offen, bestaetigt, abgelehnt]
          description: Abgeleitet, nie gespeichert - aus Entscheidung und gewährter Unterstützung.
        entschieden_am: { type: string, format: date-time, nullable: true }
        unterstuetzung:
          type: string
          format: uuid
          nullable: true
          description: Der Griff der gewährten Unterstützung, sonst null.
        angefragt_am: { type: string, format: date-time }
    UnterstuetzungPlattformAntrag:
      type: object
      description: Der Körper beider Plattform-Routen (Anfrage und Notfall-Zugriff).
      properties:
        standorte:
          type: array
          items: { type: string, format: uuid }
          description: Mindestens einer (E5).
        umfang: { type: string, nullable: true, enum: [ansehen, einrichten, einrichten_und_bedienen] }
        gueltig_ab: { type: string, format: date-time, nullable: true, description: Vorgabe jetzt. }
        gueltig_bis:
          type: string
          format: date
          nullable: true
          description: Nur bei der Anfrage; der Notfall-Zugriff gilt immer genau 24 Stunden.
        grund:
          type: string
          nullable: true
          description: Beim Notfall-Zugriff PFLICHT (422 `grund_fehlt`), bei der Anfrage frei.
    UnterstuetzungHinweis:
      type: object
      description: >
        Ein Hinweis im Postfach eines Kundenadministrators - heute die Karte im Portal, mit SMTP dieselbe Zeile
        als E-Mail.
      required: [id, anlass, text, unterstuetzung, anfrage, erzeugt_am, gelesen_am, email_versandt_am]
      properties:
        id: { type: string, format: uuid }
        anlass: { type: string, enum: [anfrage, gewaehrt, notfall, erinnerung, abgelaufen, beendet] }
        text: { type: string }
        unterstuetzung: { type: string, format: uuid, nullable: true }
        anfrage: { type: string, format: uuid, nullable: true }
        erzeugt_am: { type: string, format: date-time }
        gelesen_am: { type: string, format: date-time, nullable: true }
        email_versandt_am:
          type: string
          format: date-time
          nullable: true
          description: null, solange kein SMTP steht - dann ist das Portal der Weg.
    UnterstuetzungPerson:
      type: object
      required: [kennung, name]
      properties:
        kennung: { type: string, description: Das Subject des Kontos. }
        name: { type: string }
    UnterstuetzungFehler:
      type: object
      description: >
        Der GESCHLOSSENE Satz der Ablehnungen (UEMS AP-03 IP-8). Zwilling von
        `unterstuetzung/UnterstuetzungAbgelehnt.Ablehnung`; `standort_fehlt`, `hoechstens_12_monate` und
        `grund_fehlt` sind die Wörter des Rechte-Vertrags (rechte-vectors.json → gruende).
      required: [code, message]
      properties:
        code:
          type: string
          enum: [anfrage_ungueltig, nicht_gefunden, bereits_beendet, anfrage_entschieden, standort_fehlt,
            standort_unbekannt, hoechstens_12_monate, grund_fehlt, ende_nicht_spaeter,
            notfall_nicht_verlaengerbar, konto_nicht_erreichbar]
        message: { type: string }
        feld: { type: string, description: Nur bei `anfrage_ungueltig`. }
        standort_id: { type: string, format: uuid, description: Nur bei `standort_unbekannt`. }
    SelbstauskunftPerson:
      type: object
      required: [kennung, name]
      properties:
        kennung: { type: string, description: Das Subject des Kontos. }
        name: { type: string }
    OcppConnectorState:
      type: object
      properties:
        connectorId: { type: integer, minimum: 0 }
        status: { type: string }
        errorCode: { type: string }
        info: { type: string, nullable: true }
        vendorId: { type: string, nullable: true }
        vendorErrorCode: { type: string, nullable: true }
        stationTimestamp: { type: string, format: date-time, nullable: true }
        reportedAt: { type: string, format: date-time }
    OcppStation:
      type: object
      properties:
        deviceId: { type: string, format: uuid }
        chargePointId: { type: string }
        connected: { type: boolean }
        connectedAt: { type: string, format: date-time, nullable: true }
        disconnectedAt: { type: string, format: date-time, nullable: true }
        lastSeen: { type: string, format: date-time, nullable: true }
        bootedAt: { type: string, format: date-time, nullable: true }
        chargeBoxSerialNumber: { type: string, nullable: true }
        chargePointModel: { type: string, nullable: true }
        chargePointSerialNumber: { type: string, nullable: true }
        chargePointVendor: { type: string, nullable: true }
        firmwareVersion: { type: string, nullable: true }
        iccid: { type: string, nullable: true }
        imsi: { type: string, nullable: true }
        meterSerialNumber: { type: string, nullable: true }
        meterType: { type: string, nullable: true }
        diagnosticsStatus: { type: string, nullable: true }
        diagnosticsStatusAt: { type: string, format: date-time, nullable: true }
        firmwareStatus: { type: string, nullable: true }
        firmwareStatusAt: { type: string, format: date-time, nullable: true }
        connectors: { type: array, items: { $ref: "#/components/schemas/OcppConnectorState" } }
        supportedFeatureProfiles: { type: array, items: { type: string } }
    OcppProtocolEvent:
      type: object
      description: Privacy-redigiertes OCPP-Journal; idTags sind tagref_*, AuthorizationKey/Diagnose-URL/DataTransfer-Geheimnisse fehlen.
      properties:
        eventId: { type: string, format: uuid }
        occurredAt: { type: string, format: date-time }
        deviceId: { type: string, format: uuid }
        chargePointId: { type: string }
        direction: { enum: [station_to_csms, csms_to_station, internal] }
        messageType: { enum: [Call, CallResult, CallError, Event] }
        correlationId: { type: string, nullable: true }
        action: { type: string }
        errorCode: { type: string, nullable: true }
        errorDescription: { type: string, nullable: true, enum: ['[redacted-call-error-description]'], description: Untypisierter Originalfreitext wird nie ausgeliefert; nur dieser feste Anwesenheitsmarker. }
        errorDetails: { type: object, nullable: true, additionalProperties: true }
        payload: { type: object, additionalProperties: true }
    OcppDataGap:
      type: object
      description: Cloud-persistenter Beleg eines unvollständigen OCPP-Bereichs; totalDropped ist je Edge monoton.
      properties:
        eventId: { type: string, format: uuid }
        reportedAt: { type: string, format: date-time }
        deviceId: { type: string, format: uuid }
        droppedCount: { type: integer, format: int64, minimum: 1 }
        totalDropped: { type: integer, format: int64, minimum: 1 }
        firstOccurredAt: { type: string, format: date-time, nullable: true }
        lastOccurredAt: { type: string, format: date-time, nullable: true }
        firstEventId: { type: string, nullable: true }
        lastEventId: { type: string, nullable: true }
        reasons: { type: object, additionalProperties: { type: integer, format: int64, minimum: 1 } }
    OcppTransaction:
      type: object
      properties:
        deviceId: { type: string, format: uuid }
        chargePointId: { type: string }
        transactionId: { type: integer }
        connectorId: { type: integer, minimum: 0 }
        startedAt: { type: string, format: date-time }
        stoppedAt: { type: string, format: date-time, nullable: true }
        meterStart: { type: integer, format: int64 }
        meterStop: { type: integer, format: int64, nullable: true }
        stopReason: { type: string, nullable: true }
        startIdTagRef: { type: string, nullable: true, pattern: '^tagref_[0-9a-f]+$' }
        stopIdTagRef: { type: string, nullable: true, pattern: '^tagref_[0-9a-f]+$' }
        reservationId: { type: integer, nullable: true }
        chargingProfileId: { type: integer, nullable: true }
        chargingProfilePurpose: { type: string, nullable: true }
        startAuthStatus: { type: string, nullable: true }
        stopAuthStatus: { type: string, nullable: true }
        parentIdTagRef: { type: string, nullable: true, pattern: '^tagref_[0-9a-f]+$' }
        transactionData: { type: array, description: Nach 90 Tagen leer; Einzelwerte stehen außerdem dimensionsgetreu im MeterValue-Endpunkt. }
        transactionDataPurgedAt: { type: string, format: date-time, nullable: true }
    OcppMeterSample:
      type: object
      properties:
        sampledAt: { type: string, format: date-time }
        eventId: { type: string, format: uuid }
        meterValueIndex: { type: integer }
        sampledValueIndex: { type: integer }
        deviceId: { type: string, format: uuid }
        chargePointId: { type: string }
        connectorId: { type: integer, minimum: 0 }
        transactionId: { type: integer, nullable: true }
        source: { enum: [MeterValues, TransactionData] }
        pointKey: { type: string, description: Kanonischer Schlüssel aller sechs OCPP-SampledValue-Dimensionen. }
        measurand: { type: string }
        context: { type: string }
        format: { type: string }
        phase: { type: string }
        location: { type: string }
        unit: { type: string }
        value: { type: string }
        numericValue: { type: number, nullable: true, description: Nur endliche Raw-Zahlen; SignedData bleibt ausschließlich value. }
    OcppConfigurationKey:
      type: object
      properties:
        key: { type: string }
        value: { type: string, nullable: true, description: Für AuthorizationKey ausnahmslos null. }
        readonly: { type: boolean }
        secret: { type: boolean }
        redacted: { type: boolean }
        standardKey: { type: boolean }
        meaningKnown: { type: boolean }
        reportedAt: { type: string, format: date-time }
    OcppConfiguration:
      type: object
      properties:
        deviceId: { type: string, format: uuid }
        chargePointId: { type: string }
        keys: { type: array, items: { $ref: "#/components/schemas/OcppConfigurationKey" } }
        unknownKeys: { type: array, items: { type: string } }
        supportedFeatureProfiles: { type: array, items: { type: string } }
    OcppActionPermissions:
      type: object
      description: D4 serverseitige Auskunft für das OCPP-Command-Gateway.
      properties:
        actions: { type: object, additionalProperties: { type: boolean } }
    OcppActionIntentRequest:
      type: object
      required: [action]
      properties:
        action: { type: string }
        connectorId: { type: integer, minimum: 0 }
        transactionId: { type: integer, nullable: true }
        request: { type: object, additionalProperties: true, description: "Exakte, aktionsspezifisch validierte und an den Intent gebundene Nutzlast." }
    OcppActionIntent:
      type: object
      properties:
        id: { type: string, format: uuid }
        action: { type: string }
        phrase: { type: string, description: "Kurzlebige Bestätigung; kein Geheimnis." }
        fourEyes: { type: boolean, description: "Ein anderer Plattformoperator muss die gebundene Aktion ausführen." }
        expiresAt: { type: string, format: date-time }
    OcppActionRequest:
      type: object
      required: [action]
      properties:
        action: { type: string }
        connectorId: { type: integer, minimum: 0, nullable: true }
        transactionId: { type: integer, nullable: true }
        intentId: { type: string, format: uuid, nullable: true }
        confirmationPhrase: { type: string, nullable: true }
        request: { type: object, additionalProperties: true, description: "Wird serverseitig gegen die geschlossene OCPP-Aktionsform bzw. das registrierte DataTransfer-Schema validiert." }
    OcppAction:
      type: object
      properties:
        id: { type: string, format: uuid }
        deviceId: { type: string, format: uuid }
        chargePointId: { type: string }
        action: { type: string }
        state: { enum: [prepared, sent, accepted_waiting_effect, effect_observed, completed, rejected, call_error, edge_rejected, effect_failed, not_sendable, transport_failed, timed_out, cancelled] }
        correlationId: { type: string }
        idempotencyKey: { type: string }
        actor: { type: string }
        connectorId: { type: integer, nullable: true }
        transactionId: { type: integer, nullable: true }
        request: { type: object }
        response: { type: object }
        responseStatus: { type: string, nullable: true }
        effect: { type: object }
        reason: { type: string, nullable: true }
        preparedAt: { type: string, format: date-time }
        sentAt: { type: string, format: date-time, nullable: true }
        responseAt: { type: string, format: date-time, nullable: true }
        effectAt: { type: string, format: date-time, nullable: true }
        deadlineAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    OcppActionAudit:
      type: object
      properties:
        id: { type: integer, format: int64 }
        actor: { type: string }
        state: { type: string }
        reason: { type: string, nullable: true }
        deviceId: { type: string, format: uuid }
        chargePointId: { type: string }
        connectorId: { type: integer, nullable: true }
        transactionId: { type: integer, nullable: true }
        occurredAt: { type: string, format: date-time }
    RegisterWriteRequest:
      type: object
      description: >
        Was der Mensch im Register-Drawer eingetragen hat - ROH.
        Adresse und Wert sind absichtlich Zeichenketten: das Journal speichert
        sie VERBATIM, damit spaeter belegbar ist, was jemand wirklich getippt
        hat ("231" vs "0x00E7"), und nicht nur unsere Normalisierung.
      required: [address]
      properties:
        deviceId:
          type: string
          format: uuid
          description: >
            Das Ziel-Geraet. Ohne Angabe entscheidet das EINZIGE Geraet der
            Anlage; eine Anlage mit mehreren wird beim Namen genannt (409) -
            das Ziel wird nie geraten.
        lane:
          type: string
          enum: [primary, entity, lan]
          default: primary
          description: >
            WELCHES Ziel (Stufe 2). Die Asymmetrie ist die Sicherheit: bei
            `primary` nennt die Cloud NICHTS und die Box nimmt ihren eigenen
            Wechselrichter; bei `entity` reist NUR die Kennung, den Transport
            loest die Box aus IHRER angewandten Definition auf; nur bei `lan`
            reist der Endpunkt - und nur dort prueft die Box ihn selbst
            (LAN-Whitelist). Ein aelterer Client, der nichts schickt, arbeitet
            zeichengleich wie in Stufe 1 weiter.
        entityId:
          type: string
          format: uuid
          description: "Die Komponente - nur bei lane=entity, dann PFLICHT."
        host:
          type: string
          maxLength: 253
          description: >
            Die freie LAN-Adresse - nur bei lane=lan, dann PFLICHT. Ob sie
            belegbar privat ist, prueft die BOX; hier steht keine zweite
            Wahrheit ueber ein Netz, das die api nie gesehen hat.
        port: { type: integer, minimum: 1, maximum: 65535, default: 502 }
        unitId: { type: integer, minimum: 0, maximum: 255, default: 1 }
        registerKind: { type: string, enum: [holding, coil], default: holding }
        address:
          type: string
          maxLength: 16
          description: "Dezimal (231) oder hexadezimal (0x00E7). Ein nacktes E7 wird bewusst NICHT als Hex geraten."
        value:
          type: string
          maxLength: 16
          description: "Der ROHE Registerwert (kein kW). Nur beim Schreiben; dieselbe Schreibweise wie die Adresse."
        expectedBefore:
          type: integer
          minimum: 0
          maximum: 65535
          description: "Der Ist-Wert der Vorschau. Er wird AUF DEM GERAET verglichen, in derselben Socket-Sitzung, in der gelesen und geschrieben wird."
        writeFc:
          type: integer
          enum: [5, 6, 16]
          description: "Leer lassen: dann entscheidet die Box (Spule 5, Holding 16). FC16 ist die Vorauswahl, weil viele Firmwares einen FC6-Rahmen ANNEHMEN und nie uebernehmen."
        note:
          type: string
          maxLength: 500
          description: "Der Grund, verbatim ins Journal. PFLICHT bei Registerklasse netz_compliance (Captain-Entscheid D5), sonst freiwillig."
    RegisterWriteOutcome:
      type: object
      description: >
        Das Ergebnis EINES Schritts. Jedes Zahlenfeld darf fehlen, und ein
        fehlendes heisst "nicht gemessen", nie 0 - bei einer Einspeisegrenze
        waere 0 ein WERT ("gar keine Einspeisung erlaubt").
      properties:
        requestId: { type: string }
        mode: { type: string, enum: [lesen, schreiben] }
        ok: { type: boolean }
        outcome:
          type: string
          enum: [gelesen, uebernommen, nicht_uebernommen, abgelehnt, fehler, unbekannt]
          description: "`unbekannt` ist die ehrliche Antwort auf Schweigen und ausdruecklich NICHT 'nicht geschrieben'."
        beforeRaw: { type: integer, nullable: true }
        afterRaw: { type: integer, nullable: true }
        beforeScaled: { type: number, nullable: true, description: "Nur wo eine Skala bekannt ist - nie eine erfundene Einheit." }
        afterScaled: { type: number, nullable: true }
        adopted:
          type: boolean
          nullable: true
          description: "DREIWERTIG: null = keine Aussage (Vorschau, ausgebliebene Quittung), false = angenommen aber NICHT uebernommen, true = zurueckgelesen."
        errorCode: { type: string, nullable: true, description: "Eine der geschlossenen Klassen des Downlink-Kontrakts; ein unbekanntes Wort wird verworfen." }
        message: { type: string, nullable: true }
        targetLabel: { type: string, nullable: true, description: "WOHIN die Box geschrieben hat - ihr eigenes Echo." }
        address: { type: integer }
        addressHex: { type: string }
        registerLabel: { type: string, nullable: true, description: "null = VoltPilot kennt dieses Register nicht (nie ein erfundener Name)." }
        registerClass: { type: string, enum: [netz_compliance, bekannt, unbekannt] }
        scaleNote: { type: string, nullable: true }
        registerNote:
          type: string
          nullable: true
          description: >
            Der Betreiber-Hinweis des Register-Wissens ("gehoert zur laufenden
            Steuerung", "Skalierung haengt an der Bauart") - oder null.
        scaleUnit: { type: string, nullable: true, description: "Die Einheit des skalierten Werts; null = keine bekannte Skala." }
        noteRequired: { type: boolean }
        confirm: { type: string, nullable: true, description: "Der Bestaetigungs-Token, den dieser Schreibvorgang getragen hat." }
        writesToday:
          type: integer
          description: >
            Wie oft dieses Register auf diesem Geraet HEUTE schon angefordert
            wurde (Berliner Tag). EEPROM-Ehrlichkeit statt einer Sperre;
            gezaehlt werden ANFORDERUNGEN, nicht Quittungen - ein
            Schreibvorgang, dessen Antwort verloren ging, kann angekommen sein.
        lane: { type: string, enum: [primary, entity, lan] }
        at: { type: string, format: date-time }
    RegisterWriteTarget:
      type: object
      description: >
        Ein Ziel des Geraete-Pickers. Ein Geraet ohne Schreibweg steht MIT
        seinem Grund hier - genannt statt verschwiegen.
      properties:
        lane: { type: string, enum: [primary, entity, lan] }
        deviceId: { type: string, format: uuid }
        entityId: { type: string, format: uuid, nullable: true, description: "Nur bei lane=entity." }
        label: { type: string }
        brand: { type: string, nullable: true }
        model: { type: string, nullable: true }
        family:
          type: string
          nullable: true
          description: >
            Die Register-Familie, aus der das Register-Wissen spricht. null =
            die Box hat sie (noch) nicht gemeldet - dann bleibt jedes Register
            dort ehrlich "unbekannt" statt mit einer fremden Bedeutung
            beschriftet zu werden.
        communication: { type: string, nullable: true }
        host: { type: string, nullable: true }
        port: { type: integer, nullable: true }
        unitId: { type: integer, nullable: true }
        writable: { type: boolean, description: "ANZEIGE-Hilfe, keine Zusage - die Box entscheidet." }
        reason: { type: string, nullable: true, description: "Warum nicht - immer gesetzt, wenn writable false ist." }
    RegisterKnowledgeFamily:
      type: object
      description: "Eine Register-Familie des kuratierten Verzeichnisses."
      properties:
        family: { type: string }
        brand: { type: string, nullable: true }
        label:
          type: string
          nullable: true
          description: >
            Der gemeinsame Komponenten-Anzeigename (`measurement_point.label`),
            sobald `entityId` gebunden ist; null = Portal fällt auf die
            ChargePointId zurück. Vor der Bindung darf vorübergehend das von
            der Box gemeldete Label stehen.
        registers:
          type: array
          items:
            type: object
            properties:
              address: { type: integer }
              addressHex: { type: string }
              label: { type: string }
              clazz: { type: string, enum: [netz_compliance, bekannt, unbekannt] }
              scale: { type: number, nullable: true }
              unit: { type: string, nullable: true, description: "Eine Skala kommt NUR mit ihrer Einheit." }
              note: { type: string, nullable: true }
    RegisterWriteEvent:
      type: object
      description: >
        EIN Vorgang aus dem append-only Journal, zu einer Zeile gefaltet.
        Die Herkunft steht an der Zeile: `kunde`/`voltpilot` sind ueber ein
        validiertes Token beweisbare Identitaeten, `geraet` ist der
        Wartungszugang an der Box - fuer den es cloud-seitig KEINE Identitaet
        gibt, und genau das sagt das Wort.
      properties:
        id: { type: integer, format: int64 }
        requestId: { type: string, description: "Der Kreuz-Schluessel zum persistenten Audit AUF der Box." }
        source: { type: string, enum: [portal, geraet] }
        deviceId: { type: string, nullable: true }
        deviceRef: { type: string, nullable: true }
        lane: { type: string, nullable: true }
        entityId:
          type: string
          format: uuid
          nullable: true
          description: >
            Die Komponente der Lane `komponente` (sonst null) - die einzige
            Zuordnung, mit der sich ein Vorgang einem Geraet HINTER der Box
            zuschreiben laesst. Primaere Lane und frei getippte Adresse gehoeren
            dem Schreibweg der BOX.
        targetLabel: { type: string, nullable: true }
        registerKind: { type: string, nullable: true }
        address: { type: integer, nullable: true }
        addressHex: { type: string, nullable: true }
        addressInput: { type: string, nullable: true, description: "VERBATIM, wie getippt." }
        valueInput: { type: string, nullable: true, description: "VERBATIM, wie getippt." }
        note: { type: string, nullable: true, description: "VERBATIM." }
        valueRaw: { type: integer, nullable: true }
        expectedBefore: { type: integer, nullable: true }
        registerLabel: { type: string, nullable: true }
        registerClass: { type: string, nullable: true }
        scaleNote: { type: string, nullable: true }
        origin: { type: string, enum: [kunde, voltpilot, geraet], nullable: true }
        actorName: { type: string, nullable: true, description: "Anzeige-Name, nie die Autoritaet." }
        actorRole: { type: string, nullable: true }
        viaTenantSwitcher: { type: boolean }
        requestedAt: { type: string, format: date-time }
        beforeRaw: { type: integer, nullable: true }
        afterRaw: { type: integer, nullable: true }
        adopted: { type: boolean, nullable: true }
        outcome: { type: string, nullable: true }
        reason: { type: string, nullable: true, description: "Der deutsche Satz der Box, verbatim." }
        answeredAt: { type: string, format: date-time, nullable: true }
        actorRolle:
          type: string
          nullable: true
          enum: [kundenadministrator, energiemanager, bearbeiter, bedienberechtigt, leser, unterstuetzer, voltpilot_betrieb, null]
          description: "AP-03 IP-7: Rolle, unter der geschrieben wurde; null im Bestand und an Box-Meldungen."
        actorArt:
          type: string
          nullable: true
          enum: [kunde, unterstuetzung, voltpilot, notfall, null]
          description: "AP-03 IP-7: Akteur-Vokabular neben `origin`; null im Bestand und an Box-Meldungen."
    SelfBuildChannel:
      type: object
      description: >
        ONE measured value of a self-built Modbus device. The customer names it
        in PLAIN TEXT; the channel key is DERIVED server-side (the registry's
        open channel vocabulary) - a typed key would be a second truth about the
        same measurement.
      properties:
        label: { type: string, maxLength: 120, description: "Plain-text name, e.g. Wassertemperatur." }
        unit: { type: string, description: "One of the curated units; empty means honestly 'no unit'." }
        registerKind: { type: string, enum: [holding, input] }
        address:
          type: integer
          minimum: 0
          maximum: 65535
          description: "0-based protocol address (40001 in a manual is address 0)."
        dataType: { type: string, enum: [u16, s16, u32, s32, float32] }
        wordOrder: { type: string, enum: [big, little], description: "Only meaningful for 32-bit types." }
        scale: { type: number }
        offset: { type: number }
        minReadIntervalS:
          type: integer
          minimum: 5
          maximum: 3600
          default: 10
          description: "Poll budget - at most one read per channel per this many seconds."
    SelfBuildConnection:
      type: object
      description: Where the device sits in the customer's own network (LAN-only).
      properties:
        host: { type: string, maxLength: 253 }
        port: { type: integer, minimum: 1, maximum: 65535, default: 502 }
        unitId: { type: integer, minimum: 0, maximum: 255, default: 1 }
    SaveSelfBuildRequest:
      type: object
      required: [connection, channels]
      properties:
        label: { type: string, maxLength: 200 }
        connection: { $ref: "#/components/schemas/SelfBuildConnection" }
        channels:
          type: array
          maxItems: 16
          items: { $ref: "#/components/schemas/SelfBuildChannel" }
        note: { type: string, maxLength: 200 }
    SocCurveTemplate:
      type: object
      required: [id, label, curveCharge]
      description: >
        ONE measured OCV->SoC table. A template is DATA, not code: it prefills
        the curve fields of a `soc_derivation`, nothing more - whoever has their
        own support points enters them and needs no template.
      properties:
        id: { type: string, description: kebab-case key, e.g. `diybms-176s-nmc`. }
        label: { type: string }
        description: { type: string }
        chemistry:
          type: string
          description: >
            nmc | lfp | ... - carried visibly BECAUSE the same voltage means a
            different state of charge on a different chemistry.
        cellsInSeries: { type: integer, nullable: true }
        refTempC: { type: number, nullable: true }
        cellMinV: { type: number, nullable: true }
        cellMaxV: { type: number, nullable: true }
        source: { type: string, description: Where the table was measured, verbatim. }
        curveCharge:
          type: array
          description: Support points `[cell voltage in V, state of charge in %]`, applied to the HIGHEST cell.
          items:
            type: array
            items: { type: number }
        curveDischarge:
          type: array
          nullable: true
          description: The discharge table, applied to the LOWEST cell. The conservative minimum of both wins.
          items:
            type: array
            items: { type: number }
    SaveUserDefinedBatteryRequest:
      type: object
      description: >
        The SELF-CONNECTED battery (P5 Ebene 1, `vp-deye-diybms-luecke-l5`
        §3.2b): a source in the customer LAN plus the user-defined field mapping
        onto the standard battery channels. The mapping vocabulary is CLOSED -
        the allowed target channels are exactly
        `user-defined-battery.default_measure` in the entity-type catalog, and a
        created battery declares only the channels actually mapped.


        `transport` picks which HALF must be filled: `mqtt_local` (the default
        when the field is absent - every connection made before P5-HTTP) needs
        `broker` and a `topic` per mapping; `http_local` needs `endpoint` (plus
        optional `auth`) and only a `path` per mapping - there without `staleS`,
        because an HTTP answer is ONE point in time. Sending both halves would be
        a connection with two addresses; the server takes the one that matches
        the transport and ignores the other.
      required: [mappings]
      properties:
        label: { type: string, maxLength: 200 }
        transport:
          type: string
          enum: [mqtt_local, http_local]
          default: mqtt_local
        broker: { $ref: "#/components/schemas/UserDefinedBatteryBroker" }
        endpoint: { $ref: "#/components/schemas/UserDefinedBatteryEndpoint" }
        auth: { $ref: "#/components/schemas/UserDefinedBatteryAuth" }
        mappings:
          type: array
          minItems: 1
          maxItems: 16
          items: { $ref: "#/components/schemas/UserDefinedBatteryMapping" }
        publishIntervalS:
          type: integer
          minimum: 5
          maximum: 3600
          default: 15
          description: >
            The cadence at which the box publishes ONE telemetry message with
            every channel that currently has a fresh value. It IS the flow's
            trigger interval - there is no second cadence concept.
        socDerivation: { $ref: "#/components/schemas/UserDefinedBatterySocDerivation" }
        binding: { $ref: "#/components/schemas/UserDefinedBatteryBinding" }
        note: { type: string, maxLength: 200 }
    UserDefinedBatteryEndpoint:
      type: object
      description: >
        The HTTP/JSON endpoint of the battery (P5-HTTP) - only with
        `transport: http_local`, and there mandatory. The box GETs exactly this
        address once per tick; it appends nothing and guesses nothing.


        The LAN-only rule is the same one every self-built connection follows,
        and `path` is a PATH, never a second address: it starts with `/`, carries
        no whitespace and must not begin with `//` (which would be a
        protocol-relative URL, i.e. a target other than the checked one).
      required: [host, path]
      properties:
        host: { type: string, maxLength: 253 }
        port: { type: integer, minimum: 1, maximum: 65535, default: 80 }
        path:
          type: string
          maxLength: 200
          example: "/ha"
          description: The URL path written out, query string included.
        tls: { type: boolean, default: false }
        timeoutMs: { type: integer, minimum: 500, maximum: 30000, default: 5000 }
    UserDefinedBatteryAuth:
      type: object
      description: >
        How VoltPilot authenticates at that endpoint (P5-HTTP): `header` is the
        DIYBMS case (a freely named header key, typically `ApiKey`), `bearer` the
        usual `Authorization: Bearer …`, `basic` classic HTTP basic. `none` is the
        default, because most BMS in a home network demand nothing.


        ⚠ `secret` is the one value that never comes BACK: listings carry the
        fixed mask instead, and a save that sends the mask - or nothing - KEEPS
        the stored value; only a genuinely re-entered key replaces it. It is
        stored as the top-level `auth_secret` of the connection (so the secret
        heuristic masks it without needing a template) and reaches the box in the
        registry push. It is deliberately NOT part of the generated flow
        document, which the flow API hands to every portal user of the tenant.


        Creating with an auth mode but no secret is refused BY NAME: the box
        would not even ask, and the battery would silently read nothing.
      required: [mode]
      properties:
        mode:
          type: string
          enum: [none, header, bearer, basic]
          default: none
        header:
          type: string
          maxLength: 64
          example: ApiKey
          description: The header name - only with `header`, and there mandatory. An RFC-7230 token.
        username:
          type: string
          maxLength: 64
          description: Only with `basic`, and there mandatory. Not a secret - it travels in the flow too.
        secret:
          type: string
          maxLength: 512
          description: Absent, empty or the mask means "unchanged". Never returned.
    UserDefinedBatteryBinding:
      type: object
      description: >
        The SPEISER-BINDUNG (P6, captain decision E6 (a) of 09.09.2026): what
        this battery IS in the plant. Absent = `unbound`, and that default is
        the point - a binding NEVER arises by itself, because the channel name
        `soc_pct` does not say WHOSE state of charge it is.


        `feeds_inverter` makes it the SoC / limit / permission source of the
        hybrid inverter's storage node while the battery POWER stays with the
        inverter, where it is measured; `standalone` makes it the storage node
        itself, power included. Only channels the battery actually delivers are
        fed (a derived soc_pct from P5b counts); a binding that could feed
        nothing is refused rather than promising an effect that never arrives.


        Stored in `connection_json.binding` and materialised as
        `entity_role_assignment` rows (role `storage`, primary), so the read
        model and the registry push carry it without a second mechanism - see
        `docs/contracts/v2/topology-read-model.md`.
      required: [mode]
      properties:
        mode:
          type: string
          enum: [unbound, feeds_inverter, standalone]
          default: unbound
        inverterEntityId:
          type: string
          format: uuid
          description: >
            The hybrid inverter whose storage node this battery feeds. MANDATORY
            for `feeds_inverter` ("hangs on some inverter" is not an explicit
            binding), ignored otherwise. Must be an entity of this site in the
            `storage` category and not the battery itself - 404 when it does not
            belong to the site, 422 when it is not a storage inverter.
    UserDefinedBatteryBroker:
      type: object
      description: >
        The LOCAL MQTT broker. Same LAN-only rule as the Modbus Baukasten - only
        a provably private target is accepted. Credentials are deliberately NOT
        part of this contract: a password inside a retained, rolled-out flow
        document would be a cleartext secret; the secret path ships with the
        HTTP read type.
      required: [host]
      properties:
        host: { type: string, maxLength: 253 }
        port: { type: integer, minimum: 1, maximum: 65535, default: 1883 }
    UserDefinedBatteryMapping:
      type: object
      description: >
        ONE source field onto ONE standard battery channel. A mapping may span
        MANY topics via a filter and fold them with an aggregate - that is what
        turns 11x16 DIYBMS cell topics (`emon/diybms/+/+`, `.voltage`) into
        `cell_min_mv`/`cell_max_mv`. Scale/offset are applied PER SAMPLE, then
        the aggregate runs, so "min over all cells in mV" means exactly that.
      required: [channel, topic]
      properties:
        channel:
          type: string
          maxLength: 64
          description: >
            A standard battery channel - `soc_pct`, `voltage_v`, `current_a`,
            `power_kw`, `cell_min_mv`, `cell_max_mv`, `temp_max_c`,
            `charge_allowed`, `discharge_allowed`, `charge_limit_a`,
            `discharge_limit_a`. Anything else is REFUSED, never guessed. Each
            channel has exactly one source. `soc_source_code` is deliberately
            NOT mappable: it is DERIVED in level 2 and no BMS publishes it -
            mapping it would pass a computation off as a measurement.
        topic:
          type: string
          maxLength: 200
          description: "MQTT topic filter; `+` is exactly one level, `#` only the last."
        path:
          type: string
          maxLength: 200
          description: >
            Dot-separated value path inside the JSON payload (`voltage`,
            `bms.soc`). Empty means the payload ITSELF is the value.
        aggregate:
          type: string
          enum: [last, min, max, sum, avg, count]
          default: last
          description: >
            Folded over every FRESH sample of every matching topic. A `bool`
            mapping accepts only `last`, `min` (the conservative AND - every
            source allows it) and `max` (OR): the SUM of permissions is not a
            permission, and an average of 0.5 would be a number no device ever
            reported.
        valueType:
          type: string
          enum: [number, bool]
          default: number
          description: >
            `bool` travels as 0/1 (telemetry channels are numbers) and knows no
            scaling - 0/1 IS the statement.
        scale: { type: number, default: 1, description: "Must not be 0." }
        offset: { type: number, default: 0 }
        sentinel:
          type: number
          description: >
            A RAW value that means "not measured" (a sensor reporting -40). Such
            a sample is absent, never a reading.
        staleS:
          type: integer
          minimum: 5
          maximum: 86400
          default: 300
          description: >
            How long ONE sample counts. A channel with no fresh sample is ABSENT
            from the telemetry - never 0.
        trueValues:
          type: array
          maxItems: 16
          items: { type: string, maxLength: 32 }
          description: "Extra words meaning true; a word outside the vocabulary is DISCARDED."
        falseValues:
          type: array
          maxItems: 16
          items: { type: string, maxLength: 32 }
    UserDefinedBatterySocDerivation:
      type: object
      description: >
        How the state of charge comes about (level 2, `vp-deye-diybms-luecke-l5`
        §3.2b, package P5b). All three methods exist. `direct` takes over a
        measured `soc_pct`; `ocv_curve` interpolates two OCV->SoC curves and
        takes the CONSERVATIVE minimum of the highest and the lowest cell;
        `coulomb` counts SoC(t) = SoC(t-1) + P*dt/E*100 from an anchor.

        THE ONE RULE UNDER ALL OF THEM: no state of charge without an INPUT.
        `direct` needs a mapped `soc_pct`; `ocv_curve` needs a cell voltage (or
        pack voltage plus cells in series) AND a charge curve; `coulomb` needs
        power (or current plus voltage), a usable capacity and an anchor - a
        count that starts from a guessed value is a claim with decimal places.
        Omitted on create: `direct` when `soc_pct` is mapped, otherwise no
        derivation at all, which is a legitimate state (the optimizer then
        plans no battery, P7).

        The derived value is published as ORDINARY telemetry - `soc_pct`
        together with `soc_source_code` (1 = measured, 2 = computed from the
        curve, 3 = counted from charge) - so cloud, optimizer and portal read it
        like any other state of charge AND the history keeps the source of the
        time. There is deliberately no code 0: an unknown state of charge is an
        ABSENT channel, never a reported zero.

        With `direct` the SAME channel carries the value twice in one second -
        once as the raw level-1 reading and once from the derivation that TAKES
        IT OVER. The second one therefore travels VERBATIM, neither rounded nor
        clamped: two different numbers for one state of charge in one second
        would be a truth that hangs on delivery order. Only what the derivation
        computed ITSELF is rounded and clamped.
      properties:
        method: { type: string, enum: [direct, ocv_curve, coulomb] }
        preferDirect:
          type: boolean
          default: true
          description: >
            The selection logic of §3.2b: a FRESH measured state of charge beats
            the computation, whatever method is configured. Only a comparison
            run turns this off.
        template:
          type: string
          maxLength: 64
          description: >
            A shipped curve TEMPLATE that fills `curveCharge`/`curveDischarge`
            (and cells in series / reference temperature) when they are absent -
            own points always win. Today: `diybms-176s-nmc`, the measured tables
            of a 176s NMC pack. An unknown name is refused, never ignored.
            ⚠ A curve belongs to a CHEMISTRY: the same voltage means a
            completely different state of charge on a LiFePO4 cell.
        holdS:
          type: integer
          minimum: 60
          maximum: 86400
          default: 900
          description: >
            How long a FROZEN value is still held. A frozen value is never
            republished with a fresh timestamp - it simply ages; after this
            window it is absent, and a charge count needs a new anchor (what a
            battery did while unobserved is unknown).
        inputs: { $ref: "#/components/schemas/UserDefinedBatterySocInputs" }
        params: { $ref: "#/components/schemas/UserDefinedBatterySocParams" }
    UserDefinedBatterySocInputs:
      type: object
      description: >
        Role -> channel. Every field is optional; an absent role uses the
        like-named standard channel. A channel outside the type catalog is
        REFUSED, never guessed.
      properties:
        soc: { type: string, maxLength: 64, default: soc_pct }
        cellMin: { type: string, maxLength: 64, default: cell_min_mv }
        cellMax: { type: string, maxLength: 64, default: cell_max_mv }
        voltage: { type: string, maxLength: 64, default: voltage_v }
        current: { type: string, maxLength: 64, default: current_a }
        power: { type: string, maxLength: 64, default: power_kw }
    UserDefinedBatterySocParams:
      type: object
      description: >
        The computing values. A curve is a list of [cell voltage in V, state of
        charge in %] pairs; outside its ends the result is CLAMPED, never
        extrapolated - an extrapolation beyond a measured curve would be an
        invented chemistry.
      properties:
        curveCharge:
          type: array
          minItems: 2
          maxItems: 64
          description: >
            Applied to the HIGHEST cell. A curve must RISE with voltage - one
            that falls describes no lithium cell and is refused.
          items:
            type: array
            minItems: 2
            maxItems: 2
            items: { type: number }
        curveDischarge:
          type: array
          minItems: 2
          maxItems: 64
          description: >
            Applied to the LOWEST cell. Absent, the charge curve serves both
            ends - an invented second curve would be worse than one honestly
            named single curve.
          items:
            type: array
            minItems: 2
            maxItems: 2
            items: { type: number }
        cellsInSeries:
          type: integer
          minimum: 1
          maximum: 1024
          description: >
            Only for the SIMPLIFIED variant (pack voltage / n). It cannot see
            the cell spread and is therefore never the first choice.
        conservativeMin:
          type: boolean
          default: true
          description: >
            The HA method: `min(f(vmax, charge), f(vmin, discharge))` - the pack
            can only give what its weakest cell holds. `false` is the explicitly
            chosen optimistic reading (the maximum); an AVERAGE is not offered
            because it would be an invented cell.
        roundPct: { type: number, default: 0.1, maximum: 5 }
        capacityKwh:
          type: number
          maximum: 10000
          description: "Usable capacity - required for `coulomb`."
        efficiencyPct:
          type: number
          maximum: 100
          description: >
            Damps the CHARGE side only. Absent means the count runs lossless,
            and the drift is the known price of the method.
        nominalVoltageV:
          type: number
          maximum: 1500
          description: "Used only when current is mapped but no voltage is."
        refTempC: { type: number, minimum: -40, maximum: 100 }
        anchor:
          type: object
          description: >
            The starting point of a charge count. A mapped measured `soc_pct` is
            the best anchor there is and counts as one.
          properties:
            socPct: { type: number, minimum: 0, maximum: 100 }
            at: { type: string, maxLength: 40 }
        recalibrate:
          type: object
          description: >
            Straightens the count at a voltage ENDPOINT - a measurement beats an
            integration. Each endpoint needs BOTH its voltage and its state of
            charge; half an endpoint would have left the box guessing.
          properties:
            fullCellMv: { type: number }
            fullSocPct: { type: number, minimum: 0, maximum: 100 }
            emptyCellMv: { type: number }
            emptySocPct: { type: number, minimum: 0, maximum: 100 }
    SwitchDefinition:
      type: object
      description: >
        ONE write register per device, with a FIXED kind. `on_off` may only ever
        write the two constants fixed here; `setpoint` only values inside the
        min/max clamp, plus a `safeValue` for silence and revocation. There is
        no value outside the released one, and no address at runtime.
      required: [kind, registerKind, address]
      properties:
        kind: { type: string, enum: [on_off, setpoint] }
        registerKind: { type: string, enum: [coil, holding] }
        address: { type: integer, minimum: 0, maximum: 65535, description: "0-based." }
        writeFc:
          type: integer
          enum: [5, 6, 16]
          description: >
            Derived when absent - 5 for a coil, otherwise 16. FC16 is the
            register default, NOT FC6: several real devices accept a
            single-register write and never adopt it (the Fronius/Deye lesson).
        onValue: { type: integer, minimum: 0, maximum: 65535 }
        offValue: { type: integer, minimum: 0, maximum: 65535 }
        minValue: { type: number }
        maxValue: { type: number }
        safeValue:
          type: number
          description: Written on withdrawal, staleness or revocation.
        scale: { type: number }
        offset: { type: number }
        unit: { type: string, maxLength: 16 }
        readbackAddress:
          type: integer
          minimum: 0
          maximum: 65535
          description: Optional - when present the write is read back and compared.
        watchdogAddress:
          type: integer
          minimum: 0
          maximum: 65535
          description: >
            Optional. A generic Modbus device has NO built-in deadman; if yours
            offers a watchdog register, the executor serves it on every tick.
            Empty by default - never coerced.
        watchdogValue: { type: integer, minimum: 0, maximum: 65535 }
    SwitchConsumer:
      type: object
      description: >
        The consumer facts the clamp and the cycle guard are built from - they
        bind BEFORE the executor. Only the rated power is required: a missing
        cycle axis is inactive, never an invented protection.
      required: [ratedPowerKw]
      properties:
        ratedPowerKw: { type: number, exclusiveMinimum: 0 }
        minOnSeconds: { type: integer, minimum: 0 }
        minOffSeconds: { type: integer, minimum: 0 }
        maxStartsPerDay: { type: integer, minimum: 1 }
        powerChannel: { type: string }
    SwitchTestRequest:
      type: object
      required: [switchDef]
      properties:
        switchDef: { $ref: "#/components/schemas/SwitchDefinition" }
        testValue:
          type: number
          description: >
            Required for `setpoint` and it MUST lie inside the clamp - it is
            never silently clamped, or the customer would confirm a different
            test than the one they ran. Ignored for `on_off`.
    SwitchTestResult:
      type: object
      required: [passed, ttlSeconds]
      properties:
        passed: { type: boolean }
        ttlSeconds:
          type: integer
          description: After this the device falls back BY ITSELF - the box armed it first.
        errorCode:
          type: string
          description: >
            A named class, never invented - `timeout` is an honest outcome, not
            a failure.
        message: { type: string }
        switched:
          type: object
          description: >
            Present only on a real write. A failed operation carries NO value.
          properties:
            written: { type: integer }
            offAfterS: { type: integer }
            readback: { type: integer }
            readbackMatches: { type: boolean }
    SwitchReleaseRequest:
      type: object
      required: [switchDef, consumer, physicallyConfirmed]
      properties:
        switchDef: { $ref: "#/components/schemas/SwitchDefinition" }
        consumer: { $ref: "#/components/schemas/SwitchConsumer" }
        physicallyConfirmed:
          type: boolean
          description: The customer saw the right device react. Anything but true refuses.
    SelfBuildReadRequest:
      type: object
      required: [connection, channel]
      properties:
        deviceId:
          type: string
          format: uuid
          description: >
            Optional - the box to ask. Absent means "the plant's single device";
            a plant with several must name one.
        connection: { $ref: "#/components/schemas/SelfBuildConnection" }
        channel: { $ref: "#/components/schemas/SelfBuildChannel" }
    SelfBuildReadResult:
      type: object
      description: >
        RAW and SCALED side by side, always - that pair is what makes a scaling
        mistake visible at a glance. Every number is OPTIONAL: a failed read
        carries NO value, never a 0 that reads like a measurement.
      required: [ok, receipt]
      properties:
        ok: { type: boolean }
        raw: { type: number, description: "The value as the register delivered it." }
        registers:
          type: array
          items: { type: integer }
          description: "The raw 16-bit words - a wrong word order becomes visible, not merely wrong."
        value: { type: number, description: "raw x scale + offset." }
        unit: { type: string }
        hint:
          type: string
          description: >
            A plausibility HINT, never a block - a register we consider
            implausible can be correct on the real device.
        errorCode:
          type: string
          enum: [unreachable, no_answer, invalid_response, implausible, invalid_request, not_supported, rate_limited, timeout]
        message: { type: string }
        receipt:
          type: boolean
          description: Whether this run unlocks saving (the connection-test duty).
    SiteComponentTemplate:
      type: object
      description: >
        A PRIVATE device template of one plant ("Duplizieren"). Deliberately not
        ComponentTemplate - that one describes a PRODUCT (global, builtin or
        certified); this one is the copy of ONE customer definition, and its
        channels are mandatory because the channel list IS the template.
      properties:
        templateRef: { type: string }
        version: { type: integer }
        label: { type: string }
        communication: { type: string }
        connection: { type: object, additionalProperties: true }
        channels: { type: array, items: { $ref: "#/components/schemas/SelfBuildChannel" } }
        note: { type: string }
        createdAt: { type: string, format: date-time }
    SiteComponentTemplateRequest:
      type: object
      description: >
        Name and note of a PRIVATE template (Einheitsmodell Stufe 6). Both
        optional on duplicate (the derived "... (Vorlage)" fills in); the rename
        route requires a non-blank name.
      properties:
        label: { type: string, maxLength: 200 }
        note: { type: string, maxLength: 200 }
    SaveComponentTemplateRequest:
      type: object
      description: >
        A checked template VERSION (Einheitsmodell Stufe 6). There is
        deliberately NO templateRef field - the key is derived from brand +
        model and is opaque by contract.
      required: [brand, brandLabel, model, modelLabel, communication, transportSchema, certificationStatus]
      properties:
        brand: { type: string, maxLength: 64, pattern: "^[a-z][a-z0-9_]{0,63}$" }
        brandLabel: { type: string, maxLength: 120 }
        model: { type: string, maxLength: 64, pattern: "^[a-z][a-z0-9_]{0,63}$" }
        modelLabel: { type: string, maxLength: 120 }
        family: { type: string, maxLength: 64, description: "Decode-profile reference; absent for a template without one." }
        familyLabel: { type: string, maxLength: 120 }
        communication:
          type: string
          enum: [modbus_tcp, solarman_v5, fronius_solar_api, fronius_sunspec, goe_http_api, shelly_http]
        communicationLabel: { type: string, maxLength: 160 }
        transportSchema:
          type: array
          minItems: 1
          description: "The connection form fields {key,label,type,required,default,help,options}."
          items: { type: object, additionalProperties: true }
        channels:
          type: array
          description: >
            ABSENT = "this template does not explain it here". An EMPTY array is
            REFUSED - it would claim "delivers no measurements".
          items: { type: object, additionalProperties: true }
        writes:
          type: array
          description: >
            ABSENT = not explained here; an EMPTY array is REFUSED. Every entry
            needs its `safe_value` (what is written on silence or a revocation)
            and, per kind, the constants (`on_off`) or the clamp (`setpoint`).
            A template with writes must be `certified`.
          items: { type: object, additionalProperties: true }
        ratedKw: { type: number, minimum: 0, exclusiveMinimum: true, description: "Absent = unknown. Never 0." }
        controlTier: { type: integer, minimum: 0, maximum: 3 }
        certificationStatus:
          type: string
          enum: [certified, in_certification, not_certified]
          description: >
            `builtin` is deliberately NOT settable - that word says "ships with
            the edge software" and a hand-entered template never will.
        certificationNote: { type: string, maxLength: 500 }
        note: { type: string, maxLength: 500 }
    AdminComponentTemplate:
      allOf:
        - $ref: "#/components/schemas/ComponentTemplate"
        - type: object
          description: >
            The operator view of ONE template version. `withdrawnAt == null`
            means "selectable"; a withdrawn version is NOT deleted - the
            components using it run on unchanged.
          properties:
            createdAt: { type: string, format: date-time }
            createdBy: { type: string }
            withdrawnAt: { type: string, format: date-time, nullable: true }
            withdrawnBy: { type: string, nullable: true }
            usedByComponents:
              type: integer
              description: How many components of the whole fleet stand on this KEY.
    AdminComponentFleet:
      type: object
      required: [sites]
      properties:
        sites:
          type: array
          items:
            type: object
            required: [siteId, siteName, tenantId, tenantName, componentAuthority, componentCount, syncStatus]
            properties:
              siteId: { type: string, format: uuid }
              siteName: { type: string }
              tenantId: { type: string, format: uuid }
              tenantName: { type: string }
              componentAuthority: { type: string, enum: [box, portal] }
              componentsAdoptedAt:
                type: string
                format: date-time
                description: The RECEIPT of an automatic adoption - absent is not the same as box-managed.
              componentsAdoptedBy: { type: string }
              componentCount: { type: integer }
              sources:
                type: object
                description: "Origin of the connections. `unknown` = the row predates templates - no invented origin."
                properties:
                  builtin: { type: integer }
                  certified: { type: integer }
                  custom: { type: integer }
                  composed: { type: integer }
                  unknown: { type: integer }
              privateTemplates: { type: integer }
              syncStatus: { type: string, enum: [in_sync, pending, held, box_managed, unreported] }
              refusedRevision: { type: string }
              refusedReason: { type: string }
              appliedAt: { type: string, format: date-time }
              write:
                type: object
                description: The three release levels of the concept in ONE place.
                properties:
                  controlPoints: { type: integer }
                  platformActivated: { type: integer }
                  templateWrites: { type: integer }
                  certSource:
                    type: string
                    enum: [env, device, platform]
                    description: What the DEVICE reports. Absent = unknown, NEVER "not certified".
                  platformCertVerdict: { type: string }
                  platformCertModel: { type: string }
    SiteComponents:
      type: object
      description: >
        The plant's components plus WHO owns their configuration
        (Einheitsmodell Stufe 1).
      required: [componentAuthority, components]
      properties:
        componentAuthority:
          type: string
          enum: [box, portal]
          description: >
            `portal` = the stored Soll IS the plant's read path and the assistant
            is offered; `box` = the device's own `:8484` page still owns it
            (every plant that existed before this stage). The server always names
            an owner - it is never absent.
        sollRevision:
          type: string
          nullable: true
          description: The last composed registry-push revision. `null` = never pushed.
        appliedRevision:
          type: string
          nullable: true
          description: >
            The revision the BOX reported it really applied. `null` = the box has
            not said anything yet - UNKNOWN, never "did not arrive".
        appliedAt: { type: string, format: date-time, nullable: true }
        refusedRevision:
          type: string
          nullable: true
          description: >
            The box's LAST refusal, NEXT TO (never instead of) `appliedRevision` -
            what runs is still the last configuration that really worked.
        refusedReason: { type: string, nullable: true, description: German plain-text reason. }
        adoptedAt:
          type: string
          format: date-time
          nullable: true
          description: >
            Wann diese Anlage AUTOMATISCH vom Gerät übernommen wurde
            (Einheitsmodell Stufe 2). null = nie übernommen - ausdrücklich NICHT
            dasselbe wie box-verwaltet: eine neu angelegte Anlage ist
            portal-verwaltet, ohne je übernommen worden zu sein. Der Zustand
            steht allein in componentAuthority; dies ist der Beleg.
        components:
          type: array
          items:
            type: object
            required: [id, role, definitionVersion, syncStatus]
            properties:
              id: { type: string, format: uuid, description: The measurement point / v2 entity id. }
              role: { type: string, enum: [inverter, pv-generation, grid-meter, consumer] }
              entityType: { type: string, nullable: true }
              label: { type: string, nullable: true }
              brand: { type: string, nullable: true }
              model: { type: string, nullable: true }
              family: { type: string, nullable: true }
              communication: { type: string, nullable: true }
              connection:
                type: object
                nullable: true
                additionalProperties: true
                description: The template's transport_schema values, passed through verbatim.
              sourceKind: { type: string, nullable: true, enum: [builtin, certified, custom, composed] }
              templateRef: { type: string, nullable: true }
              templateVersion: { type: integer, nullable: true }
              definitionVersion: { type: integer, description: Bumped on every saved change; the rollback target. }
              capacityKwp: { type: number, nullable: true }
              edgeSourceId: { type: string, nullable: true }
              syncStatus:
                type: string
                enum: [in_sync, pending, held, box_managed, unreported, no_gateway_device]
                description: >
                  Derived from Soll vs. the box's reported Ist, never guessed.
                  `unreported` means the box has not reported - NEVER "the change
                  was lost". `held` is the box's deliberate hold (it saw the
                  revision and applied nothing). `box_managed` is its reported
                  RETURN of authority: it maintains its devices itself again, so
                  there is no Soll/Ist relation left at all - without it the row
                  claimed "change on its way to the box" forever after a
                  revert-to-device, because every push bumps the Soll revision
                  while the stale Ist row stood still. `no_gateway_device` is the
                  one case where the reason IS known: the site has several
                  claimed devices and none of them is set as the battery's
                  controlling device, so there is no receiver for the push at all
                  (it replaces `unreported` and `pending`, never `in_sync`). The
                  cross-tenant fleet view knows every value except
                  `no_gateway_device` - it does not know about receivers and must
                  not invent a reason.
    SaveComponentRequest:
      type: object
      required: [templateRef, role]
      properties:
        templateRef:
          type: string
          maxLength: 200
          description: >
            The OPAQUE template key. Brand, model, family and the communication
            method are taken FROM the template, never from the body - a client
            cannot save an inconsistent connection.
        templateVersion:
          type: integer
          minimum: 1
          nullable: true
          description: Exact template definition to preserve or apply; omitted only by legacy clients.
        role: { type: string, enum: [inverter, pv-generation, grid-meter, consumer] }
        label: { type: string, nullable: true, maxLength: 200 }
        connection:
          type: object
          additionalProperties: true
          description: The template's transport_schema fields (ip/port/serial/unit_id/...).
        capacityKwp: { type: number, nullable: true, description: Producer nameplate (kWp); ignored for other roles. }
        intervalS: { type: integer, nullable: true, description: Optional read interval; absent = the box default. }
        note: { type: string, nullable: true, maxLength: 200 }
        expectedRevision:
          type: integer
          minimum: 1
          nullable: true
          description: Required for PUT; the definitionVersion read by the editor.
        effectiveAt:
          type: string
          format: date-time
          nullable: true
          description: Effective marker; PUT currently activates atomically now.
    ComponentDefinition:
      type: object
      description: >
        ONE stored version of a component's connection (append-only). `null`
        everywhere means "this version says nothing about it", never a
        substitute value.
      required: [entityId, version, role, createdAt]
      properties:
        entityId: { type: string, format: uuid }
        version: { type: integer }
        role: { type: string }
        label: { type: string, nullable: true }
        brand: { type: string, nullable: true }
        model: { type: string, nullable: true }
        family: { type: string, nullable: true }
        communication: { type: string, nullable: true }
        connection: { type: object, nullable: true, additionalProperties: true }
        sourceKind: { type: string, nullable: true }
        templateRef: { type: string, nullable: true }
        templateVersion: { type: integer, nullable: true }
        createdAt: { type: string, format: date-time }
        createdBy: { type: string, nullable: true, description: The JWT subject that saved it. }
        note: { type: string, nullable: true }
    ComponentChangeEvent:
      type: object
      required: [revision, eventType, effectiveAt, createdAt]
      properties:
        revision: { type: integer, minimum: 1 }
        eventType: { type: string, enum: [edited, family_changed, rolled_back, device_replaced] }
        effectiveAt: { type: string, format: date-time }
        fromValue: { type: string, nullable: true }
        toValue: { type: string, nullable: true }
        createdBy: { type: string, nullable: true }
        note: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
    ComponentTemplate:
      type: object
      description: >
        ONE component template (Einheitsmodell Stufe 0a). Built-in templates are
        the cloud-side DATA rendering of the edge Go catalog
        (edge-app/core/internal/inverter DefaultCatalog()); the Go catalog stays
        the truth about what a box can actually read.
      properties:
        templateRef:
          type: string
          example: "builtin:deye:sun-30k-sg01hp3"
          description: Stable, OPAQUE key - never parse it.
        kind: { type: string, enum: [builtin, certified], description: Where the template comes from. }
        version:
          type: integer
          example: 1
          description: >
            Definition version. Built-in templates stay at 1 - their version IS
            the edge software release, not a separate count.
        brand: { type: string, example: deye }
        brandLabel: { type: string, example: Deye }
        model: { type: string, example: sun-30k-sg01hp3 }
        modelLabel: { type: string, example: SUN-30K-SG01HP3-EU }
        modelAliases:
          type: array
          nullable: true
          items: { type: string }
          example: ["SUN-30K-SG01HP3-EU-BM3", "SUN-30K-SG01HP3-EU-BM4"]
          description: >
            Further NAMEPLATE names of the SAME model - the variant labels a
            customer reads off the device (Deye ships this HV hybrid line as
            "...-EU-BM3" / "...-EU-BM4" while the catalog calls it
            "SUN-30K-SG01HP3-EU").

            An alias is ONLY a name: it carries no own key, no own template and
            no behaviour. It is searched and may be displayed; what gets picked
            is always the model itself - a second catalog entry would be an
            UNCERTIFIED model, because the control certification is keyed on
            brand+model.

            null/absent = this model has only its one name - never "it has no
            variants".
        deviceType:
          type: string
          nullable: true
          enum: [inverter, wallbox, switch, meter, charge_point, custom]
          example: inverter
          description: >
            The device-type dimension of the catalog - what KIND of device the
            template describes. It replaced the technical parentheses in brand
            names ("go-e (Wallbox)"). null = the template does not say; never a
            guessed type.
        supersededBy:
          type: string
          nullable: true
          example: "builtin:fronius:fronius-eco-27-3-s"
          description: >
            The templateRef that replaced THIS one. null = the template applies.
            A superseded template stays RESOLVABLE (an existing plant references
            its key, and the takeover looks it up by brand+model) but is no
            longer OFFERED - the customer list omits it, a lookup still finds it.
        family:
          type: string
          nullable: true
          example: hybrid_3p
          description: The decode-profile reference - the bridge to the code that executes it.
        familyLabel: { type: string, nullable: true }
        communication: { type: string, example: solarman_v5 }
        communicationLabel: { type: string, nullable: true }
        transportSchema:
          type: array
          description: >
            The connection form of this communication - the generic field
            vocabulary {key,label,type,required,default,help,options}.
          items: { type: object }
        channels:
          type: array
          nullable: true
          description: >
            Channel definitions (register -> plain-text measurement). NULL means
            "this template does not explain its channels HERE", never "it has
            none" - a built-in template's channels live in the edge decode
            profile, so it always carries null. `[]` would be the flat lie that a
            Deye delivers no measurements.
          items: { type: object }
        writes:
          type: array
          nullable: true
          description: >
            Write definitions (SCHEMA ONLY in this stage - nothing consumes
            them): register + constants (on_off) or the min/max clamp + safe
            value (setpoint). NULL by the same rule as `channels` - a built-in
            template's write path is the control adapter, released by the
            platform register inverter_control_certification.
          items: { type: object }
        ratedKw:
          type: number
          nullable: true
          example: 30
          description: Nameplate kW. NULL = unknown in the catalog, never 0.
        controlTier:
          type: integer
          minimum: 0
          maximum: 3
          description: >
            The brand's declared control PRIMITIVE. It AUTHORIZES NOTHING -
            kill-switch, certification register and per-plant arming decide
            whether a write ever goes out.
        certificationStatus:
          type: string
          enum: [builtin, certified, in_certification, not_certified]
          description: >
            What the platform vouches for - a DIFFERENT question from `kind`.
            `builtin` = ships with the edge software (deliberately not
            "certified", which belongs to the bench).
        certifiedAt: { type: string, format: date-time, nullable: true }
        certificationNote: { type: string, nullable: true }
        note: { type: string, nullable: true, description: Display text for the assistant. }
        updatedAt: { type: string, format: date-time, nullable: true }
    Consumer:
      type: object
      description: A controllable consumer's master data + control-activation state.
      properties:
        id: { type: string, format: uuid, description: The consumer entity id. }
        type: { type: string, example: wallbox, description: entity_type (catalog consumer type). }
        typeLabel: { type: string, example: Wallbox }
        name: { type: string, example: Wallbox Garage }
        controlKind: { type: string, enum: [on_off, stepped, continuous] }
        ratedPowerKw: { type: number, example: 11.0 }
        minPowerKw: { type: number, nullable: true }
        levelsKw: { type: array, nullable: true, items: { type: number } }
        resolutionKw: { type: number, nullable: true }
        powerRangesKw:
          type: array
          nullable: true
          description: D4 non-convex [min,max] ranges (continuous).
          items: { type: array, items: { type: number } }
        storageRelation: { type: string, enum: [consumer_first, storage_first] }
        defaultGridEnergyPolicy: { type: string, enum: [allow, avoid, forbid] }
        allowStorageDischarge: { type: boolean }
        failsafe: { type: string, enum: [off, release] }
        enabled: { type: boolean, description: The pausierbarer Gesamtschalter. }
        version: { type: integer, description: Optimistic-concurrency version of the master data. }
        connection:
          type: string
          enum: [connected, disconnected]
          description: disconnected = "Noch nicht verbunden".
        edgeSourceId: { type: string, nullable: true }
        controlActivation:
          type: string
          enum: [not_activated, active, paused]
          description: >
            Derived from the active policy (Inkrement 4): "active" = an active
            policy exists and the consumer is enabled; "paused" = active policy
            with the pause failsafe in force; else "not_activated" ("Steuerung
            noch nicht aktiviert").
        hasDraftPolicy: { type: boolean }
        draftPolicyVersion: { type: integer, nullable: true }
        minOnSeconds:
          type: integer
          nullable: true
          description: >
            Cycle-guard minimum on-time (Inkrement 3, §13.1) - enforced by the
            EDGE's stateful cycle guard; travels to the device in the entity
            registry push (guards.limits.min_on_seconds). Null = axis inactive.
        minOffSeconds: { type: integer, nullable: true, description: Cycle-guard minimum off-time. }
        maxStartsPerDay: { type: integer, nullable: true, description: Cycle-guard start budget per site-local day. }
        confirmationChannel:
          type: string
          nullable: true
          description: >
            The D3 confirmation channel derived at creation from the bound
            edge source (Bestätigungshierarchie §9.4): "power_kw" = proven
            power measurement (Stufe 2 - a metering Shelly 1PM / go-e),
            "relay_state" = relay readback only (Stufe 3, energy
            "angenommen"), null = unbound draft or created before this field
            existed.
    ProbeRequest:
      type: object
      required: [ops]
      properties:
        deviceId:
          type: string
          format: uuid
          description: >
            Which of the plant's devices to ask. Optional only while the plant
            has exactly one - several devices without a choice is a 409, because
            guessing which box sits on the right LAN segment is not something
            this api will invent.
        ops:
          type: array
          minItems: 1
          maxItems: 8
          items: { $ref: "#/components/schemas/ProbeOp" }
      example:
        ops:
          - id: soc
            host: 192.168.0.28
            registerKind: holding
            address: 588
            dataType: u16
    ProbeOp:
      type: object
      required: [id, host, registerKind, address, dataType]
      properties:
        id:
          type: string
          pattern: "^[a-z0-9][a-z0-9_-]{0,31}$"
          description: >
            Maps the answer back onto this question, so it must be unique within
            the request (the box refuses a duplicate BY NAME).
        host:
          type: string
          maxLength: 253
          description: >
            The device's address in the customer's own network. The BOX enforces
            that it is private (RFC1918/loopback/link-local/ULA/.local) and
            refuses a public address with a plain-German reason - it does not
            take the cloud's word for where it may knock.
        port: { type: integer, minimum: 1, maximum: 65535, default: 502 }
        unitId: { type: integer, minimum: 0, maximum: 255, default: 1 }
        registerKind:
          type: string
          enum: [holding, input]
          description: holding = Modbus FC3, input = FC4.
        address:
          type: integer
          minimum: 0
          maximum: 65535
          description: >
            0-BASED. The 4xxxx notation of many manuals means 4-0001 + address;
            that conversion belongs to the surface, not to this contract.
        dataType:
          type: string
          enum: [u16, s16, u32, s32, float32]
          description: >
            Also decides HOW MANY register words are read (1 or 2) - there is
            deliberately no free quantity field, because a wrong word count
            returns half a number that looks perfectly plausible.
        wordOrder: { type: string, enum: [big, little], default: big }
        scale:
          type: number
          description: >
            DISPLAY only - value = raw * scale + offset. It never changes what is
            read; the answer carries raw and value side by side.
        offset: { type: number }
    ProbeResult:
      type: object
      required: [requestId, results]
      properties:
        requestId: { type: string }
        errorCode:
          type: string
          enum: [invalid_request, unreachable, no_answer, invalid_response, implausible, timeout, not_supported, rate_limited]
          description: >
            Set only when the WHOLE probe was refused (the plant did not answer
            in time, or its own rate limit bit); `results` is then empty and the
            reason is stated once.
        message: { type: string, description: The plain-German sentence. }
        results:
          type: array
          items: { $ref: "#/components/schemas/ProbeOpResult" }
    ProbeOpResult:
      type: object
      required: [id, ok]
      properties:
        id: { type: string }
        ok: { type: boolean }
        raw:
          type: number
          description: >
            The RAW register value before scaling - the number that makes a
            scaling mistake visible. Absent when the step failed.
        registers:
          type: array
          maxItems: 2
          items: { type: integer }
          description: The 16-bit words, so a wrong word order is visible and not merely wrong.
        value:
          type: number
          description: raw * scale + offset. Absent when the step failed - a failure never carries a value.
        errorCode:
          type: string
          enum: [invalid_request, unreachable, no_answer, invalid_response, implausible, timeout, not_supported, rate_limited]
        message: { type: string }
        samples:
          type: array
          maxItems: 16
          items: { $ref: "#/components/schemas/ProbeSample" }
          description: >
            Only for the SELF-CONNECTED battery preview (P5d): one row per field
            mapping - what really arrived during the listening window. ABSENT
            when the box cannot listen yet, which means "this box cannot do it",
            never "nothing arrived".
    ProbeSample:
      type: object
      required: [channel, count]
      description: >
        What ONE field mapping received during the listening window. Its own
        block, never `reading`: `reading` is the closed four-channel snapshot of
        a CATALOG device, while these rows are named by the customer.
      properties:
        channel:
          type: string
          description: The TARGET channel of this mapping (soc_pct, cell_min_mv, ...).
        topic:
          type: string
          description: The concrete topic the value came from. Absent when nothing arrived.
        raw:
          type: number
          description: The RAW value before scale/offset - the number a scaling mistake shows up in. Absent when count is 0.
        value:
          type: number
          description: raw * scale + offset, after the aggregate over all matched topics. Absent when count is 0.
        count:
          type: integer
          description: >
            How many messages hit this mapping in the window. 0 means "nothing
            received" and is an honest statement, not an error - and it travels
            WITHOUT raw/value rather than with a fabricated 0.
        at: { type: string, format: date-time, description: When the last of them arrived. Absent when count is 0. }
    CommandHistory:
      type: object
      description: >
        Der Kommando-Verlauf einer Anlage bzw. Komponente - EINE Antwort fuer
        Kopf, "Gerade jetzt", "Grenzen & Waechter", Tages-Film und Fussnote.
      properties:
        recordingSince:
          type: string
          format: date-time
          nullable: true
          description: >
            Ab wann fuer diese Anlage aufgezeichnet wird. null = noch gar nicht;
            davor wird NICHTS behauptet, auch nichts Entlastendes.
        accuracySeconds:
          type: integer
          description: Der Herzschlag-Takt; ein Wechsel-und-zurueck dazwischen ist unsichtbar.
        from: { type: string, format: date-time }
        to: { type: string, format: date-time }
        entityId: { type: string, format: uuid, nullable: true }
        entityLabel: { type: string, nullable: true }
        deviceRef:
          type: string
          nullable: true
          description: >
            Echo des `device`-Filters; null = die ganze Anlage bzw. eine einzelne
            Komponente. Die Antwort traegt bewusst KEINEN Geraete-NAMEN - den
            bildet das Portal aus seiner einen Namensableitung.
        deviceIsBox:
          type: boolean
          nullable: true
          description: >
            true = die VoltPilot-Box (jede Zeile ihres Geraets), false = ein
            Geraet dahinter (nur die Zeilen seiner Komponenten), null = nicht
            gefiltert. Ein Server-Fakt, damit die Flaeche ihn nicht raten muss.
        writes:
          type: boolean
          description: >
            false = an diese Komponente bzw. dieses Geraet geht kein Befehl, es
            wird nur gelesen.
        truncated:
          type: boolean
          description: Der Deckel hat gegriffen - aeltere Zeilen fehlen.
        total:
          type: integer
          description: >
            Die Zeilen dieses Zeitraums OHNE Strukturfilter. Zusammen mit
            `matched` ist das der Treffer-Zaehler ("14 von 212 Zeilen"): ohne
            beide Zahlen waere ein scharfer Filter von einem leeren Zeitraum
            nicht zu unterscheiden.
        matched:
          type: integer
          description: >
            Die Zeilen dieses Zeitraums MIT den Strukturfiltern. Der FREITEXT
            ist darin nicht enthalten - er laeuft im Portal ueber die gezeigten
            Saetze und schrumpft nur, was hier schon steht.
        nextBefore:
          type: string
          format: date-time
          nullable: true
          description: >
            Der Seiten-Cursor nach hinten (der Beginn der aeltesten gelieferten
            Zeile), oder null, wenn das Fenster vollstaendig gezeigt ist - eine
            Flaeche darf "mehr laden" nie anbieten, wo es nichts mehr gibt.
        entries:
          type: array
          items: { $ref: "#/components/schemas/CommandEntry" }
        control:
          allOf: [{ $ref: "#/components/schemas/ControlStatus" }]
          nullable: true
        curtailment:
          allOf: [{ $ref: "#/components/schemas/CurtailmentStatus" }]
          nullable: true
    CommandEntry:
      type: object
      description: >
        Eine HALTEPERIODE (kind=periode) oder ein Punkt-Ereignis
        (kind=ereignis, dann traegt eventKind seine Art).
      properties:
        id: { type: integer, format: int64 }
        stream:
          type: string
          enum: [batterie, abregelung, verbraucher, waechter, register]
          description: >
            `register` ist der VIERTE Strom: die Einmal-Schreibvorgaenge auf
            Geraete-Register, zur Lesezeit aus dem append-only Journal
            register_write_event eingemischt (KEINE Doppel-Speicherung). Ein
            aelteres Portal kennt das Wort nicht und laesst die Zeile wortlos
            aus - die Unbekannt-bleibt-ohne-Behauptung-Regel.
        kind: { type: string, enum: [periode, ereignis] }
        eventKind:
          type: string
          nullable: true
          enum: [notaus_ein, notaus_aus, freigabe_erteilt, freigabe_widerrufen, luecke, verlauf_gedeckelt, register_geschrieben]
        startedAt: { type: string, format: date-time }
        endedAt:
          type: string
          format: date-time
          nullable: true
          description: null = die Periode laeuft noch.
        mode: { type: string, nullable: true }
        path: { type: string, nullable: true }
        whyKind: { type: string, nullable: true, enum: [fahrplan, sicherung] }
        whyRef: { type: string, nullable: true }
        commandedKwFirst: { type: number, nullable: true }
        commandedKwLast: { type: number, nullable: true }
        commandedKwMin: { type: number, nullable: true }
        commandedKwMax: { type: number, nullable: true }
        verdict:
          type: string
          nullable: true
          enum: [bestaetigt, abweichend, keine_antwort, prueft, unbestaetigt]
          description: >
            Das Ruecklese-Urteil. `keine_antwort` ist ausdruecklich NICHT
            `abweichend` - Schweigen ist eine Luecke, kein bewiesener Defekt.
            V1 schreibt nur bestaetigt|abweichend|unbestaetigt.
        cycles: { type: integer, nullable: true, description: In V1 IMMER null. }
        cyclesConfirmed: { type: integer, nullable: true }
        cyclesNoAnswer: { type: integer, nullable: true }
        cyclesMismatch: { type: integer, nullable: true }
        controlEnabled: { type: boolean, nullable: true }
        released: { type: boolean, nullable: true }
        foreignInfluence: { type: boolean, nullable: true }
        entityId: { type: string, format: uuid, nullable: true }
        source:
          type: string
          enum: [cloud_abgeleitet, geraet, portal]
          description: >
            Die Herkunft der Zeile. `portal` gehoert dem Strom `register`: der
            Schreibvorgang wurde ueber das Portal ausgeloest (gegenueber
            `geraet` = am Wartungszugang der Box).
        detail:
          type: object
          nullable: true
          description: Der Roh-Blick (V1 - Rollen, Zaehler, gemeldeter Zustand).
          properties:
            mismatchRoles: { type: string, nullable: true }
            certSource: { type: string, nullable: true }
            units: { type: integer, nullable: true }
            certifiedUnits: { type: integer, nullable: true }
            state: { type: string, nullable: true }
            reasonCode: { type: string, nullable: true }
        register:
          allOf: [{ $ref: "#/components/schemas/RegisterWriteEvent" }]
          nullable: true
          description: >
            NUR auf den Zeilen des Stroms `register` gesetzt: der gefaltete
            Vorgang aus dem Journal (Anforderung + Ergebnis).
        urheber:
          allOf: [{ $ref: "#/components/schemas/ProtokollUrheber" }]
          nullable: true
          description: >
            AP-03 IP-7: wer die Zeile ausgelöst hat, im Akteur-Vokabular - nur an
            Zeilen, die ein Mensch auslöst (Register-Vorgang, „Jetzt voll laden“ /
            „Laden pausieren“). null an abgeleiteten Zeilen und im Bestand.
    RuleEvents:
      type: object
      description: >
        Das Regel-Protokoll einer Anlage - EINE Antwort fuer die drei Flaechen,
        die es zeigen (Zaehler an der Karte, Ausschnitt im Detail-Einschub,
        kompaktes Gesamt-Protokoll).
      properties:
        recordingSince:
          type: string
          format: date-time
          nullable: true
          description: >
            Ab wann fuer diese Anlage aufgezeichnet wird. null = noch gar
            nicht. Ist er juenger als der heutige Berliner Tagesbeginn, sagt
            die Flaeche "seit HH:MM aufgezeichnet" statt einer 0.
        accuracySeconds:
          type: integer
          description: >
            Die Genauigkeit, die AN der Flaeche stehen muss: abgeleitet wird
            aus Wechseln im Herzschlag-Takt, ein Wechsel-und-zurueck dazwischen
            ist unsichtbar.
        countsToday:
          type: boolean
          description: >
            Ob der heutige Tag ueberhaupt zaehlbar ist. Er beantwortet den
            Fall, den `rules` nicht abbilden kann: eine Regel OHNE jedes
            Ereignis taucht dort gar nicht auf - ohne dieses Flag muesste die
            Flaeche den Berliner Tagesbeginn selbst nachrechnen und haette
            einen Zwilling der Server-Regel.
        rules:
          type: array
          items: { $ref: "#/components/schemas/RuleActivity" }
        events:
          type: array
          items: { $ref: "#/components/schemas/RuleEvent" }
    RuleActivity:
      type: object
      description: Die Zaehler EINER Regel.
      properties:
        ruleKind:
          type: string
          enum: [rezept, flow]
          description: >
            Der Schnappschuss der Zuordnung ueber den exklusiven Steuer-Anspruch
            (V-5), nie geraten.
        ruleRef:
          type: string
          description: Komponenten-Id (rezept) bzw. Flow-Id (flow).
        switchedToday:
          type: integer
          nullable: true
          description: >
            STARTS seit Berliner Mitternacht. NULL = nicht belastbar (der
            Speicher zeichnet erst seit heute auf) - nie eine erfundene 0.
        lastSwitchedAt:
          type: string
          format: date-time
          nullable: true
          description: Der juengste Schaltvorgang (Start ODER Stopp).
    RuleEvent:
      type: object
      description: Ein einzelner aufgezeichneter Wechsel.
      properties:
        id: { type: integer, format: int64 }
        ruleKind: { type: string, nullable: true, enum: [rezept, flow] }
        ruleRef:
          type: string
          nullable: true
          description: null = ehrlich keiner Regel zuzuordnen (nie eine geratene).
        entityId: { type: string, format: uuid, nullable: true }
        kind:
          type: string
          enum: [gestartet, gestoppt, zustand, ausgerollt, geraet_problem,
                 protokoll_gedeckelt]
        state: { type: string, nullable: true }
        previousState: { type: string, nullable: true }
        reasonCode: { type: string, nullable: true }
        actualKw:
          type: number
          nullable: true
          description: Der gemessene Wert im Moment des Wechsels; null = nicht gemessen.
        detail: { type: string, nullable: true }
        occurredAt: { type: string, format: date-time }
    ConsumerRuntimeStatus:
      type: object
      description: >
        The edge-reported live state of one consumer entity (heartbeat
        consumers block, table consumer_runtime_status). confirmed is
        TRI-STATE - null means no readback evidence ("Ausführung nicht
        bestätigt" territory); actualKw null = not measured (never a
        fabricated 0).
      properties:
        entityId: { type: string, format: uuid }
        state:
          type: string
          enum: [disconnected, offline, ready, running_forced, running_optimized,
                 waiting, fulfilled, clamped, missed, unknown]
          description: §14.13 vocabulary; the edge claims only what it can know.
        reasonCode:
          type: string
          nullable: true
          description: >
            §15 vocabulary incl. the cycle-guard extension (guard_min_on,
            guard_min_off, guard_max_starts, guard_ramp, guard_rated_power,
            readback_mismatch, device_offline, plan_stale, ...). The portal
            maps it through a pure, tested table - never grep German text.
        actualKw: { type: number, nullable: true }
        confirmed: { type: boolean, nullable: true }
        runtimeSecondsToday: { type: integer, nullable: true }
        startsToday: { type: integer, nullable: true }
        reportedAt: { type: string, format: date-time }
    CreateConsumerRequest:
      type: object
      required: [type, ratedPowerKw]
      properties:
        type: { type: string, example: wallbox }
        name: { type: string }
        minOnSeconds: { type: integer, nullable: true }
        minOffSeconds: { type: integer, nullable: true }
        maxStartsPerDay: { type: integer, nullable: true }
        ratedPowerKw: { type: number, example: 11.0 }
        controlKind: { type: string, enum: [on_off, stepped, continuous] }
        levelsKw: { type: array, items: { type: number } }
        minPowerKw: { type: number }
        resolutionKw: { type: number }
        powerRangesKw: { type: array, items: { type: array, items: { type: number } } }
        storageRelation: { type: string, enum: [consumer_first, storage_first] }
        defaultGridEnergyPolicy: { type: string, enum: [allow, avoid, forbid] }
        allowStorageDischarge: { type: boolean }
        failsafe: { type: string, enum: [off, release] }
        edgeSourceId:
          type: string
          description: An edge-reported source id to bind ("verbunden"); omit for a draft.
    PatchConsumerRequest:
      type: object
      description: All fields optional; send `expectedVersion` for optimistic concurrency.
      properties:
        name: { type: string }
        ratedPowerKw: { type: number }
        controlKind: { type: string, enum: [on_off, stepped, continuous] }
        levelsKw: { type: array, items: { type: number } }
        minPowerKw: { type: number }
        resolutionKw: { type: number }
        powerRangesKw: { type: array, items: { type: array, items: { type: number } } }
        storageRelation: { type: string, enum: [consumer_first, storage_first] }
        defaultGridEnergyPolicy: { type: string, enum: [allow, avoid, forbid] }
        allowStorageDischarge: { type: boolean }
        failsafe: { type: string, enum: [off, release] }
        enabled: { type: boolean }
        expectedVersion: { type: integer }
        minOnSeconds: { type: integer, nullable: true, description: Cycle guard - 0 clears the bound. }
        minOffSeconds: { type: integer, nullable: true, description: Cycle guard - 0 clears the bound. }
        maxStartsPerDay: { type: integer, nullable: true, description: Cycle guard - 0 clears the bound. }
    ConsumerOptions:
      type: object
      description: Capability-/context-filtered inputs for the assistant question tree.
      properties:
        policyActivationEnabled:
          type: boolean
          description: >
            Whether this environment can REALLY activate a consumer rule (both
            Inkrement-4 feature flags on). false = the portal keeps the honest
            "Steuerung noch nicht aktiviert" draft wording.
        types:
          type: array
          items:
            type: object
            properties:
              type: { type: string }
              label: { type: string }
              controlKinds: { type: array, items: { type: string } }
              defaultFailsafe: { type: string }
              releaseAllowed: { type: boolean }
              intents: { type: array, items: { type: string } }
        signals:
          type: array
          items:
            type: object
            properties:
              name: { type: string }
              label: { type: string }
              signalClass: { type: string, enum: [cloud, local] }
              valueType: { type: string, enum: [number, boolean] }
        intents:
          type: array
          items:
            type: object
            properties:
              key: { type: string }
              title: { type: string }
              customerLine: { type: string }
        hasStorage: { type: boolean }
        reportedSources:
          type: array
          items:
            type: object
            properties:
              sourceId: { type: string }
              label: { type: string }
              brand: { type: string }
              role: { type: string }
              measuresPower:
                type: boolean
                description: >
                  The source PROVABLY measures power (its reported reading
                  carries load_kw). false = no proven measurement (a bare
                  relay, or not reported yet) - the wizard then names the
                  honest Stufe-3 consequence (energy "angenommen").
              health: { type: string, enum: [ok, stale, never] }
        defaultStorageRelation: { type: string }
        defaultGridEnergyPolicy: { type: string }
    ConsumerPolicyVersion:
      type: object
      properties:
        entityId: { type: string, format: uuid }
        version: { type: integer }
        lifecycle: { type: string, enum: [draft, active, retired] }
        document: { type: object, description: The consumer-policy document. }
        contentHash: { type: string, example: "sha256:..." }
        createdBy: { type: string, nullable: true }
    ConsumerSchedule:
      type: object
      description: >
        Consumer slots of the newest co-optimizer run (Verbrauchssteuerung
        Inkrement 2, read from site_plan_run + entity_plan_slot). Empty state:
        planId/generatedAt null + entities [] - never fabricated slots.
      properties:
        planId: { type: string, format: uuid, nullable: true }
        generatedAt: { type: string, format: date-time, nullable: true }
        slotMinutes: { type: integer, example: 15 }
        entities:
          type: array
          items:
            type: object
            properties:
              entityId: { type: string, description: mqtt-schedule-2.0 entity id (consumer = measurement_point UUID as string). }
              name: { type: string, nullable: true, description: Customer-facing label of the consumer. }
              slots:
                type: array
                items:
                  type: object
                  properties:
                    time: { type: string, format: date-time }
                    command: { type: string, enum: [on_off, setpoint_kw] }
                    targetValue: { type: number, nullable: true, description: "Planned power in kW (on_off: rated when on, 0 when off)." }
                    reasonCode:
                      type: string
                      nullable: true
                      description: >
                        §15 vocabulary (fixed_window | price_below_threshold |
                        optimizer_selected_low_cost | flex_deadline |
                        guard_grid_limit | no_permitted_energy); null on an off
                        slot. The portal maps it through a pure, tested table -
                        never by scanning German sentences.
                    requirementId: { type: string, nullable: true }
    SimulationRequest:
      type: object
      description: >
        Simulation inputs. On the customer route every field is optional
        (defaults from the site's master data); on the admin/prospect route
        plant (incl. pvKwp + coordinates), consumption.annualKwh and
        battery.capacityKwh are required by the simulation service.
      properties:
        year:
          type: integer
          nullable: true
          description: Reference calendar year; default = the last FULL year.
        zone:
          type: string
          nullable: true
          description: Bidding zone (admin route; customers get the site's). Default DE-LU.
        plant:
          type: object
          nullable: true
          properties:
            pvKwp: { type: number, nullable: true }
            latitude: { type: number, nullable: true }
            longitude: { type: number, nullable: true }
            azimuthDeg: { type: number, nullable: true, description: 180 = Süd (default). }
            tiltDeg: { type: number, nullable: true, description: Default 30. }
        consumption:
          type: object
          nullable: true
          properties:
            annualKwh: { type: number, nullable: true, description: Customer route defaults to 4500. }
            profile:
              type: string
              nullable: true
              description: Load profile id; v1 ships "haushalt" (synthetic typical household).
        tariff:
          type: object
          nullable: true
          properties:
            plantKind: { type: string, nullable: true, enum: [direktvermarktung, eigenverbrauch] }
            tarifArt: { type: string, nullable: true, enum: [dynamisch, fest, ohne] }
            tarifParamCtKwh: { type: number, nullable: true }
            anzulegenderWertCtKwh: { type: number, nullable: true }
            commissionedOn: { type: string, format: date, nullable: true }
            netzladenErlaubt: { type: boolean, nullable: true }
        battery:
          type: object
          nullable: true
          properties:
            capacityKwh: { type: number, nullable: true }
            maxChargeKw: { type: number, nullable: true, description: Default capacity/2. }
            maxDischargeKw: { type: number, nullable: true, description: Default capacity/2. }
            roundtripEfficiencyPct: { type: number, nullable: true, description: Default 92. }
            speicherschonung:
              type: string
              nullable: true
              enum: [aggressiv, ausgewogen, schonend]
              description: Mapped server-side onto the wear ct/kWh (1/4/8).
        sizeSweep:
          type: array
          nullable: true
          items: { type: number }
          description: >
            Battery capacities (kWh) for the Kaufberatungs-Kurve; omitted =
            0.5/1/1.5/2 x the base capacity, [] = base size only. The base
            size is always included.
    SimulationStatus:
      type: object
      description: >
        Async job document. `result` is present while running (progressive:
        scenarios ohneSpeicher/standardSpeicher appear after seconds, the
        voltpilot months fill as their chunks solve) and final when status is
        `done`; German keys per the design contract - scenarios
        (ohneSpeicher/standardSpeicher/voltpilot with kostenEur/wearEur/
        vollzyklen/monatlich), headline (gesamtVorteilEur incl. -Netto,
        speicherVorteilEur, voltpilotVorteilEur incl. -Netto), sizeSweep,
        netzladenVariante (null when the site already grid-charges),
        beispielTage (typisch/bester with per-slot dispatch of BOTH batteries)
        and annahmen (preisjahr/zone/wetter/profil/hinweis). Nothing
        computable is ever a fabricated 0 - absent/null with a reason instead.
      properties:
        status: { type: string, enum: [queued, running, done, failed] }
        progress: { type: number, minimum: 0, maximum: 1 }
        result:
          type: object
          nullable: true
          additionalProperties: true
        error: { type: string, nullable: true, description: German failure reason. }
    RegistrationRequest:
      type: object
      required: [name, email, password]
      properties:
        name:
          type: string
          maxLength: 200
          description: Person or company name; becomes the tenant's display name.
          example: Erika Kaiser
        email:
          type: string
          format: email
          maxLength: 254
          description: Login email (doubles as the username; lowercased).
        password:
          type: string
          minLength: 8
          maxLength: 128
    RegistrationResponse:
      type: object
      properties:
        tenantId: { type: string, format: uuid }
        tenantName: { type: string }
        username: { type: string, description: The login name (the lowercased email). }
    EnrollmentCsrRequest:
      type: object
      required: [csrPem]
      properties:
        csrPem:
          type: string
          maxLength: 20480
          description: PEM-encoded PKCS#10 CSR, generated on the device (RSA >= 2048 or EC P-256/P-384).
        deviceInfo:
          type: string
          maxLength: 500
          description: Optional free-form device metadata (model, firmware) for support.
    EnrollmentPending:
      type: object
      properties:
        status: { type: string, example: pending }
        ref: { type: string, description: The canonical reference (POST response only). }
        message: { type: string }
    EnrollmentCertificate:
      type: object
      properties:
        deviceCertPem: { type: string, description: The CA-signed client certificate (pair with the device-held key). }
        caPem: { type: string, description: The device CA certificate (verify the broker with this). }
        mqttHost: { type: string, example: mqtt.example.com }
        mqttPort: { type: integer, example: 8883 }
        tenantId: { type: string, format: uuid }
        siteId: { type: string, format: uuid }
        deviceId: { type: string, format: uuid, description: Also the certificate CN and the device's MQTT username/clientid. }
    Site:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        biddingZone: { type: string, example: DE-LU }
        latitude:
          type: number
          nullable: true
          description: WGS84 latitude; ties the site to its weather forecast.
          example: 52.52
        longitude:
          type: number
          nullable: true
          description: WGS84 longitude.
          example: 13.405
        plantKind:
          type: string
          enum: [direktvermarktung, eigenverbrauch]
          description: >
            Anlagentyp: steers the portal's money wording per site
            ("mehr verdient" for Direktvermarktung vs. "gespart" for
            Eigenverbrauch). Defaults to eigenverbrauch.
        anzulegenderWertCtKwh:
          type: number
          nullable: true
          minimum: 0
          example: 8.11
          description: >
            Optional anzulegender Wert (ct/kWh) - the plant's fixed EEG
            reference rate from the EEG award / Direktvermarktungsvertrag;
            null = not configured. Only relevant for plantKind =
            direktvermarktung. When set, GET /earnings includes the DYNAMIC
            monthly Marktprämie max(0, anzulegender Wert - Monatsmarktwert
            Solar) on exported energy (suspended in negative-price slots -
            the simplified §51-EEG rule).
        marktpraemieCtKwh:
          type: number
          nullable: true
          deprecated: true
          description: >
            DEPRECATED (2026-07-07): the former manually entered FIXED
            premium - wrong semantics (the premium is the dynamic monthly
            difference to the Monatsmarktwert; fixed is the anzulegender
            Wert). Echoed read-only for one release, no longer accepted on
            create/update and no longer used in any computation; the column
            and this property are removed next release.
        tarifArt:
          type: string
          enum: [dynamisch, fest, ohne]
          description: >
            The customer's electricity tariff art (REPLACES the fixed
            strompreisCtKwh). 'dynamisch' = spot-coupled (each self-consumed kWh
            valued at its 15-min Börsenpreis + the optional Aufschlag); 'fest' =
            a single fixed retail price; 'ohne' = no euro value (self-consumption
            shown in kWh only). Defaults to 'ohne'.
        tarifParamCtKwh:
          type: number
          nullable: true
          minimum: 0
          example: 18.0
          description: >
            The tariff parameter (ct/kWh, >= 0): the fixed retail price for
            'fest', the optional spot-price Aufschlag (grid fees, levies, margin)
            for 'dynamisch', null for 'ohne' / unset. Editable by the site owner
            and by Portal-Admins.
        netzladenErlaubt:
          type: boolean
          description: >
            Per-site grid-charging switch (default false). false = "Nur
            Solarladen (EEG)": the optimizer charges the battery ONLY from
            the site's own PV surplus (Ausschließlichkeitsprinzip). true =
            "Netzladen aktiv": grid arbitrage allowed. Editable by the site
            owner and by Portal-Admins (the portal form carries the
            Ausschließlichkeitsprinzip warning).
        maxFeedInKw:
          type: number
          nullable: true
          exclusiveMinimum: 0
          example: 75.0
          description: >
            Static feed-in cap at the grid connection point (Einspeisegrenze
            am Netzanschlusspunkt, kW; FK1). null = no connection-point
            limit. The optimizer enforces it as a hard cap on grid EXPORT
            ONLY - import is never limited by it (unlike the telemetry-driven
            §14a limit, which is symmetric); when both exist the tighter one
            wins on export.
        leistungspreisEurKw:
          type: number
          nullable: true
          minimum: 0
          example: 120.0
          readOnly: true
          description: >
            READ-ONLY echo of the peak-shaving module (PS-1, Lastspitzenkappung):
            the RLM Leistungspreis in EUR per kW per billing period. Non-null =
            the module is ACTIVE (there is no separate flag). Configured
            exclusively by Portal-Admins via
            PUT /api/v1/admin/sites/{siteId}/optimizer-config - never accepted
            on the customer site create/update requests.
        abrechnungLeistung:
          type: string
          enum: [jahr, monat]
          readOnly: true
          description: >
            READ-ONLY echo (PS-1): the Leistungspreis billing period -
            Jahres- vs. Monatsleistungspreis (Europe/Berlin calendar periods).
            Defaults to 'jahr'. Admin-configured via optimizer-config.
        peakReserveSocPct:
          type: number
          nullable: true
          minimum: 0
          maximum: 100
          readOnly: true
          description: >
            READ-ONLY echo (PS-2): the peak-shaving SoC reserve in percent -
            a HARD floor the dispatch never discharges below, held back for
            load peaks beyond the 24h planning horizon. Stacks with the
            backup reserve (the highest configured absolute floor binds).
            null = no peak reserve. Admin-configured via optimizer-config.
        usageProfileOverride:
          type: string
          nullable: true
          enum: [arbitrage, peak, private]
          description: >
            AE7 Nutzungsprofil override (arbitrage | peak | private) or null to
            auto-derive. Set via PUT /api/v1/sites/{id}/profile; the effective +
            derived profile and the emphasis map are served by
            GET /api/v1/sites/{id}/profile (see docs/contracts/v2/usage-profile.md).
        profil:
          type: string
          nullable: true
          enum: [privat, gewerbe]
          description: >
            The site's application PRESET (Anwendungs-Programm Stufe 2,
            migration V20260838000000); null = none chosen, the state of every
            pre-Stufe-2 site. It has exactly three effects and none of them is a
            branch in the data truth: the pre-selection in the Anwendungs-Regal
            (which applications the assistant proposes - the list itself lives
            per application in `anwendungen/catalog.json` as `preset`), the
            TONALITY of the money wording (privat spart, gewerbe verdient;
            without it the wording falls back byte-identically to the plant_kind
            rule) and the reset base for the cockpit layout. It is NEVER a
            signal of the activation derivation. Written exclusively by
            PUT /api/v1/sites/{siteId}/anwendungs-preset.
    SiteAsset:
      type: object
      description: One asset row incl. registry provenance; null = not set.
      properties:
        id: { type: string, format: uuid }
        type: { type: string, example: battery, description: "'battery' | 'pv' (more later)" }
        deviceId:
          type: string
          format: uuid
          nullable: true
          description: >
            The device that controls this asset. A battery with a null deviceId
            has no control path - the optimizer plans it but cannot publish the
            plan to the edge, so the portal warns.
        capacityKwh: { type: number, nullable: true }
        maxChargeKw: { type: number, nullable: true }
        maxDischargeKw: { type: number, nullable: true }
        roundtripEfficiencyPct: { type: number, nullable: true, description: Round-trip efficiency percent; platform default 92. }
        speicherschonung:
          type: string
          enum: [aggressiv, ausgewogen, schonend, individuell]
          nullable: true
          description: >
            Battery only: the EFFECTIVE "Umgang mit dem Speicher" preset,
            derived server-side from the stored wear cost (NULL = platform
            default = ausgewogen; individuell = an admin configured a custom
            value via optimizer-config). Null for non-battery assets.
        pvCapacityKwp: { type: number, nullable: true }
        moduleCount: { type: integer, nullable: true }
        azimuthDeg: { type: number, nullable: true, description: Clockwise from North; 180 = South. }
        tiltDeg: { type: number, nullable: true }
        commissionedOn: { type: string, format: date, nullable: true }
        registry: { type: string, nullable: true, example: mastr }
        registryUnitId: { type: string, nullable: true, example: SEE966831669444 }
        registryFetchedAt: { type: string, format: date-time, nullable: true }
    MeasurementPoint:
      type: object
      description: >
        One additional read-only measurement point of a site (multi-source
        Anlage). Records Erzeuger (PV) sources whose kWp sums into the aggregate
        asset.pv, and an optional single Netz (grid-meter) source at the point of
        common coupling (no nameplate). Read-only by construction (control always
        false).
      properties:
        id: { type: string, format: uuid }
        role: { type: string, example: pv-generation, description: "'pv-generation' (Erzeuger), 'grid-meter' (Netz) or 'consumer' (Verbraucher)." }
        label: { type: string, nullable: true }
        brand: { type: string, nullable: true }
        model: { type: string, nullable: true }
        capacityKwp: { type: number, nullable: true, description: Per-source nameplate (kWp). }
        registryUnitId: { type: string, nullable: true, example: SEE900000000001 }
        control: { type: boolean, description: Always false for the roles recorded here. }
        createdAt: { type: string, format: date-time, nullable: true }
        entityType:
          type: string
          nullable: true
          enum: [battery-hybrid, producer, grid-meter, null]
          description: >
            v2 entity registry (E1a): the pilot domain type when this row is a
            v2 entity, else null. Set by the admin bootstrap only.
    V2Entity:
      type: object
      description: >
        One v2 entity-registry row (E1a). capabilities/guards are the
        docs/contracts/v2/edge-entity.schema.json $defs objects verbatim.
      properties:
        id: { type: string, format: uuid }
        entityType: { type: string, description: "Open kebab-case vocabulary (E1b); the type catalog is the truth.", example: wallbox }
        role: { type: string, example: battery-hybrid }
        label: { type: string, nullable: true }
        deviceId: { type: string, format: uuid, nullable: true }
        capabilities: { type: object, nullable: true }
        guards: { type: object, nullable: true }
    SaveV2EntityRequest:
      type: object
      description: Create/edit an entity. capabilities/guards are optional JSON overrides.
      properties:
        entityType: { type: string, description: "Catalog type (create only; open kebab-case).", example: wallbox }
        label: { type: string, nullable: true, maxLength: 200 }
        maxPowerKw: { type: number, nullable: true, description: "Rated power; bounds numeric commands + consumer max_consumption_kw." }
        capabilities: { type: object, nullable: true }
        guards: { type: object, nullable: true }
    SiteEntities:
      type: object
      description: The customer entity surface with Soll/Ist reconciliation (E1b).
      properties:
        registry:
          type: object
          nullable: true
          description: The composed registry Soll + the edge's reported revision (null = never pushed).
          properties:
            revision: { type: string }
            composedAt: { type: string, format: date-time }
            deviceId: { type: string, format: uuid, nullable: true }
            reportedRevision: { type: string, nullable: true }
            reportedAt: { type: string, format: date-time, nullable: true }
        entities:
          type: array
          items:
            allOf:
              - $ref: "#/components/schemas/V2Entity"
              - type: object
                properties:
                  typeLabel: { type: string }
                  control: { type: boolean }
                  syncStatus: { type: string, enum: [in_sync, pending, missing_on_device, unreported, never_pushed] }
                  edgeSourceId: { type: string, nullable: true, description: The edge source this entity was adopted from (U2 pin). }
                  orphanedPin:
                    type: boolean
                    nullable: true
                    description: >
                      true = the pinned edge source is no longer among the
                      device's reported sources (identity churn - offer
                      re-pin via /v2-entities/{entityId}/edge-source); false =
                      pinned + reported; null = no pin, or no local view
                      reported yet (never an unprovable orphan claim).
                  capacityKwp:
                    type: number
                    nullable: true
                    description: >
                      The adopted producer's nameplate, which sums into the
                      plant total - the delete dialog names exactly what gets
                      subtracted instead of warning vaguely.
                  observed:
                    type: object
                    nullable: true
                    properties:
                      health: { type: string, enum: [ok, stale, never] }
                      lastTelemetryAt: { type: string, format: date-time, nullable: true }
                      channels: { type: array, items: { type: string }, nullable: true }
                      appliedType: { type: string, nullable: true }
                      reportedAt: { type: string, format: date-time }
        localSetup:
          type: array
          description: The edge-authoritative commissioning view (inverter/sources; never auto-imported).
          items:
            type: object
            properties:
              id: { type: string }
              kind: { type: string, example: inverter }
              role: { type: string, nullable: true, example: pv-generation }
              brand: { type: string, nullable: true }
              model: { type: string, nullable: true }
              label:
                type: string
                nullable: true
                description: The operator-given name only - never a brand/model/role concatenation.
              reportedAt: { type: string, format: date-time }
              adoptedEntityId: { type: string, nullable: true }
              communication:
                type: string
                nullable: true
                example: solarman_v5
                description: >
                  WIE die Box dieses Gerät erreicht (Anlagen-Zentrale Stufe 2).
                  null = diese Box meldet (noch) keine Verbindungen - NIE
                  "dieses Gerät hat keine"; ein älterer Box-Stand lässt die
                  Felder weg, und daraus darf nur "Weg unbekannt" folgen.
              family: { type: string, nullable: true, example: hybrid_3p }
              host: { type: string, nullable: true, example: 192.168.254.210 }
              port: { type: integer, nullable: true, example: 8899 }
              unitId:
                type: integer
                nullable: true
                description: >
                  Die Modbus-Adresse, wie der jeweilige Transport sie nennt
                  (unit_id bzw. beim Solarman-Weg mb_slave_id) - EIN Feld, weil
                  es dieselbe Sache ist.
              serial:
                type: string
                nullable: true
                example: "2985159064"
                description: >
                  Die Logger-Nummer des Solarman-Wegs; auf einer box-verwalteten
                  Anlage liegt sie nirgendwo sonst vor.
              intervalS: { type: integer, nullable: true, example: 5 }
        staleOnDevice:
          type: array
          items: { type: string }
          description: Entities the edge still reports that the registry no longer contains.
    EntityHistory:
      type: object
      properties:
        range: { type: string, enum: [day, week, month, year] }
        from: { type: string, format: date-time }
        to: { type: string, format: date-time }
        bucketMinutes: { type: integer }
        channels:
          type: object
          additionalProperties:
            type: array
            items:
              type: object
              properties:
                start: { type: string, format: date-time }
                avg: { type: number, nullable: true }
                min: { type: number, nullable: true }
                max: { type: number, nullable: true }
                last: { type: number, nullable: true }
                n: { type: integer }
    ComponentAdoptionOutcome:
      type: object
      description: >
        Der Ausgang eines Übernahme-Versuchs (Einheitsmodell Stufe 2).
        `verdict` ist maschinenlesbar, `reason` der deutsche Klartext - beide
        stehen NEBENEINANDER, damit keine Oberfläche einen Satz nach
        Stichworten durchsuchen muss.
      properties:
        verdict:
          type: string
          enum: [ADOPTABLE, ALREADY_PORTAL, NO_REPORT, INCOMPLETE_REPORT, UNKNOWN_DEVICE, UNMAPPABLE_ROLE, AMBIGUOUS_INVERTER]
          description: >
            ADOPTABLE = übernommen · ALREADY_PORTAL = schon portal-verwaltet ·
            NO_REPORT = die Box hat sich nie geäußert · INCOMPLETE_REPORT = ein
            älterer Box-Stand ohne Verbindungsfelder · UNKNOWN_DEVICE = ein
            gemeldetes Gerät steht nicht im Vorlagen-Register ·
            UNMAPPABLE_ROLE = eine Rolle ohne eindeutige Geräteart ·
            AMBIGUOUS_INVERTER = zwei gemeldete Wechselrichter.
        reason:
          type: string
          nullable: true
          description: Der deutsche Grund; null bei Erfolg.
        components:
          type: integer
          description: Wie viele Komponenten übernommen wurden (0 wenn nicht).
    V2EntityBootstrapResult:
      type: object
      properties:
        entities:
          type: array
          items: { $ref: "#/components/schemas/V2Entity" }
        skipped:
          type: array
          items: { type: string }
          description: Honest notes about entity types that could not be created (e.g. no battery asset).
        push:
          type: object
          description: Outcome of the best-effort retained registry publish.
          properties:
            attempted: { type: boolean }
            published: { type: boolean }
            reason: { type: string, nullable: true, enum: [mqtt_not_configured, no_gateway_device, publish_failed, null] }
            deviceId: { type: string, format: uuid, nullable: true }
    V2ConversionPreview:
      type: object
      description: A read-only dry-run of the v1->v2 conversion (MIG). Writes nothing.
      properties:
        alreadyConverted: { type: boolean, description: The site already has v2 entities. }
        gatewayDevice: { type: string, format: uuid, nullable: true, description: "The resolved gateway device - the site's lead box (LeadDeviceService, UEMS AP-06 IP-5); null when none leads." }
        gatewayReason: { type: string, nullable: true, enum: [no_claimed_device, multiple_devices_no_battery_link, no_gateway_device, null], description: "Why no box leads (null when one does): no_claimed_device = the site has no box; multiple_devices_no_battery_link = several boxes, none linked to the battery and none chosen; no_gateway_device = the stored lead box is not (or no longer) registered in this site." }
        plan:
          type: array
          items:
            type: object
            properties:
              pointId: { type: string, format: uuid, nullable: true, description: Existing measurement_point id, or null when it would be created. }
              action: { type: string, enum: [create, refresh] }
              entityType: { type: string, example: battery-hybrid }
              label: { type: string, nullable: true }
              roles:
                type: array
                items: { type: string, enum: [pv, storage, consumer, grid] }
                description: The topology roles this entity's measure channels resolve to (AE1 DefaultRole).
              capabilities: { type: object, description: The capabilities bootstrap would write. }
              guards: { type: object, description: The guard config bootstrap would write. }
        skipped:
          type: array
          items: { type: string }
    V2HistoryCutover:
      type: object
      description: The site's v1->v2 history bridge cutover instant.
      properties:
        cutoverAt: { type: string, format: date-time, nullable: true, description: null = un-migrated (pure v1 history). }
    MastrPreview:
      type: object
      description: >
        Mapped registry record for confirmation. Nullable = "nicht im Register
        hinterlegt". `warnings` are honest German notes (Ost-West/nachgeführt
        orientation, symmetric-charging assumption on the keyless source).
      properties:
        mastrNummer: { type: string, example: SEE966831669444 }
        kind: { type: string, enum: [pv, storage] }
        name: { type: string, nullable: true }
        status: { type: string, nullable: true, example: In Betrieb }
        plantType: { type: string, nullable: true, example: Gebäudesolaranlage }
        powerKw: { type: number, nullable: true, description: PV = kWp (Bruttoleistung); storage = discharge kW. }
        inverterPowerKw: { type: number, nullable: true }
        moduleCount: { type: integer, nullable: true }
        azimuthLabel: { type: string, nullable: true, example: Süd }
        azimuthDeg: { type: number, nullable: true, example: 180 }
        tiltLabel: { type: string, nullable: true, example: 21 - 40 Grad }
        tiltDeg: { type: number, nullable: true, example: 30 }
        commissionedOn: { type: string, format: date, nullable: true }
        storageCapacityKwh: { type: number, nullable: true }
        chargePowerKw: { type: number, nullable: true }
        batteryTechnology: { type: string, nullable: true, example: Lithium-Batterie }
        plz: { type: string, nullable: true }
        ort: { type: string, nullable: true }
        linkedUnitNumber: { type: string, nullable: true, description: Cross-linked sibling unit (SOAP source only). }
        warnings:
          type: array
          items: { type: string }
    MastrApplyRequest:
      type: object
      description: At least one of pv/storage is required.
      properties:
        pv:
          type: object
          required: [mastrNummer]
          properties:
            mastrNummer: { type: string, pattern: "SEE\\d{12}" }
            capacityKwp: { type: number, nullable: true }
            moduleCount: { type: integer, nullable: true }
            azimuthDeg: { type: number, nullable: true }
            tiltDeg: { type: number, nullable: true }
            commissionedOn: { type: string, format: date, nullable: true }
        storage:
          type: object
          required: [mastrNummer]
          properties:
            mastrNummer: { type: string, pattern: "SEE\\d{12}" }
            capacityKwh: { type: number, nullable: true }
            maxChargeKw: { type: number, nullable: true }
            maxDischargeKw: { type: number, nullable: true }
            commissionedOn: { type: string, format: date, nullable: true }
    Teilansicht:
      type: object
      description: >
        UEMS AP-03 IP-10 (Regel R-A2): ueber WIE VIELE Standorte diese Antwort
        gebildet wurde (`sichtbar`) und wie viele der Kundenbereich hat
        (`gesamt`). Das Portal zeigt daraus "Teilansicht: n von m Standorten",
        sobald sichtbar < gesamt - denselben Satz, den GET /api/v1/me aus dem
        Rechte-Vertrag bildet (SelbstauskunftTeilansicht.kopfzeile). Beide
        zaehlen Standorte OHNE archivierte, damit Kopfzeile und Antwort nie
        Verschiedenes behaupten.

        `gesamt` ist eine ANZAHL VON STANDORTEN - keine Energie- und keine
        Geldsumme. Es ist die einzige Zahl dieser Antworten, die ueber die
        sichtbare Menge hinausweist, und bewusst nur eine Kardinalzahl: aus "3"
        laesst sich kein fremder Verbrauch, kein fremder Erloes und kein fremder
        Name ableiten. Jedes andere Feld - Liste wie Summe - entsteht
        ausschliesslich ueber die `sichtbar` Standorte (AP-03 Paragraf 6.2
        Punkt 6: kein Feld enthaelt eine Gesamtsumme, aus der sich ein fremder
        Standort ableiten liesse).

        Unternehmensweiter Zugriff: sichtbar == gesamt. Kundenbereich ohne
        Standorte: {0, 0}.
      required: [sichtbar, gesamt]
      properties:
        sichtbar:
          type: integer
          description: Die Standorte, ueber die diese Antwort gebildet wurde.
        gesamt:
          type: integer
          description: Die Standorte des Kundenbereichs - eine Anzahl, nie eine Summe.
    Overview:
      type: object
      description: >
        Fleet overview ueber die SICHTBAREN Anlagen (see GET /api/v1/overview).
        Liste und Summen entstehen ueber genau diese Menge - nie mandantenweit
        (UEMS AP-03 IP-10, Regel R-A2).
      properties:
        sites:
          type: array
          items: { $ref: "#/components/schemas/OverviewSite" }
        totals: { $ref: "#/components/schemas/OverviewTotals" }
        dailySavings:
          type: array
          description: >
            Fleet-wide ex-ante savings per Europe/Berlin day, last 14 days
            incl. today; days without any plan are absent.
          items:
            type: object
            properties:
              day: { type: string, format: date }
              savingsEur: { type: number }
        teilansicht:
          description: Additiv (AP-03 IP-10).
          allOf: [{ $ref: "#/components/schemas/Teilansicht" }]
    OverviewSite:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        plantKind: { type: string, enum: [direktvermarktung, eigenverbrauch] }
        netzladenErlaubt:
          type: boolean
          description: Per-site grid-charging switch (see Site.netzladenErlaubt).
        batteryWithoutDevice:
          type: boolean
          description: >
            The site has a battery asset with no controlling device: the
            optimizer plans it but cannot publish the plan to the edge. The
            portal warns and links to the fix.
        deviceCount: { type: integer }
        onlineCount:
          type: integer
          description: Boxes whose newest status heartbeat ARRIVED within 5 minutes (telemetry fallback until the first heartbeat after migration).
        waitingCount:
          type: integer
          description: Boxes without a status heartbeat or legacy telemetry arrival.
        worstStatus:
          type: string
          nullable: true
          enum: [online, stale, waiting]
          description: >
            Worst device status of the site (stale beats waiting beats online);
            null for a site without devices.
        lastSeenAt:
          type: string
          format: date-time
          nullable: true
          description: Newest status-heartbeat arrival over the site's boxes (legacy telemetry fallback until their first heartbeat).
        live:
          nullable: true
          description: The site's newest telemetry observation; null when none.
          allOf: [{ $ref: "#/components/schemas/OverviewLive" }]
        plannedSavingsTodayEur:
          type: number
          nullable: true
          description: >
            Ex-ante optimizer savings for today (Europe/Berlin), latest run per
            15-min slot; null when no plan covers today.
        roleCounts:
          $ref: "#/components/schemas/OverviewRoleCounts"
        usageProfile:
          type: string
          enum: [arbitrage, peak, private]
          description: >
            The site's effective AE7 usage profile (the portfolio Profil-Chip),
            same derivation as GET /sites/{id}/profile.
        lastPlanGeneratedAt:
          type: string
          format: date-time
          nullable: true
          description: >
            When the optimizer last RAN for this site (max generated_at over the
            persisted plans) - the "Plan" column of the platform overview. The
            optimizer re-plans every 15 minutes, so the age is the statement.
            null = no run inside the lookback window (7 days), never a
            fabricated age.
        storageCapacityKwh:
          type: number
          nullable: true
          description: >
            This site's battery capacity (kWh) - the WEIGHT of the portfolio's
            average state of charge (Anwendungs-Programm Stufe 4). Without it a
            fleet average would weigh a 10-kWh house like a 120-kWh plant.
            null = no battery, never a fake zero.
        energyToday:
          nullable: true
          description: >
            Energy sums of the running Europe/Berlin day from
            telemetry_rollup_15m. Energy MAY be summed across plants (unlike a
            percentage). Every field is individually null when no quarter hour
            carried that channel; the rollups lag their refresh job by up to 15
            minutes, which is correct for a day total and wrong for a live
            value (that one comes from `live`).
          allOf: [{ $ref: "#/components/schemas/OverviewEnergyToday" }]
        chargePointCount:
          type: integer
          description: >
            Number of ev-charger entities (roleCounts folds them into
            `consumer` and therefore cannot answer this).
        anwendungen:
          type: array
          items: { type: string }
          description: >
            The site's ACTIVE applications - the stored customer intent over the
            derivation, exactly the rule GET /sites/{id}/profiles applies. The
            portfolio cockpit composes its Bausteine from the UNION over the
            plants; without this it would have to guess, and a switched-off
            marktvermarktung would stay visible.
        standort:
          nullable: true
          description: >
            UEMS AP-02 IP-3 (additive): the Standort this site is assigned to
            today, or null while it is not assigned (today every existing site).
          allOf: [{ $ref: "#/components/schemas/StandortBezug" }]
    OverviewEnergyToday:
      type: object
      description: >
        One day's energy sums (kWh). Every field individually nullable - a
        generation-only plant without a grid meter reports no fabricated 0.
      properties:
        pvKwh: { type: number, nullable: true }
        loadKwh: { type: number, nullable: true }
        gridImportKwh: { type: number, nullable: true }
        gridExportKwh: { type: number, nullable: true }
    AdminFleet:
      type: object
      description: The platform-wide fleet aggregate (see GET /api/v1/admin/fleet).
      properties:
        unterstuetzungBis:
          type: object
          description: Latest exclusive end of currently active support per tenant; absent key means no active support.
          additionalProperties: { type: string, format: date-time }
        unterstuetzungStandorte:
          type: array
          description: Standort selection for the existing platform request/emergency actions; admin-only, no customer authorization.
          items:
            type: object
            required: [id, tenantId, name]
            properties:
              id: { type: string, format: uuid }
              tenantId: { type: string, format: uuid }
              name: { type: string }
        sites:
          type: array
          items: { $ref: "#/components/schemas/AdminFleetSite" }
        releases:
          type: array
          description: >
            The edge release register, NEWEST FIRST - the first entry is the
            fleet-wide TARGET (Soll). It rides along because "outdated" is only
            a statement against this ordering: a site's reported version is
            LOOKED UP here, and a version the register does not know (an
            existing build is stamped with a bare commit SHA) is "not
            registered" - explicitly NOT "outdated". Empty = no yardstick, so
            nothing is claimed outdated.
          items: { $ref: "#/components/schemas/AdminFleetRelease" }
    AdminFleetSite:
      type: object
      description: >
        One site of the platform fleet. The device fields carry exactly the
        OverviewSite semantics; control/curtailment are the SAME rows the
        per-site customer routes serve, so the platform overview and a site page
        can never claim different things.
      properties:
        siteId: { type: string, format: uuid }
        siteName: { type: string }
        tenantId: { type: string, format: uuid }
        tenantName: { type: string }
        plantKind: { type: string, enum: [direktvermarktung, eigenverbrauch] }
        netzladenErlaubt: { type: boolean }
        tarifArt: { type: string, enum: [dynamisch, fest, ohne] }
        deviceCount: { type: integer }
        onlineCount: { type: integer, description: "Boxes whose newest status heartbeat ARRIVED within 5 minutes (legacy telemetry fallback until their first heartbeat)." }
        waitingCount: { type: integer, description: "Boxes without a status heartbeat or legacy telemetry arrival." }
        worstStatus:
          type: string
          nullable: true
          enum: [online, stale, waiting]
          description: stale beats waiting beats online; null without devices.
        lastSeenAt: { type: string, format: date-time, nullable: true }
        lastPlanGeneratedAt:
          type: string
          format: date-time
          nullable: true
          description: >
            Newest optimizer run; null = no run inside the 7-day lookback
            window, never a fabricated age.
        hasStorage: { type: boolean, description: "The site owns a battery asset (the control matrix's row set)." }
        batteryWithoutDevice: { type: boolean }
        sources:
          nullable: true
          description: >
            The device-reported measurement points counted by health; null = the
            device reported none - which is NOT "0 healthy".
          allOf: [{ $ref: "#/components/schemas/AdminFleetSources" }]
        edge:
          nullable: true
          description: >
            The reported edge software version; null means UNKNOWN (the edge
            only builds the heartbeat's flows block after its first deployment
            set), never "outdated".
          allOf: [{ $ref: "#/components/schemas/AdminFleetEdge" }]
        update:
          nullable: true
          description: >
            The reported OTA state (Stufe 0); null means UNKNOWN, never
            "outdated". It sits NEXT TO `edge`, not inside it: the version
            rides TOP-LEVEL in the heartbeat and is therefore independent of
            the `flows` block an edge only builds after its first flow
            deployment - which is exactly why this block also covers the
            devices `edge` knows nothing about. Both carry their OWN
            reportedAt.
          allOf: [{ $ref: "#/components/schemas/AdminFleetUpdate" }]
        control:
          nullable: true
          description: Latest control evidence; null = the device never read back.
          allOf: [{ $ref: "#/components/schemas/ControlStatus" }]
        curtailment:
          nullable: true
          description: Latest curtailment evidence; null = no evidence.
          allOf: [{ $ref: "#/components/schemas/CurtailmentStatus" }]
        kwp: { $ref: "#/components/schemas/AdminFleetKwp" }
        forecast:
          type: array
          description: >
            Forecast quality per kind (load/pv) over the evaluation window;
            empty when the site has no evaluated day at all.
          items: { $ref: "#/components/schemas/AdminFleetForecast" }
        pflege:
          type: array
          description: >
            The SERVER-derived maintenance items (one truth - the portal renders
            them, it does not re-derive them). Empty = nothing open; "not
            checked" is never an item, it is the reason inside kwp/forecast.
          items: { $ref: "#/components/schemas/AdminFleetPflege" }
    AdminFleetSources:
      type: object
      properties:
        total: { type: integer }
        ok: { type: integer }
        stale: { type: integer, description: "Reported but without current values." }
        never: { type: integer, description: "Has never delivered." }
    AdminFleetEdge:
      type: object
      properties:
        coreVersion: { type: string, nullable: true }
        paletteVersion: { type: string, nullable: true }
        reportedAt: { type: string, format: date-time }
    AdminFleetUpdate:
      type: object
      description: >
        What the device says about the build it runs and what it is doing about
        it (OTA Stufe 0). Everything the box cannot honestly know stays null -
        in Stufe 0 there is no release register and no target on the device, so
        target/lastKnownGood are empty and a sequence number is never guessed.
      properties:
        version:
          type: string
          nullable: true
          description: >
            The build stamp VERBATIM as reported. An existing build carries a
            bare 12-char commit SHA; the portal then renders it honestly as
            "not registered", never as outdated.
        backend:
          type: string
          nullable: true
          description: "The apply backend, e.g. compose. The contract is runtime-agnostic."
        current: { type: string, nullable: true }
        target:
          type: string
          nullable: true
          description: "Empty in Stufe 0 - there is no target assignment on the device yet."
        state:
          type: string
          nullable: true
          enum: [idle, verifying, deferred, downloading, applying, self_test, succeeded, failed, rolled_back]
          description: >
            null = the device reported a version but no update block. A state
            outside this vocabulary is DROPPED on ingest rather than stored - a
            word the cloud does not understand must not become a portal
            sentence.
        reason:
          type: string
          nullable: true
          description: "German; mandatory on the device side for deferred/failed/rolled_back."
        lastKnownGood: { type: string, nullable: true }
        reportedAt: { type: string, format: date-time }
    AdminFleetRelease:
      type: object
      description: >
        One entry of the edge release register. releaseSeq is THE ordering - a
        monotonic integer, never a string or SHA comparison (decision D5).
      properties:
        releaseSeq: { type: integer, format: int64 }
        version: { type: string, description: "The release tag edge-YYYY.MM.N." }
    AdminFleetKwp:
      type: object
      description: >
        kWp plausibility (B4): the measured PV peak (max quarter-hour mean power
        over the lookback window) against the maintained nameplate. `unbekannt`
        is a full verdict - without a nameplate or without enough measured
        quarter-hours nothing is judged, and `reason` says why.
      properties:
        configuredKwp: { type: number, nullable: true }
        observedPeakKw: { type: number, nullable: true }
        buckets: { type: integer, description: "Quarter-hours WITH a PV value in the window." }
        verdict: { type: string, enum: [ok, zu_hoch, zu_niedrig, unbekannt] }
        reason: { type: string, description: "One German sentence - also (and especially) for `unbekannt`." }
    AdminFleetForecast:
      type: object
      description: >
        The site's mean normalized forecast error for one kind, plus the FLEET
        median as the yardstick. Normalized because only that is comparable
        across plant sizes. `outlier` is only ever claimed when a yardstick
        exists (enough evaluated sites) - the same discipline as the edge
        version ("without a reference nothing is outdated").
      properties:
        kind: { type: string, enum: [load, pv] }
        nmaePct: { type: number }
        days: { type: integer, description: "Evaluated days inside the window." }
        fleetMedianPct: { type: number, nullable: true, description: "null = no fleet yardstick." }
        outlier: { type: boolean }
        reason: { type: string }
    AdminFleetPflege:
      type: object
      properties:
        code:
          type: string
          description: >
            Machine-readable id (tarif-fehlt | speicher-ohne-geraet |
            kwp-unplausibel | prognose-ausreisser-{kind}).
        label: { type: string, description: "Short German chip label." }
        detail: { type: string, nullable: true, description: "The reason, where the check has one." }
    EdgeVersion:
      type: object
      description: >
        One device's reported software version. The core version prefers the
        top-level status-heartbeat build (available without any flow deployment)
        and falls back to the historical flows acknowledgement. The palette
        version still comes from that flows acknowledgement and can be absent
        while Node-RED's admin API is not configured.
      properties:
        supports:
          type: array
          nullable: true
          items: { type: string }
          description: Known capability names from the latest heartbeat; null means absent.
        capabilities:
          type: array
          items: { type: string }
          description: Effective known capabilities, reported OR supported by the release table.
        deviceId: { type: string, format: uuid }
        siteId: { type: string, format: uuid }
        coreVersion:
          type: string
          nullable: true
          description: The running vp-edge-core version.
        paletteVersion:
          type: string
          nullable: true
          description: The installed @voltpilot/node-red-vp-palette version.
        reportedAt: { type: string, format: date-time }
        newestRelease:
          type: string
          nullable: true
          description: >
            The release register's newest entry - the SOLL this device should
            run (Geräteseiten Stufe 1 R2a). null = the register is empty, i.e.
            there is no yardstick at all. Only the judgement travels; the
            register itself stays behind /api/v1/admin/**.
        upToDate:
          type: boolean
          nullable: true
          description: >
            THREE-VALUED: true = the device runs the newest registered release
            (matched by the house prefix rule, so a tag build's
            `<tag>-<shortsha>` counts), false = outdated, null = NOT judgeable
            (nothing reported, or the reported stamp is not in the register - a
            gap in the REGISTER, never an age claim about the device).
    OverviewRoleCounts:
      type: object
      description: >
        U5 portfolio "Entitäten" badge: Σ v2 entities per topology role. An
        entity's role is its entity_type's catalog category (storage→storage,
        producer→pv, meter→grid, consumer→consumer); each entity counts once (a
        battery-hybrid is one storage entity). All zero for a registry-less site.
      properties:
        pv: { type: integer }
        storage: { type: integer }
        consumer: { type: integer }
        grid: { type: integer }
    OverviewLive:
      type: object
      properties:
        ts: { type: string, format: date-time, description: Observation time. }
        pvKw: { type: number, nullable: true }
        loadKw: { type: number, nullable: true }
        gridKw: { type: number, nullable: true, description: "+ import / - export" }
        socPct: { type: number, nullable: true }
    OverviewTotals:
      type: object
      properties:
        sites: { type: integer }
        devices: { type: integer }
        online: { type: integer }
        plannedSavingsTodayEur:
          type: number
          nullable: true
          description: Sum over the sites' non-null values; null when none has a plan.
        liveSitesCovered:
          type: integer
          description: Sites whose live snapshot is inside the 5-min freshness window.
        storageCapacityKwh:
          type: number
          nullable: true
          description: >
            U5 portfolio KPI: Σ battery capacity (kWh) over the fleet's v1 asset
            rows; null when the fleet has no battery.
        storagePowerKw:
          type: number
          nullable: true
          description: Σ battery discharge power (kW) over the fleet; null when none.
    Earnings:
      type: object
      description: >
        Realized earnings (see GET /api/v1/earnings) ueber die SICHTBAREN
        Anlagen - und, wenn der Aufruf `standort` nennt, ueber genau die Anlagen
        an diesen Standorten. `totals` und `vergleich` summieren ueber `sites`
        und ueber nichts sonst (UEMS AP-03 IP-10, Regel R-A2).
      properties:
        range: { type: string, enum: [day, month, year, all] }
        from:
          type: string
          format: date-time
          description: >
            Window start. For range=all this is the fleet's first covered
            Berlin day - the honest "seit ..." of a Gesamt view.
        to: { type: string, format: date-time }
        sites:
          type: array
          items: { $ref: "#/components/schemas/EarningsSite" }
        totals: { $ref: "#/components/schemas/EarningsTotals" }
        vergleich:
          allOf: [{ $ref: "#/components/schemas/EarningsVergleich" }]
          nullable: true
          description: >
            Die Einordnung der Flotten-Zahl (Konzept vp-erloese-lesbar-konzept-u3
            §3.7 Befund B13, Entscheid E3). Absent/null, sobald es nichts
            ehrlich zu vergleichen gibt - dann bleibt die Zeile weg, NIE ein
            Prozentsatz eines laufenden Tages gegen einen vollen Vortag.
            Nur fuer range=day belegt; Monat/Jahr/Gesamt holt das Portal ueber
            seinen zweiten Abruf mit verschobenem Anker (dort waere ein Prozent
            per E3 ohnehin verboten, und eine zweite Monats-Aggregation auf
            jedem Aufruf ist die Verschwendung, die Audit vp-portal-perf-a4 B5
            abgestellt hat).
            Sie summiert ueber DIE ZEILEN DIESER ANTWORT, nicht ueber alles,
            was die Abfrage zurueckgibt (AP-03 IP-10).
        teilansicht:
          description: Additiv (AP-03 IP-10).
          allOf: [{ $ref: "#/components/schemas/Teilansicht" }]
    EarningsVergleich:
      type: object
      description: >
        Zwei Betraege derselben Groesse ueber gleich lange Grundlagen - das
        Netto der Flotte (Summe von eigenverbrauchsWertEur - actualEur ueber die
        Anlagen mit bewerteten Viertelstunden), aus DERSELBEN Preiskomposition
        wie die Zeilen (EarningsRepository.aggregate; kein zweites SQL).
      properties:
        modus:
          type: string
          enum: [gleicher_zeitpunkt, ganze_periode]
          description: >
            gleicher_zeitpunkt = ein LAUFENDER Tag, beide Seiten bis bisStunde
            summiert (die laufende Stunde bleibt bei BEIDEN draussen) - nur hier
            darf ein Prozentsatz stehen. ganze_periode = ein abgeschlossener
            Tag, beide Seiten vollstaendig.
        bisStunde:
          type: integer
          nullable: true
          minimum: 1
          maximum: 23
          description: >
            Die Berliner WANDUHR-Stunde, BIS zu der beide Seiten summiert sind;
            null bei modus=ganze_periode.
        jetztEur: { type: number, format: double }
        vorherEur: { type: number, format: double }
      required: [modus, jetztEur, vorherEur]
    EarningsSite:
      type: object
      description: >
        One site's realized earnings over the window. Money values are null
        when no slot is computable; `reason` then says why. Partial coverage
        is not an error - `coveredSlots` counts the slots in the sums.
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        plantKind: { type: string, enum: [direktvermarktung, eigenverbrauch] }
        anzulegenderWertCtKwh:
          type: number
          nullable: true
          description: >
            The site's configured anzulegender Wert (ct/kWh), echoed here so
            the portal can state the numbers INCLUDE the dynamic monthly
            Marktprämie; null = not configured (numbers are pure spot, the
            fine print keeps the generic "zzgl. Marktprämie" for
            Direktvermarktung).
        realizedExportCtKwh:
          type: number
          nullable: true
          description: >
            Benchmark KPI - the export-weighted spot price (ct/kWh) the
            site's metered feed-in actually fetched over the window; null
            when the window has no exported energy.
        marketValueSolarCtKwh:
          type: number
          nullable: true
          description: >
            Benchmark KPI - the Monatsmarktwert Solar (ct/kWh) weighted with
            the SAME exports (a range spanning months compares like with
            like); null without exported energy or market-value coverage.
        marketValueProvisional:
          type: boolean
          nullable: true
          description: >
            True while any month contributing to marketValueSolarCtKwh still
            carries the provisional (not yet TSO-published) value; the
            portal labels the number "vorläufig".
        baselineEur:
          type: number
          nullable: true
          description: >
            Signed cost of the unregulated plant (negative = revenue): its
            per-slot import (greatest(load - pv, 0)) valued at the site's real
            supply price - the SAME composition the optimizer plans with (flat
            tariff / spot + Aufschlag / (spot + Preisblatt-Komponenten) x
            (1+USt); bare spot without any price data) - its export
            (greatest(pv - load, 0)) at spot, incl. the Marktprämie its
            immediate feed-in would earn when configured (suspended in
            negative-price slots). Every slot is valued at the CURRENTLY
            maintained tariff/Preisblatt (no price-sheet history).
        actualEur:
          type: number
          nullable: true
          description: >
            Signed measured cost with VoltPilot (negative = revenue): metered
            import at the site's real supply price (same composition as
            baselineEur), metered export at spot incl. the Marktprämie when
            configured (suspended in negative-price slots).
        savedEur:
          type: number
          nullable: true
          description: >
            baseline - actual; negative when the battery ran at a loss. Since
            the structured Bezugspreis, avoided import counts at the tariff
            (not bare spot), so load-shifting is credited fully while pure
            spot churning loses its phantom contribution.
        arbitrageEur:
          type: number
          nullable: true
          description: >
            The part of savedEur the grid-charging permission concretely
            earned ("davon Arbitrage-Gewinn"): revenue attributed to
            grid-charged battery content minus its purchase cost, via a
            deterministic storage-mix walk over the window's 15-min slots
            (grid share of a slot's charge = max(0, charge - PV surplus);
            discharge draws proportionally from the pv/grid content mix;
            pre-window content counts as solar, losses debit both parts
            proportionally). Null when the site may not grid-charge
            (netzladen_erlaubt = false) or the window contains no
            grid-charged energy - never a fake zero.
        pvShiftEur:
          type: number
          nullable: true
          description: >
            The solar time-shifting remainder savedEur - arbitrageEur (incl.
            any Marktprämie delta - grid-charged energy is never
            premium-eligible), so arbitrageEur + pvShiftEur == savedEur
            exactly. Null whenever arbitrageEur is null.
        savedSpeicherEur:
          type: number
          nullable: true
          description: >
            Der Speicher-Anteil der Dreiteilung savedEur = savedSpeicherEur +
            savedSteuerungEur (Audit vp-geldzahlen-audit-x7 §2.5, B1): was ein
            STUR arbeitender Standard-Speicher - das Greedy-Referenzmodell der
            Ersparnis-Simulation (gleiche Physik: Kapazität, Lade-/
            Entladeleistung, sqrt(eta)-Split, SoC-Band inkl. Reserven; lädt
            jeden PV-Überschuss, deckt jedes Defizit, lädt nie aus dem Netz,
            null Preisbewusstsein) - gegenüber der speicherlosen Anlage
            erwirtschaftet hätte. Per-Slot-Zustands-Walk über dieselben
            covered Slots, bewertet mit denselben Preis-Kompositionen wie
            savedEur (je Slot discharge × Bezugspreis − charge × Exportwert).
            Null when savedEur itself is null OR the plant has no maintained
            battery master data (steuerungSplitReason says so) - never a
            guessed reference battery.
        savedSteuerungEur:
          type: number
          nullable: true
          description: >
            savedEur - savedSpeicherEur, the EXACT remainder: der Mehrwert
            der INTELLIGENTEN Steuerung gegenüber dem stur arbeitenden
            Speicher (Preisfenster, Netzladen-Arbitrage, Abregelung), so
            savedSpeicherEur + savedSteuerungEur == savedEur per
            construction. Null whenever savedSpeicherEur is null.
        steuerungSplitReason:
          type: string
          nullable: true
          enum: [no_battery_data]
          description: >
            Why the Speicher/Steuerung split is absent although savedEur is
            computable: no_battery_data = the site has no primary battery
            asset with maintained capacity and charge/discharge power, so the
            greedy reference cannot be simulated (honestly null, never
            guessed). Null when the split is present, or when savedEur itself
            is null (reason explains that case).
        coveredSlots: { type: integer }
        firstCoveredDate:
          type: string
          format: date
          nullable: true
          description: Europe/Berlin day of the first covered slot in the window.
        reason:
          type: string
          nullable: true
          enum: [no_data, missing_channels, no_prices]
          description: >
            Why nothing is computable (only set when savedEur is null):
            no_data = no measurements in the window; missing_channels =
            measurements lack the load/PV/grid channels (generation-only
            inverters); no_prices = no day-ahead price covers the slots yet.
        dailySaved:
          type: array
          description: >
            Realized savings per Europe/Berlin day, last 14 days incl. today,
            independent of `range` (spark bars + "Heute" teaser). Days without
            a computable slot are absent.

            `savedEur` measures against a plant WITHOUT a battery and is an
            ADMIN number since the Messlatte decision (2026-09-04);
            `savedSteuerungEur` measures the intelligent control against the
            SAME plant with a stubborn (price-blind) battery and is the only
            storage figure a customer surface renders. The latter is null when
            the site has no maintained battery master data (see
            steuerungSplitReason) - the total is never a stand-in.
          items:
            type: object
            properties:
              day: { type: string, format: date }
              savedEur: { type: number }
              savedSteuerungEur: { type: number, nullable: true }
        tarifArt:
          type: string
          enum: [dynamisch, fest, ohne]
          description: >
            The site's configured electricity tariff art, echoed so the
            money-centric view can render the provenance sentence ("... zum
            dynamischen Börsenpreis + Aufschlag" vs. "... zum festen Preis").
        tarifParamCtKwh:
          type: number
          nullable: true
          description: >
            The tariff parameter (ct/kWh): the fixed retail price for fest, the
            optional spot-price Aufschlag for dynamisch, null for ohne / unset.
        tarifPriced:
          type: boolean
          nullable: true
          description: >
            Whether savedEur's import side is valued beyond bare spot (a
            tariff parameter, a maintained site_supply_price Preisblatt, or
            the platform default-components flag). Drives the honest
            "bewertet zu Ihrem Stromtarif" provenance copy - a bare tarifArt
            echo cannot tell an 'ohne' site with a maintained Preisblatt from
            one without.
        exportVerguetungPriced:
          type: boolean
          nullable: true
          description: >
            The EXPORT-side sibling of tarifPriced (audit vp-geldzahlen-audit-x7,
            B2 fix): whether the feed-in terms are valued at the plant's feste
            EEG-Einspeisevergütung instead of bare spot (a non-grid-charging
            eigenverbrauch plant with a known, unexpired commissioning date).
            False for Direktvermarktung (its spot + Marktprämie valuation is
            explained by the premium fields) and for plants whose remuneration
            cannot be determined - those honestly stay at spot.
        einspeiseErloesEur:
          type: number
          nullable: true
          description: >
            Feed-in revenue over the window: the metered export valued per the
            plant's remuneration - spot + Marktprämie for Direktvermarktung, the
            feste EEG-Vergütung (§51a-aware per slot) for an unexpired
            eigenverbrauch plant, bare spot otherwise. Null when nothing is
            computable.
        eigenverbrauchsWertEur:
          type: number
          nullable: true
          description: >
            Value of self-consumed energy, computed slot-by-slot per the site's
            tariff (dynamisch: each self-consumed kWh at its 15-min Börsenpreis +
            Aufschlag; fest: at the fixed price); null for an 'ohne' tariff
            (never a fabricated euro) or when nothing is computable.
        gesamtertragEur:
          type: number
          nullable: true
          description: >
            Gesamtertrag = einspeiseErloesEur + eigenverbrauchsWertEur (feed-in
            revenue alone when strompreis is unset). The money-centric hero's
            big number; null when nothing is computable.
        selbstverbrauchKwh:
          type: number
          nullable: true
          description: Self-consumed energy over the window (kWh); null when nothing computable.
        eingespeistKwh:
          type: number
          nullable: true
          description: Grid-exported energy over the window (kWh).
        batterieBewegtKwh:
          type: number
          nullable: true
          description: Energy moved through the battery over the window (charged + discharged, kWh).
        expectedMarketValueSolarCtKwh:
          type: number
          nullable: true
          description: >
            Forward-looking expected Marktwert Solar (ct/kWh): the day-ahead
            price weighted with THIS site's own PV forecast over the coming
            horizon (Σ(price × pv) / Σ(pv)) - the forward companion to the
            realized marketValueSolarCtKwh. Range-independent (always the
            future). Null when there is no forward PV forecast or no forward
            day-ahead price coverage (the portal then hides the figure).
        expectedMarketValueFrom:
          type: string
          format: date-time
          nullable: true
          description: First covered forward slot of expectedMarketValueSolarCtKwh.
        expectedMarketValueTo:
          type: string
          format: date-time
          nullable: true
          description: Last covered forward slot of expectedMarketValueSolarCtKwh.
        expectedMarketValueSlots:
          type: integer
          nullable: true
          description: >
            Number of forward 15-min slots (price + forecast both present)
            behind expectedMarketValueSolarCtKwh - lets the portal state the
            horizon ("nächste N h").
        series:
          type: array
          description: >
            Gesamtertrag bucketed over the selected range (per Europe/Berlin
            hour for day, per day for month, per month for year/all) - the
            Ertrag chart. Only buckets with a computable slot are listed.
          items:
            type: object
            properties:
              start: { type: string, format: date-time, description: Berlin bucket start. }
              gesamtertragEur: { type: number }
        monthlyStrip:
          type: array
          description: >
            Gesamtertrag per Europe/Berlin month for the last 12 months
            (independent of `range`) - the tappable month strip. Months without
            a computable slot are absent. OPT-IN (audit vp-portal-perf-a4,
            finding B5): [] unless the request carries `strip=true`.
          items:
            type: object
            properties:
              month: { type: string, format: date, description: First day of the Berlin month. }
              gesamtertragEur: { type: number }
        peakShaving:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/PeakShaving" }]
          description: >
            The Lastspitzenkappung proof (PS-4) - present exactly when the
            site's peak-shaving module is active (non-null
            site.leistungspreis_eur_kw); null otherwise. Range-independent:
            always describes the RUNNING Europe/Berlin billing period.
    SiteEarnings:
      type: object
      description: >
        One Anlage's MEASURED money over one period (see GET
        /api/v1/sites/{siteId}/earnings). Every money/energy field is null when
        it is not computable - never a fabricated 0 - and `reason` says why.
        Two identities hold by construction: nettoErgebnisEur =
        einspeiseErloesEur + eigenverbrauchsWertEur - stromkostenEur, and
        stromkostenEur - einspeiseErloesEur = actualEur.
      properties:
        siteId: { type: string, format: uuid }
        name: { type: string }
        range: { type: string, enum: [day, week, month, year, all] }
        from:
          type: string
          format: date-time
          description: Window start; for range=all the site's first covered Berlin day.
        to: { type: string, format: date-time }
        plantKind: { type: string, enum: [direktvermarktung, eigenverbrauch] }
        tarifArt: { type: string, enum: [dynamisch, fest, ohne] }
        tarifParamCtKwh: { type: number, nullable: true }
        tarifPriced:
          type: boolean
          description: >
            Whether the import side is valued beyond bare spot (tariff
            parameter, maintained Preisblatt, or the platform default
            components) - the "bewertet zu Ihrem Stromtarif" vs "zu
            Börsenpreisen" switch of the surface copy.
        exportVerguetungPriced:
          type: boolean
          description: >
            The EXPORT-side sibling of tarifPriced (audit vp-geldzahlen-audit-x7,
            B2 fix): whether the feed-in is valued at the plant's feste
            EEG-Einspeisevergütung instead of bare spot. False for
            Direktvermarktung and for plants whose remuneration cannot be
            determined (no commissioned pv asset) - those honestly stay at spot.
        anzulegenderWertCtKwh: { type: number, nullable: true }
        coveredSlots: { type: integer, format: int64 }
        firstCoveredDate: { type: string, format: date, nullable: true }
        reason:
          type: string
          nullable: true
          enum: [no_data, missing_channels, no_prices]
          description: Why nothing is computable; null when coveredSlots > 0.
        einspeiseErloesEur:
          type: number
          nullable: true
          description: >
            Metered feed-in valued per the plant's remuneration: spot PLUS the
            Marktprämie for Direktvermarktung, the feste EEG-Vergütung
            (§51a-aware per slot) for an unexpired eigenverbrauch plant, bare
            spot otherwise.
        eigenverbrauchsWertEur:
          type: number
          nullable: true
          description: >
            Self-consumed energy valued per the site's tariff, slot by slot;
            null for an "ohne" tariff without a maintained Preisblatt (the
            quantity then lives in selbstverbrauchKwh alone).
        stromkostenEur:
          type: number
          nullable: true
          description: >
            The metered grid import valued at the site's real supply price -
            the import term of actualEur, exposed so the card's composition
            reconciles with the result it shows.
        nettoErgebnisEur:
          type: number
          nullable: true
          description: einspeiseErloesEur + eigenverbrauchsWertEur - stromkostenEur.
        savedEur:
          type: number
          nullable: true
          description: >
            baselineEur - actualEur, i.e. what VoltPilot's steering contributed
            against an unregulated plant. It sits INSIDE nettoErgebnisEur and
            is rendered as an attribution line, never as another summand.
        arbitrageEur: { type: number, nullable: true }
        pvShiftEur: { type: number, nullable: true }
        savedSpeicherEur:
          type: number
          nullable: true
          description: >
            Der Speicher-Anteil der Dreiteilung savedEur = savedSpeicherEur +
            savedSteuerungEur: was der stur arbeitende Standard-Speicher (das
            Greedy-Referenzmodell der Ersparnis-Simulation) gegenüber der
            speicherlosen Anlage erwirtschaftet hätte - same semantics as the
            fleet endpoint's field. Null when savedEur is null or the battery
            master data is missing (steuerungSplitReason).
        savedSteuerungEur:
          type: number
          nullable: true
          description: >
            savedEur - savedSpeicherEur (exact remainder): der Mehrwert der
            intelligenten Steuerung gegenüber dem sturen Speicher;
            savedSpeicherEur + savedSteuerungEur == savedEur per construction.
        steuerungSplitReason:
          type: string
          nullable: true
          enum: [no_battery_data]
          description: >
            Why the split is absent although savedEur is computable
            (no_battery_data = no primary battery asset with maintained
            capacity + charge/discharge power); null when the split is present
            or savedEur itself is null.
        baselineEur: { type: number, nullable: true }
        actualEur: { type: number, nullable: true }
        marktpraemieEur:
          type: number
          nullable: true
          description: >
            The Marktprämie already CONTAINED in einspeiseErloesEur (shown as
            provenance, never added again); null without an anzulegender Wert
            or without a published Monatsmarktwert.
        bezugspreisCtKwh:
          type: number
          nullable: true
          description: stromkostenEur / bezogenKwh in ct/kWh; null without imported energy.
        realizedExportCtKwh: { type: number, nullable: true }
        marketValueSolarCtKwh: { type: number, nullable: true }
        marketValueProvisional: { type: boolean, nullable: true }
        bezogenKwh: { type: number, nullable: true }
        eingespeistKwh: { type: number, nullable: true }
        selbstverbrauchKwh: { type: number, nullable: true }
        batterieBewegtKwh: { type: number, nullable: true }
        gesamtertragEur:
          type: number
          nullable: true
          description: >
            Money-centric total = einspeiseErloesEur + eigenverbrauchsWertEur
            (the feed-in revenue alone for an "ohne" tariff) - the ONE number
            the cockpit money hero reads. Parity with the fleet twin so the
            cockpit renders from this cheaper site endpoint (audit
            vp-portal-perf-a4, finding B2).
        expectedMarketValueSolarCtKwh:
          type: number
          nullable: true
          description: >
            Forward expected Marktwert Solar (day-ahead price weighted with this
            site's own PV forecast over the coming horizon) - range-independent;
            null without forward PV/price coverage.
        expectedMarketValueFrom: { type: string, format: date-time, nullable: true }
        expectedMarketValueTo: { type: string, format: date-time, nullable: true }
        expectedMarketValueSlots:
          type: integer
          format: int64
          nullable: true
          description: Covered forward slot count behind expectedMarketValueSolarCtKwh.
        speicherDeltaKwh:
          type: number
          nullable: true
          description: >
            Das BESTANDSKONTO des Zeitraums: wie viel Energie am Ende MEHR (+)
            oder weniger (-) im Speicher steckt als am Anfang (Delta-Ladestand x
            Kapazitaet). Der gemessene Gegenpol zu savedEur, das als reine
            Zahlungsbilanz eine eingelagerte kWh nur als entgangenen
            Einspeise-Erloes kennt (Diagnose vp-tagesbild-minus-f3). Null ohne
            primaeren Speicher oder ohne gemessenen Ladestand - nie eine 0.
        speicherWertCtKwh:
          type: number
          nullable: true
          description: >
            Lambda - der vom Optimierer persistierte Wert einer GESPEICHERTEN
            kWh (Verluste und Verschleiss sind darin enthalten), womit
            speicherDeltaKwh bewertet wird.
        speicherWertEur:
          type: number
          nullable: true
          description: >
            speicherDeltaKwh x speicherWertCtKwh / 100. Ausdruecklich KEIN
            Summand von savedEur, sondern der zweite, als "nach dem Plan
            bewertet" beschriftete Posten daneben; beide Vorzeichen sind
            legitim (+ in den Folgetag gespeichert, - aus dem Vortag entnommen).
        speicherWertBasis:
          type: string
          nullable: true
          enum: [plan, terminal]
          description: >
            Womit bewertet wurde: "plan" = Lambda des massgeblichen Slots,
            "terminal" = der Terminalwert des Laufs (der FK2-Rueckfall).
        series:
          type: array
          description: >
            Money per Europe/Berlin bucket (hour for a day, day for week and
            month, month for year/all) - only computable buckets are listed.
          items:
            type: object
            properties:
              start: { type: string, format: date-time }
              einspeiseErloesEur: { type: number, nullable: true }
              eigenverbrauchsWertEur: { type: number, nullable: true }
              stromkostenEur: { type: number, nullable: true }
              nettoEur: { type: number, nullable: true }
        peakShaving:
          allOf: [{ $ref: "#/components/schemas/PeakShaving" }]
          nullable: true
          description: >
            The running billing period's peak-shaving proof; null unless the
            module is active. Range-INDEPENDENT, so the surface must label its
            own period and never add it into the period result.
    PeakShaving:
      type: object
      description: >
        Peak-shaving proof of a module-active site: the running billing
        period's measured grid-import peak vs. the counterfactual no-battery
        peak (both the max 15-min mean import from the telemetry rollups; the
        counterfactual re-adds the battery's dispatch per bucket -
        "dieselbe Anlage ohne Speichereinsatz"). avoidedKw =
        max(0, baselinePeakKw - peakKw); avoidedEur = avoidedKw ×
        leistungspreisEurKw, NOT pro-rated - the Leistungspreis bills the
        period's single highest 15-min mean, so mid-period the number is the
        current standing (final only once the period closes).
      properties:
        leistungspreisEurKw:
          type: number
          description: The configured Leistungspreis (EUR per kW per billing period).
        abrechnung:
          type: string
          enum: [jahr, monat]
          description: Billing period kind (Europe/Berlin calendar year or month).
        periodStart:
          type: string
          format: date
          description: First Berlin day of the running billing period.
        peakKw:
          type: number
          nullable: true
          description: >
            Measured peak of the running period (max 15-min mean grid import,
            kW). Null while the period has no measured import bucket yet.
        baselinePeakKw:
          type: number
          nullable: true
          description: Counterfactual no-battery peak of the running period (kW).
        avoidedKw:
          type: number
          nullable: true
          description: max(0, baselinePeakKw - peakKw).
        avoidedEur:
          type: number
          nullable: true
          description: avoidedKw × leistungspreisEurKw (the period's standing saving).
        history:
          type: array
          description: >
            The last 12 billing periods with measured import data (ascending,
            incl. the running one); the current contract price is applied to
            past periods too.
          items:
            type: object
            properties:
              periodStart: { type: string, format: date }
              peakKw: { type: number }
              baselinePeakKw: { type: number }
              avoidedKw: { type: number }
              avoidedEur: { type: number }
    EarningsTotals:
      type: object
      description: >
        Fleet totals over the computable sites; null money values when no site
        has a computable slot (never a fake zero).
      properties:
        baselineEur: { type: number, nullable: true }
        actualEur: { type: number, nullable: true }
        savedEur: { type: number, nullable: true }
        arbitrageEur:
          type: number
          nullable: true
          description: >
            Sum of the sites' arbitrage attributions; null when no site
            grid-charged in the window.
        pvShiftEur:
          type: number
          nullable: true
          description: >
            savedEur - arbitrageEur over the WHOLE fleet (sites without a
            split cannot grid-charge, so their entire saved is PV-shift);
            arbitrageEur + pvShiftEur == savedEur. Null whenever
            arbitrageEur is null.
        coveredSlots: { type: integer }
        firstCoveredDate:
          type: string
          format: date
          nullable: true
          description: Earliest covered Berlin day across the fleet.
    CreateSiteRequest:
      type: object
      required: [name]
      properties:
        name: { type: string, description: Site display name (required, non-blank). }
        biddingZone:
          type: string
          enum: [DE-LU, AT, CH]
          description: "Day-ahead bidding zone; defaults to 'DE-LU'."
        latitude:
          type: number
          nullable: true
          minimum: -90
          maximum: 90
          description: Optional WGS84 latitude; needed for the site's weather forecast.
        longitude:
          type: number
          nullable: true
          minimum: -180
          maximum: 180
          description: Optional WGS84 longitude.
        plantKind:
          type: string
          enum: [direktvermarktung, eigenverbrauch]
          description: "Anlagentyp; defaults to 'eigenverbrauch'."
        anzulegenderWertCtKwh:
          type: number
          nullable: true
          minimum: 0
          description: >
            Optional anzulegender Wert (ct/kWh, >= 0, at most 3 decimals) -
            the plant's fixed EEG reference rate; only meaningful for
            direktvermarktung sites. (The deprecated marktpraemieCtKwh is no
            longer accepted; old clients sending it are ignored.)
        tarifArt:
          type: string
          enum: [dynamisch, fest, ohne]
          description: >
            The customer's electricity tariff art (default 'ohne'). 'dynamisch'
            values self-consumption per 15-min slot at spot + Aufschlag, 'fest'
            at the fixed price, 'ohne' in kWh only.
        tarifParamCtKwh:
          type: number
          nullable: true
          minimum: 0
          description: >
            The tariff parameter (ct/kWh, >= 0, at most 3 decimals): fixed price
            for 'fest', optional Aufschlag for 'dynamisch', ignored (cleared)
            for 'ohne'. Omit/null = not configured.
        netzladenErlaubt:
          type: boolean
          nullable: true
          description: >
            Grid-charging switch, settable by the site owner and by
            Portal-Admins; omitted = the safe default false ("Nur
            Solarladen (EEG)").
        maxFeedInKw:
          type: number
          nullable: true
          exclusiveMinimum: 0
          description: >
            Optional static feed-in cap at the grid connection point (kW,
            strictly positive, at most 2 decimals); omit/null = no
            connection-point limit. Enforced export-only by the optimizer.
        standortId:
          type: string
          format: uuid
          nullable: true
          description: >
            Additiv (UEMS AP-02 IP-9): the Standort the new site belongs to; it is
            assigned from the day of its creation. Omitted: pre-selected when the
            Kundenbereich has exactly one Standort, no assignment when it has
            none, required (422 `standort_waehlen`) when it has several. Only
            `POST /api/v1/sites` reads it.
    UpdateSiteRequest:
      type: object
      required: [name]
      description: Same fields and validation as CreateSiteRequest.
      properties:
        name: { type: string, description: Site display name (required, non-blank). }
        biddingZone:
          type: string
          enum: [DE-LU, AT, CH]
          description: "Day-ahead bidding zone; defaults to 'DE-LU'."
        latitude: { type: number, nullable: true, minimum: -90, maximum: 90 }
        longitude: { type: number, nullable: true, minimum: -180, maximum: 180 }
        plantKind: { type: string, enum: [direktvermarktung, eigenverbrauch] }
        anzulegenderWertCtKwh: { type: number, nullable: true, minimum: 0 }
        tarifArt:
          type: string
          enum: [dynamisch, fest, ohne]
          description: >
            The customer's electricity tariff art (default 'ohne'; a
            full-representation field like plantKind, so an omitted value resets
            to 'ohne').
        tarifParamCtKwh:
          type: number
          nullable: true
          minimum: 0
          description: >
            The tariff parameter (ct/kWh): fixed price for 'fest', optional
            Aufschlag for 'dynamisch', cleared for 'ohne'.
        netzladenErlaubt:
          type: boolean
          nullable: true
          description: >
            Grid-charging switch, settable by the site owner and by
            Portal-Admins; null keeps the stored value (a form that omits
            the field never flips it).
        maxFeedInKw:
          type: number
          nullable: true
          exclusiveMinimum: 0
          description: >
            Static feed-in cap at the grid connection point (kW, strictly
            positive, at most 2 decimals); null keeps the stored value (a
            form that omits the field never clears the cap - the
            netzladenErlaubt pattern).
    SiteDeletionPreview:
      type: object
      description: What deleting a site would remove (drives the confirm dialog).
      properties:
        deviceCount: { type: integer, description: Devices still at the site; > 0 blocks the delete (409). }
        telemetryCount: { type: integer, format: int64 }
        telemetryFrom: { type: string, format: date-time, nullable: true }
        telemetryTo: { type: string, format: date-time, nullable: true }
        forecastCount: { type: integer, format: int64 }
        scheduleCount: { type: integer, format: int64 }
        weatherCount: { type: integer, format: int64 }
    UpdateDeviceRequest:
      type: object
      description: Only kind + label are editable; the externalRef is immutable.
      properties:
        kind: { type: string, enum: [inverter, battery, meter], nullable: true, description: Unchanged when omitted. }
        name: { type: string, nullable: true, maxLength: 120, description: Customer-facing label; blank/null removes it. }
    DeviceClaimRequest:
      type: object
      required: [siteId, externalRef]
      properties:
        siteId: { type: string, format: uuid }
        externalRef: { type: string, description: Edge device identifier (globally unique). }
        kind: { type: string, description: "Device kind; defaults to 'inverter'.", example: inverter }
    Device:
      type: object
      properties:
        id: { type: string, format: uuid }
        siteId: { type: string, format: uuid }
        externalRef: { type: string }
        kind: { type: string, example: inverter }
        name:
          type: string
          nullable: true
          description: Optional customer-facing label (Bezeichnung); the immutable externalRef stays the identity.
        status: { type: string, example: claimed }
        lastSeenAt:
          type: string
          format: date-time
          nullable: true
          description: >
            Newest status-heartbeat arrival for the box; legacy telemetry is the
            compatibility fallback until its first heartbeat after migration.
            The portal derives waiting/connected from it.
        createdAt:
          type: string
          format: date-time
          nullable: true
          description: >
            When the device was claimed. The portal escalates the "wartet auf
            erste Daten" copy to troubleshooting guidance once the wait since
            claiming exceeds a threshold.
        lanHost:
          type: string
          nullable: true
          example: "192.168.254.51:8484"
          description: >
            Die eigene Erreichbarkeit der Box im Kundennetz (Anlagen-Zentrale
            Stufe 2, D5) - NUR Anzeige, verbatim inklusive Port. null = die Box
            meldet sie (noch) nicht; NIE "nicht erreichbar".
        lanSeenAt:
          type: string
          format: date-time
          nullable: true
          description: Der Frische-Anker dieser Aussage; er reist immer MIT lanHost.
        lanSource:
          type: string
        fuehrtAnlage:
          type: boolean
          nullable: true
          description: >
            true genau fuer die fuehrende Box der Anlage, false fuer weitere Boxen;
            null solange die Rolle noch nicht bestimmt werden kann.
          nullable: true
          enum: [erreicht, schnittstelle]
          description: >
            Herkunft des Belegs - `schnittstelle` = der vom Docker-Host
            erkannte/konfigurierte Kundennetz-Endpunkt (oder die eigene
            Schnittstelle ohne Container); `erreicht` = kompatibler Fallback
            aus einem Browser-Aufruf, der auch über ein Service-VPN erfolgt
            sein kann.
    BelegeImWeg:
      type: object
      description: >
        409 of a hard delete path whose target is a Beleg (UEMS AP-07 E8/IP-11,
        AP-12 E13 S2/IP-12) - thrown before anything is written. `codes` names
        every reason in fixed order (`messstellen_belege`, `berichts_belege`),
        `code` is the first. Purge and Anlage entfernen: `messstellen` = the
        Messstellen whose series hang there, `berichtsstaende` = the released
        Berichtsstände (also superseded ones) citing those Messstellen - both
        lists in the same answer. A component (Komponente entfernen, Batterie am
        Standort abmelden, Verbraucher entfernen): `code` `berichts_belege`,
        `messstellen` = the cited Messstellen it fed, where the binding ends
        instead. Drafts protect nothing.
      required: [code, codes, message, messstellen, berichtsstaende]
      properties:
        code: { type: string, enum: [messstellen_belege, berichts_belege] }
        codes:
          type: array
          minItems: 1
          items: { type: string, enum: [messstellen_belege, berichts_belege] }
        message: { type: string, description: "Kundensatz, z. B. „Diese Komponente ist Beleg in 4 freigegebenen Berichtsständen (BR-2026-0001 Nr. 1, …). Löschen ist nicht möglich — beenden Sie die Bindung stattdessen.“" }
        messstellen:
          type: array
          items:
            type: object
            required: [id, kennzeichen, name]
            properties:
              id: { type: string, format: uuid }
              kennzeichen: { type: string }
              name: { type: string }
        berichtsstaende:
          type: array
          items:
            type: object
            required: [kennung, nr]
            properties:
              kennung: { type: string, description: "BR-<Jahr>-<Nr.>" }
              nr: { type: integer, minimum: 1 }
    DevicePurgeResult:
      type: object
      properties:
        deviceId: { type: string, format: uuid }
        purgedRows:
          type: integer
          format: int64
          description: Raw telemetry rows removed (the derived rollups are rebuilt, not counted).
        purgedBefore:
          type: string
          format: date-time
          description: >
            The purge watermark. Samples observed at or before this instant are
            refused by the ingest path from now on; new data flows normally.
        deviceNotified:
          type: boolean
          description: >
            Whether the retained purge_data command reached the broker. When
            false (broker outage), the cloud data is still gone and replays are
            blocked; the device's local buffer is wiped once the command can be
            delivered.
    ProvisionDeviceRequest:
      type: object
      required: [externalRef]
      properties:
        externalRef:
          type: string
          description: Sticker Geräte-ID (prefix VP-, canonicalized to uppercase).
          example: VP-1234-ABCD
        kind: { type: string, description: "Device kind; defaults to 'inverter'.", example: inverter }
        note: { type: string, nullable: true, description: Free-form operator note (e.g. manufacturing batch). }
    EdgeRelease:
      type: object
      description: >
        One entry of the edge release register (OTA Stufe 0, decision D5;
        signature fields added in Stufe 1).
      properties:
        releaseSeq:
          type: integer
          format: int64
          description: THE ordering - a monotonic integer, never a string/SHA comparison.
        version: { type: string, example: edge-2026.08.0 }
        targetCommit: { type: string, nullable: true, description: The commit it was built from - the paper trail into the repo. }
        notes: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        createdBy: { type: string, nullable: true, description: The registering platform-admin's subject. }
        manifest:
          type: string
          nullable: true
          description: >
            The EXACT bytes of the signed release.json, verbatim (OTA Stufe 1).
            Stored as text and never re-serialized: the signature is over these
            bytes, so any normalisation (a jsonb column, a formatter, a copy out
            of a log) makes it silently unverifiable. Null on a Stufe-0 hand
            entry, which then honestly reads as "not signed".
        signature:
          type: string
          nullable: true
          description: The detached signature file (release.json.sig), verbatim.
        signingKeyId:
          type: string
          nullable: true
          description: The release key that signed the manifest, e.g. rel-2026-a.
    EdgeTrustSet:
      type: object
      description: The current trust-set plus the operator metadata (admin view).
      properties:
        trustSet: { type: string }
        signature: { type: string }
        keyIds:
          type: string
          example: rel-2026-a,rel-2026-b
          description: >
            DERIVED, display only: the RELEASE key ids this set trusts, sorted
            and comma-separated. The truth stays the raw bytes.
        signingKeyId:
          type: string
          example: root-2026-a
          description: The ROOT key that signed the set - never one of keyIds (see the PUT).
        generatedAt:
          type: string
          nullable: true
          description: What the DOCUMENT claims about itself (optional in the contract).
        uploadedAt: { type: string, format: date-time }
        uploadedBy: { type: string, nullable: true }
    SaveEdgeTrustSetRequest:
      type: object
      required: [trustSet, signature]
      properties:
        trustSet:
          type: string
          description: The exact bytes of trust-set.json.
        signature:
          type: string
          description: The exact bytes of trust-set.json.sig.
    CreateEdgeReleaseRequest:
      type: object
      required: [version]
      properties:
        version: { type: string, example: edge-2026.08.0, description: "Must match edge-YYYY.MM.N." }
        releaseSeq:
          type: integer
          format: int64
          nullable: true
          description: >
            Omit to continue the register (max + 1). If given, it must be
            strictly greater than every existing entry.
        targetCommit: { type: string, nullable: true }
        notes: { type: string, nullable: true }
        manifest:
          type: string
          nullable: true
          description: >
            The signed release.json, byte for byte (OTA Stufe 1). When present,
            ITS values win: version, releaseSeq and targetCommit are taken from
            it, and a contradicting value in this body is a 400 rather than a
            silent divergence - the register must never claim something other
            than what was signed. Requires `signature`.
        signature:
          type: string
          nullable: true
          description: >
            The detached signature file (release.json.sig), byte for byte.
            Manifest and signature belong together - sending only one is a 400.
        signingKeyId:
          type: string
          nullable: true
          description: >
            Optional cross-check; must equal the key named by the manifest and
            by the signature file.
    ProvisionedDevice:
      type: object
      properties:
        externalRef: { type: string, example: VP-1234-ABCD }
        kind: { type: string, example: inverter }
        note: { type: string, nullable: true }
        provisionedAt: { type: string, format: date-time }
        claimed: { type: boolean, description: Whether any tenant has connected this device. }
        claimedByTenant:
          type: string
          nullable: true
          description: Name of the claiming tenant (null while unclaimed).
    PendingEnrollment:
      type: object
      description: A device that enrolled (uploaded a CSR) but has no claimed device row.
      properties:
        externalRef: { type: string, example: edge-k7m2xqp }
        deviceInfo: { type: string, nullable: true, description: Free-form device info from the CSR upload. }
        csrUpdatedAt: { type: string, format: date-time, description: When the pending CSR was last (re)uploaded. }
        everIssued:
          type: boolean
          description: >
            True when a certificate was once issued and the device was later
            unclaimed (a stale enrollment), false when never claimed at all.
        issuedAt: { type: string, format: date-time, nullable: true, description: When a certificate was issued (null if never). }
    TelemetryPoint:
      type: object
      properties:
        ts: { type: string, format: date-time }
        powerKw: { type: number }
        socPct: { type: number }
        pvPowerKw: { type: number }
        loadKw: { type: number }
        gridLimitKw:
          type: number
          description: Observed effective §14a power limit.
    PriceSeries:
      type: object
      description: Day-ahead spot prices for a bidding zone (market-wide, not per tenant).
      properties:
        biddingZone: { type: string, example: DE-LU }
        resolution:
          type: string
          nullable: true
          description: ISO-8601 slot resolution of the latest data.
          example: PT15M
        currency: { type: string, example: EUR }
        points:
          type: array
          items: { $ref: "#/components/schemas/PricePoint" }
    PricePoint:
      type: object
      properties:
        ts: { type: string, format: date-time, description: Slot start (UTC). }
        end: { type: string, format: date-time, description: Slot end (exclusive). }
        priceEurMwh: { type: number, description: Spot price in EUR/MWh. }
    PriceHistory:
      type: object
      description: Day-ahead prices aggregated over a historical range (market-wide per zone).
      properties:
        biddingZone: { type: string, example: DE-LU }
        currency: { type: string, example: EUR }
        bucket:
          type: string
          description: Aggregation granularity as ISO-8601 duration (PT15M day, PT1H week, P1D month/year).
          example: P1D
        from: { type: string, format: date-time, description: Window start (inclusive, UTC). }
        to: { type: string, format: date-time, description: Window end (exclusive, UTC). }
        buckets:
          type: array
          items: { $ref: "#/components/schemas/PriceBucket" }
        summary: { $ref: "#/components/schemas/PriceRangeSummary" }
    PriceBucket:
      type: object
      properties:
        ts: { type: string, format: date-time, description: Bucket start (UTC). }
        avgEurMwh: { type: number, nullable: true, description: Mean price over the bucket (EUR/MWh). }
        minEurMwh: { type: number, nullable: true, description: Lowest slot price in the bucket (EUR/MWh). }
        maxEurMwh: { type: number, nullable: true, description: Highest slot price in the bucket (EUR/MWh). }
    PriceRangeSummary:
      type: object
      description: Headline stats over the raw slots in the range window.
      properties:
        avgEurMwh: { type: number, nullable: true }
        minEurMwh: { type: number, nullable: true }
        maxEurMwh: { type: number, nullable: true }
        cheapestTs: { type: string, format: date-time, nullable: true, description: Start of the cheapest slot. }
        mostExpensiveTs: { type: string, format: date-time, nullable: true, description: Start of the most expensive slot. }
        count: { type: integer, description: Number of raw price slots in the window. }
        coverageStart: { type: string, format: date-time, nullable: true, description: Earliest slot with data. }
        coverageEnd: { type: string, format: date-time, nullable: true, description: Latest slot with data. }
    WeatherForecast:
      type: object
      properties:
        runAt:
          type: string
          format: date-time
          nullable: true
          description: Issue time of the forecast run (null if none stored yet).
        points:
          type: array
          items: { $ref: "#/components/schemas/WeatherPoint" }
    WeatherPoint:
      type: object
      properties:
        ts: { type: string, format: date-time, description: Forecast target hour (UTC). }
        temperatureC: { type: number, nullable: true, description: 2 m air temperature. }
        cloudCoverPct: { type: number, nullable: true, description: Total cloud cover 0..100. }
        ghiWM2:
          type: number
          nullable: true
          description: Shortwave / global-horizontal irradiance (W/m²), PV-relevant.
        dniWM2: { type: number, nullable: true, description: Direct (beam) radiation (W/m²). }
        dhiWM2: { type: number, nullable: true, description: Diffuse radiation (W/m²). }
    Schedule:
      type: object
      properties:
        planId: { type: string, format: uuid, nullable: true }
        deviceId: { type: string, format: uuid, nullable: true, description: "Executing battery device (the MQTT schedule topic's device)." }
        generatedAt: { type: string, format: date-time, nullable: true }
        slotMinutes: { type: integer, example: 15 }
        savingsEur:
          type: number
          nullable: true
          description: >-
            Projected savings over the horizon vs. leaving the battery idle
            (the no-battery baseline). Null when the run makes no such
            statement - since P7 that is any run whose socSource is
            `unbekannt`: no measured start SoC means no battery was planned, so
            there is no baseline to measure a saving against. Null is "nothing
            planned", never a claimed 0,00 EUR.
        bankedValueEur:
          type: number
          nullable: true
          description: >-
            Value of the energy the plan banks into (positive) or draws out of
            (negative) the horizon: the run's persisted terminal value per
            stored kWh times the SoC swing (socEndPct - socStartPct). Makes
            savingsEur honest on bank days, where storing into the next day
            reads as negative savings. Null when not computable (runs predating
            the terminal-value column, no battery asset) - never a fabricated 0.
        socStartPct:
          type: number
          nullable: true
          description: Battery state of charge at the plan start (the SoC the banked value is measured from).
        socEndPct:
          type: number
          nullable: true
          description: Planned state of charge at the horizon end.
        peakTargetKw:
          type: number
          nullable: true
          description: >-
            The run's planned billing-period grid-import peak target (PS-1,
            schedule.peak_target_kw) - the "Ziel" a Lastspitzen (peak-shaving)
            Anlage defends. Null when the site runs no peak-shaving module or on
            a run that predates the column.
        effectiveFloorSocPct:
          type: number
          nullable: true
          minimum: 0
          maximum: 100
          description: "Full floor for corrective idle-slot discharge: max(technical minimum, backup reserve, peak reserve). Null on pre-feature runs."
        fallback14a:
          type: boolean
          nullable: true
          description: >-
            Fahrplan-Warum (schedule.fallback_14a): true = this run is the
            advisory build without the infeasible §14a grid-limit constraint
            (edge guards + grid operator enforce the physical limit) - the
            §14a copy switches to the fallback wording. Null on pre-feature
            runs or runs whose explain layer was off.
        whyTerminalAnchor:
          type: string
          nullable: true
          description: >-
            Erklaerbarkeit Stufe 1 (schedule.why_terminal_anchor, migration
            V20260824000000): WHICH branch anchored the value of stored energy -
            einspeisewert (a surplus slot's forgone feed-in) | bezugspreis (a
            deficit slot's avoided grid import - the trueb-horizon case) |
            marktpreis (cheapest refill where grid charging is allowed) |
            vorgabe (env-pinned, nothing derived). Additively extensible; a
            consumer that does not know a word IGNORES it rather than guessing.
            Null when the explain layer was off or on pre-feature runs - the
            surfaces then stay observational.
        whyRefillFreePct:
          type: number
          nullable: true
          description: >-
            Erklaerbarkeit Stufe 1 (schedule.why_refill_free_pct): how much of
            the battery's USABLE band the horizon refills for FREE (surplus in
            slots whose export value is <= 0), 0-100 - the summer statement
            "morgen fuellt Ihr Ueberschuss den Speicher ohnehin". Null when not
            evaluable (no usable band) - never a fabricated 0, which would read
            as "the horizon offers nothing".
        whyNightReserveKwh:
          type: number
          nullable: true
          description: >-
            Nacht-Wertfunktion P3 (schedule.why_night_reserve_kwh, migration
            V20260868000000): how much charge this run holds at SUNRISE, above
            the reserve floor, because the site's OWN night-error history makes
            a heavier night than the forecast plausible (kWh). NOT a fixed
            reserve - the plan holds exactly as much as price spread times error
            probability justifies, so it moves with every run. Null when nothing
            was held back (too little night history, no price spread, a winter
            horizon without a sunrise, the OPTIMIZER_NIGHT_RESERVE_ENABLED
            switch off, or a pre-feature run) - never a fabricated 0.
        whyNightReserveQ:
          type: number
          nullable: true
          description: >-
            The quantile of that amount (0.75 = it is needed in 1 of 4 nights) -
            the SECOND half of one statement. Without it the kWh figure stands
            without its frequency, and the customer sentence is omitted. Null
            exactly when whyNightReserveKwh is null.
        socSource:
          type: string
          nullable: true
          enum: [gemessen, berechnet, unbekannt]
          description: >-
            P7 (Scout vp-deye-diybms-luecke-l5 §3.3, schedule.soc_source,
            migration V20260910000000): WHERE this run's start state of charge
            came from - `gemessen` (real soc_pct telemetry inside the freshness
            window), `berechnet` (a DERIVED state of charge; the generic SoC
            building block ships in its own package - it may feed planning, but
            it never passes itself off as a measurement) or `unbekannt`.
            On `unbekannt` the optimizer planned NO battery at all: every slot's
            batteryKw is 0, socPct and baselineCostEur are null, savingsEur is
            null, and the Fahrplan page says why ("ohne Ladestand keine
            Speicherplanung") instead of drawing a flat 50 % line nobody
            measured. Null = a run predating the column, read as `gemessen`
            (pre-P7 behaviour), NEVER as `unbekannt`; the day splice carries
            null too, because it is stitched from many runs and has no single
            origin. Additively extensible; a consumer that does not know a word
            IGNORES it rather than guessing.
        slots:
          type: array
          items:
            type: object
            properties:
              start: { type: string, format: date-time }
              batteryKw: { type: number, description: "Planned battery power; + = charge, - = discharge." }
              gridKw: { type: number, description: "Projected net grid power; + = import, - = export." }
              socPct:
                type: number
                nullable: true
                description: >-
                  Planned state of charge at slot end. Null on a run without a
                  start SoC (socSource `unbekannt`, P7): the solver needs some
                  start value to stay solvable, but that value is a model
                  placeholder and never leaves it.
              priceEurMwh: { type: number }
              costEur: { type: number, description: "Projected slot cost with the plan." }
              baselineCostEur:
                type: number
                nullable: true
                description: >-
                  Projected slot cost with the battery idle - the reference the
                  planned saving is measured against. Null on a run without a
                  start SoC (socSource `unbekannt`, P7): no battery was planned,
                  so there is nothing to compare, and a 0,00 EUR would claim
                  "planned and worth nothing" instead of "not planned".
              curtailKw: { type: number, nullable: true, description: "Planned PV curtailment for the slot (kW held back, >= 0); the optimizer curtails feed-in at negative prices. Null on pre-curtailment runs." }
              pvKw: { type: number, nullable: true, description: "PV forecast the slot planned with (kW, schedule.pv_kw). Feeds the pv-aware 'Laden aus dem Netz' derivation: grid-charging = charge beyond the slot's available PV (FK3 PV-bus semantics), not merely charging while importing. Null on rows without a persisted PV input." }
              loadKw: { type: number, nullable: true, description: "Load (consumption) forecast the slot planned with (kW, schedule.load_kw). Rendered with pvKw as the two forecast lines over the Fahrplan bars, so the plan explains itself. Null on rows without a persisted load input - the line is absent then, never a fabricated 0." }
              slotRole: { type: string, nullable: true, description: "Fahrplan-Warum role id (schedule.slot_role): warten | pv_speichern | guenstig_laden | eigenverbrauch | verkaufen | spitze_kappen | reserve_halten | abregeln (additively extensible). Null on pre-feature rows - the portal renders its chargeKind fallback then, never a fabricated explanation." }
              slotFlags: { type: array, nullable: true, items: { type: string }, description: "Binding-constraint codes (schedule.slot_flags CSV, split): soc_max, soc_floor, reserve_backup, reserve_peak, charge_cap, discharge_cap, solar_only, grid_limit_14a, feed_in_cap, peak_defining, curtailing. Null = no binding recorded / pre-feature row." }
              storedValueCtKwh: { type: number, nullable: true, description: "The EXACT value of a stored kWh at slot end (the run's SoC shadow price, ct/kWh, rounded 0.1 ct - schedule.stored_value_ct_kwh). Customer copy calls it 'Wert gespeicherter Energie'. Null on pre-feature rows." }
              gridValueCtKwh: { type: number, nullable: true, description: "Effective per-kWh energy value at the grid connection point (ct/kWh, schedule.grid_value_ct_kwh); deviates from the tariff exactly when §14a or the peak module deforms the slot's economics. Null on pre-feature rows." }
              peakPressureEurKw: { type: number, nullable: true, description: "The Leistungspreis allocation on this slot (EUR/kW, schedule.peak_pressure_eur_kw; sums to Leistungspreis + ratchet over the horizon). Null when the site runs no peak-shaving module or on pre-feature rows." }
              importPriceCtKwh: { type: number, nullable: true, description: "P0 Textwahrheit: what one IMPORTED kWh really costs the site in this slot (ct/kWh) - the price the optimizer decided with, not the bare spot in priceEurMwh. Recomposed (not persisted) by the api from spot + the site's tariff/Preisblatt via optimizer/SlotEconomics, the pricing.py twin. Null when the slot has no persisted spot price (a flat 'fest' tariff needs none); the portal degrades to a number-free sentence then and never labels spot as 'Netzstrom'." }
              exportValueCtKwh: { type: number, nullable: true, description: "P0 Textwahrheit: what one EXPORTED kWh really earns in this slot (ct/kWh) - spot + dynamic Marktprämie (Direktvermarktung) or the feste Vergütung (Eigenverbrauch), merchant mode bare spot. Recomposed like importPriceCtKwh; null when not computable." }
              importPriceSource: { type: string, nullable: true, description: "Which rule priced the import: fest | preisblatt | sammelaufschlag | default-flag | spot. A SITE-level fact, identical on every slot of a run; drives the sentence's price breakdown ('Börsenpreis X + Netzentgelte/Abgaben Y')." }
              measuredLoadKw: { type: number, nullable: true, description: "P3 Ist-Last: the MEASURED house consumption of this slot (kW) - the quarter-hour MEAN of telemetry.load_kw, i.e. the same quantity loadKw forecasts. Present only for slots that already happened; the RUNNING slot carries the mean of the samples measured so far. Null for future slots, slots without telemetry and sites reporting no load channel - the portal's solid Ist line is then absent, never a fabricated 0." }
              measuredPvKw: { type: number, nullable: true, description: "Ist-PV (the mirror of measuredLoadKw): the MEASURED PV production of this slot (kW) - the quarter-hour MEAN of telemetry.pv_power_kw, aggregated in the same pass/window as the measured load, i.e. the same quantity pvKw forecasts. It makes the PV forecast error visible and the EEG solar-charging rule ('Laden <= gemessene PV') checkable in the chart. Null for future slots, slots without telemetry and sites whose device reports no PV channel - the line is then absent, never a fabricated 0." }
              coverLoadFromBattery: { type: boolean, nullable: true, description: "Duty-Vorschau (schedule.cover_load_from_battery, migration V20260802010000): true = in THIS slot the device TRACKS the measured house deficit max(load - pv, 0) instead of executing the slot's forecast-derived watt value rigidly, i.e. the plan's batteryKw is a FORECAST here, not a command. Same boolean the frozen MQTT schedule contract carries; served so the Fahrplan can say it BEFORE the slot runs. TRI-STATE: null = not evaluated (pre-feature run / duty switch off), false = evaluated and no duty - the portal marks a phase only on an explicit true." }
              unplannedLoadDischarge: { type: boolean, nullable: true, description: "Additive idle-slot authority: true permits fresh measured load coverage to START from a 0-kW plan slot when full marginal economics favor discharge. It never redefines coverLoadFromBattery and never applies to a planned charge or sale. Null = pre-feature/not evaluated; false = evaluated and intentionally held." }
              whyNextBest: { type: string, nullable: true, description: "Erklaerbarkeit Stufe 1 (schedule.why_next_best, migration V20260824000000): the best action this RESTING slot REJECTED - decken | verkaufen | solar_speichern | netzladen. Only physically ADMISSIBLE alternatives are named (charging needs SoC headroom and, in EEG mode, real PV surplus; netzladen needs the grid-charge permission), so a margin never describes an impossible action. Null on ACTIVE slots (the chosen action's marginal benefit is 0 at the optimum), when nothing else was possible, and on runs whose explain layer was off." }
              whyNextBestMarginCt: { type: number, nullable: true, description: "Erklaerbarkeit Stufe 1 (schedule.why_next_best_margin_ct): how much WORSE that rejected alternative would have been, in ct/kWh - always <= 0. |margin| <= 0.05 is a TIE and the surface must say so ('beides waere gerade fast gleich gut') instead of dressing an abwaegung up as certainty; on 17.08.2026 it was algebraically exactly 0,000 ct/kWh. Null exactly where whyNextBest is null - name and margin are ONE statement." }
              chargeFromSurplusOnly: { type: boolean, nullable: true, description: "Duty-Vorschau, charge-side mirror (schedule.charge_from_surplus_only, migration V20260802010000): true = in THIS slot the device clamps the commanded CHARGE to the measured solar surplus max(pv - load, 0) because buying the shortfall would be uneconomic - the plan's batteryKw is a forecast here too. Same tri-state discipline as coverLoadFromBattery." }
    ControlStatus:
      type: object
      description: Latest inverter-control confirmation for a site (report §5).
      properties:
        deviceId: { type: string, format: uuid }
        commandedKw: { type: number, nullable: true, description: "What the DEVICE regulates on. Since the in-slot duties this is the CORRECTED value, not the plan's watt value - executionMode/-Direction say why it differs." }
        confirmedKw: { type: number, nullable: true, description: "Setpoint the inverter read back." }
        allMatch: { type: boolean, description: "True when every controlled register confirmed its commanded value." }
        controlEnabled: { type: boolean, description: "Whether the device's control kill-switch is on." }
        certified: { type: boolean, description: "Whether the model is bench-certified for control." }
        mismatchRoles: { type: string, nullable: true, description: "Comma-separated roles that did not confirm (e.g. \"battery_power\")." }
        slotStart: { type: string, format: date-time, nullable: true }
        checkedAt: { type: string, format: date-time, description: "When the readback was taken (\"geprüft vor X\")." }
        controlSource: { type: string, nullable: true, enum: [schedule, default], description: "The device's COARSE truth: is a fresh plan driving it at all. It collapses every non-schedule mode (self-consumption fallback, a v2 desired holding the battery, calibration) into `default`, so it must never be rendered as \"the built-in safety rule is running\" - executionMode is the precise signal. Null for a pre-PR-3 row." }
        executionMode: { type: string, nullable: true, enum: [plan, follow, trim, absorb, fallback, idle_follow, high_soc_follow, high_soc_charge, autonomous_discharge], description: "Diagnostic execution path. idle_follow is the economically authorized portable exact-setpoint correction; high_soc_follow and high_soc_charge are the bounded discharge/refill halves of the full-battery top band; autonomous_discharge is the separately exact-model/firmware-certified native charge-block path. Customer UI translates these to German and never exposes the codes." }
        executionDirection: { type: string, nullable: true, enum: [deepen, reduce], description: "Only for executionMode `follow`: the discharge was RAISED to cover the house (`deepen`) or LIMITED to what it needs (`reduce`). The idle path is already unambiguous and does not expose this detail. Null otherwise." }
        executionPlannedKw: { type: number, nullable: true, description: "The setpoint BEFORE an in-slot correction, so plan and execution can stand side by side instead of being two contradicting numbers under one word." }
        executionTargetKw: { type: number, nullable: true, description: "The MEASURED value the correction tracks - the house deficit for follower modes, the PV surplus for trim/absorb; which one follows unambiguously from executionMode. Null when the device could not measure it (it never regulates blind, and never reports blind either) - never a fabricated 0." }
        executionFloorSocPct: { type: number, nullable: true, minimum: 0, maximum: 100, description: "Full technical/backup/peak reserve floor applied by idle correction." }
        executionMeasurementsFresh: { type: boolean, nullable: true, description: "True only when the idle correction started from fresh known SOC/load/PV. Null for other paths or older edges." }
        certSource: { type: string, nullable: true, enum: [env, device, platform], description: "WHICH source granted the certification: `env` = the fleet-wide allowlist, `device` = a First-Light grant on that box, `platform` = the model register. Null = not certified, or an older edge - so an absent value must never be read as a claim about the source." }
        platformCertVerdict: { type: string, nullable: true, enum: [granted, covered_not_activated, not_covered, unknown], description: "What the PLATFORM model register says about this device's selected inverter. `covered_not_activated` = the model IS certified and one deliberate click is missing; `not_covered` = a bench run is genuinely needed. NULL is \"we do not know\" (an older edge, or one that never received a cloud document) and must NEVER be collapsed into `not_covered` - that would send a customer to a bench they do not need." }
        platformCertModel: { type: string, nullable: true, description: "The register entry that matched, so a surface can name WHICH model is covered/waiting." }
        platformCertReason: { type: string, nullable: true, description: "Plain-German cause where a covered-looking model still gets nothing (today: a contradicted bench write-sign convention)." }
    ControlCertification:
      type: object
      description: One bench-certified inverter model in the platform register.
      properties:
        brand: { type: string, description: "Catalog brand, stored lower-case." }
        model: { type: string, description: "Catalog model - the KEY, together with brand. A family is not a model: one bench run must never certify untested siblings." }
        family: { type: string, description: "The model's register family. CHECKED by the device, not derived - the same model under another register map is not what was proven." }
        controlPath: { type: string, nullable: true, enum: [remote, tou], description: "The control surface the bench run drove; it seeds Layer 1's sticky path decision. Null = the run states none." }
        invertControlSign: { type: boolean, nullable: true, description: "The bench-proven WRITE sign convention, which is firmware-dependent - so the DEVICE checks it against its own setting and fails closed on a contradiction. NULL and false are different statements: null = the bench did not answer the question (nothing is checked), false = it did." }
        certifiedAt: { type: string, format: date-time, description: "When the bench run happened (the substantive truth), separate from createdAt." }
        firmwareNote: { type: string, nullable: true, description: "The firmware it was proven on, as PLAIN TEXT for the operator - deliberately NOT a machine gate: the box cannot read a firmware version from the inverter, so a firmware window would be a promise nobody can check." }
        note: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        createdBy: { type: string }
    CertifyControlModelRequest:
      type: object
      required: [brand, model, family]
      properties:
        brand: { type: string, maxLength: 64 }
        model: { type: string, maxLength: 128 }
        family: { type: string, maxLength: 64 }
        controlPath: { type: string, enum: [remote, tou] }
        invertControlSign: { type: boolean, nullable: true, description: "Omit when the bench run did not answer the sign question - omitted means the device checks nothing, false means it was proven not-inverted." }
        certifiedAt: { type: string, format: date-time, description: "Defaults to now when omitted." }
        firmwareNote: { type: string, maxLength: 512 }
        note: { type: string, maxLength: 512 }
    ControlActivation:
      type: object
      description: A plant whose battery control has been deliberately armed.
      properties:
        deviceId: { type: string, format: uuid }
        tenantId: { type: string, format: uuid }
        siteId: { type: string, format: uuid }
        siteName: { type: string }
        tenantName: { type: string }
        externalRef: { type: string }
        activatedAt: { type: string, format: date-time }
        activatedBy: { type: string, description: "The JWT subject that clicked - the paper trail." }
        note: { type: string, nullable: true }
    CurtailmentStatus:
      type: object
      description: >
        Latest FEED-IN CURTAILMENT truth a site's device(s) reported (the
        additive `curtailment` heartbeat block). It is what turns the Fahrplan's
        "Abregeln" slot from a claim into a verifiable statement: the optimizer
        plans the PV cap, but whether the plant throttles depends on a manual
        per-unit release of the curtailment actor. Absent (204) = an older edge
        or a plant without a curtailment actor - consumers keep their PLAN
        wording then, never a fabricated execution claim.
      properties:
        deviceId: { type: string, format: uuid }
        units: { type: integer, description: "Curtailment-capable units the device has configured." }
        certifiedUnits: { type: integer, description: "Units carrying a per-unit First-Light release. certifiedUnits < units is THE actionable cause the portal names (\"0 von 2 Wechselrichtern freigegeben\")." }
        controlEnabled: { type: boolean, description: "The device's control kill-switch." }
        active: { type: boolean, description: "At least one unit currently APPLIES a cap." }
        appliedCapKw: { type: number, nullable: true, description: "Summed applied caps; null = none applied (never a fabricated 0)." }
        allMatch: { type: boolean, nullable: true, description: "Over the APPLYING units' readbacks; null = nothing applied. Only true is a confirmation - null must never be read as a disagreement." }
        possibleOverride: { type: boolean, description: "A unit's MEASURED power exceeds its cap after the settle window, so a foreign controller may be overriding the limit (Modbus has the lowest priority on Fronius)." }
        checkedAt: { type: string, format: date-time, description: "Newest per-unit readback timestamp - the freshness anchor. A stale block is NO evidence; the portal falls back to its plan wording." }
        exportGuard: { allOf: [{ $ref: "#/components/schemas/ExportGuard" }], nullable: true, description: "„Grenzen & Wächter\" Stufe 0: the live feed-in watchdog. null = not reported (older edge, or no feed-in limit configured) - never a fabricated \"no limit\"." }
        deviceExportLimit: { allOf: [{ $ref: "#/components/schemas/DeviceExportLimit" }], nullable: true, description: "The feed-in limit the INVERTER ITSELF holds. null = not reported (older edge, a family without a trustworthy register, or not read yet)." }
        perUnit:
          type: array
          nullable: true
          items: { $ref: "#/components/schemas/CurtailmentUnit" }
          description: >
            The per-unit breakdown (Geräteseiten Stufe 1 / R4a, Captain-Entscheid
            E2). THREE-valued, and the two empty cases say different things:
            null/absent = not loaded on this path (an older backend, or the fleet
            aggregate which renders no per-unit list), [] = the device reported
            none (an older edge - the block predates the list). Only a non-empty
            list is an attribution. It may be SHORTER than `units`: an entry
            without a source id is dropped at ingest rather than attributed to
            nothing, so `units` stays THE count and must never be derived from
            this list's length.
    CurtailmentUnit:
      type: object
      description: >
        One curtailment-capable unit as the BOX reported it. Deriving this in
        the cloud was rejected (E2): only the box knows which unit it wrote to
        and what came back, so a heuristic would be a fabricated attribution.
      properties:
        sourceId: { type: string, description: "The JOIN KEY to the reported sources (/sources.sourceId) - how a unit becomes a device NAME. No name travels over the wire; the portal names devices through its one name builder." }
        certified: { type: boolean, description: "This unit carries a persisted per-unit First-Light release. The edge sources it from its CORE, never from the readback's own stamp, so it can never disagree with certifiedUnits." }
        appliedCapKw: { type: number, nullable: true, description: "The cap THIS unit applies; null = none applied (a release lifted the limit) - never a fabricated 0." }
        match: { type: boolean, nullable: true, description: "This unit's readback verdict; null = observed-only, nothing was commanded. Only true is a confirmation - \"nothing applied\" must never read as \"the readback disagreed\"." }
    ExportGuard:
      type: object
      description: >
        The live feed-in watchdog ("dynamische Einspeisebegrenzung") as the
        device reports it in every heartbeat. The box has been sending this
        block since it was built and the cloud read none of it, so the question
        "which feed-in limit does the box hold, and does it reach any device at
        all?" was only answerable through a maintenance tunnel - it cost two
        investigation rounds at Anlage Herzogau. `effective` is the field the
        block exists for: a watchdog that computes a perfect cap and writes it
        NOWHERE must say so instead of letting a plant believe in a protection
        it does not have.
      properties:
        limitKw: { type: number, description: "The feed-in limit at the grid connection point AS THE BOX KNOWS IT." }
        state: { type: string, enum: [ueberwacht, regelt, haelt, zieht_zusammen, sicherheitskappe], description: "The machine-readable word NEXT TO the German sentence; a word outside this vocabulary is dropped at ingest together with its whole block." }
        reason: { type: string, nullable: true, description: "The device's own German sentence for exactly that state - written ONCE on the device, so the :8484 card and the portal can never word the same verdict differently." }
        capKw: { type: number, nullable: true, description: "The plant-level PV cap the watchdog currently commands; null = none (never a fabricated 0)." }
        limiting: { type: boolean, description: "The cap is actually holding the producers back right now." }
        blind: { type: boolean, description: "The verdict was NOT formed from a fresh connection-point measurement (hold / contract / safe cap)." }
        effective: { type: boolean, description: "false = the cap can reach NO device at all." }
        reach: { type: string, nullable: true, description: "Names the gap in German; null when the cap reaches every unit." }
    DeviceExportLimit:
      type: object
      description: >
        The feed-in limit the INVERTER ITSELF holds - a foreign truth inside the
        customer's device, READ (never written) by the box. At Anlage Herzogau
        the Deye held an installer cap of 33,0 kW in register 0x00E7 while 70 kW
        were configured in the portal; the discrepancy was invisible for two
        investigation rounds because nobody read the register.
      properties:
        limitKw: { type: number, description: "The limit the device currently holds." }
        register: { type: string, description: "WHERE it came from (\"0x00e7\") - a number without its origin is not evidence." }
        readAt: { type: string, format: date-time, description: "Its OWN freshness anchor: the register is read at most once a day (one socket, no extra poll cadence), so it must never borrow checkedAt." }
    SiteSource:
      type: object
      description: >
        One measurement point of a site as the edge reports it. Absent
        measurements stay null (never a fabricated 0); a point that is stale or
        has never delivered says so via `health` instead of vanishing.
      properties:
        deviceId: { type: string, format: uuid, description: "The edge device that reported this point." }
        sourceId: { type: string, description: "Edge-local id (\"inverter\" for the primary)." }
        kind: { type: string, enum: [primary, source] }
        role: { type: string, nullable: true, description: "pv-generation | grid-meter | consumer; null for the primary." }
        label: { type: string, nullable: true }
        brand: { type: string, nullable: true }
        model: { type: string, nullable: true }
        pvKw: { type: number, nullable: true, description: "This point's OWN PV generation." }
        powerKw: { type: number, nullable: true, description: "Signed grid power, + import / - export." }
        loadKw: { type: number, nullable: true }
        health: { type: string, enum: [ok, stale, never] }
        readAt: { type: string, format: date-time, nullable: true }
        reportedAt: { type: string, format: date-time }
    ChargingConfig:
      type: object
      description: >
        Die im Portal gepflegte Lastmanagement-Konfiguration einer Anlage.
      properties:
        gridLimitKw: { type: number, nullable: true, description: "Die Anschlussgrenze in kW; null = noch nicht gepflegt (dann lädt nichts)." }
        priorityChargePointIds:
          type: array
          description: "Die Vorrang-Säulen; leer = keine."
          items: { type: string }
        surplusPolicy: { type: string, nullable: true, enum: [nur_sonne, sonne_zuerst, schnell], description: "Die Überschuss-Priorität; null = nicht gewählt (die Box behält ihre eigene)." }
        storagePriority: { type: string, nullable: true, enum: [speicher_vor_auto, auto_vor_speicher], description: "Wer den Überschuss zuerst bekommt; null = nicht gewählt." }
        chargePoints:
          type: array
          description: >
            Die ALLOWLIST: die Kennungen, unter denen die Box eine Säule
            überhaupt annimmt. Sie FÜGT NUR HINZU - die Box übernimmt jeden
            Eintrag, den sie noch nicht kennt, und überschreibt keinen
            bestehenden; eine Kennung hier WEGZULASSEN ist kein Löschen (das
            sagt `removedChargePointIds` ausdrücklich). Eine leere Liste ist
            hier deshalb - anders als beim Vorrang - keine Aussage „keine
            Säule".
          items: { $ref: "#/components/schemas/AllowedChargePoint" }
        removedChargePointIds:
          type: array
          description: >
            Die ZURÜCKGENOMMENEN Kennungen - die GRABSTEIN-Liste. Sie reist in
            JEDEM folgenden Dokument an die Box mit, nicht einmal: das retained
            Dokument wird als Ganzes ersetzt, also hätte eine nur einmal
            genannte Löschung eine gerade offline gewesene Box nie erreicht.
            Eine Kennung steht nie zugleich in `chargePoints` - ein erneutes
            Eintragen belebt sie wieder und nimmt sie hier heraus.
          items: { type: string }
        frame:
          allOf:
            - $ref: "#/components/schemas/LadeparkRahmen"
          nullable: true
          description: >
            Der Ladepark-RAHMEN (P5/E10) - die Auslegungs-Zahlen, die bis dahin
            nur `:8484` kannte. null = dazu ist nichts gepflegt und die Box
            rechnet mit ihren eigenen; GESCHRIEBEN wird er ausschliesslich über
            `PUT /api/v1/admin/sites/{siteId}/charging-frame`.
        storageRank:
          type: integer
          nullable: true
          description: >
            Die Position des SPEICHERS in der Rangliste des Kunden (P6). Sie
            steht NEBEN `storagePriority`, nicht an ihrer Stelle: es ist EINE
            Menge, zweimal gelesen. null = der Kunde hat nie eine Reihenfolge
            gezogen, und dann entscheidet allein `storagePriority`, wer in den
            ganzen Überschuss greifen darf - nie 0.
        updatedAt: { type: string, format: date-time, nullable: true }
        updatedBy: { type: string, nullable: true, description: "Wer sie zuletzt gesetzt hat (JWT-Subject)." }
    LadeparkRahmen:
      type: object
      description: >
        Der Ladepark-RAHMEN einer Anlage (Verbrauchsmanagement v1 / P5, E10).
        Jedes Feld einzeln optional: null heisst „das Portal äußert sich dazu
        nicht" und die Box behält ihre eigene Zahl - nie 0.
      properties:
        houseReserveKw: { type: number, nullable: true, description: "Die gepflegte Hausreserve; sie gilt nur, solange der Netzanschluss nicht gemessen wird." }
        marginPct: { type: number, nullable: true, description: "Der Sicherheitsabstand zur Anschlussgrenze in Prozent - er wird an der GRENZE genommen, nicht am Rest." }
        minPowerKw: { type: number, nullable: true, description: "Die Mindestleistung, unter der pausiert statt ausgehungert wird. Sie wird auf die Steckdose geklemmt: 30 kW machen eine 11-kW-Wallbox nicht unbedienbar." }
        rotationMinutes: { type: integer, nullable: true, description: "Der Takt, in dem Wartende wechseln." }
        maxHouseLoadKw: { type: number, nullable: true, description: "Die höchste bekannte Gebäudelast - die Grundlage des Ausfall-Profils, NICHT dieselbe Zahl wie die Mindestleistung." }
        staticBudget: { type: boolean, nullable: true, description: "true = rechne STATISCH, auch wenn eine Messung vorliegt. Negativ formuliert wie auf der Box, damit der Nullwert die gewollte Vorgabe ist." }
    AllowedChargePoint:
      type: object
      description: >
        Eine im Portal eingetragene Ladesäule. Alles außer der Kennung ist das,
        was der Betreiber zufällig schon weiß - null heißt „unbekannt", nie 0:
        die Box entscheidet dann aus dem, was die Säule selbst meldet.
      properties:
        chargePointId: { type: string, description: "Die OCPP-ChargePointId, zeichengleich wie in der Säule eingetragen." }
        label: { type: string, nullable: true, description: "Der Name, unter dem der Kunde sie wiedererkennt; null = die Box nimmt die Kennung." }
        ratedKw: { type: number, nullable: true, description: "Nennleistung je Stecker; null = unbekannt." }
        connectors: { type: integer, nullable: true, description: "Erklärte Steckerzahl; null = unbekannt." }
        source: { type: string, nullable: true, enum: [nur_sonne, sonne_zuerst, schnell], description: "Die STEUERART dieser Säule (P5) - woher IHR Ladestrom kommen soll. null = für sie ist nichts gewählt und es gilt der ANLAGEN-STANDARD (surplusPolicy); nie \"schnell\"." }
        minKw: { type: number, nullable: true, description: "Ab welcher Leistung diese Säule überhaupt anfängt; null = unbekannt, nie 0." }
        connection: { type: string, nullable: true, enum: [haus, eigen], description: "WO die Säule hängt (Cockpit Phase 1 / C1) - das SOLL des Kunden: \"haus\" = hinter dem Hausanschluss (ihre Leistung steckt in der Netzmessung der Anlage, das Budget-Gesetz der Box gilt für sie), \"eigen\" = eigener Netzanschluss/Zähler. null = der Kunde hat nichts gesagt und die Box behält, was sie hat - NIE \"eigen\", denn das nähme eine reale Ladeleistung aus ihrer Bilanz." }
        rank:
          type: integer
          nullable: true
          description: >
            Die Position dieser Säule in der Rangliste des Kunden (P6). null =
            sie steht in keiner, und dann entscheidet allein die Vorrang-MENGE
            (`priorityChargePointIds`) - also exakt wie vor P6.


            ⚠ GLEICHE ZAHLEN SIND GLEICHRANGIG: alle Säulen EINER Seite des
            Speichers tragen dieselbe, weil die Fläche sie als EINE Zeile zeigt
            und der Kunde zwischen ihnen gar keine Reihenfolge gewählt hat. Die
            Box wechselt zwischen ihnen weiter im Takt ab (Rotation).
        addedAt: { type: string, format: date-time, nullable: true }
        addedBy: { type: string, nullable: true, description: "Wer sie eingetragen hat (JWT-Subject)." }
    Steuerart:
      type: object
      description: >
        Genau EINE Quelle, höchstens EIN Ziel. Jedes Feld ist nullable und
        `null` heißt "diese Steuerart kennt diesen Wert nicht" - nie 0.
      properties:
        quelle:
          type: string
          enum: [sofort, ueberschuss, guenstig, feste_zeiten, eigene_regel]
          description: >
            `eigene_regel` = die Policy ist nicht eindeutig auf eine Steuerart
            abbildbar (§7.1 Regel 9); sie bleibt unverändert stehen und die
            Fläche verlinkt den Baukasten.
        herkunft:
          type: string
          enum: [policy, standard, ohne]
          description: >
            `standard` = dieser Ladepunkt folgt dem Anlagen-Standard;
            `policy` = er hat eine eigene Steuerart (die Zeile liest
            "abweichend"); `ohne` = es gibt keine Policy.
        schwelleKw: { type: number, nullable: true }
        preisgrenzeCtKwh: { type: number, nullable: true }
        ueberschussModus:
          type: string
          nullable: true
          enum: [pausieren, mindestleistung, null]
        mindestleistungKw: { type: number, nullable: true }
        fenster: { $ref: "#/components/schemas/SteuerartFenster" }
        ziel:
          type: string
          nullable: true
          enum: [bis_uhrzeit, laufzeit_bis, null]
        zielFenster: { $ref: "#/components/schemas/SteuerartFenster" }
        zielEnergieKwh: { type: number, nullable: true }
        zielLaufzeitMinuten: { type: integer, nullable: true }
        zielAmStueck: { type: boolean, nullable: true }
    SteuerartFenster:
      type: object
      nullable: true
      properties:
        tage: { type: string, enum: [daily, weekdays, weekend] }
        von: { type: string, example: "13:00" }
        bis: { type: string, example: "15:00" }
    SteuerartWahl:
      type: object
      description: >
        Eine anbietbare Wahl. Gesperrt heisst SICHTBAR mit Grund, nie versteckt -
        eine fehlende Karte erklärt nichts.
      properties:
        id: { type: string, example: ueberschuss }
        gesperrt: { type: boolean }
        grund:
          type: string
          nullable: true
          description: Bei einer gesperrten Wahl IMMER gesetzt.
    SteuerartWunsch:
      type: object
      description: >
        Die GEWÜNSCHTE Steuerart - die Spiegelform von `Steuerart` ohne
        `herkunft` (die entscheidet der Server). Jedes Feld ausser `quelle` ist
        optional; null heisst "dazu sagt der Kunde nichts", dann gilt die
        Vorgabe aus `optionen.vorgaben`. Was die gewählte Quelle nicht fragt,
        wird nicht gesendet.
      required: [quelle]
      properties:
        quelle:
          type: string
          enum: [sofort, ueberschuss, guenstig, feste_zeiten, freigabe_ueberschuss, freigabe_guenstig]
        schwelleKw: { type: number, nullable: true }
        preisgrenzeCtKwh: { type: number, nullable: true }
        mindestlaufzeitMinuten:
          type: integer
          nullable: true
          description: >
            Profil-Feld (`min_on_seconds`), kein Dokument-Feld - der
            Zyklen-Wächter der Box liest es aus dem Registry-Push.
        sperrzeitMinuten:
          type: integer
          nullable: true
          description: Profil-Feld (`min_off_seconds`); nur die SG-Ready-Freigabe fragt danach.
        fenster: { $ref: "#/components/schemas/SteuerartFenster" }
        ziel:
          type: string
          nullable: true
          enum: [bis_uhrzeit, laufzeit_bis, null]
        zielFenster:
          allOf: [{ $ref: "#/components/schemas/SteuerartFenster" }]
          description: >
            `von` darf FEHLEN - dann setzt der Server ihn auf `bis` minus
            `optionen.vorgaben.zielFensterStunden`.
        zielEnergieKwh: { type: number, nullable: true }
        zielLaufzeitMinuten: { type: integer, nullable: true }
        zielAmStueck: { type: boolean, nullable: true }
    SteuerartErgebnis:
      type: object
      properties:
        steuerart: { $ref: "#/components/schemas/Steuerart" }
        aktiv:
          type: boolean
          description: >
            Ob die Steuerart jetzt WIRKLICH läuft. false = sie ist gespeichert,
            aber die Aktivierung wurde benannt abgelehnt (Flag aus, Compiler
            weg, fremder Anspruch) - der Entwurf bleibt stehen.
        grund: { type: string, nullable: true }
        nachricht: { type: string, nullable: true }
        verteilt:
          type: boolean
          description: Ob das Artefakt an das Gerät verteilt werden konnte.
        policyVersion: { type: integer, nullable: true }
    Fahrzeuge:
      type: object
      description: >
        Die Ladekarten einer Anlage (Paket P7). Der Schlüssel ist ein Pseudonym
        der BOX - der Klartext-IdTag kommt hier nie vor.
      properties:
        fahrzeuge:
          type: array
          items:
            type: object
            required: [tagRef]
            properties:
              tagRef:
                type: string
                pattern: "^tagref_[0-9a-f]{8,64}$"
                description: Der Pseudonym der Box, NIE der Bezug des OCPP-Journals.
              name:
                type: string
                nullable: true
                description: null = gesehen, aber noch nicht benannt.
              steuerart:
                nullable: true
                description: null = kein Profil - die Karte lädt wie ihr Ladepunkt.
                allOf: [{ $ref: "#/components/schemas/Steuerart" }]
              mindestleistungKw: { type: number, nullable: true }
              ersteSichtungAm: { type: string, format: date-time, nullable: true }
              letzteSichtungAm: { type: string, format: date-time, nullable: true }
              letzterLadepunkt:
                type: string
                nullable: true
                description: Die Kennung der Säule, an der sie zuletzt geladen hat.
              laedt:
                type: boolean
                description: >
                  Aus dem Herzschlag, nie aus der Sichtungszeit geraten -
                  "vor zwei Minuten gesehen" ist keine laufende Ladung.
    Verbraucher:
      type: object
      description: Die Zone "Verbraucher" einer Anlage (lesend).
      properties:
        verbraucher:
          type: array
          items:
            type: object
            properties:
              entityId: { type: string, format: uuid }
              name:
                type: string
                nullable: true
                description: >
                  Der Anzeigename: der vergebene Name, sonst die OCPP-Kennung,
                  sonst null.
              typ: { type: string, example: ev-charger }
              typLabel: { type: string, example: Ladepunkt }
              ladepunkt:
                type: boolean
                description: >
                  true = für den Kunden ein "Ladepunkt" (OCPP-Säule ODER
                  go-e/Modbus-Wallbox - ein Modell, dieselben Steuerart-Wörter).
              chargePointId: { type: string, nullable: true }
              steuerart: { $ref: "#/components/schemas/Steuerart" }
              regeln:
                type: integer
                description: >
                  Wie viele AKTIVE Wenn/Dann-Flows diese Komponente
                  beanspruchen - die Regeln, die NICHT die Steuerart bilden.
              fortschritt:
                nullable: true
                description: >
                  Der Ziel-Fortschritt aus dem Erfüllungs-Ledger (dieselbe Form
                  wie `/consumers/{id}/fulfillment`), null ohne Beleg.
              aktiv: { type: boolean, nullable: true }
              optionen:
                type: object
                description: >
                  Was hier WÄHLBAR ist (P2). Aus der reinen SteuerartSatz -
                  derselben Klasse, mit der der Schreibweg die Wahl ein zweites
                  Mal prüft; der Dialog rendert damit nur, was der Server auch
                  annehmen würde. Eine gesperrte Wahl bleibt SICHTBAR und trägt
                  ihren Grund.
                properties:
                  schreibbar:
                    type: boolean
                    description: >
                      false = die Steuerart lässt sich hier (noch) nicht setzen
                      (heute: die OCPP-Säule bis Paket P5).
                  nichtSchreibbarGrund: { type: string, nullable: true }
                  quellen:
                    type: array
                    items: { $ref: "#/components/schemas/SteuerartWahl" }
                  ziele:
                    type: array
                    description: Leer = dieser Typ kennt gar kein Ziel.
                    items: { $ref: "#/components/schemas/SteuerartWahl" }
                  vorgaben:
                    type: object
                    description: >
                      Die Startwerte der Folgefragen (§3.2). Ein Feld, das nicht
                      BELEGT ist, bleibt null und das Formular startet leer -
                      nie eine erfundene Zahl.
                    properties:
                      schwelleKw: { type: number, nullable: true }
                      preisgrenzeCtKwh: { type: number, nullable: true }
                      mindestlaufzeitMinuten: { type: integer, nullable: true }
                      sperrzeitMinuten: { type: integer, nullable: true }
                      fenster: { $ref: "#/components/schemas/SteuerartFenster" }
                      zielUhrzeit: { type: string, nullable: true, example: "06:00" }
                      zielTage: { type: string, nullable: true, example: daily }
                      zielEnergieKwh: { type: number, nullable: true }
                      zielLaufzeitMinuten: { type: integer, nullable: true }
                      zielFensterStunden:
                        type: integer
                        description: >
                          Wie lang das Ziel-Fenster VOR der Frist ist (§3.2
                          fragt nur nach der Frist, den Beginn setzt der
                          Server) - die Fläche zeigt das entstehende Fenster
                          wörtlich, statt es zu verschweigen.
        ladepunkte:
          type: object
          properties:
            standard:
              allOf: [{ $ref: "#/components/schemas/Steuerart" }]
              nullable: true
              description: null nur, wenn die Anlage gar keinen Ladepunkt hat.
            standardFolger: { type: integer }
            gesamt: { type: integer }
            rahmen:
              type: object
              nullable: true
              description: >
                Der Ladepark-Rahmen (§4.2) - eine AUSWAHL aus dem Budget-Block
                der Box plus die im Portal gepflegte Anschlussgrenze. Jede Zahl
                nullable; `hinweis` ist der Satz der Box, wörtlich.
              properties:
                netzanschlussKw: { type: number, nullable: true }
                gepflegteGrenzeKw: { type: number, nullable: true }
                effektivGrenzeKw: { type: number, nullable: true }
                hausLastKw: { type: number, nullable: true }
                hoechsteHausLastKw: { type: number, nullable: true }
                verteiltKw: { type: number, nullable: true }
                budgetKw: { type: number, nullable: true }
                sicherheitsabstandPct: { type: number, nullable: true }
                mindestleistungKw: { type: number, nullable: true }
                modus: { type: string, nullable: true }
                blind: { type: boolean }
                hinweis: { type: string, nullable: true }
                steckerAnzahl: { type: integer }
                gemeldetAm: { type: string, format: date-time, nullable: true }
        rangliste:
          type: array
          description: >
            Die Reihenfolge bei knapper Leistung (§5/§7.3), Speicher als
            Eintrag. Gespeichert wird sie über PUT /rangliste - als PROJEKTION
            auf consumer_profile.default_service_rank + storage_relation und die
            Speicher-Frage des Ladeparks, nie als eigene Tabelle. Gleichrangige
            Ladepunkte (OCPP-Säulen ohne consumer_profile) stehen als EINE Zeile
            mit mehreren `mitglieder` - der Ganzzahl-Rang je Säule ist Paket P6.
          items:
            type: object
            properties:
              position:
                type: integer
                description: >
                  1-basiert über die GERÄTE: eine Gruppe aus drei Säulen auf
                  Platz 6 belegt 6, 7 und 8, die nächste Zeile steht auf 9.
              art: { type: string, enum: [speicher, ladepunkt, verbraucher] }
              entityId:
                type: string
                format: uuid
                nullable: true
                description: null beim Speicher UND bei einer Gruppe.
              name:
                type: string
                nullable: true
                description: null bei einer Gruppe - die Fläche nennt ihre Mitglieder.
              mitglieder:
                type: array
                description: Leer beim Speicher, sonst mindestens eine Komponente.
                items:
                  type: object
                  properties:
                    entityId: { type: string, format: uuid }
                    name: { type: string }
    SiteCharging:
      type: object
      description: >
        Die Ladepunkt-Sicht EINER Anlage: das geteilte Budget + die Säulen.
      properties:
        budget: { allOf: [{ $ref: "#/components/schemas/ChargingBudget" }], nullable: true }
        budgets:
          type: array
          description: "Additive Mehrbox-Sicht: der gemeldete OCPP-Anschluss je aktiver Box. `budget` bleibt für Bestandsleser bestehen."
          items: { $ref: "#/components/schemas/ChargingBudget" }
        chargers:
          type: array
          items: { $ref: "#/components/schemas/ChargePoint" }
    ChargingBudget:
      type: object
      description: >
        Das Standort-Budget, wie die Box es gerechnet hat. Alle Messwert-Felder
        sind nullable - kein Messwert ist NIE eine 0.
      properties:
        deviceId: { type: string, format: uuid }
        enabled: { type: boolean, description: "Der OCPP-Server der Box läuft." }
        controlEnabled: { type: boolean, description: "Die LEBENDE Zuteilung darf geschrieben werden; ohne sie fährt die Anlage auf n x Sicherheitsprofil." }
        controlNote: { type: string, nullable: true, description: "Warum nicht - eine Verweigerung, die niemand sieht, ist ein Rätsel." }
        gridLimitKw: { type: number, nullable: true, description: "Die gepflegte Anschlussgrenze." }
        marginPct: { type: number, nullable: true, description: "Sicherheitsabstand, genommen VON der Anschlussgrenze." }
        minPowerKw: { type: number, nullable: true, description: "Unter dieser Leistung wird pausiert statt ausgehungert." }
        budgetKw: { type: number, nullable: true }
        allocatedKw: { type: number, nullable: true }
        reservedKw: { type: number, nullable: true, description: "Zurückgehalten für nicht erreichbare Ladepunkte oder noch nicht abgeglichene Sitzungen beim Wiederanlauf." }
        measuredKw: { type: number, nullable: true, description: "Was die Säulen wirklich ziehen." }
        siteLoadKw: { type: number, nullable: true }
        siteGridKw: { type: number, nullable: true }
        budgetMode: { type: string, nullable: true, enum: [statisch, gemessen, haelt, zieht_zusammen, sicherheitsbudget] }
        budgetNote: { type: string, nullable: true, description: "Der deutsche Satz der Box, unverändert." }
        budgetBlind: { type: boolean }
        effLimitKw: { type: number, nullable: true, description: "Die wirksame Grenze (gepflegt oder §14a, die engere)." }
        safeDefaultKw: { type: number, nullable: true, description: "Das Ausfall-Profil je Stecker." }
        safeDefaultNote: { type: string, nullable: true }
        safeDefaultHolds: { type: boolean, nullable: true, description: "Stecker x Ausfall-Profil + höchste Gebäudelast bleibt unter der Grenze. null = nicht gemeldet." }
        safeWorstCaseKw: { type: number, nullable: true }
        maxHouseLoadKw: { type: number, nullable: true }
        connectorCount: { type: integer }
        ocppPort:
          type: integer
          nullable: true
          description: >
            Der Port, unter dem der OCPP-Server der Box lauscht - zusammen mit
            der LAN-Adresse des Geräts der Endpunkt, den eine Säule anwählt.
            null heißt „eine ältere Box meldet es nicht" ODER „der Server
            lauscht gerade nicht", nie Port 0: eine Adresse, auf der niemand
            antwortet, ist die schlechtere Auskunft als gar keine.
        ocppUrlPath: { type: string, nullable: true, description: "Der Pfad des OCPP-Servers (z. B. `/ocpp`); null wie oben." }
        reportedAt: { type: string, format: date-time }
    ChargePoint:
      type: object
      description: >
        Eine Ladesäule. Hersteller/Modell/Firmware sind SELBSTAUSKUNFT der
        Station und ausschließlich zur Anzeige (herstellerneutral: kein
        Mechanismus verzweigt auf sie).
      properties:
        deviceId: { type: string, format: uuid }
        chargePointId: { type: string, description: "Die OCPP-ChargePointId - DIE Identität." }
        label: { type: string, nullable: true }
        priority: { type: boolean, description: "Vorrang-Säule: lädt zuerst voll, der Rest teilt fair." }
        connected: { type: boolean }
        vendor: { type: string, nullable: true }
        model: { type: string, nullable: true }
        firmware: { type: string, nullable: true }
        ready: { type: boolean, description: "Die zwei dauerhaften Schutzprofile sind hinterlegt." }
        note: { type: string, nullable: true, description: "Warum nicht bereit - nie ein unerklärtes Nein." }
        lastSeen: { type: string, format: date-time, nullable: true }
        connection: { type: string, nullable: true, enum: [haus, eigen], description: "WO die Säule laut BOX hängt (Cockpit Phase 1 / C1) - das IST. null = eine ältere Box meldet es nicht; das ist weder \"haus\" noch \"eigen\", denn erst eine Meldung belegt, dass die Unterscheidung dort angekommen ist. Was der KUNDE gewählt hat, steht in der Allowlist (charging-config) - Soll und Ist sind zwei Aussagen." }
        entityId: { type: string, format: uuid, nullable: true, description: "Die Komponente, die die Plattform für diese Säule komponiert hat." }
        reportedAt: { type: string, format: date-time }
        connectors:
          type: array
          items: { $ref: "#/components/schemas/ChargeConnector" }
    ChargeConnector:
      type: object
      description: >
        Ein Stecker: ein Fahrzeug, ein Anspruch auf das Budget. Ein nicht
        gemeldeter Messwert bleibt null, nie 0; ein Zustandswort außerhalb des
        OCPP-1.6-Vokabulars wird beim Ingest verworfen statt gespeichert.
      properties:
        connectorId: { type: integer }
        status: { type: string, nullable: true, enum: [Available, Preparing, Charging, SuspendedEVSE, SuspendedEV, Finishing, Reserved, Unavailable, Faulted] }
        charging: { type: boolean }
        allocatedKw: { type: number, nullable: true, description: "Was das Lastmanagement zugeteilt hat (0 = pausiert); null = nicht Teil der Entscheidung." }
        reason: { type: string, nullable: true, description: "Das Maschinenwort des Verteilers." }
        reasonText: { type: string, nullable: true, description: "Sein deutscher Satz, unverändert." }
        nextTurn: { type: string, format: date-time, nullable: true, description: "Geschätzter Termin des Wartenden; null = nichts behaupten." }
        powerKw: { type: number, nullable: true }
        energyKwh: { type: number, nullable: true }
        socPct: { type: number, nullable: true, description: "Der Fahrzeug-Ladestand, wenn die Säule ihn meldet (DC meist, AC praktisch nie)." }
        commandStatus: { type: string, nullable: true, description: "Die Antwort der Säule auf die letzte Grenze." }
        readback: { type: string, nullable: true, enum: [ok, abweichend, unbekannt], description: "Ein angenommener Befehl ist kein Befehl in Kraft." }
        readbackNote: { type: string, nullable: true }
        sessionSince: { type: string, format: date-time, nullable: true }
        sessionKwh: { type: number, nullable: true, description: "Die Bilanz DIESES Ladevorgangs (Cockpit Phase 1 / E2). energyKwh daneben ist ein KUMULATIVES Register - was in der laufenden Sitzung geflossen ist, weiss nur die Box (nur sie kennt den Registerstand bei StartTransaction). null = kein Ladevorgang / kein Register / ein rueckwaerts gesprungenes Register, nie eine erfundene 0." }
        meteredAt: { type: string, format: date-time, nullable: true, description: "Wann die Saeule zuletzt MeterValues gemeldet hat - das ALTER von powerKw/energyKwh/socPct. null = nie gemessen bzw. ein aelterer Edge-Stand; die Flaeche darf die Werte dann NICHT als aktuell ausgeben." }
    SupplyPrice:
      type: object
      description: >
        One Anlage's structured supply-price sheet (site_supply_price, report
        vp-nacht-bezug-e7 §3.1). Components ct/kWh NETTO, all nullable (null =
        unknown, contributes nothing).
      properties:
        present: { type: boolean, description: "A row exists (false => portal prefills suggestions)." }
        hasComponents: { type: boolean, description: "At least one component maintained (the activation gate)." }
        netzentgeltArbeitspreisCt: { type: number, nullable: true }
        stromsteuerCt: { type: number, nullable: true }
        konzessionsabgabeCt: { type: number, nullable: true }
        umlagenCt: { type: number, nullable: true, description: "KWKG + Offshore + section 19 StromNEV as ONE sum." }
        vertriebsaufschlagCt: { type: number, nullable: true }
        ustPct: { type: number, nullable: true, description: "USt on everything incl. spot; 0 for Vorsteuer-Abzug (C&I)." }
        komponentenStand: { type: string, format: date, nullable: true, description: "Preisblatt gültig ab." }
        updatedAt: { type: string, format: date-time, nullable: true }
    UsageProfile:
      type: object
      description: >
        AE7 Nutzungsprofil read-model (docs/contracts/v2/usage-profile.md): the
        effective usage profile, the derived default, the override, the emphasis
        map and the signals it was derived from.
      properties:
        usageProfile:
          type: string
          enum: [arbitrage, peak, private]
          description: The EFFECTIVE profile (override when set, else the derived default).
        derivedProfile:
          type: string
          enum: [arbitrage, peak, private]
          description: The derived default, ignoring any override.
        override:
          type: string
          nullable: true
          enum: [arbitrage, peak, private]
        emphasis:
          type: object
          description: Which surfaces are prominent | secondary | minimal | hidden.
          properties:
            money: { type: string, enum: [prominent, secondary, minimal, hidden] }
            peak: { type: string, enum: [prominent, secondary, minimal, hidden] }
            flow: { type: string, enum: [prominent, secondary, minimal, hidden] }
            devices: { type: string, enum: [prominent, secondary, minimal, hidden] }
        signals:
          type: object
          properties:
            hasStorage: { type: boolean }
            hasPv: { type: boolean }
            hasControllableConsumer: { type: boolean }
            activeStrategyNodeTypes:
              type: array
              items: { type: string, example: vp.strategy.market }
            plantKind: { type: string, nullable: true }
            hasLeistungspreis: { type: boolean }
    CockpitLayoutDocument:
      type: object
      description: >
        A layout document - INTENT only, never the rendered surface. Which
        Bausteine a plant actually has stays applications x capabilities and is
        derived on every render, which is why a document may name a Baustein
        that is currently absent: it is skipped silently and the preference
        survives switching the application back on.
      properties:
        version: { type: integer, example: 1 }
        order:
          type: array
          items: { type: string }
          description: >
            Bausteine in their order. UNNAMED Bausteine keep their CANONICAL
            position (not appended) - that is how a newly activated
            application's Baustein appears where it belongs.
        hidden:
          type: array
          items: { type: string }
          description: >
            Explicitly hidden. MANDATORY Bausteine (Status, Zustand) ignore it -
            a catalog property, not an admin's will (E2).
        shown:
          type: array
          items: { type: string }
          description: >
            Explicitly shown - takes back a LOWER layer's `hidden`, which is why
            it is not the same as "absent from hidden". Without it a customer
            could never undo their operator's Vorgabe.
        lead:
          type: string
          nullable: true
          description: The highlighted CockpitBlockId (peak-band, erloes-komposition, energiefluss, lade-budget).
        custom:
          type: array
          description: >
            The customer's OWN evaluations (Anwendungs-Programm Stufe 5). They
            are the only Bausteine a document DEFINES rather than merely names:
            each carries its key (`eigen:<id>`), which additionally appears in
            `order`/`hidden`. Only the `cockpit` surface of ONE plant may carry
            them (the portfolio hangs off the customer and has no single
            component to validate a channel against); at most 12 per plant.
            Server-side validation: the component must belong to this plant
            (RLS), it must actually report that channel, and the aggregate must
            be HONEST for that channel - `tagessumme` only on an energy channel
            (a kWh channel reports a METER READING, so its day figure is the
            ADVANCE), and `tagesmax`/`tagesmittel` never on one (the maximum of
            a counter is its closing reading, its mean is no statement). Each
            refusal is a 400 with a German reason and writes nothing. The rule
            lives in `cockpit/EigeneAuswertung` and is pinned against the portal
            twin by `docs/contracts/v2/eigene-auswertung-vectors.json`.
          items: { $ref: "#/components/schemas/EigeneAuswertungDefinition" }
    EigeneAuswertungDefinition:
      type: object
      required: [id, titel, darstellung, entityId, channel, aggregat]
      properties:
        id:
          type: string
          example: "eigen:a1b2c3"
          description: The Baustein key - `eigen:` plus lowercase letters, digits and hyphens.
        titel: { type: string, maxLength: 60, example: "Wärmepumpe heute" }
        darstellung:
          type: string
          enum: [kachel, chart]
          description: One number, or the day's curve with that number as its headline.
        entityId: { type: string, format: uuid, description: A component of THIS plant. }
        channel: { type: string, example: power_kw, description: A measure channel that component reports. }
        aggregat:
          type: string
          enum: [jetzt, tagessumme, tagesmax, tagesmittel]
    CockpitLayout:
      type: object
      description: >
        All layers separately + the Baustein catalog. The resolution
        (catalog -> preset -> Vorgabe -> Eigen) is a pure portal function.
      properties:
        surface: { type: string, enum: [cockpit, portfolio] }
        profil:
          type: string
          nullable: true
          enum: [privat, gewerbe]
          description: The plant's application preset - the base of the preset layer.
        presetLayout:
          allOf: [{ $ref: "#/components/schemas/CockpitLayoutDocument" }]
          nullable: true
          description: The preset layer straight from the catalog (never null in practice, possibly empty).
        tenantVorgabe:
          allOf: [{ $ref: "#/components/schemas/CockpitLayoutLayer" }]
          nullable: true
          description: The customer-wide Vorgabe (E1); a per-plant Vorgabe beats it.
        siteVorgabe:
          allOf: [{ $ref: "#/components/schemas/CockpitLayoutLayer" }]
          nullable: true
        eigen:
          allOf: [{ $ref: "#/components/schemas/CockpitLayoutLayer" }]
          nullable: true
          description: The customer's own arrangement - it WINS (E2).
        darfVorgabe:
          type: boolean
          description: True = this caller may write the `vorgabe` layer (platform-admin).
        bausteine:
          type: array
          description: The Baustein catalog of this surface - labels, the mandatory rule, lead capability.
          items:
            type: object
            properties:
              id: { type: string, example: kacheln }
              label: { type: string, example: Kennzahlen }
              pflicht: { type: boolean, description: "True = never hideable (Status, Zustand)." }
              beweglich: { type: boolean, description: "False = stays at its canonical position (head, stage)." }
              leadBlock:
                type: string
                nullable: true
                description: The block this Baustein's star sets as lead; null = no star.
              beigesteuertVon:
                type: array
                items: { type: string }
                description: >
                  The applications contributing this Baustein - DERIVED from the
                  catalog, never a second list. Empty = base equipment.
        vorlagen:
          type: array
          description: >
            The KINDS of own evaluation this surface knows (Anwendungs-Programm
            Stufe 5) - catalog data, empty where there are none. A Vorlage is not
            renderable; only its instance in `document.custom` is.
          items:
            type: object
            properties:
              id: { type: string, example: eigene-kachel }
              label: { type: string, example: Eigene Kachel }
              satz: { type: string, nullable: true, description: ONE sentence - what this kind shows. }
              darstellung: { type: string, enum: [kachel, chart] }
              anwendung: { type: string, example: eigene-auswertung }
              nach:
                type: string
                nullable: true
                description: The Baustein an instance is canonically sorted behind.
    EigeneAuswertungWerte:
      type: object
      properties:
        at: { type: string, format: date }
        from: { type: string, format: date-time }
        to: { type: string, format: date-time }
        bucketMinutes: { type: integer, example: 15 }
        werte:
          type: array
          items:
            type: object
            properties:
              id: { type: string, example: "eigen:a1b2c3" }
              titel: { type: string }
              darstellung: { type: string, enum: [kachel, chart] }
              entityId: { type: string, format: uuid }
              channel: { type: string }
              aggregat: { type: string, enum: [jetzt, tagessumme, tagesmax, tagesmittel] }
              wert:
                type: number
                nullable: true
                description: The figure - null where there is none, NEVER a 0.
              kanalart:
                type: string
                nullable: true
                enum: [energie, leistung, anteil, messwert]
                description: >
                  What the channel measures - it tells the surface which unit and
                  which time reference are meant without parsing the channel name.
              komponente: { type: string, nullable: true, description: The component's name. }
              entityType: { type: string, nullable: true }
              hinweis:
                type: string
                nullable: true
                description: The honest German sentence when there is nothing to show.
              verlauf:
                type: array
                description: The day's buckets - filled for `chart` only (a tile shows ONE number).
                items:
                  type: object
                  properties:
                    start: { type: string, format: date-time }
                    avg: { type: number, nullable: true }
                    min: { type: number, nullable: true }
                    max: { type: number, nullable: true }
                    last: { type: number, nullable: true }
                    n: { type: integer }
    CockpitLayoutLayer:
      type: object
      properties:
        document: { $ref: "#/components/schemas/CockpitLayoutDocument" }
        updatedBy: { type: string, nullable: true, description: The writer's JWT subject (the paper trail). }
        updatedAt: { type: string, format: date-time, nullable: true }
    SiteProfiles:
      type: object
      description: >
        TWO lists over ONE computation: `profiles` is the shelf (the four
        Betriebsmodelle), `weitere` the applications hidden from it since
        Steuerung Stufe 0. Both carry the same card shape. `weitere` is
        ADDITIVE - an older client reads only `profiles`.
      properties:
        weitere:
          type: array
          description: >
            The applications that are NOT on the shelf (classes basis and
            regel). Hidden, never deleted: their states are still answered
            because surfaces outside the Steuerung read them. Same item shape as
            `profiles`.
          items:
            type: object
        profiles:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
                example: marktvermarktung
                description: >
                  The application id - an OPEN vocabulary whose truth is the
                  data-driven catalog `anwendungen/catalog.json` (byte-identical
                  in the portal), NOT this schema: adding an application is
                  catalog data, never a contract release (the
                  `entitytypes/catalog.json` precedent). Today's SHELF is
                  marktvermarktung, lastspitzenkappung, atypische-netznutzung,
                  lastmanagement (catalog field `regal`); monitoring,
                  speicher-fahrplan, ueberschuss, verbraucher and
                  eigene-auswertung ride in `weitere`; reserved catalog entries
                  are never served.
              label: { type: string, description: Customer-facing German name. }
              state:
                type: string
                nullable: true
                enum: [an, aus]
                description: The STORED customer intent; null = derived default.
              derivedActive: { type: boolean, description: What the derivation alone says. }
              active: { type: boolean, description: Effective state after the intent overlay. }
              unlocks:
                type: object
                properties:
                  views: { type: array, items: { type: string } }
                  widgets: { type: array, items: { type: string } }
                  moneyStream: { type: string, nullable: true }
              requirements:
                type: array
                items:
                  type: object
                  properties:
                    label: { type: string }
                    met: { type: boolean }
                    art:
                      type: string
                      enum: [hardware, einstellung]
                      description: >
                        Steuerung Stufe 5, ADDITIVE. `hardware` = what the plant
                        must physically have; a missing one means the operating
                        model cannot run HERE at all (the surface files the card
                        under "Nicht moeglich auf dieser Anlage", with the
                        reason). `einstellung` = a value somebody enters - the
                        card stays, and the traffic light shows the WAY.
                    behebung:
                      type: object
                      nullable: true
                      description: >
                        Where the customer has to go for it; null = there is
                        nothing a click could solve, and no way is claimed.
                      properties:
                        ziel:
                          type: string
                          enum: [einstellungen, modell, ladepark, voltpilot]
                        label:
                          type: string
                          nullable: true
                          description: >
                            null for `voltpilot` - an admin-conditional value has
                            no button the customer could press; VoltPilot enters
                            it (concept 3.4).
              blockedReason:
                type: string
                nullable: true
                description: >
                  Honest German sentence for a switched-on profile that cannot
                  fully run yet. Never a request prompt.
              origin: { type: string, nullable: true, enum: [masterdata, flow] }
              flowRef:
                type: object
                nullable: true
                properties:
                  flowId: { type: string, format: uuid }
                  name: { type: string }
              gatedNodeTypes: { type: array, items: { type: string } }
              gatedNodesEnabled: { type: boolean }
              exklusivGruppe:
                type: string
                nullable: true
                example: speicher
                description: >
                  Steuerung Stufe 5, ADDITIVE. Two operating models of the SAME
                  group are never on together - the switch is a RADIO, and the
                  server enforces it on write (switching one on ends every other
                  of its group in the same transaction). Today's only group is
                  `speicher`: the three models with a `vp.strategy.*` node that
                  compete for the SAME battery. Ladepark-Lastmanagement has NO
                  group on purpose - it is protection, not market participation,
                  keeps guarding the connection on the box whatever a card says,
                  and is derived-active from a connected charge point. There is
                  deliberately NO DB check: an existing plant with two active
                  models keeps them until the customer chooses (Multi-Use is its
                  own concept).
              seit:
                type: string
                format: date-time
                nullable: true
                description: >
                  Since when it runs ("laeuft seit ..."), Steuerung Stufe 5,
                  ADDITIVE. Only ever PROVEN: it is the `site_profile_state`
                  row's `updated_at`, and that row exists only for an explicit
                  customer decision. null for a DERIVED-active model (no row -
                  every existing plant) and for a switched-off one; the surface
                  then says "laeuft" without a date instead of inventing one.
    FlowNodeGovernance:
      type: object
      description: AE7 node governance - the gated strategy node types + per-site enablement.
      properties:
        gatedNodes:
          type: array
          items:
            type: object
            properties:
              type: { type: string, example: vp.strategy.market }
              label: { type: string, example: Marktoptimierung }
              gated: { type: boolean }
              enabled: { type: boolean, description: Enabled for THIS site. }
    FlowLayout:
      type: object
      description: >
        Portal v3 M5 - the editor canvas positions of a flow, stored OUTSIDE the
        hashed flow document (a dragged position must never change content_hash).
      properties:
        positions:
          type: object
          additionalProperties:
            type: object
            properties:
              x: { type: number }
              y: { type: number }
          description: node id -> {x, y} in canvas coordinates.
    FlowLiveStatus:
      type: object
      description: >
        Portal v3 M5 - what the DEVICE reports about a site's flows. Read-only.
      properties:
        acks:
          type: array
          description: One deployment ack per flow the device runs.
          items:
            type: object
            properties:
              flowId: { type: string, format: uuid }
              flowVersion: { type: integer }
              contentHash: { type: string, nullable: true }
              state: { type: string, enum: [active, error, unsupported] }
              detail: { type: string, nullable: true }
              reportedAt: { type: string, format: date-time, nullable: true }
        nodes:
          type: array
          description: >
            The per-node live states. EMPTY when the device does not send the
            feature-flagged block - the editor then shows channel values only,
            never a guessed node state.
          items:
            type: object
            properties:
              flowId: { type: string, format: uuid }
              nodeId: { type: string }
              state: { type: string, enum: [active, idle, error] }
              text: { type: string, nullable: true }
              since: { type: string, format: date-time, nullable: true }
    AutoStartOutcome:
      type: object
      description: Result of an AE7 auto-start seeding attempt.
      properties:
        created: { type: boolean }
        reason:
          type: string
          nullable: true
          description: null on success; already_has_flow | no_battery | not_found otherwise.
        profile: { type: string, nullable: true, enum: [arbitrage, peak, private] }
        flowId: { type: string, format: uuid, nullable: true }
        version: { type: integer, nullable: true }
        name: { type: string, nullable: true }
        message: { type: string }
    TopologyResponse:
      type: object
      description: >
        Anlagen-Topologie-Read-Model (AE1): the entity graph + the server-derived
        hub topology. See docs/contracts/v2/topology-read-model.md.
      properties:
        schemaVersion: { type: string, example: "1.0" }
        entities:
          type: array
          items:
            type: object
            properties:
              id: { type: string, format: uuid }
              entityType: { type: string, example: battery-hybrid }
              typeLabel: { type: string, example: "Batteriespeicher (Hybrid)" }
              label: { type: string, nullable: true }
              category: { type: string, example: storage, description: storage | producer | meter | consumer }
              health: { type: string, enum: [ok, stale, never] }
              capabilities:
                type: array
                items:
                  type: object
                  properties:
                    channel: { type: string, example: pv_power_kw }
                    unit: { type: string, nullable: true, example: kW }
                    role: { type: string, nullable: true, description: "pv | storage | grid | consumer; null = informational." }
                    primary: { type: boolean, description: The maßgebliche member of its role. }
                    value: { type: number, nullable: true, description: "Latest live value; null = unknown (never a fabricated 0)." }
        topology:
          type: object
          description: The role-grouped hub topology (the shared Go/TS/Java derivation output).
          properties:
            schema_version: { type: string, example: "1.0" }
            nodes:
              type: array
              items:
                type: object
                properties:
                  role: { type: string, enum: [pv, storage, consumer, grid] }
                  value_kw: { type: number, description: "Display magnitude (>=0); omitted when unknown." }
                  soc_pct: { type: number, description: "Storage only; omitted otherwise/when unknown." }
                  soc_source:
                    type: object
                    description: >
                      WHICH entity supplied `soc_pct` (P6 Speiser-Bindung).
                      Present whenever `soc_pct` is. It need not be the device
                      whose kilowatts the node carries: an explicitly bound
                      self-connected battery supplies the state of charge while
                      the hybrid inverter keeps measuring the power - which is
                      what lets a surface say "Ladestand von: <Batterie>"
                      instead of printing a percentage nobody stands behind.
                    properties:
                      entity_id: { type: string }
                      label: { type: string }
                  limits:
                    type: object
                    description: >
                      What the storage node's BMS currently permits (P6).
                      Omitted when nobody reports a limit - true for every
                      battery read through a catalogue driver. Every field is
                      optional: an unmapped or silent channel is ABSENT, never a
                      fabricated 0 (which on a limit would read as "charging
                      forbidden") and never a fabricated true. All fields come
                      from the ONE entity named in `source`.
                    properties:
                      source:
                        type: object
                        properties:
                          entity_id: { type: string }
                          label: { type: string }
                      charge_limit_a: { type: number }
                      discharge_limit_a: { type: number }
                      charge_allowed: { type: boolean }
                      discharge_allowed: { type: boolean }
                  flow_active: { type: boolean }
                  direction: { type: string, enum: [in, out], description: "node->hub (in) / hub->node (out); omitted when idle." }
                  members:
                    type: array
                    items:
                      type: object
                      properties:
                        entity_id: { type: string }
                        label: { type: string }
                        primary: { type: boolean }
                        value_kw: { type: number, description: "Raw signed contribution; omitted when unknown." }
    History:
      type: object
      properties:
        range: { type: string, enum: [day, week, month, year] }
        from: { type: string, format: date-time, description: Period start (inclusive). }
        to: { type: string, format: date-time, description: Period end (exclusive). }
        bucketMinutes: { type: integer, description: "15 (day), 60 (week) or 1440 (month/year)." }
        buckets:
          type: array
          items:
            type: object
            properties:
              start: { type: string, format: date-time }
              pvKwh: { type: number, nullable: true }
              loadKwh: { type: number, nullable: true }
              gridImportKwh: { type: number, nullable: true }
              gridExportKwh: { type: number, nullable: true }
              batteryChargeKwh: { type: number, nullable: true, description: Derived from the telemetry power balance. }
              batteryDischargeKwh: { type: number, nullable: true }
              socMinPct: { type: number, nullable: true }
              socMaxPct: { type: number, nullable: true }
              socLastPct: { type: number, nullable: true }
              priceEurMwh: { type: number, nullable: true, description: Day-ahead price of the slot (day range only). }
              costEur: { type: number, nullable: true, description: Import energy x matching day-ahead price. }
        totals:
          type: object
          properties:
            consumptionKwh: { type: number, nullable: true, description: Null when no bucket carried the channel (0-bucket period) - never a fabricated 0. }
            pvGenerationKwh: { type: number, nullable: true, description: Null when no bucket carried the channel. }
            gridImportKwh: { type: number, nullable: true, description: Null when no bucket carried the channel. }
            gridExportKwh: { type: number, nullable: true, description: Null when no bucket carried the channel. }
            gridCostEur: { type: number, nullable: true, description: "Import energy x the slot's real supply price - the SAME composition the optimizer plans with (flat tariff / spot + Aufschlag / (spot + Preisblatt-Komponenten) x (1+USt); bare spot without any price data, byte-identical to before). Valued at the CURRENTLY maintained tariff (no price-sheet history). Null when nothing is computable in the period. See tarifArt/tarifPriced for the labeling context." }
            tarifArt: { type: string, nullable: true, enum: [dynamisch, fest, ohne], description: "The site's configured tariff kind - labeling context for gridCostEur." }
            tarifPriced: { type: boolean, nullable: true, description: "Whether gridCostEur is valued beyond bare spot (tariff parameter, maintained Preisblatt, or the platform default-components flag): true = label 'bewertet zu Ihrem Stromtarif', false = 'zu Börsenpreisen'." }
            batterySavingsPlannedEur: { type: number, nullable: true, description: "EX-ANTE PLANNED savings from the persisted optimizer plans (latest run per slot); null where no plan exists. NOT measured money - the measured counterpart is savedEur on /earnings. Surfaces must say 'geplant'." }
            batterySavingsEur: { type: number, nullable: true, deprecated: true, description: Deprecated alias of batterySavingsPlannedEur (same value); kept one release. }
            steuerungPlannedEur: { type: number, nullable: true, description: "EX-ANTE PLANNED savings of the STEERING: sum(stur_cost_eur - cost_eur) over the same plan slots - what the plan earns against THE SAME BATTERY WITHOUT smart control (the stur self-consumption reference; measured twin: savedSteuerungEur on /sites/{id}/earnings). This is the customer-facing planned figure (Captain 04.09.2026); batterySavingsPlannedEur keeps answering the operator question. Null when even ONE covered slot carries no Messlatte (a run predating migration V20260867000000, or a site without battery master data) - a partial sum would describe a window nobody computed. By construction <= batterySavingsPlannedEur. Surfaces must say 'geplant'." }
            autarkiePct: { type: number, nullable: true, description: "(1 - grid import / consumption) x 100, NOT clamped - a value outside 0..100 is delivered as computed and flagged by autarkieUnplausibel." }
            eigenverbrauchPct: { type: number, nullable: true, description: "(pv - grid export) / pv x 100, NOT clamped - a value outside 0..100 is delivered as computed and flagged by eigenverbrauchUnplausibel." }
            autarkieUnplausibel: { type: boolean, nullable: true, description: "true when autarkiePct lies outside 0..100 (the meters do not add up; portal: 'Messwerte passen nicht zusammen'); null exactly when autarkiePct is null. Additive (AP-10 E16 Nr. 5)." }
            eigenverbrauchUnplausibel: { type: boolean, nullable: true, description: "true when eigenverbrauchPct lies outside 0..100 (the meters do not add up; portal: 'Messwerte passen nicht zusammen'); null exactly when eigenverbrauchPct is null. Additive (AP-10 E16 Nr. 5)." }
        protocol:
          type: array
          description: Tagesprotokoll (day range only, else empty).
          items:
            type: object
            properties:
              type: { type: string, enum: [batterie-laden, batterie-entladen, pv-spitze, preis-tief, preis-hoch] }
              start: { type: string, format: date-time }
              end: { type: string, format: date-time }
              text: { type: string, description: Plain-German event description. }
              energyKwh: { type: number, nullable: true }
              avgPriceEurMwh: { type: number, nullable: true }
              avoidedCostEur: { type: number, nullable: true }
              peakKw: { type: number, nullable: true }
        plan:
          type: array
          description: >
            Plan-vs-actual overlay (day range only, else empty): per 15-min slot
            the battery power/SoC of the latest persisted optimizer run.
          items:
            type: object
            properties:
              time: { type: string, format: date-time }
              batteryKw: { type: number, nullable: true, description: "+ = charge, - = discharge." }
              socPct: { type: number, nullable: true }
        coverage:
          nullable: true
          type: object
          description: >
            How completely the period is MEASURED (F4/P7) - from its own cheap
            min/max/count query on telemetry_rollup_15m, independent of the
            series. Counts QUARTER-HOURS (resolutionMinutes), not display
            buckets. Null when the site never measured a single quarter-hour
            (incl. a purely v2-native plant with no v1 rollup) - a surface must
            then claim no coverage at all. No percentage is served: the counts
            are the fact, the wording belongs to the client.
          properties:
            firstDataAt: { type: string, format: date-time, nullable: true, description: The site's earliest ever measured quarter-hour ("Daten ab …"). }
            lastDataAt: { type: string, format: date-time, nullable: true, description: The site's latest ever measured quarter-hour. }
            expectedFrom:
              type: string
              format: date-time
              description: >
                Start of the span the counters refer to - never before
                firstDataAt, so a calendar year of a plant commissioned in June
                is not reported as 45 % incomplete.
            expectedTo:
              type: string
              format: date-time
              description: >
                End (exclusive) of that span - never inside the running
                quarter-hour, and one rollup cadence (15 min) behind now, so a
                just-finished quarter is not reported as a gap.
            expectedBuckets: { type: integer, format: int64, description: Quarter-hours the span could have carried. }
            measuredBuckets: { type: integer, format: int64, description: Quarter-hours actually measured (capped at expectedBuckets - never over 100 %). }
            gaps: { type: integer, description: Contiguous missing runs inside the span, incl. its leading/trailing edge (0 = continuous). }
            resolutionMinutes: { type: integer, example: 15, description: Length of one counted unit. }
        events:
          type: array
          description: >
            The event track of the series (F6) - the NOTABLE things that explain
            an outlier, present in EVERY range (unlike `protocol`, which stays
            the day's ordinary events as a list). Windows are merged runs of
            quarter-hours; at most 20 windows per type (the longest first), and
            the coverage block still reports the true gap count. `text` carries
            the statement WITHOUT a time - the client puts the range-appropriate
            time in front. `end` is exclusive.

            netzgrenze is evaluated for day and week only (grid_limit_kw lives in
            the RAW telemetry, which the rollup cascade does not carry) - a
            client must SAY so rather than let a missing marker read as "no grid
            limit". abregelung comes from the persisted plans
            (`schedule.curtail_kw`), so its text says "eingeplant".
            abendverkauf is emitted for range=day only: the plan SOLD energy in
            the evening, told together with the night that followed (forecast vs
            measured load, an empty battery, the night's grid import). Every
            sentence is a persisted fact - a fact that is missing DROPS ITS
            SENTENCE instead of being estimated.
          items:
            type: object
            properties:
              type:
                type: string
                enum: [negativpreis, abregelung, netzgrenze, netzladen, abendverkauf, datenluecke, geraet-still]
              start: { type: string, format: date-time }
              end: { type: string, format: date-time, description: Exclusive. }
              text: { type: string, description: Plain-German statement without a time. }
    ForecastModelChoice:
      type: object
      description: >
        Der Zustand des PLATTFORMWEITEN Prognose-Schalters - die Vorgabe für
        jede Anlage ohne eigene Wahl (die anlagenbezogene Antwort ist
        SiteForecastModels). Die Tabelle dahinter ist append-only und damit
        zugleich das Audit-Journal.
      properties:
        kinds:
          type: array
          items:
            type: object
            properties:
              kind: { type: string, enum: [load, pv] }
              activeModel: { type: string, example: load-xgb, description: Das Modell, dessen Prognosen der Optimierer konsumiert. }
              source:
                type: string
                enum: [portal, env]
                description: >
                  portal = jemand hat entschieden (eine Zeile in
                  forecast_model_choice); env = der ausgelieferte Vorgabewert.
                  Der Unterschied wird nie geraten.
              envDefault: { type: string, description: Was ohne Portal-Wahl gälte. }
              setByName: { type: string, nullable: true, description: "Anzeige-Name des Umstellers; null = nicht im Token (die Fläche sagt dann „von einem Portal-Admin\")." }
              setAt: { type: string, format: date-time, nullable: true, description: "null = nie umgestellt (Quelle env)." }
              selectable:
                type: array
                items: { type: string }
                description: Alle Modelle dieser Art - die wählbare Menge.
        history:
          type: array
          description: Die Umstellungen, NEUESTE zuerst (append-only).
          items:
            type: object
            properties:
              kind: { type: string, enum: [load, pv] }
              model: { type: string }
              previousModel: { type: string, nullable: true, description: "null = die erste Umstellung dieser Art." }
              setByName: { type: string, nullable: true }
              setAt: { type: string, format: date-time }
    SiteForecastModels:
      type: object
      description: >
        Der Zustand des Prognose-Schalters EINER Anlage (Migration
        V20260826000000). Die Tabelle dahinter ist append-only und damit
        zugleich das Audit-Journal dieser Anlage.
      properties:
        siteId: { type: string, format: uuid }
        kinds:
          type: array
          items:
            type: object
            properties:
              kind: { type: string, enum: [load, pv] }
              activeModel: { type: string, example: load-xgb, description: Das Modell, dessen Prognosen der Optimierer FÜR DIESE ANLAGE konsumiert. }
              source:
                type: string
                enum: [anlage, plattform, env]
                description: >
                  anlage = jemand hat das für DIESE Anlage entschieden (eine
                  Zeile in site_forecast_model_choice); plattform = sie folgt
                  der Vorgabe von VoltPilot; env = dem ausgelieferten
                  Standardmodell. Der Unterschied wird nie geraten.
              platformDefault: { type: string, description: Was ohne eigene Wahl DIESER Anlage gälte. }
              envDefault: { type: string, description: Was ohne jede Portal-Wahl gälte. }
              setByName: { type: string, nullable: true, description: "Anzeige-Name des Umstellers; null = nicht im Token (die Fläche sagt dann „im Portal umgestellt\")." }
              setAt: { type: string, format: date-time, nullable: true, description: "null = für diese Anlage nie umgestellt." }
              selectable:
                type: array
                items: { type: string }
                description: Alle Modelle dieser Art - die wählbare Menge.
        history:
          type: array
          description: Die Umstellungen DIESER Anlage, NEUESTE zuerst (append-only).
          items:
            type: object
            properties:
              kind: { type: string, enum: [load, pv] }
              model: { type: string }
              previousModel: { type: string, nullable: true, description: "null = die erste Umstellung dieser Anlage und Art." }
              setByName: { type: string, nullable: true }
              setAt: { type: string, format: date-time }
    ForecastQuality:
      type: object
      description: >
        Shadow-mode forecast transparency for one site. Model ids are the
        registry vocabulary: load-persistence / pv-physical (baselines),
        load-xgb / pv-residual-xgb (challengers).
      properties:
        activeLoadModel: { type: string, example: load-persistence, description: The model whose load forecasts the optimizer consumes. }
        activePvModel: { type: string, example: pv-physical }
        models:
          type: array
          description: Lifecycle of every model that has run on this site.
          items:
            type: object
            properties:
              model: { type: string, example: load-xgb }
              kind: { type: string, enum: [load, pv] }
              status:
                type: string
                enum: [collecting, ready]
                description: "'collecting' = self-gate unmet, the challenger emits NO predictions yet."
              active: { type: boolean, description: Whether the optimizer consumes this model. }
              daysCollected: { type: integer, nullable: true }
              daysRequired: { type: integer, nullable: true, description: Training-day gate (default 21). }
              trainedAt: { type: string, format: date-time, nullable: true }
              trainRows: { type: integer, nullable: true }
              featureImportance:
                type: array
                description: Top features of the last training run, most important first.
                items:
                  type: object
                  properties:
                    feature: { type: string, example: lag_1d_kw }
                    label: { type: string, example: Verbrauch gestern zur gleichen Zeit, description: Plain-German label. }
                    weight: { type: number, description: Normalized share of total gain, 0..1. }
              updatedAt: { type: string, format: date-time }
        accuracy:
          type: array
          description: Daily forecast-vs-actual metrics per model (Europe/Berlin days).
          items:
            type: object
            properties:
              day: { type: string, format: date }
              model: { type: string }
              kind: { type: string, enum: [load, pv] }
              maeKw: { type: number, description: Mean absolute error over the day's evaluated 15-min slots. }
              nmaePct: { type: number, nullable: true, description: MAE / mean absolute actual x 100; null on all-zero days. }
              biasKw: { type: number, nullable: true, description: Mean signed error (forecast - actual). }
              skillVsBaseline: { type: number, nullable: true, description: "1 - mae/mae_reference; positive = better than the ACTIVE model of that kind; null for the reference itself. (While nothing is promoted the active model IS the baseline, so the numbers are unchanged; after a promotion the roles swap.)" }
              nSlots: { type: integer }
        planAccuracy:
          type: array
          description: Daily plan-vs-actual economics of the optimizer's persisted plans.
          items:
            type: object
            properties:
              day: { type: string, format: date }
              plannedCostEur: { type: number, nullable: true }
              baselineCostEur: { type: number, nullable: true, description: Projected cost with the battery idle. }
              realizedCostEur: { type: number, nullable: true, description: Actual signed grid cost (telemetry x price). }
              nSlots: { type: integer }
    SiteKpis:
      type: object
      properties:
        siteId: { type: string, format: uuid }
        selfConsumptionPct: { type: number }
        estimatedSavingsEur: { type: number }
        window: { type: string, example: last_30_days }
    Tenant:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        segment: { type: string, enum: [CI, B2C], example: CI }
        plan: { type: string, example: mvp }
        betriebsart:
          type: string
          enum: [endkunde, betreiber]
          nullable: true
          description: >
            The explicit U0 shell-frame override; null = automatic (the portal
            derives the shell from the site count).
        betriebsartEffective:
          type: string
          enum: [endkunde, betreiber]
          nullable: true
          description: >
            The resolved Betriebsart the portal shell keys on - the override
            when set, else null = unknown (never segment-derived).
        createdAt: { type: string, format: date-time }
    TenantContext:
      type: object
      description: >
        The caller's tenant as the portal's login bootstrap reads it.
        `betriebsart` is the EFFECTIVE shell frame (the override when set, else
        null = unknown) - resolved server-side in the one authoritative place.
      properties:
        tenantId: { type: string, format: uuid }
        name: { type: string }
        segment: { type: string, enum: [CI, B2C] }
        betriebsart: { type: string, enum: [endkunde, betreiber], nullable: true }
    Unternehmen:
      type: object
      description: >
        GET /api/v1/unternehmen (UEMS AP-02 IP-3). Zahlen gelten heute.
        `zustand` = `nicht_angelegt` für einen Kundenbereich ohne Unternehmen-Zeile:
        dann sind id, name, kurzname und zeitzone null (nie erfunden).
      required: [zustand, standortZahl, anlagenZahl, nochNichtZugeordnetZahl]
      properties:
        zustand: { type: string, enum: [angelegt, nicht_angelegt] }
        id: { type: string, format: uuid, nullable: true }
        name: { type: string, nullable: true, example: Kunststoffwerk Ahrenberg GmbH }
        kurzname: { type: string, nullable: true, example: Ahrenberg }
        zeitzone:
          type: string
          nullable: true
          enum: [Europe/Berlin, Europe/Vienna, Europe/Zurich]
        standortZahl:
          type: integer
          description: Standorte, die es heute gibt (archivierte zählen nicht).
        anlagenZahl: { type: integer, description: Anlagen des Kundenbereichs. }
        nochNichtZugeordnetZahl:
          type: integer
          description: Anlagen, die heute keinem Standort zugeordnet sind.
        sitz:
          nullable: true
          description: Der Sitz (additiv, IP-4) - null, wenn kein Feld angegeben ist; nur Anzeige und Berichtskopf.
          allOf: [{ $ref: "#/components/schemas/StandortAdresse" }]
        rechtsform: { type: string, nullable: true, example: GmbH, description: "Additiv (IP-4)." }
    UnternehmenBearbeiten:
      type: object
      description: >
        PUT /api/v1/unternehmen (UEMS AP-02 IP-4) - die ganze Menge; ein
        fehlendes Feld ist leer. Eine PLZ braucht ihr Land (DE fünf-, AT/CH
        vierstellig).
      required: [name, zeitzone]
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        kurzname: { type: string, nullable: true, maxLength: 24 }
        zeitzone: { type: string, enum: [Europe/Berlin, Europe/Vienna, Europe/Zurich] }
        sitz:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/StandortAdresse" }]
        rechtsform: { type: string, nullable: true, maxLength: 40 }
    StandortStammdaten:
      type: object
      description: >
        POST und PUT /api/v1/standorte (UEMS AP-02 IP-4) - die Stammdaten (§4.1).
        POST: `kurzzeichen` optional (fehlt: automatisch ST-n), `zeitzone`
        optional (fehlt: die Vorgabe des Unternehmens), die Adresse Pflicht
        (Straße, Ort, Land; PLZ optional). PUT: die ganze Menge, `kurzzeichen`
        und `zeitzone` Pflicht, die Adresse nur außerhalb des Entwurfs.
        `nutzung` sind Codes des geschlossenen Vokabulars (E4), jede höchstens
        einmal, die erste ist die Hauptnutzung. Die Bezugsfläche (IP-5) und die
        Anlagen-Zuordnung (IP-11) haben eigene Routen - hier sind sie 400.
      required: [name]
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1, maxLength: 120, example: Werk Ahrenberg }
        kurzzeichen: { type: string, nullable: true, maxLength: 24, example: ST-1 }
        adresse:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/StandortAdresse" }]
        zeitzone: { type: string, nullable: true, enum: [Europe/Berlin, Europe/Vienna, Europe/Zurich] }
        nutzung:
          type: array
          nullable: true
          items: { $ref: "#/components/schemas/Nutzung" }
        notiz: { type: string, nullable: true, maxLength: 500 }
        lage:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/StandortLage" }]
    StandortWiederherstellen:
      type: object
      description: Optional ein neuer Name - das Umbenennen im selben Dialog, wenn der alte inzwischen vergeben ist.
      additionalProperties: false
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
    StandortKurzzeichenVorschlag:
      type: object
      properties:
        kurzzeichen: { type: string, example: ST-3 }
    StandortZuordnungAnlage:
      type: object
      required: [vorschlagId, anlageId, anlageName, gueltigAb]
      properties:
        vorschlagId: { type: string, format: uuid }
        anlageId: { type: string, format: uuid }
        anlageName: { type: string }
        gueltigAb: { type: string, format: date }
    StandortZuordnungGruppe:
      type: object
      required: [name, zeitzone, anlagen]
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        zeitzone: { type: string, enum: [Europe/Berlin, Europe/Vienna, Europe/Zurich] }
        adresse:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/StandortAdresse" }]
        anlagen:
          type: array
          minItems: 1
          items: { $ref: "#/components/schemas/StandortZuordnungAnlage" }
    StandortZuordnungVorschau:
      type: object
      required: [gruppen, anlagenZahl]
      properties:
        gruppen:
          type: array
          items: { $ref: "#/components/schemas/StandortZuordnungGruppe" }
        anlagenZahl: { type: integer, minimum: 0 }
    StandortZuordnungBestaetigen:
      type: object
      required: [gruppen]
      additionalProperties: false
      properties:
        gruppen:
          type: array
          minItems: 1
          items:
            type: object
            required: [name, adresse, vorschlagIds]
            additionalProperties: false
            properties:
              name: { type: string, minLength: 1, maxLength: 120 }
              zeitzone: { type: string, nullable: true, enum: [Europe/Berlin, Europe/Vienna, Europe/Zurich] }
              adresse: { $ref: "#/components/schemas/StandortAdresse" }
              vorschlagIds:
                type: array
                minItems: 1
                uniqueItems: true
                items: { type: string, format: uuid }
    StandortZuordnungErgebnis:
      type: object
      required: [standortIds, zuordnungen]
      properties:
        standortIds:
          type: array
          items: { type: string, format: uuid }
        zuordnungen: { type: integer, minimum: 1 }
    Nutzung:
      type: string
      description: AP-02 E4 - Code = Kundenwort in Kleinbuchstaben, ä→ae, ö→oe, ü→ue, ß→ss.
      enum: [produktion, montage, lager, logistik, buero, technik, aussenflaeche, werkstatt, labor,
             verkauf, sozialraeume, sonstiges]
    StandortLage:
      type: object
      description: Die Lage auf der Karte - beide Koordinaten oder keine (W4).
      properties:
        breitengrad: { type: number, minimum: -90, maximum: 90, example: 48.25 }
        laengengrad: { type: number, minimum: -180, maximum: 180, example: 11.43 }
    OrtFehler:
      type: object
      description: >
        Die Ablehnung der Ortsstruktur-Schreibrouten (UEMS AP-02 IP-4). Fakten
        je Code: `anfrage_ungueltig` → `feld`; `name_belegt` und
        `kurzzeichen_belegt` → `verweis` (der Träger - bei Kurzzeichen auch
        `archiviert`, `frueher`, `heute`); `archiviert` → `archiviert_am`;
        `archivieren_gesperrt` → `gruende`; `wiederherstellen_gesperrt` →
        `grund` (nicht_archiviert | eltern_archiviert | name_belegt) und bei
        name_belegt `verweis`. Gebäude/Bereich (IP-5): die Gründe des
        Ortsbaum-Vertrags mit seinem Satz - `ziel_art_unzulaessig` (400),
        `ziel_gab_es_noch_nicht` (422), `ziel_archiviert` (409) → `feld`
        (elternId); `flaeche_ungueltig` und `gleiche_flaeche` (400),
        `gab_es_noch_nicht` (422), `archiviert` (409) an der Fläche → `feld`.
        Anlegen einer Anlage (`POST /api/v1/sites`, IP-9): `standort_waehlen`
        (422) → `feld` (standortId) und `standorte` (die Auswahl);
        `ziel_archiviert` (409) und `anfrage_ungueltig` (400) → `feld`.
        Löschen ohne Historie (IP-15): `loeschen_gesperrt` (409) → `historie`.
        Anlage zuordnen/umziehen (IP-11): `vor_dem_ersten_intervall` (422),
        `objekt_archiviert` (409), `gleicher_tag` (409) → `feld` (gueltigAb);
        `ziel_ist_bisheriger_eltern` (400) → `feld` (standortId).
      required: [code, message]
      properties:
        code:
          type: string
          enum: [anfrage_ungueltig, nicht_gefunden, name_belegt, kurzzeichen_belegt, archiviert,
                 archivieren_gesperrt, wiederherstellen_gesperrt, ziel_art_unzulaessig,
                 ziel_gab_es_noch_nicht, ziel_archiviert, flaeche_ungueltig, gab_es_noch_nicht,
                 gleiche_flaeche, standort_waehlen, loeschen_gesperrt, vor_dem_ersten_intervall,
                 objekt_archiviert, gleicher_tag, ziel_ist_bisheriger_eltern, vorschlag_geaendert]
        message: { type: string }
        feld: { type: string }
        verweis:
          type: object
          properties:
            objekt_art: { type: string, enum: [standort, gebaeude, bereich] }
            id: { type: string, format: uuid, nullable: true }
            kurzzeichen: { type: string }
            name: { type: string, nullable: true }
            heute: { type: string, nullable: true, description: Das heutige Kurzzeichen des Trägers. }
            archiviert: { type: boolean }
            frueher: { type: boolean }
        archiviert_am: { type: string, format: date-time, nullable: true }
        grund: { type: string, enum: [nicht_archiviert, eltern_archiviert, name_belegt] }
        gruende:
          type: array
          items: { $ref: "#/components/schemas/ArchivSperrgrund" }
        historie:
          type: array
          description: >
            `loeschen_gesperrt`: was der Ort je getragen hat, in der Reihenfolge des
            Vertrags (`gruende_loeschen`); `hat_bezugsgroessen` ergänzt die Datenbank
            (AP-09), `hat_kennzahlen` ebenso (AP-11). Leer, wenn erst die Datenbank widersprochen hat.
          items: { $ref: "#/components/schemas/OrtLoeschGrund" }
        standorte:
          type: array
          description: Nur bei `standort_waehlen` - die nicht archivierten Standorte zur Wahl.
          items:
            type: object
            required: [id, kurzzeichen, name]
            properties:
              id: { type: string, format: uuid }
              kurzzeichen: { type: string }
              name: { type: string }
    ArchivSperrgrund:
      type: object
      description: >
        Ein Sperrgrund beim Archivieren, in der festen Reihenfolge des Vertrags
        (docs/contracts/v2/ortsbaum-vectors.json `gruende_archivieren`).
        `kennzeichen` ist das Kurzzeichen eines Orts bzw. das Kennzeichen einer
        Messstelle; eine Anlage nennt ihre `id`. `weg` ist der Weg als Code.
      properties:
        art:
          type: string
          enum: [gab_es_noch_nicht, archiviert, anlage_aktiv, messstelle_aktiv, geplante_zuordnung]
        objekt: { type: string, enum: [anlage, messstelle, standort, gebaeude, bereich] }
        id: { type: string, format: uuid, nullable: true }
        kennzeichen: { type: string, nullable: true }
        name: { type: string }
        ab: { type: string, format: date, description: Nur bei geplante_zuordnung. }
        eltern: { type: string, description: Nur bei geplante_zuordnung - wohin. }
        weg:
          type: string
          nullable: true
          enum: [anlage_zuordnen, messstelle_umziehen, zuordnung_aufheben, null]
    StandorteAmStichtag:
      type: object
      description: >
        GET /api/v1/standorte (UEMS AP-02 IP-3). Die Liste schneidet der
        Standort-Zaun (AP-03 IP-5, `site_scope` auf `standort`): ein Standort
        ausserhalb des Zugriffs steht weder unter `standorte` noch unter
        `nichtGezeigt`. `teilansicht {sichtbar, gesamt}` ist additiv (AP-03
        IP-10).
      required: [stichtag, standorte, nichtGezeigt, teilansicht]
      properties:
        stichtag: { type: string, format: date }
        standorte:
          type: array
          description: Die Standorte, die es am Stichtag gab, in der Reihenfolge des Anlegens.
          items: { $ref: "#/components/schemas/StandortAmStichtag" }
        nichtGezeigt:
          type: array
          description: >
            Standorte, die es am Stichtag noch nicht gab oder die archiviert
            waren - mit `bestand` und `bestandText`
            ("Am 15.09.2026 gab es Werk Ahrenberg im Portal noch nicht.").
          items: { $ref: "#/components/schemas/StandortAmStichtag" }
        nochNichtZugeordnet:
          nullable: true
          description: >
            Die Gruppe "Noch nicht zugeordnet" - null, sobald jede Anlage am
            Stichtag einem Standort zugeordnet ist (kein "0 nicht zugeordnet", A15).
          allOf: [{ $ref: "#/components/schemas/NochNichtZugeordnet" }]
        teilansicht:
          description: Additiv (AP-03 IP-10).
          allOf: [{ $ref: "#/components/schemas/Teilansicht" }]
    StandortAmStichtag:
      type: object
      description: >
        Ein Standort zum Stichtag. Zeitgültig sind `bestand`, `anlagen`,
        die Zahlen und die Fläche; Name, Adresse, Zeitzone, Zustand und
        `esFehlt` sind einfache Felder und stehen wie heute. Ohne Bestand am
        Stichtag: `anlagen` leer, Zahlen und Fläche null - nie eine 0.
      properties:
        id: { type: string, format: uuid }
        kurzzeichen: { type: string, example: ST-1 }
        name: { type: string, example: Werk Ahrenberg }
        adresse:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/StandortAdresse" }]
        zeitzone: { type: string, enum: [Europe/Berlin, Europe/Vienna, Europe/Zurich] }
        zustand: { type: string, enum: [entwurf, eingerichtet, aktiv, archiviert] }
        esFehlt:
          type: array
          description: >
            Was einem Entwurf zum Einrichten fehlt ("Noch nicht eingerichtet -
            es fehlt: Adresse"); leer außerhalb des Entwurfs.
          items: { type: string, enum: [adresse] }
        bestand: { type: string, enum: [vorhanden, gab_es_noch_nicht, archiviert] }
        bestandText:
          type: string
          nullable: true
          description: Der Satz zum Bestand, nur wenn nicht `vorhanden`.
        anlagen:
          type: array
          items: { $ref: "#/components/schemas/StandortAnlage" }
        anlagenZahl: { type: integer, nullable: true }
        gebaeudeZahl: { type: integer, nullable: true }
        bereichZahl: { type: integer, nullable: true }
        flaecheM2:
          type: integer
          nullable: true
          description: >
            Die eigene Bezugsfläche des Standorts, sonst die Summe seiner
            Gebäude - nur, wenn jedes Gebäude eine hat; null, wenn keine da
            ist (nie 0; Bereiche werden nie addiert).
        flaecheQuelle:
          type: string
          nullable: true
          enum: [eigen, aus_gebaeuden_summiert]
        nutzung:
          type: array
          nullable: true
          description: Additiv (IP-4) - Codes, die erste ist die Hauptnutzung; null = nichts gewählt.
          items: { $ref: "#/components/schemas/Nutzung" }
        notiz: { type: string, nullable: true, description: "Additiv (IP-4)." }
        lage:
          nullable: true
          description: Additiv (IP-4).
          allOf: [{ $ref: "#/components/schemas/StandortLage" }]
        archiviertAm:
          type: string
          format: date-time
          nullable: true
          description: Additiv (IP-4) - wann er archiviert wurde; null, solange er es nicht ist.
    StandortAusfall:
      type: object
      required: [standort_id, boxen_gesamt, boxen_ausgefallen, messstellen_unvollstaendig, boxen, messstellen]
      properties:
        standort_id: { type: string, format: uuid }
        boxen_gesamt: { type: integer, minimum: 0 }
        boxen_ausgefallen: { type: integer, minimum: 0 }
        messstellen_unvollstaendig: { type: integer, minimum: 0 }
        boxen:
          type: array
          items:
            type: object
            required: [id, name, seit, anlagen]
            properties:
              id: { type: string, format: uuid }
              name: { type: string }
              seit: { type: string, format: date-time }
              anlagen: { type: array, items: { type: string, format: uuid } }
        messstellen:
          type: array
          items:
            type: object
            required: [id, kennzeichen, art, fehlt]
            properties:
              id: { type: string, format: uuid }
              kennzeichen: { type: string }
              name: { type: string, nullable: true }
              art: { type: string, enum: [gemessen, berechnet] }
              seit: { type: string, format: date-time, nullable: true }
              box_id: { type: string, format: uuid, nullable: true }
              box: { type: string, nullable: true }
              fehlt: { type: array, items: { type: string } }
    Versorgung:
      type: object
      required: [stichtag, standort, gebaeude, ausserhalbGebaeude]
      properties:
        stichtag: { type: string, format: date }
        standort: { $ref: "#/components/schemas/VersorgungBezug" }
        gebaeude:
          type: array
          items: { $ref: "#/components/schemas/GebaeudeVersorgung" }
        ausserhalbGebaeude:
          type: array
          description: Messstellen am Standort, aber außerhalb jedes Gebäudes; keine davon wird still zugeschlagen.
          items: { $ref: "#/components/schemas/VersorgungAusserhalb" }
    VersorgungBezug:
      type: object
      required: [id, kennzeichen, name]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, example: G-2 }
        name: { type: string, example: Halle 2 }
    VersorgungAnlage:
      type: object
      required: [id, name]
      properties:
        id: { type: string, format: uuid }
        name: { type: string, example: Werk Ahrenberg – Halle 2 }
        netzanschlussKennzeichen: { type: string, nullable: true, example: NA-2 }
    VersorgungMessstelle:
      type: object
      required: [kennzeichen]
      properties:
        kennzeichen: { type: string, example: MS-11 }
        name: { type: string, nullable: true, example: Spritzguss Halle 2 }
    GebaeudeVersorgung:
      type: object
      required: [gebaeude, messbar, systeme]
      properties:
        gebaeude: { $ref: "#/components/schemas/VersorgungBezug" }
        messbar: { type: boolean, description: false, wenn kein System durch eine wirksame Messstelle belegt ist. }
        systeme:
          type: array
          items:
            type: object
            required: [anlage, messstellen]
            properties:
              anlage: { $ref: "#/components/schemas/VersorgungAnlage" }
              messstellen:
                type: array
                items: { $ref: "#/components/schemas/VersorgungMessstelle" }
    VersorgungAusserhalb:
      type: object
      required: [messstelle, anlage]
      properties:
        messstelle: { $ref: "#/components/schemas/VersorgungMessstelle" }
        anlage: { $ref: "#/components/schemas/VersorgungAnlage" }
    OrtsbaumAmStichtag:
      type: object
      description: >
        GET /api/v1/standorte/{id}/orte (UEMS AP-02 IP-5). Additiv vorgesehen:
        `teilansicht` (AP-03 IP-10).
      required: [stichtag, standort, gebaeudeOhneFlaeche, gebaeude]
      properties:
        stichtag: { type: string, format: date }
        standort: { $ref: "#/components/schemas/StandortAmStichtag" }
        summeGebaeudeM2:
          type: integer
          nullable: true
          description: >
            Die Summe der Gebäudeflächen am Stichtag - nur, wenn jedes Gebäude
            eine hat (sonst null); ein Hinweis neben der eigenen Fläche, nie
            saldiert.
        gebaeudeOhneFlaeche:
          type: array
          description: Die Gebäude, denen am Stichtag die Fläche fehlt ("für kWh/m² fehlt die Fläche").
          items: { type: string, format: uuid }
        gebaeude:
          type: array
          description: Die Gebäude am Standort, in der Reihenfolge des Anlegens.
          items: { $ref: "#/components/schemas/OrtsbaumGebaeude" }
        direktAmStandort:
          nullable: true
          description: Was ohne Gebäude am Standort hängt - null, wenn es den Standort am Stichtag nicht gab.
          allOf: [{ $ref: "#/components/schemas/OrtsbaumDirektAmStandort" }]
        archiviert:
          type: array
          description: >
            IP-15 (Z3): die Gebäude und Bereiche dieses Standorts, die am Stichtag
            archiviert waren - im Baum fehlen sie (§4.4), hier stehen sie mit dem
            Archivtag und dem Knoten, an dem sie zuletzt hingen. Leer, wenn es den
            Standort am Stichtag nicht gab.
          items: { $ref: "#/components/schemas/OrtsbaumArchivierterOrt" }
        aktionen:
          nullable: true
          description: IP-15 - was man heute mit dem Standort tun kann (nur `archivieren`); null mit Stichtag.
          allOf: [{ $ref: "#/components/schemas/OrtAktionen" }]
    OrtsbaumArchivierterOrt:
      type: object
      properties:
        id: { type: string, format: uuid }
        art: { type: string, enum: [gebaeude, bereich] }
        kurzzeichen: { type: string, example: B-5 }
        name: { type: string, example: Halle 2 Lager }
        nutzung: { type: array, nullable: true, items: { $ref: "#/components/schemas/Nutzung" } }
        archiviertAm: { type: string, format: date, example: "2027-06-30", description: Der Tag nach dem Ende des letzten Intervalls vor dem Stichtag. }
        elternId: { type: string, format: uuid, description: Der Knoten, an dem der Ort zuletzt hing. }
        elternArt: { type: string, enum: [standort, gebaeude] }
        aktionen:
          nullable: true
          description: Wiederherstellen und Löschen - null mit Stichtag.
          allOf: [{ $ref: "#/components/schemas/OrtAktionen" }]
    OrtAktionen:
      type: object
      description: >
        IP-15: was man HEUTE mit einem Knoten des Ortsbaums tun kann, bevor jemand
        einen Knopf drückt - dieselben Urteile und Sätze wie die Schreibrouten.
        Ein Knoten im Baum: `archivieren` + `loeschen`; ein archivierter:
        `wiederherstellen` + `loeschen`; der Standort: `archivieren`. Was nicht
        dazugehört, ist null.
      properties:
        archivieren:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/OrtArchivierenAktion" }]
        wiederherstellen:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/OrtWiederherstellenAktion" }]
        loeschen:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/OrtLoeschenAktion" }]
        verschieben:
          nullable: true
          description: IP-12 - nur an einem Gebäude oder Bereich, der heute im Baum steht.
          allOf: [{ $ref: "#/components/schemas/OrtVerschiebenAktion" }]
    OrtVerschiebenAktion:
      type: object
      description: >
        IP-12 (V1/V2): wohin der Knoten heute ziehen kann - jeder Knoten, der heute
        im Baum steht und als Elternknoten erlaubt ist (nie ein Bereich), ohne den
        bisherigen. Ob der Tag geht, urteilt die Vorschau.
      properties:
        erlaubt: { type: boolean }
        text: { type: string, nullable: true, description: Ohne Ziel - warum nicht. }
        ziele: { type: array, items: { $ref: "#/components/schemas/OrtVerschiebenZiel" } }
    OrtVerschiebenZiel:
      type: object
      properties:
        id: { type: string, format: uuid }
        art: { type: string, enum: [standort, gebaeude] }
        kurzzeichen: { type: string }
        name: { type: string }
        standortName: { type: string, nullable: true, description: Nur beim Gebäude. }
    OrtsbaumDanach:
      type: object
      description: IP-12 (V4) - die schon eingetragene Zuordnung ab dem Tag nach `gueltigBis` ("ab 01.03.2027 → Werk Ahrenberg Nord").
      properties:
        ab: { type: string, format: date }
        elternId: { type: string, format: uuid }
        elternArt: { type: string, enum: [standort, gebaeude] }
        elternName: { type: string, nullable: true }
        standortId: { type: string, format: uuid, nullable: true }
        standortName: { type: string, nullable: true }
    OrtArchivierenAktion:
      type: object
      properties:
        erlaubt: { type: boolean }
        text: { type: string, nullable: true, description: Gesperrt - der Satz mit Grund und Weg (Z1), wie `message` der 409. }
        gruende: { type: array, items: { $ref: "#/components/schemas/ArchivSperrgrund" } }
        letzterTag: { type: string, format: date, nullable: true, description: Erlaubt - der letzte Tag der Zuordnung (gestern). }
        mitarchiviert:
          type: array
          description: Erlaubt - die leeren Kinder, die mitarchiviert werden.
          items: { $ref: "#/components/schemas/OrtMitarchiviert" }
    OrtMitarchiviert:
      type: object
      properties:
        id: { type: string, format: uuid }
        art: { type: string, enum: [gebaeude, bereich] }
        kurzzeichen: { type: string }
        name: { type: string }
    OrtWiederherstellenAktion:
      type: object
      properties:
        erlaubt: { type: boolean }
        grund: { type: string, nullable: true, enum: [nicht_archiviert, eltern_archiviert, name_belegt] }
        text: { type: string, nullable: true, description: Gesperrt - der Satz; bei name_belegt öffnet der Dialog zum Umbenennen. }
        ab: { type: string, format: date, description: Der erste Tag des neuen Intervalls (heute). }
    OrtLoeschenAktion:
      type: object
      properties:
        erlaubt: { type: boolean }
        gruende: { type: array, items: { $ref: "#/components/schemas/OrtLoeschGrund" } }
        text: { type: string, nullable: true, description: Gesperrt - der Satz, der die Historie nennt. }
    OrtLoeschGrund:
      type: string
      enum: [hat_messstellen, hat_anlagen, hat_flaeche, hat_kinder, hat_bezugsgroessen, hat_kennzahlen]
    OrtsbaumGebaeude:
      type: object
      properties:
        id: { type: string, format: uuid }
        kurzzeichen: { type: string, example: G-2 }
        name: { type: string, example: Halle 2 }
        nutzung: { type: array, nullable: true, description: "E4 - das erste Wort ist die Hauptnutzung; null = keine gewählt.", items: { $ref: "#/components/schemas/Nutzung" } }
        baujahr: { type: integer, nullable: true, example: 2019 }
        notiz: { type: string, nullable: true }
        zustand: { type: string, enum: [entwurf, eingerichtet, aktiv, archiviert] }
        gueltigAb: { type: string, format: date, description: Die am Stichtag gültige Zuordnung. }
        gueltigBis: { type: string, format: date, nullable: true, description: Einschließlich; null = offen. }
        flaecheM2: { type: integer, nullable: true, description: Die eigene Fläche am Stichtag - null, nie 0. }
        flaecheQuelle: { type: string, nullable: true, enum: [eigen] }
        messstellenZahl:
          type: integer
          nullable: true
          description: "Die Messstellen, deren Ort am Stichtag dieses Gebäude selbst ist (nicht seine Bereiche); 0 wenn keine, null nur ohne Messstellen-Quelle."
        datenlage:
          nullable: true
          description: Dieselbe Register-Zählung für das Gebäude einschließlich seiner Bereiche.
          allOf: [{ $ref: "#/components/schemas/OrtsbaumDatenlage" }]
        bereiche:
          type: array
          items: { $ref: "#/components/schemas/OrtsbaumBereich" }
        danach:
          nullable: true
          description: IP-12 (V4) - wohin der Knoten nach `gueltigBis` zieht; null ohne Ende.
          allOf: [{ $ref: "#/components/schemas/OrtsbaumDanach" }]
        aktionen:
          nullable: true
          description: IP-15 - Archivieren und Löschen; null mit Stichtag.
          allOf: [{ $ref: "#/components/schemas/OrtAktionen" }]
    OrtsbaumBereich:
      type: object
      description: Ein Bereich erbt nie die Fläche seines Gebäudes (§4.1).
      properties:
        id: { type: string, format: uuid }
        kurzzeichen: { type: string, example: B-3 }
        name: { type: string, example: Halle 2 Montage }
        nutzung: { type: array, nullable: true, description: "E4 - das erste Wort ist die Hauptnutzung; null = keine gewählt.", items: { $ref: "#/components/schemas/Nutzung" } }
        notiz: { type: string, nullable: true }
        zustand: { type: string, enum: [entwurf, eingerichtet, aktiv, archiviert] }
        gueltigAb: { type: string, format: date }
        gueltigBis: { type: string, format: date, nullable: true }
        flaecheM2: { type: integer, nullable: true }
        flaecheQuelle: { type: string, nullable: true, enum: [eigen] }
        messstellenZahl: { type: integer, nullable: true, description: "Die Messstellen, deren Ort am Stichtag genau dieser Knoten ist; 0 wenn keine." }
        datenlage:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/OrtsbaumDatenlage" }]
        danach:
          nullable: true
          description: IP-12 (V4) - wohin der Knoten nach `gueltigBis` zieht; null ohne Ende.
          allOf: [{ $ref: "#/components/schemas/OrtsbaumDanach" }]
        aktionen:
          nullable: true
          description: IP-15 - Archivieren und Löschen; null mit Stichtag.
          allOf: [{ $ref: "#/components/schemas/OrtAktionen" }]
    OrtsbaumDirektAmStandort:
      type: object
      properties:
        bereiche:
          type: array
          items: { $ref: "#/components/schemas/OrtsbaumBereich" }
        messstellenZahl: { type: integer, nullable: true, description: "Die Messstellen, deren Ort am Stichtag genau dieser Knoten ist; 0 wenn keine." }
        datenlage:
          nullable: true
          description: Dieselbe Register-Zählung für die direkt am Standort hängenden Messstellen und Bereiche.
          allOf: [{ $ref: "#/components/schemas/OrtsbaumDatenlage" }]
    OrtsbaumDatenlage:
      type: object
      required: [erfuellt, gesamt, text]
      properties:
        erfuellt: { type: integer, minimum: 0 }
        gesamt: { type: integer, minimum: 0 }
        text: { type: string, example: "4 von 5 Messstellen liefern Daten" }
    OrtAnlegen:
      type: object
      additionalProperties: false
      required: [art, name]
      properties:
        art: { type: string, enum: [gebaeude, bereich] }
        name: { type: string, minLength: 1, maxLength: 120, description: Eindeutig unter den Geschwistern am Tag. }
        kurzzeichen:
          type: string
          nullable: true
          maxLength: 24
          description: Fehlend = automatisch (G-n / B-n).
        elternId:
          type: string
          format: uuid
          nullable: true
          description: Nur Bereich - das Gebäude dieses Standorts; fehlend = direkt am Standort.
        gueltigAb: { type: string, format: date, nullable: true, description: Fehlend = heute in der Zeitzone des Standorts. }
        nutzung: { type: array, nullable: true, description: "E4 - das erste Wort ist die Hauptnutzung; null = keine gewählt.", items: { $ref: "#/components/schemas/Nutzung" } }
        baujahr: { type: integer, nullable: true, description: Nur Gebäude; 1800 ... laufendes Jahr. }
        notiz: { type: string, nullable: true, maxLength: 500 }
        flaecheM2: { type: integer, nullable: true, minimum: 1, description: Die erste Fläche, ab gueltigAb. }
    OrtBearbeiten:
      type: object
      additionalProperties: false
      required: [name, kurzzeichen]
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        kurzzeichen: { type: string, maxLength: 24 }
        nutzung: { type: array, nullable: true, description: "E4 - das erste Wort ist die Hauptnutzung; null = keine gewählt.", items: { $ref: "#/components/schemas/Nutzung" } }
        baujahr: { type: integer, nullable: true }
        notiz: { type: string, nullable: true, maxLength: 500 }
    OrtFlaeche:
      type: object
      additionalProperties: false
      required: [m2]
      properties:
        m2: { type: integer, minimum: 1, example: 3400 }
        gueltigAb: { type: string, format: date, nullable: true, example: "2027-01-01" }
    Ort:
      type: object
      description: >
        Ein Gebäude oder Bereich nach dem Schreiben. `standortId`: der Standort,
        an dessen Baum der Ort heute hängt (sonst der seines ersten Tages).
        `rueckwirkung` gehört zum "gültig ab" dieses Vorgangs - null beim
        Bearbeiten (einfache Felder gelten ab sofort). Keine Zeitzone: sie ist
        die des Standorts (A16).
      properties:
        id: { type: string, format: uuid }
        art: { type: string, enum: [gebaeude, bereich] }
        kurzzeichen: { type: string, example: G-2 }
        name: { type: string, example: Halle 2 }
        nutzung: { type: array, nullable: true, description: "E4 - das erste Wort ist die Hauptnutzung; null = keine gewählt.", items: { $ref: "#/components/schemas/Nutzung" } }
        baujahr: { type: integer, nullable: true }
        notiz: { type: string, nullable: true }
        zustand: { type: string, enum: [entwurf, eingerichtet, aktiv, archiviert] }
        standortId: { type: string, format: uuid, nullable: true }
        zuordnungen:
          type: array
          items: { $ref: "#/components/schemas/OrtZuordnung" }
        flaechen:
          type: array
          items: { $ref: "#/components/schemas/OrtFlaechenStand" }
        rueckwirkung:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/OrtRueckwirkung" }]
    OrtZuordnung:
      type: object
      properties:
        elternId: { type: string, format: uuid }
        elternArt: { type: string, enum: [standort, gebaeude] }
        elternName: { type: string }
        gueltigAb: { type: string, format: date }
        gueltigBis: { type: string, format: date, nullable: true }
        zustand: { type: string, enum: [gueltig, geplant, beendet] }
    OrtFlaechenStand:
      type: object
      properties:
        m2: { type: integer, example: 3100 }
        gueltigAb: { type: string, format: date, example: "2026-10-01" }
        gueltigBis: { type: string, format: date, nullable: true, example: "2026-12-31" }
        zustand: { type: string, enum: [gueltig, geplant, beendet] }
    OrtRueckwirkung:
      type: object
      description: E2 - erlaubt, aber sichtbar; der Eintragstag in der Zeitzone des Standorts.
      properties:
        art: { type: string, enum: [rueckwirkend, ab_heute, geplant] }
        tage: { type: integer, description: Abstand zwischen "gilt ab" und Eintragstag. }
        abzeichen: { type: string, nullable: true, example: rückwirkend (14 Tage) }
    StandortAdresse:
      type: object
      properties:
        strasse: { type: string, nullable: true }
        plz: { type: string, nullable: true }
        ort: { type: string, nullable: true }
        land: { type: string, nullable: true, enum: [DE, AT, CH] }
    StandortAnlage:
      type: object
      description: Eine Anlage am Standort mit dem am Stichtag gültigen Intervall (`gueltigBis` einschließlich) und ihrem Netzanschluss an dem Tag.
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        gueltigAb: { type: string, format: date }
        gueltigBis: { type: string, format: date, nullable: true }
        netzanschluss:
          nullable: true
          description: >
            Additiv (AP-10 IP-6) - der Netzanschluss, an dem die Anlage am Stichtag hängt, mit den
            Tagen der Bindung; null, wenn keiner gebunden ist (nie erfunden).
          type: object
          properties:
            id: { type: string, format: uuid }
            kennzeichen: { type: string, example: NA-1 }
            gueltigAb: { type: string, format: date }
            gueltigBis: { type: string, format: date, nullable: true }
    NochNichtZugeordnet:
      type: object
      properties:
        anlagenZahl: { type: integer }
        anlagen:
          type: array
          items:
            type: object
            properties:
              id: { type: string, format: uuid }
              name: { type: string }
    StandortBezug:
      type: object
      description: >
        Der Standort, dem eine Anlage heute zugeordnet ist (UEMS AP-02 IP-3);
        `gueltigAb` ist der Beginn der laufenden Zuordnung.
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        kurzzeichen: { type: string }
        gueltigAb: { type: string, format: date }
    SiteDetail:
      description: GET /api/v1/sites/{siteId} - the Site fields unchanged, plus `standort`.
      allOf:
        - { $ref: "#/components/schemas/Site" }
        - type: object
          properties:
            standort:
              nullable: true
              allOf: [{ $ref: "#/components/schemas/StandortBezug" }]
    AnlageUmzugAnfrage:
      type: object
      additionalProperties: false
      required: [standortId]
      properties:
        standortId: { type: string, format: uuid }
        gueltigAb: { type: string, format: date, description: Fehlend = heute in der Zeitzone des Ziels. }
        begruendung: { type: string, maxLength: 500, nullable: true }
    AnlageUmzugStandort:
      type: object
      properties:
        id: { type: string, format: uuid }
        kurzzeichen: { type: string, nullable: true }
        name: { type: string, nullable: true }
    AnlageUmzugZuordnung:
      type: object
      properties:
        standort: { $ref: "#/components/schemas/AnlageUmzugStandort" }
        gueltigAb: { type: string, format: date }
        gueltigBis: { type: string, format: date, nullable: true, description: Der letzte Tag, einschließlich. }
        zustand: { type: string, enum: [gueltig, geplant, beendet, aufgehoben] }
    AnlageUmzug:
      type: object
      description: >
        Vorschau und Eintrag der Zuordnung einer Anlage (UEMS AP-02 IP-11). `bleibt`
        nennt in fester Reihenfolge, was die Zuordnung nie berührt - `box` nur mit
        einer eingebauten Box, `ladepark_rahmen` nur mit Ladepark-Rahmen;
        `befehle` ist immer 0.
      properties:
        anlageId: { type: string, format: uuid }
        anlageName: { type: string }
        bisher:
          nullable: true
          description: Der Standort am "gültig ab" ohne die Zuordnung; null = noch keiner.
          allOf: [{ $ref: "#/components/schemas/AnlageUmzugStandort" }]
        neu: { $ref: "#/components/schemas/AnlageUmzugStandort" }
        gueltigAb: { type: string, format: date }
        gueltigBis: { type: string, format: date, nullable: true, description: Von der laufenden Zuordnung geerbt. }
        danach:
          nullable: true
          description: Die schon geplante Zuordnung ab dem Tag nach `gueltigBis`.
          allOf: [{ $ref: "#/components/schemas/AnlageUmzugStandort" }]
        rueckwirkung: { $ref: "#/components/schemas/OrtRueckwirkung" }
        zuordnungen:
          type: array
          description: Alle Zuordnungen der Anlage nach dem Eintrag, nach Beginn.
          items: { $ref: "#/components/schemas/AnlageUmzugZuordnung" }
        bleibt:
          type: array
          items: { type: string, enum: [box, topics, freigaben, betriebsmodell, ladepark_rahmen, fahrplaene, messstellen] }
        boxen: { type: integer, description: Eingebaute Boxen der Anlage. }
        netzanschluss:
          type: object
          nullable: true
          properties:
            id: { type: string, format: uuid }
            kennzeichen: { type: string }
        steuern:
          type: object
          nullable: true
          description: Die Teilnahme an "Steuern & Optimieren" und der Standort ihrer Funktion - sie bleibt.
          properties:
            funktion: { type: string, enum: [messen, steuern] }
            zustand: { type: string }
            standort: { $ref: "#/components/schemas/AnlageUmzugStandort" }
        befehle: { type: integer, enum: [0] }
        begruendung: { type: string, nullable: true }
        protokoll:
          type: array
          description: In der Vorschau leer; nach dem Eintrag Anlage, neuer und bisheriger Standort.
          items:
            type: object
            properties:
              id: { type: integer, format: int64 }
              objektArt: { type: string, enum: [anlage, standort] }
              objektId: { type: string, format: uuid }
    OrtVerschiebenAnfrage:
      type: object
      additionalProperties: false
      required: [zielId]
      properties:
        zielId: { type: string, format: uuid, description: "Ein Standort - für einen Bereich auch ein Gebäude." }
        gueltigAb: { type: string, format: date, description: Fehlend = heute in der Zeitzone des Standorts. }
        begruendung: { type: string, maxLength: 500, nullable: true }
    OrtVerschiebungKnoten:
      type: object
      properties:
        id: { type: string, format: uuid, nullable: true }
        art: { type: string, nullable: true, enum: [unternehmen, standort, gebaeude, bereich] }
        kurzzeichen: { type: string, nullable: true }
        name: { type: string, nullable: true }
    OrtVerschiebungZuordnung:
      type: object
      properties:
        eltern: { $ref: "#/components/schemas/OrtVerschiebungKnoten" }
        gueltigAb: { type: string, format: date }
        gueltigBis: { type: string, format: date, nullable: true, description: Der letzte Tag, einschließlich. }
        zustand: { type: string, enum: [gueltig, geplant, beendet, aufgehoben] }
    OrtVerschiebungMessstelle:
      type: object
      description: Eine Messstelle mit dem Ort, an dem sie am "gültig ab" hängt - das Verschieben ändert ihn nie.
      properties:
        id: { type: string, format: uuid, nullable: true }
        kennzeichen: { type: string, example: MS-14 }
        name: { type: string, nullable: true }
        ort: { $ref: "#/components/schemas/OrtVerschiebungKnoten" }
    OrtVerschiebungAnlage:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        standort:
          nullable: true
          description: Der Standort, dem die Anlage am "gültig ab" zugeordnet ist - er bleibt.
          allOf: [{ $ref: "#/components/schemas/OrtVerschiebungKnoten" }]
    OrtVerschiebungNetzanschluss:
      type: object
      properties:
        id: { type: string, format: uuid, nullable: true }
        kennzeichen: { type: string, example: NA-2 }
    OrtVerschiebungFolgen:
      type: object
      description: >
        Das Urteil von OrtsbaumAbleitung.verschiebenFolgen in seiner Reihenfolge
        (Vektor-Familie `verschieben`, Fall `a13-folgen-karte`).
      properties:
        ziehenMit: { type: array, items: { $ref: "#/components/schemas/OrtVerschiebungKnoten" } }
        messstellenWechselnStandort: { type: array, items: { $ref: "#/components/schemas/OrtVerschiebungMessstelle" } }
        bleibenAnlagen: { type: array, items: { $ref: "#/components/schemas/OrtVerschiebungAnlage" } }
        bleibenNetzanschluesse: { type: array, items: { $ref: "#/components/schemas/OrtVerschiebungNetzanschluss" } }
        bleibenMessstellen: { type: array, items: { $ref: "#/components/schemas/OrtVerschiebungMessstelle" } }
    OrtVerschiebungZeitraum:
      type: object
      properties:
        von: { type: string, format: date }
        bis: { type: string, format: date, description: Einschließlich. }
    OrtVerschiebungEintrag:
      type: object
      properties:
        id: { type: integer, format: int64 }
        objektArt: { type: string, enum: [gebaeude, bereich] }
        objektId: { type: string, format: uuid }
        text: { type: string, example: "Gebäude verschoben: Werk Ahrenberg → Werk Ahrenberg Nord (ST-3)" }
        giltAb: { type: string, format: date }
        rueckwirkend: { type: boolean }
        wer: { type: string }
        eingetragenAm: { type: string, format: date-time }
    OrtVerschiebung:
      type: object
      description: >
        Vorschau und Eintrag des Verschiebens eines Gebäudes oder Bereichs
        (UEMS AP-02 IP-12) - dieselbe Form; die Vorschau ohne Protokoll.
      properties:
        ortId: { type: string, format: uuid }
        art: { type: string, enum: [gebaeude, bereich] }
        kurzzeichen: { type: string }
        name: { type: string }
        bisher: { $ref: "#/components/schemas/OrtVerschiebungKnoten" }
        bisherStandort:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/OrtVerschiebungKnoten" }]
        neu: { $ref: "#/components/schemas/OrtVerschiebungKnoten" }
        neuStandort:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/OrtVerschiebungKnoten" }]
        gueltigAb: { type: string, format: date }
        gueltigBis: { type: string, format: date, nullable: true, description: Von der laufenden Zuordnung geerbt. }
        danach:
          nullable: true
          description: Der Elternknoten der schon geplanten Zuordnung ab dem Tag nach `gueltigBis`.
          allOf: [{ $ref: "#/components/schemas/OrtVerschiebungKnoten" }]
        rueckwirkung: { $ref: "#/components/schemas/OrtRueckwirkung" }
        rueckwirkendBetroffen:
          nullable: true
          description: Nur rückwirkend - die Tage, die nachträglich anders zählen (der Fakt für AP-12).
          allOf: [{ $ref: "#/components/schemas/OrtVerschiebungZeitraum" }]
        zuordnungen: { type: array, items: { $ref: "#/components/schemas/OrtVerschiebungZuordnung" } }
        folgen: { $ref: "#/components/schemas/OrtVerschiebungFolgen" }
        befehle: { type: integer, enum: [0] }
        begruendung: { type: string, nullable: true }
        protokoll:
          type: array
          description: In der Vorschau leer; nach dem Eintrag genau einer, am Ort.
          items: { $ref: "#/components/schemas/OrtVerschiebungEintrag" }
    CreateTenantRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }
        segment: { type: string, enum: [CI, B2C], description: "Defaults to 'CI'." }
    AdminUser:
      type: object
      properties:
        id: { type: string, description: Keycloak user id. }
        username: { type: string }
        email: { type: string, nullable: true }
        firstName: { type: string, nullable: true }
        lastName: { type: string, nullable: true }
        enabled: { type: boolean }
        tenantId: { type: string, format: uuid, nullable: true, description: From the user's tenant_id attribute. }
    BenutzerAnlage:
      type: object
      required: [username, email, rolle, standorte]
      properties:
        username: { type: string, maxLength: 255 }
        email: { type: string, format: email }
        vorname: { type: string }
        nachname: { type: string }
        rolle: { type: string, enum: [kundenadministrator, energiemanager, bearbeiter, bedienberechtigt, leser, einsicht] }
        standorte:
          type: array
          description: Bei standortbezogener Rolle mindestens ein Standort; sonst leer.
          items: { type: string, format: uuid }
        gueltig_bis: { type: string, format: date, nullable: true, description: "Nur bei `einsicht`: letzter Tag, einschließlich (AP-19 Folge IP-13)." }
    BenutzerAngelegt:
      type: object
      required: [benutzer, startpasswort]
      properties:
        benutzer:
          type: object
          required: [sub, anzeigename, email, zustand]
          properties:
            sub: { type: string }
            anzeigename: { type: string }
            email: { type: string, format: email }
            zustand: { type: string, enum: [angelegt, aktiv, gesperrt, entfernt] }
        startpasswort:
          type: string
          description: Nur diese einmalige Antwort; nicht speichern. Pflichtwechsel bei der nächsten Anmeldung.
    CreateUserRequest:
      type: object
      required: [username, email]
      properties:
        username: { type: string }
        email: { type: string, format: email }
        firstName: { type: string }
        lastName: { type: string }
    UpdateTenantRequest:
      type: object
      required: [name]
      description: Same validation as CreateTenantRequest.
      properties:
        name: { type: string }
        segment: { type: string, enum: [CI, B2C], description: "Defaults to 'CI'." }
        betriebsart:
          type: string
          enum: [endkunde, betreiber]
          nullable: true
          description: >
            Full-representation U0 shell-frame override: 'endkunde'/'betreiber'
            set it, null (or omitted) clears it back to the automatic
            segment-derived default.
    DeleteTenantRequest:
      type: object
      required: [confirmName]
      properties:
        confirmName:
          type: string
          description: Must equal the tenant's exact name (type-to-confirm fat-finger protection).
    TenantOffboardingReport:
      type: object
      description: >
        What the offboarding removed. The database part is transactional
        (all-or-nothing); `failedUsers` lists Keycloak logins that could not be
        deleted, remain disabled and can be retried with the directory cleanup route.
      properties:
        tenantId: { type: string, format: uuid }
        tenantName: { type: string }
        deletedSites: { type: integer }
        deletedDevices: { type: integer }
        deletedTelemetryRows: { type: integer, format: int64 }
        deletedUsers:
          type: array
          items: { type: string }
        failedUsers:
          type: array
          items: { type: string }
    TenantOffboardingCleanup:
      type: object
      required: [tenantId, deletedUsers, failedUsers]
      properties:
        tenantId: { type: string, format: uuid }
        deletedUsers:
          type: array
          items: { type: string }
        failedUsers:
          type: array
          description: Disabled accounts whose final deletion still needs a retry.
          items: { type: string }
    UpdateUserRequest:
      type: object
      description: Email/name only; the username is immutable.
      properties:
        email: { type: string, format: email, nullable: true }
        firstName: { type: string, nullable: true, maxLength: 120 }
        lastName: { type: string, nullable: true, maxLength: 120 }
    OptimizerDiagnostics:
      type: object
      description: >
        One persisted optimizer run with the per-slot euro decomposition (see
        the endpoint description). Run-level fields carry the "inputs at a
        glance" master data; `storedEnergyValueIsApproximation` is false when
        every rendered slot carries the optimizer-persisted EXACT
        stored-energy value (the run's SoC shadow price,
        schedule.stored_value_ct_kwh - Fahrplan-Warum), and true when any slot
        fell back to the forward best-use heuristic (pre-feature runs, explain
        layer off - the UI must label it then).
      properties:
        siteId: { type: string, format: uuid }
        planId: { type: string, format: uuid, nullable: true }
        generatedAt: { type: string, format: date-time, nullable: true, description: Null when the site has no plan yet. }
        slotMinutes: { type: integer, example: 15 }
        availableRuns:
          type: array
          items: { type: string, format: date-time }
          description: >
            The run timestamps (newest first) of ONE Europe/Berlin day - the
            `date` query param's day, else the shown run's day.
        availableRunsDate:
          type: string
          format: date
          nullable: true
          description: The Berlin day `availableRuns` covers; null without any plan.
        firstRunDate:
          type: string
          format: date
          nullable: true
          description: Berlin day of the site's OLDEST persisted run - lower bound for the date picker.
        lastRunDate:
          type: string
          format: date
          nullable: true
          description: Berlin day of the site's NEWEST persisted run - upper bound for the date picker.
        plantKind: { type: string, enum: [direktvermarktung, eigenverbrauch] }
        netzladenErlaubt: { type: boolean }
        tarifArt: { type: string, enum: [dynamisch, fest, ohne] }
        tarifParamCtKwh: { type: number, nullable: true }
        anzulegenderWertCtKwh: { type: number, nullable: true }
        backupReserveSocPct: { type: number, nullable: true }
        battery:
          type: object
          nullable: true
          description: Null for sites without a battery asset.
          properties:
            capacityKwh: { type: number, nullable: true }
            roundtripEfficiencyPct: { type: number, nullable: true }
            wearCostCtPerKwh: { type: number, description: The EFFECTIVE per-cycle wear rate. }
            wearCostSource: { type: string, enum: [asset, platform-default] }
            socMinPct: { type: number, description: Effective usable-band floor (override or 5). }
            socMaxPct: { type: number, description: Effective usable-band ceiling (override or 95). }
        activeLoadModel: { type: string }
        activePvModel: { type: string }
        storedEnergyValueIsApproximation: { type: boolean, example: false }
        pvAnchorRatio:
          type: number
          nullable: true
          example: 2.4
          description: >
            The PV nowcast anchor this RUN corrected its near-horizon PV
            forecast by - measured over predicted on the last completed slots
            (schedule.pv_anchor_ratio, Morgenprognose). Above 1 the active
            model was too low (the east-heavy-morning case), below 1 too high;
            the factor decays back to the untouched forecast over the following
            couple of hours, so the number describes the FIRST slot. Null =
            no anchor was established (too little evidence, the optimizer's
            OPTIMIZER_PV_ANCHOR_ENABLED kill switch off, or a run predating the
            column) - render nothing then, never "1,0", which is itself a real
            established statement ("belegt, und das Modell lag richtig").
        nightReserveKwh:
          type: number
          nullable: true
          example: 4.92
          description: >
            Nacht-Wertfunktion P3 (schedule.why_night_reserve_kwh): how much
            charge this RUN holds at SUNRISE, above the reserve floor, because
            the site's OWN night-error distribution makes a heavier night
            plausible. Priced against the night's price spread, so it is never a
            fixed reserve and moves with every run. Null = nothing was held back
            (too little night history, no price spread, no sunrise in the
            horizon, the OPTIMIZER_NIGHT_RESERVE_ENABLED kill switch off, or a
            run predating the column).
        nightReserveQ:
          type: number
          nullable: true
          example: 0.75
          description: >
            That amount's quantile - 0.75 means it is needed in 1 of 4 nights.
            Null exactly when nightReserveKwh is null.
        slots:
          type: array
          items: { $ref: "#/components/schemas/OptimizerDiagnosticsSlot" }
    OptimizerDiagnosticsSlot:
      type: object
      description: >
        One 15-min plan slot: the persisted schedule columns plus the computed
        ct/kWh decomposition. Every computed field is null when honestly not
        computable for this slot (no persisted spot price, pre-P2 row without
        wear, battery-less site) - never a fabricated 0.
      properties:
        time: { type: string, format: date-time }
        batteryKw: { type: number, nullable: true, description: +charge / -discharge. }
        gridKw: { type: number, nullable: true, description: +import / -export. }
        socPct: { type: number, nullable: true }
        loadKw: { type: number, nullable: true }
        pvKw: { type: number, nullable: true }
        curtailKw: { type: number, nullable: true }
        costEur: { type: number, nullable: true }
        baselineCostEur: { type: number, nullable: true }
        wearCostEur: { type: number, nullable: true, description: The persisted P2 wear this slot spends. }
        solverPriceCtKwh: { type: number, nullable: true, description: The bare day-ahead spot price (schedule.price_eur_mwh / 10). }
        importPriceCtKwh: { type: number, nullable: true, description: Real cost of an imported kWh per the site's tariff. }
        exportValueCtKwh: { type: number, nullable: true, description: Real value of an exported kWh per the plant's remuneration. }
        wearCostCtKwh: { type: number, nullable: true, description: Derived wear rate per kWh of throughput (null for idle slots). }
        valueOfStoredEnergyCtKwh: { type: number, nullable: true, description: "The value of a stored kWh: the persisted EXACT SoC shadow price (Fahrplan-Warum) where the run carries it, else the forward best-use approximation (the run-level flag marks which)." }
        decisionLabel: { type: string, enum: [solarladen, netzladen, entladen, ruhe] }
        whyText: { type: string, description: One composed German sentence explaining the decision. }
        slotRole:
          type: string
          nullable: true
          description: >-
            Optimizer-persisted Fahrplan-Warum role (schedule.slot_role;
            vocabulary: warten | pv_speichern | guenstig_laden |
            eigenverbrauch | verkaufen | spitze_kappen | reserve_halten |
            abregeln, additively extensible). Null on pre-feature runs -
            decisionLabel stays the fallback.
        slotFlags:
          type: array
          nullable: true
          items: { type: string }
          description: >-
            Binding-constraint codes of the slot (schedule.slot_flags CSV,
            split): soc_max, soc_floor, reserve_backup, reserve_peak,
            charge_cap, discharge_cap, solar_only, grid_limit_14a,
            feed_in_cap, peak_defining, curtailing. Null = no binding
            recorded / pre-feature run.
        limitDischargeToLoad:
          type: boolean
          nullable: true
          description: >-
            Netz-null-Reduzieren (2026-09-08): whether this slot's SHAPE grants
            the REDUCE-only right limit_discharge_to_load - a real planned
            DISCHARGE (batteryKw < -0.05) into a planned grid exchange of ~ 0
            (|gridKw| <= 0.05). The box may then LIMIT the commanded discharge
            to the measured deficit max(load - pv, 0), never raise it, floor at
            0 kW. DERIVED from this row's own batteryKw/gridKw, because the flag
            is deliberately not persisted and - unlike its three siblings - needs
            neither lambda nor a price. It therefore says "this slot qualified",
            NOT "the box received the flag": a run published with the
            OPTIMIZER_LIMIT_DISCHARGE_ENABLED switch off, or by an optimizer
            older than the field, has the same shape and reads the same. Null =
            the row carries no battery or grid power, so nothing is claimed.
    OptimizerConfig:
      type: object
      description: The optimizer knobs of one site (see the endpoint description).
      properties:
        siteId: { type: string, format: uuid }
        hasBattery: { type: boolean }
        defaults:
          type: object
          description: Platform-wide tunables (the optimizer's OPTIMIZER_* env; read-only).
          properties:
            wearCostCtPerKwh: { type: number, example: 4.0 }
            socMinPct: { type: number, example: 5 }
            socMaxPct: { type: number, example: 95 }
            terminalValueQuantile: { type: number, example: 0.3 }
            terminalValueCtPerKwh: { type: number, nullable: true, description: Null = derived per plan. }
        overrides:
          type: object
          description: The nullable per-site/per-asset columns (null = default applies).
          properties:
            wearCostCtPerKwh: { type: number, nullable: true }
            socMinPct: { type: number, nullable: true }
            socMaxPct: { type: number, nullable: true }
            backupReserveSocPct: { type: number, nullable: true }
        effective:
          type: object
          description: >
            What the optimizer resolves (override ?? default). Battery fields
            are null for battery-less sites; a null backupReserveSocPct means
            "no reserve" (the technical floor applies unchanged).
          properties:
            wearCostCtPerKwh: { type: number, nullable: true }
            socMinPct: { type: number, nullable: true }
            socMaxPct: { type: number, nullable: true }
            backupReserveSocPct: { type: number, nullable: true }
        peakShaving:
          type: object
          description: >
            The peak-shaving module (PS-1/PS-2, Lastspitzenkappung) - the ONE
            writable surface for it (captain decision: vertragsnahe Module
            richtet VoltPilot ein, nicht der Kunde). A non-null
            leistungspreisEurKw IS the module-active flag; the optimizer then
            prices the billing period's 15-min import peak economically
            (epigraph + peak_so_far anchor) and publishes the planned peak
            target to the edge. Echoed read-only on the customer Site.
          properties:
            leistungspreisEurKw:
              type: number
              nullable: true
              minimum: 0
              description: EUR per kW per billing period; null = module off.
            abrechnungLeistung:
              type: string
              enum: [jahr, monat]
              description: Billing period of the Leistungspreis (Europe/Berlin calendar).
            peakReserveSocPct:
              type: number
              nullable: true
              minimum: 0
              maximum: 100
              description: >
                Hard SoC floor reserved for out-of-horizon peaks; stacks with
                the backup reserve (highest absolute floor binds). null = none.
        site:
          type: object
          description: Read-only echo of the levers editable via PUT /api/v1/sites/{siteId}.
          properties:
            netzladenErlaubt: { type: boolean }
            plantKind: { type: string }
            tarifArt: { type: string }
            tarifParamCtKwh: { type: number, nullable: true }
            anzulegenderWertCtKwh: { type: number, nullable: true }
    UpdateOptimizerConfigRequest:
      type: object
      description: >
        Full-representation override write: a null/absent field clears that
        override back to the platform default. wearCostCtPerKwh/socMinPct/
        socMaxPct live on the battery asset (409 without one);
        backupReserveSocPct and the peak-shaving module fields on the site.
        A null/absent abrechnungLeistung resets to 'jahr' (the column default).
      properties:
        wearCostCtPerKwh: { type: number, nullable: true, minimum: 0 }
        socMinPct: { type: number, nullable: true, minimum: 0, maximum: 100 }
        socMaxPct: { type: number, nullable: true, minimum: 0, maximum: 100 }
        backupReserveSocPct: { type: number, nullable: true, minimum: 0, maximum: 100 }
        leistungspreisEurKw:
          type: number
          nullable: true
          minimum: 0
          description: PS-1 Leistungspreis (EUR/kW per billing period); null switches the module off.
        abrechnungLeistung:
          type: string
          nullable: true
          enum: [jahr, monat]
          description: Billing period; null resets to 'jahr'.
        peakReserveSocPct:
          type: number
          nullable: true
          minimum: 0
          maximum: 100
          description: PS-2 peak-shaving SoC reserve; null clears it.

    WhatIfRequest:
      type: object
      description: >
        The admin what-if knobs (§2.8/§4.3). Every field is OPTIONAL and absent
        means "leave this as the site has it" - never zero, which is what makes
        an empty body the meaningful "solve it as configured" baseline.
        `backupReserveSocPct: 0` IS "no reserve". Prices, forecasts, tariff,
        §14a and the feed-in cap are deliberately NOT overridable: they come
        from the real site, so a preview can never become a fantasy plant.
      properties:
        wearCostCtPerKwh: { type: number, nullable: true, minimum: 0, maximum: 100 }
        backupReserveSocPct: { type: number, nullable: true, minimum: 0, maximum: 100 }
        socMinPct: { type: number, nullable: true, minimum: 0, maximum: 100 }
        socMaxPct: { type: number, nullable: true, minimum: 0, maximum: 100 }
        netzladenErlaubt:
          type: boolean
          nullable: true
          description: Grid-charging posture to preview (EEG vs. merchant).
        horizonSlots:
          type: integer
          nullable: true
          minimum: 4
          maximum: 192
          description: Planning horizon in 15-min slots; absent = 96 (24 h).

    WhatIfResult:
      type: object
      description: >
        Two plans solved over ONE freshly gathered set of inputs, plus their
        delta. Nothing here was persisted or published.
      properties:
        siteId: { type: string, format: uuid }
        computedAt: { type: string, format: date-time }
        horizonSlots: { type: integer }
        slotMinutes: { type: integer, example: 15 }
        appliedOverrides:
          type: object
          description: Echo of the knobs that actually differed (untouched ones are absent).
          additionalProperties: true
        baseline: { $ref: "#/components/schemas/WhatIfPlan" }
        variant: { $ref: "#/components/schemas/WhatIfPlan" }
        delta:
          type: object
          description: >
            variant minus baseline per headline figure. A figure that is null
            on either side stays null - never read as a zero difference.
          additionalProperties: { type: number, nullable: true }

    WhatIfPlan:
      type: object
      description: >
        One ephemeral solve. Money follows the persisted schedule's discipline:
        savingsEur is GROSS of wear, wearCostEur is separate, netSavingsEur is
        the honest difference, and bankedValueEur prices what the plan carries
        into the next day at its own terminal value.
      properties:
        slotMinutes: { type: integer }
        costEur: { type: number, nullable: true }
        baselineCostEur: { type: number, nullable: true }
        savingsEur: { type: number, nullable: true }
        wearCostEur: { type: number, nullable: true }
        netSavingsEur: { type: number, nullable: true }
        terminalValueEurPerKwh: { type: number, nullable: true }
        bankedValueEur: { type: number, nullable: true }
        chargedKwh: { type: number, nullable: true }
        dischargedKwh: { type: number, nullable: true }
        gridImportKwh: { type: number, nullable: true }
        gridExportKwh: { type: number, nullable: true }
        curtailedKwh: { type: number, nullable: true }
        cycles: { type: number, nullable: true, description: Throughput / (2 x capacity). }
        socStartPct: { type: number, nullable: true }
        socEndPct: { type: number, nullable: true }
        peakTargetKw: { type: number, nullable: true }
        fallback14a:
          type: boolean
          description: True when the §14a cap had to be dropped as infeasible (advisory plan).
        knobs:
          type: object
          description: The knob values this solve actually used.
          properties:
            wearCostCtPerKwh: { type: number }
            backupReserveSocPct: { type: number, nullable: true }
            socMinPct: { type: number }
            socMaxPct: { type: number }
            netzladenErlaubt: { type: boolean }
        slots:
          type: array
          items:
            type: object
            properties:
              time: { type: string, format: date-time }
              batteryKw: { type: number, nullable: true, description: "+ charge / - discharge" }
              gridKw: { type: number, nullable: true, description: "+ import / - export" }
              socPct: { type: number, nullable: true }
              pvKw: { type: number, nullable: true }
              loadKw: { type: number, nullable: true }
              curtailKw: { type: number, nullable: true }
              priceEurMwh: { type: number, nullable: true }
              costEur: { type: number, nullable: true }
              baselineCostEur: { type: number, nullable: true }
              wearCostEur: { type: number, nullable: true }
              slotRole: { type: string, nullable: true }
    MeasurementSelectionState:
      type: object
      description: >
        Der gewünschte Pollplan eines Geräts (oder einer seiner Komponenten)
        samt seiner append-only Papier-Spur. Nichts hier behauptet eine
        Anwendung: `applyStatus` wird erst durch die Quittung der Box wahr.
      properties:
        deviceId: { type: string, format: uuid }
        siteId: { type: string, format: uuid }
        entityId:
          type: string
          format: uuid
          nullable: true
          description: >
            Die Komponente, auf die diese Sicht zugeschnitten ist. null = das
            ganze Gerät (die Semantik vor Geräteseite Stufe 3b).
        desiredRevision:
          type: integer
          format: int64
          description: >
            Monotone Revision des einen veröffentlichten Plan-Dokuments. Sie ist
            GERÄTEWEIT, auch in einer Komponenten-Sicht, und ist das
            `expectedRevision` der nächsten Änderung.
        catalogVersion: { type: string }
        status:
          type: string
          enum: [no_selection, pending_edge, rejected, first_sample, applied]
        statusReason: { type: string, nullable: true }
        activationNotice: { type: string, nullable: true }
        disableNotice: { type: string, nullable: true }
        selections:
          type: array
          items: { $ref: "#/components/schemas/MeasurementSelectionPoint" }
        events:
          type: array
          items: { $ref: "#/components/schemas/MeasurementSelectionEvent" }
        volumeEstimate:
          type: object
          nullable: true
          additionalProperties: true
          description: >
            Die Budget-/Volumen-Schätzung. Sie zählt IMMER das ganze Gerät - der
            Bus ist physisch einer, auch wenn die Sicht eine Komponente zeigt.
    MeasurementSelectionPoint:
      type: object
      properties:
        entityId:
          type: string
          format: uuid
          nullable: true
          description: Die Komponente, die diesen Punkt beobachtet; null = das Gerät.
        pointKey: { type: string }
        enabled: { type: boolean }
        cadenceS: { type: integer, nullable: true }
        desiredRevision: { type: integer, format: int64 }
        enabledAt:
          type: string
          format: date-time
          nullable: true
          description: >
            Serverseitige No-Backfill-Grenze: aufgezeichnet wird ab hier,
            frühere Werte werden nie ergänzt.
        disabledAt: { type: string, format: date-time, nullable: true }
        catalogVersion: { type: string }
        changedBy: { type: string }
        changedByName: { type: string, nullable: true }
        changedAt: { type: string, format: date-time }
        applyStatus:
          type: string
          enum: [pending_edge, applied, rejected, first_sample]
        applyReason: { type: string, nullable: true }
        appliedAt: { type: string, format: date-time, nullable: true }
        customDefinition:
          nullable: true
          description: Bei einem freien Register die vollständige, nur lesbare Definition.
          allOf:
            - $ref: "#/components/schemas/CustomMeasurementDefinition"
        retentionClass: { type: string }
        rawRetentionDays: { type: integer }
        longTermCadenceS: { type: integer, nullable: true }
        longTermStrategy: { type: string }
        label: { type: string, nullable: true }
        family: { type: string, nullable: true }
        group: { type: string, nullable: true }
        semanticStatus: { type: string }
    MeasurementSelectionEvent:
      type: object
      description: >
        Eine unveränderliche Zeile der Papier-Spur. Spätere Zustandsübergänge
        derselben Revision werden ANGEHÄNGT, nie in die Anforderung gepatcht.
      properties:
        id: { type: integer, format: int64 }
        entityId:
          type: string
          format: uuid
          nullable: true
          description: >
            Zuordnungsnotiz der Anforderung. Ohne Fremdschlüssel, damit die
            Spur die Komponente überlebt, über die sie berichtet.
        pointKey: { type: string }
        desiredRevision: { type: integer, format: int64 }
        eventKind:
          type: string
          enum: [selection_requested, edge_ack, first_sample]
        idempotencyKey: { type: string, format: uuid, nullable: true }
        requestedAt: { type: string, format: date-time }
        requestedEnabled: { type: boolean }
        requestedCadenceS: { type: integer, nullable: true }
        enabledAt: { type: string, format: date-time, nullable: true }
        disabledAt: { type: string, format: date-time, nullable: true }
        catalogVersion: { type: string }
        actor: { type: string }
        actorName: { type: string, nullable: true }
        applyStatus: { type: string }
        applyReason: { type: string, nullable: true }
        appliedAt: { type: string, format: date-time, nullable: true }
        customDefinition:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/CustomMeasurementDefinition"
        retentionClass: { type: string }
        rawRetentionDays: { type: integer }
        longTermCadenceS: { type: integer, nullable: true }
        longTermStrategy: { type: string }
    CustomMeasurementDefinition:
      type: object
      description: >
        Ein freies Modbus-Register als vollständige, NUR LESBARE Definition. Es
        gibt hier keinen Schreibweg: ein Schreibparameter im Rumpf ist 400, und
        `requestCostMs` setzt ausschließlich der Server konservativ.
      required: [label, sourceKind, address, selector, valueType, widthBits, signed,
        endian, scale, unit, cadenceS, retentionClass, readOnly]
      properties:
        label: { type: string, minLength: 1, maxLength: 120 }
        sourceKind: { type: string, enum: [modbus_holding, modbus_input] }
        address: { type: integer, minimum: 0, maximum: 65535 }
        selector: { type: string, minLength: 1, maxLength: 80 }
        valueType:
          type: string
          enum: [uint16, int16, uint32, int32, float32, float64]
        widthBits: { type: integer, enum: [16, 32, 64] }
        signed: { type: boolean }
        endian: { type: string, enum: [big, word_little_byte_big] }
        scale: { type: number }
        unit: { type: string, minLength: 1, maxLength: 32 }
        cadenceS: { type: integer, minimum: 1, maximum: 86400 }
        retentionClass: { type: string, minLength: 1 }
        readOnly: { type: boolean, enum: [true] }
        requestCostMs:
          type: integer
          readOnly: true
          description: Serverseitige, konservative Kostenannahme; im Request nicht erlaubt.
        measures:
          $ref: "#/components/schemas/CustomMeasurementMeasures"
    CustomMeasurementMeasures:
      type: object
      description: >
        Wahlfrei und additiv: was der eigene Messwert misst, in den Wörtern des
        Messpunkt-Katalogs (`quantity`, `direction`, `aggregation_kind`). Nur mit
        dieser Angabe trägt er Größe, Richtung und Wertart am Messkanal und damit
        eine Messstelle; ohne sie bleibt er Beobachtung wie bisher. Erlaubt sind
        genau die Kombinationen, die einen Fluss des Messstellen-Vorschlags tragen:
        `active_energy` mit `counter` (Einheit Wh, kWh oder MWh, Aufbewahrungsklasse
        `energy_counter`) und `active_power` mit `gauge` (Einheit W, kW oder MW,
        Aufbewahrungsklasse `live_power`, `phase_mppt_string` oder `thermal_bms`),
        je mit `import`, `export`, `generation`, `charge` oder `discharge`; sonst 400
        mit Kundensatz. Die Angabe bleibt in der Cloud: an die Box geht die
        Definition ohne `measures` (`mqtt-measurement-config` bleibt unverändert).
      additionalProperties: false
      required: [quantity, direction, aggregationKind]
      properties:
        quantity: { type: string, enum: [active_energy, active_power] }
        direction: { type: string, enum: [import, export, generation, charge, discharge] }
        aggregationKind: { type: string, enum: [counter, gauge] }
    MessGroesse:
      type: object
      description: >
        Eine Messgröße wie `$defs/groesse` in docs/contracts/v2/messstelle.schema.json.
        Nur zusammen mit dem Medium im Größen-Katalog gültig (Vertrag §2).
      additionalProperties: false
      required: [groesse, richtung, einheit, wertart]
      properties:
        groesse: { type: string, enum: [Wirkenergie, Wirkleistung, Blindenergie, Scheinleistung, Ladestand, Volumen] }
        richtung: { type: string, enum: [Bezug, Abgabe, Erzeugung, Laden, Entladen, "Laden / Entladen", richtungslos] }
        einheit: { type: string, enum: [kWh, kW, kvarh, kVA, "%", "m³"] }
        wertart: { type: string, enum: [Zählerstand, Intervallmenge, Momentanwert] }
    Messstelle:
      type: object
      description: >
        Eine Messstelle nach docs/contracts/v2/messstelle.schema.json (dieselben
        Feldnamen in snake_case) plus die vier Felder der Schnittstelle `id`, `fehlt`,
        `angehalten_ab`, `archiviert_am`. `orte` und `elektrische_stellung` tragen die
        wirksamen Zuordnungen nach Beginn (IP-7), `fuehrende_quelle` und
        `vergleichsquellen` (auch je Nebengröße) die Quellenbindungen nach Beginn,
        beendete eingeschlossen (IP-13); `kadenz_s` bleibt null (AP-07). `lebenszyklus` und `fehlt`
        leitet MessstelleRegeln.lebenszyklus aus den gespeicherten Eingängen ab (ohne
        Ort ist eine gemessene Messstelle ein Entwurf, eine berechnete ohne Formel
        ebenso; ein Ort, gleich ab wann, zählt als vorhanden); gespeichert ist nie
        die Stufe.
      required: [id, schema_version, kennzeichen, name, art, medium, hauptgroesse, fuehrende_quelle,
        vergleichsquellen, nebengroessen, orte, elektrische_stellung, kadenz_s, lebenszyklus, fehlt,
        notiz, angehalten_ab, archiviert_am, anschlussleistung_kw]
      properties:
        id: { type: string, format: uuid }
        schema_version: { type: string, enum: ["1.0"] }
        kennzeichen: { type: string, pattern: "^[A-Z0-9./-]{2,16}$" }
        name: { type: string, nullable: true, description: "null = fehlt noch (Entwurf)." }
        art: { type: string, enum: [gemessen, berechnet] }
        medium: { type: string, enum: [Strom, Gas, Wärme, Kälte, Wasser, Druckluft] }
        hauptgroesse: { $ref: "#/components/schemas/MessGroesse" }
        fuehrende_quelle:
          type: array
          description: "Woher die Hauptgröße kommt, mit Gültigkeit (IP-13); leer = keine Datenquelle, nie 0."
          items: { $ref: "#/components/schemas/MessstelleQuellenbindung" }
        vergleichsquellen:
          type: array
          description: "Vergleichsquellen der Hauptgröße, jede mit Zweck (IP-13)."
          items: { $ref: "#/components/schemas/MessstelleVergleichsbindung" }
        nebengroessen:
          type: array
          items:
            type: object
            required: [groesse, richtung, einheit, wertart, lebenszyklus, fuehrende_quelle, vergleichsquellen]
            properties:
              groesse: { type: string }
              richtung: { type: string }
              einheit: { type: string }
              wertart: { type: string }
              lebenszyklus: { type: string, enum: [aktiv, archiviert], description: "archiviert auch mit der Messstelle." }
              fuehrende_quelle: { type: array, items: { $ref: "#/components/schemas/MessstelleQuellenbindung" } }
              vergleichsquellen: { type: array, items: { $ref: "#/components/schemas/MessstelleVergleichsbindung" } }
        orte:
          type: array
          description: "Wo die Messstelle sitzt, tagesgenau gültig - je Tag genau einer."
          items: { $ref: "#/components/schemas/MessstelleOrtZuordnung" }
        elektrische_stellung:
          type: array
          description: "Wo sie im elektrischen Baum einer Anlage steht, tagesgenau gültig."
          items: { $ref: "#/components/schemas/MessstelleStellungZuordnung" }
        kadenz_s: { type: integer, nullable: true }
        lebenszyklus: { type: string, enum: [entwurf, eingerichtet, aktiv, angehalten, archiviert] }
        fehlt:
          type: array
          items: { type: string, enum: [kennzeichen, name, hauptgroesse, ort, formel, eingaenge] }
        notiz: { type: string, nullable: true }
        angehalten_ab: { type: string, format: date-time, nullable: true, description: "Auf die Minute, Versatz Europe/Berlin." }
        archiviert_am: { type: string, format: date-time, nullable: true, description: "Auf die Minute, Versatz Europe/Berlin." }
        anschlussleistung_kw: { type: number, nullable: true, description: "Additiv (AP-08 IP-7): die Anschlussleistung am Messort; daraus der Höchstzuwachs je Kadenz (Z6). null = nicht deklariert." }
    MessstelleListe:
      type: object
      description: >
        Die Liste als Objekt: `messstellen` die Vertrags-Form (IP-3), `register` die
        Zeilen zum Stichtag (IP-4) - dieselben Messstellen, dieselbe Reihenfolge.
        `stichtag` ist der Tag, an dem Ort und Stellung gelten, `zeitpunkt` der
        Augenblick, zu dem die Quelle gilt.
      required: [messstellen, register, stichtag, zeitpunkt, teilansicht, aggregat]
      properties:
        messstellen:
          type: array
          items: { $ref: "#/components/schemas/Messstelle" }
        register:
          type: array
          items: { $ref: "#/components/schemas/MessstelleRegisterZeile" }
        stichtag: { type: string, format: date }
        zeitpunkt: { type: string, format: date-time }
        teilansicht:
          type: boolean
          description: "Ob die Liste nur einen Teil der Messstellen des Unternehmens zeigt (AP-03); bis dahin immer false."
        aggregat: { $ref: "#/components/schemas/MessstelleRegisterAggregat" }
    MessstelleRegisterZeile:
      type: object
      description: >
        Eine Zeile des Registers (AP-04 §5.16) zum Stichtag. `lebenszyklus` und `fehlt`
        sind die der Messstellen-Antwort - der HEUTIGE Lebenszyklus (gespeichert ist nur
        der heutige Eingang); ein Stichtag verschiebt Ort, Stellung und Quelle, nicht ihn.
      required: [id, kennzeichen, name, art, medium, hauptgroesse, ort, elektrische_stellung, quelle,
        lebenszyklus, fehlt, angehalten_ab, archiviert_am, beobachtung, letzter_wert, nebengroessen, fakten,
        berechnung, geplant_fuer_einsaetze]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        name: { type: string, nullable: true }
        art: { type: string, enum: [gemessen, berechnet] }
        medium: { type: string }
        hauptgroesse: { $ref: "#/components/schemas/MessGroesse" }
        ort: { $ref: "#/components/schemas/MessstelleRegisterOrt" }
        elektrische_stellung:
          nullable: true
          description: "Die Stellung, die am Stichtag gilt; null = keine."
          allOf: [{ $ref: "#/components/schemas/MessstelleRegisterStellung" }]
        quelle: { $ref: "#/components/schemas/MessstelleRegisterQuelle" }
        lebenszyklus: { type: string, enum: [entwurf, eingerichtet, aktiv, angehalten, archiviert] }
        fehlt: { type: array, items: { type: string } }
        angehalten_ab: { type: string, format: date-time, nullable: true }
        archiviert_am: { type: string, format: date-time, nullable: true }
        beobachtung:
          nullable: true
          description: >
            Die Beobachtung der HAUPTGRÖSSE über ihre führende Quelle (AP-04 IP-15). null nur
            bei einer BERECHNETEN Messstelle - sie hat keine Quelle, ihre Vollständigkeit steht
            in `berechnung` (AP-10 IP-9); nie geraten.
          allOf: [{ $ref: "#/components/schemas/MessstelleRegisterBeobachtung" }]
        letzter_wert:
          nullable: true
          description: "Der letzte gute Wert der Hauptgröße mit Einheit und Zeitpunkt; null, solange es keinen gibt - nie eine 0."
          allOf: [{ $ref: "#/components/schemas/MessstelleRegisterWert" }]
        nebengroessen:
          type: array
          description: "Je Nebengröße dieselbe Beobachtung, über ihre EIGENE führende Quelle."
          items: { $ref: "#/components/schemas/MessstelleRegisterNebengroesse" }
        fakten:
          type: array
          description: "Wirksame, append-only protokollierte Tatsachen an dieser Messstelle."
          items: { $ref: "#/components/schemas/MessstelleRegisterFakt" }
        berechnung:
          nullable: true
          description: >
            Nur BERECHNET (AP-10 IP-9): „Vollständig“ nur, wenn ALLE Eingänge der Formel DES
            TAGES liefern (ZustandAbleitung.berechnet) - bei einem Rest die Terme aus der
            Stellung. null an einer gemessenen Messstelle und an einer berechneten ohne Formel
            am Tag.
          allOf: [{ $ref: "#/components/schemas/MessstelleRegisterBerechnung" }]
        geplant_fuer_einsaetze:
          type: array
          description: Eingelöste Messbedarfe; die Messstelle ist damit „geplant für EE-…“.
          items:
            type: object
            required: [id, kennzeichen, name]
            properties:
              id: { type: string, format: uuid }
              kennzeichen: { type: string }
              name: { type: string }
    MessstelleRegisterFakt:
      type: object
      required: [art, gilt_ab]
      properties:
        art: { type: string, enum: [einstellung_geaendert] }
        gilt_ab: { type: string, format: date-time }
    MessstelleRegisterBerechnung:
      type: object
      required: [zustand, fehlend, seit, text]
      properties:
        zustand:
          type: string
          enum: [vollstaendig, unvollstaendig, ausserhalb_zugriff]
          description: "`ausserhalb_zugriff` (AP-03 R-A3/R-A6): ein Eingang liegt außerhalb des Zugriffs des Lesers und alle sichtbaren liefern - das Urteil ist nicht zu fällen (`fehlend` leer, `seit` null, `text` der Hinweis). Fehlt ein SICHTBARER Eingang, bleibt es `unvollstaendig` mit genau den sichtbaren, und `text` endet mit ` · umfasst Standorte außerhalb Ihres Zugriffs`. Ein Eingang außerhalb wird nie genannt."
        fehlend: { type: array, items: { type: string }, description: "Die Kennzeichen der Eingänge, die nicht liefern - nur Eingänge im Zugriff des Lesers." }
        seit: { type: string, format: date-time, nullable: true, description: "Seit wann der früheste fehlende Eingang schweigt; null, wenn keiner einen Zeitpunkt hat." }
        text: { type: string, example: "Unvollständig seit 14:00 Uhr (fehlt: MS-12)" }
    MessstelleRegisterOrt:
      type: object
      description: >
        Der Ort am Stichtag und der daraus abgeleitete Standort - die Verortung des
        Ortsbaum-Vertrags wie MessstelleStandortAm (`kennzeichen` = dessen `ort`).
        Ohne Ort an dem Tag sind alle Felder null und `grund` ist nicht_verortet.
      required: [id, kennzeichen, ort_art, name, gueltig_ab, gueltig_bis, pfad, standort, standort_id,
        standort_name, grund]
      properties:
        id: { type: string, format: uuid, nullable: true, description: "Die ID des Standorts, Gebäudes, Bereichs bzw. Unternehmens." }
        kennzeichen: { type: string, nullable: true, description: "Das Kurzzeichen des Orts; U = das Unternehmen.", example: B-1 }
        ort_art: { type: string, nullable: true, enum: [unternehmen, standort, gebaeude, bereich, null] }
        name: { type: string, nullable: true }
        gueltig_ab: { type: string, format: date, nullable: true, description: "Beginn des Intervalls, das am Stichtag gilt." }
        gueltig_bis: { type: string, format: date, nullable: true, description: "Letzter Tag; null = offen." }
        pfad: { type: array, items: { type: string }, example: [B-1, G-1, ST-1] }
        standort: { type: string, nullable: true }
        standort_id: { type: string, format: uuid, nullable: true }
        standort_name: { type: string, nullable: true }
        grund: { type: string, enum: [verortet, am_unternehmen, nicht_verortet, ort_nicht_im_baum] }
    MessstelleRegisterStellung:
      type: object
      required: [anlage, anlage_name, stellung, unterzaehler_von, gueltig_ab, gueltig_bis]
      properties:
        anlage: { type: string, format: uuid }
        anlage_name: { type: string, nullable: true }
        stellung: { type: string, enum: [Hauptzähler, Unterzähler, Erzeuger, Speicher, Abzweig, keine] }
        unterzaehler_von: { type: string, nullable: true, description: "Das heutige Kennzeichen der Bezug-Messstelle (nur bei Unterzähler)." }
        gueltig_ab: { type: string, format: date }
        gueltig_bis: { type: string, format: date, nullable: true }
    MessstelleRegisterQuelle:
      type: object
      description: >
        Die Quelle der Hauptgröße zum Zeitpunkt. `stand`: gebunden (eine führende Quelle
        gilt), berechnet (eine berechnete Messstelle hat keine - ihre Formel kommt mit
        AP-10) oder keine_datenquelle (gemessen, aber keine führende Quelle - nie eine 0).
      required: [stand, fuehrend, davor, vergleichsquellen]
      properties:
        stand: { type: string, enum: [gebunden, berechnet, keine_datenquelle] }
        fuehrend:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/MessstelleRegisterBindung" }]
        davor:
          nullable: true
          description: "Die führende Quelle, die vor der geltenden (bzw. vor dem Zeitpunkt) zuletzt endete - „davor Z-5a“."
          allOf: [{ $ref: "#/components/schemas/MessstelleRegisterBindung" }]
        vergleichsquellen: { type: integer, description: "Wie viele Vergleichsquellen der Hauptgröße zu dem Zeitpunkt laufen." }
    MessstelleRegisterBindung:
      type: object
      required: [id, komponente, komponente_name, kanal, kanal_name, geraet, gueltig_ab, gueltig_bis]
      properties:
        id: { type: string, format: uuid }
        komponente: { type: string, format: uuid }
        komponente_name: { type: string, nullable: true }
        kanal: { type: string, description: "Der Punkt des Messpunkt-Katalogs (point_key)." }
        kanal_name: { type: string, nullable: true, description: "Sein Anzeigename - aus der eigenen Definition (Selbstbau) oder dem Katalog." }
        geraet: { $ref: "#/components/schemas/MessstelleRegisterGeraet" }
        gueltig_ab: { type: string, format: date-time, description: "Das „seit“ der Zeile." }
        gueltig_bis: { type: string, format: date-time, nullable: true }
    MessstelleRegisterGeraet:
      type: object
      required: [id, geraet, einbau, bezeichnung]
      properties:
        id: { type: string, format: uuid, description: "Der Einbau - für GET /api/v1/geraete/{id}." }
        geraet: { type: string, example: GR-4 }
        einbau: { type: string, example: Z-5b }
        bezeichnung: { type: string, nullable: true, description: "Der Name des Geräts; null, solange niemand einen vergeben hat." }
    MessstelleRegisterBeobachtung:
      type: object
      description: >
        Die Beobachtung EINER Größe (AP-04 IP-15) - abgeleitet, nie gespeichert und nie geraten.
        `zustand` ist eines der vier Wörter des Zustandsvertrags über die REIHE, `text` der Kundensatz dazu;
        `seit` trägt nur liefert_nicht_seit (ein Satz ohne Zeitpunkt wäre eine halbe Aussage).
        `toleranz_s` ist das angewandte Fenster min(max(3 x kadenz_s, 300), 86400) - die Kante
        gehört zu „liefert“. ⚠ 3 x Kadenz ist die BEOBACHTUNG; 2 x Kadenz ist die LÜCKE, eine
        andere Aussage (AP-07 IP-9), die hier nicht vorkommt. Es gibt keinen Fehler- und keinen
        Störungszustand: Schweigen ist nie ein bewiesener Fehlschlag.
      required: [zustand, text, seit, toleranz_s, kadenz_s, geraet]
      properties:
        zustand: { type: string, enum: [liefert, liefert_nicht_seit, wartet_auf_erste_daten, keine_datenquelle] }
        text: { type: string, example: "Wartet auf erste Daten von Z-5b" }
        seit: { type: string, format: date-time, nullable: true, description: "Nur bei liefert_nicht_seit: der letzte gute Wert." }
        toleranz_s: { type: integer, format: int64, nullable: true, description: "null bei keine_datenquelle - ohne Kanal ist kein Fenster bekannt." }
        kadenz_s: { type: integer, format: int64, nullable: true, description: "Die erwartete Häufigkeit des Kanals (Mess-Selektion, sonst Katalog); null bei keine_datenquelle." }
        geraet: { type: string, nullable: true, description: "Der Einbau der führenden Quelle (Z-5b); null ohne Quelle.", example: Z-5b }
        zuordnung:
          type: string
          enum: [nicht_zugeordnet]
          description: >
            Nur vorhanden, wenn an der Box Werte ankommen (sie „liefert“ nach derselben Toleranz), die zu keiner
            Reihe gehören, während die Reihe selbst nicht liefert - der Komponente fehlt die Datenquelle.
            `zustand` spricht immer über die REIHE (nur zugeordnete Werte, dasselbe Kriterium wie die
            Werte-Karte), `text` sagt dann „Daten kommen an – noch keiner Messreihe zugeordnet“, `letzter_wert`
            bleibt der der Box. Fehlt das Feld, ist die Zeile Zeichen für Zeichen die von vorher.
    MessstelleRegisterWert:
      type: object
      description: >
        Der letzte Wert mit Qualität „gut“ der führenden Quelle. Genau eines von `wert` und `text`
        ist gesetzt (die Wert-Regel der Messreihe); `einheit` ist die des Messkanals, ohne jede
        Umrechnung.
      required: [wert, text, einheit, zeitpunkt]
      properties:
        wert: { type: number, nullable: true }
        text: { type: string, nullable: true }
        einheit: { type: string, nullable: true, example: kWh }
        zeitpunkt: { type: string, format: date-time }
    MessstelleRegisterNebengroesse:
      type: object
      description: "Eine Nebengröße der Zeile mit ihrer eigenen Beobachtung über ihre eigene führende Quelle."
      required: [id, groesse, beobachtung, letzter_wert]
      properties:
        id: { type: string, format: uuid }
        groesse: { $ref: "#/components/schemas/MessGroesse" }
        beobachtung:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/MessstelleRegisterBeobachtung" }]
        letzter_wert:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/MessstelleRegisterWert" }]
    MessstelleRegisterAggregat:
      type: object
      description: >
        „x von y Messstellen liefern Daten“ (AP-04 IP-15) über die Ableitung aggregat des
        Zustandsvertrags: nur liefert zählt im Zähler, „keine Datenquelle“ steht im Nenner.
        Gezählt werden GENAU die Zeilen dieser Antwort (die Filter gelten also auch hier). Seit
        AP-10 IP-9 zählen BERECHNETE Messstellen mit: `berechnung.zustand` vollstaendig =
        liefert, unvollstaendig = liefert nicht, ohne Formel am Tag wie „keine Datenquelle“
        (im Nenner, nie im Zähler). Eine Zeile ohne Standort an dem Tag zählt nur beim Unternehmen.
      required: [unternehmen, standorte]
      properties:
        unternehmen: { $ref: "#/components/schemas/MessstelleRegisterAbdeckung" }
        standorte:
          type: array
          items: { $ref: "#/components/schemas/MessstelleRegisterStandortAbdeckung" }
    MessstelleRegisterAbdeckung:
      type: object
      required: [erfuellt, gesamt, text]
      properties:
        erfuellt: { type: integer }
        gesamt: { type: integer }
        text: { type: string, example: "14 von 17 Messstellen liefern Daten" }
    MessstelleRegisterStandortAbdeckung:
      type: object
      required: [id, kurzzeichen, name, erfuellt, gesamt, text]
      properties:
        id: { type: string, format: uuid, nullable: true }
        kurzzeichen: { type: string, example: ST-1 }
        name: { type: string, nullable: true }
        erfuellt: { type: integer }
        gesamt: { type: integer }
        text: { type: string }
    MesskanalListe:
      type: object
      required: [site_id, komponente, inhaltsstand, messkanaele]
      properties:
        site_id: { type: string, format: uuid }
        komponente: { type: string, format: uuid }
        inhaltsstand: { type: string, description: "Der Stand des Messpunkt-Katalogs, aus dem Anzeigename, Einheit, Wertart, Größe und Richtung stammen." }
        messkanaele:
          type: array
          items: { $ref: "#/components/schemas/Messkanal" }
    Messkanal:
      type: object
      required: [kanal, anzeigename, einheit, wertart, groesse, richtung, quantity, direction, kadenz_s, aktiv, lesende_box, geraet, speist]
      properties:
        kanal: { type: string, description: "Der Kanalname: point_key des Katalogs oder des Selbstbaus." }
        anzeigename: { type: string, nullable: true }
        einheit: { type: string, nullable: true }
        wertart: { type: string, nullable: true, enum: [counter, gauge, state, bitfield, text, null], description: "Wertart der Quelle aus dem Katalog (AP-07 E12); null für Selbstbau und Punkte ohne Wertart." }
        groesse: { type: string, nullable: true, enum: [Wirkenergie, Wirkleistung, Blindenergie, Scheinleistung, Ladestand, null] }
        richtung: { type: string, nullable: true, enum: [Bezug, Abgabe, Erzeugung, Laden, Entladen, "Laden / Entladen", richtungslos, null] }
        quantity: { type: string, nullable: true, description: "Katalogwort der Größe (catalog.schema.json)." }
        direction: { type: string, nullable: true, description: "Katalogwort der Richtung (catalog.schema.json)." }
        kadenz_s: { type: integer, nullable: true, description: "Die gewünschte Kadenz der Mess-Selektion (AP-07 E9)." }
        aktiv: { type: boolean, description: "Ob die Box den Kanal gerade aufzeichnet; abgewählt bleibt er sichtbar." }
        lesende_box: { type: string, format: uuid, description: "Die Box, die den Kanal liest." }
        geraet:
          nullable: true
          description: "Das Gerät, das die Komponente jetzt speist (AP-04 IP-10); null ohne laufende Speisung - nie geraten."
          allOf: [{ $ref: "#/components/schemas/MesskanalGeraet" }]
        speist:
          type: array
          description: "Welche Messstellen der Kanal zum Stichtag (sonst jetzt) speist (AP-04 IP-13) - leer, wenn keine Quellenbindung läuft."
          items: { $ref: "#/components/schemas/MesskanalSpeist" }
    MesskanalSpeist:
      type: object
      description: >
        Eine Quellenbindung, die den Kanal zum Stichtag liest - genug für „speist
        MS-06 (führend)“: die Messstelle, die Größe, an der die Quelle hängt, und die Rolle.
      required: [messstelle_id, messstelle, groesse, richtung, rolle, zweck, gueltig_ab, gueltig_bis]
      properties:
        messstelle_id: { type: string, format: uuid }
        messstelle: { type: string, description: "Das Kennzeichen der Messstelle (MS-06)." }
        groesse: { type: string }
        richtung: { type: string }
        rolle: { type: string, enum: [fuehrend, vergleich] }
        zweck: { type: string, nullable: true, enum: [Plausibilität, Ersatz bei Ausfall, Abrechnungszähler, null] }
        gueltig_ab: { type: string, format: date-time }
        gueltig_bis: { type: string, format: date-time, nullable: true }
        anteil: { type: string, nullable: true, enum: [positiv, negativ, null], description: "Additiv (AP-04 IP-14): welchen Teil eines Vorzeichen-Werts die Bindung liest (AP-08 IP-7, E15); leer = der ganze Wert. „Quelle binden“ graut einen Messwert nur dann aus, wenn der GLEICHE Teil schon führend gelesen wird." }
    MesskanalGeraet:
      type: object
      description: >
        Die Form von `geraet_einbau` des Herkunftsvertrags
        (docs/contracts/v2/messwert-herkunft.schema.json, Angabe 10) plus `id`:
        der Nachschlag je Wert (AP-07) und das Read-Model sagen dasselbe.
      required: [id, geraet, einbau, seriennummer]
      properties:
        id: { type: string, format: uuid, description: "Der Einbau - für GET /api/v1/geraete/{id}." }
        geraet: { type: string, description: "Das Gerät (GR-4), das über einen Wechsel an seiner Stelle bleibt." }
        einbau: { type: string, nullable: true, description: "Das konkret eingebaute Gerät (Z-5a); ohne Wechsel dasselbe wie `geraet`. Die Tabelle hält es NOT NULL; fehlte es, stünde hier null, nie ein geratenes." }
        seriennummer: { type: string, nullable: true, description: "Die Seriennummer des Einbaus; null = nicht erhoben." }
    MessstelleStand:
      type: object
      description: "Ein abgelesener Zählerstand (E2); `einheit` darf fehlen - dann gibt es den Hinweis ablesestand_pruefen."
      required: [wert]
      properties:
        wert: { type: number }
        einheit: { type: string, nullable: true }
    MessstelleQuellenbindung:
      type: object
      description: "Eine führende Quelle in der Form von $defs/quellenbindung des Messstellen-Vertrags."
      required: [komponente, kanal, geraet, einbau, kanal_wertart, gueltig_ab, gueltig_bis, anfangsstand, endstand]
      properties:
        komponente: { type: string, description: "Die Kennung der Komponente." }
        kanal: { type: string, description: "Der Kanalname (point_key)." }
        geraet: { type: string, description: "Das Gerät (GR-4)." }
        einbau: { type: string, description: "Der Einbau, der die Komponente in diesem Zeitraum speist (Z-5a)." }
        kanal_wertart: { type: string, enum: [counter, gauge] }
        gueltig_ab: { type: string, format: date-time }
        gueltig_bis: { type: string, format: date-time, nullable: true }
        anfangsstand: { nullable: true, allOf: [{ $ref: "#/components/schemas/MessstelleStand" }] }
        endstand: { nullable: true, allOf: [{ $ref: "#/components/schemas/MessstelleStand" }] }
        anteil: { type: string, nullable: true, enum: [positiv, negativ, null], description: "Additiv (AP-08 IP-7): positiv/negativ eines Vorzeichen-Werts; leer = der ganze Wert." }
    MessstelleVergleichsbindung:
      type: object
      description: "Eine Vergleichsquelle in der Form von $defs/vergleichsbindung des Messstellen-Vertrags."
      required: [komponente, kanal, geraet, einbau, kanal_wertart, zweck, gueltig_ab, gueltig_bis]
      properties:
        komponente: { type: string }
        kanal: { type: string }
        geraet: { type: string }
        einbau: { type: string }
        kanal_wertart: { type: string, enum: [counter, gauge] }
        zweck: { type: string, enum: [Plausibilität, Ersatz bei Ausfall, Abrechnungszähler] }
        gueltig_ab: { type: string, format: date-time }
        gueltig_bis: { type: string, format: date-time, nullable: true }
    MessstelleQuelleBinden:
      type: object
      additionalProperties: false
      description: >
        Eine Quelle binden (AP-04 IP-13). Das Gerät wählt nie der Kunde: es ist der
        Einbau, der die Komponente zu `gueltig_ab` speist.
      required: [komponente, kanal, rolle]
      properties:
        groesse:
          type: object
          nullable: true
          description: "Fehlend = die Hauptgröße; sonst Größe + Richtung einer Nebengröße."
          required: [groesse, richtung]
          properties:
            groesse: { type: string }
            richtung: { type: string }
        komponente: { type: string, format: uuid }
        kanal: { type: string, description: "Der Kanalname (point_key) - eine Zeile der Mess-Selektion der Komponente." }
        rolle: { type: string, enum: [fuehrend, vergleich] }
        zweck: { type: string, nullable: true, description: "Nur und immer bei vergleich: Plausibilität · Ersatz bei Ausfall · Abrechnungszähler." }
        gueltig_ab: { type: string, format: date-time, nullable: true, description: "Auf die Minute mit Versatz; fehlend = jetzt. Vergangenheit = rückwirkend, Zukunft = angekündigt." }
        gueltig_bis: { type: string, format: date-time, nullable: true, description: "Fehlend = bis auf Weiteres." }
        anfangsstand: { nullable: true, allOf: [{ $ref: "#/components/schemas/MessstelleStand" }] }
        endstand_vorgaenger: { nullable: true, allOf: [{ $ref: "#/components/schemas/MessstelleStand" }], description: "Nur, wenn die neue offene führende Quelle die laufende beendet (Regel 2)." }
        grund: { type: string, nullable: true, description: "Frei; steht im Änderungsprotokoll." }
        anteil: { type: string, nullable: true, enum: [positiv, negativ, null], description: "AP-08 IP-7 (E15): nur den positiven (Bezug) oder nur den negativen Anteil (Abgabe) eines Vorzeichen-Werts lesen — geteilt je Rohwert, nie je Mittelwert. Fehlend = der ganze Wert. Ein anderes Wort ist 400." }
    MessstelleQuelleBeenden:
      type: object
      additionalProperties: false
      properties:
        gueltig_bis: { type: string, format: date-time, nullable: true, description: "Auf die Minute; fehlend = jetzt." }
        endstand: { nullable: true, allOf: [{ $ref: "#/components/schemas/MessstelleStand" }] }
        grund: { type: string, nullable: true }
    MessstelleQuelleGeraet:
      type: object
      required: [id, geraet, einbau, hersteller]
      properties:
        id: { type: string, format: uuid, description: "Der Einbau - für GET /api/v1/geraete/{id}." }
        geraet: { type: string }
        einbau: { type: string }
        hersteller:
          type: string
          nullable: true
          description: "AP-05 IP-11, additiv: der Hersteller des Einbaus. Die Fläche fragt Kartenangaben nur dort nach, wo ein Hersteller sie haben kann."
    MessstelleQuelle:
      type: object
      description: "Eine Quellenbindung, wie sie gespeichert ist; `status` gegen jetzt."
      required: [id, messstelle_id, groesse, richtung, rolle, zweck, komponente, komponente_name, anlage, kanal,
        kanal_wertart, herleitung, geraet, gueltig_ab, gueltig_bis, status, anfangsstand, endstand, rueckwirkend,
        herkunft, eingetragen_am, eingetragen_von]
      properties:
        id: { type: string, format: uuid }
        messstelle_id: { type: string, format: uuid }
        groesse: { type: string }
        richtung: { type: string }
        rolle: { type: string, enum: [fuehrend, vergleich] }
        zweck: { type: string, nullable: true }
        komponente: { type: string, format: uuid }
        komponente_name: { type: string, nullable: true }
        anlage: { type: string, format: uuid }
        kanal: { type: string }
        kanal_wertart: { type: string, enum: [counter, gauge] }
        herleitung: { type: string, enum: [zaehlerstand, differenzen, integration, momentanwert], description: "integration ist für AP-08 gekennzeichnet (Menge aus einer Leistung)." }
        geraet: { $ref: "#/components/schemas/MessstelleQuelleGeraet" }
        gueltig_ab: { type: string, format: date-time }
        gueltig_bis: { type: string, format: date-time, nullable: true }
        status: { type: string, enum: [geplant, gilt, beendet] }
        anfangsstand: { nullable: true, allOf: [{ $ref: "#/components/schemas/MessstelleStand" }] }
        endstand: { nullable: true, allOf: [{ $ref: "#/components/schemas/MessstelleStand" }] }
        rueckwirkend: { type: boolean, description: "Lag der Beginn beim Eintragen schon zurück (E2)?" }
        herkunft: { type: string, nullable: true, enum: [bestandsuebernahme, null], description: "Woher die Bindung stammt: leer = von Hand gebunden, `bestandsuebernahme` = aus der Vorschlagsliste des Standorts übernommen (IP-16, E6)." }
        eingetragen_am: { type: string, format: date-time }
        eingetragen_von: { type: string }
        anteil: { type: string, nullable: true, enum: [positiv, negativ, null], description: "Additiv (AP-08 IP-7): der Teil des Vorzeichen-Werts, den die Bindung liest; leer = der ganze Wert." }
        kanal_name: { type: string, nullable: true, description: "Additiv (AP-04 IP-14): der Anzeigename des Messwerts, dieselbe Regel wie im Messkanal-Read-Model; leer, wenn weder Katalog noch Selbstbau einen nennt." }
        letzter_wert:
          nullable: true
          description: >
            Additiv (AP-04 IP-14, E3): der letzte gute Wert DIESER Bindung — dieselbe Ableitung wie
            der letzte Wert des Registers (Einheit des Messkanals ohne Umrechnung, Anteil-Schnitt
            nach AP-08 IP-7). Nur an einer Bindung gesetzt, die zum Stichtag GILT; leer heißt
            „nichts bekannt“, nie eine 0. Die Quelle-Karte stellt damit die führende und die
            Vergleichsquelle nebeneinander — ohne jede Bewertung.
          allOf: [{ $ref: "#/components/schemas/MessstelleRegisterWert" }]
    MessstelleQuelleRueckwirkung:
      type: object
      description: "Wie weit das eingetragene „gültig ab“ bzw. „gültig bis“ von jetzt entfernt ist, auf die Minute (E2)."
      required: [art, minuten, abzeichen]
      properties:
        art: { type: string, enum: [rueckwirkend, ab_jetzt, angekuendigt] }
        minuten: { type: integer }
        abzeichen: { type: string, nullable: true, description: "Nur rückwirkend: „rückwirkend (25 min)“, „rückwirkend (2 h 5 min)“, „rückwirkend (933 Tage)“." }
    MessstelleQuelleVorgang:
      type: object
      description: "Die Antwort auf Binden und Beenden."
      required: [quelle, beendet, rueckwirkung, hinweise]
      properties:
        quelle: { $ref: "#/components/schemas/MessstelleQuelle" }
        beendet:
          nullable: true
          description: "Beim Binden: die laufende führende Quelle, die die neue genau zu ihrem Beginn beendet hat (Regel 2)."
          allOf: [{ $ref: "#/components/schemas/MessstelleQuelle" }]
        rueckwirkung: { $ref: "#/components/schemas/MessstelleQuelleRueckwirkung" }
        hinweise: { type: array, items: { type: string, enum: [ablesestand_pruefen] } }
    ZaehlerwechselNeuesGeraet:
      type: object
      additionalProperties: false
      description: >
        Das Kästchen, das kommt. Jedes Feld darf fehlen: `einbau_kennzeichen` vergibt dann
        der Server (GR-4.2, GR-4.3 …), Hersteller, Typ und Bezeichnung übernimmt er vom
        Vorgänger - die `seriennummer` NIE: sie ist die des alten Kästchens und wird nie
        erfunden (null heißt „nicht erhoben").
      properties:
        einbau_kennzeichen: { type: string, nullable: true, description: "1-32 Zeichen: Buchstaben, Ziffern, „-“, „.“, „/“, „′“. Belegt = 409 `kennzeichen_belegt`." }
        hersteller: { type: string, nullable: true }
        typ: { type: string, nullable: true }
        seriennummer: { type: string, nullable: true }
        bezeichnung: { type: string, nullable: true }
    ZaehlerwechselVerbindung:
      type: object
      additionalProperties: false
      description: >
        Wie das neue Gerät antwortet. Fehlt das Feld GANZ, bleibt die Verbindung des
        Vorgängers - „gleiche Datenquelle und Geräte-ID" (§5.5), und dann entsteht auch
        keine neue Komponenten-Fassung.
      properties:
        datenquelle: { type: string, format: uuid, nullable: true, description: "Die Datenquelle des neuen Geräts (AP-06); fehlend = die des Vorgängers." }
        geraete_id: { type: integer, nullable: true, description: "Die Geräte-ID hinter dem Weg (Modbus-Unit-ID); fehlend = die des Vorgängers. Eine geänderte schreibt eine neue Komponenten-Fassung." }
    Zaehlerwechsel:
      type: object
      additionalProperties: false
      description: >
        Der Zählerwechsel als EIN Vorgang (AP-04 IP-17) - dieselbe Anfrage an beiden
        Einstiegen.
      properties:
        zeitpunkt: { type: string, format: date-time, nullable: true, description: "Auf die Minute mit Versatz; fehlend = jetzt. Vergangenheit = rückwirkend, Zukunft = angekündigt (E2)." }
        neues_geraet: { nullable: true, allOf: [{ $ref: "#/components/schemas/ZaehlerwechselNeuesGeraet" }] }
        verbindung: { nullable: true, allOf: [{ $ref: "#/components/schemas/ZaehlerwechselVerbindung" }] }
        endstand_vorgaenger: { nullable: true, allOf: [{ $ref: "#/components/schemas/MessstelleStand" }], description: "Der abgelesene Endstand des alten Zählers; er landet an der Bindung, die endet." }
        anfangsstand: { nullable: true, allOf: [{ $ref: "#/components/schemas/MessstelleStand" }], description: "Der Anfangsstand des neuen Zählers; er landet an der Bindung, die beginnt." }
        einstellungen_uebernehmen: { type: boolean, nullable: true, description: "Fehlend = true: jede Fassung, die am alten Einbau zum Wechselzeitpunkt gilt, steht danach auch am neuen." }
        grund: { type: string, nullable: true, description: "Frei; steht im Änderungsprotokoll." }
        karten_uebernommen:
          type: array
          items: { type: string, format: uuid }
          description: "Beim Controller Pflicht: übernommene Karten aus der Vorschau; leere Liste = alle ebenfalls neu. Neue Karten erhalten keine erfundene Seriennummer."
        bestaetigte_bindungen:
          type: array
          items: { type: string, format: uuid }
          description: "Beim Controller alle Bindungen aus der bestätigten Vorschau; veraltete Auswahl ergibt 409."
        ablesestaende:
          type: array
          items:
            type: object
            additionalProperties: false
            required: [bindung]
            properties:
              bindung: { type: string, format: uuid }
              endstand: { nullable: true, allOf: [{ $ref: "#/components/schemas/MessstelleStand" }] }
              anfangsstand: { nullable: true, allOf: [{ $ref: "#/components/schemas/MessstelleStand" }] }
          description: "Optional je führender Zählerstand-Bindung; keine doppelten oder fremden Bindungen, nicht zusammen mit den einzelnen Ablesestand-Feldern."
    ControllerwechselVorschau:
      type: object
      required: [zeitpunkt, karten, folgen]
      properties:
        zeitpunkt: { type: string, format: date-time }
        karten:
          type: array
          items:
            type: object
            required: [id, steckplatz, bezeichnung, typ, seriennummer]
            properties:
              id: { type: string, format: uuid }
              steckplatz: { type: integer, nullable: true }
              bezeichnung: { type: string, nullable: true }
              typ: { type: string, nullable: true }
              seriennummer: { type: string, nullable: true }
        folgen:
          type: array
          items:
            type: object
            required: [bindung, karte, komponente, messstelle, kennzeichen, groesse, richtung, rolle, einheit, zaehlerstand]
            properties:
              bindung: { type: string, format: uuid }
              karte: { type: string, format: uuid, nullable: true }
              komponente: { type: string, format: uuid }
              messstelle: { type: string, format: uuid }
              kennzeichen: { type: string }
              groesse: { type: string }
              richtung: { type: string }
              rolle: { type: string, enum: [fuehrend, vergleich] }
              einheit: { type: string, nullable: true }
              zaehlerstand: { type: boolean }
    ZaehlerwechselEinbau:
      type: object
      required: [id, geraet, einbau, seriennummer, eingebaut_am, ausgebaut_am]
      properties:
        id: { type: string, format: uuid, description: "Der Einbau - für GET /api/v1/geraete/{id}." }
        geraet: { type: string, description: "Das Gerät an der Stelle (GR-4) - es bleibt über den Wechsel." }
        einbau: { type: string, description: "Das konkrete Kästchen (Z-5a bzw. Z-5b)." }
        seriennummer: { type: string, nullable: true, description: "null = nicht erhoben, nie erfunden." }
        eingebaut_am: { type: string, format: date-time }
        ausgebaut_am: { type: string, format: date-time, nullable: true }
    ZaehlerwechselGeraet:
      type: object
      required: [alt, neu, verbindung_neu]
      properties:
        alt: { $ref: "#/components/schemas/ZaehlerwechselEinbau" }
        neu: { $ref: "#/components/schemas/ZaehlerwechselEinbau" }
        verbindung_neu: { type: boolean, description: "Datenquelle oder Geräte-ID haben sich geändert - nur dann bekommt die Komponente eine neue Fassung." }
    ZaehlerwechselBindung:
      type: object
      required: [messstelle, kennzeichen, groesse, richtung, rolle, beendet, neu]
      properties:
        messstelle: { type: string, format: uuid }
        kennzeichen: { type: string }
        groesse: { type: string }
        richtung: { type: string }
        rolle: { type: string, enum: [fuehrend, vergleich] }
        beendet: { $ref: "#/components/schemas/MessstelleQuelle" }
        neu: { $ref: "#/components/schemas/MessstelleQuelle" }
    ZaehlerwechselEinstellung:
      type: object
      required: [id, art, komponente, kanal, anwendung]
      properties:
        id: { type: string, format: uuid }
        art: { type: string }
        komponente: { type: string, format: uuid, nullable: true }
        kanal: { type: string, nullable: true }
        anwendung: { type: string, enum: [angewendet, dokumentiert] }
    ZaehlerwechselVorgang:
      type: object
      description: "Die Antwort beider Einstiege (201)."
      required: [geraet, komponenten, bindungen, einstellungen, marken, rueckwirkung, hinweise]
      properties:
        geraet: { $ref: "#/components/schemas/ZaehlerwechselGeraet" }
        komponenten: { type: array, items: { type: string, format: uuid }, description: "Die Komponenten, deren Speisung mitgewandert ist." }
        bindungen: { type: array, items: { $ref: "#/components/schemas/ZaehlerwechselBindung" } }
        einstellungen: { type: array, items: { $ref: "#/components/schemas/ZaehlerwechselEinstellung" }, description: "Die Fassungen, die auf den neuen Einbau übernommen wurden." }
        marken: { type: integer, description: "In wie vielen Komponenten-Verläufen die Marke „Zähler gewechselt“ steht. Sie ist ein isolierter Zusatz und lässt den Wechsel nie scheitern - eine kleinere Zahl als `komponenten` ist ehrlich, kein Fehler des Vorgangs." }
        rueckwirkung: { $ref: "#/components/schemas/MessstelleQuelleRueckwirkung" }
        hinweise: { type: array, items: { type: string, enum: [ablesestand_pruefen] } }
    MessstelleQuelleAbschnitt:
      type: object
      required: [von, bis, quelle]
      properties:
        von: { type: string, format: date-time }
        bis: { type: string, format: date-time, nullable: true }
        quelle: { type: string, format: uuid, nullable: true, description: "null = eine sichtbare Lücke, nie aufgefüllt." }
    MessstelleQuelleGroesse:
      type: object
      required: [groesse, richtung, einheit, wertart, hauptgroesse, lebenszyklus, fuehrend, vergleich, zeitstrahl]
      properties:
        groesse: { type: string }
        richtung: { type: string }
        einheit: { type: string }
        wertart: { type: string }
        hauptgroesse: { type: boolean }
        lebenszyklus: { type: string, enum: [aktiv, archiviert] }
        fuehrend:
          nullable: true
          description: "Die führende Quelle zum Stichtag; null = keine Quelle, nie 0."
          allOf: [{ $ref: "#/components/schemas/MessstelleQuelle" }]
        vergleich: { type: array, items: { $ref: "#/components/schemas/MessstelleQuelle" } }
        zeitstrahl: { type: array, items: { $ref: "#/components/schemas/MessstelleQuelleAbschnitt" } }
    MessstelleQuellenListe:
      type: object
      required: [messstelle_id, kennzeichen, stichtag, groessen, quellen]
      properties:
        messstelle_id: { type: string, format: uuid }
        kennzeichen: { type: string }
        stichtag: { type: string, format: date-time }
        groessen: { type: array, items: { $ref: "#/components/schemas/MessstelleQuelleGroesse" }, description: "Hauptgröße zuerst, dann die Nebengrößen." }
        quellen: { type: array, items: { $ref: "#/components/schemas/MessstelleQuelle" }, description: "Die ganze Historie, beendete eingeschlossen." }
    MessstelleAnlegen:
      type: object
      additionalProperties: false
      required: [art, medium, hauptgroesse]
      properties:
        kennzeichen:
          type: string
          nullable: true
          pattern: "^[A-Z0-9./-]{2,16}$"
          description: "Fehlend oder null = das nächste automatische (MS-0001 …). Nichts wird umgewandelt: „ms-01“ ist 400."
        name: { type: string, nullable: true, description: "Fehlend, leer oder nur Leerzeichen = fehlt noch (Entwurf)." }
        art: { type: string, enum: [gemessen, berechnet] }
        medium: { type: string, enum: [Strom, Gas, Wärme, Kälte, Wasser, Druckluft] }
        hauptgroesse: { $ref: "#/components/schemas/MessGroesse" }
        nebengroessen:
          type: array
          description: "Je (Größe, Richtung) einmal und nie die Hauptgröße."
          items: { $ref: "#/components/schemas/MessGroesse" }
        notiz: { type: string, nullable: true }
        anschlussleistung_kw: { type: number, nullable: true, exclusiveMinimum: 0, description: "Optional (AP-08 IP-7), größer 0; sonst 400." }
    MessstelleBearbeiten:
      type: object
      additionalProperties: false
      required: [kennzeichen]
      properties:
        kennzeichen: { type: string, pattern: "^[A-Z0-9./-]{2,16}$" }
        name: { type: string, nullable: true }
        notiz: { type: string, nullable: true }
        anschlussleistung_kw: { type: number, nullable: true, exclusiveMinimum: 0, description: "Optional (AP-08 IP-7); die ganze Menge wie die übrigen Felder — fehlend = leer." }
    MessstelleUebergang:
      type: object
      additionalProperties: false
      properties:
        zeitpunkt:
          type: string
          format: date-time
          description: "Auf die Minute mit Versatz (E2), fehlend = jetzt; vor jetzt = rückwirkend."
        grund: { type: string, nullable: true, description: "Steht im Änderungsprotokoll." }
    MessstelleOrtZuordnung:
      type: object
      description: "Ein Ort mit Gültigkeit wie `$defs/ortZuordnung` in messstelle.schema.json."
      additionalProperties: false
      required: [ort_art, kennzeichen, gueltig_ab, gueltig_bis]
      properties:
        ort_art: { type: string, enum: [unternehmen, standort, gebaeude, bereich] }
        kennzeichen: { type: string, description: "Das Kurzzeichen des Orts; U = das Unternehmen.", example: B-2 }
        gueltig_ab: { type: string, format: date }
        gueltig_bis: { type: string, format: date, nullable: true, description: "Der LETZTE gültige Tag, einschließlich; null = offen." }
    MessstelleStellungZuordnung:
      type: object
      description: "Die elektrische Stellung mit Gültigkeit wie `$defs/stellungZuordnung` in messstelle.schema.json."
      additionalProperties: false
      required: [anlage, stellung, unterzaehler_von, gueltig_ab, gueltig_bis]
      properties:
        anlage: { type: string, format: uuid, description: Die ID der Anlage. }
        stellung: { type: string, enum: [Hauptzähler, Unterzähler, Erzeuger, Speicher, Abzweig, keine] }
        unterzaehler_von: { type: string, nullable: true, description: "Das heutige Kennzeichen der Bezug-Messstelle - nur bei Unterzähler." }
        gueltig_ab: { type: string, format: date }
        gueltig_bis: { type: string, format: date, nullable: true, description: "Der LETZTE gültige Tag, einschließlich; null = offen." }
    MessstelleOrtAendern:
      type: object
      additionalProperties: false
      required: [kennzeichen, gueltig_ab]
      properties:
        kennzeichen: { type: string, description: "Das Kurzzeichen eines Standorts, Gebäudes oder Bereichs; U = das Unternehmen (nur berechnet)." }
        gueltig_ab: { type: string, format: date, description: "Ab welchem Tag (in der Zeitzone des Unternehmens)." }
        korrektur: { type: boolean, nullable: true, description: "true = das Intervall ersetzen, das an gueltig_ab beginnt." }
        grund: { type: string, nullable: true, description: Steht im Änderungsprotokoll. }
    MessstelleStellungAendern:
      type: object
      additionalProperties: false
      required: [anlage, stellung, gueltig_ab]
      properties:
        anlage: { type: string, format: uuid }
        stellung: { type: string, enum: [Hauptzähler, Unterzähler, Erzeuger, Speicher, Abzweig, keine] }
        unterzaehler_von: { type: string, nullable: true, description: "Das Kennzeichen der Bezug-Messstelle - genau bei Unterzähler." }
        gueltig_ab: { type: string, format: date }
        korrektur: { type: boolean, nullable: true, description: "true = das Intervall ersetzen, das an gueltig_ab beginnt." }
        grund: { type: string, nullable: true, description: Steht im Änderungsprotokoll. }
    MessstelleStandortAm:
      type: object
      description: >
        Der Stand der Zuordnungen einer Messstelle an einem Tag: die Verortung des
        Ortsbaum-Vertrags (`ort`, `pfad` vom Ort hinauf bis zum Standort,
        `standort`, `grund`) und die Stellung, die an dem Tag gilt.
      required: [am, ort, ort_art, pfad, standort, standort_id, grund, elektrische_stellung]
      properties:
        am: { type: string, format: date }
        ort: { type: string, nullable: true, description: "Das Kurzzeichen des Orts an dem Tag; U = das Unternehmen; null = keiner.", example: B-3 }
        ort_art: { type: string, nullable: true, enum: [unternehmen, standort, gebaeude, bereich, null] }
        pfad: { type: array, items: { type: string }, example: [B-3, G-2, ST-1] }
        standort: { type: string, nullable: true, description: "Das Kurzzeichen des abgeleiteten Standorts.", example: ST-1 }
        standort_id: { type: string, format: uuid, nullable: true }
        grund: { type: string, enum: [verortet, am_unternehmen, nicht_verortet, ort_nicht_im_baum] }
        elektrische_stellung:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/MessstelleStellungZuordnung" }]
    MessstelleVorschlagQuelle:
      type: object
      description: "Der Messwert hinter einer Größe des Vorschlags, mit der Herleitung aus Regel 7."
      required: [kanal, anzeigename, kanal_wertart, herleitung]
      properties:
        kanal: { type: string, description: "Der point_key der Mess-Selektion." }
        anzeigename: { type: string, nullable: true, description: "Der Name aus dem Katalog; fehlt er, spricht die Fläche vom Kanal." }
        kanal_wertart: { type: string, enum: [counter, gauge] }
        herleitung: { type: string, enum: [zaehlerstand, differenzen, integration, momentanwert] }
    MessstelleVorschlagNebengroesse:
      type: object
      description: "Eine Nebengröße des Vorschlags (der Ladestand am Speicher) mit ihrem Messwert."
      required: [groesse, quelle]
      properties:
        groesse: { $ref: "#/components/schemas/MessGroesse" }
        quelle: { $ref: "#/components/schemas/MessstelleVorschlagQuelle" }
    MessstelleVorschlagBezug:
      type: object
      description: "„Unterzähler von“: eine bestehende Messstelle oder ein Vorschlag DIESER Liste."
      required: [messstelle, bestehend, komponente, kanal]
      properties:
        messstelle: { type: string, description: "Das Kennzeichen des Hauptzählers (MS-0001 oder das bestehende)." }
        bestehend: { type: boolean, description: "true = eine schon vorhandene Messstelle; false = ein Vorschlag dieser Liste." }
        komponente: { type: string, format: uuid, nullable: true, description: "Nur beim Vorschlag: welche Zeile gemeint ist." }
        kanal: { type: string, nullable: true }
    MessstelleVorschlagHinweis:
      type: object
      description: "Was an einer Zeile hängt, ohne sie zu verhindern."
      required: [code, text]
      properties:
        code: { type: string, enum: [integration, ladestand_herkunft, geraet_gewechselt, standort_spaeter] }
        text: { type: string }
    MessstelleVorschlag:
      type: object
      description: >
        Eine Zeile der Vorschlagsliste: was aus diesem Messwert eine Messstelle machen
        würde. `kennzeichen` ist der automatische Vorschlag (E7) - vergeben wird es erst
        bei der Übernahme; `ab` der Beginn der Bindung, `stellung_ab` der Tag, ab dem die
        Stellung gilt (nie vor dem Tag, an dem ihr Hauptzähler einer ist).
      required: [kennzeichen, name, anlage, anlage_name, komponente, komponente_name, hauptgroesse, quelle,
        nebengroessen, stellung, unterzaehler_von, ort, ab, stellung_ab, hinweise]
      properties:
        kennzeichen: { type: string, example: "MS-0001" }
        name: { type: string, description: "Der Alias der Komponente; liest sie mehr als einen Fluss, nennt der Name ihn („Netzzähler Halle 1 · Bezug“)." }
        anlage: { type: string, format: uuid }
        anlage_name: { type: string, nullable: true }
        komponente: { type: string, format: uuid }
        komponente_name: { type: string, nullable: true }
        hauptgroesse: { $ref: "#/components/schemas/MessGroesse" }
        quelle: { $ref: "#/components/schemas/MessstelleVorschlagQuelle" }
        nebengroessen: { type: array, items: { $ref: "#/components/schemas/MessstelleVorschlagNebengroesse" } }
        stellung:
          type: string
          nullable: true
          enum: [Hauptzähler, Unterzähler, Erzeuger, Speicher, Abzweig, keine, null]
          description: "null = offen; sie wird nie geraten."
        unterzaehler_von:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/MessstelleVorschlagBezug" }]
        ort: { type: string, description: "Das Kurzzeichen des Standorts (Verfeinerung später, §5.15)." }
        ab: { type: string, format: date-time, description: "Der Beginn der Bindung - rückwirkend, Herkunft „Bestandsübernahme“." }
        stellung_ab: { type: string, format: date }
        hinweise: { type: array, items: { $ref: "#/components/schemas/MessstelleVorschlagHinweis" } }
    MessstelleVorschlagAusgelassen:
      type: object
      description: "Was nicht vorgeschlagen wird, mit Grund und Satz; `kanal` leer = die ganze Komponente."
      required: [anlage, komponente, komponente_name, kanal, grund, zu, text]
      properties:
        anlage: { type: string, format: uuid }
        komponente: { type: string, format: uuid }
        komponente_name: { type: string, nullable: true }
        kanal: { type: string, nullable: true }
        grund:
          type: string
          enum: [abgeleitet, ohne_messkanal, ohne_geraet, attribut_kanal, keine_messgroesse, ohne_richtung,
            weitere_groesse, vorzeichen_wert, vergleich_kandidat, gleicher_fluss, passt_nicht, ausserhalb_zugriff]
          description: "`ausserhalb_zugriff` (AP-03 R-A6): die Zeile hinge an einer Messstelle außerhalb des Zugriffs des Lesers (Unterzähler eines bestehenden Hauptzählers, oder `zu`) - `zu` ist dann null, `text` der Hinweis ohne Kennzeichen."
        zu: { type: string, nullable: true, description: "Die Messstelle, auf die der Grund zeigt (Vergleichsquelle, gleicher Fluss)." }
        text: { type: string }
    MessstelleVorschlagsliste:
      type: object
      description: "Die Vorschlagsliste eines Standorts; `leer` und `text` sind gesetzt, WENN es keinen Vorschlag gibt."
      required: [standort, standort_kennzeichen, standort_name, vorschlaege, ausgelassen, leer, text]
      properties:
        standort: { type: string, format: uuid }
        standort_kennzeichen: { type: string, example: "ST-1" }
        standort_name: { type: string }
        vorschlaege: { type: array, items: { $ref: "#/components/schemas/MessstelleVorschlag" } }
        ausgelassen: { type: array, items: { $ref: "#/components/schemas/MessstelleVorschlagAusgelassen" } }
        leer: { type: string, nullable: true, enum: [alle_zugeordnet, keine_komponente, null] }
        text: { type: string, nullable: true, description: "„Alle Komponenten von Werk Ahrenberg sind Messstellen zugeordnet.“" }
    MessstelleVorschlagBestaetigt:
      type: object
      description: >
        Eine bestätigte Zeile: die Felder, die das GET gezeigt hat. `name` darf
        überschrieben werden (leer = der vorgeschlagene); alles andere muss noch so sein,
        sonst 409 `vorschlag_geaendert`.
      required: [komponente, kanal, hauptgroesse, ab]
      properties:
        komponente: { type: string, format: uuid }
        kanal: { type: string }
        hauptgroesse: { $ref: "#/components/schemas/MessGroesse" }
        nebengroessen: { type: array, items: { $ref: "#/components/schemas/MessstelleVorschlagNebengroesse" } }
        stellung: { type: string, nullable: true }
        ab: { type: string, format: date-time }
        name: { type: string, nullable: true }
    MessstelleVorschlagUebernehmen:
      type: object
      required: [vorschlaege]
      properties:
        vorschlaege:
          type: array
          minItems: 1
          items: { $ref: "#/components/schemas/MessstelleVorschlagBestaetigt" }
    MessstelleVorschlagUebernommen:
      type: object
      description: "Wie viele Messstellen neu entstanden sind, wie viele es schon gab, und sie selbst in der Reihenfolge der Anfrage."
      required: [neu, unveraendert, messstellen]
      properties:
        neu: { type: integer }
        unveraendert: { type: integer, description: "Zeilen, die schon übernommen waren - ein zweiter Aufruf legt nichts an." }
        messstellen: { type: array, items: { $ref: "#/components/schemas/Messstelle" } }
    KadenzFehler:
      type: object
      description: >
        Die Ablehnung der Kadenz-Schnittstelle (AP-07 IP-10). Die Codes der Regeln
        (uems/KadenzRegeln) in ihrer Prüfreihenfolge: `kadenz_ungueltig` 400 (keine
        Zahl oder außerhalb 1 … 86 400 s - die Schranken des Drahtvertrags),
        `zeitpunkt_ungueltig` 400 (`gueltig_ab` nicht auf die volle Minute),
        `vor_beginn` 422 und `nach_ende` 422 (die Fassung liegt in ihrer
        Quellenbindung), `beginn_belegt` 409 (dort beginnt schon eine - eine Fassung
        wird nie überschrieben), `unveraendert` 400. Dazu der Code der Schnittstelle:
        `anfrage_ungueltig` 400 (`feld`).
      required: [code, message]
      properties:
        code:
          type: string
          enum: [kadenz_ungueltig, zeitpunkt_ungueltig, vor_beginn, nach_ende, beginn_belegt,
            unveraendert, anfrage_ungueltig]
        message: { type: string }
        feld: { type: string }
        quelle_id: { type: string, format: uuid }
        erwartet_s: { type: integer, nullable: true }
        gueltig_ab: { type: string, nullable: true }
    KadenzEintragen:
      type: object
      required: [erwartet_s]
      properties:
        erwartet_s: { type: integer, minimum: 1, maximum: 86400 }
        gueltig_ab:
          type: string
          format: date-time
          description: Auf die volle Minute, mit Versatz; fehlend = jetzt.
        grund: { type: string, maxLength: 500 }
    KadenzFassung:
      type: object
      required: [id, erwartet_s, herkunft, gueltig_ab, status, rueckwirkend]
      properties:
        id: { type: string, format: uuid }
        erwartet_s: { type: integer }
        herkunft:
          type: string
          enum: [bestand, eintrag]
          description: >
            `bestand` - beim Umstieg aus der Mess-Selektion übernommen, gilt seit dem
            Beginn der Bindung; `eintrag` - von Hand eingetragen.
        gueltig_ab: { type: string, format: date-time }
        gueltig_bis: { type: string, format: date-time, nullable: true }
        status: { type: string, enum: [geplant, gilt, beendet] }
        rueckwirkend: { type: boolean }
        begruendung: { type: string, nullable: true }
        eingetragen_am: { type: string, format: date-time }
        eingetragen_von: { type: string }
    Kadenz:
      type: object
      required: [messstelle_id, quelle_id, kanal, stichtag, erwartet_s, herkunft, vorgabe_s, fassungen]
      properties:
        messstelle_id: { type: string, format: uuid }
        quelle_id: { type: string, format: uuid }
        komponente: { type: string, format: uuid }
        kanal: { type: string }
        stichtag: { type: string, format: date-time }
        erwartet_s: { type: integer }
        herkunft:
          type: string
          enum: [fassung, auswahl, katalog, vorgabe]
          description: >
            Die Vorgabe-Kette: eingetragene Fassung, sonst die Kadenz der
            Mess-Selektion, sonst die des Katalogs (`default_cadence_s`), sonst 300 s.
        vorgabe_s: { type: integer, description: Was ohne eingetragene Fassung gälte. }
        vorgabe_herkunft: { type: string, enum: [auswahl, katalog, vorgabe] }
        gueltig:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/KadenzFassung" }]
        fassungen:
          type: array
          items: { $ref: "#/components/schemas/KadenzFassung" }
    MessstelleFormelTerm:
      type: object
      description: >
        Ein Term der Formel, wie er gespeichert ist (snake_case - das Portal wandelt nichts um).
        `verteilung_ziel` und `anteil` (AP-10 IP-5) stehen nur im Körper, wenn der Term sie trägt;
        ein Term ohne sie (`anteil` = gesamt) antwortet wie vor IP-5. `gilt_als_erzeugung` ist der
        AP-08-Haken (`messstelle-formel.md` §2.2) und steht immer da.
      required: [position, eingang_art, vorzeichen, faktor, gilt_als_erzeugung, eingerichtet]
      properties:
        position: { type: integer }
        eingang_art: { type: string, enum: [messkanal, messstelle, verteilung] }
        entity_id: { type: string, format: uuid, nullable: true }
        point_key: { type: string, nullable: true }
        quell_messstelle_id: { type: string, format: uuid, nullable: true }
        vorzeichen: { type: string, enum: ["+", "-"] }
        faktor: { type: number }
        gilt_als_erzeugung:
          type: boolean
          description: AP-08-Haken - ein Messkanal ohne Katalog-Richtung zählt als Erzeugung; sonst false.
        groesse:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/MessGroesse" }]
        eingerichtet: { type: boolean }
        verteilung_ziel:
          type: string
          format: uuid
          description: Bei `eingang_art` = verteilung - die Kostenstelle, deren Anteil DES TAGES an `quell_messstelle_id` der Term liest.
        anteil:
          type: string
          enum: [positiv, negativ]
          description: Nur der positive bzw. negative Teil eines Messwerts; fehlt = gesamt.
    MessstelleFormelFassung:
      type: object
      description: >
        Eine Fassung der Formel (AP-10 IP-3). `gueltig_ab` null = gilt seit Beginn (Fassung 1
        des Bestands und des Anlegens); `gueltig_bis` ist der LETZTE Tag, einschließlich
        (null = bis auf Weiteres).
      required: [nummer, formel_typ, gueltig_ab, gueltig_bis, herkunft, rueckwirkend, abzeichen, begruendung, eingetragen_am]
      properties:
        nummer: { type: integer, minimum: 1 }
        formel_typ: { type: string, enum: [gewichtete_summe, rest, saldo] }
        gueltig_ab: { type: string, format: date, nullable: true }
        gueltig_bis: { type: string, format: date, nullable: true }
        herkunft:
          type: string
          enum: [bestand, anlage, eintrag]
          description: >
            `bestand` - die Terme von vor AP-10 IP-3 (Migration); `anlage` - beim Anlegen der
            berechneten Messstelle; `eintrag` - über `POST …/formel/fassungen`.
        rueckwirkend: { type: boolean }
        abzeichen: { type: string, nullable: true, example: "rückwirkend (5 Tage)" }
        begruendung: { type: string, nullable: true }
        eingetragen_am: { type: string, format: date-time }
    MessstelleFormel:
      type: object
      required: [messstelle_id, schema_version, hauptgroesse, terme, formel_vorhanden, eingaenge_eingerichtet]
      properties:
        messstelle_id: { type: string, format: uuid }
        schema_version: { type: string, example: "1.0" }
        hauptgroesse: { $ref: "#/components/schemas/MessGroesse" }
        terme: { type: array, items: { $ref: "#/components/schemas/MessstelleFormelTerm" } }
        formel_vorhanden: { type: boolean }
        eingaenge_eingerichtet: { type: boolean }
        fassung_am:
          type: object
          description: NUR mit `am` - ohne `am` fehlt der Block (die Antwort von vor AP-10 IP-3).
          required: [tag, fassung]
          properties:
            tag: { type: string, format: date }
            fassung:
              nullable: true
              allOf: [{ $ref: "#/components/schemas/MessstelleFormelFassung" }]
        ausserhalb_zugriff:
          type: string
          description: "NUR wenn ein Eingang (auch über eine berechnete Messstelle oder einen Rest) außerhalb des Zugriffs des Lesers liegt (AP-03 R-A3/R-A6): dann fehlt jeder Term, der eine Messstelle oder die Komponente eines Messkanal-Terms außerhalb nennt, die übrigen sind lückenlos durchnummeriert. Ohne Namen, ohne Anzahl."
          example: "umfasst Standorte außerhalb Ihres Zugriffs"
        geteilte_register:
          type: array
          items: { $ref: "#/components/schemas/MessstelleFormelGeteiltesRegister" }
          description: >
            Summen-Wächter des geteilten Punkts (AP-07 IP-18b): Terme mit demselben Vorzeichen lesen denselben Messpunkt
            derselben Box über zwei Komponenten (ein Messkanal-Term über seine Komponente, ein Messstellen-Term über die
            führende Quelle der Hauptgröße), an dem Tag der Fassung. Liest jede Komponente dasselbe Gerät, zählt die
            Formel den Wert zweimal. Eine Warnung neben den Termen - sie ändert keine Zahl. Fehlt ohne Fund und fehlt
            immer zusammen mit `ausserhalb_zugriff`.
    MessstelleFormelGeteiltesRegister:
      type: object
      required: [register, positionen]
      properties:
        register: { type: string, description: "Der Messpunkt (point_key) an der Box.", example: sunspec.model_203.totwhimp }
        positionen: { type: array, items: { type: integer }, minItems: 2, example: [0, 1], description: "Die `position` der Terme." }
    MessstelleFormelFassungEintragen:
      type: object
      additionalProperties: false
      required: [gueltig_ab, terme]
      properties:
        gueltig_ab: { type: string, format: date }
        formel_typ: { type: string, enum: [gewichtete_summe, saldo], description: "Fehlend = gewichtete_summe; ein Rest folgt der Stellung." }
        terme:
          type: array
          minItems: 1
          items:
            type: object
            required: [eingang_art, vorzeichen]
            properties:
              eingang_art: { type: string, enum: [messkanal, messstelle, verteilung] }
              entity_id: { type: string, format: uuid }
              point_key: { type: string }
              quell_messstelle_id: { type: string, format: uuid }
              vorzeichen: { type: string, enum: ["+", "-"] }
              faktor: { type: number, description: "Fehlend = 1, nie 0; bei verteilung nur 1." }
              gilt_als_erzeugung: { type: boolean, description: "Fehlend = false. AP-08-Haken, nur an einem messkanal-Term ohne Katalog-Richtung (messstelle-formel.md §2.2), sonst 400." }
              verteilung_ziel: { type: string, format: uuid, description: "Nur bei eingang_art = verteilung: die Kostenstelle." }
              anteil:
                type: string
                enum: [gesamt, positiv, negativ]
                description: >
                  Fehlend = gesamt. `positiv`/`negativ` lehnt die Schnittstelle bis AP-08 IP-7 mit 422
                  `anteil_wartet_auf_ap08` ab (Vertrag verteilung-vectors.json, Block `leseweg`). Ein Term
                  mit `eingang_art` = verteilung liest seit AP-10 IP-8 den Anteil des Tages; eine
                  Kostenstelle, die es nicht gibt, ist 404.
        begruendung: { type: string, maxLength: 500 }
    KostenstelleProzessFehler:
      type: object
      description: >
        Die Ablehnung der Kostenstellen- und Prozess-Schnittstelle (AP-10 IP-7): 400
        `anfrage_ungueltig` (`feld`); 404 `nicht_gefunden`; 409 `unternehmen_nicht_angelegt`,
        `kennzeichen_belegt` (`kennzeichen`), `bereits_beendet` (`kennzeichen`, `gueltig_bis`),
        `zuordnung_besteht` (`kennzeichen`, `gueltig_bis`, `zuordnungen`),
        `messstelle_archiviert` (`kennzeichen`), `zuordnung_ueberlappt`; 422
        `kennzeichen_format`, `zeitraum_ungueltig` (`gueltig_ab`, `gueltig_bis`), `eine_ebene`
        (`eltern`, `eltern_von`), `eltern_unbekannt`, `prozess_unbekannt` (`prozess_id`),
        `ziel_besteht_nicht` (`kennzeichen`, `besteht_ab`, `besteht_bis`, `gueltig_ab`, `gueltig_bis`).
      required: [code, message]
      properties:
        code:
          type: string
          enum: [anfrage_ungueltig, nicht_gefunden, unternehmen_nicht_angelegt, kennzeichen_format, kennzeichen_belegt, zeitraum_ungueltig, bereits_beendet, zuordnung_besteht, eine_ebene, eltern_unbekannt, prozess_unbekannt, ziel_besteht_nicht, messstelle_archiviert, zuordnung_ueberlappt]
        message: { type: string }
        feld: { type: string }
        kennzeichen: { type: string }
        gueltig_ab: { type: string, format: date }
        gueltig_bis: { type: string, format: date, nullable: true }
        besteht_ab: { type: string, format: date }
        besteht_bis: { type: string, format: date, nullable: true }
        eltern: { type: string }
        eltern_von: { type: string }
        prozess_id: { type: string, format: uuid }
        zuordnungen:
          type: array
          items:
            type: object
            required: [art, kennzeichen, gueltig_ab, gueltig_bis]
            properties:
              art: { type: string, description: "`messstelle` · `unterprozess` - eine Tabelle eines späteren Pakets nennt sich selbst." }
              kennzeichen: { type: string, nullable: true }
              gueltig_ab: { type: string, format: date }
              gueltig_bis: { type: string, format: date, nullable: true }
    NetzanschlussFehler:
      type: object
      description: >
        Die Ablehnung der Netzanschluss-Schnittstelle (AP-10 IP-6): 400 `anfrage_ungueltig`
        (`feld`); 404 `nicht_gefunden`; 409 `kennzeichen_belegt` (`feld`, `kennzeichen`),
        `bereits_beendet` (`kennzeichen`, `gueltig_bis`), `bindung_besteht` (`kennzeichen`,
        `bindungen`), `bindung_ueberlappt` und `anschluss_belegt` (`anlage_id`, `netzanschluss`,
        `gueltig_ab`, `bindungen`); 422 `kennzeichen_format` (`feld`, `kennzeichen`), `malo_form`
        (`feld`), `zeitraum_ungueltig` (`gueltig_ab`, `gueltig_bis`), `anlage_unbekannt` (`feld`),
        `netzanschluss_besteht_nicht` (`kennzeichen`, `besteht_ab`, `besteht_bis`, `gueltig_ab`);
        seit AP-15 IP-3 das Grenzblatt: 422 `grenze_ueber_vereinbart` und `grenze_ueber_anschluss`
        (`kennzeichen`, `einspeisegrenze_kw`, `bezugsgrenze_kw`, `vereinbart_kw`, `anschluss_kva`)
        aus netzanschluss-grenze-vectors.json.
        Die Codes des Vertrags (`kennzeichen_*`, `malo_form`, `anfrage_ungueltig`,
        `bindung_ueberlappt`, `anschluss_belegt`) sind die von netzanschluss-vectors.json.
      required: [code, message]
      properties:
        code:
          type: string
          enum: [anfrage_ungueltig, nicht_gefunden, kennzeichen_format, kennzeichen_belegt, malo_form, zeitraum_ungueltig, bereits_beendet, bindung_besteht, anlage_unbekannt, netzanschluss_besteht_nicht, bindung_ueberlappt, anschluss_belegt, grenze_ueber_vereinbart, grenze_ueber_anschluss]
        message: { type: string }
        feld: { type: string }
        kennzeichen: { type: string, nullable: true }
        anlage_id: { type: string, format: uuid }
        netzanschluss: { type: string }
        gueltig_ab: { type: string, format: date }
        gueltig_bis: { type: string, format: date, nullable: true }
        besteht_ab: { type: string, format: date, nullable: true }
        besteht_bis: { type: string, format: date, nullable: true }
        einspeisegrenze_kw: { type: number, nullable: true }
        bezugsgrenze_kw: { type: number, nullable: true }
        vereinbart_kw: { type: number, nullable: true }
        anschluss_kva: { type: number, nullable: true }
        bindungen:
          type: array
          items:
            type: object
            required: [anlage_id, anlage_name, netzanschluss, gueltig_ab, gueltig_bis]
            properties:
              anlage_id: { type: string, format: uuid }
              anlage_name: { type: string, nullable: true }
              netzanschluss: { type: string }
              gueltig_ab: { type: string, format: date }
              gueltig_bis: { type: string, format: date, nullable: true }
    NetzanschlussAnschluss:
      type: object
      additionalProperties: false
      description: >
        POST und PUT (die ganze Menge). Jedes Feld Text oder null; die Leistungen Zahl oder
        Dezimaltext. Beim Anlegen heißt `kennzeichen` null: automatisch vergeben.
      required: [name, messung]
      properties:
        kennzeichen: { type: string, nullable: true, pattern: "^[A-Z0-9./-]{2,16}$" }
        name: { type: string }
        malo: { type: string, nullable: true, pattern: "^[0-9]{11}$", description: "Marktlokation - null = nicht erhoben, nie 0." }
        netzbetreiber: { type: string, nullable: true }
        anschluss_kva: { type: number, nullable: true, exclusiveMinimum: 0 }
        vereinbart_kw: { type: number, nullable: true, exclusiveMinimum: 0 }
        messung: { type: string, enum: [RLM, SLP] }
        gueltig_ab: { type: string, format: date, nullable: true, description: "Erster Tag; null = kein erster Tag erhoben." }
        gueltig_bis: { type: string, format: date, nullable: true, description: "Der LETZTE gültige Tag, einschließlich." }
    NetzanschlussUebernehmen:
      type: object
      additionalProperties: false
      description: >
        Vom Kunden ergänzte Anschlussangaben und Beginn der Bindung. Jedes Feld Text oder null;
        die Leistungen Zahl oder Dezimaltext. `kennzeichen` null heißt automatisch vergeben.
      required: [name, messung, bindung_ab]
      properties:
        kennzeichen: { type: string, nullable: true, pattern: "^[A-Z0-9./-]{2,16}$" }
        name: { type: string }
        malo: { type: string, nullable: true, pattern: "^[0-9]{11}$", description: "Marktlokation - null = nicht erhoben, nie 0." }
        netzbetreiber: { type: string, nullable: true }
        anschluss_kva: { type: number, nullable: true, exclusiveMinimum: 0 }
        vereinbart_kw: { type: number, nullable: true, exclusiveMinimum: 0 }
        messung: { type: string, enum: [RLM, SLP] }
        gueltig_ab: { type: string, format: date, nullable: true, description: "Erster Tag; null = kein erster Tag erhoben." }
        gueltig_bis: { type: string, format: date, nullable: true, description: "Der LETZTE gültige Tag, einschließlich." }
        bindung_ab: { type: string, format: date, description: "Beginn der Bindung; unabhängig vom Bestehensdatum des Anschlusses." }
        grund: { type: string, nullable: true, description: "Bei rückwirkender Bindung erforderlich." }
    NetzanschlussVorschlag:
      type: object
      additionalProperties: false
      required: [anlage_id, anlage_name, bindung_ab, kennzeichen, name]
      properties:
        anlage_id: { type: string, format: uuid }
        anlage_name: { type: string }
        bindung_ab: { type: string, format: date }
        kennzeichen: { type: string }
        name: { type: string }
    NetzanschlussBinden:
      type: object
      additionalProperties: false
      required: [anlage_id, gueltig_ab]
      properties:
        anlage_id: { type: string, format: uuid }
        gueltig_ab: { type: string, format: date }
        grund: { type: string, nullable: true }
    NetzanschlussGrenzeSetzen:
      type: object
      additionalProperties: false
      description: >
        UEMS AP-15 IP-3. Ab `gueltig_ab` gilt diese Fassung bis zum Vortag der nächsten. null in
        einer Richtung = unbekannt (die Anlage gilt). `einspeisegrenze_keine: true` erklärt ausdrücklich
        keine Einspeisegrenze und ist nur ohne `einspeisegrenze_kw` zulässig (sonst 400). Leistungen als Zahl
        oder Dezimaltext, höchstens drei Nachkommastellen.
      required: [gueltig_ab]
      not:
        required: [einspeisegrenze_keine, einspeisegrenze_kw]
        properties:
          einspeisegrenze_keine: { enum: [true] }
          einspeisegrenze_kw: { type: number }
      properties:
        gueltig_ab: { type: string, format: date }
        einspeisegrenze_kw: { type: [number, "null"], exclusiveMinimum: 0 }
        einspeisegrenze_keine: { type: boolean, default: false }
        bezugsgrenze_kw: { type: [number, "null"], exclusiveMinimum: 0 }
        grund: { type: string, nullable: true }
    NetzanschlussGrenzFassung:
      type: object
      required: [gueltig_ab, einspeisegrenze_kw, einspeisegrenze_keine, bezugsgrenze_kw, eingetragen_am]
      properties:
        gueltig_ab: { type: string, format: date }
        einspeisegrenze_kw: { type: [number, "null"] }
        einspeisegrenze_keine: { type: boolean, description: "Ausdrücklich keine Einspeisegrenze, nur ohne Zahlenwert." }
        bezugsgrenze_kw: { type: [number, "null"] }
        eingetragen_am: { type: string, format: date-time }
    NetzanschlussGrenzblatt:
      type: object
      required: [netzanschluss_id, kennzeichen, stichtag, gilt, fassungen]
      properties:
        netzanschluss_id: { type: string, format: uuid }
        kennzeichen: { type: string }
        stichtag: { type: string, format: date }
        gilt:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/NetzanschlussGrenzFassung" }]
        fassungen:
          type: array
          items: { $ref: "#/components/schemas/NetzanschlussGrenzFassung" }
    NetzanschlussGrenzNachweis:
      type: object
      description: UEMS AP-15 IP-31 - netzanschluss.md §6, Vektoren netzanschluss-grenznachweis-vectors.json.
      required: [netzanschluss_id, kennzeichen, monat, von, bis, zeitraum_von, zeitraum_bis, zeitzone,
        grenze_geprueft, grund, urteil, richtungen]
      properties:
        netzanschluss_id: { type: string, format: uuid }
        kennzeichen: { type: string }
        monat: { type: string, nullable: true, example: "2026-09", description: "Leer beim freien Zeitraum." }
        von: { type: string, format: date, nullable: true, description: "Erster Tag; null ohne abgeschlossenen Tag." }
        bis: { type: string, format: date, nullable: true, description: "Letzter Tag, eingeschlossen; nie heute." }
        zeitraum_von: { type: string, format: date-time, nullable: true, description: "Beginn des freien Zeitraums." }
        zeitraum_bis: { type: string, format: date-time, nullable: true, description: "Ende des freien Zeitraums, ausgeschlossen." }
        zeitzone: { type: string }
        grenze_geprueft: { type: boolean }
        grund: { type: string, nullable: true, enum: [keine_grenze, kein_hauptzaehler, kein_abgeschlossener_tag, null] }
        urteil:
          type: string
          nullable: true
          enum: [eingehalten, ueberschritten, nicht_belegt, null]
          description: Über beide Richtungen; null ohne geprüfte Richtung oder mit einer Richtung außerhalb des Zugriffs.
        richtungen:
          type: array
          items: { $ref: "#/components/schemas/NetzanschlussGrenzNachweisRichtung" }
    NetzanschlussGrenzNachweisRichtung:
      type: object
      required: [richtung, grenze_geprueft, grund, urteil, grenzen, hauptzaehler, viertelstunden, belegt_prozent,
        hoechstes_mittel, darueber, unterbrechungen, augenblick, grenzherkunft, grenzhinweis]
      properties:
        richtung: { type: string, enum: [bezug, einspeisung] }
        grenze_geprueft: { type: boolean }
        grund: { type: string, nullable: true, enum: [keine_grenze, kein_hauptzaehler, kein_abgeschlossener_tag, null] }
        urteil: { type: string, nullable: true, enum: [eingehalten, ueberschritten, nicht_belegt, null] }
        grenzen:
          type: array
          items:
            type: object
            properties:
              von: { type: string, format: date }
              bis: { type: string, format: date }
              kw: { type: number }
              quelle: { type: string, enum: [anlage, netzanschluss] }
        hauptzaehler:
          type: array
          items:
            type: object
            properties:
              id: { type: string, format: uuid }
              kennzeichen: { type: string }
              name: { type: string }
        viertelstunden:
          type: object
          nullable: true
          description: Nur Viertelstunden mit Grenze.
          properties:
            erwartet: { type: integer }
            belegt: { type: integer }
            unvollstaendig: { type: integer, description: "Unvollständig oder mit Ersatzwert." }
            fehlend: { type: integer }
        belegt_prozent: { type: integer, nullable: true, description: "Abgerundet - 100 nur, wenn nichts fehlt." }
        hoechstes_mittel:
          type: object
          nullable: true
          description: Die belegte Viertelstunde mit dem größten Abstand Mittel − Grenze.
          properties:
            von: { type: string, format: date-time }
            bis: { type: string, format: date-time }
            mittel_kw: { type: number }
            grenze_kw: { type: number }
            abstand_kw: { type: number }
        darueber:
          type: object
          nullable: true
          properties:
            viertelstunden: { type: integer }
            minuten: { type: integer }
        unterbrechungen:
          type: array
          items:
            type: object
            properties:
              von: { type: string, format: date-time }
              bis: { type: string, format: date-time }
              minuten: { type: integer }
              hoechstwert_kw: { type: number }
              grenze_kw: { type: number }
        augenblick:
          type: object
          description: M-2 - heute `nicht_gemessen`, Grund `keine_dauer_unter_der_viertelstunde`.
          properties:
            status: { type: string, enum: [nicht_gemessen] }
            grund: { type: string }
        grenzherkunft:
          type: array
          uniqueItems: true
          items: { type: string, enum: [grenzblatt, anlage] }
          description: Grenzblatt = zeitgültig; Anlage = nicht zeitgültiges Anlagenfeld.
        grenzhinweis:
          type: string
          nullable: true
          description: Bei Herkunft Anlage der sichtbare Hinweis, die Grenze für den Nachweis ins Grenzblatt einzutragen.
        ausserhalb_zugriff: { type: string, description: "Nur, wenn ein Hauptzähler außerhalb des Zugriffs liegt; dann ohne Zahlen." }
    NetzanschlussStandort:
      type: object
      properties:
        id: { type: string, format: uuid }
        kurzzeichen: { type: string }
    NetzanschlussAnlage:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string, nullable: true, description: "null, wenn die Anlage gelöscht ist - die Bindung bleibt (W5)." }
    NetzanschlussBindung:
      type: object
      properties:
        id: { type: string, format: uuid }
        anlage: { $ref: "#/components/schemas/NetzanschlussAnlage" }
        gueltig_ab: { type: string, format: date }
        gueltig_bis: { type: string, format: date, nullable: true }
    Netzanschluss:
      type: object
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, example: NA-1 }
        name: { type: string }
        standort: { $ref: "#/components/schemas/NetzanschlussStandort" }
        malo: { type: string, nullable: true }
        netzbetreiber: { type: string, nullable: true }
        anschluss_kva: { type: number, nullable: true }
        vereinbart_kw: { type: number, nullable: true }
        messung: { type: string, enum: [RLM, SLP] }
        gueltig_ab: { type: string, format: date, nullable: true }
        gueltig_bis: { type: string, format: date, nullable: true }
        hinweise:
          type: array
          items: { type: string, enum: [vereinbart_ueber_anschluss] }
        anlagen:
          type: array
          items: { $ref: "#/components/schemas/NetzanschlussBindung" }
        angelegt_am: { type: string, format: date-time }
    NetzanschlussListe:
      type: object
      properties:
        standort: { $ref: "#/components/schemas/NetzanschlussStandort" }
        stichtag: { type: string, format: date, nullable: true }
        kennzeichen_vorschlag: { type: string, example: NA-0001 }
        netzanschluesse:
          type: array
          items: { $ref: "#/components/schemas/Netzanschluss" }
    KostenstelleProzessAnlegen:
      type: object
      additionalProperties: false
      description: Jedes Feld Text oder null. `eltern_id` nur beim Prozess.
      required: [kennzeichen, name, gueltig_ab]
      properties:
        kennzeichen: { type: string, pattern: "^[A-Z0-9./-]{2,16}$" }
        name: { type: string }
        eltern_id: { type: string, format: uuid, nullable: true }
        gueltig_ab: { type: string, format: date }
        gueltig_bis: { type: string, format: date, nullable: true, description: "Der LETZTE gültige Tag, einschließlich." }
    KostenstelleProzessUmbenennen:
      type: object
      additionalProperties: false
      required: [name]
      properties:
        name: { type: string }
    KostenstelleProzessBeenden:
      type: object
      additionalProperties: false
      required: [gueltig_bis]
      properties:
        gueltig_bis: { type: string, format: date, description: "Der LETZTE gültige Tag, einschließlich." }
    Kostenstelle:
      type: object
      description: Eine Kostenstelle - flach, ohne Elternteil.
      required: [id, kennzeichen, name, gueltig_ab, gueltig_bis]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        name: { type: string }
        gueltig_ab: { type: string, format: date }
        gueltig_bis: { type: string, format: date, nullable: true }
        angelegt_am: { type: string, format: date-time, description: "Immer da, außer im Auswahl-Katalog der Liste (nur Stammdaten, Entscheid 21.09.2026, Lesart B)." }
    KostenstelleListe:
      type: object
      required: [stichtag, kostenstellen]
      properties:
        stichtag: { type: string, format: date, nullable: true }
        kostenstellen: { type: array, items: { $ref: "#/components/schemas/Kostenstelle" } }
    KostenstelleProzessVerweis:
      type: object
      required: [id, kennzeichen]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
    Prozess:
      type: object
      description: Ein Prozess; `eltern` ist der übergeordnete Prozess (eine Ebene) oder null.
      required: [id, kennzeichen, name, eltern, gueltig_ab, gueltig_bis]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        name: { type: string }
        eltern: { allOf: [{ $ref: "#/components/schemas/KostenstelleProzessVerweis" }], nullable: true }
        gueltig_ab: { type: string, format: date }
        gueltig_bis: { type: string, format: date, nullable: true }
        angelegt_am: { type: string, format: date-time, description: "Immer da, außer im Auswahl-Katalog der Liste (nur Stammdaten, Entscheid 21.09.2026, Lesart B)." }
    ProzessListe:
      type: object
      required: [stichtag, prozesse]
      properties:
        stichtag: { type: string, format: date, nullable: true }
        prozesse: { type: array, items: { $ref: "#/components/schemas/Prozess" } }
    MessstelleProzesseSetzen:
      type: object
      additionalProperties: false
      required: [gueltig_ab, prozesse]
      properties:
        gueltig_ab: { type: string, format: date }
        prozesse: { type: array, items: { type: string, format: uuid }, description: "Die IDs der Prozesse ab dem Tag; leer = keiner." }
        grund: { type: string, nullable: true }
    MessstelleProzessZuordnung:
      type: object
      required: [id, prozess, name, gueltig_ab, gueltig_bis, endet_mit_prozess]
      properties:
        id: { type: string, format: uuid }
        prozess: { $ref: "#/components/schemas/KostenstelleProzessVerweis" }
        name: { type: string, description: Name des Prozesses. }
        gueltig_ab: { type: string, format: date }
        gueltig_bis: { type: string, format: date, nullable: true }
        endet_mit_prozess: { type: boolean, description: "Der letzte Tag ist der des Prozesses - die Zuordnung gilt nie länger als er." }
    MessstelleProzesse:
      type: object
      required: [messstelle_id, kennzeichen, am, prozesse]
      properties:
        messstelle_id: { type: string, format: uuid }
        kennzeichen: { type: string }
        am: { type: string, format: date, nullable: true }
        prozesse: { type: array, items: { $ref: "#/components/schemas/MessstelleProzessZuordnung" } }
    ProzessMessstellenVerweis:
      type: object
      required: [id, kennzeichen, name]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        name: { type: string }
    ProzessMessstelle:
      type: object
      required: [id, kennzeichen, name]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        name: { type: string }
    ProzessSummeHinweis:
      type: object
      required: [code, summe, messstelle, prozesse, ueber_verteilung, verteilung, anteil_prozent]
      properties:
        code: { type: string, enum: [prozess_summe_passt] }
        summe: { $ref: "#/components/schemas/ProzessMessstelle" }
        messstelle: { $ref: "#/components/schemas/ProzessMessstelle" }
        prozesse: { type: array, items: { $ref: "#/components/schemas/ProzessMessstellenVerweis" } }
        ueber_verteilung: { type: boolean }
        verteilung: { type: string, nullable: true, description: "Kennzeichen der Kostenstelle, z. B. 4100; nur über Verteilung." }
        anteil_prozent: { type: string, nullable: true, description: "Ungerundeter Tagesanteil der Verteilung." }
    ProzessMessstellen:
      type: object
      required: [prozess, am, gemessen, berechnet, hinweise]
      properties:
        prozess: { $ref: "#/components/schemas/ProzessMessstellenVerweis" }
        am: { type: string, format: date }
        gemessen: { type: array, items: { $ref: "#/components/schemas/ProzessMessstelle" } }
        berechnet: { type: array, items: { $ref: "#/components/schemas/ProzessMessstelle" } }
        hinweise: { type: array, items: { $ref: "#/components/schemas/ProzessSummeHinweis" } }
    FunktionFehler:
      type: object
      required: [code, message, fehlt, wege]
      properties:
        code: { type: string, enum: [anfrage_ungueltig, nicht_gefunden, nicht_aufgenommen, pruefliste_offen, noch_nicht_gestartet, laeuft_bereits, ist_angehalten, bereits_angehalten, beendet, bereits_angelegt, standort_archiviert] }
        message: { type: string }
        fehlt: { type: array, items: { type: string } }
        wege: { type: array, items: { $ref: "#/components/schemas/FunktionWeg" } }
    Funktionen:
      type: object
      required: [unternehmen, standorte]
      properties:
        unternehmen: { $ref: "#/components/schemas/FunktionUnternehmen" }
        standorte: { type: array, items: { $ref: "#/components/schemas/FunktionStandort" } }
    FunktionUnternehmen:
      type: object
      required: [messen, steuern]
      properties:
        messen: { $ref: "#/components/schemas/FunktionVerbreitung" }
        steuern: { $ref: "#/components/schemas/FunktionVerbreitung" }
    FunktionVerbreitung:
      type: object
      required: [laeuft_an, standorte, text]
      properties:
        laeuft_an: { type: integer, description: "Standorte, an denen die Funktion aktiv ist." }
        standorte: { type: integer, description: "Alle nicht archivierten Standorte." }
        text: { type: string, nullable: true, description: "„Steuern & Optimieren läuft an 1 von 2 Standorten“; null ohne Standort." }
    FunktionStandort:
      type: object
      required: [id, kurzzeichen, name, zeitzone, messen, steuern]
      properties:
        id: { type: string, format: uuid }
        kurzzeichen: { type: string }
        name: { type: string }
        zeitzone: { type: string }
        messen: { $ref: "#/components/schemas/FunktionMessen" }
        steuern: { $ref: "#/components/schemas/FunktionSteuern" }
    FunktionMessen:
      type: object
      required: [zustand, seit, text, fehlt, datenlage]
      properties:
        zustand: { type: string, enum: [kein_objekt, archiviert, entwurf, aktiv] }
        seit: { type: string, format: date-time, nullable: true }
        text: { type: string }
        fehlt: { type: array, items: { type: string } }
        datenlage: { type: string, nullable: true }
    FunktionSteuern:
      type: object
      required: [zustand, seit, text, fehlt, aktionen, anlagen]
      properties:
        zustand: { type: string, enum: [kein_objekt, archiviert, entwurf, eingerichtet, angehalten, aktiv] }
        seit: { type: string, format: date-time, nullable: true }
        text: { type: string }
        fehlt: { type: array, items: { type: string } }
        aktionen: { type: array, items: { type: string, enum: [anhalten, fortsetzen, beenden] } }
        anlagen: { type: array, items: { $ref: "#/components/schemas/FunktionAnlage" } }
    FunktionAnlage:
      type: object
      required: [id, name, teilnahme]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        teilnahme: { $ref: "#/components/schemas/FunktionTeilnahme" }
    FunktionTeilnahme:
      type: object
      required: [zustand, seit, text, uebernommen, ruhe_hinweis, pruefliste, fehlt, wege, aktionen]
      properties:
        zustand: { type: string, enum: [kein_objekt, archiviert, entwurf, eingerichtet, angehalten, aktiv] }
        seit: { type: string, format: date-time, nullable: true }
        text: { type: string }
        uebernommen: { type: boolean }
        ruhe_hinweis: { $ref: "#/components/schemas/FunktionRuheHinweis" }
        pruefliste: { type: array, items: { $ref: "#/components/schemas/FunktionPruefZeile" } }
        fehlt: { type: array, items: { type: string } }
        wege: { type: array, items: { $ref: "#/components/schemas/FunktionWeg" } }
        aktionen: { type: array, items: { type: string, enum: [aufnehmen, starten, anhalten, fortsetzen, beenden] } }
    FunktionRuheHinweis:
      type: object
      required: [jetzt, beim_anhalten]
      properties:
        jetzt: { type: boolean }
        beim_anhalten: { type: boolean }
    FunktionPruefZeile:
      type: object
      required: [pruefung, bestanden]
      properties:
        pruefung: { type: string, enum: [box, freigabe, verbindungstest, grenze, hauptzaehler, betriebsweise] }
        bestanden: { type: boolean, nullable: true, description: "null = nicht prüfbar, nie „bestanden“." }
    FunktionWeg:
      type: object
      required: [pruefung, satz]
      properties:
        pruefung: { type: string, enum: [box, freigabe, verbindungstest, grenze, hauptzaehler, betriebsweise] }
        satz: { type: string }
    FunktionSteuernAnfrage:
      type: object
      additionalProperties: false
      required: [aktion]
      properties:
        aktion: { type: string, enum: [aufnehmen, starten, anhalten, fortsetzen, beenden] }
    FunktionSteuernErgebnis:
      type: object
      required: [aktion, betroffen, standort]
      properties:
        aktion: { type: string, enum: [aufnehmen, starten, anhalten, fortsetzen, beenden] }
        betroffen: { type: array, items: { $ref: "#/components/schemas/FunktionAnlageRef" } }
        standort: { $ref: "#/components/schemas/FunktionStandort" }
    FunktionSteuernPruefung:
      type: object
      required: [anlage_id, anlage, standort_id, standort, bereit, zeilen, freigaben, folgen]
      properties:
        anlage_id: { type: string, format: uuid }
        anlage: { type: string }
        standort_id: { type: string, format: uuid }
        standort: { type: string }
        bereit: { type: boolean }
        zeilen: { type: array, items: { $ref: "#/components/schemas/FunktionSteuernPruefZeile" } }
        freigaben: { $ref: "#/components/schemas/FunktionFreigabeStand" }
        folgen: { type: string }
    FunktionFreigabeStand:
      type: object
      required: [freigegeben, gesamt, text, komponenten]
      properties:
        freigegeben: { type: integer, minimum: 0 }
        gesamt: { type: integer, minimum: 0 }
        text: { type: string }
        komponenten: { type: array, items: { $ref: "#/components/schemas/FunktionFreigabeZeile" } }
    FunktionFreigabeZeile:
      type: object
      required: [entity_id, name, weg, freigegeben, status, station_verbunden, steuerart_gesetzt]
      properties:
        entity_id: { type: string, format: uuid }
        name: { type: string }
        weg: { type: string, enum: [selbstbau, ocpp, wechselrichter] }
        freigegeben: { type: boolean }
        status: { type: string }
        station_verbunden: { type: boolean, nullable: true }
        steuerart_gesetzt: { type: boolean, nullable: true }
    FunktionSteuernPruefZeile:
      type: object
      required: [pruefung, bestanden, fakt, grund, weg]
      properties:
        pruefung: { type: string, enum: [box, freigabe, verbindungstest, grenze, hauptzaehler, betriebsweise] }
        bestanden: { type: boolean, nullable: true }
        fakt: { type: string }
        grund: { type: string, nullable: true }
        weg: { type: string, nullable: true }
    FunktionAnlageRef:
      type: object
      required: [id, name]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
    FunktionMessenAnfrage:
      type: object
      additionalProperties: false
      required: [aktion]
      properties:
        aktion: { type: string, enum: [einrichten] }
    FunktionMessenErgebnis:
      type: object
      required: [aktion, standort]
      properties:
        aktion: { type: string, enum: [einrichten] }
        standort: { $ref: "#/components/schemas/FunktionStandort" }
    BilanzFehler:
      type: object
      description: >
        Die Ablehnung der Bilanz-Schnittstelle (AP-10 IP-9): 400 `anfrage_ungueltig` (`feld`);
        422 `rest_ohne_hauptzaehler` (`hauptzaehler`, Code aus `fehler_neu` des Bilanz-Vertrags).
      required: [code, message]
      properties:
        code: { type: string, enum: [anfrage_ungueltig, rest_ohne_hauptzaehler] }
        message: { type: string }
        feld: { type: string }
        hauptzaehler: { type: string }
    Bilanz:
      type: object
      required: [anlage, periode, am, von, bis, zeitzone, hauptzaehler]
      properties:
        anlage: { $ref: "#/components/schemas/BilanzAnlage" }
        periode: { type: string, enum: [tag, monat, jahr] }
        am: { type: string, format: date }
        von: { type: string, format: date }
        bis: { type: string, format: date, description: "Der LETZTE Tag der Periode (einschließlich)." }
        zeitzone: { type: string, example: Europe/Berlin }
        hauptzaehler: { type: array, items: { $ref: "#/components/schemas/BilanzHauptzaehler" } }
        ausserhalb_zugriff:
          type: string
          description: "NUR wenn ein Hauptzähler fehlt, weil sein Block eine Messstelle außerhalb des Zugriffs des Lesers braucht (er selbst, sein Rest oder ein Term seiner Stellung; AP-03 R-A3/R-A6): Rest, zugeordnete Summe, Abdeckung und Live-Wert fehlen dann ganz. Ohne Namen, ohne Anzahl."
          example: "umfasst Standorte außerhalb Ihres Zugriffs"
    BilanzAnlage:
      type: object
      required: [id, name]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
    BilanzMessstelleRef:
      type: object
      required: [id, kennzeichen, name]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        name: { type: string, nullable: true }
    BilanzHauptzaehler:
      type: object
      required: [messstelle, rest_messstelle, vorschlag, stellung_geaendert, abschnitte, live]
      properties:
        messstelle: { $ref: "#/components/schemas/BilanzMessstelleRef" }
        rest_messstelle:
          nullable: true
          description: "Die bestätigte Rest-Messstelle (E18); null = noch keine."
          allOf: [{ $ref: "#/components/schemas/BilanzMessstelleRef" }]
        vorschlag:
          nullable: true
          description: "„Rest anlegen“ - nur ohne Rest-Messstelle und nur, wenn der Hauptzähler heute Hauptzähler der Anlage ist."
          allOf: [{ $ref: "#/components/schemas/BilanzVorschlag" }]
        stellung_geaendert: { type: boolean, description: "In der Periode gelten verschiedene Terme - dann rechnet jeder Abschnitt Tag für Tag." }
        abschnitte: { type: array, items: { $ref: "#/components/schemas/BilanzAbschnitt" } }
        live: { $ref: "#/components/schemas/BilanzLive" }
    BilanzVorschlag:
      type: object
      required: [aktion, hauptzaehler_id, name]
      properties:
        aktion: { type: string, enum: [rest_anlegen] }
        hauptzaehler_id: { type: string, format: uuid }
        name: { type: string, example: "Werk Ahrenberg – Halle 2 nicht zugeordnet" }
    BilanzAbschnitt:
      type: object
      required: [von, bis, raster, terme, ausserhalb, werte]
      properties:
        von: { type: string, format: date }
        bis: { type: string, format: date }
        raster: { type: string, enum: [tag, monat, jahr] }
        terme: { type: array, items: { $ref: "#/components/schemas/BilanzTerm" } }
        ausserhalb: { type: array, items: { type: string }, description: "Abzweige desselben Systems ohne Vorgänger - gemessen, aber in keiner Bilanz." }
        werte: { type: array, items: { $ref: "#/components/schemas/BilanzWerte" } }
        geteilte_register:
          type: array
          items: { $ref: "#/components/schemas/BilanzGeteiltesRegister" }
          description: >
            Summen-Wächter des geteilten Punkts (AP-07 IP-18b): in einer Summe lesen zwei Messstellen denselben
            Messpunkt derselben Box über zwei Komponenten. Liest jede Komponente dasselbe Gerät, zählt die Summe den
            Wert zweimal. Eine Warnung neben den Zahlen - sie ändert keine. Fehlt ohne Fund.
    BilanzGeteiltesRegister:
      type: object
      required: [rolle, register, messstellen]
      properties:
        rolle: { type: string, enum: [zufluss, abfluss, zugeordnet] }
        register: { type: string, description: "Der Messpunkt (point_key) an der Box.", example: sunspec.model_203.totwhimp }
        messstellen: { type: array, items: { type: string }, minItems: 2, example: [MS-12, MS-13] }
    BilanzTerm:
      type: object
      required: [messstelle, messstelle_id, name, rolle, anteil]
      properties:
        messstelle: { type: string, example: MS-11 }
        messstelle_id: { type: string, format: uuid, nullable: true }
        name: { type: string, nullable: true }
        rolle: { type: string, enum: [zufluss, abfluss, zugeordnet] }
        anteil: { type: string, enum: [gesamt, positiv, negativ] }
    BilanzWerte:
      type: object
      required: [von, bis, zufluss, abfluss, zugeordnet, rest, eingaenge]
      properties:
        von: { type: string, format: date }
        bis: { type: string, format: date }
        zufluss: { $ref: "#/components/schemas/BilanzSumme" }
        abfluss: { $ref: "#/components/schemas/BilanzSumme" }
        zugeordnet: { $ref: "#/components/schemas/BilanzSumme" }
        rest: { $ref: "#/components/schemas/BilanzRest" }
        eingaenge: { type: array, items: { $ref: "#/components/schemas/BilanzEingang" } }
    BilanzSumme:
      type: object
      required: [menge, zustand, abdeckung_prozent, mit_werten, gesamt, fehlend, kennzeichen, anzeige]
      properties:
        menge: { type: number, nullable: true, description: "Ungerundet; null ohne Eingang dieser Rolle - nie 0." }
        zustand: { type: string, nullable: true }
        abdeckung_prozent: { type: integer, nullable: true }
        mit_werten: { type: integer }
        gesamt: { type: integer }
        fehlend: { type: array, items: { type: string } }
        kennzeichen: { type: array, items: { type: string } }
        anzeige: { type: string, nullable: true, example: "mindestens 1.055 kWh (MS-14 fehlt)" }
    BilanzRest:
      type: object
      required: [menge, groesse, richtung, einheit, zustand, abdeckung_prozent, fehlend, kennzeichen, kundensatz, herkunft]
      properties:
        menge: { type: number, nullable: true, description: "Ungerundet; null = keine Werte, nie 0. Negativ wird gezeigt, nie geklemmt." }
        groesse: { type: string, example: Wirkenergie }
        richtung: { type: string, example: Bezug }
        einheit: { type: string, example: kWh }
        zustand: { type: string }
        abdeckung_prozent: { type: integer, nullable: true }
        fehlend: { type: array, items: { type: string } }
        kennzeichen: { type: array, items: { type: string } }
        kundensatz: { type: string, nullable: true, example: "10 kWh sind keiner Messstelle zugeordnet" }
        herkunft:
          type: object
          description: "AP-10 IP-12: {satz, fehlt} nach bilanzwert-herkunft.schema.json (Art berechnet, Typ rest). Ohne bestätigte Rest-Messstelle ist satz null und fehlt nennt messstelle und formel_fassung."
          additionalProperties: true
    BilanzEingang:
      type: object
      required: [messstelle, rolle, anteil, menge, zustand, abdeckung_prozent, version, kennzeichen, grund]
      properties:
        messstelle: { type: string }
        rolle: { type: string, enum: [zufluss, abfluss, zugeordnet] }
        anteil: { type: string, enum: [gesamt, positiv, negativ] }
        menge: { type: number, nullable: true }
        zustand: { type: string }
        abdeckung_prozent: { type: integer, nullable: true }
        version: { type: integer }
        kennzeichen: { type: array, items: { type: string } }
        grund: { type: string, nullable: true, description: "Warum das Lese-Modell „Werte je Messstelle“ keine Menge hat." }
    BilanzLive:
      type: object
      required: [wert, einheit, unvollstaendig, fehlende, stand]
      properties:
        wert: { type: number, nullable: true, example: 1.6, description: "null, sobald EIN Term fehlt oder veraltet ist - nie eine Teilsumme, nie 0." }
        einheit: { type: string, example: kW }
        unvollstaendig: { type: boolean }
        fehlende: { type: array, items: { $ref: "#/components/schemas/BilanzLiveFehlend" } }
        stand: { type: string, format: date-time, nullable: true }
    BilanzLiveFehlend:
      type: object
      required: [term, grund]
      properties:
        term: { type: string, example: MS-14 }
        grund: { type: string, enum: [kein_geraet, kein_wert, veraltet] }
    BilanzRestAnlegen:
      type: object
      additionalProperties: false
      required: [hauptzaehler_id]
      properties:
        hauptzaehler_id: { type: string, format: uuid }
        name: { type: string, description: "Ohne Angabe „<Anlage> nicht zugeordnet“." }
    BilanzRestAngelegt:
      type: object
      required: [neu, hauptzaehler, messstelle]
      properties:
        neu: { type: boolean }
        hauptzaehler: { $ref: "#/components/schemas/BilanzMessstelleRef" }
        messstelle: { $ref: "#/components/schemas/Messstelle" }
    KostenstelleEnergie:
      type: object
      description: >
        Die Kostenstellen-Sicht (AP-10 IP-11). `summe` = gemessen + verteilt + berechnet (ohne Posten);
        `nicht_verteilt` zählt dort nie mit.
      required: [kostenstelle, periode, am, von, bis, zeitzone, version, berechnet_am, gemessen, verteilt, berechnet, summe, nicht_verteilt, doppelzaehlung]
      properties:
        kostenstelle: { $ref: "#/components/schemas/KostenstelleEnergieKostenstelle" }
        periode: { type: string, enum: [tag, monat, jahr] }
        am: { type: string, format: date }
        von: { type: string, format: date }
        bis: { type: string, format: date, description: "Der letzte Tag, einschließlich." }
        zeitzone: { type: string, example: Europe/Berlin }
        version: { type: integer, nullable: true, description: "Die angefragte Version; null = je Tag die neueste." }
        berechnet_am: { type: string, example: "2027-02-01T00:15:00+01:00" }
        gemessen: { $ref: "#/components/schemas/KostenstelleEnergieBlock" }
        verteilt: { $ref: "#/components/schemas/KostenstelleEnergieBlock" }
        berechnet: { $ref: "#/components/schemas/KostenstelleEnergieBlock" }
        summe: { $ref: "#/components/schemas/KostenstelleEnergieBlock" }
        nicht_verteilt: { $ref: "#/components/schemas/KostenstelleEnergieBlock" }
        doppelzaehlung: { $ref: "#/components/schemas/KostenstelleEnergieDoppelzaehlung" }
    KostenstelleEnergieKostenstelle:
      type: object
      required: [id, kennzeichen, name, gueltig_ab, gueltig_bis]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, example: "4100" }
        name: { type: string, example: Spritzguss }
        gueltig_ab: { type: string, format: date }
        gueltig_bis: { type: string, format: date, nullable: true }
    KostenstelleEnergieBlock:
      type: object
      required: [menge, einheit, zustand, grund, summen, posten]
      properties:
        menge: { type: number, nullable: true, description: "Ungerundet; nur bei GENAU einer Größe. null = keine Menge, nie 0." }
        einheit: { type: string, nullable: true, example: kWh }
        zustand: { type: string, nullable: true }
        grund: { type: string, nullable: true, enum: [keine_zuordnung, groessen_gemischt, null] }
        summen: { type: array, items: { $ref: "#/components/schemas/KostenstelleEnergieSumme" } }
        posten: { type: array, items: { $ref: "#/components/schemas/KostenstelleEnergiePosten" } }
    KostenstelleEnergieSumme:
      type: object
      required: [groesse, richtung, einheit, menge, zustand, abdeckung_prozent, vorhanden, gesamt, fehlend]
      properties:
        groesse: { type: string, example: Wirkenergie }
        richtung: { type: string, example: Bezug }
        einheit: { type: string, example: kWh }
        menge: { type: number, nullable: true }
        zustand: { type: string }
        abdeckung_prozent: { type: integer, nullable: true }
        vorhanden: { type: integer }
        gesamt: { type: integer }
        fehlend: { type: array, items: { type: string } }
    KostenstelleEnergiePosten:
      type: object
      required: [messstelle, groesse, richtung, einheit, menge, zustand, abdeckung_prozent, version, kennzeichen, fassungen, fehlend, tage, herkunft]
      properties:
        messstelle: { $ref: "#/components/schemas/KostenstelleEnergieMessstelle" }
        groesse: { type: string }
        richtung: { type: string }
        einheit: { type: string }
        menge: { type: number, nullable: true, description: "Summe der Tagesanteile; null = keine Werte, nie 0." }
        zustand: { type: string }
        abdeckung_prozent: { type: integer, nullable: true }
        version: { type: integer, description: "Die höchste Version der Tage." }
        kennzeichen: { type: array, items: { type: string }, example: ["verteilt (70 % von MS-07)", "verteilt (60 % von MS-07)", "Verteilung geändert am 15.01.2027"] }
        fassungen: { type: array, items: { type: integer }, description: "Die Verteilungs-Fassungen der Tage; bei nicht_verteilt leer." }
        fehlend: { type: array, items: { type: string }, description: "Tage ohne Menge." }
        tage: { type: array, items: { $ref: "#/components/schemas/KostenstelleEnergieTag" } }
        herkunft:
          type: object
          nullable: true
          description: "{satz, fehlt} nach bilanzwert-herkunft.schema.json (Art verteilt); bei nicht_verteilt null — die Rechnung einer berechneten Messstelle steht je Tag an tage[].herkunft."
          additionalProperties: true
    KostenstelleEnergieMessstelle:
      type: object
      required: [id, kennzeichen, name, art]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, example: MS-07 }
        name: { type: string, nullable: true }
        art: { type: string, enum: [gemessen, berechnet] }
    KostenstelleEnergieTag:
      type: object
      required: [tag, anteil_prozent, quelle_menge, menge, zustand, abdeckung_prozent, version, grund, herkunft]
      properties:
        tag: { type: string, format: date }
        anteil_prozent: { type: number, nullable: true, description: "Der Anteil DIESES Tages; bei nicht_verteilt null." }
        quelle_menge: { type: number, nullable: true }
        menge: { type: number, nullable: true }
        zustand: { type: string }
        abdeckung_prozent: { type: integer, nullable: true }
        version: { type: integer }
        grund: { type: string, nullable: true, enum: [kein_tageswert, quelle_keine_werte, rest_unplausibel, null] }
        herkunft:
          type: object
          nullable: true
          description: "AP-10 IP-12: {satz, fehlt} nach bilanzwert-herkunft.schema.json (Art berechnet) am gespeicherten Tageswert einer BERECHNETEN Quelle in der gezeigten Version; null = nicht berechnet (gemessene Quelle) oder kein Tageswert."
          additionalProperties: true
    KostenstelleEnergieDoppelzaehlung:
      type: object
      description: >
        Die Warnung vor doppelter Zählung - NEBEN den Blöcken, sie ändert keine Zahl. Beide Listen leer =
        kein Posten ist in einem anderen enthalten.
      required: [enthalten, nicht_pruefbar]
      properties:
        enthalten: { type: array, items: { $ref: "#/components/schemas/KostenstelleEnergieEnthalten" } }
        nicht_pruefbar: { type: array, items: { $ref: "#/components/schemas/KostenstelleEnergieNichtPruefbar" } }
    KostenstelleEnergieEnthalten:
      type: object
      required: [teil, summe, umfang, kette, zeitraeume, satz]
      properties:
        teil: { type: string, example: MS-06 }
        summe: { type: string, example: MS-20 }
        umfang: { type: string, enum: [ganz, teilweise], description: "Nie eine Menge: ganz, wenn Anteil der Summe × Beitrag des Terms ≥ 100 %, sonst zum Teil." }
        kette: { type: array, items: { type: string }, example: [MS-20, MS-06], description: "Der Weg durch die Formeln, die Summe zuerst." }
        zeitraeume: { type: array, items: { $ref: "#/components/schemas/KostenstelleEnergieZeitraum" } }
        satz: { type: string, example: MS-06 ist bereits in MS-20 enthalten }
    KostenstelleEnergieZeitraum:
      type: object
      required: [von, bis]
      properties:
        von: { type: string, format: date }
        bis: { type: string, format: date, description: "Der letzte Tag, einschließlich." }
    KostenstelleEnergieNichtPruefbar:
      type: object
      required: [messstelle, grund, kette, satz]
      properties:
        messstelle: { type: string, example: MS-20 }
        grund: { type: string, enum: [formel_kreis, haengt_an_kreis] }
        kette: { type: array, items: { type: string }, example: [MS-20, MS-23, MS-20] }
        satz: { type: string }
    VerteilungFehler:
      type: object
      description: >
        Die Ablehnung der Verteilungs-Schnittstelle (AP-10 IP-8): 400 `anfrage_ungueltig` (`feld`);
        404 `nicht_gefunden`; 409 `unternehmen_nicht_angelegt`, `messstelle_archiviert`
        (`kennzeichen`), `zuordnung_ueberlappt`; 422 `kostenstelle_unbekannt` (`kostenstelle_id`),
        `anteil_ungueltig` (`kostenstelle_id`, `kennzeichen`, `anteil_prozent`),
        `ziel_besteht_nicht` (`kostenstelle_id`, `kennzeichen`, `tag`), `verteilung_summe`
        (`summe`, `tag`), `formel_fassung_ueberlappt` (`gueltig_ab`, `laufend_ab`).
      required: [code, message]
      properties:
        code:
          type: string
          enum: [anfrage_ungueltig, nicht_gefunden, unternehmen_nicht_angelegt, messstelle_archiviert, kostenstelle_unbekannt, anteil_ungueltig, ziel_besteht_nicht, verteilung_summe, formel_fassung_ueberlappt, zuordnung_ueberlappt]
        message: { type: string }
        feld: { type: string }
        summe: { type: string }
        tag: { type: string, format: date }
    MessstelleVerteilungSetzen:
      type: object
      additionalProperties: false
      required: [gueltig_ab, zeilen]
      properties:
        gueltig_ab: { type: string, format: date }
        zeilen:
          type: array
          description: "Alle Ziele des Tages; leer = ab dem Tag nicht verteilt. Jede Kostenstelle höchstens einmal."
          items: { $ref: "#/components/schemas/MessstelleVerteilungZeile" }
        korrektur: { type: boolean, nullable: true, description: "true = ersetzt die Verteilung, die genau an diesem Tag beginnt." }
        grund: { type: string, nullable: true }
    MessstelleVerteilungZeile:
      type: object
      additionalProperties: false
      required: [kostenstelle_id, anteil_prozent]
      properties:
        kostenstelle_id: { type: string, format: uuid }
        anteil_prozent: { type: string, description: "Dezimaltext in Prozent: (0, 100], höchstens eine Nachkommastelle (sonst 422 anteil_ungueltig)." }
    VerteilungKostenstelle:
      type: object
      required: [id, kennzeichen]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
    MessstelleVerteilungAnteil:
      type: object
      required: [id, kostenstelle, name, anteil_prozent, gueltig_ab, gueltig_bis, endet_mit_kostenstelle]
      properties:
        id: { type: string, format: uuid }
        kostenstelle: { $ref: "#/components/schemas/VerteilungKostenstelle" }
        name: { type: string, description: Name der Kostenstelle. }
        anteil_prozent: { type: string, description: "Dezimaltext in Prozent." }
        gueltig_ab: { type: string, format: date }
        gueltig_bis: { type: string, format: date, nullable: true }
        endet_mit_kostenstelle: { type: boolean, description: "Der letzte Tag ist der der Kostenstelle - der Anteil gilt nie länger als sie." }
    MessstelleVerteilung:
      type: object
      required: [messstelle_id, kennzeichen, am, zustand, anteile]
      properties:
        messstelle_id: { type: string, format: uuid }
        kennzeichen: { type: string }
        am: { type: string, format: date, nullable: true }
        zustand: { type: string, enum: [verteilt, nicht verteilt], nullable: true, description: "Nur mit `am` - nie „zu 0 % verteilt“." }
        anteile: { type: array, items: { $ref: "#/components/schemas/MessstelleVerteilungAnteil" } }
        doppelzaehlung:
          type: array
          description: >
            Nur in der Antwort auf PUT (beim Lesen fehlt das Feld): je Kostenstelle mit Befund ein
            Eintrag, leer = nichts zählt doppelt. Ein Hinweis, keine Ablehnung - keine Zahl ändert sich.
          items: { $ref: "#/components/schemas/MessstelleVerteilungDoppelzaehlung" }
    MessstelleVerteilungDoppelzaehlung:
      type: object
      description: >
        Die Warnung vor doppelter Zählung an einer Ziel-Kostenstelle am Tag `am` (= `gueltig_ab`):
        die Einträge von `KostenstelleEnergieDoppelzaehlung`, in denen die gesetzte Messstelle Teil
        oder Summe ist.
      required: [kostenstelle, am, enthalten, nicht_pruefbar]
      properties:
        kostenstelle: { $ref: "#/components/schemas/VerteilungKostenstelle" }
        am: { type: string, format: date }
        enthalten: { type: array, items: { $ref: "#/components/schemas/KostenstelleEnergieEnthalten" } }
        nicht_pruefbar: { type: array, items: { $ref: "#/components/schemas/KostenstelleEnergieNichtPruefbar" } }
    BezugsgroesseFehler:
      type: object
      description: >
        Die Ablehnung der Bezugsgrößen-Schnittstelle - der geschlossene Satz
        `verwalten.ablehnungen` des Bezugsdaten-Vertrags: 400 `anfrage_ungueltig`
        (`feld`), `wort_unbekannt` (`feld`, `erlaubt`), `einheit_unbekannt`,
        `kennzeichen_format`, `zeitraum_ungueltig`, `wert_ungueltig` (`feld`); 404
        `nicht_gefunden`; 409 `kennzeichen_belegt`, `archiviert`, `hat_werte` (`werte`),
        `konflikt_anderer_wert` (`wirksamer_betrag`), `kein_wert`, `vorschlag_offen`; 422
        `periode_passt_nicht_zur_wertart`, `flaeche_aus_struktur`, `kein_stammdatum`
        (`wertart`), `kein_periodenwert` (`wertart`), `periode_passt_nicht`, `periode_nicht_zu_ende`,
        `zahl_unlesbar` (`feld`, bei ganzen Zahlen `hinweis`), `wert_negativ`, `begruendung_zu_kurz`,
        `begruendung_zu_lang` (`zeichen`), `geltung_nicht_waehlbar`, `geltung_unbekannt`,
        `bedeutung_fest` (`felder`).
      required: [code, message]
      properties:
        code:
          type: string
          enum: [anfrage_ungueltig, wort_unbekannt, einheit_unbekannt, kennzeichen_format, zeitraum_ungueltig, wert_ungueltig, periode_passt_nicht_zur_wertart, flaeche_aus_struktur, kein_stammdatum, kein_periodenwert, periode_passt_nicht, periode_nicht_zu_ende, zahl_unlesbar, wert_negativ, begruendung_zu_kurz, begruendung_zu_lang, geltung_nicht_waehlbar, geltung_unbekannt, bedeutung_fest, kennzeichen_belegt, archiviert, hat_werte, konflikt_anderer_wert, kein_wert, vorschlag_offen, nicht_gefunden]
        message: { type: string }
        feld: { type: string }
        erlaubt: { type: array, items: { type: string } }
        felder: { type: array, items: { type: string, enum: [art, wertart, einheit, periode_art, geltung_art, geltung_id] } }
        werte: { type: integer }
        wertart: { type: string, description: "`kein_stammdatum`, `kein_periodenwert`: die Wertart der Bezugsgröße." }
        hinweis: { type: string, description: "`zahl_unlesbar` bei Stück, Personen und Schichten: „Stück sind ganze Zahlen.“ (U5)." }
        wirksamer_betrag: { type: string, description: "`konflikt_anderer_wert`: der wirksame Betrag des Werts (F5)." }
        zeichen: { type: integer, description: "`begruendung_zu_kurz`, `begruendung_zu_lang`: wie viele Zeichen die Begründung hat." }
    KennzahlFehler:
      type: object
      description: >
        Die Ablehnung der Kennzahl-Schnittstelle - der geschlossene Satz `schnittstelle.ablehnungen`
        des Kennzahl-Vertrags: 400 `anfrage_ungueltig` (`feld`), `kennzeichen_format` (`feld`); 403
        `recht_fehlt` (`rolle_noetig`); 404 `nicht_gefunden`; 409 `kennzeichen_belegt` (`feld`),
        `archiviert`, `hat_werte` (`werte`), `wird_gelesen` (`leser`); 422 `periode_passt_nicht`
        (`grundperiode`, `perioden`), `einheit_unpassend`, `groesse_unbekannt` (`eingang`),
        `eingang_ausserhalb_geltung` (`eingang`), `formel_zyklus` (`kette`), `fassung_ueberlappt`
        (`fassung`, `gueltig_ab`), `geltung_unbekannt` (`feld`), `eingang_unbekannt` (`eingang`),
        `rechenform_unbekannt` (`feld`).
      required: [code, message]
      properties:
        code:
          type: string
          enum: [anfrage_ungueltig, kennzeichen_format, recht_fehlt, nicht_gefunden, kennzeichen_belegt, archiviert, hat_werte, wird_gelesen, periode_passt_nicht, einheit_unpassend, groesse_unbekannt, eingang_ausserhalb_geltung, formel_zyklus, fassung_ueberlappt, geltung_unbekannt, eingang_unbekannt, rechenform_unbekannt]
        message: { type: string }
        feld: { type: string }
        rolle_noetig: { type: string, nullable: true, description: "`recht_fehlt`: die kleinste Rolle, die es dürfte (Rechte-Ableitung)." }
        werte: { type: integer }
        leser: { type: array, items: { type: string }, description: "`wird_gelesen`: die Kennzeichen der lesenden Kennzahlen." }
        grundperiode: { type: string, nullable: true, enum: [tag, woche, monat, jahr] }
        perioden: { type: array, items: { type: string, enum: [tag, woche, monat, jahr] } }
        eingang: { type: string, description: "Kennzeichen des Eingangs." }
        kette: { type: array, items: { type: string }, description: "`formel_zyklus`: KZ-0012 → KZ-0011 → KZ-0012." }
        fassung: { type: integer, description: "`fassung_ueberlappt`: die jüngste Fassung." }
        gueltig_ab: { type: string, format: date, nullable: true }
    KennzahlEingang:
      type: object
      additionalProperties: false
      required: [rolle, art, kennzeichen]
      properties:
        rolle: { type: string, enum: [zaehler, nenner, paar] }
        art: { type: string, enum: [messstelle, bezugsgroesse, bezugsflaeche, kennzahl] }
        kennzeichen:
          type: string
          description: >
            Das Kennzeichen des Objekts (MS-12, BZ-6, KZ-0001) — bei `bezugsflaeche` das KURZZEICHEN des
            Standorts, Gebäudes oder Bereichs (G-2), an dem die Fläche steht. Die Fläche wird aus der
            Ortsstruktur gelesen; gespeichert wird der Eingang als `bezugsgroesse` (Stammdatum, m²).
    KennzahlAnfrage:
      type: object
      additionalProperties: false
      description: >
        Der Körper von POST und der Vorschau. Quotient und Anteil: genau ein `zaehler` und ein
        `nenner`; Zusammenfassung: nur `paar`e aus Kennzahlen. Ohne `kennzeichen` vergibt der Server
        KZ-0001 …, ohne `verantwortlich_name` ist der Aufrufer verantwortlich. Einen Faktor nimmt die
        Schnittstelle nicht an - die Regel rechnet keinen.
      required: [name, rechenform, geltung_art, geltung_id, eingaenge]
      properties:
        kennzeichen: { type: string, nullable: true, pattern: "^[A-Z0-9./-]{2,16}$" }
        name: { type: string }
        rechenform: { type: string, description: "quotient · anteil · zusammenfassung; ein anderes Wort ist 422 `rechenform_unbekannt`." }
        geltung_art: { type: string, enum: [unternehmen, standort, gebaeude, bereich, prozess, kostenstelle, messstelle] }
        geltung_id: { type: string, format: uuid }
        verantwortlich_name: { type: string, nullable: true }
        zweck: { type: string, nullable: true, maxLength: 500 }
        periode_art: { type: string, nullable: true, enum: [tag, woche, monat, jahr], description: "Gewünschte Periode - geprüft, nicht gespeichert." }
        komplement: { type: boolean, nullable: true, description: "Nur beim Anteil: 100 − Teil ÷ Ganzes × 100." }
        eingaenge: { type: array, items: { $ref: "#/components/schemas/KennzahlEingang" } }
    KennzahlStammdatenAnfrage:
      type: object
      additionalProperties: false
      required: [kennzeichen, name, verantwortlich_name]
      properties:
        kennzeichen: { type: string, pattern: "^[A-Z0-9./-]{2,16}$" }
        name: { type: string }
        verantwortlich_name: { type: string }
        zweck: { type: string, nullable: true, maxLength: 500 }
    KennzahlFassungAnfrage:
      type: object
      additionalProperties: false
      required: [gueltig_ab, begruendung, eingaenge]
      properties:
        gueltig_ab: { type: string, format: date }
        begruendung: { type: string, maxLength: 500 }
        periode_art: { type: string, nullable: true, enum: [tag, woche, monat, jahr] }
        komplement: { type: boolean, nullable: true }
        eingaenge: { type: array, items: { $ref: "#/components/schemas/KennzahlEingang" } }
    KennzahlPerson:
      type: object
      required: [name, rolle, art]
      properties:
        name: { type: string }
        rolle: { type: string, nullable: true }
        art: { type: string, enum: [kunde, unterstuetzung, voltpilot, notfall] }
    KennzahlEingangAntwort:
      type: object
      required: [rolle, art, id, kennzeichen, name]
      properties:
        rolle: { type: string, enum: [zaehler, nenner, paar] }
        art: { type: string, enum: [messstelle, bezugsgroesse, kennzahl] }
        id: { type: string, format: uuid }
        kennzeichen: { type: string, description: "Das HEUTIGE Kennzeichen des Objekts." }
        name: { type: string, nullable: true }
    KennzahlFassung:
      type: object
      required: [nummer, gueltig_ab, gueltig_bis, aufgehoben_am, herkunft, rueckwirkend, abzeichen, begruendung, eingetragen_von, eingetragen_am, rechenform, einheit, einheit_anzeige, komplement, eingaenge]
      properties:
        nummer: { type: integer, minimum: 1 }
        gueltig_ab: { type: string, format: date, nullable: true, description: "null = gilt seit Beginn." }
        gueltig_bis: { type: string, format: date, nullable: true, description: "Der LETZTE gültige Tag, einschließlich." }
        aufgehoben_am: { type: string, format: date-time, nullable: true }
        herkunft: { type: string, enum: [anlage, eintrag, kopie] }
        rueckwirkend: { type: boolean }
        abzeichen: { type: string, nullable: true, description: "„rückwirkend (n Tage)“ - nur bei einer rückwirkenden Fassung." }
        begruendung: { type: string, nullable: true }
        eingetragen_von: { $ref: "#/components/schemas/KennzahlPerson" }
        eingetragen_am: { type: string, format: date-time }
        rechenform: { type: string, enum: [quotient, anteil, zusammenfassung] }
        einheit: { type: string, description: "Ungekürzt aus den Eingängen (U1): kWh/Stück, %." }
        einheit_anzeige: { type: string, description: "„kWh je Stück“." }
        komplement: { type: boolean }
        eingaenge: { type: array, items: { $ref: "#/components/schemas/KennzahlEingangAntwort" } }
    Kennzahl:
      type: object
      required: [id, kennzeichen, name, rechenform, geltung_art, geltung_id, geltung_name, rechte_geltung, standort_id, kennung, verantwortlich_name, zweck, fassung, einheit, einheit_anzeige, grundperiode, perioden, hat_werte, archiviert_am, angelegt_am, bezugsbasis]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        name: { type: string }
        rechenform: { type: string, enum: [quotient, anteil, zusammenfassung] }
        geltung_art: { type: string, enum: [unternehmen, standort, gebaeude, bereich, prozess, kostenstelle, messstelle] }
        geltung_id: { type: string, format: uuid }
        geltung_name: { type: string, nullable: true }
        rechte_geltung: { type: string, enum: [standort, unternehmen], description: "G1." }
        standort_id: { type: string, format: uuid, nullable: true, description: "Der Standort des Geltungsobjekts heute; null beim Unternehmen." }
        kennung: { type: string, enum: [kennzahl.standort_definieren, kennzahl.unternehmen_definieren], description: "Das Recht zum Definieren." }
        verantwortlich_name: { type: string }
        zweck: { type: string, nullable: true }
        fassung: { type: integer, nullable: true, description: "Die Nummer der heute geltenden Fassung." }
        einheit: { type: string, nullable: true }
        einheit_anzeige: { type: string, nullable: true }
        grundperiode: { type: string, nullable: true, enum: [tag, woche, monat, jahr] }
        perioden: { type: array, items: { type: string, enum: [tag, woche, monat, jahr] }, description: "Die bildbaren Perioden (P2)." }
        hat_werte: { type: boolean, description: "Trägt sie einen Wert? Dann ist sie nicht löschbar (V5)." }
        archiviert_am: { type: string, format: date-time, nullable: true }
        angelegt_am: { type: string, format: date-time }
        bezugsbasis: { allOf: [{ $ref: "#/components/schemas/KennzahlBezugsbasis" }], nullable: true, description: "Die laufende Bezugsbasis (AP-17 IP-8, B3) oder null." }
    KennzahlBezugsbasis:
      type: object
      description: >
        Die laufende Bezugsbasis am Register-Eintrag: `fassung` ist die laufende freigegebene Fassung, sonst die jüngste
        (`null` ohne Fassung). Aus `freigabe_status = freigegeben` leitet der Leser das Wort „Energieleistungskennzahl“ ab
        (B3) — es steht an der Kennzahl, nicht in ihr.
      required: [kennzeichen, fassung, freigabe_status, vorlaeufig]
      properties:
        kennzeichen: { type: string, description: "BB-0001 …" }
        fassung: { type: integer, nullable: true }
        freigabe_status: { type: string, enum: [entwurf, beantragt, freigegeben, abgelehnt], nullable: true }
        vorlaeufig: { type: boolean, description: "Datenlage der Fassung vorläufig (P2/P3)." }
    BezugsbasisAnlegen:
      type: object
      additionalProperties: false
      properties:
        zweck: { type: string, nullable: true }
    BezugsbasisEntwurf:
      type: object
      additionalProperties: false
      required: [referenzperiode, methode]
      properties:
        referenzperiode: { type: string, pattern: "^[0-9]{4}-(0[1-9]|1[0-2])/[0-9]{4}-(0[1-9]|1[0-2])$" }
        methode: { type: string, enum: [verhaeltnis, regression_eine_variable, regression_zwei_variablen, gradtage] }
        variablen: { type: array, maxItems: 2, items: { type: string, format: uuid }, nullable: true, description: "Variable 1 ist der Nenner (V2, darf fehlen); nur `regression_zwei_variablen` nimmt genau eine weitere Bezugsgröße mit Periodenwerten." }
        toleranz_prozent: { type: string, nullable: true, description: "Dezimaltext, Startwert 2." }
        wiedervorlage_monate: { type: integer, nullable: true, description: "Startwert 12." }
        anpassungsgruende: { type: array, nullable: true, items: { type: string, enum: [referenzperiode_vervollstaendigt, grundlage_korrigiert, struktur_geaendert, variable_geaendert, methode_geaendert, nicht_mehr_anwendbar, sonstiger] }, description: "Ab Fassung 2 Pflicht (A1, F4); Fassung 1 nennt keinen." }
        anpassung_wortlaut: { type: string, nullable: true, description: "Nur und immer mit `sonstiger`." }
        begruendung: { type: string, nullable: true, minLength: 10, maxLength: 500, description: "Ab Fassung 2 Pflicht; sonst beim Beantragen oder Freigeben." }
        gilt_ab: { type: string, format: date, nullable: true, description: "Vorgabe der Tag nach der Referenzperiode (P4); nie früher, nie vor der Vorgängerin." }
        faktoren: { type: array, nullable: true, items: { $ref: "#/components/schemas/BezugsbasisFaktorWahl" }, description: "Statische Faktoren (V3, IP-16b, bezugsbasis.md §17); Verweise nur aus dem Faktoren-Vorschlag am Bildungstag." }
    BezugsbasisFaktorWahl:
      type: object
      additionalProperties: false
      required: [art]
      properties:
        art: { type: string, enum: [flaeche, standort, anlage, prozess, kostenstelle, wortlaut] }
        objekt_id: { type: string, format: uuid, nullable: true, description: "Beim Verweis Pflicht: die objekt_id des Vorschlags (§14)." }
        wortlaut: { type: string, nullable: true, maxLength: 500, description: "Nur bei art wortlaut: ohne Wert, ohne Anstoß." }
    BezugsbasisFaktor:
      type: object
      description: Ein statischer Faktor der Fassung - Kopie zum Stichtag (bezugsbasis.md §17).
      properties:
        position: { type: integer }
        art: { type: string, enum: [flaeche, standort, anlage, prozess, kostenstelle, wortlaut] }
        objekt_id: { type: string, format: uuid, nullable: true }
        kennung: { type: string, nullable: true }
        bezeichnung: { type: string, nullable: true }
        wortlaut: { type: string, nullable: true }
        wert: { type: string, nullable: true, description: "Dezimaltext; nur bei der Fläche." }
        einheit: { type: string, nullable: true }
        gueltig_ab: { type: string, format: date, nullable: true }
        stichtag: { type: string, format: date, description: "Tag der Kopie (im Entwurf der Bildungstag)." }
        ohne_anstoss: { type: boolean, description: "true genau beim Wortlaut." }
        satz: { type: string }
    BezugsbasisEntscheid:
      type: object
      additionalProperties: false
      properties:
        begruendung: { type: string, nullable: true, minLength: 10, maxLength: 500 }
    BezugsbasisVerantwortlicher:
      type: object
      additionalProperties: false
      required: [benutzer]
      properties:
        benutzer: { type: string, description: "Die Kennung (sub) eines aktiven Benutzers des Kundenbereichs." }
    BezugsbasisListe:
      type: object
      properties:
        bezugsbasen: { type: array, items: { $ref: "#/components/schemas/Bezugsbasis" } }
    BezugsbasisPerson:
      type: object
      properties:
        name: { type: string }
        rolle: { type: string, nullable: true }
        am: { type: string, format: date-time }
    Bezugsbasis:
      type: object
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, description: "BB-0001 …" }
        kennzahl_id: { type: string, format: uuid }
        kennzahl: { type: string }
        zweck: { type: string, nullable: true }
        verantwortlich_name: { type: string }
        beendet_zum: { type: string, format: date, nullable: true }
        beendet_grund: { type: string, nullable: true }
        angelegt_am: { type: string, format: date-time }
        fassungen: { type: array, items: { $ref: "#/components/schemas/BezugsbasisFassungKurz" } }
        anstoesse:
          type: array
          description: "Nachlese 3 (A2–A4): jeder Anstoß an einer Fassung der Basis, jüngster zuerst; ohne Anstoß []."
          items: { $ref: "#/components/schemas/BezugsbasisAnstoss" }
        frist:
          allOf: [{ $ref: "#/components/schemas/BezugsbasisFrist" }]
          nullable: true
          description: "F5 (§15): Frist der laufenden freigegebenen Fassung; null ohne sie und an einer beendeten Basis."
    BezugsbasisAnstoss:
      type: object
      description: Ein Anstoß an einer Fassung (bezugsbasis.md §15 „Anstöße lesen“).
      properties:
        art: { type: string, enum: [grundlage_korrigiert, struktur_geaendert, variable_geaendert, nicht_mehr_anwendbar] }
        pfad: { type: integer, enum: [1, 2], description: "1 = Kaskade (A2), 2 = Struktur-Läufer (A3)" }
        anlass_kennung: { type: string, description: "K-2026-0007 … bzw. ort_aenderung:<id> …" }
        anlass_satz: { type: string, description: "Kundensatz nach §10, ohne Code-Wörter" }
        zeitpunkt: { type: string, format: date-time }
        fassung: { type: integer }
        offen: { type: boolean }
        antwort:
          allOf: [{ $ref: "#/components/schemas/BezugsbasisAnstossAntwort" }]
          nullable: true
    BezugsbasisAnstossAntwort:
      type: object
      properties:
        art: { type: string, enum: [neue_fassung, beendet, bleibt] }
        person: { type: string }
        am: { type: string, format: date-time }
        begruendung: { type: string, nullable: true }
    BezugsbasisFrist:
      type: object
      description: Die Frist der laufenden Fassung über BezugsbasisRegeln.frist (§15).
      properties:
        ueberpruefung_faellig: { type: boolean }
        faellig_am: { type: string, format: date }
        faellig_seit_tagen: { type: integer, nullable: true }
        wiedervorlage_monate: { type: integer }
        bestaetigt_am: { type: string, format: date, nullable: true }
    BezugsbasisFassungKurz:
      type: object
      properties:
        fassung: { type: integer }
        referenzperiode: { type: string }
        methode: { type: string }
        datenlage: { type: string, enum: [vollstaendig, vorlaeufig] }
        freigabe_status: { type: string, enum: [entwurf, beantragt, freigegeben, abgelehnt] }
        basiswert: { type: string, nullable: true }
        gilt_ab: { type: string, format: date }
        gilt_bis: { type: string, format: date, nullable: true }
        pruefsumme: { type: string, nullable: true }
    BezugsbasisVariable:
      type: object
      properties:
        position: { type: integer }
        rolle: { type: string, enum: [nenner, variable] }
        bezugsgroesse_id: { type: string, format: uuid }
        kennzeichen: { type: string }
        fassung: { type: integer, nullable: true }
        spannweite_von: { type: string, nullable: true }
        spannweite_bis: { type: string, nullable: true }
    BezugsbasisFassung:
      type: object
      description: Vorschau und gespeicherte Kopie in einem (bezugsbasis.md §13).
      properties:
        bezugsbasis_id: { type: string, format: uuid }
        bezugsbasis: { type: string }
        kennzahl_id: { type: string, format: uuid }
        kennzahl: { type: string }
        fassung: { type: integer }
        referenzperiode: { type: string }
        methode: { type: string }
        gilt_ab: { type: string, format: date }
        monate: { type: integer }
        mindest_monate: { type: integer }
        datenlage: { type: string, enum: [vollstaendig, vorlaeufig] }
        datenlage_gruende: { type: array, items: { type: object, additionalProperties: true } }
        vorbehalte: { type: array, items: { type: string } }
        basiswert: { type: string, description: "Σ Zähler ÷ Σ Nenner, 4 Stellen (M1, M5)." }
        koeffizienten:
          type: object
          nullable: true
          description: "Modell (M2/M4): a, b und beim Modell mit zwei Einflussgrößen c — Dezimaltext, 4 Stellen; beim Verhältnis null."
          additionalProperties: { type: string }
        r2: { type: string, nullable: true, description: "Bestimmtheitsmaß, 3 Stellen (M2)." }
        streuung_prozent: { type: string, nullable: true, description: "Streuung der Residuen in % des Mittels, 1 Stelle (M2)." }
        abgelehnte_variablen:
          type: array
          description: "G4: eine zweite Variable mit |r| ≥ 0,9 — objekt, position, grund `variablen_abhaengig`, r, startwert_r."
          items: { type: object, additionalProperties: true }
        kennzeichen: { type: array, items: { type: string }, description: "Was jede Zahl aus der Fassung trägt, etwa „ohne Grundlast“ (M3)." }
        toleranz_prozent: { type: string }
        wiedervorlage_monate: { type: integer }
        variablen: { type: array, items: { $ref: "#/components/schemas/BezugsbasisVariable" } }
        faktoren: { type: array, items: { $ref: "#/components/schemas/BezugsbasisFaktor" } }
        freigabe_status: { type: string, enum: [entwurf, beantragt, freigegeben, abgelehnt] }
        gebildet_am: { type: string, format: date-time }
        gebildet_von: { type: string }
        gilt_bis: { type: string, format: date, nullable: true, description: "Letzter Tag; Fassung n + 1 beendet Fassung n am Vortag ihres gilt_ab (F4)." }
        anpassungsgruende: { type: array, items: { type: string } }
        anpassung_wortlaut: { type: string, nullable: true }
        begruendung: { type: string, nullable: true }
        vieraugen: { type: boolean }
        freigabe: { allOf: [{ $ref: "#/components/schemas/BezugsbasisPerson" }], nullable: true, description: "Wer freigegeben bzw. beantragt hat." }
        entscheidung: { allOf: [{ $ref: "#/components/schemas/BezugsbasisPerson" }], nullable: true, description: "Die zweite Person bei Vier-Augen." }
        entscheidungs_begruendung: { type: string, nullable: true }
        freigegeben_am: { type: string, format: date-time, nullable: true, description: "Beginn der Wiedervorlage (F5)." }
        grundlage: { type: object, additionalProperties: true, description: "Der gespeicherte kanonische Text, roh eingebettet (F3)." }
        pruefsumme: { type: string, description: "sha256: über die Grundlage." }
    FaktorenVorschlag:
      type: object
      description: Der Vorschlag statischer Faktoren (bezugsbasis.md §14) - nichts davon ist gespeichert.
      required: [kennzahl_id, kennzeichen, geltung_art, geltung_id, geltung_name, stichtag, faktoren, flaeche, hinweis]
      properties:
        kennzahl_id: { type: string, format: uuid }
        kennzeichen: { type: string, example: "KZ-0005" }
        geltung_art: { type: string, enum: [unternehmen, standort, gebaeude, bereich, prozess, kostenstelle, messstelle] }
        geltung_id: { type: string, format: uuid }
        geltung_name: { type: string, nullable: true }
        stichtag: { type: string, format: date }
        faktoren: { type: array, items: { $ref: "#/components/schemas/FaktorenVorschlagFaktor" }, description: "Fläche, Standorte, Anlagen, Prozesse, Kostenstellen - in dieser Reihenfolge." }
        flaeche: { $ref: "#/components/schemas/FaktorenVorschlagFlaeche" }
        hinweis: { type: string, description: "„Vorschlag aus der Struktur am … - nichts ist gespeichert. …“" }
    FaktorenVorschlagFaktor:
      type: object
      description: Ein Kandidat; `wert` und `einheit` nur bei der Fläche, sonst ein Verweis ohne Zahl.
      required: [art, objekt_id, kennung, bezeichnung, wert, einheit, gueltig_ab, gueltig_bis, satz]
      properties:
        art: { type: string, enum: [flaeche, standort, anlage, prozess, kostenstelle], description: "Vokabular `faktor_art` ohne `wortlaut` (den schreibt der Kunde)." }
        objekt_id: { type: string, format: uuid }
        kennung: { type: string, nullable: true, description: "G-2, ST-1, P-1, 4100 - eine Anlage hat keine Kennung (null)." }
        bezeichnung: { type: string }
        wert: { type: integer, nullable: true, minimum: 1, description: "Nur bei `flaeche`: m² am Stichtag." }
        einheit: { type: string, nullable: true, enum: ["m²", null] }
        gueltig_ab: { type: string, format: date }
        gueltig_bis: { type: string, format: date, nullable: true, description: "Einschließlich; null = offen." }
        satz: { type: string, example: "Fläche Halle 2 (G-2): 3 100 m² am 01.10.2026 · gültig 01.10.2026 bis 31.12.2026." }
    FaktorenVorschlagFlaeche:
      type: object
      description: Die Fläche der Geltung als Summe der Flächen-Kandidaten - null, sobald einem Ort der Geltung die Fläche fehlt.
      required: [wert, einheit, objekte, ohne_flaeche, gueltig_ab, gueltig_bis, satz]
      properties:
        wert: { type: integer, nullable: true }
        einheit: { type: string, enum: ["m²"] }
        objekte: { type: array, items: { type: string }, description: "Die Kennungen der summierten Orte." }
        ohne_flaeche: { type: array, items: { type: string }, description: "Orte der Geltung ohne Fläche am Stichtag." }
        gueltig_ab: { type: string, format: date, nullable: true, description: "Der Schnitt der Teil-Gültigkeiten." }
        gueltig_bis: { type: string, format: date, nullable: true }
        satz: { type: string }
    BezugsbasisZustand:
      type: object
      description: >
        Zustand einer Bezugsbasis (UEMS AP-17 IP-17): `zustand` aus `basis_zustand` - entwurf (keine
        freigegebene Fassung), freigegeben, ueberpruefung_faellig (Frist abgeleitet), beendet. Der Anstoß
        steht getrennt in `anstoss_liegt_vor`.
      required: [bezugsbasis_id, kennzeichen, kennzahl_id, zustand, anstoss_liegt_vor]
      properties:
        bezugsbasis_id: { type: string, format: uuid }
        kennzeichen: { type: string, example: BB-0001 }
        kennzahl_id: { type: string, format: uuid }
        kennzahl_kennzeichen: { type: string, example: KZ-0004 }
        kennzahl_name: { type: string }
        fassung: { type: integer, nullable: true, description: Nummer der laufenden freigegebenen Fassung. }
        freigegeben_am: { type: string, format: date, nullable: true }
        datenlage: { type: string, enum: [vollstaendig, vorlaeufig], nullable: true }
        zustand: { type: string, enum: [entwurf, freigegeben, ueberpruefung_faellig, beendet] }
        faellig_am: { type: string, format: date, nullable: true }
        faellig_seit_tagen: { type: integer, minimum: 0, nullable: true }
        anstoss_liegt_vor: { type: boolean }
        beendet_zum: { type: string, format: date, nullable: true }
        beendet_grund: { type: string, nullable: true }
    BezugsbasisUebersicht:
      type: object
      required: [stichtag, laufend, freigegeben, vorlaeufig, mit_anstoss, ueberpruefung_faellig, faellig]
      properties:
        stichtag: { type: string, format: date }
        laufend: { type: integer, minimum: 0, description: Laufende (nicht beendete) Bezugsbasen, auch ohne freigegebene Fassung. }
        freigegeben: { type: integer, minimum: 0 }
        vorlaeufig: { type: integer, minimum: 0 }
        mit_anstoss: { type: integer, minimum: 0 }
        ueberpruefung_faellig: { type: integer, minimum: 0 }
        faellig:
          type: array
          items: { $ref: "#/components/schemas/BezugsbasisZustand" }
    VerbesserungUebersicht:
      type: object
      description: >
        UEMS AP-18 IP-19: die Zähler aus R9 und die fälligen Vorgänge - am längsten fällig zuerst, dann nach
        Kennzeichen. Nichts davon ist gespeichert.
      required: [abruf, zaehler, faellig]
      properties:
        abruf: { type: string, format: date }
        zaehler: { $ref: "#/components/schemas/VerbesserungUebersichtZaehler" }
        faellig: { type: array, items: { $ref: "#/components/schemas/VerbesserungUebersichtZeile" } }
    VerbesserungUebersichtZaehler:
      type: object
      description: >
        `massnahmen_umgesetzt_ohne_bewertung` = Zustand `umgesetzt`; `energieziele_laufend` = Zustand `offen`;
        `messbedarfe_ueberfaellig` = offene Messbedarfe, deren Frist erreicht ist (W7).
      required: [auffaelligkeiten_offen, abweichungen_offen, abweichungen_ueberfaellig, massnahmen_geplant, massnahmen_ueberfaellig, massnahmen_umgesetzt_ohne_bewertung, energieziele_laufend, energieziele_bewertung_faellig, anstoesse_offen, messbedarfe_ueberfaellig]
      properties:
        auffaelligkeiten_offen: { type: integer, minimum: 0 }
        abweichungen_offen: { type: integer, minimum: 0 }
        abweichungen_ueberfaellig: { type: integer, minimum: 0 }
        massnahmen_geplant: { type: integer, minimum: 0 }
        massnahmen_ueberfaellig: { type: integer, minimum: 0 }
        massnahmen_umgesetzt_ohne_bewertung: { type: integer, minimum: 0 }
        energieziele_laufend: { type: integer, minimum: 0 }
        energieziele_bewertung_faellig: { type: integer, minimum: 0 }
        anstoesse_offen: { type: integer, minimum: 0 }
        messbedarfe_ueberfaellig: { type: integer, minimum: 0 }
    VerbesserungUebersichtZeile:
      type: object
      description: >
        Ein fälliger Vorgang. `satz` ist der Kundensatz „Überfällig“ (§5.9) bei Maßnahme und Abweichung, beim
        Energieziel null. Sprungziele: `id` (die Seite des Vorgangs), `kennzahl_id` (Messgrundlage bzw. Anker),
        `einsatz_id` (Maßnahme mit Energieeinsatz).
      required: [art, id, kennzeichen, titel, zustand, termin, faellig, seit_tagen, verantwortlich, satz, kennzahl_id, einsatz_id]
      properties:
        art: { type: string, enum: [massnahme, abweichung, energieziel] }
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        titel: { type: string }
        zustand: { type: string }
        termin: { type: string, format: date }
        faellig: { type: string, enum: [ueberfaellig, bewertung_faellig] }
        seit_tagen: { type: integer, minimum: 0 }
        verantwortlich: { type: string }
        satz: { type: string, nullable: true }
        kennzahl_id: { type: string, format: uuid, nullable: true }
        einsatz_id: { type: string, format: uuid, nullable: true }
    BezugsbasisMethoden:
      type: object
      description: Der Methoden-Katalog (bezugsbasis-methoden.schema.json), Byte für Byte die Vertragsdatei.
      required: [_comment, schema_version, $schema_datei, herkunft, methoden, parameter]
      properties:
        _comment: { type: array, items: { type: string }, description: "Entwickler-Doku; keine Kundenfläche." }
        schema_version: { type: string, description: "Die Fassung der Katalog-Form (1.0)." }
        $schema_datei: { type: string }
        herkunft: { type: string }
        methoden: { type: array, items: { $ref: "#/components/schemas/BezugsbasisMethode" } }
        parameter: { $ref: "#/components/schemas/BezugsbasisMethodenParameter" }
    BezugsbasisMethode:
      type: object
      description: Eine Methode der Bereinigung (bezugsbasis-methoden.schema.json, $defs/methode).
      required: [kennung, kundenwort, formel, variablen_anzahl, variablen, datenbedarf, mindest_perioden_startwert, belastbar_ab_monaten_startwert, grenze, kennzeichen]
      properties:
        kennung: { type: string, enum: [verhaeltnis, regression_eine_variable, regression_zwei_variablen, gradtage] }
        kundenwort: { type: string, description: "Etwa „Modell mit einer Einflussgröße“." }
        formel: { type: string }
        variablen_anzahl: { type: integer, minimum: 1, maximum: 2 }
        variablen: { type: string, description: "Welche Variablen und ihre Rolle." }
        datenbedarf: { type: string }
        mindest_perioden_startwert: { type: integer, description: "Ab wie vielen abgeschlossenen Monaten die Methode rechnet (G1)." }
        belastbar_ab_monaten_startwert: { type: integer, description: "Ab wie vielen Monaten die Bezugsbasis nicht mehr vorläufig ist." }
        grenze: { type: string }
        kennzeichen: { type: string, description: "Das Kennzeichen an jeder bereinigten Zahl (G5)." }
    BezugsbasisMethodenParameter:
      type: object
      description: Die Startwerte ohne Norm-Herleitung (G6).
      required: [mindest_monate_referenzperiode, toleranz_prozent_startwert, spannweite_prozent_startwert, abhaengigkeit_r_startwert, wiedervorlage_monate_startwert, grundperiode, hinweis]
      properties:
        mindest_monate_referenzperiode: { type: integer }
        toleranz_prozent_startwert: { type: number }
        spannweite_prozent_startwert: { type: number }
        abhaengigkeit_r_startwert: { type: number }
        wiedervorlage_monate_startwert: { type: integer }
        grundperiode: { type: string }
        hinweis: { type: string }
    KennzahlVorlagen:
      type: object
      required: [schema_version, vorlagen]
      properties:
        schema_version: { type: string, description: "Die Fassung der Katalog-Form (1.0)." }
        vorlagen: { type: array, items: { $ref: "#/components/schemas/KennzahlVorlage" } }
    KennzahlVorlage:
      type: object
      description: Eine Vorlage (kennzahl-vorlagen.schema.json, $defs/vorlage).
      required: [kennung, name_vorschlag, zweck_vorschlag, hilfesatz, rechenform, komplement, zaehler_erwartung, nenner_erwartung]
      properties:
        kennung: { type: string, description: "Stabil, etwa stromeinsatz_je_stueck." }
        name_vorschlag: { type: string, description: "„Stromeinsatz je Stück — {Geltungsbereich}“; der Platzhalter wird der Name des Geltungsobjekts." }
        zweck_vorschlag: { type: string }
        hilfesatz: { type: string, description: "Der Satz der Karte in Schritt 1 des Assistenten." }
        rechenform: { type: string, enum: [quotient, anteil] }
        komplement: { type: boolean, description: "Nur beim Anteil: 100 minus Teil durch Ganzes mal 100." }
        zaehler_erwartung:
          description: Menge (quotient) bzw. Teil (anteil).
          oneOf:
            - { $ref: "#/components/schemas/KennzahlVorlageMessstelle" }
            - { $ref: "#/components/schemas/KennzahlVorlageBezugsgroesse" }
        nenner_erwartung:
          description: Bezugsgröße (quotient) bzw. Ganzes (anteil).
          oneOf:
            - { $ref: "#/components/schemas/KennzahlVorlageMessstelle" }
            - { $ref: "#/components/schemas/KennzahlVorlageBezugsgroesse" }
    KennzahlVorlageMessstelle:
      type: object
      required: [art, messstelle_arten, groesse, richtungen, wertarten, satz]
      properties:
        art: { type: string, enum: [messstelle] }
        messstelle_arten: { type: array, items: { type: string, enum: [gemessen, berechnet] }, description: "berechnet = Gesamtwert." }
        groesse: { type: string, description: "Eine Größe des Messstellen-Vertrags, etwa Wirkenergie." }
        richtungen: { type: array, nullable: true, items: { type: string }, description: "null = die Vorlage verlangt keine Richtung." }
        wertarten: { type: array, items: { type: string, enum: [Zählerstand, Intervallmenge] } }
        satz: { type: string }
    KennzahlVorlageBezugsgroesse:
      type: object
      required: [art, bezugsgroesse_arten, einheiten, wertarten, satz]
      properties:
        art: { type: string, enum: [bezugsgroesse] }
        bezugsgroesse_arten: { type: array, items: { type: string }, description: "Arten des Bezugsdaten-Vertrags (arten.je_art), etwa gutteile." }
        einheiten: { type: array, items: { type: string }, description: "Nie umgerechnet." }
        wertarten: { type: array, items: { type: string, enum: [periodenwert, stammdatum] } }
        satz: { type: string }
    KennzahlListe:
      type: object
      required: [kennzahlen]
      properties:
        kennzahlen: { type: array, items: { $ref: "#/components/schemas/Kennzahl" } }
        ausserhalb_zugriff:
          $ref: "#/components/schemas/KennzahlZugriffHinweis"
    KennzahlZugriffHinweis:
      type: object
      description: "Nur die Anzahl unsichtbarer standortübergreifender Kennzahlen; niemals Kennung, Name, Formel oder Wert. Fehlt bei voller Sicht (AP-03 IP-11, R-A1 und R-A6)."
      required: [anzahl, text]
      properties:
        anzahl: { type: integer, minimum: 1 }
        text: { type: string, example: "1 Kennzahl umfasst Standorte außerhalb Ihres Zugriffs" }
    KennzahlPaare:
      type: object
      required: [gruppen]
      properties:
        gruppen: { type: array, items: { $ref: "#/components/schemas/KennzahlPaarGruppe" } }
    KennzahlPaarGruppe:
      type: object
      required: [rechenform, einheit, einheit_anzeige, kennzahlen]
      properties:
        rechenform: { type: string, enum: [quotient, anteil, zusammenfassung] }
        einheit: { type: string, description: "Die Einheit der heute geltenden Fassung, ungekürzt (kWh/Stück, %)." }
        einheit_anzeige: { type: string, nullable: true, description: "Das Kundenwort (kWh je Stück)." }
        kennzahlen: { type: array, items: { $ref: "#/components/schemas/Kennzahl" } }
    KennzahlFassungen:
      type: object
      required: [kennzahl_id, kennzeichen, fassungen]
      properties:
        kennzahl_id: { type: string, format: uuid }
        kennzeichen: { type: string }
        fassungen: { type: array, items: { $ref: "#/components/schemas/KennzahlFassung" } }
    KennzahlBerechnung:
      type: object
      required: [kennzahl_id, kennzeichen, am, fassung]
      properties:
        kennzahl_id: { type: string, format: uuid }
        kennzeichen: { type: string }
        am: { type: string, format: date }
        fassung: { $ref: "#/components/schemas/KennzahlFassung" }
    KennzahlBefund:
      type: object
      required: [code, message, fakten]
      properties:
        code: { type: string, description: "Ein Code aus KennzahlFehler." }
        message: { type: string }
        fakten: { type: object, additionalProperties: true }
    KennzahlVorschauPeriode:
      type: object
      required: [periode_art, schluessel, beschriftung, von, bis, wert, zaehler, nenner, zustand, richtung, grund, abdeckung_prozent, fassung, kennzeichen, anzeige, kundensatz]
      properties:
        periode_art: { type: string, enum: [tag, woche, monat, jahr] }
        schluessel: { type: string, description: "2026-10 · 2026-10-31 · 2026." }
        beschriftung: { type: string, description: "„Oktober 2026“." }
        von: { type: string, format: date }
        bis: { type: string, format: date, description: "Der LETZTE Tag, einschließlich." }
        wert: { type: string, nullable: true, description: "Dezimaltext; null = keine Werte, nie 0." }
        zaehler: { type: string, nullable: true }
        nenner: { type: string, nullable: true }
        zustand: { type: string }
        richtung: { type: string, nullable: true, enum: [untergrenze, obergrenze, unbestimmt] }
        grund: { type: string, nullable: true }
        abdeckung_prozent: { type: string, nullable: true }
        fassung: { type: string, nullable: true, description: "vorläufig · endgültig." }
        kennzeichen: { type: array, items: { type: string } }
        anzeige: { type: string }
        kundensatz: { type: string, nullable: true }
    KennzahlVorschau:
      type: object
      required: [befunde, rechte_geltung, standort_id, kennung, einheit, einheit_anzeige, grundperiode, perioden, periode_art, letzte_perioden]
      properties:
        befunde: { type: array, items: { $ref: "#/components/schemas/KennzahlBefund" }, description: "Leer = das Anlegen würde gelingen." }
        rechte_geltung: { type: string, nullable: true, enum: [standort, unternehmen] }
        standort_id: { type: string, format: uuid, nullable: true }
        kennung: { type: string, nullable: true }
        einheit: { type: string, nullable: true }
        einheit_anzeige: { type: string, nullable: true }
        grundperiode: { type: string, nullable: true, enum: [tag, woche, monat, jahr] }
        perioden: { type: array, items: { type: string, enum: [tag, woche, monat, jahr] } }
        periode_art: { type: string, nullable: true, enum: [tag, woche, monat, jahr], description: "Die Periode der Vorschau - der Wunsch, sonst die Grundperiode." }
        letzte_perioden: { type: array, items: { $ref: "#/components/schemas/KennzahlVorschauPeriode" }, description: "Die letzten drei abgeschlossenen Perioden, die jüngste zuerst." }
    KennzahlWerteKennzahl:
      type: object
      description: Die Kennzahl im Kopf ihrer Werte - Einheit und Anzeige-Wort der HEUTE geltenden Berechnung.
      required: [id, kennzeichen, name, rechenform, einheit, einheit_anzeige]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        name: { type: string }
        rechenform: { type: string, enum: [quotient, anteil, zusammenfassung] }
        einheit: { type: string, nullable: true, description: "kWh/Stück · %." }
        einheit_anzeige: { type: string, nullable: true, description: "„kWh je Stück“." }
    KennzahlVariablenVorschlag:
      type: object
      description: Der Variablen-Vorschlag einer Kennzahl aus dem Energieeinsatz (AP-17 IP-11a, V4). Nur Lesen.
      required: [kennzahl, geltung_art, bezug, referenzperiode, einsaetze, variable_1, kandidaten, ohne_zahl, satz]
      properties:
        kennzahl: { $ref: "#/components/schemas/KennzahlWerteKennzahl" }
        geltung_art: { type: string, enum: [unternehmen, standort, gebaeude, bereich, prozess, kostenstelle, messstelle] }
        bezug: { type: string, enum: [prozess, zaehler_messstellen, keiner], description: "Woher die Einsätze kommen: Prozess der Geltung, sonst Prozesse der Zähler-Messstellen." }
        referenzperiode: { type: string, example: "2026-11/2027-10", description: "JJJJ-MM/JJJJ-MM, beide Monate eingeschlossen." }
        einsaetze: { type: array, items: { $ref: "#/components/schemas/KennzahlVariablenEinsatz" } }
        variable_1:
          nullable: true
          description: Der Nenner der heute geltenden Fassung, wenn er eine Bezugsgröße ist - sonst null.
          allOf: [{ $ref: "#/components/schemas/KennzahlVariable" }]
        kandidaten: { type: array, items: { $ref: "#/components/schemas/KennzahlVariablenKandidat" } }
        ohne_zahl: { type: array, items: { $ref: "#/components/schemas/KennzahlVariablenOhneZahl" } }
        satz: { type: string, nullable: true, description: "Nur bei `bezug` keiner: warum es nichts vorzuschlagen gibt." }
    KennzahlVariablenEinsatz:
      type: object
      required: [id, kennzeichen, name, traeger]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, example: "EE-1" }
        name: { type: string }
        traeger: { type: string }
    KennzahlVariable:
      type: object
      description: Eine Bezugsgröße im Vorschlag - Art, Einheit und ob Periodenwerte oder ein Kanal vorhanden sind.
      required: [id, kennzeichen, name, art, wertart, einheit, periode_art, hat_werte, hat_kanal]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, example: "BZ-1" }
        name: { type: string }
        art: { type: string, nullable: true, description: "Art der Bezugsgröße (produktionsmenge · betriebszeit · gradtagzahl · …)." }
        wertart: { type: string, enum: [periodenwert, stammdatum] }
        einheit: { type: string }
        periode_art: { type: string, nullable: true }
        hat_werte: { type: boolean }
        hat_kanal: { type: boolean }
    KennzahlVariablenKandidat:
      type: object
      required: [bezugsgroesse, einfluss_art, vorschlag, einsaetze, abhaengigkeit, satz]
      properties:
        bezugsgroesse: { $ref: "#/components/schemas/KennzahlVariable" }
        einfluss_art: { type: string, enum: [produktion, betriebszeit, wetter, sonstige], description: "Die Art der Einflussgröße am Einsatz." }
        vorschlag: { type: string, enum: [variable_1, variable, statischer_faktor] }
        einsaetze: { type: array, items: { type: string }, description: "Kennzeichen der Einsätze, die diese Bezugsgröße nennen." }
        abhaengigkeit:
          nullable: true
          description: Nur bei `variable` - G4 gegen Variable 1, ein Hinweis, keine Ablehnung.
          allOf: [{ $ref: "#/components/schemas/KennzahlVariablenAbhaengigkeit" }]
        satz: { type: string, nullable: true, description: "Kundensatz bei `variablen_abhaengig` oder `statischer_faktor`." }
    KennzahlVariablenAbhaengigkeit:
      type: object
      required: [ergebnis, r, paare, grund, gegen, schwelle]
      properties:
        ergebnis: { type: string, enum: [unabhaengig, variablen_abhaengig, nicht_pruefbar] }
        r: { type: number, nullable: true, description: "Pearson-r, ungerundet; null bei nicht_pruefbar." }
        paare: { type: integer, minimum: 0, description: "Monate, in denen beide einen Wert haben." }
        grund: { type: string, nullable: true, enum: [zu_wenig_paare, keine_streuung, keine_variable_1, null] }
        gegen: { type: string, nullable: true, description: "Kennzeichen von Variable 1." }
        schwelle: { type: number, example: 0.9, description: "Startwert (G6)." }
    KennzahlVariablenOhneZahl:
      type: object
      required: [wortlaut, einfluss_art, einsatz, satz]
      properties:
        wortlaut: { type: string, example: "Leckagerate" }
        einfluss_art: { type: string, enum: [produktion, betriebszeit, wetter, sonstige] }
        einsatz: { type: string, example: "EE-3" }
        satz: { type: string, enum: ["ohne Zahl — erst als Bezugsgröße erfassen"] }
    BezugsbasisVergleich:
      type: object
      description: Der Vergleich einer Kennzahl mit ihrer Bezugsbasis (AP-17 IP-19, U1–U6). Nur Lesen; Zahlen als Dezimaltext, Prozent eine Stelle (M5).
      required: [kennzahl, bezugsbasis, von, bis, zeitzone, monate, zeitraum, staende, stand_satz, satz]
      properties:
        kennzahl: { $ref: "#/components/schemas/KennzahlWerteKennzahl" }
        bezugsbasis:
          nullable: true
          description: Die gelesene Bezugsbasis - null, wenn die Kennzahl keine hat (jeder Monat `basis_fehlt`).
          allOf: [{ $ref: "#/components/schemas/BezugsbasisVergleichBasis" }]
        von: { type: string, example: "2027-11" }
        bis: { type: string, example: "2028-02" }
        zeitzone: { type: string, example: Europe/Berlin }
        monate: { type: array, items: { $ref: "#/components/schemas/BezugsbasisVergleichMonat" } }
        zeitraum: { $ref: "#/components/schemas/BezugsbasisVergleichZeitraum" }
        staende: { type: array, items: { $ref: "#/components/schemas/BezugsbasisVergleichStand" }, description: "Leistungsvergleichs-Stände (S5, AP-17 IP-21b): die freigegebenen Stände, die diese Bezugsbasis zitieren (Quellenart `bezugsbasis`), der jüngste vorn; leer ohne Stand." }
        stand_satz: { type: string, example: "ungesichert — noch kein Stand", description: "„Stand Nr. n vom TT.MM.JJJJ“ (jüngster Stand) oder „ungesichert — noch kein Stand“." }
        satz: { type: string, nullable: true, description: "Nur ohne Bezugsbasis: der Leer-Satz (§10)." }
    BezugsbasisVergleichBasis:
      type: object
      required: [id, kennzeichen, beendet_zum, beendet_grund]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, example: BB-0001 }
        beendet_zum: { type: string, format: date, nullable: true }
        beendet_grund: { type: string, nullable: true }
    Energieziel:
      type: object
      description: >
        Ein Energieziel (UEMS AP-18 IP-6/IP-7, Z1/Z2/Z5); `anstoesse` und `verlauf` nur am einzelnen Ziel, sonst null.
        `frist` wird beim Abruf abgeleitet (Operation `frist`), `bewertung` ist null, solange niemand bewertet hat.
      required: [id, kennzeichen, kennzahl, bezugsbasis, zielwert_prozent, zielperiode, wortlaut, begruendung, verantwortlich, standort_id, zustand, angelegt_am, beendet_zum, beendet_grund, ergebnis, frist, bewertung, anstoesse, verlauf]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, pattern: "^EZ-[0-9]{4}-[0-9]{4,9}$" }
        kennzahl:
          type: object
          required: [id, kennzeichen, name]
          properties: { id: { type: string, format: uuid }, kennzeichen: { type: string }, name: { type: string } }
        bezugsbasis:
          type: object
          required: [id, kennzeichen, fassung]
          properties: { id: { type: string, format: uuid }, kennzeichen: { type: string }, fassung: { type: integer } }
        zielwert_prozent: { type: string, description: "Dezimaltext mit einer Stelle, weniger Energie negativ" }
        zielperiode: { type: string }
        wortlaut: { type: string }
        begruendung: { type: string }
        verantwortlich:
          type: object
          required: [sub, name]
          properties: { sub: { type: string }, name: { type: string } }
        standort_id: { type: string, format: uuid, nullable: true }
        zustand: { type: string, enum: [offen, bewertet, beendet] }
        angelegt_am: { type: string, format: date }
        beendet_zum: { type: string, format: date, nullable: true }
        beendet_grund: { type: string, nullable: true }
        ergebnis: { type: string, nullable: true, enum: [erreicht, verfehlt, nicht_bewertbar, null] }
        frist: { $ref: "#/components/schemas/EnergiezielFrist" }
        bewertung:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/EnergiezielBewertung" }]
        anstoesse:
          type: array
          nullable: true
          items: { $ref: "#/components/schemas/EnergiezielAnstoss" }
        verlauf:
          type: array
          nullable: true
          items: { $ref: "#/components/schemas/EnergiezielEintrag" }
    EnergiezielFrist:
      type: object
      description: >
        F1 (Operation `frist`): Termin = letzter Tag der Zielperiode; `faellig` = `bewertung_faellig` („Bewertung fällig
        seit n Tagen“, 0 am Termintag), solange das Ziel offen und der letzte Monat endgültig ist - sonst null.
      required: [termin, faellig, seit_tagen]
      properties:
        termin: { type: string, format: date }
        faellig: { type: string, nullable: true, enum: [bewertung_faellig, null] }
        seit_tagen: { type: integer, nullable: true, minimum: 0 }
    EnergiezielBewertung:
      type: object
      description: >
        Z5: die Bewertung - `kopie` ist der kanonische Text des Ziel-Stands zum Bewertungstag, `pruefsumme` sein
        sha256; `person`/`am` wer bewertet bzw. beantragt hat, bei Vier-Augen `entscheidung` die zweite Person.
      required: [status, ergebnis, begruendung, vorschlag, vieraugen, person, am, entscheidung, entschieden_am, entscheidungs_begruendung, kopie, pruefsumme]
      properties:
        status: { type: string, enum: [beantragt, bewertet, abgelehnt] }
        ergebnis: { type: string, enum: [erreicht, verfehlt, nicht_bewertbar] }
        begruendung: { type: string }
        vorschlag: { type: string, nullable: true, enum: [erreicht, nicht_erreicht, null] }
        vieraugen: { type: boolean }
        person:
          type: object
          required: [sub, name]
          properties: { sub: { type: string, nullable: true }, name: { type: string } }
        am: { type: string, format: date-time }
        entscheidung:
          type: object
          nullable: true
          required: [sub, name]
          properties: { sub: { type: string, nullable: true }, name: { type: string } }
        entschieden_am: { type: string, format: date-time, nullable: true }
        entscheidungs_begruendung: { type: string, nullable: true }
        kopie: { type: string }
        pruefsumme: { type: string, pattern: "^sha256:[0-9a-f]{64}$" }
    EnergiezielAnstoss:
      type: object
      description: >
        Ein Anstoß am Ziel (`vorgang_anstoss`, Z5) - Basis beendet oder neu gefasst (Pfad 2), die Bewertung zitiert
        einen Monat, der eine neue Version bekam (Pfad 1, IP-17); eine Person antwortet.
      required: [id, art, anlass_kennung, angestossen_am, zustand, antwort, antwort_begruendung, beantwortet_am, beantwortet_von]
      properties:
        id: { type: string, format: uuid }
        art: { type: string, enum: [ausgangslage_korrigiert, bewertung_korrigiert, messgrundlage_beendet, messgrundlage_neu_gefasst] }
        anlass_kennung: { type: string }
        angestossen_am: { type: string, format: date-time }
        zustand: { type: string, enum: [offen, beantwortet] }
        antwort: { type: string, nullable: true, enum: [bleibt, neu_kopiert, neu_bewertet, null] }
        antwort_begruendung: { type: string, nullable: true }
        beantwortet_am: { type: string, format: date-time, nullable: true }
        beantwortet_von: { type: string, nullable: true }
    EnergiezielEintrag:
      type: object
      description: Eine Zeile des Protokolls `energieziel_aenderung`.
      required: [art, alt, neu, begruendung, person, am]
      properties:
        art: { type: string, enum: [energieziel_angelegt, energieziel_geaendert, verantwortlicher_geaendert, bewertung_beantragt, bewertung_abgelehnt, energieziel_bewertet, energieziel_beendet, anstoss_gesetzt, anstoss_beantwortet] }
        alt: { type: object, nullable: true, additionalProperties: true }
        neu: { type: object, nullable: true, additionalProperties: true }
        begruendung: { type: string, nullable: true }
        person: { type: string }
        am: { type: string, format: date-time }
    EnergiezielListe:
      type: object
      required: [energieziele]
      properties:
        energieziele: { type: array, items: { $ref: "#/components/schemas/Energieziel" } }
    EnergiezielStand:
      type: object
      description: Der Ziel-Stand (Z3, Z4) - Ausgang der Operation `zielstand` und je Monat die Vergleichszeile.
      required: [energieziel, abruf, zielperiode, zielwert_prozent, monate, monate_bewertbar, monate_endgueltig, monate_soll, monate_text, vollstaendig, nicht_gezaehlt, summe, vorschlag, satz, vorschlag_satz]
      properties:
        energieziel: { $ref: "#/components/schemas/Energieziel" }
        abruf: { type: string, format: date }
        zielperiode: { type: string }
        zielwert_prozent: { type: string }
        monate:
          type: array
          items:
            type: object
            required: [periode, endgueltig, vergleich]
            properties:
              periode: { type: string }
              endgueltig: { type: boolean }
              vergleich: { $ref: "#/components/schemas/BezugsbasisVergleichMonat" }
        monate_bewertbar: { type: integer }
        monate_endgueltig: { type: integer }
        monate_soll: { type: integer }
        monate_text: { type: string, description: "x von y" }
        vollstaendig: { type: boolean }
        nicht_gezaehlt:
          type: array
          items:
            type: object
            required: [monat, grund]
            properties:
              monat: { type: string }
              grund: { type: string, enum: [basis_fehlt, basis_beendet, zu_wenig_perioden, variable_fehlt, variable_ausserhalb, periode_nicht_zu_ende, keine_werte, unvollstaendig] }
        summe:
          type: object
          required: [gemessen, erwartet, delta_prozent, band_prozent, richtung, urteil, kennzeichen]
          properties:
            gemessen: { type: string, nullable: true }
            erwartet: { type: string, nullable: true }
            delta_prozent: { type: string, nullable: true }
            band_prozent: { type: string, nullable: true }
            richtung: { type: string, nullable: true }
            urteil: { type: string, enum: [besser, schlechter, im_rahmen, nicht_anwendbar, ohne_urteil] }
            kennzeichen: { type: array, items: { type: string } }
        vorschlag: { type: string, nullable: true, enum: [erreicht, nicht_erreicht, null] }
        satz: { type: string, nullable: true }
        vorschlag_satz: { type: string, nullable: true }
    Massnahme:
      type: object
      description: >
        Eine Maßnahme (UEMS AP-18 IP-10, M1–M4, M6); `messgrundlage` oder `ohne_messgrundlage` ist gesetzt, nie beide;
        `frist` über die Operation `frist` beim Abruf; `kopf_satz` erst ab umgesetzt; `verlauf` nur an der einzelnen.
      required: [id, kennzeichen, titel, verantwortlich, termin, standort_id, zustand, herkunft, messgrundlage, ohne_messgrundlage, einsatz, einstufung_fassung, energieziel, erwartete_wirkung_prozent, erwartete_wirkung_wortlaut, angelegt_am, umgesetzt_am, umgesetzt_begruendung, verworfen_am, verworfen_grund, frist, kopf_satz, bewertung, bewertung_antrag, anstoesse, verlauf]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, pattern: "^M-[0-9]{4}-[0-9]{4,9}$" }
        titel: { type: string }
        verantwortlich:
          type: object
          required: [sub, name]
          properties: { sub: { type: string }, name: { type: string } }
        termin: { type: string, format: date }
        standort_id: { type: string, format: uuid, nullable: true }
        zustand: { type: string, enum: [geplant, umgesetzt, bewertet, verworfen] }
        herkunft:
          type: object
          required: [art, kennung]
          properties:
            art:
              type: string
              enum: [abweichung, energieziel, einsatz, von_hand, nichtkonformitaet, audit, managementbewertung]
            kennung: { type: string, nullable: true }
        messgrundlage: { $ref: "#/components/schemas/MassnahmeMessgrundlage" }
        ohne_messgrundlage:
          type: object
          nullable: true
          required: [kennzeichen, hinweis, satz]
          properties:
            kennzeichen: { type: string, description: "„ohne Messgrundlage — Wirkung nicht messbar“" }
            hinweis: { type: string, description: "welche Kennzahl fehlt" }
            satz: { type: string, description: "Kundensatz `ohne_messgrundlage` (§5.9)" }
        einsatz: { $ref: "#/components/schemas/MassnahmeVerweis" }
        einstufung_fassung: { type: integer, nullable: true }
        energieziel: { $ref: "#/components/schemas/MassnahmeVerweis" }
        erwartete_wirkung_prozent: { type: string, nullable: true, description: "Dezimaltext, eine Stelle, weniger Energie negativ; nur mit Messgrundlage" }
        erwartete_wirkung_wortlaut: { type: string }
        angelegt_am: { type: string, format: date }
        umgesetzt_am: { type: string, format: date, nullable: true }
        umgesetzt_begruendung: { type: string, nullable: true }
        verworfen_am: { type: string, format: date-time, nullable: true }
        verworfen_grund: { type: string, nullable: true }
        frist:
          type: object
          required: [abruf, termin, faellig, seit_tagen, satz]
          properties:
            abruf: { type: string, format: date }
            termin: { type: string, format: date }
            faellig: { type: string, nullable: true, enum: [ueberfaellig, null] }
            seit_tagen: { type: integer, nullable: true }
            satz: { type: string, nullable: true, description: "Kundensatz `ueberfaellig` (§5.9)" }
        kopf_satz: { type: string, nullable: true, description: "Kundensatz `massnahme_kopf` (§5.9)" }
        bewertung: { $ref: "#/components/schemas/MassnahmeBewertung", description: "der jüngste bewertete Stand; null ohne Stand (die Fläche sagt „beobachtet — nicht belegt“)" }
        bewertung_antrag: { $ref: "#/components/schemas/MassnahmeBewertung", description: "der offene Antrag (Vier-Augen) oder null" }
        anstoesse:
          type: array
          nullable: true
          description: die Anstöße am Vorgang (M5, IP-17), älteste zuerst - wie `verlauf` nur an der einzelnen
          items: { $ref: "#/components/schemas/MassnahmeAnstoss" }
        verlauf:
          type: array
          nullable: true
          items: { $ref: "#/components/schemas/MassnahmeEintrag" }
    MassnahmeAnstoss:
      type: object
      description: >
        Ein Anstoß an der Maßnahme (`vorgang_anstoss`, M5, IP-17): die Ausgangslage oder ein Bewertungs-Stand zitiert
        einen Monat in einer älteren Version (Pfad 1, Kaskade), die Basis endete oder bekam eine Fassung mit
        Referenzperiode nach der Umsetzung (Pfad 2, Struktur-Läufer). Die Kopie bleibt byte-gleich; eine Person antwortet.
      required: [id, art, anlass_kennung, angestossen_am, zustand, antwort, antwort_begruendung, beantwortet_am, beantwortet_von]
      properties:
        id: { type: string, format: uuid }
        art: { type: string, enum: [ausgangslage_korrigiert, bewertung_korrigiert, messgrundlage_beendet, messgrundlage_neu_gefasst] }
        anlass_kennung: { type: string, description: "K-… (Pfad 1, wie der Bezugsbasis-Anstoß), BB-…/beendet, BB-…/Fassung-n" }
        angestossen_am: { type: string, format: date-time }
        zustand: { type: string, enum: [offen, beantwortet] }
        antwort: { type: string, nullable: true, enum: [bleibt, neu_kopiert, neu_bewertet, null] }
        antwort_begruendung: { type: string, nullable: true }
        beantwortet_am: { type: string, format: date-time, nullable: true }
        beantwortet_von: { type: string, nullable: true }
    MassnahmeBewertung:
      type: object
      nullable: true
      description: >
        Ein Stand Nr. n der Bewertung (IP-12, WK6) - nie zurückgenommen. `kopie` ist der kanonische Text der Wirkung
        zum Bewertungstag (ohne Messgrundlage null), `pruefsumme` sein sha256; `person`/`am` wer bewertet bzw.
        beantragt hat, bei Vier-Augen `entscheidung` die zweite Person; `satz` der Kundensatz `bewertung_belegt` bzw.
        `bewertung_nicht_messbar` (§5.9) an einem bewerteten Stand, sonst null.
      required: [stand_nr, status, ergebnis, begruendung, vieraugen, person, am, entscheidung, entschieden_am, entscheidungs_begruendung, kopie, pruefsumme, satz]
      properties:
        stand_nr: { type: integer, minimum: 1 }
        status: { type: string, enum: [beantragt, bewertet, abgelehnt] }
        ergebnis: { type: string, enum: [belegt, nicht_belegt, nicht_messbar] }
        begruendung: { type: string }
        vieraugen: { type: boolean }
        person:
          type: object
          required: [sub, name]
          properties: { sub: { type: string, nullable: true }, name: { type: string } }
        am: { type: string, format: date-time }
        entscheidung:
          type: object
          nullable: true
          required: [sub, name]
          properties: { sub: { type: string, nullable: true }, name: { type: string } }
        entschieden_am: { type: string, format: date-time, nullable: true }
        entscheidungs_begruendung: { type: string, nullable: true }
        kopie: { type: string, nullable: true }
        pruefsumme: { type: string, nullable: true, pattern: "^sha256:[0-9a-f]{64}$" }
        satz: { type: string, nullable: true }
    MassnahmeBewertungen:
      type: object
      required: [id, kennzeichen, zustand, bewertungen]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        zustand: { type: string, enum: [geplant, umgesetzt, bewertet, verworfen] }
        bewertungen: { type: array, items: { $ref: "#/components/schemas/MassnahmeBewertung" } }
    MassnahmeVerweis:
      type: object
      nullable: true
      required: [id, kennzeichen, name]
      properties: { id: { type: string, format: uuid }, kennzeichen: { type: string }, name: { type: string, nullable: true } }
    MassnahmeMessgrundlage:
      type: object
      nullable: true
      description: >
        M2/M3: Kennzahl × Bezugsbasis-Fassung × Ausgangslage. `ausgangslage` ist der gespeicherte kanonische Text
        (byte-gleich, Kopie des Vergleich-Lesers je Monat und des Zeitraums), `pruefsumme` `sha256:` darüber (wie
        `bericht_pruefsumme`); `ausgangslage_inhalt` derselbe Inhalt als Objekt; `bewertungsmethode` die Methode der
        Fassung in Kundenwörtern; `satz` der Kundensatz `messgrundlage` (§5.9) für einen Monat mit Zahl, sonst null.
      required: [kennzahl, bezugsbasis, fassung, bewertungsmethode, ausgangslage, pruefsumme, ausgangslage_inhalt, satz]
      properties:
        kennzahl: { $ref: "#/components/schemas/MassnahmeVerweis" }
        bezugsbasis: { $ref: "#/components/schemas/MassnahmeVerweis" }
        fassung: { type: integer }
        bewertungsmethode: { type: string }
        ausgangslage: { type: string }
        pruefsumme: { type: string, pattern: "^sha256:[0-9a-f]{64}$" }
        ausgangslage_inhalt: { type: object, additionalProperties: true }
        satz: { type: string, nullable: true }
    MassnahmeEintrag:
      type: object
      description: Eine Zeile des Protokolls `massnahme_aenderung` - Zustandswechsel, Änderung oder Kommentar.
      required: [nr, art, alt, neu, begruendung, kommentar, person, am]
      properties:
        nr: { type: integer, format: int64 }
        art: { type: string, enum: [massnahme_angelegt, massnahme_geaendert, verantwortlicher_geaendert, kommentar, massnahme_umgesetzt, massnahme_verworfen, bewertung_beantragt, bewertung_abgelehnt, massnahme_bewertet, anstoss_gesetzt, anstoss_beantwortet] }
        alt: { type: object, nullable: true, additionalProperties: true }
        neu: { type: object, nullable: true, additionalProperties: true }
        begruendung: { type: string, nullable: true }
        kommentar: { type: string, nullable: true }
        person: { type: string }
        am: { type: string, format: date-time }
    MassnahmeListe:
      type: object
      required: [abruf, massnahmen]
      properties:
        abruf: { type: string, format: date }
        massnahmen: { type: array, items: { $ref: "#/components/schemas/Massnahme" } }
    MassnahmeWirkung:
      type: object
      description: >
        Die Wirkung (WK1–WK5) - Ausgang der Operation `wirkung` und je Monat die Vergleichszeile. Mit `grund`
        (`ohne_messgrundlage`, `nicht_umgesetzt`) sind die Zahlen null und `monate`/`nicht_gezaehlt` leer.
      required: [massnahme, abruf, grund, umsetzungsmonat, nachher_von, nachher_bis, monate, monate_bewertbar, monate_endgueltig, monate_soll, monate_text, vorlaeufig, nicht_gezaehlt, summe, satz]
      properties:
        massnahme: { $ref: "#/components/schemas/Massnahme" }
        abruf: { type: string, format: date }
        grund: { type: string, nullable: true, enum: [ohne_messgrundlage, nicht_umgesetzt, null] }
        umsetzungsmonat: { type: string, nullable: true }
        nachher_von: { type: string, nullable: true }
        nachher_bis: { type: string, nullable: true }
        monate: { type: array, items: { $ref: "#/components/schemas/MassnahmeWirkungMonat" } }
        monate_bewertbar: { type: integer, nullable: true }
        monate_endgueltig: { type: integer, nullable: true }
        monate_soll: { type: integer, nullable: true }
        monate_text: { type: string, nullable: true, description: "x von N" }
        vorlaeufig: { type: boolean, nullable: true }
        nicht_gezaehlt: { type: array, items: { $ref: "#/components/schemas/MassnahmeWirkungAusschluss" } }
        summe:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/MassnahmeWirkungSumme" }]
        satz: { type: string, nullable: true, description: "Kundensatz `wirkung_vorlaeufig` bzw. `ohne_messgrundlage` (§5.9)" }
    MassnahmeWirkungMonat:
      type: object
      description: >
        Ein Monat vom Umsetzungsmonat an - `gezaehlt` nur endgültig und bewertbar, sonst `grund` mit Kundensatz
        (`wirkung_umsetzungsmonat`, `wirkung_nicht_bewertbar`, `wirkung_basis_nach_umsetzung`); `kennzahl_roh` der rohe
        Kennzahl-Wert ohne Wort (WK5).
      required: [periode, endgueltig, gezaehlt, grund, satz, kennzahl_roh, vergleich]
      properties:
        periode: { type: string }
        endgueltig: { type: boolean }
        gezaehlt: { type: boolean }
        grund: { type: string, nullable: true, enum: [umsetzungsmonat, basis_nach_umsetzung, unvollstaendig, basis_fehlt, basis_beendet, zu_wenig_perioden, variable_fehlt, variable_ausserhalb, periode_nicht_zu_ende, keine_werte, null] }
        satz: { type: string, nullable: true }
        kennzahl_roh: { type: string, nullable: true }
        vergleich: { $ref: "#/components/schemas/BezugsbasisVergleichMonat" }
    MassnahmeWirkungAusschluss:
      type: object
      required: [monat, grund]
      properties:
        monat: { type: string }
        grund: { type: string, enum: [umsetzungsmonat, basis_nach_umsetzung, unvollstaendig, basis_fehlt, basis_beendet, zu_wenig_perioden, variable_fehlt, variable_ausserhalb, periode_nicht_zu_ende, keine_werte] }
    MassnahmeWirkungSumme:
      type: object
      description: Σ ÷ Σ über die bewertbaren Nachher-Monate (Operation `zeitraum` gegen die Fassung des letzten).
      required: [gemessen, erwartet, delta_prozent, band_prozent, richtung, urteil, kennzeichen]
      properties:
        gemessen: { type: string, nullable: true }
        erwartet: { type: string, nullable: true }
        delta_prozent: { type: string, nullable: true }
        band_prozent: { type: string, nullable: true }
        richtung: { type: string, nullable: true }
        urteil: { type: string, enum: [besser, schlechter, im_rahmen, nicht_anwendbar, ohne_urteil] }
        kennzeichen: { type: array, items: { type: string } }
    AbweichungVerweis:
      type: object
      nullable: true
      required: [id, kennzeichen, name]
      properties: { id: { type: string, format: uuid }, kennzeichen: { type: string, nullable: true }, name: { type: string, nullable: true } }
    Auffaelligkeit:
      type: object
      description: >
        Ein Vermerk an der Kennzahl (UEMS AP-18 IP-16, A1, A2): `anlass` ist die gespeicherte kanonische Kopie des
        Vergleichsergebnisses (byte-gleich), `anlass_pruefsumme` `sha256:` darüber, `anlass_inhalt` dasselbe als Objekt;
        `vorbehalte` die Kennzeichen mit „vorläufig“ daraus (geerbt, R8); `satz` §5.9 nur bei `zur_kenntnis`.
      required: [id, kennzahl, bezugsbasis, fassung, periode, standort_id, anlass, anlass_pruefsumme, anlass_inhalt, vorbehalte, vermerkt_am, zustand, antwort, antwort_begruendung, abweichung, beantwortet_am, beantwortet_von, satz]
      properties:
        id: { type: string, format: uuid }
        kennzahl: { $ref: "#/components/schemas/AbweichungVerweis" }
        bezugsbasis: { $ref: "#/components/schemas/AbweichungVerweis" }
        fassung: { type: integer }
        periode: { type: string, pattern: "^[0-9]{4}-(0[1-9]|1[0-2])$" }
        standort_id: { type: string, format: uuid, nullable: true }
        anlass: { type: string }
        anlass_pruefsumme: { type: string, pattern: "^sha256:[0-9a-f]{64}$" }
        anlass_inhalt: { type: object, additionalProperties: true }
        vorbehalte: { type: array, items: { type: string } }
        vermerkt_am: { type: string, format: date-time }
        zustand: { type: string, enum: [offen, beantwortet] }
        antwort: { type: string, nullable: true, enum: [abweichung, zur_kenntnis, null] }
        antwort_begruendung: { type: string, nullable: true }
        abweichung: { $ref: "#/components/schemas/AbweichungVerweis" }
        beantwortet_am: { type: string, format: date-time, nullable: true }
        beantwortet_von: { type: string, nullable: true }
        satz: { type: string, nullable: true, description: "Kundensatz `auffaelligkeit_zur_kenntnis` (§5.9)" }
    AuffaelligkeitListe:
      type: object
      required: [kennzahl, abruf, offen, vermerke]
      properties:
        kennzahl: { $ref: "#/components/schemas/AbweichungVerweis" }
        abruf: { type: string, format: date }
        offen: { type: integer }
        vermerke: { type: array, items: { $ref: "#/components/schemas/Auffaelligkeit" } }
    AuffaelligkeitBeantwortet:
      type: object
      required: [vermerk, abweichung]
      properties:
        vermerk: { $ref: "#/components/schemas/Auffaelligkeit" }
        abweichung:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/Abweichung" }]
    Abweichung:
      type: object
      description: >
        Eine Abweichung (UEMS AP-18 IP-16, A3–A6): Kennzahl × Fassung × Monate, Anlass als Kopie mit Prüfsumme,
        `vorbehalte` geerbt (R8); `frist` über die Operation `frist` beim Abruf; `abschluss` erst abgeschlossen;
        `kopf_satz` §5.9 für einen Monat mit Δ; `vermerke` und `verlauf` nur an der einzelnen.
      required: [id, kennzeichen, kennzahl, bezugsbasis, fassung, monate, herkunft, anlass, anlass_pruefsumme, anlass_inhalt, vorbehalte, verantwortlich, frist, standort_id, zustand, eroeffnet_am, eroeffnet_von, abschluss, kopf_satz, vermerke, verlauf]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, pattern: "^AW-[0-9]{4}-[0-9]{4,9}$" }
        kennzahl: { $ref: "#/components/schemas/AbweichungVerweis" }
        bezugsbasis: { $ref: "#/components/schemas/AbweichungVerweis" }
        fassung: { type: integer }
        monate: { type: array, items: { type: string } }
        herkunft:
          type: object
          required: [art, wortlaut]
          properties:
            art: { type: string, enum: [auffaelligkeit, von_hand] }
            wortlaut: { type: string, nullable: true }
        anlass: { type: string }
        anlass_pruefsumme: { type: string, pattern: "^sha256:[0-9a-f]{64}$" }
        anlass_inhalt: { type: object, additionalProperties: true }
        vorbehalte: { type: array, items: { type: string } }
        verantwortlich:
          type: object
          required: [sub, name]
          properties: { sub: { type: string }, name: { type: string } }
        frist:
          type: object
          required: [abruf, termin, faellig, seit_tagen]
          properties:
            abruf: { type: string, format: date }
            termin: { type: string, format: date }
            faellig: { type: string, nullable: true, enum: [ueberfaellig, null] }
            seit_tagen: { type: integer, nullable: true }
        standort_id: { type: string, format: uuid, nullable: true }
        zustand: { type: string, enum: [offen, abgeschlossen] }
        eroeffnet_am: { type: string, format: date }
        eroeffnet_von: { type: string }
        abschluss: { $ref: "#/components/schemas/AbweichungAbschluss" }
        kopf_satz: { type: string, nullable: true, description: "Kundensatz `abweichung_kopf` (§5.9)" }
        vermerke:
          type: array
          nullable: true
          items: { $ref: "#/components/schemas/Auffaelligkeit" }
        verlauf:
          type: array
          nullable: true
          items: { $ref: "#/components/schemas/AbweichungEintrag" }
    AbweichungAbschluss:
      type: object
      nullable: true
      required: [ergebnis, massnahme, begruendung, am, person, satz]
      properties:
        ergebnis: { type: string, enum: [massnahme, erklaert, keine_abweichung, nicht_bewertbar] }
        massnahme: { $ref: "#/components/schemas/AbweichungVerweis" }
        begruendung: { type: string }
        am: { type: string, format: date }
        person: { type: string }
        satz: { type: string, nullable: true, description: "Kundensatz `abschluss_massnahme`/`abschluss_erklaert` (§5.9)" }
    AbweichungAussage:
      type: object
      nullable: true
      description: U1/U2 - die Aussage einer Person, nie ein Fakt des Systems.
      required: [wortlaut, sub, name, am, beleg_kennung, kennzeichen, satz]
      properties:
        wortlaut: { type: string }
        sub: { type: string, nullable: true }
        name: { type: string }
        am: { type: string, format: date }
        beleg_kennung: { type: string, nullable: true }
        kennzeichen: { type: string, description: "„Aussage von …, TT.MM.JJJJ — keine Messung“ bzw. „— mit Beleg …“" }
        satz: { type: string, nullable: true, description: "Kundensatz `ursache_aussage`/`ursache_aussage_mit_beleg` (§5.9)" }
    AbweichungEintrag:
      type: object
      description: Eine Zeile des Protokolls `abweichung_aenderung`; `person` hat eingetragen (nicht zwingend ausgesagt).
      required: [nr, art, alt, neu, begruendung, kommentar, aussage, person, am]
      properties:
        nr: { type: integer, format: int64 }
        art: { type: string, enum: [abweichung_eroeffnet, kommentar, ursache_aussage, abweichung_geaendert, verantwortlicher_geaendert, abweichung_abgeschlossen] }
        alt: { type: object, nullable: true, additionalProperties: true }
        neu: { type: object, nullable: true, additionalProperties: true }
        begruendung: { type: string, nullable: true }
        kommentar: { type: string, nullable: true }
        aussage: { $ref: "#/components/schemas/AbweichungAussage" }
        person: { type: string }
        am: { type: string, format: date-time }
    AbweichungListe:
      type: object
      required: [abruf, abweichungen]
      properties:
        abruf: { type: string, format: date }
        abweichungen: { type: array, items: { $ref: "#/components/schemas/Abweichung" } }
    BezugsbasisVergleichMonat:
      type: object
      description: Ein Monat - roh und bereinigt getrennt (U1); nur `bereinigt` trägt ein Urteil.
      required: [periode, beschriftung, roh, bereinigt, satz]
      properties:
        periode: { type: string, example: "2027-12" }
        beschriftung: { type: string, example: Dezember 2027 }
        roh: { $ref: "#/components/schemas/BezugsbasisVergleichRoh" }
        bereinigt: { $ref: "#/components/schemas/BezugsbasisVergleichBereinigt" }
        satz: { type: string, description: "Der Kundensatz des Monats (§10/§16, U6: keine Ursache)." }
    BezugsbasisVergleichRoh:
      type: object
      description: Die rohe Veränderung zum Vormonat (Operation `roh`) - nie ein Urteil (U1, VG3).
      required: [gemessen, vorher, delta_prozent, richtung, variable_delta_prozent, urteil]
      properties:
        gemessen: { type: string, nullable: true }
        vorher: { type: string, nullable: true }
        delta_prozent: { type: string, nullable: true, example: "-8.8" }
        richtung: { type: string, nullable: true, enum: [mehr, weniger, gleich, null] }
        variable_delta_prozent: { type: string, nullable: true, example: "-21.9", description: "Die Veränderung des Nenners (Variable 1) zum Vormonat." }
        urteil: { type: string, enum: [ohne_urteil] }
    BezugsbasisVergleichBereinigt:
      type: object
      description: Das Ergebnis der Operation `vergleich` gegen die Fassung am letzten Tag des Monats (P4), mit gemessen und Bedingung (U4).
      required: [fassung, gemessen, bedingung, erwartet, delta_prozent, band_prozent, richtung, urteil, grund, kennzeichen]
      properties:
        fassung:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/BezugsbasisVergleichFassung" }]
        gemessen: { $ref: "#/components/schemas/BezugsbasisVergleichGemessen" }
        bedingung: { type: array, items: { $ref: "#/components/schemas/BezugsbasisVergleichBedingung" } }
        erwartet: { type: string, nullable: true, example: "69098" }
        delta_prozent: { type: string, nullable: true, example: "12.9" }
        band_prozent: { type: string, nullable: true, example: "2.0" }
        richtung: { type: string, nullable: true, enum: [mehr, weniger, gleich, null] }
        urteil: { type: string, enum: [besser, schlechter, im_rahmen, ohne_urteil, nicht_anwendbar] }
        grund: { type: string, nullable: true, enum: [basis_fehlt, basis_beendet, zu_wenig_perioden, variable_fehlt, variable_ausserhalb, variablen_abhaengig, keine_werte, periode_nicht_zu_ende, null] }
        kennzeichen: { type: array, items: { type: string }, description: "G5, in dieser Reihenfolge, ohne Doppel." }
    BezugsbasisVergleichFassung:
      type: object
      required: [fassung, methode, referenzperiode, datenlage, gilt_ab, gilt_bis]
      properties:
        fassung: { type: integer }
        methode: { type: string, enum: [verhaeltnis, regression_eine_variable, regression_zwei_variablen, gradtage] }
        referenzperiode: { type: string, example: "2026-11/2027-10" }
        datenlage: { type: string, enum: [vollstaendig, vorlaeufig] }
        gilt_ab: { type: string, format: date }
        gilt_bis: { type: string, format: date, nullable: true }
    BezugsbasisVergleichGemessen:
      type: object
      description: Der gespeicherte Zähler der Kennzahl (die Energie) mit Version.
      required: [wert, einheit, version, zustand]
      properties:
        wert: { type: string, nullable: true }
        einheit: { type: string, example: kWh }
        version: { type: integer, nullable: true }
        zustand: { type: string, nullable: true }
    BezugsbasisVergleichBedingung:
      type: object
      description: Eine Variable mit Wert - Bezugsgröße mit wirksamer Fassung, oder ohne Bezugsgröße der gespeicherte Nenner der Kennzahl mit Version.
      required: [position, quelle, kennzeichen, name, wert, einheit, fassung, version, zustand]
      properties:
        position: { type: integer, enum: [1, 2] }
        quelle: { type: string, enum: [bezugsgroesse, kennzahl] }
        kennzeichen: { type: string, nullable: true, example: BZ-1 }
        name: { type: string }
        wert: { type: string, nullable: true }
        einheit: { type: string }
        fassung: { type: integer, nullable: true }
        version: { type: integer, nullable: true }
        zustand: { type: string, nullable: true }
    BezugsbasisVergleichZeitraum:
      type: object
      description: Operation `zeitraum` (U5) gegen die Fassung am letzten Tag von `bis` - Σ gemessen ÷ Σ erwartet, nie ein Mittel.
      required: [fassung, gemessen, erwartet, delta_prozent, band_prozent, richtung, urteil, grund, monate, kennzeichen, satz]
      properties:
        fassung: { type: integer, nullable: true }
        gemessen: { type: string, nullable: true }
        erwartet: { type: string, nullable: true }
        delta_prozent: { type: string, nullable: true, example: "1.8" }
        band_prozent: { type: string, nullable: true }
        richtung: { type: string, nullable: true, enum: [mehr, weniger, gleich, null] }
        urteil: { type: string, enum: [besser, schlechter, im_rahmen, ohne_urteil, nicht_anwendbar] }
        grund: { type: string, nullable: true, enum: [basis_fehlt, basis_beendet, zu_wenig_perioden, variable_fehlt, variable_ausserhalb, variablen_abhaengig, keine_werte, periode_nicht_zu_ende, null] }
        monate: { type: string, nullable: true, example: "4 von 4" }
        kennzeichen: { type: array, items: { type: string } }
        satz: { type: string }
    BezugsbasisVergleichStand:
      type: object
      required: [nummer, am]
      properties:
        nummer: { type: integer }
        am: { type: string, format: date }
    KennzahlWerte:
      type: object
      required: [kennzahl, periode, von, bis, zeitzone, version, werte]
      properties:
        kennzahl: { $ref: "#/components/schemas/KennzahlWerteKennzahl" }
        periode: { type: string, enum: [tag, woche, monat, jahr] }
        von: { type: string, format: date }
        bis: { type: string, format: date, description: "Der LETZTE Tag, einschließlich." }
        zeitzone: { type: string }
        version: { type: integer, nullable: true, description: "Die angefragte Version; null = je Schritt die neueste." }
        werte: { type: array, items: { $ref: "#/components/schemas/KennzahlWert" } }
        geteilte_register:
          type: array
          items: { $ref: "#/components/schemas/KennzahlGeteiltesRegister" }
          description: >
            Summen-Wächter des geteilten Punkts (AP-07 IP-18b), nur an einer Zusammenfassung: in Σ Zähler oder Σ Nenner
            der Paare (Fassungen am letzten Tag) lesen zwei Messstellen denselben Messpunkt derselben Box über zwei
            Komponenten. Liest jede Komponente dasselbe Gerät, zählt die Summe den Wert zweimal. Eine Warnung neben den
            Werten - sie ändert keine. Fehlt ohne Fund.
    KennzahlGeteiltesRegister:
      type: object
      required: [rolle, register, messstellen]
      properties:
        rolle: { type: string, enum: [zaehler, nenner] }
        register: { type: string, description: "Der Messpunkt (point_key) an der Box.", example: sunspec.model_203.totwhimp }
        messstellen: { type: array, items: { type: string }, minItems: 2, example: [MS-12, MS-18] }
    KennzahlWert:
      type: object
      description: >
        Eine Periode - die Trägerform von MessstelleWerteWert für eine Kennzahl. Beträge als Dezimaltext,
        ungerundet; gerundet wird nur angezeigt (U4). `fassung` (vorläufig/endgültig) und
        `definition_fassung` (die Berechnung) sind zwei Achsen, `version` eine dritte.
      required: [von, bis, schluessel, beschriftung, wert, zaehler, nenner, einheit, zustand, richtung, kennzeichen,
        abdeckung_prozent, fassung, endgueltig_ab, version, definition_fassung, berechnet_am, grund, herkunft, versionen]
      properties:
        von: { type: string, format: date }
        bis: { type: string, format: date, description: "Der LETZTE Tag der Periode, einschließlich." }
        schluessel: { type: string, description: "2026-10-05 · 2026-W40 · 2026-10 · 2026." }
        beschriftung: { type: string, description: "„05.10.2026“ · „KW 40/2026“ · „Oktober 2026“ · „2026“." }
        wert: { type: string, nullable: true, description: "Dezimaltext, ungerundet; null = keine Zahl, nie 0. Ein Anteil in Prozent (50.82)." }
        zaehler: { type: string, nullable: true }
        nenner: { type: string, nullable: true }
        einheit: { type: string, nullable: true, description: "Die Einheit der Fassung, mit der der Wert gebildet wurde (U1)." }
        zustand: { type: string, nullable: true, enum: [vollständig, unvollständig, keine Werte, mit Ersatzwert], description: "null nur ohne Zeile oder ohne die angefragte Version - dann steht `grund`." }
        richtung: { type: string, nullable: true, enum: [untergrenze, obergrenze, unbestimmt], description: "Genau bei „unvollständig“ (Q3)." }
        kennzeichen: { type: array, items: { type: string }, description: "Die gespeicherten Sätze - Wortlaut und Reihenfolge wie im Vertrag." }
        abdeckung_prozent: { type: string, nullable: true, description: "Dezimaltext, nie auf 100 gerundet (Q4)." }
        fassung: { type: string, nullable: true, enum: [vorlaeufig, endgueltig] }
        endgueltig_ab: { type: string, nullable: true }
        version: { type: integer, nullable: true, minimum: 1, description: "Die Version, deren Zahl der Schritt zeigt - ohne Anfrage die neueste; null = noch keine (ohne Zahl und ohne früheren Wert, K8)." }
        definition_fassung: { type: integer, nullable: true, minimum: 1, description: "Die Fassung der Berechnung, die am letzten Tag der Periode galt (V2)." }
        berechnet_am: { type: string, nullable: true }
        grund:
          type: string
          nullable: true
          enum: [nenner_fehlt, nenner_null, zaehler_fehlt, periode_nicht_zu_ende, vor_bestehen, haengt_an_kreis, eingang_archiviert, noch_nicht_gebildet, version_nicht_gespeichert]
          description: Warum der Schritt keine Zahl trägt - ein Wort der Zeile (grund_ohne_zahl) oder des Lesers.
        herkunft:
          type: object
          nullable: true
          description: "Die Hülle nach kennzahlwert-herkunft.schema.json an jeder Version, auch ohne Zahl (K19); null ohne Zeile oder ohne Version."
          required: [satz, fehlt]
          properties:
            satz: { type: object, nullable: true, additionalProperties: true, description: "Die Form von kennzahlwert-herkunft.schema.json → $defs/satz." }
            fehlt: { type: array, items: { type: string, enum: [kennzahl, definition_fassung, berechnet_am, eingaenge, anlass] } }
        versionen: { type: integer, nullable: true, description: "Wie viele Versionen die Periode hat - ab 2 gibt es eine Historie unter …/werte/versionen; null, solange keine gebildet ist." }
    KennzahlWerteHistorie:
      type: object
      description: Die Versions-Historie EINER Periode (AP-11 IP-7, Muster AP-08 IP-18). `grund` nur, wenn die Periode keine Version hat.
      required: [kennzahl, periode, von, bis, zeitzone, grund, versionen]
      properties:
        kennzahl: { $ref: "#/components/schemas/KennzahlWerteKennzahl" }
        periode: { type: string, enum: [tag, woche, monat, jahr] }
        von: { type: string, format: date }
        bis: { type: string, format: date }
        zeitzone: { type: string }
        grund: { type: string, nullable: true, description: "Ein Wort von KennzahlWert.grund." }
        versionen: { type: array, items: { $ref: "#/components/schemas/KennzahlWertVersion" } }
    KennzahlWertVersion:
      type: object
      required: [version, wert_alt, wert_neu, gebildet_am, nachgezogen_am, anlass, entscheidungen]
      properties:
        version: { type: integer, minimum: 1 }
        wert_alt: { allOf: [{ $ref: "#/components/schemas/KennzahlWert" }], nullable: true, description: "Version n − 1 wie …/werte?version=n-1; an Version 1 null." }
        wert_neu: { $ref: "#/components/schemas/KennzahlWert" }
        gebildet_am: { type: string, description: "Die erste Zeile der Version." }
        nachgezogen_am: { type: string, nullable: true, description: "Eine vorläufige Version zieht nach (V3) - wann zuletzt." }
        anlass: { allOf: [{ $ref: "#/components/schemas/KennzahlWertAnlass" }], nullable: true }
        entscheidungen: { type: array, items: { $ref: "#/components/schemas/KennzahlWertEntscheidung" } }
    KennzahlWertAnlass:
      type: object
      required: [art, beleg]
      properties:
        art: { type: string, enum: [eingang, definition] }
        beleg: { type: string, example: "K-2026-0007 (freigegeben 12.11.2026)" }
    KennzahlWertEntscheidung:
      type: object
      description: Die Form von MessstelleWerteEntscheidung mit dem weiteren Vorgang `berechnung` (die Fassung der Kennzahl).
      required: [vorgang, kennung, fassung, status, methode, art, wer, wann, warum, beleg, fehlt, angelegt]
      properties:
        vorgang: { type: string, enum: [ersatzwert, korrektur, berechnung] }
        kennung: { type: string, description: "K-… · EW-… · bei `berechnung` das Kennzeichen der Kennzahl." }
        fassung: { type: integer, nullable: true, minimum: 1, description: "null, wenn die Fassung des genannten Vorgangs nicht lesbar ist (`fehlt` fassung)." }
        status: { type: string, nullable: true }
        methode: { type: string, nullable: true }
        art: { type: string, nullable: true, description: "Korrektur: ihre Art; Berechnung: die Herkunft der Fassung (anlage · eintrag · kopie)." }
        wer: { allOf: [{ $ref: "#/components/schemas/MessstelleWerteUrheber" }], nullable: true }
        wann: { type: string, nullable: true }
        warum: { type: string, nullable: true, description: "Der Text dieser Fassung - null, wenn keiner geschrieben wurde." }
        beleg: { type: string, nullable: true }
        fehlt: { type: array, items: { type: string, enum: [warum, fassung] } }
        angelegt: { allOf: [{ $ref: "#/components/schemas/MessstelleWerteAngelegt" }], nullable: true }
    BezugsgroesseAnfrage:
      type: object
      additionalProperties: false
      description: >
        Der Körper von POST (ohne `kennzeichen` vergibt der Server) und PUT (die ganze
        Bezugsgröße, mit Kennzeichen). Jedes Feld ist Text oder null; die Wörter sind die
        Vokabulare des Bezugsdaten-Vertrags. `periode_art` genau beim Periodenwert.
      required: [name, wertart, einheit, geltung_art, geltung_id]
      properties:
        kennzeichen: { type: string, nullable: true, pattern: "^[A-Z0-9./-]{2,16}$" }
        name: { type: string }
        wertart: { type: string, enum: [periodenwert, stand, stammdatum] }
        einheit: { type: string, description: "Aus `einheiten` des Vertrags (kg, t, Stück, h, min, m³, l, Personen, Schichten, Kd, °C; m² nicht - Flächen stehen am Gebäude)." }
        periode_art: { type: string, nullable: true, enum: [tag, woche, monat, jahr] }
        geltung_art: { type: string, enum: [unternehmen, standort, gebaeude, bereich, prozess, kostenstelle, messstelle], description: "Alle sieben sind seit AP-10 IP-7 wählbar (Prozess und Kostenstelle haben ihre Tabellen)." }
        geltung_id: { type: string, format: uuid }
        art: { type: string, nullable: true, enum: [produktionsmenge, gutteile, betriebszeit, schichten, bezugsflaeche, mitarbeitende, gradtagzahl, zaehlerstand, sonstige_menge, betriebszeit_aus_leistung], description: "Eigene Art; null = nicht angegeben. POST darf sie weglassen; PUT ohne Art oder mit null erhält die vorhandene Art." }
    Bezugsgroesse:
      type: object
      required: [id, kennzeichen, name, art, wertart, einheit, periode_art, geltung_art, geltung_id, geltung_name, hat_werte, archiviert_am, angelegt_am]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        name: { type: string }
        wertart: { type: string, enum: [periodenwert, stand, stammdatum] }
        einheit: { type: string }
        periode_art: { type: string, nullable: true, enum: [tag, woche, monat, jahr] }
        geltung_art: { type: string, enum: [unternehmen, standort, gebaeude, bereich, prozess, kostenstelle, messstelle] }
        geltung_id: { type: string, format: uuid }
        geltung_name: { type: string, description: "Name des Objekts; bei einer Messstelle ohne Namen ihr Kennzeichen." }
        hat_werte: { type: boolean, description: "Trägt sie eine Wert-Zeile oder ein Intervall eines Stammdatums? Dann gilt M1 und sie ist nicht löschbar (M6)." }
        archiviert_am: { type: string, format: date-time, nullable: true }
        angelegt_am: { type: string, format: date-time }
        art: { type: string, nullable: true, enum: [produktionsmenge, gutteile, betriebszeit, schichten, bezugsflaeche, mitarbeitende, gradtagzahl, zaehlerstand, sonstige_menge, betriebszeit_aus_leistung], description: "Eigene Art; null = nicht angegeben. POST darf sie weglassen; PUT ohne Art oder mit null erhält die vorhandene Art." }
    BezugsgroesseListe:
      type: object
      required: [bezugsgroessen, bezugsflaechen]
      properties:
        bezugsgroessen: { type: array, items: { $ref: "#/components/schemas/Bezugsgroesse" } }
        bezugsflaechen:
          type: array
          description: "AP-09 IP-6: die Bezugsflächen der Ortsstruktur - gelesen, ohne ID und Kennzeichen einer Bezugsgröße."
          items: { $ref: "#/components/schemas/Bezugsflaeche" }
    Bezugsflaeche:
      type: object
      description: >
        Eine Bezugsfläche aus der Ortsstruktur (`flaeche_gueltigkeit`) als Bezugsgröße (E17).
        `schreibbar` ist immer false - geändert wird sie am Gebäude; `pflegen` sagt es.
      required: [name, wertart, einheit, herkunft_art, geltung_art, geltung_id, geltung_kennzeichen, geltung_name, schreibbar, pflegen]
      properties:
        name: { type: string }
        wertart: { type: string, enum: [stammdatum] }
        einheit: { type: string, enum: ["m²"] }
        herkunft_art: { type: string, enum: [stammdatum_ap02] }
        geltung_art: { type: string, enum: [standort, gebaeude, bereich] }
        geltung_id: { type: string, format: uuid }
        geltung_kennzeichen: { type: string, nullable: true }
        geltung_name: { type: string }
        schreibbar: { type: boolean, enum: [false] }
        pflegen: { type: string, description: "Der Satz von `flaeche_aus_struktur`." }
    BezugsgroesseStichtagwert:
      type: object
      description: >
        Der Wert eines Stammdatums für EINE Periode am Stichtag = ihrem letzten Tag (E17).
        `betrag` null = nicht erhoben, nie 0. `kennzeichen`: jeder Übergang nach dem ersten Tag
        der Periode (S3). `quelle` nur bei einer Bezugsfläche.
      required: [periode, von, stichtag, betrag, quelle, gilt_ab, eingetragen_am, abzeichen, kennzeichen]
      properties:
        periode: { type: string, description: "Der Schlüssel der Periode (2026-10, 2026-W53, 2026, 2026-10-31)." }
        von: { type: string, format: date }
        stichtag: { type: string, format: date }
        betrag: { type: string, nullable: true }
        quelle: { type: string, nullable: true, enum: [eigen, aus_gebaeuden_summiert, null] }
        gilt_ab: { type: string, format: date, nullable: true, description: "Beginn des Intervalls, das am Stichtag gilt." }
        eingetragen_am: { type: string, format: date, nullable: true }
        abzeichen: { type: string, nullable: true, description: "„rückwirkend (n Tage)“ des Intervalls, das am Stichtag gilt." }
        kennzeichen: { type: array, items: { type: string } }
    BezugsflaecheWerte:
      type: object
      required: [bezugsflaeche, perioden]
      properties:
        bezugsflaeche: { $ref: "#/components/schemas/Bezugsflaeche" }
        perioden: { type: array, items: { $ref: "#/components/schemas/BezugsgroesseStichtagwert" } }
    Bezugsflaechen:
      type: object
      required: [periode_art, von, bis, bezugsflaechen]
      properties:
        periode_art: { type: string, enum: [tag, woche, monat, jahr] }
        von: { type: string, format: date }
        bis: { type: string, format: date }
        bezugsflaechen: { type: array, items: { $ref: "#/components/schemas/BezugsflaecheWerte" } }
    BezugsgroesseStammdatumAnfrage:
      type: object
      additionalProperties: false
      required: [wert, gueltig_ab]
      properties:
        wert: { type: string, pattern: "^[0-9]{1,12}(\\.[0-9]{1,6})?$", description: "Dezimaltext größer als 0, Punkt als Trenner." }
        gueltig_ab: { type: string, format: date }
    BezugsgroesseStammdatumIntervall:
      type: object
      required: [wert, gueltig_ab, gueltig_bis, aufgehoben_am, eingetragen_am, abzeichen]
      properties:
        wert: { type: string }
        gueltig_ab: { type: string, format: date }
        gueltig_bis: { type: string, format: date, nullable: true, description: "Der LETZTE Tag, einschließlich; null = offen." }
        aufgehoben_am: { type: string, format: date-time, nullable: true, description: "Eine Korrektur - das Intervall bleibt lesbar, belegt aber keinen Tag." }
        eingetragen_am: { type: string, format: date-time }
        abzeichen: { type: string, nullable: true }
    BezugsgroesseStammdatum:
      type: object
      required: [bezugsgroesse_id, kennzeichen, name, einheit, zeitzone, schreibbar, intervalle, periode_art, von, bis, perioden]
      properties:
        bezugsgroesse_id: { type: string, format: uuid }
        kennzeichen: { type: string }
        name: { type: string }
        einheit: { type: string }
        zeitzone: { type: string, description: "Die Zone der Tage: die des Standorts, sonst die des Unternehmens." }
        schreibbar: { type: boolean, description: "false, sobald die Bezugsgröße archiviert ist." }
        intervalle: { type: array, items: { $ref: "#/components/schemas/BezugsgroesseStammdatumIntervall" } }
        periode_art: { type: string, nullable: true, enum: [tag, woche, monat, jahr, null] }
        von: { type: string, format: date, nullable: true }
        bis: { type: string, format: date, nullable: true }
        perioden: { type: array, items: { $ref: "#/components/schemas/BezugsgroesseStichtagwert" } }
    BezugsgroessePerson:
      type: object
      required: [name, rolle, art]
      properties:
        name: { type: string }
        rolle: { type: string, nullable: true }
        art: { type: string, enum: [kunde, unterstuetzung, voltpilot, notfall] }
    BezugsgroesseHerkunft:
      type: object
      description: Die Herkunft EINER Fassung (Invariante 7).
      required: [art, von_hand, import_kennung, import_zeile, geliefert_text, geliefert_einheit]
      properties:
        art: { type: string, enum: [eingabe, import, messkanal] }
        von_hand: { type: boolean, description: "Von Hand eingetragen (`art` = eingabe)." }
        import_kennung: { type: string, nullable: true }
        import_zeile: { type: integer, nullable: true }
        geliefert_text: { type: string, nullable: true }
        geliefert_einheit: { type: string, nullable: true }
    BezugsgroesseFassung:
      type: object
      required: [fassung, vorgang, status, stand, betrag, ersetzt_fassung, begruendung, kennzeichen, herkunft, urheber, freigeber, eingetragen_am]
      properties:
        fassung: { type: integer, minimum: 1 }
        vorgang: { type: string, enum: [erstwert, berichtigung, ruecknahme] }
        status: { type: string, enum: [wirksam, vorschlag, zurueckgenommen, abgelehnt], description: "Das Wort bei Entstehen (gespeichert)." }
        stand: { type: string, nullable: true, description: "Die Lesart der Regel `fassung`: „wirksam“, „wirksam bis Fassung n“, „zurueckgenommen“; null bei `stand_offen`." }
        betrag: { type: string, nullable: true, description: "Dezimaltext in der Einheit der Bezugsgröße; null bei einer Rücknahme ohne Vorfassung." }
        ersetzt_fassung: { type: integer, nullable: true }
        begruendung: { type: string, nullable: true }
        kennzeichen: { type: array, items: { type: string } }
        herkunft: { $ref: "#/components/schemas/BezugsgroesseHerkunft" }
        urheber: { $ref: "#/components/schemas/BezugsgroessePerson" }
        freigeber:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/BezugsgroessePerson" }]
        eingetragen_am: { type: string, format: date-time }
        kanal: { $ref: "#/components/schemas/BezugsgroesseKanalherkunft" }
    BezugsgroesseKanalherkunft:
      type: object
      description: Nur bei Herkunft messkanal vorhanden; zusammen mit der Fassung unveränderlich.
      required: [bindung, entity_id, kanal, regel, zustand, abdeckung_prozent, endgueltig_ab, vorlaeufig]
      properties:
        bindung: { type: string, format: uuid }
        entity_id: { type: string, format: uuid }
        kanal: { type: string }
        regel: { type: string }
        zustand: { type: string, enum: [vollständig, unvollständig, keine Werte] }
        abdeckung_prozent: { type: number, minimum: 0, maximum: 100 }
        endgueltig_ab: { type: string, format: date-time }
        vorlaeufig: { type: boolean }
        bindungen:
          type: array
          description: Bei einem Wechsel innerhalb der Periode alle zeitlich disjunkten Quellen; die einzelnen Quellangaben oben nennen die erste.
          items: { type: object, additionalProperties: true }
    BezugsgroesseKanalbindung:
      type: object
      required: [id, entity_id, kanal, wertart, zustand, von, bis]
      properties:
        id: { type: string, format: uuid }
        entity_id: { type: string, format: uuid }
        kanal: { type: string }
        wertart: { type: string, enum: [counter, state, gauge] }
        messstelle_id: { type: string, format: uuid }
        schwelle_kw: { type: number, minimum: 0 }
        begruendung: { type: string }
        fassung: { type: integer, minimum: 1 }
        ersetzt_bindung_id: { type: string, format: uuid }
        regel: { type: string, description: "Bei Betriebszeit aus Leistung: aus Leistung über x kW (Annahme)." }
        raumtemperatur: { type: number, nullable: true, description: "Nur Temperatur; dauerhaft gespeicherter Parameter." }
        heizgrenze: { type: number, nullable: true, description: "Nur Temperatur; dauerhaft gespeicherter Parameter." }
        zustand: { type: string, nullable: true }
        von: { type: string, format: date-time }
        bis: { type: string, format: date-time, nullable: true }
    BezugsgroesseWetterbezug:
      type: object
      description: AP-17 IP-12c — Wetter-Sicht einer Bezugsgröße.
      required: [moeglich, koordinaten, bindung]
      properties:
        moeglich: { type: boolean, description: "Gradtagzahl am Standort mit Tages- oder Monatswerten, nicht archiviert." }
        koordinaten: { type: boolean }
        satz: { type: string, description: "Nur ohne Koordinaten an einer Gradtagzahl: der Satz aus AP-17 §5.8." }
        bindung:
          nullable: true
          type: object
          required: [raumtemperatur, heizgrenze, regel, von, gebunden_von, gebunden_am]
          properties:
            raumtemperatur: { type: number }
            heizgrenze: { type: number }
            regel: { type: string, example: "Gradtage G20/15" }
            von: { type: string, format: date }
            gebunden_von: { type: string }
            gebunden_am: { type: string, format: date-time }
            quelle: { type: string, description: "Fehlt vor dem ersten Abruf.", example: "Open-Meteo-Archiv" }
            letzter_abruf: { type: string, format: date-time, description: "Spätester Abruf eines bezogenen Werts." }
            stand:
              type: object
              description: "Letzter Monat mit bezogenen Tagen: „x von y Tagen“ (y bis gestern, ab `von`; bei Monatswerten aus der gespeicherten Fassung)."
              required: [monat, tage, tage_erwartet, zustand]
              properties:
                monat: { type: string, example: "2027-03" }
                tage: { type: integer }
                tage_erwartet: { type: integer }
                zustand: { type: string, enum: [vollständig, unvollständig] }
    BezugsgroesseKanalfehler:
      type: object
      required: [code, message]
      properties:
        code: { type: string, description: "Benannter Grund, u. a. kanal_gebunden, bindung_ueberlappt, kanal_liefert_nicht, vor_erster_messung, zeitraum_hat_werte, ende_ungueltig." }
        message: { type: string }
    BezugsgroesseWert:
      type: object
      required: [periode_von, periode_bis, zeitpunkt, zeitzone, wirksamer_betrag, wirksame_fassung, stand_offen, fassungen, vorschlag]
      properties:
        periode_von: { type: string, format: date, nullable: true }
        periode_bis: { type: string, format: date, nullable: true, description: "Der LETZTE Tag der Periode, einschließlich." }
        zeitpunkt: { type: string, format: date-time, nullable: true }
        zeitzone: { type: string }
        wirksamer_betrag: { type: string, nullable: true, description: "null nach einer Rücknahme (nie 0) und bei `stand_offen`." }
        wirksame_fassung: { type: integer, nullable: true }
        stand_offen: { type: boolean }
        fassungen: { type: array, items: { $ref: "#/components/schemas/BezugsgroesseFassung" } }
        vorschlag:
          nullable: true
          description: "AP-09 IP-7: die offene Berichtigung dieses Werts (Vier-Augen an) - noch keine Fassung; null, wenn keine offen ist."
          allOf: [{ $ref: "#/components/schemas/BezugsgroesseVorschlag" }]
    BezugsgroesseWerte:
      type: object
      required: [bezugsgroesse_id, kennzeichen, wertart, einheit, periode_art, von, bis, fassungen, werte]
      properties:
        bezugsgroesse_id: { type: string, format: uuid }
        kennzeichen: { type: string }
        wertart: { type: string }
        einheit: { type: string }
        periode_art: { type: string, nullable: true }
        von: { type: string, format: date, nullable: true }
        bis: { type: string, format: date, nullable: true }
        fassungen: { type: string, enum: [wirksam, alle] }
        werte: { type: array, items: { $ref: "#/components/schemas/BezugsgroesseWert" } }
    BezugsgroesseWertAnfrage:
      type: object
      additionalProperties: false
      required: [periode, wert]
      description: >
        AP-09 IP-7: ein Wert für eine Periode. `periode` nennt die Periode selbst (Z3: „2026-10“,
        „10/2026“, „Oktober 2026“, „2026-W40“, „2026“); `wert` ist der Text, wie ein Mensch ihn tippt -
        deutsch, Punkt = Tausender, Komma = Dezimal („48.200“, „312.400,5“).
      properties:
        periode: { type: string }
        wert: { type: string }
    BezugsgroesseBerichtigungAnfrage:
      type: object
      additionalProperties: false
      required: [wert, begruendung]
      properties:
        wert: { type: string, description: "Der neue Wert als Text (wie `BezugsgroesseWertAnfrage.wert`)." }
        begruendung: { type: string, description: "10 bis 500 Zeichen (F2)." }
    BezugsgroesseHinweis:
      type: object
      required: [code, satz]
      properties:
        code: { type: string, enum: [wert_unplausibel] }
        satz: { type: string }
    BezugsgroesseVorschlag:
      type: object
      description: "AP-09 IP-7: die offene Berichtigung eines Werts (Vier-Augen an) - bis zur Freigabe gilt die wirksame Fassung."
      required: [kennung, betrag, ersetzt_fassung, begruendung, urheber, eingetragen_am]
      properties:
        kennung: { type: string, pattern: "^BK-[0-9]{4}-[0-9]{4,}$" }
        betrag: { type: string }
        ersetzt_fassung: { type: integer, minimum: 1 }
        begruendung: { type: string }
        urheber: { $ref: "#/components/schemas/BezugsgroessePerson" }
        eingetragen_am: { type: string, format: date-time }
    BezugsgroesseEingabe:
      type: object
      description: >
        AP-09 IP-7: die Antwort auf eine Eingabe oder Berichtigung - `urteil` mit seinem Satz aus dem
        Vertrag (verwalten.eingabe.urteile), die Kennung des Vorgangs (nur bei einer Berichtigung), die
        Hinweise (U6, sie verhindern nichts) und der Wert mit allen Fassungen.
      required: [urteil, satz, kennung, hinweise, wert]
      properties:
        urteil: { type: string, enum: [neu, wiederholung, berichtigung, vorschlag] }
        satz: { type: string }
        kennung: { type: string, nullable: true, pattern: "^BK-[0-9]{4}-[0-9]{4,}$" }
        hinweise: { type: array, items: { $ref: "#/components/schemas/BezugsgroesseHinweis" } }
        wert: { $ref: "#/components/schemas/BezugsgroesseWert" }
    BezugsdatenZuordnung:
      type: object
      description: C3 - die Zuordnung der Spalten (Form `vorschau_zuordnung` des Bezugsdaten-Vertrags). Genau eins von Bezug-Spalte und `bezugsgroesse`.
      required: [spalten, deutung, zahlformat]
      additionalProperties: false
      properties:
        csv:
          type: object
          nullable: true
          additionalProperties: false
          properties:
            kodierung: { type: string, enum: [utf-8, windows-1252] }
            trennzeichen: { type: string, enum: [";", ",", "\t"] }
            kopfzeile: { type: boolean }
        spalten:
          type: object
          additionalProperties: false
          description: 1-basierte Spalten je Rolle.
          properties:
            periode: { type: integer, minimum: 1 }
            bis: { type: integer, minimum: 1, nullable: true }
            wert: { type: integer, minimum: 1 }
            einheit: { type: integer, minimum: 1, nullable: true }
            bezug: { type: integer, minimum: 1, nullable: true }
            bemerkung: { type: integer, minimum: 1, nullable: true }
        deutung: { type: string, enum: [periode, periodenbeginn, periodenende, von_bis, zeitpunkt] }
        zahlformat: { type: string, enum: [de, en, auto] }
        einheit: { type: string, nullable: true, description: "Feste Einheit ohne Einheitsspalte (U3)." }
        bezugsgroesse: { type: string, nullable: true, description: "Kennzeichen für die ganze Datei, ohne Bezug-Spalte." }
        bezug_tabelle: { type: object, additionalProperties: { type: string } }
        synonyme: { type: object, additionalProperties: { type: string } }
    BezugsdatenBefund:
      type: object
      required: [befund, satz, hinweis]
      properties:
        befund: { type: string, enum: [datei_bekannt, zeile_bekannt, konflikt_anderer_wert, einheit_unbekannt, einheit_umgerechnet, periode_passt_nicht, periode_nicht_zu_ende, zeit_mehrdeutig, zeit_nicht_vorhanden, zahl_unlesbar, datum_unlesbar, bezug_unbekannt, wert_negativ, wert_unplausibel, keine_datenzeilen, datei_zu_gross, kodierung_unlesbar] }
        satz: { type: string }
        hinweis: { type: boolean, description: "Ein Hinweis verhindert die Zeile nicht." }
    BezugsdatenVorlageAnfrage:
      type: object
      additionalProperties: false
      required: [vorlage_id, name, zuordnung]
      properties:
        vorlage_id: { type: string, format: uuid, nullable: true, description: "Leer für Fassung 1; gesetzt für die nächste Fassung." }
        name: { type: string, minLength: 1, maxLength: 120 }
        zuordnung: { $ref: "#/components/schemas/BezugsdatenZuordnung" }
    BezugsdatenVorlageUrheber:
      type: object
      required: [name, rolle, art]
      properties:
        name: { type: string }
        rolle: { type: string, nullable: true }
        art: { type: string, enum: [kunde, unterstuetzung, voltpilot, notfall] }
    BezugsdatenVorlage:
      type: object
      required: [vorlage_id, fassung, name, zuordnung, urheber, erstellt_am]
      properties:
        vorlage_id: { type: string, format: uuid }
        fassung: { type: integer, minimum: 1 }
        name: { type: string }
        zuordnung: { $ref: "#/components/schemas/BezugsdatenZuordnung" }
        urheber: { $ref: "#/components/schemas/BezugsdatenVorlageUrheber" }
        erstellt_am: { type: string, format: date-time }
    BezugsdatenVorlageListe:
      type: object
      required: [vorlagen]
      properties:
        vorlagen: { type: array, items: { $ref: "#/components/schemas/BezugsdatenVorlage" } }
    BezugsdatenVorlageVerweis:
      type: object
      required: [vorlage_id, fassung, name]
      properties:
        vorlage_id: { type: string, format: uuid }
        fassung: { type: integer, minimum: 1 }
        name: { type: string }
    BezugsdatenImportVorschau:
      type: object
      required: [vorschau, vorlage, datei, frueherer_import, zeilen, import]
      properties:
        vorschau:
          type: object
          required: [kennung, status, ausgestellt_am, gueltig_bis, ergebnis_fingerabdruck]
          properties:
            kennung: { type: string, pattern: "^VS1\\.[0-9]+\\.[0-9a-f]{32}$" }
            status: { type: string, enum: [vorschau] }
            ausgestellt_am: { type: string, format: date-time }
            gueltig_bis: { type: string, format: date-time, description: "ausgestellt_am + 30 Minuten." }
            ergebnis_fingerabdruck: { type: string }
        vorlage:
          allOf: [{ $ref: "#/components/schemas/BezugsdatenVorlageVerweis" }]
          nullable: true
          description: "Die benutzte Fassung; ohne `vorlage_id` null. Ein Import speichert genau diesen Verweis."
        datei:
          type: object
          required: [name, bytes, sha256, befund, zusatz, zusatz_satz, zeile, kodierung, bom, trennzeichen, kopfzeile, kopf, spalten, datenzeilen]
          properties:
            name: { type: string, nullable: true }
            bytes: { type: integer }
            sha256: { type: string }
            befund: { allOf: [{ $ref: "#/components/schemas/BezugsdatenBefund" }], nullable: true }
            zusatz: { type: string, nullable: true }
            zusatz_satz: { type: string, nullable: true }
            zeile: { type: integer, nullable: true }
            kodierung: { type: string, nullable: true }
            bom: { type: boolean, nullable: true }
            trennzeichen: { type: string, nullable: true }
            kopfzeile: { type: boolean, nullable: true }
            kopf: { type: array, nullable: true, items: { type: string } }
            spalten: { type: integer, nullable: true }
            datenzeilen: { type: integer, nullable: true }
        frueherer_import:
          type: object
          nullable: true
          required: [kennung, status, am]
          properties:
            kennung: { type: string }
            status: { type: string }
            am: { type: string, format: date-time }
        zeilen: { type: array, items: { $ref: "#/components/schemas/BezugsdatenVorschauZeile" } }
        import:
          type: object
          required: [status, zaehler, uebernahme_moeglich, import_datensatz, bestaetigung, aenderungen, befunde]
          properties:
            status: { type: string, nullable: true }
            zaehler:
              type: object
              required: [zeilen, neu, wiederholung, konflikt, berichtigung, uebersprungen, abgelehnt, mit_hinweis]
              properties:
                zeilen: { type: integer }
                neu: { type: integer }
                wiederholung: { type: integer }
                konflikt: { type: integer }
                berichtigung: { type: integer }
                uebersprungen: { type: integer }
                abgelehnt: { type: integer }
                mit_hinweis: { type: integer }
            uebernahme_moeglich: { type: boolean }
            import_datensatz: { type: boolean }
            bestaetigung: { type: string, nullable: true }
            aenderungen: { type: integer }
            befunde: { type: array, items: { $ref: "#/components/schemas/BezugsdatenBefund" } }
    BezugsdatenVorschauZeile:
      type: object
      required: [nr, felder, bezugsgroesse, bezugsgroesse_id, schluessel, periode_von, periode_bis, zeitpunkt, betrag, einheit, geliefert, urteil, befunde, fingerabdruck, bestand]
      properties:
        nr: { type: integer, description: "Die Zeile der Datei." }
        felder: { type: array, items: { type: string }, description: "Zur Anzeige neutralisiert (C1)." }
        bezugsgroesse: { type: string, nullable: true }
        bezugsgroesse_id: { type: string, format: uuid, nullable: true }
        schluessel: { type: string, nullable: true }
        periode_von: { type: string, format: date, nullable: true }
        periode_bis: { type: string, format: date, nullable: true, description: "Letzter Tag EINSCHLIESSLICH." }
        zeitpunkt: { type: string, format: date-time, nullable: true }
        betrag: { type: string, nullable: true, description: "Dezimaltext in der Einheit der Bezugsgröße." }
        einheit: { type: string, nullable: true }
        geliefert:
          type: object
          required: [wert, einheit]
          properties:
            wert: { type: string, nullable: true }
            einheit: { type: string, nullable: true }
        urteil: { type: string, enum: [neu, wiederholung, konflikt, berichtigung, uebersprungen, abgelehnt] }
        befunde: { type: array, items: { $ref: "#/components/schemas/BezugsdatenBefund" } }
        fingerabdruck: { type: string, nullable: true }
        bestand:
          type: object
          nullable: true
          required: [betrag, fassung, import_kennung]
          properties:
            betrag: { type: string }
            fassung: { type: integer }
            import_kennung: { type: string, nullable: true }
    MessstelleWerte:
      type: object
      description: Das Lese-Modell „Werte je Messstelle“ (AP-08 IP-9). Jede Zahl steht in `messstelle.einheit`.
      required: [messstelle, raster, von, bis, zeitzone, zeitzone_herkunft, version, quellen, werte, zuordnung]
      properties:
        messstelle: { $ref: "#/components/schemas/MessstelleWerteMessstelle" }
        raster: { type: string, enum: [viertelstunde, stunde, tag, monat, jahr] }
        von: { type: string, description: "ISO-8601 mit Versatz in der Zeitzone des Standorts." }
        bis: { type: string, description: "ISO-8601 mit Versatz, ausschließlich." }
        zeitzone: { type: string, enum: [Europe/Berlin, Europe/Vienna, Europe/Zurich] }
        zeitzone_herkunft: { type: string, enum: [standort, unternehmen, vorgabe] }
        version: { type: integer, nullable: true }
        quellen: { type: array, items: { $ref: "#/components/schemas/MessstelleWerteQuelle" } }
        werte: { type: array, items: { $ref: "#/components/schemas/MessstelleWerteWert" } }
        zuordnung:
          type: string
          nullable: true
          enum: [nicht_zugeordnet]
          description: >
            nicht_zugeordnet, wenn im Zeitraum an der Box gute Werte einer führenden Bindung ankamen, die Reihe
            der Messstelle aber keinen trägt (der Komponente fehlt die Datenquelle) - dasselbe Wort und dieselben
            zwei Kriterien wie `MessstelleRegisterBeobachtung.zuordnung`. Kein Schritt, keine Zahl und kein
            `grund` ändern sich; sonst null. An der Woche und an einer Messstelle mit Ablesungen immer null.
        ausserhalb_zugriff:
          type: string
          description: "NUR an einer BERECHNETEN Messstelle, von deren Eingängen im Zeitraum einer (auch über eine berechnete Messstelle, einen Rest oder als Komponente eines Messkanal-Terms) außerhalb des Zugriffs des Lesers liegt (AP-03 R-A3/R-A6): dann ist `werte` leer - keine Zahl, keine Abdeckung, keine Version. Ohne Namen, ohne Anzahl."
          example: "umfasst Standorte außerhalb Ihres Zugriffs"
    MessstelleWerteMessstelle:
      type: object
      required: [id, kennzeichen, name, art, groesse, richtung, einheit, wertart]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string }
        name: { type: string, nullable: true }
        art: { type: string }
        groesse: { type: string }
        richtung: { type: string }
        einheit: { type: string }
        wertart: { type: string }
    MessstelleWerteQuelle:
      type: object
      description: Eine FÜHRENDE Bindung der Hauptgröße, die den Zeitraum berührt.
      required: [id, komponente, kanal, herleitung, anteil, gueltig_ab, gueltig_bis]
      properties:
        id: { type: string, format: uuid }
        komponente: { type: string, format: uuid }
        kanal: { type: string }
        herleitung: { type: string, enum: [zaehlerstand, differenzen, integration, momentanwert] }
        anteil: { type: string, enum: [positiv, negativ], nullable: true }
        gueltig_ab: { type: string }
        gueltig_bis: { type: string, nullable: true }
    MessstelleWerteWert:
      type: object
      description: >
        Ein Schritt des Rasters. `menge` trägt ein Zählerstand und eine Integration (nur mit
        dem Kennzeichen „aus Leistung integriert“), `mittel`/`min`/`max` ein Momentanwert - nie
        beides. Ungerundet; gerundet wird nur angezeigt (E11).
      required: [von, bis, beschriftung, stunden, tagesdauer, menge, mittel, min, max, zustand, kennzeichen,
        erhalten, erwartet, abdeckung_prozent, fassung, endgueltig_ab, version, gebildet_aus, quelle, grund, ereignisse,
        herkunft, versionen]
      properties:
        von: { type: string }
        bis: { type: string }
        beschriftung: { type: string, nullable: true, description: "Viertelstunde und Stunde: die Beschriftung aus ErgebnisZustand.raster (E10, „02:00–03:00 MESZ“)." }
        stunden: { type: integer, nullable: true, description: "Tag, Monat, Jahr: die Stunden der Periode (23 · 24 · 25 am Tag)." }
        tagesdauer: { type: string, nullable: true, description: "Tag: ErgebnisZustand.tagesdauer („25 Stunden (Zeitumstellung)“), an einem 24-Stunden-Tag null." }
        menge: { type: number, nullable: true }
        mittel: { type: number, nullable: true }
        min: { type: number, nullable: true }
        max: { type: number, nullable: true }
        zustand: { type: string, nullable: true, enum: [vollständig, unvollständig, keine Werte, mit Ersatzwert] }
        kennzeichen: { type: array, items: { type: string }, description: "Die gespeicherten Sätze der geschlossenen Liste, Wortlaut und Reihenfolge wie im Vertrag." }
        erhalten: { type: integer, nullable: true }
        erwartet: { type: integer, nullable: true }
        abdeckung_prozent: { type: integer, nullable: true, description: "Abgeschnitten, nie auf 100 gerundet." }
        fassung: { type: string, nullable: true, enum: [vorlaeufig, endgueltig] }
        endgueltig_ab: { type: string, nullable: true }
        version: { type: integer, nullable: true, description: "Die Version, deren Zahl der Schritt zeigt - ohne Anfrage die neueste (AP-08 IP-18)." }
        gebildet_aus: { type: string, nullable: true, enum: [viertelstunde, zeitraum, tag, monat, jahr] }
        quelle: { type: string, format: uuid, nullable: true, description: "Die Bindung, deren Reihe den Schritt beantwortet." }
        grund:
          type: string
          nullable: true
          enum: [keine_quelle, quelle_teilweise, anteil_nicht_gespeichert, berechnet, noch_nicht_gebildet,
            ohne_menge_gespeichert, version_nicht_gespeichert, version_nicht_gebildet]
          description: Warum der Schritt keine Zahl trägt, obwohl die Anfrage gültig ist.
        ereignisse: { type: array, items: { $ref: "#/components/schemas/MessstelleWerteEreignis" } }
        herkunft:
          type: object
          nullable: true
          description: "AP-10 IP-12: {satz, fehlt} nach bilanzwert-herkunft.schema.json an jeder BERECHNETEN Zahl (Spur berechnet, Eingänge aus bilanzwert_eingang). null = nicht berechnet: ein gemessener Schritt oder einer ohne Zahl (grund gesetzt)."
          additionalProperties: true
        versionen: { type: integer, nullable: true, description: "AP-08 IP-18, die NAHT der Portal-Fläche „Versionen“ am Wert: wie viele Versionen die Periode hat (ab 2 gibt es eine Historie unter …/werte/versionen). null an der Stunde (keine eigenen Versionen), ohne Reihe und wo nichts gebildet ist." }
    MessstelleWerteHistorie:
      type: object
      description: Die Versions-Historie EINER Periode (AP-08 IP-18). `grund` nur, wenn die Periode keine Versionen hat.
      required: [messstelle, raster, von, bis, zeitzone, zeitzone_herkunft, grund, versionen]
      properties:
        messstelle: { $ref: "#/components/schemas/MessstelleWerteMessstelle" }
        raster: { type: string, enum: [viertelstunde, tag, monat, jahr] }
        von: { type: string }
        bis: { type: string }
        zeitzone: { type: string }
        zeitzone_herkunft: { type: string }
        grund: { type: string, nullable: true, description: "Ein Wort von MessstelleWerteWert.grund - warum die Periode keine Versionen hat." }
        versionen: { type: array, items: { $ref: "#/components/schemas/MessstelleWerteVersion" } }
        ausserhalb_zugriff:
          type: string
          description: "NUR wie an MessstelleWerte.ausserhalb_zugriff: ein Eingang der berechneten Messstelle liegt in der Periode außerhalb des Zugriffs - dann ist `versionen` leer und `grund` null. Ohne Namen, ohne Anzahl."
          example: "umfasst Standorte außerhalb Ihres Zugriffs"
    MessstelleWerteVersion:
      type: object
      required: [version, wert_alt, wert_neu, gebildet_am, nachgezogen_am, anlass, entscheidungen]
      properties:
        version: { type: integer, minimum: 1 }
        wert_alt: { allOf: [{ $ref: "#/components/schemas/MessstelleWerteWert" }], nullable: true, description: "Was vorher dastand: Version n − 1 wie …/werte?version=n-1; an Version 1 null." }
        wert_neu: { $ref: "#/components/schemas/MessstelleWerteWert" }
        gebildet_am: { type: string, nullable: true, description: "Wann die Zeile geschrieben wurde (Version 1: von der Verdichtung; ohne Zeile null)." }
        nachgezogen_am: { type: string, nullable: true, description: "Eine vorläufige Version wächst mit ihrer Grundlage (IP-17) - wann zuletzt." }
        anlass: { allOf: [{ $ref: "#/components/schemas/MessstelleWerteAnlass" }], nullable: true }
        entscheidungen: { type: array, items: { $ref: "#/components/schemas/MessstelleWerteEntscheidung" } }
    MessstelleWerteAnlass:
      type: object
      required: [kennung, fassung]
      properties:
        kennung: { type: string, example: EW-2026-0003 }
        fassung: { type: integer, minimum: 1 }
    MessstelleWerteEntscheidung:
      type: object
      required: [vorgang, kennung, fassung, status, methode, art, wer, wann, warum, beleg, fehlt, angelegt]
      properties:
        vorgang: { type: string, enum: [ersatzwert, korrektur] }
        kennung: { type: string }
        fassung: { type: integer, minimum: 1 }
        status: { type: string, nullable: true }
        methode: { type: string, nullable: true }
        art: { type: string, nullable: true }
        wer: { allOf: [{ $ref: "#/components/schemas/MessstelleWerteUrheber" }], nullable: true }
        wann: { type: string, nullable: true }
        warum: { type: string, nullable: true, description: "Der Text dieser Fassung (Begründung bzw. Grund) - null, wenn keiner geschrieben wurde." }
        beleg: { type: string, nullable: true }
        fehlt: { type: array, items: { type: string, enum: [warum, fassung] } }
        angelegt: { allOf: [{ $ref: "#/components/schemas/MessstelleWerteAngelegt" }], nullable: true }
    MessstelleWerteUrheber:
      type: object
      required: [name, rolle, art]
      properties:
        name: { type: string }
        rolle: { type: string, nullable: true }
        art: { type: string }
    MessstelleWerteAngelegt:
      type: object
      required: [wer, wann, warum, beleg]
      properties:
        wer: { $ref: "#/components/schemas/MessstelleWerteUrheber" }
        wann: { type: string }
        warum: { type: string, nullable: true }
        beleg: { type: string, nullable: true }
    MessstelleWerteVersionGibtEsNicht:
      type: object
      description: Die angefragte Version gibt es an keinem Schritt der Anfrage (AP-08 IP-18).
      required: [code, message, feld, grund, version, hoechste_version]
      properties:
        code: { type: string, enum: [version_gibt_es_nicht] }
        message: { type: string }
        feld: { type: string, enum: [version] }
        grund: { type: string, enum: [version_gibt_es_nicht] }
        version: { type: integer }
        hoechste_version: { type: integer }
    MessstelleWerteNichtMehrGespeichert:
      type: object
      description: >
        Die EINE Periode der Anfrage (Monat oder Jahr) ist nach den Fristen nicht mehr gespeichert (AP-12 IP-16,
        Bericht-Vertrag S4/B16) - `message` ist der Satz des Bericht-Vertrags, `stand` der früheste freigegebene
        Berichtsstand, der genau diese Periode in Version 1 zitiert, sonst null.
      required: [code, message, zeitraum, stand]
      properties:
        code: { type: string, enum: [wert_nicht_mehr_gespeichert] }
        message: { type: string, example: "Der Wert vom Oktober 2026 wird nicht mehr gespeichert (Aufbewahrung 10 Jahre). Der Berichtsstand Nr. 1 vom 10.11.2026 hält ihn fest." }
        zeitraum:
          type: object
          required: [art, schluessel]
          properties:
            art: { type: string, enum: [monat, jahr] }
            schluessel: { type: string, example: "2026-10" }
        stand:
          type: object
          nullable: true
          required: [nr, freigegeben_am]
          properties:
            nr: { type: integer, minimum: 1 }
            freigegeben_am: { type: string, format: date-time }
    MessstelleWerteEreignis:
      type: object
      description: Ein Verweis auf eine Meldung des Ereignis-Vertrags - der Inhalt bleibt dort.
      required: [id, art, von, bis]
      properties:
        id: { type: string, format: uuid }
        art: { type: string }
        von: { type: string }
        bis: { type: string, nullable: true }
    MessstelleWerteFehler:
      type: object
      required: [code, message, feld, grund]
      properties:
        code: { type: string, enum: [anfrage_ungueltig] }
        message: { type: string }
        feld: { type: string, enum: [raster, von, bis, version] }
        grund:
          type: string
          enum: [fehlt, raster_unbekannt, form, nicht_im_raster, von_nicht_vor_bis, ausserhalb, zu_viele_schritte,
            version_ungueltig, raster_ohne_versionen, nicht_genau_eine_periode]
    MessstelleFormelFehler:
      type: object
      description: >
        Die Ablehnung der Formel-Schnittstelle (Vertrag messstelle-formel.md §5 und §6):
        `anfrage_ungueltig` 400 (`feld`), `groessen_gemischt` 422 (`grund`: groesse ·
        wertart · richtung), `formel_zyklus` 422 (`kette`), `formel_fassung_ueberlappt` 422
        (`fassung`, `gueltig_ab` der jüngsten Fassung). AP-10 IP-5 (Vertrag verteilung-vectors.json,
        Block `leseweg`, mit Kundensatz): `verteilungs_term_ohne_faktor` 422 (`feld`),
        `anteil_wartet_auf_ap08` 422 (`feld`, `wartet_auf`) - der Teil eines Messwerts ist noch
        nicht lesbar, der Term wird nicht gespeichert, nichts wird geschätzt. Die Ablehnung
        `verteilung_wartet_auf_ip8` ist mit AP-10 IP-8 eingelöst.
      required: [code, message]
      properties:
        code: { type: string, enum: [anfrage_ungueltig, groessen_gemischt, formel_zyklus, formel_fassung_ueberlappt, verteilungs_term_ohne_faktor, anteil_wartet_auf_ap08] }
        message: { type: string }
        feld: { type: string }
        wartet_auf: { type: string, example: "AP-08 IP-7" }
        grund: { type: string }
        kette: { type: array, items: { type: string } }
        fassung: { type: integer }
        gueltig_ab: { type: string, format: date, nullable: true }
    KadenzVorgang:
      type: object
      required: [fassung]
      properties:
        fassung: { $ref: "#/components/schemas/KadenzFassung" }
        beendet:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/KadenzFassung" }]
        rueckwirkung: { $ref: "#/components/schemas/MessstelleQuelleRueckwirkung" }
    MessstelleFehler:
      type: object
      description: >
        Die Ablehnung der Messstellen-Schnittstelle. Die Codes des Regel-Vertrags
        (docs/contracts/v2/messstelle.md §7) tragen dessen Status - aus dieser
        Schnittstelle heute `kennzeichen_format` 400, `kennzeichen_belegt` 409
        (`bestehend`), `groesse_ungueltig` 400 (`grund`, `feld`),
        `zeitpunkt_vor_vorgaenger` 422 (`vorgaenger` oder `beginn`); aus den
        Zuordnungs-Routen (IP-7) `ort_ungueltig` 422 (`grund`, `gueltig_ab`),
        `stellung_ungueltig` 422 (`grund`, `tag`, `bestehend`, `kette` oder
        `betroffen`) und `hauptzaehler_vorhanden` 409 (`bestehend`, `tag`); aus der
        Quellenbindung (IP-13) `medium_ohne_quelle` 422, `vergleich_ohne_zweck` 400,
        `quelle_passt_nicht` 422 (`grund` wertart · groesse · einheit · richtung · anteil, `kanal`),
        `zeitraum_ungueltig` 400, `kein_geraet_zum_zeitpunkt` 422 (`zeitpunkt`,
        `komponente`), `kanal_bereits_fuehrend` 409 (`bestehende_messstelle`),
        `bindung_ueberlappt` 409 (`bestehend`) und `bindung_bereits_beendet` 409
        (`gueltig_ab`, `gueltig_bis`); `anteile_summe` antworten die Kostenstellen-Anteile.
        Dazu die Codes der Schnittstelle: `anfrage_ungueltig` 400 (`feld` - fehlt,
        falsche Form, gibt es hier nicht), `zustand_passt_nicht` 409
        (`angehalten_ab`, `archiviert_am`), `zeitpunkt_in_zukunft` 422
        (`zeitpunkt`), `zuordnung_ueberlappt` 409 (an dem Tag beginnt schon eine
        Zuordnung - `grund: gleicher_tag`, `gueltig_ab`), `zuordnung_unveraendert` 400
        (schon genau so zugeordnet - `grund: ziel_ist_bisheriger_eltern`) und
        `zuordnung_ungueltig` 422 (`grund` des Ortsbaum-Vertrags:
        vor_dem_ersten_intervall · objekt_archiviert · kein_beginn_an_dem_tag) und
        `vorschlag_geaendert` 409 (die bestätigte Zeile der Vorschlagsliste gibt es
        so nicht mehr - `komponente`, `kanal`; IP-16). Den Satz baut die
        Fläche später aus den Fakten (IP-6); `message` ist der Wortlaut von AP-04
        §5.12, wo es ihn gibt.
      required: [code, message]
      properties:
        code:
          type: string
          enum: [kennzeichen_format, kennzeichen_belegt, groesse_ungueltig, medium_ohne_quelle,
            vergleich_ohne_zweck, quelle_passt_nicht, kanal_bereits_fuehrend, zeitraum_ungueltig,
            zeitpunkt_vor_vorgaenger, bindung_ueberlappt, hauptzaehler_vorhanden, stellung_ungueltig,
            anteile_summe, ort_ungueltig, kein_geraet_zum_zeitpunkt, bindung_bereits_beendet,
            anfrage_ungueltig, zustand_passt_nicht, zeitpunkt_in_zukunft,
            zuordnung_ueberlappt, zuordnung_ungueltig, zuordnung_unveraendert, vorschlag_geaendert]
        message: { type: string }
        feld: { type: string, description: "anfrage_ungueltig, groesse_ungueltig: hauptgroesse, nebengroessen[1], zeitpunkt, gueltig_ab, anlage, stellung …" }
        grund:
          type: string
          enum: [groesse, medium, einheit, richtung, wertart,
            unbekannt, unternehmen_nur_berechnet, ziel_art_unzulaessig, ziel_gab_es_noch_nicht, ziel_archiviert,
            nicht_elektrisch, bezug_nur_bei_unterzaehler, bezug_fehlt, selbst, fremde_anlage, zyklus,
            vor_dem_ersten_intervall, objekt_archiviert, gleicher_tag, kein_beginn_an_dem_tag,
            ziel_ist_bisheriger_eltern]
          description: >
            groesse_ungueltig: das Merkmal; ort_ungueltig: unbekannt ·
            unternehmen_nur_berechnet · ziel_*; stellung_ungueltig: die Gründe von
            Vertrag §6; zuordnung_*: der Grund der Tages-Mechanik.
        bestehend:
          description: >
            kennzeichen_belegt: wer es trägt (frueher false) oder trug (frueher true).
            hauptzaehler_vorhanden, stellung_ungueltig (fremde_anlage): die bestehende
            Messstelle mit ihrer Anlage am Tag.
          oneOf:
            - type: object
              required: [kennzeichen, messstelle, name, archiviert, frueher]
              properties:
                kennzeichen: { type: string }
                messstelle: { type: string, description: "Das HEUTIGE Kennzeichen des Trägers." }
                name: { type: string, nullable: true }
                archiviert: { type: boolean }
                frueher: { type: boolean }
            - $ref: "#/components/schemas/MessstelleImBaum"
        betroffen:
          description: "stellung_ungueltig: die Messstelle, die Unterzähler der geänderten ist und dadurch in eine fremde Anlage zeigte."
          allOf: [{ $ref: "#/components/schemas/MessstelleImBaum" }]
        kette: { type: array, items: { type: string }, description: "stellung_ungueltig (zyklus): der Kreis, von der Messstelle zurück zu ihr." }
        tag: { type: string, format: date, description: "Der erste Tag, an dem Regel 8 verletzt wäre." }
        gueltig_ab: { type: string, format: date }
        beginn: { type: string, format: date-time, description: "zeitpunkt_vor_vorgaenger: der Beginn der Messstelle." }
        vorgaenger:
          type: object
          description: "zeitpunkt_vor_vorgaenger: der letzte Übergang, nach dem der neue liegen muss."
          properties:
            art: { type: string, enum: [angehalten, fortgesetzt] }
            gilt_ab: { type: string, format: date-time }
        zeitpunkt: { type: string, format: date-time }
        angehalten_ab: { type: string, format: date-time, nullable: true }
        archiviert_am: { type: string, format: date-time, nullable: true }
      example:
        code: kennzeichen_belegt
        message: "MS-13 ist bereits vergeben (Lager Halle 2 (Allgemein)). Kennzeichen sind je Unternehmen eindeutig — auch archivierte bleiben belegt."
        bestehend: { kennzeichen: MS-13, messstelle: MS-13, name: Lager Halle 2 (Allgemein), archiviert: true, frueher: false }
    MessstelleImBaum:
      type: object
      description: Eine Messstelle mit der Anlage, in der sie am Tag steht.
      required: [kennzeichen, name, anlage]
      properties:
        kennzeichen: { type: string, example: MS-01 }
        name: { type: string, nullable: true, example: Netzbezug Halle 1 }
        anlage: { type: string, format: uuid }
    DatenquelleBox:
      type: object
      description: Eine Box, wie ein Satz sie nennt - Kundenname, sonst die Geräte-ID vom Aufkleber; null bei einer entfernten Box.
      required: [id, name, heimat_anlage]
      properties:
        id: { type: string, format: uuid }
        name: { type: string, nullable: true }
        heimat_anlage: { type: string, format: uuid, nullable: true }
    DatenquelleZeitraum:
      type: object
      description: Box · ab · bis, halboffen auf die Minute; `effective_to` null = offen.
      required: [id, box, effective_from, effective_to]
      properties:
        id: { type: string, format: uuid, description: Kennung für die Rücknahme eines noch nicht wirksamen Plans. }
        box: { $ref: "#/components/schemas/DatenquelleBox" }
        effective_from: { type: string, format: date-time }
        effective_to: { type: string, format: date-time, nullable: true }
    Datenquelle:
      type: object
      required: [id, kennzeichen, name, anlage, protokoll, adresse, geraete_ids, netz, mehrere_leser,
        steuerquelle, vergleichsquelle, kadenz_s, archiviert_am, zustaendige_box, zeitraeume, uebergabe,
        rueckmeldung]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, pattern: "^[A-Z0-9./-]{2,16}$", example: DQ-6 }
        name: { type: string, nullable: true }
        anlage: { type: string, format: uuid, description: "Die Anlage, in der die Geräte verdrahtet sind." }
        protokoll: { type: string, enum: [modbus_tcp, sunspec_modbus, mqtt, http, ocpp, solarman_v5] }
        adresse: { type: string, example: "192.168.30.10:502" }
        geraete_ids: { type: array, items: { type: integer, minimum: 0 } }
        netz: { type: string, nullable: true }
        mehrere_leser: { type: boolean }
        steuerquelle: { type: boolean }
        vergleichsquelle: { type: boolean, description: "Bestätigte Vergleichsquelle (Vertrag §6) - gekennzeichnet, ohne Bewertung." }
        kadenz_s: { type: integer, minimum: 1, maximum: 86400, nullable: true, description: "null = nicht erhoben (eine aus dem Bestand übernommene Quelle, deren Komponenten keinen Takt nennen, IP-4)." }
        archiviert_am: { type: string, format: date-time, nullable: true }
        zustaendige_box:
          allOf: [{ $ref: "#/components/schemas/DatenquelleBox" }]
          nullable: true
          description: Die Box, deren Zeitraum JETZT läuft; null ohne (Entwurf, Lücke, erst geplant).
        zeitraeume: { type: array, items: { $ref: "#/components/schemas/DatenquelleZeitraum" } }
        uebergabe:
          allOf: [{ $ref: "#/components/schemas/DatenquelleUebergabe" }]
          nullable: true
          description: Ausführung getrennt vom Zeitraum; null ohne ausstehende Übergabe.
        rueckmeldung:
          allOf: [{ $ref: "#/components/schemas/DatenquelleRueckmeldung" }]
          nullable: true
          description: >
            Jüngste Rückmeldung der zuständigen Box; null nur ohne zuständige Box. Eine
            Bestandsbox ohne Quellblock meldet den ehrlichen Zustand meldet_noch_nicht_je_quelle.
    DatenquelleRueckmeldung:
      type: object
      required: [zustand, fehlerklasse, seit, gelesen_am, anfragen_pro_minute,
        messwerte_pro_minute, gemeldet_am, text]
      properties:
        zustand: { type: string, enum: [liefert, liefert_nicht, meldet_noch_nicht_je_quelle] }
        fehlerklasse: { type: string, nullable: true, description: "Geschlossenes Vokabular des Quellstatus-Vertrags; das Portal zeigt Kundenwörter." }
        seit: { type: string, format: date-time, nullable: true }
        gelesen_am: { type: string, format: date-time, nullable: true }
        anfragen_pro_minute: { type: number, format: double, nullable: true }
        messwerte_pro_minute: { type: number, format: double, nullable: true }
        gemeldet_am: { type: string, format: date-time, nullable: true }
        text: { type: string }
    DatenquelleUebergabe:
      type: object
      required: [zustand, seit, box_alt, box_neu]
      properties:
        zustand: { type: string, enum: [Übergabe ausstehend] }
        seit: { type: string, format: date-time }
        box_alt: { $ref: "#/components/schemas/DatenquelleBox" }
        box_neu: { $ref: "#/components/schemas/DatenquelleBox" }
    DatenquelleListe:
      type: object
      required: [datenquellen]
      properties:
        datenquellen: { type: array, items: { $ref: "#/components/schemas/Datenquelle" } }
    DatenquelleAnlegen:
      type: object
      required: [name, protokoll, adresse, kadenz_s]
      properties:
        name: { type: string, maxLength: 120 }
        protokoll: { type: string, enum: [modbus_tcp, sunspec_modbus, mqtt, http, ocpp, solarman_v5] }
        adresse: { type: string }
        geraete_ids: { type: array, items: { type: integer, minimum: 0 } }
        netz: { type: string, nullable: true }
        mehrere_leser: { type: boolean, default: false }
        steuerquelle: { type: boolean, default: false }
        kadenz_s: { type: integer, minimum: 1, maximum: 86400 }
        device_id: { type: string, format: uuid, description: "Die gewählte Box - prüft die Regeln vor der Prüfung schon beim Anlegen." }
        vergleich_bestaetigt: { type: boolean, default: false }
    DatenquelleBearbeiten:
      type: object
      description: Die volle Darstellung (fehlend = leer bzw. nein).
      required: [name, protokoll, adresse, kadenz_s]
      properties:
        name: { type: string, maxLength: 120 }
        protokoll: { type: string, enum: [modbus_tcp, sunspec_modbus, mqtt, http, ocpp, solarman_v5] }
        adresse: { type: string }
        geraete_ids: { type: array, items: { type: integer, minimum: 0 } }
        netz: { type: string, nullable: true }
        mehrere_leser: { type: boolean, default: false }
        steuerquelle: { type: boolean, default: false }
        kadenz_s: { type: integer, minimum: 1, maximum: 86400 }
    DatenquelleZuweisen:
      type: object
      required: [device_id]
      properties:
        device_id: { type: string, format: uuid }
        effective_from: { type: string, format: date-time, description: "Volle Minute mit Versatz; fehlend = jetzt (auf die Minute abgerundet)." }
        vergleich_bestaetigt: { type: boolean, default: false }
    DatenquelleZugewiesen:
      type: object
      required: [urteil, text, hinweis, vergleichsquelle, datenquelle]
      properties:
        urteil: { type: string, enum: [erlaubt] }
        text: { type: string, example: "Ab 10.04.2027 07:30 liest Box Halle 2 (neu)" }
        hinweis: { type: string, nullable: true, example: "Gleiche Adresse wie DQ-4 — anderes Netz" }
        vergleichsquelle: { type: boolean }
        datenquelle: { $ref: "#/components/schemas/Datenquelle" }
    DatenquellePruefen:
      type: object
      required: [device_id, unit_id]
      properties:
        device_id: { type: string, format: uuid }
        unit_id: { type: integer, minimum: 0, maximum: 255 }
        register: { type: integer, minimum: 0, maximum: 65535, description: "Bei Modbus TCP Pflicht, bei SunSpec-Modbus 40000." }
        register_kind: { type: string, enum: [holding, input], default: holding }
        data_type:
          type: string
          enum: [u16, s16, u32, s32, float32, uint16, int16, uint32, int32]
          description: "Das Wort des Probe-Vertrags oder die Katalog-Schreibweise (uint16 → u16, int16 → s16, uint32 → u32, int32 → s32); zur Box geht immer das Vertragswort. Bei op wago_kopf nicht erlaubt (400)."
        word_order: { type: string, enum: [big, little], description: "Bei op wago_kopf mit Vorgabe big." }
        op:
          type: string
          enum: [read, wago_kopf]
          default: read
          description: "read = EIN Lese-Schritt; wago_kopf = der Kopf eines VoltPilot-Registerbilds WAGO v1 an der Basisadresse register (AP-05)."
    DatenquellePruefergebnis:
      type: object
      required: [box, adresse, ergebnis, gewertet, text, zeitpunkt, dauer_ms, antwort]
      properties:
        box: { $ref: "#/components/schemas/DatenquelleBox" }
        adresse: { type: string }
        ergebnis:
          type: string
          nullable: true
          description: "ok, eine Fehlerklasse der Box (unreachable, no_answer, invalid_response, implausible, fronius_api, timeout, layout_changed, budget), box_meldet_sich_nicht oder ein Wort des Prüf-Kanals."
        gewertet: { type: boolean, description: "Zählt für eine Zuständigkeit und steht im Protokoll." }
        text: { type: string, example: "Box Halle 2 (neu) erreicht 192.168.10.31:502 nicht — Netz/VLAN prüfen (Bogen D1/D2)" }
        zeitpunkt: { type: string, format: date-time }
        dauer_ms: { type: integer, minimum: 0 }
        antwort:
          allOf: [{ $ref: "#/components/schemas/ProbeResult" }]
          nullable: true
          description: Die rohe Antwort der Box; null, wenn sie nicht geantwortet hat.
        wago: { $ref: "#/components/schemas/DatenquelleWagoPruefung" }
    DatenquelleWagoPruefung:
      type: object
      description: >
        Nur bei op wago_kopf (fehlt sonst): was die Steuerung über ihr Registerbild gemeldet
        hat — nur Gelesenes, Lücke statt Null. Kennung, Kartenzahl und Karten stehen nur bei
        einem erkannten v1-Kopf da; kein v1-Kopf und eine stumme Box sind ehrliche Ausgänge
        (erkannt false, ohne Karte).
      required: [erkannt, satz]
      properties:
        erkannt: { type: boolean }
        satz: { type: string, example: "Registerbild v1 erkannt — Controller-Kennung 8212, 4 Energiekarten." }
        controller_kennung: { type: integer, format: int64, minimum: 0, maximum: 4294967295 }
        kartenzahl: { type: integer, minimum: 0, maximum: 65535, description: "Wie viele Karten der Kopf meldet." }
        karten:
          type: array
          items:
            type: object
            required: [karte, steckplatz, kartentyp, variante]
            properties:
              karte: { type: integer, minimum: 1, description: "n = der n-te Kartenblock." }
              steckplatz: { type: integer, nullable: true, minimum: 0, maximum: 65535 }
              kartentyp: { type: integer, nullable: true, minimum: 0, maximum: 65535, description: "Artikelnummer ohne „750-“." }
              variante: { type: integer, nullable: true, minimum: 0, maximum: 65535 }
    DatenquelleProtokollEintrag:
      type: object
      required: [id, art, box, ergebnis, alt, neu, gilt_ab, urheber, zeitpunkt]
      properties:
        id: { type: integer, format: int64 }
        art: { type: string, enum: [angelegt, bearbeitet, erreichbarkeit_geprueft, zustaendigkeit_begonnen, zustaendigkeit_gewechselt, aus_bestand_uebernommen] }
        box:
          allOf: [{ $ref: "#/components/schemas/DatenquelleBox" }]
          nullable: true
        ergebnis: { type: string, nullable: true }
        alt: { type: object, nullable: true, additionalProperties: true }
        neu: { type: object, nullable: true, additionalProperties: true }
        gilt_ab: { type: string, format: date-time }
        urheber:
          type: object
          required: [name, rolle, art]
          properties:
            name: { type: string }
            rolle: { type: string, nullable: true }
            art: { type: string, enum: [kunde, unterstuetzung, voltpilot, notfall] }
        zeitpunkt: { type: string, format: date-time }
    DatenquelleProtokoll:
      type: object
      required: [eintraege]
      properties:
        eintraege: { type: array, items: { $ref: "#/components/schemas/DatenquelleProtokollEintrag" } }
    DatenquelleVorschlagKomponente:
      type: object
      required: [id, name, art]
      properties:
        id: { type: string, format: uuid }
        name: { type: string, nullable: true, description: "Der Kundenname der Komponente; null = keiner vergeben." }
        art: { type: string, nullable: true, example: battery-hybrid }
    DatenquelleVorschlag:
      type: object
      description: >
        EINE Quelle, die `box` ab `ab` (dem Reihenbeginn) ohnehin liest. `kennzeichen`
        bekäme sie, wenn alles (oder der Anfang der Liste) bestätigt wird - vergeben wird
        es erst beim Speichern. `grund` null: übernehmbar; sonst der Grund des Vertrags
        (`adresse_an_box_vergeben`), `text` der Satz dazu. `ziel`: bei
        `adresse_an_box_vergeben` die EINE vorhandene Quelle derselben Anlage, an die die
        Bestätigung mit `datenquelle_id` die Komponenten hängt („Gerät dort hinzufügen?“), sonst null.
      required: [kennzeichen, box, protokoll, adresse, geraete_ids, kadenz_s, steuerquelle, ab,
        komponenten, grund, text, ziel]
      properties:
        kennzeichen: { type: string, example: DQ-3 }
        box: { $ref: "#/components/schemas/DatenquelleBox" }
        protokoll: { type: string, enum: [modbus_tcp, sunspec_modbus, mqtt, http, ocpp, solarman_v5] }
        adresse: { type: string, example: "192.168.10.31:502" }
        geraete_ids: { type: array, items: { type: integer, minimum: 0 } }
        kadenz_s: { type: integer, nullable: true, description: "Der kleinste bekannte Lesetakt der Komponenten; null = von keiner erhoben." }
        steuerquelle: { type: boolean }
        ab: { type: string, format: date-time, description: "Reihenbeginn - der Beginn der Zuständigkeit, auf die Minute." }
        komponenten: { type: array, items: { $ref: "#/components/schemas/DatenquelleVorschlagKomponente" } }
        grund: { type: string, nullable: true, enum: [adresse_an_box_vergeben, null] }
        text: { type: string, example: "Ab 12.03.2024 00:00 liest Box Halle 1" }
        ziel:
          allOf: [{ $ref: "#/components/schemas/DatenquelleVorschlagZiel" }]
          nullable: true
    DatenquelleVorschlagZiel:
      type: object
      description: Die vorhandene Quelle, die einen gesperrten Vorschlag aufnehmen kann (Vertrag §8 „Gerät dort hinzufügen“).
      required: [id, kennzeichen, name]
      properties:
        id: { type: string, format: uuid }
        kennzeichen: { type: string, example: DQ-1 }
        name: { type: string, nullable: true }
    DatenquelleAusgelassen:
      type: object
      required: [komponente, grund, protokoll, anker, text]
      properties:
        komponente: { $ref: "#/components/schemas/DatenquelleVorschlagKomponente" }
        grund: { type: string, enum: [keine_box, keine_adresse, protokoll_unbekannt, anker_ohne_vorschlag] }
        protokoll: { type: string, nullable: true, description: "protokoll_unbekannt: das verworfene Wort - die Rückwand für einen Transport, den keine Vorlage und kein Treiber trägt (z. B. opc_ua)." }
        anker:
          allOf: [{ $ref: "#/components/schemas/DatenquelleVorschlagKomponente" }]
          nullable: true
          description: "anker_ohne_vorschlag: der Wechselrichter, über den gelesen wird."
        text: { type: string }
    DatenquelleVorschlagsliste:
      type: object
      required: [fuehrende_box, fuehrung, vorschlaege, ausgelassen]
      properties:
        fuehrende_box:
          allOf: [{ $ref: "#/components/schemas/DatenquelleBox" }]
          nullable: true
        fuehrung: { type: string, enum: [gespeichert, speicher, einzige, keine_wahl, keine_box, gespeichert_nicht_in_anlage] }
        vorschlaege: { type: array, items: { $ref: "#/components/schemas/DatenquelleVorschlag" } }
        ausgelassen: { type: array, items: { $ref: "#/components/schemas/DatenquelleAusgelassen" } }
    DatenquelleBestaetigt:
      type: object
      description: Ein bestätigter Vorschlag - genau so, wie das GET ihn zeigte.
      required: [device_id, protokoll, adresse, komponenten]
      properties:
        device_id: { type: string, format: uuid, description: "Die Box des Vorschlags (`box.id`)." }
        protokoll: { type: string }
        adresse: { type: string }
        komponenten: { type: array, minItems: 1, items: { type: string, format: uuid } }
        datenquelle_id:
          type: string
          format: uuid
          nullable: true
          description: "Das `ziel` des GET: die Komponenten an diese vorhandene Quelle hängen statt eine neue anzulegen; zeigt das GET es nicht mehr, 409 `vorschlag_geaendert`."
    DatenquelleUebernehmen:
      type: object
      required: [vorschlaege]
      properties:
        vorschlaege: { type: array, minItems: 1, items: { $ref: "#/components/schemas/DatenquelleBestaetigt" } }
    DatenquelleUebernommen:
      type: object
      required: [neu, unveraendert, angehaengt, datenquellen]
      properties:
        neu: { type: integer, description: "Angelegte Quellen." }
        unveraendert: { type: integer, description: "Schon übernommene Vorschläge - ein zweiter Aufruf legt nichts an." }
        angehaengt: { type: integer, description: "Vorschläge, deren Komponenten an eine vorhandene Quelle gehängt wurden (`datenquelle_id`)." }
        datenquellen: { type: array, items: { $ref: "#/components/schemas/Datenquelle" }, description: "In der Reihenfolge der Anfrage." }
    DatenquelleFehler:
      type: object
      description: >
        Die Ablehnung der Datenquellen-Schnittstelle. Die Gründe des Vertrags
        (docs/contracts/v2/data-source-assignment.md §4/§5) mit ihrem Satz und `urteil`;
        der Vertrag nennt keinen HTTP-Status, die Schnittstelle genau einen je Grund:
        400 `protokoll_unbekannt`; 422 `keine_volle_minute`, `rueckwirkend`,
        `leerer_zeitraum`, `nur_ein_leser`; 409 `steuerquelle`, `spaeterer_wechsel_geplant`,
        `schon_zustaendig`, `ueberschneidung`, `adresse_an_box_vergeben`,
        `netzlage_fehlt`, `vergleich_bestaetigen` (urteil
        `bestaetigung_noetig`), `pruefung_fehlt`, `pruefung_gescheitert`. Dazu die
        Codes der Schnittstelle: `anfrage_ungueltig` 400 (`feld`), `weg_fest` 409
        (`felder`), `quelle_archiviert` 409 (`kennzeichen`),
        `pruefung_nicht_moeglich` 422 (`protokoll`), `gleichzeitig_geaendert` 409
        (`grund`), und für die Bestands-Übernahme (IP-4) `komponente_hat_quelle` 409
        (`komponenten`, `datenquellen`) und `vorschlag_geaendert` 409 (`device_id`,
        `protokoll`, `adresse`). `budget_ueberschritten` ist 422 und trägt unter
        `rechnung` Zahlen, freie Kapazität am Standort und beide konkrete Auswege.
        `gemeinsame_steuerung_aendern` 409 (`kennzeichen`, `anlage`, AP-15 T6): die Quelle ist
        Netzzähler, Messpunkt oder Steuerquelle eines Mitglieds einer scharfen
        Gemeinsamen Steuerung oder eine Steuerquelle in einer Anlage mit eingerichteter
        Gemeinsamer Steuerung, deren Ziel-Box kein Mitglied ist oder deren Anteile in Kraft
        sind (IP-26) - ihre Box wechselt nur über „Gemeinsame Steuerung ändern“.
      required: [code, message]
      properties:
        code:
          type: string
          enum: [protokoll_unbekannt, keine_volle_minute, leerer_zeitraum, rueckwirkend, steuerquelle,
            spaeterer_wechsel_geplant, schon_zustaendig, ueberschneidung, adresse_an_box_vergeben,
            netzlage_fehlt, nur_ein_leser, vergleich_bestaetigen, pruefung_fehlt, pruefung_gescheitert,
            anfrage_ungueltig, weg_fest, quelle_archiviert, pruefung_nicht_moeglich, gleichzeitig_geaendert,
            komponente_hat_quelle, vorschlag_geaendert, budget_ueberschritten, gemeinsame_steuerung_aendern]
        message: { type: string }
        grund: { type: string, description: "Bei Vertragsgründen identisch zu `code`." }
        satz: { type: string, description: "Bei Vertragsgründen identisch zu `message`; stabiler Kundensatz des Vertrags." }
        urteil: { type: string, enum: [abgelehnt, bestaetigung_noetig] }
        feld: { type: string, description: "anfrage_ungueltig: device_id, adresse, register …" }
        felder: { type: array, items: { type: string }, description: "weg_fest: die festen Felder, die sich ändern sollten." }
        kennzeichen: { type: string }
        anlage: { type: string, format: uuid, description: "gemeinsame_steuerung_aendern: die Anlage, deren Gemeinsame Steuerung sich ändern muss (…/sites/{anlage}/gemeinsame-steuerung)." }
        protokoll: { type: string }
        grund: { type: string, description: "gleichzeitig_geaendert: der Grund des Constraints, der gegriffen hat." }
        komponenten: { type: array, items: { type: string, format: uuid }, description: "komponente_hat_quelle: die Komponenten, die inzwischen eine Quelle haben." }
        datenquellen: { type: array, items: { type: string }, description: "komponente_hat_quelle: deren Kennzeichen." }
        device_id: { type: string, format: uuid, description: "vorschlag_geaendert / komponente_hat_quelle: die Box des bestätigten Vorschlags." }
        adresse: { type: string }
        rechnung:
          type: object
          description: "budget_ueberschritten: serverseitige Vorrechnung; die Zahlen stammen nicht aus der Anfrage."
          required: [code, kennzeichen, box, quelle, box_nachher, grenzen, freie_kapazitaet, auswege, gruende]
          properties:
            code: { type: string, enum: [budget_ueberschritten] }
            kennzeichen: { type: string }
            box: { type: string }
            quelle: { $ref: "#/components/schemas/DatenquelleBudgetQuellenRechnung" }
            box_nachher: { $ref: "#/components/schemas/DatenquelleBudgetZahlen" }
            grenzen: { $ref: "#/components/schemas/DatenquelleBudgetGrenzen" }
            freie_kapazitaet: { type: array, items: { $ref: "#/components/schemas/DatenquelleBudgetBox" } }
            auswege: { $ref: "#/components/schemas/DatenquelleBudgetAuswege" }
            gruende: { type: array, items: { type: string } }
      example:
        code: steuerquelle
        message: "Diese Quelle steuert — ihre Box kann erst mit der gemeinsamen Steuerung wechseln"
        urteil: abgelehnt
    DatenquelleBudgetZahlen:
      type: object
      required: [channels, samples_per_minute, requests_per_minute, duty_cycle_percent]
      properties:
        channels: { type: integer, minimum: 0 }
        samples_per_minute: { type: number, minimum: 0 }
        requests_per_minute: { type: number, minimum: 0 }
        duty_cycle_percent: { type: number, minimum: 0 }
    DatenquelleBudgetQuellenRechnung:
      type: object
      required: [protokoll, channels, takt_s, anfragen, last]
      properties:
        protokoll: { type: string }
        channels: { type: integer, minimum: 0 }
        takt_s: { type: integer, minimum: 1, maximum: 86400 }
        anfragen:
          type: array
          items:
            type: object
            required: [anfragen_je_takt, kosten_ms_je_anfrage]
            properties:
              anfragen_je_takt: { type: integer, minimum: 0 }
              kosten_ms_je_anfrage: { type: integer, minimum: 0 }
        last: { $ref: "#/components/schemas/DatenquelleBudgetZahlen" }
    DatenquelleBudgetGrenzen:
      type: object
      required: [samples_per_minute, requests_per_minute, duty_cycle_percent]
      properties:
        samples_per_minute: { type: number, example: 600 }
        requests_per_minute: { type: number, example: 30 }
        duty_cycle_percent: { type: number, example: 20 }
    DatenquelleBudgetBox:
      type: object
      required: [id, name, belegt, frei, quelle_passt]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        belegt: { $ref: "#/components/schemas/DatenquelleBudgetZahlen" }
        frei: { $ref: "#/components/schemas/DatenquelleBudgetZahlen" }
        quelle_passt: { type: boolean }
    DatenquelleBudgetAuswege:
      type: object
      required: [takt_s, takt, boxen, andere_box]
      properties:
        takt_s: { type: integer, nullable: true, minimum: 1, maximum: 86400 }
        takt: { type: string, nullable: true, example: "Takt 60 s wählen" }
        boxen: { type: array, items: { $ref: "#/components/schemas/DatenquelleBudgetBox" } }
        andere_box: { type: string, nullable: true, example: "Box Halle 2 wählen (29 Anfragen/min frei)" }
    WagoKartenAnlegen:
      type: object
      required: [karten]
      properties:
        karten:
          type: array
          minItems: 1
          maxItems: 64
          items:
            type: object
            required: [steckplatz, komponente]
            properties:
              steckplatz: { type: integer, minimum: 1, maximum: 65535 }
              kartentyp: { type: integer, nullable: true, enum: [493, 494, 495], description: "Aus der Kartenlesung; null = nicht gelesen." }
              komponente: { $ref: "#/components/schemas/SaveComponentRequest" }
    WagoKartenNachtragen:
      type: object
      required: [karten]
      properties:
        karten:
          type: array
          minItems: 1
          maxItems: 64
          items:
            type: object
            required: [entityId, steckplatz]
            properties:
              entityId: { type: string, format: uuid }
              steckplatz: { type: integer, minimum: 1, maximum: 65535 }
              kartentyp: { type: integer, nullable: true, enum: [493, 494, 495], description: "Aus der Kartenlesung; null = nicht gelesen." }
    WagoKartenAnlage:
      type: object
      properties:
        geraetId: { type: string, format: uuid, description: Der Controller. }
        kennzeichen: { type: string, description: "GR-n des Controllers." }
        eingebautAm: { type: string, format: date-time, description: "Beginn von Einbau, Karten und Speisung (volle Minute)." }
        karten:
          type: array
          items:
            type: object
            properties:
              entityId: { type: string, format: uuid }
              teilId: { type: string, format: uuid }
              steckplatz: { type: integer }
              typ: { type: string, nullable: true, description: "750-<kartentyp>; null = nicht gelesen." }
              index: { type: integer, description: "Karte n im Registerbild (n-te nach Steckplatz)." }
    WagoSollLesenEingabe:
      type: object
      required: [deviceId]
      properties:
        deviceId: { type: string, format: uuid, description: Die Box, die die Steuerung liest. }
    WagoSollKarte:
      type: object
      properties:
        steckplatz: { type: integer, nullable: true }
        typ: { type: string, nullable: true, description: "Kartentyp als Text der Karte (z. B. 750-495)." }
        variante: { type: integer, nullable: true, minimum: 0, maximum: 65535, description: "Gelesen; null = nicht gelesen, nie 0." }
    WagoSoll:
      type: object
      properties:
        controllerKennung: { type: integer, format: int64, nullable: true, minimum: 0, maximum: 4294967295 }
        karten: { type: array, items: { $ref: "#/components/schemas/WagoSollKarte" } }
    WagoSollLesung:
      type: object
      required: [ergebnis, satz, soll, abweichungen]
      properties:
        ergebnis: { type: string, enum: [gespeichert, unveraendert, abweichung, nicht_gelesen] }
        satz: { type: string }
        soll: { $ref: "#/components/schemas/WagoSoll" }
        abweichungen:
          type: array
          items:
            type: object
            properties:
              feld: { type: string, enum: [controller_kennung, kartenzahl, steckplatz, kartentyp, variante] }
              steckplatz: { type: integer, nullable: true }
              soll: { type: integer, format: int64, nullable: true }
              gelesen: { type: integer, format: int64, nullable: true }
    MessmittelAngaben:
      type: object
      description: AP-16 IP-15/IP-16 (G1–G4). NULL heißt nicht erhoben; Hersteller- und Einbau-Angaben bleiben getrennt.
      required: [geraet_id, kennzeichen, einbau_kennzeichen, zustand, genauigkeitsklasse, pruefungsart, pruefung_am, pruefung_gueltig_bis, beleg, wandler, laut_hersteller]
      properties:
        geraet_id: { type: string, format: uuid }
        kennzeichen: { type: string }
        einbau_kennzeichen: { type: string }
        zustand: { type: string, enum: [erhoben, nicht_erhoben] }
        genauigkeitsklasse: { type: string, nullable: true, maxLength: 60 }
        pruefungsart: { type: string, enum: [eichung, mid_konformitaet, kalibrierung, werksbescheinigung, keine, nicht_erhoben] }
        pruefung_am: { type: string, format: date, nullable: true }
        pruefung_gueltig_bis: { type: string, format: date, nullable: true }
        beleg:
          nullable: true
          allOf: [{ $ref: "#/components/schemas/MessmittelBeleg" }]
        wandler: { type: array, items: { $ref: "#/components/schemas/MessmittelWandler" } }
        laut_hersteller:
          type: array
          description: Reine Cloud-Katalogangaben für den Gerätetyp und eingebaute Karten; niemals eine Einbau-Angabe.
          items: { $ref: "#/components/schemas/MessmittelHerstellerangabe" }
    MessmittelHerstellerangabe:
      type: object
      description: G4 „laut Hersteller“ mit öffentlich prüfbarer Provenienz; getrennt von Klasse, Prüfung und Wandler am Einbau.
      required: [ziel_art, ziel, bezeichnung, hersteller, modell, zustand, klasse, wert, bezug, fundstelle, source_url, source_sha256]
      properties:
        ziel_art: { type: string, enum: [geraet, teil] }
        ziel: { type: string, format: uuid }
        bezeichnung: { type: string }
        hersteller: { type: string }
        modell: { type: string }
        zustand: { type: string, enum: [belegt, nicht_belegt] }
        klasse: { type: string, nullable: true }
        wert: { type: string, nullable: true }
        bezug: { type: string, nullable: true }
        fundstelle: { type: string, nullable: true }
        source_url: { type: string, format: uri, nullable: true }
        source_sha256: { type: string, nullable: true, pattern: "^[0-9a-f]{64}$" }
    MessmittelBeleg:
      type: object
      description: Ein Verweis, keine Datei (G2). Person und Zeitpunkt sind die des Eintragens.
      required: [bezeichnung, ablage, sha256, person, zeitpunkt]
      properties:
        bezeichnung: { type: string, maxLength: 200 }
        ablage: { type: string, nullable: true, maxLength: 200 }
        sha256: { type: string, pattern: "^[0-9a-f]{64}$" }
        person: { $ref: "#/components/schemas/BewertungKriterienAkteur" }
        zeitpunkt: { type: string, format: date-time }
    MessmittelWandler:
      type: object
      description: Eine Wandler-Fassung des Einbaus mit ihrer Klasse; die Klasse ändert keinen Wert.
      required: [fassung, art, wert, gueltig_ab, gueltig_bis, klasse, zustand]
      properties:
        fassung: { type: string, format: uuid }
        art: { type: string, enum: [wandler_strom, wandler_spannung] }
        wert: { type: object, additionalProperties: true }
        gueltig_ab: { type: string, format: date-time }
        gueltig_bis: { type: string, format: date-time, nullable: true }
        klasse: { type: string, nullable: true, maxLength: 60 }
        zustand: { type: string, enum: [erhoben, nicht_erhoben] }
    MessmittelEintrag:
      type: object
      additionalProperties: false
      properties:
        genauigkeitsklasse: { type: string, nullable: true, maxLength: 60 }
        pruefungsart: { type: string, nullable: true, enum: [eichung, mid_konformitaet, kalibrierung, werksbescheinigung, keine, nicht_erhoben, null] }
        pruefung_am: { type: string, format: date, nullable: true }
        pruefung_gueltig_bis: { type: string, format: date, nullable: true }
        beleg:
          type: object
          nullable: true
          additionalProperties: false
          required: [bezeichnung, sha256]
          properties:
            bezeichnung: { type: string, maxLength: 200 }
            ablage: { type: string, nullable: true, maxLength: 200 }
            sha256: { type: string, pattern: "^[0-9a-fA-F]{64}$", description: Im Portal aus der gewählten Datei gebildet. }
        wandler:
          type: array
          nullable: true
          items:
            type: object
            additionalProperties: false
            required: [fassung]
            properties:
              fassung: { type: string, format: uuid }
              klasse: { type: string, nullable: true, maxLength: 60 }
    MessmittelFehler:
      type: object
      required: [code, feld, message]
      properties:
        code: { type: string, enum: [anfrage_ungueltig, nicht_gefunden, pruefsumme_ungueltig, beleg_unvollstaendig, pruefungsart_unbekannt, zeitraum_ungueltig, text_zu_lang, fassung_unbekannt, klasse_nur_am_wandler] }
        feld: { type: string }
        message: { type: string }
    VergleichToleranzEintrag:
      type: object
      additionalProperties: false
      required: [prozent, begruendung]
      properties:
        prozent: { type: string, pattern: "^[0-9]{1,3}(\\.[0-9]{1,2})?$", example: "2" }
        begruendung: { type: string, maxLength: 500 }
    VergleichToleranzFassung:
      type: object
      required: [fassung, prozent, startwert, gilt_ab_monat, begruendung, person, eingetragen_am]
      properties:
        fassung: { type: integer, minimum: 1 }
        prozent: { type: string, example: "2" }
        startwert: { type: boolean, description: "Fassung 1 = Startwert des Vertrags, ohne Person und Begründung." }
        gilt_ab_monat: { type: string, format: date }
        begruendung: { type: string, nullable: true }
        person: { $ref: "#/components/schemas/BewertungKriterienAkteur" }
        eingetragen_am: { type: string, format: date-time, nullable: true }
    VergleichToleranzMonat:
      type: object
      required: [monat, fuehrend, fuehrend_zustand, vergleich, vergleich_zustand, zustand, grund,
        abweichung_prozent, toleranz_prozent, toleranz_fassung, befund]
      properties:
        monat: { type: string, example: "2026-12" }
        fuehrend: { type: string, nullable: true, example: "131200" }
        fuehrend_zustand: { type: string, nullable: true }
        vergleich: { type: string, nullable: true, example: "129700" }
        vergleich_zustand: { type: string, nullable: true }
        zustand: { type: string, enum: [passt, abweichung, nicht_vergleichbar] }
        grund:
          type: string
          nullable: true
          enum: [vergleich_nicht_ganzer_monat, fuehrend_luecke, vergleich_luecke, fuehrend_ersatzwert,
            vergleich_ersatzwert, fuehrend_nicht_positiv, null]
        abweichung_prozent: { type: string, nullable: true, example: "1.1" }
        toleranz_prozent: { type: string, example: "2" }
        toleranz_fassung: { type: integer, minimum: 1 }
        befund: { type: boolean, nullable: true }
    VergleichToleranzQuelle:
      type: object
      required: [quelle_id, entity_id, komponente, kanal, zweck, groesse, richtung, herleitung, gueltig_ab,
        gueltig_bis, monatsvergleich, toleranz, fassungen, monate]
      properties:
        quelle_id: { type: string, format: uuid }
        entity_id: { type: string, format: uuid }
        komponente: { type: string, nullable: true }
        kanal: { type: string }
        zweck: { type: string, enum: ["Plausibilität", "Ersatz bei Ausfall", "Abrechnungszähler"] }
        groesse: { type: string }
        richtung: { type: string }
        herleitung: { type: string }
        gueltig_ab: { type: string, format: date-time }
        gueltig_bis: { type: string, format: date-time, nullable: true }
        monatsvergleich: { type: string, enum: [ja, ohne_monatsmenge] }
        toleranz: { $ref: "#/components/schemas/VergleichToleranzFassung" }
        fassungen: { type: array, items: { $ref: "#/components/schemas/VergleichToleranzFassung" } }
        monate: { type: array, items: { $ref: "#/components/schemas/VergleichToleranzMonat" } }
    VergleichToleranzBefund:
      type: object
      required: [art, quelle_id, monat, abweichung_prozent, toleranz_prozent, toleranz_fassung]
      properties:
        art: { type: string, enum: [abweichung_vergleichsquelle] }
        quelle_id: { type: string, format: uuid }
        monat: { type: string, example: "2026-12" }
        abweichung_prozent: { type: string, example: "3.4" }
        toleranz_prozent: { type: string, example: "2" }
        toleranz_fassung: { type: integer, minimum: 1 }
    VergleichToleranzVergleich:
      type: object
      required: [messstelle_id, kennzeichen, von, bis, einheit, vergleichsquellen, befunde]
      properties:
        messstelle_id: { type: string, format: uuid }
        kennzeichen: { type: string }
        von: { type: string, example: "2026-11" }
        bis: { type: string, example: "2026-12" }
        einheit: { type: string, example: "kWh" }
        vergleichsquellen: { type: array, items: { $ref: "#/components/schemas/VergleichToleranzQuelle" } }
        befunde: { type: array, items: { $ref: "#/components/schemas/VergleichToleranzBefund" } }
    VergleichToleranzFehler:
      type: object
      required: [code, feld, message]
      properties:
        code: { type: string, enum: [anfrage_ungueltig, nicht_gefunden, toleranz_ungueltig, begruendung_fehlt, text_zu_lang, keine_vergleichsquelle] }
        feld: { type: string }
        message: { type: string }
    Geraet:
      type: object
      description: >
        Ein Einbau - das konkret eingebaute Gerät (AP-04 IP-10). Zeitpunkte auf die
        Minute mit dem Versatz von Europe/Berlin. `null` heißt nicht erhoben - nie
        geraten; `ausgebaut_am: null` heißt eingebaut.
      required: [id, site_id, kennzeichen, einbau_kennzeichen, geraeteart, hersteller, typ,
        seriennummer, bezeichnung, data_source_id, geraete_id, eingebaut_am, ausgebaut_am,
        aus_bestand, komponenten, teile, vorgaenger]
      properties:
        id: { type: string, format: uuid }
        site_id: { type: string, format: uuid, description: "Die Anlage, in der das Gerät verdrahtet ist." }
        kennzeichen: { type: string, description: "Das Gerät (GR-4) - bleibt über einen Wechsel an derselben Stelle." }
        einbau_kennzeichen: { type: string, description: "Das konkret eingebaute Gerät (Z-5a); ohne Wechsel = kennzeichen." }
        geraeteart:
          type: string
          enum: [zaehler, wechselrichter, controller, ladestation, speicher, sonstiges]
          description: "Die Energiekarte ist ein Teil des Controllers (teile), keine Geräteart."
        hersteller: { type: string, nullable: true }
        typ: { type: string, nullable: true }
        seriennummer: { type: string, nullable: true }
        bezeichnung: { type: string, nullable: true }
        data_source_id: { type: string, format: uuid, nullable: true, description: "Die Datenquelle, über die das Gerät antwortet (AP-06); null = noch keine angelegt." }
        geraete_id: { type: integer, minimum: 0, nullable: true, description: "Die Geräte-ID hinter der Quelle (Modbus-Unit-ID)." }
        eingebaut_am: { type: string, format: date-time }
        ausgebaut_am: { type: string, format: date-time, nullable: true }
        aus_bestand: { type: boolean, description: "Aus der Komponente abgeleitet (Bestand oder Anlege-Weg, dieselbe Regel): eingebaut_am ist der Beginn ihres Verlaufs in VoltPilot, nicht der Einbautag." }
        komponenten:
          type: array
          items: { $ref: "#/components/schemas/GeraetKomponente" }
        teile:
          type: array
          items: { $ref: "#/components/schemas/GeraetTeil" }
        vorgaenger:
          type: array
          description: "Die früheren Einbauten desselben Geräts, der jüngste zuerst."
          items: { $ref: "#/components/schemas/GeraetVorgaenger" }
    GeraetKomponente:
      type: object
      description: >
        Ein Zeitraum, in dem der Einbau die Komponente speist, halboffen [ab, bis);
        `gueltig_bis: null` heißt bis auf Weiteres. Über eine Karte (`teil_id`,
        `steckplatz`) oder direkt (null). Je Komponente und Zeitpunkt EIN Gerät.
      required: [entity_id, teil_id, steckplatz, gueltig_ab, gueltig_bis]
      properties:
        entity_id: { type: string, format: uuid, description: "Die Komponente." }
        teil_id: { type: string, format: uuid, nullable: true }
        steckplatz: { type: integer, nullable: true }
        gueltig_ab: { type: string, format: date-time }
        gueltig_bis: { type: string, format: date-time, nullable: true }
    GeraetTeil:
      type: object
      description: "Eine Energiekarte im Steckplatz eines Controllers (AP-05 E4: Identität = Gerät + Steckplatz)."
      required: [id, teilart, steckplatz, bezeichnung, typ, seriennummer, eingebaut_am, ausgebaut_am]
      properties:
        id: { type: string, format: uuid }
        teilart: { type: string, enum: [energiekarte] }
        steckplatz: { type: integer, minimum: 0, nullable: true, description: "null = nicht erhoben." }
        bezeichnung: { type: string, nullable: true }
        typ: { type: string, nullable: true }
        seriennummer: { type: string, nullable: true }
        eingebaut_am: { type: string, format: date-time }
        ausgebaut_am: { type: string, format: date-time, nullable: true }
    GeraetVorgaenger:
      type: object
      description: "Ein früherer Einbau desselben Geräts."
      required: [id, einbau_kennzeichen, hersteller, typ, seriennummer, eingebaut_am, ausgebaut_am]
      properties:
        id: { type: string, format: uuid }
        einbau_kennzeichen: { type: string }
        hersteller: { type: string, nullable: true }
        typ: { type: string, nullable: true }
        seriennummer: { type: string, nullable: true }
        eingebaut_am: { type: string, format: date-time }
        ausgebaut_am: { type: string, format: date-time, nullable: true }
    ProtokollUrheber:
      type: object
      description: >
        Wer den Eintrag geschrieben hat, im Akteur-Vokabular von AP-03 - dasselbe in
        Register-Journal, Handeingriff, Befehls-Verlauf und Aenderungsprotokoll (IP-7).
        `rolle` ist null, wo das Journal sie nicht festhielt (Eintraege der Ortsstruktur
        von vor IP-7) - nie geraten.
      required: [name, rolle, art]
      properties:
        name: { type: string, example: "Ines Kaltenbach" }
        rolle: { type: string, nullable: true, enum: [kundenadministrator, energiemanager, bearbeiter, bedienberechtigt, leser, unterstuetzer, voltpilot_betrieb, null] }
        art: { type: string, nullable: true, enum: [kunde, unterstuetzung, voltpilot, notfall, null] }
    ProtokollBezug:
      type: object
      description: >
        Das Objekt, um das es geht. `kennzeichen` ist MS-06, DQ-1 oder das
        Kurzzeichen eines Ortes - null, wo die Art keins traegt. `name` ist null,
        wenn das Objekt geloescht wurde: das Protokoll ueberlebt es.
      required: [art, id, kennzeichen, name]
      properties:
        art: { type: string, enum: [messstelle, datenquelle, unternehmen, standort, gebaeude, bereich, anlage] }
        id: { type: string, format: uuid }
        kennzeichen: { type: string, nullable: true, example: MS-06 }
        name: { type: string, nullable: true, example: "Hauptzaehler Werk" }
    ProtokollEintrag:
      type: object
      description: >
        Ein Eintrag. `id` ist Herkunft und laufende Nummer zusammen
        ("messstelle:42") - die drei Journale zaehlen unabhaengig voneinander.
        `text` ist der Kundensatz "was wurde geaendert", gebaut allein aus
        `alt`/`neu`; `grund` ist die Begruendung, die der Schreiber mitgegeben hat
        (null = keine).
      required: [id, quelle, art, text, bezug, gilt_ab, gilt_bis, eingetragen_am, zeitform, grund, urheber, alt, neu]
      properties:
        id: { type: string, example: "messstelle:42" }
        quelle: { type: string, enum: [messstelle, ort, datenquelle], description: "Aus welchem Journal der Eintrag stammt." }
        art: { type: string, example: zaehler_gewechselt, description: "Der Code des Eintrags in seinem Journal." }
        text: { type: string, example: "Zaehler gewechselt: Z-5a -> Z-5b" }
        bezug: { $ref: "#/components/schemas/ProtokollBezug" }
        gilt_ab: { type: string, format: date-time, description: "WANN die Aenderung gilt." }
        gilt_bis:
          type: string
          format: date
          nullable: true
          description: >
            Der letzte TAG, an dem ein Eintrag der Ortsstruktur (`quelle: ort`) noch
            gilt - der Vortag, an dem derselbe Sachverhalt am selben Objekt wieder
            geaendert wurde (Zuordnung, Flaeche, Bestehen, Felder). null = bis heute
            offen; bei Messstellen und Datenquellen immer null (AP-02 IP-14).
        eingetragen_am: { type: string, format: date-time, description: "WANN sie eingetragen wurde." }
        zeitform: { type: string, enum: [rueckwirkend, angekuendigt, sofort], description: "Das Urteil ueber die beiden Zeitpunkte; `rueckwirkend` ist das gespeicherte Urteil des Schreibwegs." }
        grund: { type: string, nullable: true }
        urheber: { $ref: "#/components/schemas/ProtokollUrheber" }
        alt: { type: object, nullable: true, additionalProperties: true, description: "Der Stand davor - bei `bearbeitet` NUR die geaenderten Felder." }
        neu: { type: object, nullable: true, additionalProperties: true }
    Protokoll:
      type: object
      description: >
        Das Aenderungsprotokoll (UEMS AP-04 IP-21), juengster Eintrag zuerst.
        `achse` sagt, WELCHE Zeitachse gefiltert und sortiert hat - sie steht in der
        ANTWORT, damit sie niemand verwechselt. `von`/`bis` ist der Zeitraum,
        halboffen [von, bis); null heisst ohne Grenze. `weiter` ist der
        Fortsetzungszeiger fuer die naechste Seite - null, wenn es keine gibt.
      required: [eintraege, achse, von, bis, weiter]
      properties:
        eintraege:
          type: array
          items: { $ref: "#/components/schemas/ProtokollEintrag" }
        achse: { type: string, enum: [wirkung, eintrag, gueltigkeit] }
        von: { type: string, format: date-time, nullable: true }
        bis: { type: string, format: date-time, nullable: true }
        weiter: { type: string, nullable: true, example: "1763458800000:messstelle:42" }
    GeraetListe:
      type: object
      required: [geraete]
      properties:
        geraete:
          type: array
          items: { $ref: "#/components/schemas/Geraet" }
    Einstellungen:
      type: object
      description: >
        Die Einstellungen eines Einbaus (UEMS AP-04 IP-11). `geraet`/`einbau`
        sind die Kennzeichen des Geräts und des konkret eingebauten (GR-4 /
        Z-5b); `gueltig` je Quelle und Art die zum `stichtag` gültige Fassung.
      required: [geraet_id, geraet, einbau, stichtag, gueltig, historie]
      properties:
        geraet_id: { type: string, format: uuid }
        geraet: { type: string }
        einbau: { type: string }
        stichtag: { type: string, format: date-time }
        gueltig:
          type: array
          items: { $ref: "#/components/schemas/EinstellungFassung" }
        historie:
          type: array
          items: { $ref: "#/components/schemas/EinstellungFassung" }
    EinstellungFassung:
      type: object
      description: >
        Eine Fassung, halboffen [gueltig_ab, gueltig_bis) auf die Minute;
        `gueltig_bis` null = bis auf Weiteres. Quelle = der Einbau
        (`entity_id` null), eine Komponente, die er speist, oder ein Kanal dieser
        Komponente. `wert` je Art (quelle-einstellung.md §2): wandler_strom
        {primaer_a, sekundaer_a} · wandler_spannung {primaer_v, sekundaer_v} ·
        skalierung {faktor} oder {automatisch: true} · offset {wert, einheit} ·
        vorzeichen_umgekehrt {umgekehrt} · impulswertigkeit {impulse_je_kwh} ·
        zaehlerkonstante {je_kwh}. `zustellung`: verbindung (die Box wendet sie
        mit der Verbindung der Komponente an - Bestand und Hebel), ausstehend
        (angewendet eingetragen, Zustellung AP-06) oder null (im Gerät
        eingestellt, nur dokumentiert). `wert_text`/`anwendung_text` sind die
        Kundensätze des Vertrags („1.000/5 A", „angewendet - Zustellung
        ausstehend").
      required: [id, entity_id, kanal, art, art_kundenwort, wert, wert_text, anwendung, anwendung_text,
        zustellung, herkunft, gueltig_ab, gueltig_bis, status, tatsaechlich_ab, rueckwirkend,
        begruendung, eingetragen]
      properties:
        id: { type: string, format: uuid }
        entity_id: { type: string, format: uuid, nullable: true }
        kanal: { type: string, nullable: true }
        art:
          type: string
          enum: [wandler_strom, wandler_spannung, skalierung, offset, vorzeichen_umgekehrt,
            impulswertigkeit, zaehlerkonstante]
        art_kundenwort: { type: string }
        wert: { type: object, additionalProperties: true }
        wert_text: { type: string }
        anwendung: { type: string, enum: [angewendet, dokumentiert] }
        anwendung_text: { type: string }
        zustellung: { type: string, enum: [verbindung, ausstehend], nullable: true }
        herkunft: { type: string, enum: [bestand, verbindung, eintrag] }
        gueltig_ab: { type: string, format: date-time }
        gueltig_bis: { type: string, format: date-time, nullable: true }
        status: { type: string, enum: [geplant, gueltig, beendet] }
        tatsaechlich_ab: { type: string, format: date-time, nullable: true }
        rueckwirkend: { type: boolean }
        begruendung: { type: string, nullable: true }
        eingetragen: { $ref: "#/components/schemas/EinstellungEintrag" }
    EinstellungEintrag:
      type: object
      description: >
        Das Protokoll an der Quelle: wann, von wem (Anzeigename), mit welcher
        Rolle und welcher Art Urheber (AP-03: kunde · unterstuetzung ·
        voltpilot · notfall). Der Bestand ist VoltPilot selbst.
      required: [am, von, rolle, art]
      properties:
        am: { type: string, format: date-time }
        von: { type: string }
        rolle: { type: string, nullable: true }
        art: { type: string, enum: [kunde, unterstuetzung, voltpilot, notfall] }
    EinstellungNeu:
      type: object
      description: >
        Eine neue Fassung. Ohne `entity_id` gilt sie für den Einbau; `kanal` nur
        mit `entity_id`. `gueltig_ab` auf der vollen Minute (Sekunden werden nie
        gerundet). `tatsaechlich_ab`: wann die Änderung wirklich geschah, wenn
        früher - der Zeitraum dazwischen ist mit der vorigen Fassung erfasst
        (Folgen-Satz, Korrektur AP-08). Ein Feld, das es hier nicht gibt, ist 400.
      required: [art, wert, anwendung, gueltig_ab]
      additionalProperties: false
      properties:
        entity_id: { type: string, format: uuid, nullable: true }
        kanal: { type: string, nullable: true, maxLength: 240 }
        art:
          type: string
          enum: [wandler_strom, wandler_spannung, skalierung, offset, vorzeichen_umgekehrt,
            impulswertigkeit, zaehlerkonstante]
        wert: { type: object, additionalProperties: true }
        anwendung: { type: string, enum: [angewendet, dokumentiert] }
        gueltig_ab: { type: string, format: date-time }
        tatsaechlich_ab: { type: string, format: date-time, nullable: true }
        begruendung: { type: string, nullable: true, maxLength: 500 }
    EinstellungEingetragen:
      type: object
      description: >
        Die eingetragene Fassung, die Fassung, die sie beendet hat (null ohne
        Vorgänger), die Folgen-Sätze (AP-04 §5.7, A5) und die Kennzeichen der
        Messstellen, an denen sie protokolliert ist.
      required: [fassung, beendet, folgen, messstellen]
      properties:
        fassung: { $ref: "#/components/schemas/EinstellungFassung" }
        beendet:
          allOf: [{ $ref: "#/components/schemas/EinstellungFassung" }]
          nullable: true
        folgen:
          type: array
          items: { type: string }
        messstellen:
          type: array
          items: { type: string }
    EinstellungFehler:
      type: object
      description: >
        Die Ablehnung der Einstellungs-Schnittstelle. Die Codes des Vertrags
        (quelle-einstellung.md §4) mit ihrem Status: `wert_ungueltig` 400
        (`art`, `felder`), `zeitpunkt_ungueltig` 400, `vor_beginn` 422
        (`beginn`), `nach_ende` 422 (`ende`), `tatsaechlich_ungueltig` 422,
        `beginn_belegt` 409 (`bestehend`), `unveraendert` 400. Dazu die der
        Schnittstelle: `anfrage_ungueltig` 400 (`feld`) und `keine_speisung` 422
        (`entity_id` - die Komponente wird von diesem Einbau nie gespeist).
      required: [code, message]
      properties:
        code:
          type: string
          enum: [wert_ungueltig, zeitpunkt_ungueltig, vor_beginn, nach_ende, tatsaechlich_ungueltig,
            beginn_belegt, unveraendert, anfrage_ungueltig, keine_speisung]
        message: { type: string }
        feld: { type: string }
        art: { type: string }
        felder: { type: array, items: { type: string } }
        beginn: { type: string, format: date-time }
        ende: { type: string, format: date-time }
        entity_id: { type: string, format: uuid }
        bestehend:
          type: object
          properties:
            id: { type: string, format: uuid }
            wert_text: { type: string }
    VierAugen:
      type: object
      required: [vieraugen, vorgabe]
      properties:
        vieraugen: { type: boolean, description: "„Freigabe durch eine zweite Person“ gilt." }
        vorgabe: { type: boolean, description: "Nie eingestellt - es gilt die Vorgabe aus." }
    VierAugenSetzen:
      type: object
      required: [vieraugen]
      additionalProperties: false
      properties:
        vieraugen: { type: boolean }
    ErsatzwertStand:
      type: object
      required: [kennung, status, fassung, korrektur]
      properties:
        kennung: { type: string }
        status: { type: string, enum: [zurueckgenommen] }
        fassung: { type: integer }
        korrektur: { type: string, nullable: true }
    ErsatzwertLuecke:
      type: object
      required: [id, art, von, bis, zuwachs, einheit]
      properties:
        id: { type: string, format: uuid }
        art: { type: string, enum: [data_gap, counter_reset, device_boundary] }
        von: { type: string, format: date-time }
        bis: { type: string, format: date-time, nullable: true }
        zuwachs: { type: number, nullable: true }
        einheit: { type: string, nullable: true }
    ErsatzwertEingabe:
      type: object
      additionalProperties: false
      required: [quelle_id, methode, von, bis, begruendung]
      properties:
        quelle_id: { type: string, format: uuid }
        methode:
          type: string
          enum: [gleichmaessig_verteilen, profil_vorperiode, profil_vergleichsquelle, ablesestand_nachtragen, wert_eingeben, vorperiode_uebernehmen, vergleichsquelle_uebernehmen]
        von: { type: string, format: date-time, description: Viertelstundenraster, einschließlich }
        bis: { type: string, format: date-time, description: Viertelstundenraster, ausschließlich; Methode e höchstens ein Kalendermonat in Standort-Zeitzone }
        begruendung: { type: string, minLength: 10, maxLength: 500 }
        beleg: { type: string, minLength: 10, maxLength: 500 }
        luecke_ereignis_id: { type: string, format: uuid, description: Pflicht bei a–c; Zuwachs und Stände kommen ausschließlich aus diesem Ereignis }
        vorperiode_von: { type: string, format: date-time, description: Nur b/f; Vorgabe sieben örtliche Tage vorher }
        vergleich_quelle_id: { type: string, format: uuid, description: Nur c/g; markierte Vergleichsquelle derselben Messstelle und Größe }
        zeitpunkt: { type: string, format: date-time, description: Nur d; volle Minute in der Viertelstunde }
        endstand: { type: number }
        anfangsstand: { type: number }
        betrag: { type: number, description: Nur e; Beleg ist Pflicht }
        einheit: { type: string, description: Pflicht bei d/e; a–c lesen sie aus dem Lückenereignis }
    KorrekturPeriodenStand:
      type: object
      required: [menge, menge_zustand]
      properties:
        menge: { type: number, nullable: true }
        menge_zustand: { type: string }
        kennzeichen: { type: array, items: { type: string } }
        erhalten: { type: integer, nullable: true }
        erwartet: { type: integer, nullable: true }
        abdeckung_prozent: { type: integer, nullable: true }
    KorrekturPortalPeriode:
      type: object
      required: [periode, von, bis, neu]
      properties:
        periode: { type: string }
        von: { type: string, format: date-time }
        bis: { type: string, format: date-time }
        version_alt: { type: integer }
        version_neu: { type: integer }
        alt: { $ref: '#/components/schemas/KorrekturPeriodenStand' }
        neu: { $ref: '#/components/schemas/KorrekturPeriodenStand' }
    KorrekturPortalAktion:
      type: object
      required: [erlaubt, grund]
      properties:
        erlaubt: { type: boolean }
        grund: { type: string, nullable: true, description: Zustand statt eines nicht ausführbaren Knopfs }
    KorrekturAuswirkungen:
      type: object
      required: [perioden, berechnete_messstellen, kennzahlen, berichte]
      properties:
        perioden: { type: array, items: { type: string } }
        berechnete_messstellen: { type: string }
        kennzahlen: { type: string }
        berichte: { type: string }
    ErsatzwertVorschau:
      type: object
      required: [perioden, auswirkungen, vieraugen, freigabe_noetig]
      properties:
        perioden: { type: array, items: { $ref: '#/components/schemas/KorrekturPortalPeriode' } }
        auswirkungen: { $ref: '#/components/schemas/KorrekturAuswirkungen' }
        vieraugen: { type: boolean }
        freigabe_noetig: { type: boolean, description: Vier-Augen oder nachgetragener Ablesestand nach Endgültigkeit (F12) }
    KorrekturPortalDetail:
      type: object
      required: [kennung, art, status, fassung, von, bis, begruendung, beleg, ersatzwert_kennung, methode, einheit, messstellen, ersteller, erstellt_am, vieraugen, vorschau, auswirkungen, freigeben, zuruecknehmen, ablehnen]
      properties:
        kennung: { type: string }
        art: { type: string }
        status: { type: string, enum: [vorschlag, freigegeben, abgelehnt, zurueckgenommen] }
        fassung: { type: integer }
        von: { type: string, format: date-time }
        bis: { type: string, format: date-time }
        begruendung: { type: string }
        beleg: { type: string, nullable: true }
        ersatzwert_kennung: { type: string, nullable: true }
        methode: { type: string, nullable: true }
        einheit: { type: string, nullable: true }
        messstellen:
          type: array
          items:
            type: object
            required: [kennzeichen, name]
            properties:
              kennzeichen: { type: string }
              name: { type: string }
        ersteller: { $ref: '#/components/schemas/KorrekturUrheber' }
        erstellt_am: { type: string, format: date-time }
        vieraugen: { type: boolean }
        vorschau: { type: array, items: { $ref: '#/components/schemas/KorrekturPortalPeriode' } }
        auswirkungen: { $ref: '#/components/schemas/KorrekturAuswirkungen' }
        freigeben: { $ref: '#/components/schemas/KorrekturPortalAktion' }
        zuruecknehmen: { $ref: '#/components/schemas/KorrekturPortalAktion' }
        ablehnen: { $ref: '#/components/schemas/KorrekturPortalAktion' }
    KorrekturFreigeben:
      type: object
      required: [begruendung]
      additionalProperties: false
      properties:
        begruendung: { type: string, minLength: 10, maxLength: 500 }
    KorrekturZuruecknehmen:
      type: object
      required: [grund]
      additionalProperties: false
      properties:
        grund: { type: string, minLength: 10, maxLength: 500 }
    KorrekturUrheber:
      type: object
      required: [name, rolle, art]
      properties:
        name: { type: string }
        rolle: { type: string, nullable: true }
        art: { type: string, enum: [kunde, unterstuetzung, voltpilot, notfall] }
    BerichtFehler:
      type: object
      description: >
        Die Ablehnung der Berichts-Routen (AP-12 IP-7): 400 `anfrage_ungueltig` (`feld`); 403
        `recht_fehlt` (`rolle_noetig`, null bei der Unterstützung - „nie“); 404 `nicht_gefunden`,
        `geltung_unbekannt` (`feld`), `stand_gibt_es_nicht` (`nr`, `neueste_nr`); 409
        `bericht_gibt_es_schon` (`kennung`), `entwurf_veraltet` (`datenstand_uebermittelt`,
        `datenstand_aktuell` - null, wenn der gespeicherte Entwurf selbst veraltet ist -, `abweichungen`
        gegen den gültigen Stand, `aenderungen` seit dem Datenstand), `anstoss_nicht_offen` (`zustand`),
        `gleichzeitig`; 422 `vorlage_unbekannt` (`feld`), `keine_quellen` (`feld`),
        `zeitraum_nicht_zu_ende` (`moeglich_ab`), `werte_vorlaeufig` (`vorlaeufig`, `vorlaeufige`,
        `moeglich_ab`), `begruendung_fehlt` (`feld`, `zeichen`), `basis_fehlt` (Leistungsvergleich ohne freigegebene
        Bezugsbasis, AP-17 IP-21b), `ausgabe_fehlt` (eine Vorlage ohne PDF/CSV; seit AP-17 IP-22 keine), `sitzung_fehlt` · `leitung_fehlt` (`tag`) · `beschluss_fehlt` (Freigabe einer Managementbewertung nur mit Sitzung, Leitung und Beschluss, AP-19 IP-23); 500 `abzug_beschaedigt` (`nr`). Zeitpunkte
        der Fakten mit dem Versatz der Zone der Geltung.
      required: [code, message]
      properties:
        code:
          type: string
          enum: [anfrage_ungueltig, nicht_gefunden, recht_fehlt, vorlage_unbekannt, geltung_unbekannt, bericht_gibt_es_schon, keine_quellen, zeitraum_nicht_zu_ende, werte_vorlaeufig, entwurf_veraltet, stand_gibt_es_nicht, abzug_beschaedigt, begruendung_fehlt, anstoss_nicht_offen, gleichzeitig, basis_fehlt, ausgabe_fehlt, sitzung_fehlt, leitung_fehlt, beschluss_fehlt]
        message: { type: string }
        feld: { type: string }
        rolle_noetig: { type: string, nullable: true }
        kennung: { type: string }
        nr: { type: integer }
        neueste_nr: { type: integer, nullable: true }
        moeglich_ab: { type: string, format: date-time }
        vorlaeufig: { type: integer }
        vorlaeufige: { type: array, items: { type: string } }
        datenstand_uebermittelt: { type: string, format: date-time }
        datenstand_aktuell: { type: string, format: date-time, nullable: true }
        abweichungen: { type: array, items: { $ref: "#/components/schemas/BerichtAbweichung" } }
        aenderungen:
          type: array
          items:
            type: object
            required: [quelle, art, zeitpunkt]
            properties:
              quelle: { type: string, nullable: true }
              art: { type: string }
              zeitpunkt: { type: string, format: date-time }
        zustand: { type: string }
        zeichen: { type: integer }
    BerichtAnlegen:
      type: object
      description: >
        Der Körper von `POST /api/v1/berichte` (streng gelesen).
      additionalProperties: false
      required: [vorlage, geltung_id]
      properties:
        vorlage: { type: string, description: "Ein Schlüssel aus GET /api/v1/bericht-vorlagen." }
        geltung_id: { type: string, format: uuid, description: "Der Standort; beim Unternehmensbericht das Unternehmen." }
        zeitraum: { type: string, pattern: "^([0-9]{4}|[0-9]{4}-(0[1-9]|1[0-2])|[0-9]{4}-(0[1-9]|1[0-2])/[0-9]{4}-(0[1-9]|1[0-2]))$", description: "2026-10 (Monat), 2026 (Jahr) bzw. 2025-11/2026-10 (Datengrundlage); nur bei der energetischen Bewertung optional, dann die letzten zwölf vollen Monate." }
        kennzahlen_abgewaehlt:
          type: array
          items: { type: string, format: uuid }
          maxItems: 200
          description: >
            V3 (AP-12 IP-14), freiwillig: die Kennzahlen (`id`), die dieser Bericht weglässt; fehlt die Liste oder ist sie
            leer, sind alle Kennzahlen der Geltung gewählt. Kein Array, ein Eintrag ohne UUID-Text, mehr als 200 Einträge
            oder eine Kennzahl, die es im Kundenbereich nicht gibt → 400 `anfrage_ungueltig` mit `feld` =
            `kennzahlen_abgewaehlt`; doppelte fallen zusammen. Die Abwahl steht vor der ersten Bildung des Entwurfs.
        kennzahl:
          type: string
          format: uuid
          description: >
            AP-17 IP-21b (S1, V4): genau beim Leistungsvergleich Pflicht, bei jeder anderen Vorlage 400 `anfrage_ungueltig`
            (`feld` = `kennzahl`). Die Kennzahl, deren Vergleich mit ihrer Bezugsbasis der Bericht zeigt — ein Bericht je
            Geltung × Zeitraum × Kennzahl (409 `bericht_gibt_es_schon`). Geltung (Standort oder Unternehmen) und
            Zeitraum-Art (Monat, Jahr, Datengrundlage) folgen aus `geltung_id` und `zeitraum`; am Standort nur eine Kennzahl
            dieses Standorts (sonst 400). Außerhalb der Sicht 404 `nicht_gefunden` (`feld` = `kennzahl`); ohne
            freigegebene Bezugsbasis-Fassung am letzten Tag des Zeitraums 422 `basis_fehlt`, nichts angelegt.
    BerichtFreigeben:
      type: object
      description: >
        Der Körper von `POST …/freigeben`: der Datenstand des Entwurfs, den die Person sah (F2).
      additionalProperties: false
      required: [entwurf_datenstand]
      properties:
        entwurf_datenstand: { type: string, format: date-time }
    BerichtWiedervorlage:
      type: object
      description: Wiedervorlage der energetischen Bewertung in Monaten, mit Begründung.
      additionalProperties: false
      required: [wiedervorlage_monate, begruendung]
      properties:
        wiedervorlage_monate: { type: integer, minimum: 1, maximum: 120 }
        begruendung: { type: string, minLength: 10, maxLength: 500 }
    BerichtAnstossVerwerfen:
      type: object
      description: >
        Der Körper von `POST …/anstoesse/{id}/verwerfen` (R4).
      additionalProperties: false
      required: [begruendung]
      properties:
        begruendung: { type: string, minLength: 10, maxLength: 500 }
    BerichtPerson:
      type: object
      description: >
        Wer etwas tat: der Name und - wo gespeichert - die Rolle, die das Recht gab.
      required: [name, rolle]
      properties:
        name: { type: string }
        rolle: { type: string, nullable: true }
    Bericht:
      type: object
      description: >
        Ein Bericht in der Liste und im Kopf: `stand_zeichen` ist der Vermerk aus R5 als Wort,
        `stand_text` sein Kundensatz („Entwurf · Datenstand …“, „Berichtsstand Nr. 1“, „Revision nötig —
        Korrektur K-2026-0007“, „Anstoß verworfen (…)“).
      required: [kennung, vorlage, vorlage_fassung, geltung_art, geltung_id, geltung_name, zeitraum_art, zeitraum, zeitraum_text, zeitzone, angelegt_von, angelegt_am, archiviert_am, stand_zeichen, stand_text, neueste_nr, entwurf_datenstand, wiedervorlage_monate, ueberpruefung]
      properties:
        kennung: { type: string }
        vorlage: { type: string }
        vorlage_fassung: { type: integer, minimum: 1 }
        geltung_art: { type: string, enum: [standort, unternehmen] }
        geltung_id: { type: string, format: uuid }
        geltung_name: { type: string, nullable: true }
        zeitraum_art: { type: string, enum: [monat, jahr, datengrundlage] }
        zeitraum: { type: string }
        zeitraum_text: { type: string }
        zeitzone: { type: string }
        angelegt_von: { $ref: "#/components/schemas/BerichtPerson" }
        angelegt_am: { type: string, format: date-time }
        archiviert_am: { type: string, format: date-time, nullable: true }
        stand_zeichen: { type: string, enum: [entwurf, berichtsstand, revision_noetig, anstoss_verworfen] }
        stand_text: { type: string, nullable: true }
        neueste_nr: { type: integer, nullable: true }
        entwurf_datenstand: { type: string, format: date-time, nullable: true }
        wiedervorlage_monate: { type: integer, minimum: 1, maximum: 120, nullable: true, description: "Nur bei der energetischen Bewertung; Startwert 12." }
        ueberpruefung:
          allOf: [{ $ref: "#/components/schemas/BerichtUeberpruefung" }]
          nullable: true
          description: "AP-16 S5/S6 (IP-24): nur an einer energetischen Bewertung mit freigegebenem Stand, sonst null."
    BerichtUeberpruefung:
      type: object
      description: >
        AP-16 S5/S6 (IP-24): beim Abruf abgeleitet, nie geschrieben (kein Läufer, kein Ereignis). `faellig_am` = Tag der
        Freigabe des jüngsten gültigen Stands (Zone des Berichts) + `wiedervorlage_monate`; ab diesem Tag ist
        `ueberpruefung_faellig` wahr, am Frist-Tag selbst mit `faellig_seit_tagen` 0. Hat eine andere nicht archivierte
        Bewertung desselben Unternehmens einen später freigegebenen Stand, ist diese abgelöst (`abgeloest_durch`) und hat
        keine Frist. Wesentliche Einsätze (wirksame Einstufung), ihre Verantwortlichen und offene Messbedarfe gelten am
        Abruftag.
      required: [stand_nr, stand_vom, wiedervorlage_monate, faellig_am, ueberpruefung_faellig, faellig_seit_tagen, abgeloest_durch, wesentliche_einsaetze, offene_bedarfe, verantwortliche, ohne_verantwortliche]
      properties:
        stand_nr: { type: integer, minimum: 1 }
        stand_vom: { type: string, format: date }
        wiedervorlage_monate: { type: integer, minimum: 1, maximum: 120 }
        faellig_am: { type: string, format: date, nullable: true }
        ueberpruefung_faellig: { type: boolean }
        faellig_seit_tagen: { type: integer, minimum: 0, nullable: true }
        abgeloest_durch: { type: string, nullable: true, description: "Kennung der Bewertung, die diese ablöst." }
        wesentliche_einsaetze: { type: integer, minimum: 0 }
        offene_bedarfe: { type: integer, minimum: 0 }
        verantwortliche:
          type: array
          items:
            type: object
            required: [name, einsaetze]
            properties:
              name: { type: string }
              einsaetze: { type: array, items: { type: string } }
        ohne_verantwortliche: { type: array, items: { type: string }, description: "Wesentliche Einsätze ohne verantwortliche Person." }
    BerichtListe:
      type: object
      required: [berichte]
      properties:
        berichte: { type: array, items: { $ref: "#/components/schemas/Bericht" } }
    BerichtStandRef:
      type: object
      description: Ein Berichtsstand in einer Folgen-Karte - „BR-2026-0001 Nr. 2“.
      required: [kennung, nr]
      properties:
        kennung: { type: string, example: BR-2026-0001 }
        nr: { type: integer, minimum: 1 }
    BerichteBetroffen:
      type: object
      description: >
        GET /api/v1/berichte/betroffen (AP-12 IP-9): die Zeile „Freigegebene Berichte: …“ der Folgen-Karten.
      required: [anlass, gilt_ab, berichte_vorhanden, betroffen, zitieren]
      properties:
        anlass: { type: string, enum: [zuordnung_rueckwirkend, anlage_umzug_rueckwirkend, flaeche_rueckwirkend, verteilung_rueckwirkend] }
        gilt_ab: { type: string, format: date }
        berichte_vorhanden: { type: boolean }
        betroffen: { type: array, items: { $ref: "#/components/schemas/BerichtStandRef" } }
        zitieren: { type: array, items: { $ref: "#/components/schemas/BerichtStandRef" } }
    BerichtStandKurz:
      type: object
      description: >
        Ein Berichtsstand im Verlauf - ohne Abzug.
      required: [nr, datenstand, freigegeben_am, freigegeben_von, pruefsumme, ersetzt_durch_nr, anlass_anstoss_id]
      properties:
        nr: { type: integer, minimum: 1 }
        datenstand: { type: string, format: date-time }
        freigegeben_am: { type: string, format: date-time }
        freigegeben_von: { $ref: "#/components/schemas/BerichtPerson" }
        pruefsumme: { type: string, pattern: "^sha256:[0-9a-f]{64}$" }
        ersetzt_durch_nr: { type: integer, nullable: true }
        anlass_anstoss_id: { type: string, format: uuid, nullable: true }
    BerichtAnstoss:
      type: object
      description: >
        Ein Revisions-Anstoß an Stand `nr` (R1–R4); `anlass_text` in Kundensprache.
      required: [id, nr, art, anlass_kennung, anlass_fassung, anlass_text, erkannt_am, zustand, erledigt_durch_nr, verworfen_begruendung, verworfen_von, verworfen_am]
      properties:
        id: { type: string, format: uuid }
        nr: { type: integer, minimum: 1 }
        art: { type: string, enum: [korrektur_freigegeben, korrektur_zurueckgenommen, ersatzwert_wirksam, ersatzwert_zurueckgenommen, bezugsgroesse_fassung, kennzahl_fassung_rueckwirkend, zuordnung_rueckwirkend, anlage_umzug_rueckwirkend, flaeche_rueckwirkend, verteilung_rueckwirkend, einstufung_fassung, kriterien_fassung, umfang_fassung, messbedarf_zustand, prozess_zuordnung_rueckwirkend, messmittel_angabe, bezugsbasis_anstoss, bezugsbasis_fassung, bezugsbasis_beendet] }
        anlass_kennung: { type: string }
        anlass_fassung: { type: integer, nullable: true }
        anlass_text: { type: string }
        erkannt_am: { type: string, format: date-time }
        zustand: { type: string, enum: [offen, erledigt, verworfen] }
        erledigt_durch_nr: { type: integer, nullable: true }
        verworfen_begruendung: { type: string, nullable: true }
        verworfen_von: { allOf: [{ $ref: "#/components/schemas/BerichtPerson" }], nullable: true }
        verworfen_am: { type: string, format: date-time, nullable: true }
    BerichtDetail:
      type: object
      required: [bericht, staende, anstoesse]
      properties:
        bericht: { $ref: "#/components/schemas/Bericht" }
        staende: { type: array, items: { $ref: "#/components/schemas/BerichtStandKurz" } }
        anstoesse: { type: array, items: { $ref: "#/components/schemas/BerichtAnstoss" } }
    BerichtEntwurf:
      type: object
      description: >
        Der gespeicherte Entwurf nach der D4-Prüfung. `abzug` ist docs/contracts/v2/bericht.schema.json
        $defs/abzug als der gespeicherte kanonische Text; `teilansicht` die Standortnamen einer
        Teilansicht (G3), unternehmensweit null.
      required: [kennung, datenstand, gebildet_von, neu_gebildet, pruefsumme, kopf, teilansicht, abzug]
      properties:
        kennung: { type: string }
        datenstand: { type: string, format: date-time }
        gebildet_von: { type: string, enum: [anlegen, abruf, kaskade, struktur] }
        neu_gebildet: { type: boolean }
        pruefsumme: { type: string, pattern: "^sha256:[0-9a-f]{64}$" }
        kopf: { type: string }
        teilansicht: { type: array, items: { type: string }, nullable: true }
        abzug: { type: object, additionalProperties: true }
    BerichtAbweichung:
      type: object
      description: >
        Eine Abweichung zwischen Stand und Entwurf (R1); fehlt eine Seite, ist sie null.
      required: [quelle, menge_art, vorher, nachher, version, anlass]
      properties:
        quelle: { type: string }
        menge_art: { type: string, nullable: true }
        vorher: { type: string, nullable: true }
        nachher: { type: string, nullable: true }
        version: { type: string }
        anlass: { type: string, nullable: true }
    BerichtVergleich:
      type: object
      required: [kennung, gegen, entwurf_datenstand, abweichungen]
      properties:
        kennung: { type: string }
        gegen: { type: integer, minimum: 1 }
        entwurf_datenstand: { type: string, format: date-time }
        abweichungen: { type: array, items: { $ref: "#/components/schemas/BerichtAbweichung" } }
    BerichtStand:
      type: object
      description: >
        Ein freigegebener Berichtsstand. `pruefsumme_geprueft` ist immer true - ein Abzug, dessen
        Prüfsumme nicht stimmt, verlässt den Server nie. `darstellung`, `regelwerk` und `abzug` sind die
        eingefrorenen Texte (F3, A1).
      required: [kennung, nr, datenstand, freigegeben_am, freigegeben_von, pruefsumme, pruefsumme_geprueft, ersetzt_durch_nr, anlass_anstoss_id, vorlage_fassung, kopf, teilansicht, darstellung, regelwerk, abzug]
      properties:
        kennung: { type: string }
        nr: { type: integer, minimum: 1 }
        datenstand: { type: string, format: date-time }
        freigegeben_am: { type: string, format: date-time }
        freigegeben_von: { $ref: "#/components/schemas/BerichtPerson" }
        pruefsumme: { type: string, pattern: "^sha256:[0-9a-f]{64}$" }
        pruefsumme_geprueft: { type: boolean, enum: [true] }
        ersetzt_durch_nr: { type: integer, nullable: true }
        anlass_anstoss_id: { type: string, format: uuid, nullable: true }
        vorlage_fassung: { type: integer, minimum: 1 }
        kopf: { type: string }
        teilansicht: { type: array, items: { type: string }, nullable: true }
        darstellung: { type: object, additionalProperties: true }
        regelwerk: { type: object, additionalProperties: true }
        abzug: { type: object, additionalProperties: true }
    KorrekturEntscheidung:
      type: object
      description: >
        Die Entscheidung mit den vier Angaben aus E8: `ersteller` (Fassung 1), `entschieden_von`,
        `entschieden_am`, `begruendung` (die neue Fassung). `vieraugen` ist die Einstellung, unter der
        freigegeben wurde (null beim Zurücknehmen); `von_ersteller` = Ersteller und Entscheider sind
        dieselbe Person (bei einer Freigabe nur unter Vier-Augen aus möglich).
      required: [kennung, status, fassung, ersteller, entschieden_von, entschieden_am, begruendung, vieraugen, von_ersteller]
      properties:
        kennung: { type: string }
        status: { type: string, enum: [freigegeben, zurueckgenommen] }
        fassung: { type: integer, minimum: 2 }
        ersteller: { $ref: "#/components/schemas/KorrekturUrheber" }
        entschieden_von: { $ref: "#/components/schemas/KorrekturUrheber" }
        entschieden_am: { type: string, format: date-time }
        begruendung: { type: string }
        vieraugen: { type: boolean, nullable: true }
        von_ersteller: { type: boolean }
    KorrekturFreigabeFehler:
      type: object
      description: >
        Die Ablehnung der Korrektur-Routen (AP-08 IP-15): 400 `anfrage_ungueltig` (`feld`); 403
        `recht_fehlt` (`rolle_noetig`, null bei einem Unterstützer - „nie“), `zweite_person_noetig`
        (`rolle_noetig` null); 404 `nicht_gefunden`; 409 `status_passt_nicht` (`status`), `gleichzeitig`,
        `unternehmen_nicht_angelegt`; 422 `begruendung_fehlt` (`feld`, `zeichen`).
      required: [code, message]
      properties:
        code:
          type: string
          enum: [anfrage_ungueltig, nicht_gefunden, recht_fehlt, zweite_person_noetig, begruendung_fehlt, status_passt_nicht, gleichzeitig, unternehmen_nicht_angelegt]
        message: { type: string }
        feld: { type: string }
        rolle_noetig: { type: string, nullable: true }
        status: { type: string }
        zeichen: { type: integer }
