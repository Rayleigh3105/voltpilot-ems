# Contracts v2 (EMS replatforming track)

**BINDING interface contracts for the VoltPilot EMS-v2 track** — the flow-based, multi-entity
platform (E0 "Contract-Artefakte"). Everything under `docs/contracts/` (the 1.0 files) is
**FROZEN and untouched**; v2 lives here, alongside.

| File | Contract |
|---|---|
| [`edge-desired-arbitration.md`](./edge-desired-arbitration.md) + [`edge-desired.schema.json`](./edge-desired.schema.json) | Local-bus v2 entity topic family; desired ("Wunsch") payloads from flows; priority classes, TTL, ownership/conflicts; arbitration result events. |
| [`mqtt-schedule-2.0.md`](./mqtt-schedule-2.0.md) + [`mqtt-schedule-2.0.schema.json`](./mqtt-schedule-2.0.schema.json) | Multi-entity plan Cloud → Edge on `ems/{t}/{s}/{d}/v2/plan` (retained); generic command vocabulary; 1.0 staleness/x-failsafe semantics carried over and generalized per entity. |
| [`plan-execution-ownership.md`](./plan-execution-ownership.md) | Decision record: the Go core remains the executor; flows emit desires, never register writes. Sequence diagrams for the four canonical situations. |
| [`flow-graph.md`](./flow-graph.md) + [`flow-graph.schema.json`](./flow-graph.schema.json) | The typed flow model: catalog node instances, typed ports, edges, triggers, validation rules, lifecycle. |
| [`flow-artifact.md`](./flow-artifact.md) + [`flow-artifact.schema.json`](./flow-artifact.schema.json) | The deployable compiled flow: manifest (hash, version gates, capability requirements), Node-RED tab bundle, retained deployment set on `…/v2/flows`, heartbeat ack, rollback, reseed coexistence. |
| [`edge-simulator-v2.md`](./edge-simulator-v2.md) | The hardware-free test rig proving Flow → desired → arbitration → guards → write-readback; reused v1 assets vs. new components. |
| [`mqtt-telemetry-2.0.md`](./mqtt-telemetry-2.0.md) + [`mqtt-telemetry-2.0.schema.json`](./mqtt-telemetry-2.0.schema.json) | Multi-entity telemetry Edge → Cloud on `ems/{t}/{s}/{d}/v2/telemetry` (E1a): per-entity numeric channels; envelope-only cloud validation. |
| [`edge-entity-config.md`](./edge-entity-config.md) + [`edge-entity.schema.json`](./edge-entity.schema.json) | The E1a half of the entity topic family: retained per-entity config, local per-entity telemetry, core-owned retained command, and the cloud → edge entity-registry push on `…/v2/entities`. |
| [`telemetry-v2-raw.event.schema.json`](./telemetry-v2-raw.event.schema.json) | Redpanda `telemetry-v2.raw` event (Ingest → Writer, next to the untouched v1 `telemetry.raw`). |
| [`anwendung-vectors.json`](./anwendung-vectors.json) | Geteilte Vektoren der ANWENDUNGS-Ableitung (Anwendungs-Programm Stufe 1): welche Anwendung eine Anlage von selbst aktiviert und welche Voraussetzung erfüllt ist. Kein Draht-Vertrag — sie pinnen die Zwillinge Java `profile/AnwendungDerivation` ⟷ TS `src/anwendungen.ts` (und, für die vier Modus-Arten, `surface.ts activeModes`) gegeneinander, das `usage-profile-vectors.json`-Muster. Der BESCHREIBENDE Teil einer Anwendung lebt im Katalog `anwendungen/catalog.json` (Server + byte-gleiche Portal-Kopie), nicht hier. |
| [`uems-zustand-vectors.json`](./uems-zustand-vectors.json) + [`uems-zustand.schema.json`](./uems-zustand.schema.json) | Geteilte Vektoren des UEMS-ZUSTANDSVOKABULARS „liefert Daten“ und „steuert“ (AP-00 IP-3). Die Prosa-Wahrheit steht in [`docs/fachmodell/zustaende.md`](../../fachmodell/zustaende.md); hier steht, wie aus Fakten GENAU EIN Zustand und GENAU EIN Kundensatz wird: Toleranz = `min(max(3 × Kadenz, 300 s), 86 400 s)` (AP-07 E9), Lücke der Reihe ab 2 × Kadenz, und die Reihenfolge der „steuert nicht“-Gründe (angehalten → nicht freigegeben → Funktion nicht gestartet → kein Betriebsmodell → Box meldet sich nicht → Box bestätigt nicht). Kein Draht-Vertrag — sie pinnen die Zwillinge Java `uems/ZustandAbleitung` ⟷ TS `src/uemsZustand.ts` gegeneinander, das `usage-profile-vectors.json`-Muster. ⚠ Noch ruft niemand an: in `services/api` und im Portal lebt weiterhin das harte 5-Minuten-Fenster. |
| [`examples/`](./examples/) | ajv-validated fixtures: ≥ 2 valid + 1 invalid per schema ([`examples/README.md`](./examples/README.md)). |

## v1 / v2 coexistence philosophy

1. **The 1.0 contracts are frozen.** Deployed edges keep speaking them forever; no dual-publish
   obligation ever lands on an old device. Evolution of 1.0 remains what it always was:
   additive optional fields only.
2. **Dual-consume, not migration.** The cloud consumes both eras side by side (writer/ingest
   learn v2 inputs next to v1 in E1a); a device is EITHER v1 OR v2 at any moment, and switching
   is an explicit, reversible cutover (E13a), rehearsed on the simulator rig first.
3. **Separation by topic, not by version negotiation.** All v2 cloud topics live in the
   per-device `ems/{t}/{s}/{d}/v2/#` subtree; the ACL's default-deny keeps v1 devices blind to
   it. Retained slots of the two eras can never collide — which is what makes the shadow phase
   (v2 publishes, v1 controls) possible at all.
4. **Per-contract `schema_version`.** Version numbers belong to a contract, not to the platform
   era: `mqtt-schedule-2.0` is genuinely the second version of the schedule contract; brand-new
   contracts (desired, flow-graph, flow-artifact) start at `1.0` even though they are v2-track
   artifacts. Bump on breaking change; consumers stay tolerant of unknown additive fields.
5. **The guard line is non-negotiable in every era.** Whatever commands an entity — 1.0
   schedule, 2.0 plan, a user flow, later an EEBUS master — the Go core's restrict-only guard
   chain clamps it before any certified register write. v2 changes who may WISH, never who
   ENFORCES.

## Decision log

| # | Decision | Chosen | Rationale (short — details in the linked doc) |
|---|---|---|---|
| D-1 | Schedule 2.0 coexistence: separate topic vs `schema_version` negotiation on `…/schedule` | **Separate topic** `…/v2/plan` | One retained message per topic — negotiation would clobber the v1 retained plan; shadow phase needs both plans side by side; subscription IS the negotiation; rollback trivial. ([schedule 2.0 §1](./mqtt-schedule-2.0.md)) |
| D-2 | v2 cloud topic namespace | **One `v2/#` subtree per device**, two wildcard ACL grant lines (sub down / pub up) | The ACL grant template lives in three hand-lockstep writers (three real prod outages); one template change covers ALL current and future v2 topics (plan, flows, prices, telemetry). E14 ACL consolidation is the prerequisite. ([schedule 2.0 §1](./mqtt-schedule-2.0.md)) |
| D-3 | Local entity topic family | **Per-entity topics** `edge/entities/{id}/config·telemetry·desired·command·readback·arbitration` | Follows the proven per-source `edge/sources/{id}/telemetry` scaling + error isolation; per-entity retained config beats the v1 all-in-one array (subscribe-what-you-use, retained-clear per entity, wildcard enumeration). ([desired §1](./edge-desired-arbitration.md)) |
| D-4 | Priority model | **5 classes** safety > grid > contract > market > flow; safety/grid/contract manifest today as guard clamps, reserved as desired classes for the E8 masters; flows are always class `flow` (core-enforced) | Compliance never competes — it clamps; matches D9 (co-optimization reconciles upstream, the edge cascade secures) and the E8 research ("Netzbetreiber gewinnt immer" > DV > site optimizer). ([desired §3](./edge-desired-arbitration.md)) |
| D-5 | Flow vs. plan on one entity | **`market` > `flow` by default; explicit TTL-bounded `override` (≤ 4 h) elevates a flow desire above market only** | D9 makes the solver the arbiter of multi-use; the override is the "boost" escape hatch — owner intent may cost money ("kann nerven"), never compliance. ([desired §4](./edge-desired-arbitration.md)) |
| D-6 | Same-class conflicts | **Holder keeps; challenger rejected (`conflict`) until TTL lapse** — no last-writer-wins; activation-time exclusive-resource validation prevents the case | Two flows must never oscillate a physical device; the editor refuses the conflict before it exists. ([desired §4](./edge-desired-arbitration.md), [flow-graph V-5](./flow-graph.md)) |
| D-6a | The MANUAL INTERVENTION lost against a holding rule | **Source `local-ui` + `override` ranks 75 (above a rule's 70, below `contract`) and is EXEMPT from the D-6 same-class rejection** — in both directions of the pairing: the intervention preempts a holding rule, and a rule challenging a held entity is stored + `rejected`/`priority` (not `conflict`), so its 15-s re-emission lets it resume seamlessly on expiry. Two interventions share ONE `local-ui` slot per entity, so the later replaces the earlier. Rule-vs-rule, the guard chain, the 4-h cap and the plant pause are untouched. | Both the Sofortaktion and the storage intervention travelled as class `flow` + override = rank 70 — the same rank a compiled `must_run` rule carries — so D-6 rejected the human for as long as the rule kept renewing (concept `vp-verbrauchsmgmt-konzept-v1` §1.2 S9 / §2.2 K1). The intervention is the only wish with a person behind it; the Captain ordering is *Schutz > Handeingriff > Regel*. ([desired §3+§4](./edge-desired-arbitration.md)) — 2026-08-31, Verbrauchsmanagement v1, Korrektur K1. |
| D-7 | Desired retention/TTL | **Desires are never retained; `ttl_s` required** | A retained wish from a dead flow is a command from the past; core-owned OUTPUTS stay retained (command topic), inputs expire. ([desired §2/§5](./edge-desired-arbitration.md)) |
| D-8 | `charge_from_grid_allowed` default | **Absent = NOT allowed** (only explicit `true` releases the solar-only clamp) | Promotes the shipped edge's deliberate fail-safe deviation into the contract — see discrepancy log #1. ([schedule 2.0 §4](./mqtt-schedule-2.0.md)) |
| D-9 | Failsafe location | **Entity registry config, not the plan** — except the PS-3 staleness survivors (`grid_import_limit_kw`, `reserve_soc_pct`), which stay plan-carried | A failsafe carried only in the plan dies with the plan; the peak survivors are restrict-only, verified v1 behavior. ([schedule 2.0 §3](./mqtt-schedule-2.0.md)) |
| D-10 | Execution ownership | **Go core remains the executor**; flows consume plan data and emit desires; drivers execute only core-published commands | The reliability substrate and guard chain are the proven asset; safety separation must be structural (D1: editor for everyone). ([plan-execution-ownership.md](./plan-execution-ownership.md)) |
| D-11 | Flow artifact delivery | **Inline retained deployment set** on `…/v2/flows`, size-budgeted (256/512 KiB); out-of-band fetch reserved | The retained-config self-wiring pattern is proven three times over; retained = convergence on reconnect with zero round-trips. ([flow-artifact §3](./flow-artifact.md)) |
| D-12 | Reseed coexistence | **Per-tab ownership markers** (`@vp-flow`); reseed replaces the vendor tab group only; retained deployment self-heals dropped artifact tabs | Today's wholesale `flows.json` reseed is fatal to user flows — see discrepancy log #3. ([flow-artifact §4](./flow-artifact.md)) |
| D-13 | Control claims in the graph | **Explicit `claims` on node instances** (editor-filled from the catalog) | Exclusive-resource validation must work generically, without the validator knowing every node type's parameter schema. ([flow-graph §1](./flow-graph.md)) |
| D-14 | Command vocabulary | `setpoint_kw`, `on_off`, `limit_pct`, `limit_kw`, `mode` — shared verbatim between desired and plan | `limit_kw` added to the ticket's four: absolute caps are what curtailment/§14a plans emit (v1 `pv_limit_kw` precedent); limits are reduce-only by contract. ([desired §2](./edge-desired-arbitration.md)) |
| D-15 | Generic Modbus in the flow graph vs. flow-graph §6 "no registers" | **Catalog types in the `vp.modbus.*` domain MAY carry transport-level parameters** (host, register address, data type) — a scoped power-user escape hatch: runtime-`edge`-only, read (`vp.modbus.read`) free, write governance-gated (AE7) and outside the guard/arbitration model; a read's optional entity mapping publishes normal edge-entity telemetry and is validator-refused onto COMPOSED types (guard-chain inputs). The entity abstraction remains the default and the only certified path — §6 is amended by exactly this exception, not eroded. | "Lease any Modbus device" needs raw register access by definition; scoping it to one catalog domain + the existing governance/validator machinery keeps the abstraction boundary enforceable. ([flow-graph §6](./flow-graph.md)) |
| D-16 | Sandboxed code node vs. flow-graph §6 "no free code" | **A sandboxed code node (`vp.logic.function`) is permitted as a CATALOG ADDITION** — no graph-schema change: runtime **`edge` only** (the cloud never executes customer code), the compiled Node-RED `function` node is wrapped by flowc in a **CPU/time watchdog** (`timeout_ms` ≤ 500, abort → `node.status` red + `node.error`, never a silent hang), the sandbox context carries **no network handles** (`net`/`http`/`https` are shadowed inside the wrapper scope), and every device effect still leaves through `vp-desired` → arbitration → the **guard chain**. Customer code can therefore only ever *wish*; §6's "no free code" is amended by exactly this exception (the entity abstraction + the guard/arbitration model remain the only certified control path). | Portal v3 makes the editor a real Node-RED-quality surface for customers; the honest alternative to free code is customers hand-editing the edge's Node-RED (which "Layer 1 is wired by VoltPilot" forbids). The safety argument is structural — the guard chain clamps a code node's command exactly like any other. ([flow-graph §6](./flow-graph.md)) |
| D-17 | The device does not know which of its OWN sources feeds which entity (the adoption pin lives only in the cloud DB), so its local display could only order-guess producer values — the Pilsting "vier Erzeuger, drei davon –" symptom (vp-vier-erzeuger-p9). | **The registry-push `descriptor` carries the OPTIONAL, ADDITIVE `edge_source_id`** — the cloud's `measurement_point.edge_source_id` pin, echoed so the device can map its own source readings onto the entity DETERMINISTICALLY for its LOCAL display (Topology / `:8484`). Display-only on the device: it never feeds the store-and-forward buffer, the v2 uplink or the heartbeat's observed Ist (the ComposeLocal boundary — the cloud fan-out stays the single telemetry_v2 writer). An old edge ignores the field; an old cloud simply omits it. | 2026-07-29, Geister-Fix PR 4a. |
| D-18 | How to model steuerbare Verbraucher (controllable consumers) without a second automation system (docs/verbrauchssteuerung.md). | **A declarative per-consumer `ConsumerPolicy` (`consumer-policy.schema.json`), NOT a second flow/automation model.** One policy governs exactly one consumer entity; its `requirements[]` carry the four operating shapes (reactive / fixed_window / flexible_task / opportunistic), merged into ONE runtime target (§7). The semantic rules JSON Schema can't express (kind/enforcement coherence, `must_run`⇒grid allow, condition-tree depth, cloud-vs-local signal hysteresis per D1, catalog signal names, the D4 `power_ranges_kw` disjointness, 24:00 normalisation) live in `consumer-policy-vectors.json` and are enforced by the `ConsumerPolicyValidator` twins (Java + TS), the FlowGraphValidator precedent. `content_hash` is derived server-side from canonical JSON. Increment 1 (§19) ships the contract + master data + customer CRUD + the Anlagen-Modell assistant only — no compiler, optimizer or edge command; every surface says honestly "Steuerung noch nicht aktiviert". | 2026-08-09, Konzept-Review; Inkrement 1. |
| D-19 | How a reactive consumer policy reaches the edge without a second deploy path — and without handing customer flows the plan-override lever (docs/verbrauchssteuerung.md §13.2/D1). | **The policy compiler derives at most ONE generated flow per consumer and rolls it out through the EXISTING flowc/flow-deployment machinery.** Three additive pieces: (1) the flow-graph document gains an OPTIONAL `origin` marker (`kind: consumer-policy` + policy identity) that is **server-stamped only** — the flow save API refuses customer documents carrying it; (2) a new **generated-only catalog type `vp.consumer.reactive`** (all three catalogs, `generated: true` in api/portal) holds the compiled reactive spec — precomputed UTC windows for cloud (price) conditions per D1, local signals (availability / SoC / PV-surplus / grid-flow) with three-state logic, `max_age_s` freshness and hysteresis, and the requirement's `must_run` → the D-5 `override` on its desires (short, continuously renewed TTLs; the 4-h override cap holds). The validator twins + flowc refuse the type in any document WITHOUT the origin marker; (3) `override` on `vp.entity.control` is REFUSED everywhere — the compiler-stamped generated artifact is the only source of override desires, so no catalog flow can outrank the market plan. One node = one entity claim, so V-5 exclusivity holds unchanged. | 2026-08-09, Verbrauchssteuerung Inkrement 4. |
| D-20 | A `required_by_deadline` flexible task lives ONLY in the cloud plan, so a cloud outage past "deadline minus remaining need" silently misses the customer's duty (docs/verbrauchssteuerung.md §13.5/§19 Inkrement 6, review decision D5). | **The edge-local deadline fallback, fed by an ADDITIVE `flex_requirements` block on the registry-push `descriptor`** (edge-entity.schema.json `$defs/flex_requirement`): the cloud composes the ACTIVE policy's deadline duties (recurrence window + timezone, demand, RESOLVED run power + D-14 command — power/target resolution stays a CLOUD truth) into the retained push; the device starts the task itself at the latest at `deadline − remaining need − margin`, but ONLY while no fresh v2 plan lies and only from CONFIRMED own progress (measured telemetry; unknown progress starts nothing). The wish enters the NORMAL chain via a new **internal arbitration class `deadline-fallback` (rank 50: below `market`, above plain `flow`, below the D-5 flow override)** — never `override`, never claimable by any external publisher (Parse rejects the source kind and no `classAllowed` entry exists), so a fresh plan and every reactive Pflichtregel preempt it seamlessly and ALL guards (consumer clamp, cycle guard, §14a/grid/contract) bind unchanged. Deliberate deviation from §7's ordering (fällige Aufgabe > Fahrplan): at the edge the fresh plan ALREADY CONTAINS the flexible task's dispatch, so the plan wins whenever fresh — the fallback is a Notnagel, not a second optimizer. Price/opportunistic/reactive requirements are NEVER pushed (a price rule without a window is `unknown` and stays off, §13.5). Old edges ignore the block; an old cloud simply omits it. | 2026-08-10, Verbrauchssteuerung Inkrement 6. |
| D-21 | The generated READ flow of a SELF-BUILT Modbus device needs a provenance marker, but `origin` was shaped for exactly one source (a consumer policy) with a `const` kind and a closed field set (vp-modbus-baukasten-k6 §2.2, Einheitsmodell Stufe 3). | **`origin` becomes a discriminated `oneOf` of CLOSED branches; the second branch is `kind: "modbus-device"` (`point_id` + `definition_version`).** Both branches keep `additionalProperties: false`, so a document can never mix the two provenance vocabularies - the discriminator is the only thing a reader needs. The marker unlocks NOTHING here (a device read flow carries only the free `vp.modbus.read`): it says the platform owns this flow, so portals open the device assistant instead of the flow editor, and it names the definition version the plan was compiled from - which IS `flow_version`, so rolling a definition back redeploys its own read plan. The D-19 rule that the save API refuses ANY customer-supplied `origin` is untouched and now guards both kinds. | 2026-08-11, Einheitsmodell Stufe 3. |
| D-22 | A SELF-CONNECTED battery (DIYBMS, Seplos, JK, an ESP at a shunt) publishes its raw values on a LOCAL MQTT broker, in whatever field names its firmware happens to use — and the customer's real pack sends 11 banks × 16 cells as 176 SEPARATE topics (`emon/diybms/<bank>/<cell>`, `.voltage`). Neither the entity model nor the flow catalog had a way to read that (`vp-deye-diybms-luecke-l5` §3.2b, Paket P5). | **Two additive catalog pieces plus a third `origin` branch — no schema release beyond that branch.** (1) A new entity type **`user-defined-battery`** whose `default_measure` IS the closed target vocabulary of level 1 (`soc_pct`, `voltage_v`, `current_a`, `power_kw`, `cell_min_mv`, `cell_max_mv`, `temp_max_c`, `charge_allowed`, `discharge_allowed`, `charge_limit_a`, `discharge_limit_a`); a created battery declares only the channels actually MAPPED — promising a state of charge nobody reads would be an invented measurement. (2) A **generated-only catalog type `vp.mqtt.read`** carrying the user-defined field mapping: per target channel a topic FILTER (`+`/`#`), a value path, an aggregate (`last`/`min`/`max`/`sum`/`avg`/`count`) over every matching topic, unit scaling, an explicit `sentinel` raw value meaning "not measured", and a per-sample `stale_s`. The aggregate is the whole point: `min`/`max` over `emon/diybms/+/+` turns 176 cell topics into two channels. ONE node per device (one broker connection, shared subscriptions), never one per channel. (3) `origin` gains the closed branch **`kind: "mqtt-device"`** (`point_id` + `definition_version`) which unlocks that type and nothing else — there is deliberately no FREE MQTT block while the mapping UI with live preview (P5d) is still to come, so the api is its single author. Broker credentials are deliberately NOT in the contract: a password inside a retained, rolled-out flow document would be a cleartext secret, and the secret path ships with the HTTP read type. `soc_derivation` is reserved in the stored definition as the docking point for level 2 (P5b); today only `direct` exists and the computing methods are refused BY NAME rather than stored unexecuted. | 2026-09-09, P5 Ebene 1. |
| D-23 | The self-connected battery of D-22 now has RAW values but still no state of charge: neither a Deye in voltage mode nor a DIYBMS without a shunt MEASURES one, and the customer computes it himself in his own Node-RED from the cell voltages (`vp-deye-diybms-luecke-l5` §3.2b Ebene 2, Paket P5b). Home Assistant is expressly NOT a source. | **One more generated-only catalog type under the SAME `mqtt-device` origin, plus one derived channel - no schema release.** (1) **`vp.soc.derive`** carries `soc_derivation = {method, prefer_direct, inputs, params, hold_s}` and computes ONE state of charge from the standard channels a level-1 source just delivered: `direct` takes over a measured `soc_pct`; `ocv_curve` interpolates two OCV→SoC curves linearly, clamps 0..100 and takes the CONSERVATIVE minimum of `f(highest cell, charge curve)` and `f(lowest cell, discharge curve)` - the customer's method 1:1; `coulomb` counts `SoC(t) = SoC(t-1) + P·dt/E·100` from an anchor, with optional recalibration at the voltage endpoints. It hangs on an EDGE behind the read node, never on the trigger: the tick hits the SOURCE, and the derivation computes on what it just sent. It is reusable for any level-1 source (MQTT today, HTTP later) because it depends on CHANNELS, not on a transport. (2) The type catalog gains the derived channel **`soc_source_code`** (1 = gemessen, 2 = berechnet:kennlinie, 3 = berechnet:ladungszählung), published in the SAME telemetry message as the derived `soc_pct`. A CODE and not a word because telemetry channels are numbers by contract (`$defs/channels`, `telemetry_v2.value` is `DOUBLE PRECISION`): it travels the PROVEN chain unchanged and stands per measurement time in the history, so a later definition change never falsifies an old row. There is deliberately no code 0 - an unknown state of charge is an ABSENT channel, never a reported zero, and it is the one channel nobody may MAP (mapping it would pass a computation off as a measurement). The honesty rules are load-bearing: no `soc_pct` without an input; a frozen value is never republished with a fresh timestamp, it simply ages, and after `hold_s` it is absent and a charge count needs a new anchor. Curve TEMPLATES are data (`services/api` `soccurves/catalog.json`); exactly one ships - the measured 176s NMC customer tables, byte-checked against `soc-derivation-vectors.json` - because an invented curve is not an offer but a wrong state of charge with decimal places. | 2026-09-09, P5b Ebene 2. |
| D-24 | The self-connected battery of D-22 could only be read over MQTT, yet "my BMS has a web interface" was the second most common starting point — the assistant already showed the HTTP connection kind, greyed out. Its template case is DIYBMS v4: `GET http://<controller>/ha` with an `ApiKey` header returns a flat JSON document (`vp-deye-diybms-luecke-l5` §3.2b "Ebene 1, HTTP/JSON"). | **A SECOND level-1 read type, additive — no schema release beyond one more `origin` branch.** (1) A generated-only catalog type **`vp.http.read`**: one endpoint (host/port/path/tls/timeout), one GET per tick, and per target channel a VALUE PATH instead of a topic filter. The path carries the wildcard `*` as a whole segment (`cells.*.v`) — the HTTP counterpart of `emon/diybms/+/+`, and without it the `min`/`max` aggregate would be a field with no meaning here. It is PATH-mandatory (an HTTP answer is a document, not a bare value, so the MQTT "empty path = the payload IS the value" reading has no equivalent) and it has NO `stale_s` (an answer is ONE point in time; what it does not contain is absent, and a previous answer is never republished with a fresh timestamp). ONE node per device and ONE request in flight per HOST — a BMS with a single web server holds no queue. (2) `origin` gains the closed branch **`kind: "http-device"`**, and it is its OWN kind on purpose: a level-1 read type valid under its sibling's origin would let a forged `mqtt-device` document unlock an HTTP fetch. Consequently `generated_origin` now accepts a STRING **or a list**, because `vp.soc.derive` belongs to BOTH level-1 origins — it works on the standard channels and does not know the transport. (3) **Credentials, the first in this kit:** header key / bearer / basic. The mode (and, for a header key, its name; for basic, the user name) travels in the flow document; **the SECRET never does** — a flow document is readable through `GET /sites/{siteId}/flows/{flowId}/versions/{v}` by every portal user of the tenant, so a password in it would be a password in the browser. It is stored as the TOP-LEVEL key `auth_secret` of the connection (so `ComponentSecrets.isSecretKey` masks it in every listing without needing a template) and reaches the box in the registry push, where the read node picks it up from the per-entity retained config. Without a secret the box does not even ask: a 401 would look like a device fault nobody caused. | 2026-09-09, P5-HTTP. |
| D-25 | The self-connected battery of D-22/D-23 now REPORTS - but nothing keeps VoltPilot's own setpoints under what the pack currently allows. The customer protects his pack himself, in his own Node-RED: a SoC→current staircase per direction plus a cell-voltage hysteresis hard stop, written straight into the Deye's current limits (`vp-deye-diybms-luecke-l5` §3.2b "OPTIONAL - Schutz-/Grenzbaustein" + §3.3, Paket P5c, evidence F24). | **One more generated-only catalog type under BOTH level-1 origins, plus one guard stage - no schema release.** (1) **`vp.bms.limit`** carries `{charge, discharge, hysteresis, inputs, round_a, hold_s}` and computes what the battery ALLOWS: a STAIRCASE ("the last support point with threshold <= SoC wins" - no interpolation, clamped BELOW the first threshold instead of extrapolated, and clamped to the device maximum) and a hysteresis LATCH (charging stops at the highest cell's stop threshold and resumes only at its resume threshold; discharging mirrors it on the lowest cell). It publishes `charge_limit_a`, `discharge_limit_a`, `charge_allowed`, `discharge_allowed` - channels the type catalog already knew since D-22, now with a second possible author. Like `vp.soc.derive` it hangs on an EDGE, never on the trigger, and sits LAST in the chain because its staircase needs the DERIVED state of charge - while its latch needs only cell voltages, which is why the block is still useful on a battery with no state of charge at all. (2) The guard chain gains the stage **`guard:bms_limit`**: `guards.Limits.Bms` is a POINTER (nil = the pack said nothing, and nothing changes - a zero value meaning "0 kW allowed" would shut down every plant that never had a protection block), applied TWICE like the rated band (before the §14a correction so it reasons about a reachable value, and after it so that correction can never push the setpoint back above what the battery allows). Amperes become kilowatts with the MEASURED pack voltage only - a guessed nominal voltage would invent a power limit no device stated; 0 A are 0 kW at any voltage. (3) ⚠ NOTHING WRITES. The customer flow writes the Deye's current limits; VoltPilot deliberately does not - the limits are PROVIDED, for the surface and as the guard cap, and an actual write path only opens behind the certification gate (`vp-deye-bench-cert` / `hybrid-control-p2`). Until then the customer flow remains the active writer and a known foreign influence. Honesty rules, load-bearing: what the block cannot say it does not say (no staircase without a state of charge, no permission without a cell voltage, an absent channel means "not said" - never 0, never "allowed"); a latched direction reports BOTH `allowed = 0` AND `limit_a = 0`; a channel has exactly ONE author (a mapped `charge_limit_a` and a computed one are refused together); and a MISSING `protection` block on save means "unchanged", never "gone" - a safety limit is removed only explicitly. Protection PROFILES are data (`protectionprofiles/catalog.json`); exactly one ships - the customer's measured tables, byte-checked against `limit-protection-vectors.json`. | 2026-09-09, P5c. |

### E2 implementation clarifications (additive; the contracts above are untouched)

| # | Clarification | Where implemented |
|---|---|---|
| E2-a | **Class-per-source enforcement**: `flow` AND `local-ui` sources may claim only class `flow` (the operator surface is flow-level owner intent and may use `override`); `plan-executor` only `market`; `cloud-command` the reserved `grid`/`contract` plus `market`. `safety` is accepted from NO source today — device protection stays a guard clamp until the E8 masters define a producer. | `internal/desired` `classAllowed` |
| E2-b | **Same-class challengers are rejected, not queued** (D-6 “until the holder's TTL lapses” = the challenger's RE-EMISSION wins after lapse — flows re-emit on triggers anyway). LOWER-class desires ARE kept within their TTL and resume as the contract §5 next-highest rule; a superseded holder resumes likewise. **D-6a exempts a pairing involving the manual intervention** (`local-ui` + `override`, rank 75): it is then the ordinary priority path, so the loser IS stored and resumes. | `internal/desired` `admit`/`selectHolder`/`manualIntervention` |
| E2-c | **On-device plan precedence during the shadow phase**: while a FRESH v1 plan exists it stays the battery entity's market injection (“v1 controls”); the v2 plan drives every entity the v1 plan does not command, and the battery too once no fresh v1 plan exists. | `internal/agent` `runPlanExecutors` |
| E2-d | **Source slot = the desired's replace key**: `kind` (+ `flow_id`+`node_id` for flows) — a new desired from the same source replaces its previous one; `request_id` correlates events and refreshes the TTL on re-emission. | `internal/desired` `Source.Key` |
| E2-e | **Reserved LOCAL feed topics** for compiled data nodes: `edge/prices`, `edge/forecast/pv` (retained, core → flows; fed with E4) and `edge/notify` (flow → core). Local v1-style namespace, documented in `localbus.go`; not cloud contracts. | `vp-feed`/`vp-notify` palette nodes |

## Discrepancy log (v1 reality vs. planning documents — the CODE wins)

| # | Discrepancy | Consequence here |
|---|---|---|
| 1 | The **1.0 schedule contract text** says absent `grid_charge_allowed` = allowed; the shipped Go edge (`plan.go SolarOnlyCharge()`) deliberately clamps fail-safe — only explicit `true` releases. | v2 adopts the code's reading as contract (D-8); the v2 optimizer must always publish the field for merchant storage. |
| 2 | The plan draft (§2.2) speaks of flows consuming `edge/entities/{id}/telemetry` as if the family existed; **in code it does not** — today there are `edge/telemetry` + `edge/sources/{id}/telemetry` (`localbus.go`, `sources.go`). | The entity family is defined HERE for the first time, justified against the per-source precedent (D-3); E1a builds it. |
| 3 | The plan draft says the reseed mechanism "learns" vendor-vs-user tab groups; **today's `reseed-entrypoint.sh` replaces `flows.json` wholesale** on template-hash change. | The contract binds the target per-tab-group behavior (D-12); until E2 lands it, deploying user flows to a device would be unsafe — the rig's P6 proof gates this. |
| 4 | The plan draft's §2.1 telemetry-2.0 shape (`entities.{id}.channels`) and the prices down-channel are named but unauthored. | Deliberately OUT of E0 scope; their topic homes are reserved in the `v2/#` subtree ([schedule 2.0 §1](./mqtt-schedule-2.0.md)) so the ACL template already covers them. |

## Validating the schemas

All schemas here are JSON Schema 2020-12 and ajv-compatible (like the 1.0 contracts, they use
`x-*` annotation keywords — compile with `strict: false` or declare the keywords). Fixture
expectations for the WIRE schemas are pinned in [`examples/README.md`](./examples/README.md).
`uems-zustand.schema.json` needs no fixture directory: it describes a vector file, and its
fixture IS [`uems-zustand-vectors.json`](./uems-zustand-vectors.json) — every case in that
file validates against it, and the twins' tests assert the vocabulary, the unit table and the
rule constants back against it.

## Versioning

Every payload carries `schema_version` (per contract — see coexistence philosophy #4). Bump on
any breaking change; keep consumers tolerant of unknown additive fields. Changing anything in
this directory after E0 is accepted is a contract change and needs the same discipline as
`docs/contracts/`.
