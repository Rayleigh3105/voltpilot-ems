{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://voltpilot.de/contracts/v2/bezugsdaten.schema.json",
  "title": "Bezugsdaten: Eingang, Ergebnis und die Vektor-Datei",
  "description": "Der Vertrag der BEZUGSDATEN (UEMS AP-09 §4, Prosa: bezugsdaten.md): wie aus einer gelieferten Zeile — getippt oder aus einer CSV-Datei — ein Wert einer Bezugsgröße wird, welches Urteil eine Zeile bekommt, wie Fassungen entstehen und wann etwas ein Befund ist statt einer Zahl. Er beschreibt die Vektor-Datei bezugsdaten-vectors.json, gegen die BEIDE Umsetzungen gefahren werden: Java services/api .../uems/BezugsdatenRegeln und TypeScript frontend/portal/src/bezugsdaten.ts. Ein Fall trägt Prüfungen; jede Prüfung nennt ihre Regel, ihren Eingang und ihr Ergebnis. Wer die Regel ändert, ändert die Vektor-Datei UND beide Zwillinge. E3: dies ist ein EIGENER Vertrag — der Messwert-Herkunftsvertrag (messwert-herkunft.schema.json) bleibt unberührt.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "herkunft",
    "zeitzone",
    "referenzunternehmen",
    "vokabulare",
    "einheiten",
    "umrechnung",
    "befund_saetze",
    "regeln",
    "zwillinge",
    "cases"
  ],
  "properties": {
    "_comment": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string" },
      "description": "Die Regel in Prosa — dieselbe wie in bezugsdaten.md."
    },
    "schema_version": {
      "type": "string",
      "const": "1.0",
      "description": "Fassung dieses Vertrags. Bleibt; Erweiterungen sind additiv."
    },
    "$schema_datei": { "type": "string", "description": "Verweis auf dieses Schema." },
    "herkunft": {
      "type": "string",
      "minLength": 1,
      "description": "Woher die Regeln stammen: Konzeptpaket, Regelnummern und Entscheide."
    },
    "zeitzone": {
      "type": "string",
      "const": "Europe/Berlin",
      "description": "Z1/Z5/E7: die Zeitzone des Standorts, in der Tag, Woche, Monat und Jahr liegen und in der ein Zeitstempel ohne Zone gedeutet wird. Ahrenberg liegt in Europe/Berlin; das Feld ist die Zeitzone DIESER Fälle, nicht eine Festlegung für jeden Kunden."
    },
    "referenzunternehmen": {
      "type": "string",
      "const": "./uems-referenzunternehmen.json",
      "description": "Die EINE Beispielwelt (Kunststoffwerk Ahrenberg GmbH). Bezugsgrößen, Messstellen, Personen und Beträge der Fälle stammen von dort."
    },
    "referenz_stand": {
      "type": "string",
      "minLength": 1,
      "description": "Welcher Stand des Referenzunternehmens gemeint ist — und was ein späteres Paket dort nachträgt, solange es nur hier steht."
    },
    "regeln_kurz": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 },
      "description": "Die sieben Kurzregeln, gegen die jeder Fall des Referenzfallkatalogs geprüft wurde (AP-09 §7), byte-gleich zur Vorlage."
    },
    "plan_abnahmen": {
      "type": "object",
      "additionalProperties": { "type": "string", "minLength": 1 },
      "description": "Die Plan-Abnahmen des Programms und der Fall, der sie einlöst."
    },
    "vokabulare": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "wertart",
        "geltung_art",
        "periode_art",
        "deutung",
        "herkunft_art",
        "status",
        "import_status",
        "zeilen_urteil",
        "befunde",
        "entscheidung",
        "vorgang"
      ],
      "description": "Die geschlossenen Vokabulare. Ein Wort außerhalb wird abgelehnt, nie auf eine Vorgabe aufgelöst (Invariante 6).",
      "properties": {
        "wertart": {
          "type": "array",
          "items": { "type": "string", "enum": ["periodenwert", "stand", "stammdatum"] },
          "description": "E1: genau EINE Wertart je Bezugsgröße. Periodenwert = Menge je Periode, Stand = Ablesung zu einem Zeitpunkt, Stammdatum = zeitgültiger Wert (Fläche)."
        },
        "geltung_art": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["unternehmen", "standort", "gebaeude", "bereich", "prozess", "kostenstelle", "messstelle"]
          },
          "description": "E1: die sieben Fachobjekte, auf die sich eine Bezugsgröße beziehen kann — genau EINES je Bezugsgröße."
        },
        "periode_art": {
          "type": "array",
          "items": { "type": "string", "enum": ["tag", "woche", "monat", "jahr"] },
          "description": "Z1: Kalenderperioden in der Zeitzone des Standorts. Woche = Montag bis Sonntag."
        },
        "deutung": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["periode", "periodenbeginn", "periodenende", "von_bis", "zeitpunkt"]
          },
          "description": "Z3: wie eine Datumsspalte zu lesen ist. Steht in der Zuordnungs-Vorlage, wird nie geraten."
        },
        "herkunft_art": {
          "type": "array",
          "items": { "type": "string", "enum": ["eingabe", "import", "messkanal", "stammdatum_ap02"] },
          "description": "§4.8: woher ein Wert kommt. Jeder Wert trägt genau eine Art."
        },
        "status": {
          "type": "array",
          "items": { "type": "string", "enum": ["wirksam", "vorschlag", "zurueckgenommen", "abgelehnt"] },
          "description": "Der Status einer FASSUNG. „vorschlag“ entsteht nur mit eingeschaltetem Vier-Augen-Prinzip (F3)."
        },
        "import_status": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["vorschau", "uebernommen", "teilweise_uebernommen", "wiederholt", "zurueckgenommen", "verworfen"]
          },
          "description": "C4–C7: der Status eines Imports."
        },
        "zeilen_urteil": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["neu", "wiederholung", "konflikt", "berichtigung", "uebersprungen", "abgelehnt"]
          },
          "description": "C5: das Urteil einer Zeile."
        },
        "befunde": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/befund" },
          "description": "C8: das geschlossene Befund-Vokabular. Jeder Befund hat einen Kundensatz in befund_saetze."
        },
        "entscheidung": {
          "type": "array",
          "items": { "type": "string", "enum": ["behalten", "ersetzen"] },
          "description": "E9: die Entscheidung des Kunden bei einem Konflikt. Vorgabe ist „behalten“ — ein fehlender Eintrag ersetzt nie."
        },
        "vorgang": {
          "type": "array",
          "items": { "type": "string", "enum": ["erstwert", "berichtigung", "ruecknahme"] },
          "description": "F1–F4/C7: was mit einem Wert geschieht. Es gibt kein „ändern“ und kein „löschen“."
        }
      }
    },
    "einheiten": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "array",
        "minItems": 1,
        "items": { "type": "string", "minLength": 1 }
      },
      "description": "E4: das geschlossene Einheiten-Vokabular JE GRÖSSE. Umgerechnet wird nur innerhalb derselben Größe; „lbs“ und „Paletten“ stehen nirgends und werden deshalb abgelehnt, nie geraten."
    },
    "umrechnung": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/umrechnung" },
      "description": "U1: die erlaubten Umrechnungen mit festem Faktor. Alles, was nicht hier steht, ist keine Umrechnung."
    },
    "hinweis_befunde": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/befund" },
      "description": "C8: die Befunde, die eine Zeile NICHT verhindern — sie sind Hinweise. Alle anderen verhindern die Zeile bzw. die Datei."
    },
    "befund_saetze": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": { "type": "string", "minLength": 1 },
      "description": "C8: je Befund ein Kundensatz de-DE. Das Portal zeigt ihn, ergänzt um die Werte der Zeile; es erfindet keinen zweiten Wortlaut."
    },
    "stammdatum_saetze": {
      "type": "object",
      "additionalProperties": false,
      "required": ["herkunft", "regel", "geaendert", "beginnt", "endet"],
      "description": "S3 (AP-09 IP-6): die Kennzeichen-Sätze eines Übergangs in einer Periode. Platzhalter {bezeichnung}, {tag}, {alt}, {neu}, {einheit}; die Java-Regel hält ihre Vorlagen Zeichen für Zeichen daran.",
      "properties": {
        "herkunft": { "type": "string", "minLength": 1 },
        "regel": { "type": "string", "minLength": 1 },
        "geaendert": { "type": "string", "minLength": 1, "description": "Ein Wert vorher und nachher." },
        "beginnt": { "type": "string", "minLength": 1, "description": "Vorher war kein Wert erhoben." },
        "endet": { "type": "string", "minLength": 1, "description": "Ab dem Übergang ist kein Wert erhoben; {tag} ist der letzte Tag mit Wert." }
      }
    },
    "regeln": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "pruefreihenfolge",
        "begruendung_min_zeichen",
        "begruendung_max_zeichen",
        "stichtag",
        "zuordnung_hoechstens_monate",
        "anteil_nachkommastellen",
        "abdeckung_nachkommastellen",
        "vergleich_nachkommastellen",
        "zahlformat_vorgabe",
        "vier_augen_vorgabe"
      ],
      "description": "Die Schwellen und Reihenfolgen der Regeln als Daten — die Zwillinge lesen sie hier, statt sie je für sich festzuschreiben.",
      "properties": {
        "pruefreihenfolge": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 },
          "description": "Die Reihenfolge, in der eine Zeile geprüft wird. Sie ist ERGEBNISRELEVANT: eine Zeile mit unbekannter Einheit wird abgelehnt, BEVOR ihr Schlüssel mit einer anderen Zeile derselben Datei verglichen wird (§7 B13)."
        },
        "begruendung_min_zeichen": {
          "type": "integer",
          "minimum": 1,
          "description": "F2: eine Berichtigung ohne ausreichende Begründung wird nicht wirksam."
        },
        "begruendung_max_zeichen": { "type": "integer", "minimum": 1 },
        "stichtag": {
          "type": "string",
          "const": "letzter Tag der Periode",
          "description": "E17: der Tag, an dem ein zeitgültiges Stammdatum für eine Periode gelesen wird. Ein neuer Wert ab Tag X ändert keine Periode vor X (Plan-Abnahme 2)."
        },
        "zuordnung_hoechstens_monate": {
          "type": "integer",
          "minimum": 1,
          "description": "Z6/E5: bis zu so vielen berührten Kalendermonaten gibt es eine Vorgabe (der Monat mit dem größten Anteil); bei mehr entscheidet nur der Kunde."
        },
        "anteil_nachkommastellen": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9,
          "description": "Auf so viele Stellen wird der Anteil eines Monats am Ablesezeitraum ausgewiesen (kaufmännisch gerundet); die VORGABE entscheidet über die Minuten, nicht über die gerundete Zahl."
        },
        "abdeckung_nachkommastellen": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9,
          "description": "K: die Abdeckung eines Kanal-Werts ist ZEITBASIERT (gemessene Zeit ÷ Periodenlänge) und wird auf so viele Stellen ausgewiesen. ⚠ Nicht zu verwechseln mit der wertbasierten, abgeschnittenen Abdeckung der Verbrauchsregel (erhalten ÷ erwartet, AP-08 Z9)."
        },
        "vergleich_nachkommastellen": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9,
          "description": "Auf so viele Stellen wird verglichen; gerechnet wird mit dem gespeicherten Betrag (Invariante 12)."
        },
        "zahlformat_vorgabe": {
          "type": "string",
          "enum": ["de", "en"],
          "description": "U4: was bei einem mehrdeutigen Zahlentext gilt, solange die Vorlage nichts anderes sagt („312.400“ ist deutsch der Tausenderpunkt)."
        },
        "vier_augen_vorgabe": {
          "type": "boolean",
          "const": false,
          "description": "F3/E6: das Vier-Augen-Prinzip folgt der Einstellung des Unternehmens aus AP-08 E8 und ist per Vorgabe AUS."
        }
      }
    },
    "verwalten": {
      "type": "object",
      "additionalProperties": false,
      "required": ["herkunft", "kennzeichen", "fest_nach_erstem_wert", "immer_aenderbar", "pruefreihenfolge", "lesarten", "eingabe", "ablehnungen"],
      "description": "AP-09 IP-5: das Verwalten einer Bezugsgröße (M1–M6) — Kennzeichen-Vergabe, was nach dem ersten Wert fest bleibt, die Prüfreihenfolge je Vorgang, die Lesarten des Lesemodells und der GESCHLOSSENE Satz der Ablehnungen mit Status und Kundensatz. Die Regel `verwalten` der Fälle prüft ihn.",
      "properties": {
        "herkunft": { "type": "string", "minLength": 1 },
        "kennzeichen": {
          "type": "object",
          "additionalProperties": false,
          "required": ["praefix", "stellen", "muster", "regel"],
          "properties": {
            "praefix": { "type": "string", "const": "BZ-" },
            "stellen": { "type": "integer", "minimum": 1 },
            "muster": { "type": "string", "minLength": 1 },
            "regel": { "type": "string", "minLength": 1 }
          }
        },
        "fest_nach_erstem_wert": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/bezugsgroesse_feld" },
          "description": "M1: was nach dem ersten Wert nicht mehr geändert wird."
        },
        "immer_aenderbar": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string", "enum": ["kennzeichen", "name"] },
          "description": "M1: was immer änderbar bleibt (außer an einer archivierten Bezugsgröße, M6)."
        },
        "pruefreihenfolge": {
          "type": "object",
          "additionalProperties": false,
          "required": ["anlegen", "aendern", "archivieren", "loeschen", "stammdatum", "eingeben", "berichtigen"],
          "description": "Je Vorgang die Reihenfolge der Prüfungen — die ERSTE, die nicht besteht, ist die Ablehnung.",
          "properties": {
            "anlegen": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 } },
            "aendern": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 } },
            "archivieren": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 } },
            "loeschen": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 } },
            "stammdatum": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 }, "description": "E15/S4 (AP-09 IP-6): ein Wert eines Stammdatums ab einem Tag." },
            "eingeben": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 }, "description": "F1/F5 (AP-09 IP-7): ein Wert für eine Periode." },
            "berichtigen": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 }, "description": "F2/F3 (AP-09 IP-7): die Berichtigung eines gespeicherten Werts." }
          }
        },
        "lesarten": {
          "type": "array",
          "minItems": 2,
          "uniqueItems": true,
          "items": { "type": "string", "enum": ["wirksam", "alle"] },
          "description": "`GET …/{id}/werte?fassungen=` — die erste ist die Vorgabe."
        },
        "eingabe": {
          "type": "object",
          "additionalProperties": false,
          "required": ["herkunft", "urteile", "ganze_zahlen", "kennung"],
          "description": "AP-09 IP-7: was die Schnittstelle nach einem eingegebenen oder berichtigten Wert sagt (F1–F5), der Zusatz zu `zahl_unlesbar` bei ganzen Zahlen (U5) und die Kennung des Vorgangs einer Berichtigung.",
          "properties": {
            "herkunft": { "type": "string", "minLength": 1 },
            "urteile": {
              "type": "object",
              "additionalProperties": false,
              "required": ["neu", "wiederholung", "berichtigung", "vorschlag"],
              "properties": {
                "neu": { "type": "string", "minLength": 1 },
                "wiederholung": { "type": "string", "minLength": 1 },
                "berichtigung": { "type": "string", "minLength": 1 },
                "vorschlag": { "type": "string", "minLength": 1 }
              }
            },
            "ganze_zahlen": { "type": "string", "minLength": 1, "description": "U5: `{einheit}` ist die Einheit der Bezugsgröße (Stück, Personen, Schichten)." },
            "kennung": {
              "type": "object",
              "additionalProperties": false,
              "required": ["praefix", "muster"],
              "properties": {
                "praefix": { "type": "string", "const": "BK" },
                "muster": { "type": "string", "minLength": 1 }
              }
            }
          }
        },
        "ablehnungen": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/ablehnung" } },
        "hinweis": { "type": "array", "items": { "type": "string", "minLength": 1 } }
      }
    },
    "arten": {
      "type": "object",
      "additionalProperties": false,
      "required": ["herkunft", "je_art", "pruefungen"],
      "description": "AP-09 §4.2 (nachgetragen am 13.09.2026): das Vokabular der ARTEN einer Bezugsgröße — die Schlüssel von `je_art` — und je Art, welche Einheiten, welche Wertart, welche Perioden, welche Geltungsbereiche und welche Herkünfte möglich sind. Jede Angabe ist ein Wort der VORHANDENEN Vokabulare dieser Datei (`einheiten`, `vokabulare`); ob sie dort steht, prüfen beide Zwillings-Tests, nicht dieses Schema — so gibt es keine zweite Liste. Keine Tabelle und keine Route tragen die Art.",
      "properties": {
        "herkunft": { "type": "string", "minLength": 1 },
        "je_art": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": { "$ref": "#/$defs/art" },
          "description": "Je Art (Schlüssel, in der Reihenfolge der Konzept-Tabelle) ihre Angaben."
        },
        "pruefungen": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/art_pruefung" },
          "description": "Was beide Zwillinge nachrechnen: passt eine Bezugsgröße zu ihrer Art? Mit den Beispielen des Referenzunternehmens (`beispiel`)."
        },
        "hinweis": { "type": "array", "items": { "type": "string", "minLength": 1 } }
      }
    },
    "csv": {
      "type": "object",
      "additionalProperties": false,
      "required": ["herkunft", "grenzen", "kodierungen", "zeilenenden", "trennzeichen", "erkennung_zeilen", "reihenfolge", "befunde", "neutralisieren", "zusaetze", "pruefungen"],
      "description": "AP-09 §4.6 C1 (nachgetragen mit IP-11 am 13.09.2026): der CSV-Leser — Grenzen, Kodierungen, Zeilenenden, Trennzeichen, die Reihenfolge der Stufen, die Befunde, die Formel-Neutralisierung beim Anzeigen, die Zusätze und die Erkennungs-Fälle. Umsetzung Java `uems/CsvLeser`; kein TS-Zwilling (`zwillinge_grund.csv`).",
      "properties": {
        "herkunft": { "type": "string", "minLength": 1 },
        "grenzen": {
          "type": "object",
          "additionalProperties": false,
          "required": ["bytes_hoechstens", "datenzeilen_hoechstens"],
          "properties": {
            "bytes_hoechstens": { "type": "integer", "const": 5242880, "description": "5 MB = 5 × 1 024 × 1 024 Bytes (E14)." },
            "datenzeilen_hoechstens": { "type": "integer", "const": 100000, "description": "Ohne Kopfzeile und ohne Leerzeilen (E14)." }
          }
        },
        "kodierungen": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/csv_kodierung" }, "description": "In der Reihenfolge, in der sie versucht werden." },
        "zeilenenden": { "type": "array", "minItems": 1, "items": { "type": "string", "enum": ["\r\n", "\n", "\r"] } },
        "trennzeichen": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/csv_trennzeichen" } },
        "erkennung_zeilen": { "type": "integer", "minimum": 1, "description": "So viele Zeilen mit Inhalt stimmen über das Trennzeichen ab." },
        "reihenfolge": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "enum": ["groesse", "leer", "kodierung", "trennzeichen", "anfuehrungszeichen", "leerzeilen", "kopfzeile", "datenzeilen"] }
        },
        "befunde": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/befund" }, "description": "Die Befunde aus C8, die der Leser nennen kann." },
        "neutralisieren": {
          "type": "object",
          "additionalProperties": false,
          "required": ["zeichen", "praefix", "spiegel"],
          "properties": {
            "zeichen": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1, "maxLength": 1 } },
            "praefix": { "type": "string", "const": "'" },
            "spiegel": { "type": "string", "minLength": 1, "description": "Die Datei des Exports, dessen Regel gespiegelt wird." }
          }
        },
        "zusaetze": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "type": "object",
            "additionalProperties": false,
            "required": ["befund", "satz"],
            "properties": {
              "befund": { "$ref": "#/$defs/befund" },
              "satz": { "type": "string", "minLength": 1 }
            }
          },
          "description": "Je Zusatz (Schlüssel = `csv_zusatz`) der Befund, zu dem er gehört, und sein Satz."
        },
        "pruefungen": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/csv_pruefung" } },
        "hinweis": { "type": "array", "items": { "type": "string", "minLength": 1 } }
      }
    },
    "zwillinge": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string", "enum": ["java", "ts"] }
      },
      "description": "Je Regel, WELCHE Umsetzung sie prüft. Beide Zwillings-Tests lesen diese Angabe und prüfen genau die Regeln, die sie nennt — so ist eine Regel, die nur eine Seite kennt, DEKLARIERT und nicht stillschweigend übersprungen. Ein Eintrag muss mindestens „java“ nennen."
    },
    "zwillinge_grund": {
      "type": "object",
      "additionalProperties": { "type": "string", "minLength": 1 },
      "description": "Für jede Regel ohne TypeScript-Zwilling: warum sie dort nicht hingehört."
    },
    "_abweichungen": {
      "type": "array",
      "items": { "$ref": "#/$defs/abweichung" },
      "description": "Jede Stelle, an der diese Datei bewusst von der Vorlage data/vp-uems-ap09-bezugsgroessen/referenzfaelle.json abweicht. Leer heißt: nichts wurde stillschweigend geändert."
    },
    "_nicht_geprueft": {
      "type": "array",
      "items": { "$ref": "#/$defs/nicht_geprueft" },
      "description": "Jede Erwartung der Vorlage, die hier steht, aber (noch) kein Zwilling nachrechnet — mit dem Paket, das sie einlöst. Eine Erwartung verschwindet nie, nur weil niemand sie heute prüft."
    },
    "cases": {
      "type": "array",
      "minItems": 14,
      "description": "Die 14 handgerechneten Referenzfälle B1–B14 (AP-09 §7).",
      "items": { "$ref": "#/$defs/fall" }
    }
  },

  "$defs": {
    "zeitpunkt": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}[+-][0-9]{2}:[0-9]{2}$",
      "description": "ISO-8601 mit Offset. Gerechnet wird in UTC; der Offset trägt die Ortszeit des Standorts (E7)."
    },

    "tag": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
      "description": "Ein Kalendertag in der Zeitzone des Standorts."
    },

    "betrag": {
      "type": "string",
      "pattern": "^-?[0-9]+([.][0-9]+)?$",
      "description": "Ein Betrag als DEZIMALTEXT mit Punkt — nie als Gleitkommazahl. Beide Zwillinge lesen ihn in eine Dezimalzahl (Java BigDecimal, TypeScript ganzzahlige Mantisse) und vergleichen ihn numerisch: „312400“ und „312400.0“ sind derselbe Betrag."
    },

    "befund": {
      "type": "string",
      "enum": [
        "datei_bekannt",
        "zeile_bekannt",
        "konflikt_anderer_wert",
        "einheit_unbekannt",
        "einheit_umgerechnet",
        "periode_passt_nicht",
        "periode_nicht_zu_ende",
        "zeit_mehrdeutig",
        "zeit_nicht_vorhanden",
        "zahl_unlesbar",
        "datum_unlesbar",
        "bezug_unbekannt",
        "wert_negativ",
        "wert_unplausibel",
        "keine_datenzeilen",
        "datei_zu_gross",
        "kodierung_unlesbar"
      ],
      "description": "C8: das geschlossene Befund-Vokabular."
    },

    "ablehnung_code": {
      "type": "string",
      "enum": [
        "anfrage_ungueltig",
        "wort_unbekannt",
        "einheit_unbekannt",
        "kennzeichen_format",
        "zeitraum_ungueltig",
        "wert_ungueltig",
        "periode_passt_nicht_zur_wertart",
        "flaeche_aus_struktur",
        "kein_stammdatum",
        "kein_periodenwert",
        "periode_passt_nicht",
        "periode_nicht_zu_ende",
        "zahl_unlesbar",
        "wert_negativ",
        "begruendung_zu_kurz",
        "begruendung_zu_lang",
        "geltung_nicht_waehlbar",
        "geltung_unbekannt",
        "bedeutung_fest",
        "kennzeichen_belegt",
        "archiviert",
        "hat_werte",
        "konflikt_anderer_wert",
        "kein_wert",
        "vorschlag_offen",
        "nicht_gefunden"
      ],
      "description": "AP-09 IP-5: der geschlossene Satz der Ablehnungen der Bezugsgrößen-Schnittstelle."
    },

    "ablehnung": {
      "type": "object",
      "additionalProperties": false,
      "required": ["code", "status", "regel", "satz"],
      "properties": {
        "code": { "$ref": "#/$defs/ablehnung_code" },
        "status": { "type": "integer", "enum": [400, 404, 409, 422], "description": "400 Form · 404 nicht da (auch fremd, nie 403) · 409 Zustand · 422 Regel." },
        "regel": { "type": "string", "minLength": 1, "description": "Die Regel des Konzepts, aus der die Ablehnung folgt." },
        "satz": { "type": "string", "minLength": 1, "description": "Der Kundensatz — die EINE Formulierung." }
      }
    },

    "bezugsgroesse_feld": {
      "type": "string",
      "enum": ["wertart", "einheit", "periode_art", "geltung_art", "geltung_id"],
      "description": "verwalten (M1): ein Feld, das die Bedeutung eines Betrags trägt."
    },

    "bezugsgroesse_entwurf": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kennzeichen", "name", "wertart", "einheit", "periode_art", "geltung_art", "geltung_id"],
      "description": "verwalten: die Felder einer Bezugsgröße, wie die Schnittstelle sie liest. `geltung_id` ist hier das Kennzeichen des Objekts im Referenzunternehmen; die Schnittstelle nimmt dessen ID.",
      "properties": {
        "kennzeichen": { "type": ["string", "null"], "description": "null heißt: der Server vergibt es (M2)." },
        "name": { "type": ["string", "null"] },
        "wertart": { "type": ["string", "null"] },
        "einheit": { "type": ["string", "null"] },
        "periode_art": { "type": ["string", "null"] },
        "geltung_art": { "type": ["string", "null"] },
        "geltung_id": { "type": ["string", "null"] }
      }
    },

    "verwaltung": {
      "type": "object",
      "additionalProperties": false,
      "required": ["vorgang"],
      "description": "verwalten (M1–M6): der Eingang eines Vorgangs an einer Bezugsgröße.",
      "properties": {
        "vorgang": { "type": "string", "enum": ["anlegen", "aendern", "archivieren", "loeschen", "stammdatum", "eingeben", "berichtigen"] },
        "entwurf": { "$ref": "#/$defs/bezugsgroesse_entwurf" },
        "bestand": { "$ref": "#/$defs/bezugsgroesse_entwurf" },
        "archiviert": { "type": "boolean" },
        "werte": { "type": "integer", "minimum": 0, "description": "Wie viele Wert-Zeilen (alle Fassungen) die Bezugsgröße trägt." },
        "waehlbar": { "type": "array", "items": { "type": "string", "minLength": 1 }, "description": "Die Geltungsbereich-Arten, deren Objekte gebaut sind (E1: „wählbar, sobald gebaut“)." },
        "belegt": { "type": "array", "items": { "type": "string", "minLength": 1 }, "description": "Jedes Kennzeichen, das eine ANDERE Bezugsgröße des Kundenbereichs trägt oder je trug." },
        "wert": { "type": ["string", "null"], "description": "stammdatum (E15): der Wert, wie die Schnittstelle ihn liest — Dezimaltext, nichts wird umgewandelt." },
        "wertart": { "type": "string", "minLength": 1, "description": "eingeben · berichtigen (AP-09 IP-7): die Wertart der Bezugsgröße." },
        "periode_befund": { "anyOf": [{ "$ref": "#/$defs/befund" }, { "type": "null" }], "description": "eingeben · berichtigen: der Befund der Periode (Regel `periode`), oder null." },
        "zahl_befund": { "anyOf": [{ "$ref": "#/$defs/befund" }, { "type": "null" }], "description": "eingeben · berichtigen: der Befund der Zahl (Regel `zahl`), oder null." },
        "betrag": { "type": ["string", "null"], "description": "eingeben · berichtigen: der gelesene Betrag, oder null (nicht lesbar)." },
        "wirksamer_betrag": { "type": ["string", "null"], "description": "eingeben · berichtigen: der wirksame Betrag des Werts (Regel `fassung`), null = kein Wert." },
        "begruendung": { "type": ["string", "null"], "description": "berichtigen (F2): die Begründung, wie sie ankam." },
        "vorschlag_offen": { "type": "boolean", "description": "berichtigen (F3): für diesen Wert liegt ein offener Vorschlag vor." }
      }
    },

    "art": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "einheiten", "wertart", "perioden", "geltung", "herkunft", "konzept"],
      "description": "arten (§4.2): eine Art einer Bezugsgröße. `konzept` hält die Zellen der Konzept-Tabelle wörtlich fest; die übrigen Felder sind ihre Lesart in den Wörtern der vorhandenen Vokabulare.",
      "properties": {
        "name": { "type": "string", "minLength": 1, "description": "Das Kundenwort, wie §4.2 die Art nennt." },
        "einheiten": {
          "anyOf": [
            { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 } },
            { "type": "string", "enum": ["alle", "messstelle"] }
          ],
          "description": "Wörter aus `einheiten`; `alle` = jede Einheit des Vokabulars, `messstelle` = die Einheit der Messstelle, an der die Größe hängt."
        },
        "wertart": { "type": "string", "minLength": 1, "description": "Ein Wort aus `vokabulare.wertart`." },
        "perioden": { "type": "array", "items": { "type": "string", "minLength": 1 }, "description": "Wörter aus `vokabulare.periode_art`; leer = keine Periode (jede Wertart außer `periodenwert`)." },
        "geltung": {
          "anyOf": [
            { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 } },
            { "type": "string", "const": "alle" }
          ],
          "description": "Wörter aus `vokabulare.geltung_art`; `alle` = jedes Objekt."
        },
        "herkunft": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 }, "description": "Wörter aus `vokabulare.herkunft_art`." },
        "konzept": {
          "type": "object",
          "additionalProperties": false,
          "required": ["einheiten", "wertart", "periode", "geltungsbereich", "herkunft", "ahrenberg"],
          "description": "Die Zellen der Zeile in §4.2, Zeichen für Zeichen — Beleg, keine Regel.",
          "properties": {
            "einheiten": { "type": "string", "minLength": 1 },
            "wertart": { "type": "string", "minLength": 1 },
            "periode": { "type": "string", "minLength": 1 },
            "geltungsbereich": { "type": "string", "minLength": 1 },
            "herkunft": { "type": "string", "minLength": 1 },
            "ahrenberg": { "type": "string", "minLength": 1 }
          }
        }
      }
    },

    "art_feld": {
      "type": "string",
      "enum": ["art", "wertart", "geltung_art", "einheit", "periode_art", "herkunft_art"],
      "description": "arten: ein Feld, das nicht zur Art passt — in dieser Reihenfolge genannt."
    },

    "art_pruefung": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "art", "beispiel", "eingang", "ergebnis"],
      "description": "arten: passt eine Bezugsgröße zu ihrer Art?",
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "art": { "type": "string", "minLength": 1 },
        "beispiel": { "type": ["string", "null"], "description": "Das Kennzeichen der Bezugsgröße im Referenzunternehmen, deren Angaben der Eingang trägt." },
        "eingang": {
          "type": "object",
          "additionalProperties": false,
          "required": ["wertart", "geltung_art", "einheit", "periode_art", "herkunft_art", "einheit_der_messstelle"],
          "properties": {
            "wertart": { "type": ["string", "null"] },
            "geltung_art": { "type": ["string", "null"] },
            "einheit": { "type": ["string", "null"] },
            "periode_art": { "type": ["string", "null"] },
            "herkunft_art": { "type": ["string", "null"], "description": "null = nicht geprüft; die Herkunft gehört zum Wert (§4.1)." },
            "einheit_der_messstelle": { "type": ["string", "null"], "description": "Nur für `einheiten: messstelle`." }
          }
        },
        "ergebnis": {
          "type": "object",
          "additionalProperties": false,
          "required": ["abweichend"],
          "properties": {
            "abweichend": { "type": "array", "items": { "$ref": "#/$defs/art_feld" }, "description": "Leer = passt." }
          }
        },
        "hinweis": { "type": "string", "minLength": 1 }
      }
    },

    "urteil_wort": {
      "type": "string",
      "enum": ["neu", "wiederholung", "konflikt", "berichtigung", "uebersprungen", "abgelehnt"],
      "description": "C5: das Urteil einer Zeile."
    },

    "zustand_wort": {
      "type": "string",
      "enum": ["vollständig", "unvollständig", "keine Werte"],
      "description": "AP-08 §4.5, von der Verbrauchsregel geerbt (K4). „keine Werte“ ist nie 0."
    },

    "umrechnung": {
      "type": "object",
      "additionalProperties": false,
      "required": ["von", "nach", "hinweis"],
      "description": "U1: eine erlaubte Umrechnung. Entweder ein exakter Zehnerschritt (zehnerpotenz) oder ein Teiler mit ausgewiesener Genauigkeit — nie ein geschätzter Faktor. Jeder Eintrag gilt in BEIDE Richtungen: t → kg ist kg → t mit umgekehrtem Vorzeichen des Zehnerschritts. Eine Einheit derselben Größe OHNE Eintrag ist keine Umrechnung, sondern eine Annahme — sie wird abgelehnt wie ein unbekanntes Wort.",
      "properties": {
        "von": { "type": "string", "minLength": 1 },
        "nach": { "type": "string", "minLength": 1 },
        "zehnerpotenz": {
          "type": "integer",
          "description": "Verschiebung des Kommas: 3 heißt × 1 000, −3 heißt ÷ 1 000. Exakt, ohne Rundung."
        },
        "teiler": { "type": "integer", "minimum": 2, "description": "Kein Zehnerschritt — dann wird gerundet." },
        "nachkommastellen": { "type": "integer", "minimum": 0, "maximum": 9 },
        "hinweis": { "type": "string", "minLength": 1 }
      }
    },

    "abweichung": {
      "type": "object",
      "additionalProperties": false,
      "required": ["fall", "feld", "vorlage", "statt", "grund"],
      "properties": {
        "fall": { "type": "string", "minLength": 1, "description": "Fall und Prüfung, an der abgewichen wird." },
        "feld": { "type": "string", "minLength": 1, "description": "Das Feld des Eingangs oder des Ergebnisses." },
        "vorlage": { "description": "Was in referenzfaelle.json steht (null heißt: dort steht nichts)." },
        "statt": { "description": "Was hier steht." },
        "grund": { "type": "string", "minLength": 1, "description": "Warum — und ausdrücklich, ob ein Ergebnis der Vorlage davon berührt ist." }
      }
    },

    "nicht_geprueft": {
      "type": "object",
      "additionalProperties": false,
      "required": ["fall", "feld", "warum"],
      "properties": {
        "fall": { "type": "string", "minLength": 1 },
        "feld": { "type": "string", "minLength": 1 },
        "warum": { "type": "string", "minLength": 1, "description": "Warum keine reine Regel das prüfen kann — und welches Paket es einlöst." }
      }
    },

    "bestand": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "required": ["betrag", "fassung"],
      "description": "Der wirksame Stand am Schlüssel der Zeile, BEVOR sie übernommen wird. null heißt: der Schlüssel ist unbelegt.",
      "properties": {
        "betrag": { "anyOf": [{ "$ref": "#/$defs/betrag" }, { "type": "null" }] },
        "fassung": { "type": "integer", "minimum": 1 },
        "import_kennung": { "type": ["string", "null"] }
      }
    },

    "zeilenurteil": {
      "type": "object",
      "additionalProperties": false,
      "required": ["urteil"],
      "description": "Eine schon beurteilte Zeile, wie die Import-Regel sie zählt.",
      "properties": {
        "urteil": { "$ref": "#/$defs/urteil_wort" },
        "befunde": { "type": "array", "items": { "$ref": "#/$defs/befund" } }
      }
    },

    "stand": {
      "type": "object",
      "additionalProperties": false,
      "required": ["t", "stand"],
      "description": "Eine Ablesung: ein Zählerstand zu einem Zeitpunkt.",
      "properties": {
        "t": { "$ref": "#/$defs/zeitpunkt" },
        "stand": { "$ref": "#/$defs/betrag" }
      }
    },

    "vorgang": {
      "type": "object",
      "additionalProperties": false,
      "required": ["art", "urheber", "zeitpunkt"],
      "description": "F1–F4/C7: was mit einem Wert geschieht. Ein Vorgang ändert nie eine bestehende Fassung — er legt die nächste an.",
      "properties": {
        "art": { "type": "string", "enum": ["erstwert", "berichtigung", "ruecknahme"] },
        "betrag": { "anyOf": [{ "$ref": "#/$defs/betrag" }, { "type": "null" }] },
        "begruendung": { "type": ["string", "null"], "description": "F2: ab Fassung 2 Pflicht, begruendung_min_zeichen bis begruendung_max_zeichen." },
        "urheber": { "type": "string", "minLength": 1 },
        "freigeber": { "type": ["string", "null"], "description": "F3: bei eingeschaltetem Vier-Augen-Prinzip die ZWEITE Person; gleich dem Urheber ist nie eine Freigabe." },
        "zeitpunkt": { "$ref": "#/$defs/zeitpunkt" },
        "herkunft_art": { "type": "string", "enum": ["eingabe", "import", "messkanal", "stammdatum_ap02"] },
        "import_kennung": { "type": ["string", "null"] }
      }
    },

    "fassung": {
      "type": "object",
      "additionalProperties": false,
      "required": ["fassung", "betrag", "status"],
      "description": "Eine Fassung eines Werts. Sie wird nie geändert und nie gelöscht (Invariante 1).",
      "properties": {
        "fassung": { "type": "integer", "minimum": 1 },
        "betrag": { "anyOf": [{ "$ref": "#/$defs/betrag" }, { "type": "null" }], "description": "null heißt „zurückgenommen“ — kein Betrag, nie 0." },
        "status": { "type": "string", "minLength": 1, "description": "„wirksam“, „vorschlag“, „zurueckgenommen“ oder „wirksam bis Fassung n“ für eine abgelöste Fassung." },
        "begruendung": { "type": ["string", "null"] },
        "ersetzt_fassung": { "type": ["integer", "null"], "minimum": 1 },
        "herkunft_art": { "type": "string", "enum": ["eingabe", "import", "messkanal", "stammdatum_ap02"] },
        "import_kennung": { "type": ["string", "null"] }
      }
    },

    "ereignis": {
      "type": "object",
      "additionalProperties": false,
      "required": ["art", "fassung_alt", "fassung_neu"],
      "description": "F4: jede wirksame Fassung ≥ 2 erzeugt ein `correction`-Ereignis — der Auslöser für AP-11 und AP-12. Fassung 1 erzeugt keines.",
      "properties": {
        "art": { "type": "string", "const": "correction" },
        "fassung_alt": { "type": "integer", "minimum": 1 },
        "fassung_neu": { "type": "integer", "minimum": 2 },
        "import_kennung": { "type": ["string", "null"] }
      }
    },

    "stammdatum_wechsel": {
      "type": "object",
      "additionalProperties": false,
      "required": ["tag", "alt", "neu"],
      "description": "S3: an `tag` (00:00) gilt ein anderer Wert als am Vortag; null heißt „nicht erhoben“, nie 0.",
      "properties": {
        "tag": { "$ref": "#/$defs/tag" },
        "alt": { "anyOf": [{ "$ref": "#/$defs/betrag" }, { "type": "null" }] },
        "neu": { "anyOf": [{ "$ref": "#/$defs/betrag" }, { "type": "null" }] }
      }
    },

    "intervall": {
      "type": "object",
      "additionalProperties": false,
      "required": ["betrag", "gueltig_ab"],
      "description": "Ein zeitgültiges Stammdatum (Fläche, AP-02). „gültig bis“ ist der LETZTE Tag, nicht der erste danach.",
      "properties": {
        "betrag": { "$ref": "#/$defs/betrag" },
        "gueltig_ab": { "$ref": "#/$defs/tag" },
        "gueltig_bis": { "anyOf": [{ "$ref": "#/$defs/tag" }, { "type": "null" }] },
        "eingetragen_am": {
          "anyOf": [{ "$ref": "#/$defs/tag" }, { "type": "null" }],
          "description": "Der Tag des Eintrags. Liegt er nach „gültig ab“, ist der Eintrag rückwirkend — das Abzeichen nennt die Tage."
        }
      }
    },

    "zustandswechsel": {
      "type": "object",
      "additionalProperties": false,
      "required": ["t", "zustand"],
      "properties": {
        "t": { "$ref": "#/$defs/zeitpunkt" },
        "zustand": { "type": "string", "minLength": 1 }
      }
    },

    "luecke": {
      "type": "object",
      "additionalProperties": false,
      "required": ["von", "bis"],
      "description": "Eine Strecke ohne Werte (`data_gap`). Sie zählt weder als der gewählte Zustand noch als sein Gegenteil — sie macht die Periode unvollständig (K4).",
      "properties": {
        "von": { "$ref": "#/$defs/zeitpunkt" },
        "bis": { "$ref": "#/$defs/zeitpunkt" },
        "quelle": { "type": "string", "minLength": 1, "description": "Wer sich nicht gemeldet hat — er wird im Kennzeichen genannt." }
      }
    },

    "anteil": {
      "type": "object",
      "additionalProperties": false,
      "required": ["monat", "minuten", "prozent"],
      "description": "Z6: der Anteil EINES Kalendermonats am Ablesezeitraum.",
      "properties": {
        "monat": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}$" },
        "minuten": { "type": "integer", "minimum": 0 },
        "prozent": { "type": "number", "minimum": 0, "maximum": 100 }
      }
    },

    "zaehler": {
      "type": "object",
      "additionalProperties": false,
      "required": ["zeilen", "neu", "wiederholung", "konflikt", "berichtigung", "uebersprungen", "abgelehnt", "mit_hinweis"],
      "description": "C4: die Zähler der Vorschau. `konflikt` zählt die Zeilen, die als Konflikt ERKANNT wurden, `berichtigung` die, für die „ersetzen“ entschieden wurde — eine Zeile steht in beiden (§7 B3). `mit_hinweis` zählt Zeilen mit einem Hinweis-Befund; ein Hinweis nimmt einer Zeile nicht ihr Urteil.",
      "properties": {
        "zeilen": { "type": "integer", "minimum": 0 },
        "neu": { "type": "integer", "minimum": 0 },
        "wiederholung": { "type": "integer", "minimum": 0 },
        "konflikt": { "type": "integer", "minimum": 0 },
        "berichtigung": { "type": "integer", "minimum": 0 },
        "uebersprungen": { "type": "integer", "minimum": 0 },
        "abgelehnt": { "type": "integer", "minimum": 0 },
        "mit_hinweis": { "type": "integer", "minimum": 0 }
      }
    },

    "eingang": {
      "type": "object",
      "additionalProperties": false,
      "description": "Der Eingang einer Prüfung. Welche Felder gelten, sagt `regel`; die Prosa nennt sie je Regel.",
      "properties": {
        "text": { "type": ["string", "null"], "description": "zahl · periode · zeit: der gelieferte Text, wie er in der Datei oder im Feld steht." },
        "format": { "type": "string", "enum": ["de", "en", "auto"], "description": "zahl (U4): das Zahlformat der Spalte aus der Vorlage." },
        "ganzzahlig": { "type": "boolean", "description": "zahl (U5): Stück, Personen und Schichten nehmen keine Nachkommastellen an." },
        "betrag": { "anyOf": [{ "$ref": "#/$defs/betrag" }, { "type": "null" }], "description": "einheit · plausibilitaet · urteil: der schon gelesene Betrag." },
        "geliefert": { "type": ["string", "null"], "description": "einheit (U1–U3): die gelieferte Einheit; null heißt „keine Einheitsspalte“ → die Einheit der Bezugsgröße gilt." },
        "ziel": { "type": "string", "minLength": 1, "description": "einheit: die Einheit der Bezugsgröße." },
        "einheit": { "type": "string", "minLength": 1, "description": "plausibilitaet: die Einheit des Betrags." },
        "stunden_des_tages": { "type": ["integer", "null"], "minimum": 0, "description": "plausibilitaet (U6): die Obergrenze einer Betriebszeit am Umstellungstag — 23 oder 25 statt 24." },
        "einheiten_gebunden": { "type": "integer", "minimum": 1, "description": "plausibilitaet (U6): wie viele Maschinen bzw. Anlagen die Bezugsgröße umfasst; bei einer Messstelle als Bezug 1." },
        "von_text": { "type": "string", "minLength": 1, "description": "periode (Z3 von_bis): die Von-Spalte." },
        "bis_text": { "type": "string", "minLength": 1, "description": "periode (Z3 von_bis): die Bis-Spalte (der LETZTE Tag, nicht der erste danach)." },
        "deutung": { "type": "string", "enum": ["periode", "periodenbeginn", "periodenende", "von_bis", "zeitpunkt"], "description": "periode (Z3)." },
        "periode_art": { "type": "string", "enum": ["tag", "woche", "monat", "jahr"], "description": "periode · stammdatum: welche Periode die Bezugsgröße führt." },
        "jetzt": { "$ref": "#/$defs/zeitpunkt", "description": "periode (Z4): der Zeitpunkt, gegen den „ist die Periode zu Ende?“ geprüft wird. Er wird ÜBERGEBEN — die Regel liest keine Uhr." },
        "zeitzone": { "type": "string", "minLength": 1, "description": "zeit · zuordnung: die Zeitzone des Standorts (E7)." },
        "offset_in_datei": { "type": ["string", "null"], "description": "zeit (Z5): ein Offset in der Datei gewinnt immer; null heißt „ohne Zone“." },
        "tag": { "$ref": "#/$defs/tag", "description": "stunden (P3): der Kalendertag, dessen Länge gefragt ist." },
        "schluessel": { "type": ["string", "null"], "description": "urteil (C2): der fachliche Schlüssel der Zeile — Bezugsgröße + Periode bzw. Reihe + Zeitpunkt. null heißt: es gibt keinen (die Zeile trägt schon einen Befund)." },
        "bestand": { "$ref": "#/$defs/bestand" },
        "datei_fingerabdruck_bekannt": { "type": "boolean", "description": "urteil (C2/E8): der SHA-256 der Datei ist schon einmal angekommen." },
        "fingerabdruck_bekannt": { "type": "boolean", "description": "import: dasselbe auf Dateiebene." },
        "frueherer_import_status": {
          "type": ["string", "null"],
          "enum": ["vorschau", "uebernommen", "teilweise_uebernommen", "wiederholt", "zurueckgenommen", "verworfen", null],
          "description": "§4.7: der Status des früheren Imports derselben Datei. Nach einer Rücknahme ist dieselbe Zeile wieder „neu“."
        },
        "entscheidung": { "type": ["string", "null"], "enum": ["behalten", "ersetzen", null], "description": "urteil (E9): die Entscheidung des Kunden je Zeile; null heißt „noch nicht entschieden“ (Vorschau)." },
        "befunde_vorher": { "type": "array", "items": { "$ref": "#/$defs/befund" }, "description": "urteil: was die früheren Schritte der Prüfreihenfolge an dieser Zeile schon festgestellt haben." },
        "datenzeilen": { "type": "integer", "minimum": 0, "description": "import (C4): Datenzeilen der Datei ohne Kopfzeile. 0 heißt `keine_datenzeilen`." },
        "zeilen": { "type": "array", "items": { "$ref": "#/$defs/zeilenurteil" }, "description": "import: die schon beurteilten Zeilen." },
        "staende": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/stand" }, "description": "menge: die Ablesungen, aus denen die Verbrauchsregel die Menge bildet." },
        "von": { "$ref": "#/$defs/zeitpunkt", "description": "menge · zuordnung · kanal: Anfang des Zeitraums (einschließlich)." },
        "bis": { "$ref": "#/$defs/zeitpunkt", "description": "menge · zuordnung · kanal: Ende des Zeitraums (ausschließlich bei Perioden, der schließende Ablesezeitpunkt bei einer Ablesung)." },
        "kadenz_s": { "type": "integer", "minimum": 1, "description": "menge: die Kadenz der Reihe in Sekunden, wie die Verbrauchsregel sie braucht." },
        "vier_augen": { "type": "boolean", "description": "fassung (F3/E6): die Einstellung des Unternehmens." },
        "vorgaenge": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/vorgang" }, "description": "fassung: die Vorgänge in der Reihenfolge, in der sie geschehen sind." },
        "intervalle": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/intervall" }, "description": "stammdatum (E17) · stammdatum_eintrag (S4): die wirksamen Intervalle." },
        "bezeichnung": { "type": "string", "minLength": 1, "description": "stammdatum (S3): wie das Stammdatum im Kennzeichen-Satz heißt („Fläche“)." },
        "gueltig_ab": { "$ref": "#/$defs/tag", "description": "stammdatum_eintrag (S4): der Tag, ab dem der neue Wert gilt." },
        "wert": { "$ref": "#/$defs/betrag", "description": "stammdatum_eintrag (S4): der neue Wert." },
        "perioden": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 }, "description": "stammdatum: die Perioden, für die der Wert gefragt ist." },
        "zustand_gewaehlt": { "type": "string", "minLength": 1, "description": "kanal (K): der Zustand, dessen Dauer gezählt wird." },
        "zustand_am_anfang": { "type": ["string", "null"], "description": "kanal: der Zustand am Periodenanfang = letzter Wechsel davor; null heißt „vor dem ersten Wechsel wurde nicht gemessen“." },
        "wechsel": { "type": "array", "items": { "$ref": "#/$defs/zustandswechsel" }, "description": "kanal: die Statuswechsel innerhalb der Periode." },
        "luecken": { "type": "array", "items": { "$ref": "#/$defs/luecke" }, "description": "kanal: die Strecken ohne Werte." },
        "verwaltung": { "$ref": "#/$defs/verwaltung" },
        "csv": { "$ref": "#/$defs/csv_eingang", "description": "csv (C1, AP-09 IP-11): die Datei als Bytes und was die Vorlage festhält." },
        "vorschau": { "$ref": "#/$defs/vorschau_eingang", "description": "vorschau (C2–C5, AP-09 IP-12): die Datei, die Zuordnung und was schon gespeichert ist." }
      }
    },

    "ergebnis": {
      "type": "object",
      "additionalProperties": false,
      "description": "Das erwartete Ergebnis einer Prüfung. Welche Felder gelten, sagt `regel`; ein fehlendes Feld wird nicht verglichen.",
      "properties": {
        "betrag": { "anyOf": [{ "$ref": "#/$defs/betrag" }, { "type": "null" }], "description": "zahl · einheit · kanal: der Betrag; null heißt „kein Betrag bildbar“ — nie 0." },
        "befund": { "anyOf": [{ "$ref": "#/$defs/befund" }, { "type": "null" }], "description": "zahl · periode · zeit · plausibilitaet: der EINE Befund dieser Prüfung, oder null." },
        "befunde": { "type": "array", "items": { "$ref": "#/$defs/befund" }, "description": "einheit · urteil · import: die Befunde in der Reihenfolge, in der die Regel sie feststellt." },
        "einheit": { "type": "string", "minLength": 1, "description": "einheit · kanal: die Einheit des Ergebnisses." },
        "schluessel": { "type": ["string", "null"], "description": "periode: die Periode als Schlüssel (`2026-10`, `2026-W40`, `2026-10-25`, `2026`); null bei einem Befund." },
        "von": { "anyOf": [{ "$ref": "#/$defs/zeitpunkt" }, { "type": "null" }], "description": "periode: Anfang der Periode (einschließlich)." },
        "bis": { "anyOf": [{ "$ref": "#/$defs/zeitpunkt" }, { "type": "null" }], "description": "periode: Ende der Periode (ausschließlich)." },
        "stunden": { "type": ["integer", "null"], "minimum": 0, "description": "periode · stunden (P3): die Länge in Stunden — am Umstellungstag 23 oder 25. Sie kommt von der Verbrauchsregel AP-08, nicht aus einer zweiten Rechnung." },
        "zeitpunkt": { "anyOf": [{ "$ref": "#/$defs/zeitpunkt" }, { "type": "null" }], "description": "zeit: der gedeutete Zeitpunkt; null bei einem Befund." },
        "varianten": { "type": "array", "items": { "$ref": "#/$defs/zeitpunkt" }, "description": "zeit: bei `zeit_mehrdeutig` die beiden möglichen Zeitpunkte — die Regel wählt keinen, sie zeigt beide." },
        "dauer_minuten": { "type": "integer", "minimum": 0, "description": "zuordnung: die Länge des Ablesezeitraums in Minuten." },
        "dauer_text": { "type": "string", "minLength": 1, "description": "zuordnung: dieselbe Länge als Kundensatz („32 Tage 1 h 25 min“)." },
        "monate_beruehrt": { "type": "integer", "minimum": 1, "description": "zuordnung (Z6)." },
        "anteile": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/anteil" }, "description": "zuordnung: je berührter Monat sein Anteil, in Kalenderreihenfolge." },
        "vorgabe": { "type": ["string", "null"], "description": "zuordnung (E5): der vorbelegte Kalendermonat, oder null bei mehr als zuordnung_hoechstens_monate berührten Monaten." },
        "urteil": { "$ref": "#/$defs/urteil_wort", "description": "urteil (C5)." },
        "status": {
          "type": ["string", "null"],
          "enum": ["vorschau", "uebernommen", "teilweise_uebernommen", "wiederholt", "zurueckgenommen", "verworfen", null],
          "description": "import: der Status des Imports; null heißt „es entsteht kein Import“ (§7 B12)."
        },
        "zaehler": { "$ref": "#/$defs/zaehler" },
        "uebernahme_moeglich": { "type": "boolean", "description": "import (C4): ob der Übernahme-Knopf anwählbar ist." },
        "import_datensatz": { "type": "boolean", "description": "import: ob ein Import-Datensatz entsteht. Eine Vorschau ohne Datenzeilen schreibt nichts, auch keinen Datensatz." },
        "bestaetigung": { "type": ["string", "null"], "description": "import (E10): der Satz, den der Kunde bestätigen muss, wenn nicht alle Zeilen übernommen werden." },
        "aenderungen": { "type": "integer", "minimum": 0, "description": "import: wie viele Werte die Übernahme schreibt. 0 heißt: nichts wird verdoppelt (Plan-Abnahme 1)." },
        "menge": { "anyOf": [{ "$ref": "#/$defs/betrag" }, { "type": "null" }], "description": "menge: das Ergebnis der Verbrauchsregel AP-08." },
        "zustand": { "$ref": "#/$defs/zustand_wort", "description": "menge · kanal." },
        "fassungen": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/fassung" }, "description": "fassung: alle Fassungen, älteste zuerst. Keine verschwindet." },
        "ereignisse": { "type": "array", "items": { "$ref": "#/$defs/ereignis" }, "description": "fassung · stammdatum (F4)." },
        "wirksamer_betrag": { "anyOf": [{ "$ref": "#/$defs/betrag" }, { "type": "null" }], "description": "fassung: der Betrag der wirksamen Fassung; null nach einer Rücknahme." },
        "abgelehnt": {
          "type": "string",
          "enum": ["ersteller_gleich_freigeber", "begruendung_zu_kurz"],
          "description": "fassung: warum ein Vorgang NICHT wirksam wurde. Fehlt das Feld, wurde nichts abgelehnt."
        },
        "je_periode": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": { "anyOf": [{ "$ref": "#/$defs/betrag" }, { "type": "null" }] },
          "description": "stammdatum: der Wert je Periode am Stichtag; null heißt „keine Werte“, nie 0."
        },
        "stichtage": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": { "$ref": "#/$defs/tag" },
          "description": "stammdatum: der Tag, an dem gelesen wurde — der LETZTE Tag der Periode (E17)."
        },
        "rueckwirkend_tage": { "type": "integer", "minimum": 0, "description": "stammdatum: Tage zwischen „gültig ab“ und dem Eintrag; 0 heißt „nicht rückwirkend“." },
        "wechsel_je_periode": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": { "type": "array", "items": { "$ref": "#/$defs/stammdatum_wechsel" } },
          "description": "stammdatum (S3): je Periode die Übergänge nach ihrem ersten Tag bis einschließlich zum Stichtag, nach Tag sortiert."
        },
        "kennzeichen_je_periode": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": { "type": "array", "items": { "type": "string", "minLength": 1 } },
          "description": "stammdatum (S3): je Periode der Satz zu jedem Übergang (`stammdatum_saetze`), in derselben Reihenfolge — Vertrag nach Text UND Reihenfolge."
        },
        "eintrag": {
          "type": "object",
          "additionalProperties": false,
          "required": ["unveraendert", "beendet", "aufgehoben", "neu"],
          "description": "stammdatum_eintrag (S4): was der neue Wert an den Intervallen ändert.",
          "properties": {
            "unveraendert": { "type": "boolean", "description": "An dem Tag gilt schon genau dieser Wert — nichts wird geschrieben." },
            "beendet": { "anyOf": [{ "$ref": "#/$defs/intervall" }, { "type": "null" }], "description": "Das laufende Intervall mit seinem neuen letzten Tag (dem Vortag)." },
            "aufgehoben": { "anyOf": [{ "$ref": "#/$defs/intervall" }, { "type": "null" }], "description": "Das Intervall, das am selben Tag begann (Korrektur) — es bleibt lesbar." },
            "neu": { "anyOf": [{ "$ref": "#/$defs/intervall" }, { "type": "null" }], "description": "Das neue Intervall." }
          }
        },
        "minuten_im_zustand": { "type": "integer", "minimum": 0, "description": "kanal: die gemessene Zeit im gewählten Zustand." },
        "abdeckung_prozent": { "type": "number", "minimum": 0, "maximum": 100, "description": "kanal: gemessene Zeit ÷ Periodenlänge. ⚠ Zeitbasiert — nicht die wertbasierte Abdeckung der Verbrauchsregel." },
        "gemessene_stunden": { "$ref": "#/$defs/betrag", "description": "kanal: wie viele Stunden der Periode überhaupt gemessen wurden." },
        "kennzeichen": { "type": "array", "items": { "type": "string", "minLength": 1 }, "description": "kanal: was an dieser Periode zu sagen ist, in der Reihenfolge der Feststellung — Vertrag nach Text UND Reihenfolge." },
        "ablehnung": { "anyOf": [{ "$ref": "#/$defs/ablehnung_code" }, { "type": "null" }], "description": "verwalten: die Ablehnung, oder null — der Vorgang ist erlaubt." },
        "ablehnung_felder": { "type": "array", "items": { "$ref": "#/$defs/bezugsgroesse_feld" }, "description": "verwalten (`bedeutung_fest`): die Felder, die nach dem ersten Wert nicht geändert werden." },
        "kennzeichen_vorschlag": { "type": "string", "minLength": 1, "description": "verwalten (M2): das Kennzeichen, das der Server ohne Angabe vergibt." },
        "csv": { "$ref": "#/$defs/csv_ergebnis", "description": "csv (C1, AP-09 IP-11): was der Leser aus der Datei macht." },
        "vorschau": { "$ref": "#/$defs/vorschau_ergebnis", "description": "vorschau (C2–C5, AP-09 IP-12): die Vorschau der ganzen Datei — sie schreibt nichts." }
      }
    },

    "csv_datei": {
      "type": "object",
      "additionalProperties": false,
      "description": "csv: eine Datei als Bytes. `zeilen` (mit `zeilenende` und einem Zeilenende nach jeder Zeile, außer `ohne_schluss`) ODER `text` (roh) ODER `hex` (rohe Bytes); `fuellen` hängt `anzahl`-mal `zeile` + Zeilenende an; `kodierung` und `bom` machen aus dem Text Bytes.",
      "properties": {
        "zeilen": { "type": "array", "items": { "type": "string" } },
        "text": { "type": "string" },
        "hex": { "type": "string", "pattern": "^([0-9a-f]{2})*$" },
        "zeilenende": { "type": "string", "enum": ["\r\n", "\n", "\r"], "description": "Vorgabe \\n." },
        "ohne_schluss": { "type": "boolean", "description": "Kein Zeilenende nach der letzten Zeile von `zeilen`. Vorgabe false." },
        "fuellen": {
          "type": "object",
          "additionalProperties": false,
          "required": ["zeile", "anzahl"],
          "properties": {
            "zeile": { "type": "string", "minLength": 1 },
            "anzahl": { "type": "integer", "minimum": 1 }
          }
        },
        "kodierung": { "$ref": "#/$defs/csv_kodierung", "description": "Vorgabe utf-8." },
        "bom": { "type": "boolean", "description": "Ein UTF-8-BOM vor dem Text. Vorgabe false." }
      }
    },

    "csv_kodierung": { "type": "string", "enum": ["utf-8", "windows-1252"], "description": "C1: die lesbaren Kodierungen." },

    "csv_trennzeichen": { "type": "string", "enum": [";", ",", "\t"], "description": "C1: die Trennzeichen, in der Reihenfolge, die einen Gleichstand entscheidet." },

    "csv_zusatz": {
      "type": "string",
      "enum": ["zu_viele_bytes", "zu_viele_datenzeilen", "leer_0_byte", "nur_leerzeilen", "nur_kopfzeile", "kodierung_nicht_erlaubt", "kodierung_nicht_wie_vorgabe", "anfuehrungszeichen_offen"],
      "description": "csv: WORAN ein Befund lag. Der Befund bleibt das geschlossene Wort aus C8."
    },

    "csv_eingang": {
      "type": "object",
      "additionalProperties": false,
      "required": ["datei"],
      "properties": {
        "datei": { "$ref": "#/$defs/csv_datei" },
        "vorgabe": {
          "type": "object",
          "additionalProperties": false,
          "description": "Was die Vorlage festhält; ein fehlendes Feld wird erkannt.",
          "properties": {
            "kodierung": { "$ref": "#/$defs/csv_kodierung" },
            "trennzeichen": { "$ref": "#/$defs/csv_trennzeichen" },
            "kopfzeile": { "type": "boolean" }
          }
        }
      }
    },

    "csv_ergebnis": {
      "type": "object",
      "additionalProperties": false,
      "required": ["befund", "zusatz", "zeile", "kodierung", "bom", "trennzeichen", "kopfzeile", "kopf", "spalten", "datenzeilen"],
      "description": "csv: befund null heißt gelesen. Was vor einer Ablehnung feststand, steht da; alles danach ist null. Fehlen `zeilen` oder `anzeige`, werden sie nicht verglichen.",
      "properties": {
        "befund": { "anyOf": [{ "type": "string", "enum": ["datei_zu_gross", "kodierung_unlesbar", "keine_datenzeilen"] }, { "type": "null" }] },
        "zusatz": { "anyOf": [{ "$ref": "#/$defs/csv_zusatz" }, { "type": "null" }] },
        "zeile": { "type": ["integer", "null"], "minimum": 1, "description": "Die Zeile der Datei, an der es lag, wenn es eine gibt." },
        "kodierung": { "anyOf": [{ "$ref": "#/$defs/csv_kodierung" }, { "type": "null" }] },
        "bom": { "type": ["boolean", "null"] },
        "trennzeichen": { "anyOf": [{ "$ref": "#/$defs/csv_trennzeichen" }, { "type": "null" }] },
        "kopfzeile": { "type": ["boolean", "null"] },
        "kopf": { "anyOf": [{ "type": "array", "items": { "type": "string" } }, { "type": "null" }] },
        "spalten": { "type": ["integer", "null"], "minimum": 1 },
        "datenzeilen": { "type": ["integer", "null"], "minimum": 0 },
        "zeilen": { "type": "array", "items": { "$ref": "#/$defs/csv_zeile" } },
        "anzeige": {
          "type": "array",
          "items": { "type": "array", "items": { "type": "string" } },
          "description": "Die Felder je Datenzeile, wie sie ANGEZEIGT werden (Formel-Neutralisierung)."
        }
      }
    },

    "csv_zeile": {
      "type": "object",
      "additionalProperties": false,
      "required": ["nr", "text", "felder"],
      "properties": {
        "nr": { "type": "integer", "minimum": 1, "description": "Die Zeile der Datei, in der die Zeile beginnt." },
        "text": { "type": "string", "description": "Der Zeilentext ohne Zeilenende." },
        "felder": { "type": "array", "minItems": 1, "items": { "type": "string" }, "description": "Die Felder nach RFC 4180, unverändert." }
      }
    },

    "csv_pruefung": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "eingang", "ergebnis"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "eingang": { "$ref": "#/$defs/csv_eingang" },
        "ergebnis": { "$ref": "#/$defs/csv_ergebnis" }
      }
    },

    "vorschau_zuordnung": {
      "type": "object",
      "additionalProperties": false,
      "required": ["csv", "spalten", "deutung", "zahlformat", "einheit", "bezugsgroesse", "bezug_tabelle", "synonyme"],
      "description": "vorschau (C3): die Zuordnung, wie die Route sie nimmt. Spalten 1-basiert. `einheit` gilt ohne Einheitsspalte (U3), `bezugsgroesse` (ein Kennzeichen) ohne Bezug-Spalte; genau eins von Bezug-Spalte und `bezugsgroesse`.",
      "properties": {
        "csv": {
          "anyOf": [{ "type": "null" }, {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "kodierung": { "$ref": "#/$defs/csv_kodierung" },
              "trennzeichen": { "$ref": "#/$defs/csv_trennzeichen" },
              "kopfzeile": { "type": "boolean" }
            }
          }]
        },
        "spalten": {
          "type": "object",
          "additionalProperties": false,
          "required": ["periode", "bis", "wert", "einheit", "bezug", "bemerkung"],
          "properties": {
            "periode": { "type": "integer", "minimum": 1, "description": "Periode, Periodenbeginn/-ende, „von“ oder Zeitpunkt (Z3)." },
            "bis": { "type": ["integer", "null"], "minimum": 1, "description": "Nur bei der Deutung von_bis." },
            "wert": { "type": "integer", "minimum": 1 },
            "einheit": { "type": ["integer", "null"], "minimum": 1 },
            "bezug": { "type": ["integer", "null"], "minimum": 1 },
            "bemerkung": { "type": ["integer", "null"], "minimum": 1 }
          }
        },
        "deutung": { "type": "string", "enum": ["periode", "periodenbeginn", "periodenende", "von_bis", "zeitpunkt"] },
        "zahlformat": { "type": "string", "enum": ["de", "en", "auto"] },
        "einheit": { "type": ["string", "null"], "minLength": 1 },
        "bezugsgroesse": { "type": ["string", "null"], "pattern": "^BZ-[0-9]+$" },
        "bezug_tabelle": { "type": "object", "additionalProperties": { "type": "string", "pattern": "^BZ-[0-9]+$" } },
        "synonyme": { "type": "object", "additionalProperties": { "type": "string", "minLength": 1 } }
      }
    },

    "vorschau_eingang": {
      "type": "object",
      "additionalProperties": false,
      "required": ["datei", "zuordnung", "bezugsgroessen", "bestand", "importe_dieser_datei", "jetzt"],
      "properties": {
        "datei": { "$ref": "#/$defs/csv_datei" },
        "zuordnung": { "$ref": "#/$defs/vorschau_zuordnung" },
        "bezugsgroessen": {
          "type": "array",
          "description": "Die Bezugsgrößen, die Werte aufnehmen können, mit der Zeitzone ihres Standorts (Z1). `id` ist Teil des Zeilen-Fingerabdrucks.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["kennzeichen", "id", "wertart", "einheit", "periode_art", "zeitzone", "einheiten_gebunden"],
            "properties": {
              "kennzeichen": { "type": "string", "pattern": "^BZ-[0-9]+$" },
              "id": { "type": "string", "format": "uuid" },
              "wertart": { "type": "string", "enum": ["periodenwert", "stand"] },
              "einheit": { "type": "string", "minLength": 1 },
              "periode_art": { "type": ["string", "null"] },
              "zeitzone": { "type": "string", "minLength": 1 },
              "einheiten_gebunden": { "type": "integer", "minimum": 1 }
            }
          }
        },
        "bestand": {
          "type": "array",
          "description": "Der wirksame Stand je belegtem Schlüssel: Periodenschlüssel oder Zeitpunkt in UTC.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["bezugsgroesse", "schluessel", "betrag", "fassung", "import_kennung"],
            "properties": {
              "bezugsgroesse": { "type": "string", "pattern": "^BZ-[0-9]+$" },
              "schluessel": { "type": "string", "minLength": 1 },
              "betrag": { "$ref": "#/$defs/betrag" },
              "fassung": { "type": "integer", "minimum": 1 },
              "import_kennung": { "type": ["string", "null"] }
            }
          }
        },
        "importe_dieser_datei": {
          "type": "array",
          "description": "Was die Datenbank über gespeicherte Importe mit DIESEM Datei-Fingerabdruck sagt: Kennung, heutiger Status, Zeitpunkt.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["kennung", "status", "am"],
            "properties": {
              "kennung": { "type": "string", "pattern": "^I-[0-9]{4}-[0-9]{4,}$" },
              "status": { "type": "string", "enum": ["uebernommen", "teilweise_uebernommen", "wiederholt", "zurueckgenommen", "verworfen"] },
              "am": { "$ref": "#/$defs/zeitpunkt" }
            }
          }
        },
        "jetzt": { "$ref": "#/$defs/zeitpunkt" }
      }
    },

    "vorschau_ergebnis": {
      "type": "object",
      "additionalProperties": false,
      "required": ["datei", "frueherer_import", "zeilen", "import"],
      "properties": {
        "datei": {
          "type": "object",
          "additionalProperties": false,
          "required": ["sha256", "bytes", "befund", "zusatz", "kodierung", "trennzeichen", "kopfzeile", "datenzeilen"],
          "properties": {
            "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$", "description": "C2/E8: SHA-256 der hochgeladenen Bytes." },
            "bytes": { "type": "integer", "minimum": 0 },
            "befund": { "anyOf": [{ "type": "string", "enum": ["datei_zu_gross", "kodierung_unlesbar", "keine_datenzeilen"] }, { "type": "null" }] },
            "zusatz": { "anyOf": [{ "$ref": "#/$defs/csv_zusatz" }, { "type": "null" }] },
            "kodierung": { "anyOf": [{ "$ref": "#/$defs/csv_kodierung" }, { "type": "null" }] },
            "trennzeichen": { "anyOf": [{ "$ref": "#/$defs/csv_trennzeichen" }, { "type": "null" }] },
            "kopfzeile": { "type": ["boolean", "null"] },
            "datenzeilen": { "type": ["integer", "null"], "minimum": 0 }
          }
        },
        "frueherer_import": {
          "anyOf": [{ "type": "null" }, {
            "type": "object",
            "additionalProperties": false,
            "required": ["kennung", "status"],
            "description": "Der jüngste gespeicherte Import dieser Datei, der etwas geschrieben hat — die Grundlage von `datei_bekannt`.",
            "properties": {
              "kennung": { "type": "string" },
              "status": { "type": "string" }
            }
          }]
        },
        "zeilen": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["nr", "bezugsgroesse", "schluessel", "betrag", "einheit", "urteil", "befunde", "fingerabdruck"],
            "properties": {
              "nr": { "type": "integer", "minimum": 1 },
              "bezugsgroesse": { "type": ["string", "null"] },
              "schluessel": { "type": ["string", "null"], "description": "Der FACHLICHE Schlüssel (C2) zur Anzeige — null, bevor die Zeile die Stufe Schlüssel erreicht." },
              "betrag": { "anyOf": [{ "$ref": "#/$defs/betrag" }, { "type": "null" }] },
              "einheit": { "type": ["string", "null"] },
              "urteil": { "$ref": "#/$defs/urteil_wort" },
              "befunde": { "type": "array", "items": { "$ref": "#/$defs/befund" } },
              "fingerabdruck": { "type": ["string", "null"], "pattern": "^[0-9a-f]{64}$", "description": "C2: SHA-256 von `<bezugsgroesse_id>|<schluessel>|<betrag>` — nie der Zeilentext." }
            }
          }
        },
        "import": {
          "type": "object",
          "additionalProperties": false,
          "required": ["status", "zaehler", "uebernahme_moeglich", "import_datensatz", "bestaetigung", "aenderungen", "befunde"],
          "description": "Was die Regel `import` über diese Zeilen sagt — dieselben Felder.",
          "properties": {
            "status": { "type": ["string", "null"] },
            "zaehler": { "$ref": "#/$defs/zaehler" },
            "uebernahme_moeglich": { "type": "boolean" },
            "import_datensatz": { "type": "boolean" },
            "bestaetigung": { "type": ["string", "null"] },
            "aenderungen": { "type": "integer", "minimum": 0 },
            "befunde": { "type": "array", "items": { "$ref": "#/$defs/befund" } }
          }
        }
      }
    },

    "pruefung": {
      "type": "object",
      "additionalProperties": false,
      "required": ["regel", "name", "eingang", "ergebnis"],
      "properties": {
        "regel": {
          "type": "string",
          "enum": ["zahl", "einheit", "periode", "zeit", "stunden", "plausibilitaet", "zuordnung", "urteil", "import", "menge", "fassung", "stammdatum", "stammdatum_eintrag", "kanal", "verwalten", "csv", "vorschau"],
          "description": "Welche Regel gefragt wird. `zwillinge` sagt, wer sie prüft."
        },
        "name": { "type": "string", "minLength": 1, "description": "Was die Prüfung behauptet — sie steht in der Meldung, wenn sie bricht." },
        "eingang": { "$ref": "#/$defs/eingang" },
        "ergebnis": { "$ref": "#/$defs/ergebnis" }
      }
    },

    "fall": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "id", "familie", "abnahme", "titel", "why", "wertart", "einheit", "schritte", "pruefungen", "beschreibend", "annahmen"],
      "properties": {
        "name": { "type": "string", "pattern": "^b[0-9]+-[a-z0-9-]+$", "description": "Kennung des Falls aus dem Referenzfallkatalog plus Kurztitel." },
        "id": { "type": "string", "pattern": "^B[0-9]+$", "description": "B1 … B14." },
        "familie": {
          "type": "string",
          "enum": ["import", "eingabe", "berichtigung", "stammdatum", "kanal", "ablesung", "einheit", "zeit", "leer"],
          "description": "Die Familie des Falls (AP-09 §6.2)."
        },
        "abnahme": { "type": ["string", "null"], "enum": ["plan-1", "plan-2", null], "description": "Bezug auf eine Plan-Abnahme des Programms, sonst null." },
        "titel": { "type": "string", "minLength": 1, "description": "Der Titel des Falls, byte-gleich zur Vorlage." },
        "why": { "type": "string", "minLength": 1, "description": "Wozu der Fall da ist (der „Zweck“ der Vorlage) — er steht in der Meldung, wenn eine Prüfung bricht." },
        "bezugsgroesse": { "type": ["string", "null"], "description": "Beschreibend: die Bezugsgröße des Referenzunternehmens, in der der Fall spielt; null, wenn der Fall an einer Messstelle spielt." },
        "messstelle": { "type": "string", "minLength": 1, "description": "Beschreibend: die Messstelle, wenn der Fall dort spielt." },
        "geltung": { "type": ["string", "null"], "description": "Beschreibend: der Geltungsbereich (E1)." },
        "wertart": { "type": "string", "minLength": 1, "description": "Beschreibend: die Wertart(en), in denen der Fall spielt — B10 spielt in zwei." },
        "periode": { "type": ["string", "null"], "description": "Beschreibend: die Periode der Bezugsgröße; null bei einem Stammdatum oder einem Stand." },
        "einheit": { "type": "string", "minLength": 1, "description": "Beschreibend: die Einheit(en) des Falls." },
        "schritte": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 }, "description": "Die Handrechnung der Vorlage, Schritt für Schritt — byte-gleich. Sie ist der Beweis, dass die Zahlen nicht aus dem Code stammen." },
        "pruefungen": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/pruefung" } },
        "beschreibend": { "type": "object", "description": "Was die Vorlage erwartet, aber keine reine Regel dieses Pakets bildet: Anzeigesätze, Herkunfts-Angaben, Kennzahlen (AP-11), Lesemodell-Aussagen (IP-8). `_nicht_geprueft` sagt je Eintrag, welches Paket ihn einlöst." },
        "annahmen": { "type": "array", "items": { "type": "string", "minLength": 1 }, "description": "Was in diesem Fall NICHT aus dem Referenzunternehmen stammt." }
      }
    }
  }
}
