{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://voltpilot.local/contracts/v2/uems-referenzunternehmen.schema.json",
  "title": "UEMS-Referenzunternehmen (Kunststoffwerk Ahrenberg GmbH)",
  "description": "Die Form der EINEN geteilten Beispiel-Anlagenwelt des Programms Unternehmens-Energiemanagement. Sie beschreibt ein erfundenes Unternehmen mit zwei Standorten, fünf Gebäuden, sieben Bereichen, drei Anlagen und 23 Messstellen einschließlich energetischer Bewertung und Messplanung und dient jedem Folgepaket als gemeinsame Beispieldatenbasis. Kennzeichen sind stabile Schlüssel; jeder Verweis geht über ein Kennzeichen. Zeitgültige Angaben haben zwei Formen: tagesgenau (Orte, Anlage → Standort, Messstelle → Ort, Stellung, Kostenstellen, Anteile, Flächen) mit einem Tag „gültig ab“ und dem LETZTEN gültigen Tag als „gültig bis“; auf die Minute (Quellen, Einbauten, Wandler, Datenquelle → Box) halboffen mit „gültig bis“ ausschließend. Eine Änderung überschreibt nie, sie beendet die alte Gültigkeit und beginnt eine neue. Regeln, die eine Form nicht ausdrücken kann — genau ein Hauptzähler je Anlage und Richtung, Kostenstellen-Anteile von 100 %, ein Ort je Messstelle, kein Ziel vor seinem Bestehen, eine zuständige Box je Datenquelle und Zeitpunkt, das Abzeichen eines rückwirkenden Eintrags sowie die Invarianten der Bewertung — prüfen die beiden Zwillings-Tests (Java services/api .../uems/UemsReferenzunternehmenVectorsTest und TypeScript frontend/portal/src/uemsReferenzunternehmen.test.ts) gegen dieselbe Datei.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "beschreibung", "stand", "unternehmen", "standorte", "gebaeude", "bereiche", "prozesse", "kostenstellen", "netzanschluesse", "anlagen", "boxen", "datenquellen", "geraete", "komponenten", "messstellen", "personen", "bewertung_umfang", "energieeinsaetze", "bewertung_kriterien", "einstufungen", "messbedarfe", "messmittel_angaben", "bezugsgroessen", "kennzahl_beispiel", "kennzahlen", "korrekturen", "berichte", "netzanschluss_grenzen", "gemeinsame_steuerungen", "geraete_rueckfaelle", "zuordnungen", "zeitachse", "abnahmefaelle_ap16", "abnahmefaelle_ap15"],
  "properties": {
    "version": { "type": "string", "const": "1.7", "description": "Fassung dieser Beispieldaten. 1.7 legt die Vergleichsquelle K-1 an MS-01 an die Hauptgröße (Herleitung `integration`, Monatsmenge für G5) und ergänzt R9 in `abnahmefaelle_ap16`. 1.6 ergänzt additiv die energetische Bewertung und Messplanung aus AP-16 E11: Umfang, Energieeinsätze, Kriterien- und Einstufungs-Fassungen, Messbedarf, Messmittel-Angaben, Bewertungs-Berichte sowie P-7/MS-23/K-15/GR-19. Die Fassungen 1.1 bis 1.6 bleiben durch Fingerabdrucktests geschützt." },
    "beschreibung": { "type": "string", "description": "Wofür diese Datei da ist, in einem Satz." },
    "stand": { "type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$", "description": "Tag, an dem die Daten zuletzt bestätigt wurden." },
    "_comment": { "type": "array", "items": { "type": "string" }, "description": "Erklärender Fließtext für Menschen; ohne Wirkung." },
    "_herkunft": { "type": "object", "additionalProperties": { "type": "string" }, "description": "Je Abschnitt: aus welchem Konzeptpaket die Angaben stammen und was bewusst weggelassen wurde." },

    "unternehmen": {
      "type": "object", "additionalProperties": false,
      "description": "Das Unternehmen, dem alles darunter gehört. Genau eines je Kundenbereich.",
      "required": ["kennzeichen", "kundenbereich", "name", "zeitzone", "kunde_seit", "ems_seit", "momentaufnahme"],
      "properties": {
        "kennzeichen": { "type": "string", "const": "U", "description": "Das Unternehmen als Ort: dasselbe Kennzeichen wie im Ortsbaum-Vertrag (ortsbaum-vectors.json `regeln.unternehmen_kennzeichen`). Eine unternehmensweite Messstelle hängt an U; ein Standort hängt an keinem Elternknoten." },
        "kundenbereich": { "$ref": "#/$defs/kennzeichen", "description": "Kennzeichen des Kundenbereichs, zu dem genau dieses eine Unternehmen gehört." },
        "name": { "type": "string", "description": "Vollständiger Firmenname; steht im Berichtskopf." },
        "kurzname": { "type": ["string", "null"], "description": "Kurzform für Pfad und Kopfzeile; leer heißt „Name gekürzt“." },
        "rechtsform": { "type": ["string", "null"], "description": "Nur für den Berichtskopf." },
        "branche": { "type": ["string", "null"], "description": "Was das Unternehmen herstellt." },
        "mitarbeitende": { "type": ["integer", "null"], "minimum": 0, "description": "Zahl der Beschäftigten." },
        "zeitzone": { "type": "string", "description": "Vorbelegung für jeden neuen Standort; bestimmt Tages- und Monatsgrenzen der Auswertung." },
        "geschaeftsjahr": { "type": ["string", "null"], "description": "Zeitraum, auf den sich Jahresberichte beziehen." },
        "sitz": { "$ref": "#/$defs/adresse", "description": "Anschrift des Firmensitzes; reine Anzeige." },
        "kunde_seit": { "$ref": "#/$defs/zeitpunkt", "description": "Seit wann das Unternehmen VoltPilot nutzt." },
        "kunde_seit_grund": { "type": ["string", "null"], "description": "Womit die Kundenbeziehung begann." },
        "ems_seit": { "$ref": "#/$defs/zeitpunkt", "description": "Seit wann das Unternehmens-Energiemanagement läuft." },
        "momentaufnahme": { "$ref": "#/$defs/zeitpunkt", "description": "Der Zeitpunkt, für den alle Momentanwerte dieser Datei gelten. Jede Abnahme, die „jetzt“ meint, meint diesen Zeitpunkt." },
        "momentaufnahme_beschreibung": { "type": ["string", "null"], "description": "Derselbe Zeitpunkt in Kundensprache." }
      }
    },

    "standorte": {
      "type": "array", "minItems": 1,
      "description": "Räumlich abgegrenzte Orte des Unternehmens mit eigener Anschrift. Der Standort ist die Rechte- und die Berichts-Einheit.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kennzeichen", "name", "adresse", "zeitzone", "nutzung", "bezugsflaechen", "aktiv_seit"],
        "properties": {
          "kennzeichen": { "$ref": "#/$defs/kennzeichen", "description": "Automatisch vergebenes Kurzzeichen (ST-1, ST-2 …); erscheint in Listen, Exporten und Berichten, nie in Überschriften." },
          "name": { "type": "string", "description": "Name, den der Kunde vergibt." },
          "adresse": { "$ref": "#/$defs/adresse", "description": "Anschrift des Standorts." },
          "zeitzone": { "type": "string", "description": "Zeitzone des Standorts; Gebäude und Bereiche erben sie." },
          "nutzung": { "$ref": "#/$defs/nutzung", "description": "Wozu der Standort dient; die erste Auswahl ist die Hauptnutzung." },
          "nutzung_beschreibung": { "type": ["string", "null"], "description": "Die Nutzung, wie das Fachmodell sie in Prosa nennt." },
          "bezugsflaechen": { "type": "array", "items": { "$ref": "#/$defs/flaeche" }, "description": "Eigene Bezugsfläche mit Gültigkeit; Kennzahlen je Quadratmeter rechnen mit der Fläche, die im Berichtszeitraum galt. Leer heißt: keine eigene — Kennzahlen nehmen die Summe der Gebäude (AP-02 E3)." },
          "bezugsflaechen_beschreibung": { "type": ["string", "null"], "description": "Die Fläche in Prosa, wo der Standort keine eigene trägt." },
          "lage": { "$ref": "#/$defs/lage", "description": "Punkt auf der Karte; belegt beim Zuordnen einer Anlage ohne eigenen Punkt deren Feld vor." },
          "notiz": { "type": ["string", "null"], "description": "Freie Bemerkung des Kunden." },
          "gebaeudeebene": { "type": "boolean", "description": "Ob dieser Standort die Gebäudeebene nutzt; ohne sie hängen Bereiche direkt am Standort." },
          "aktiv_seit": { "$ref": "#/$defs/zeitpunkt", "description": "Seit wann der Standort eingerichtet ist (Zustand). Sein Bestehen im Ortsbaum steht in `zuordnungen` (Art `ort_eltern`)." }
        }
      }
    },

    "gebaeude": {
      "type": "array",
      "description": "Bauwerke an einem Standort. Zuordnungsziel für Messstellen und Bezugsflächen, nie eine Versorgungsgrenze.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kennzeichen", "standort", "name", "nutzung", "bezugsflaechen"],
        "properties": {
          "kennzeichen": { "$ref": "#/$defs/kennzeichen", "description": "Automatisches Kurzzeichen (G-1, G-2 …)." },
          "standort": { "$ref": "#/$defs/kennzeichen", "description": "Standort, an dem das Gebäude zur Momentaufnahme steht; zeitgültig in `zuordnungen` (Art `ort_eltern`)." },
          "name": { "type": "string", "description": "Name des Gebäudes, eindeutig je Standort." },
          "nutzung": { "$ref": "#/$defs/nutzung", "description": "Wozu das Gebäude dient." },
          "nutzung_beschreibung": { "type": ["string", "null"], "description": "Die Nutzung in Prosa." },
          "baujahr": { "type": ["integer", "null"], "description": "Jahr der Errichtung." },
          "bezugsflaechen": { "type": "array", "items": { "$ref": "#/$defs/flaeche" }, "description": "Fläche mit Gültigkeit; ein Anbau beendet die alte Fläche und beginnt eine neue." }
        }
      }
    },

    "bereiche": {
      "type": "array",
      "description": "Räumliche Teile eines Gebäudes oder eines Standorts — eine Halle, eine Etage, eine Außenfläche.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kennzeichen", "eltern_art", "eltern", "name"],
        "properties": {
          "kennzeichen": { "$ref": "#/$defs/kennzeichen", "description": "Automatisches Kurzzeichen (B-1, B-2 …)." },
          "eltern_art": { "type": "string", "enum": ["gebaeude", "standort"], "description": "Ob der Bereich an einem Gebäude oder direkt am Standort hängt." },
          "eltern": { "$ref": "#/$defs/kennzeichen", "description": "Gebäude oder Standort, zu dem der Bereich zur Momentaufnahme gehört; zeitgültig in `zuordnungen` (Art `ort_eltern`)." },
          "name": { "type": "string", "description": "Name des Bereichs." },
          "beschreibung": { "type": ["string", "null"], "description": "Was in diesem Bereich steht." },
          "nutzung": { "$ref": "#/$defs/nutzung", "description": "Wozu der Bereich dient." }
        }
      }
    },

    "prozesse": {
      "type": "array",
      "description": "Wofür Energie eingesetzt wird. Ein Prozess ist unabhängig vom Ort und darf über mehrere Gebäude und Anlagen laufen.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kennzeichen", "name"],
        "properties": {
          "kennzeichen": { "$ref": "#/$defs/kennzeichen", "description": "Kurzzeichen des Prozesses (P-1 …)." },
          "name": { "type": "string", "description": "Name des Prozesses in Kundensprache." },
          "eltern": { "type": ["string", "null"], "description": "Übergeordneter Prozess, wenn der Kunde staffelt." },
          "gueltig_ab": { "$ref": "#/$defs/tag", "description": "Optionaler Beginn für einen nach dem Grundbestand angelegten Prozess." },
          "hinweis": { "type": ["string", "null"], "description": "Wo der Prozess läuft und was ihn besonders macht." }
        }
      }
    },

    "kostenstellen": {
      "type": "array",
      "description": "Wem der Verbrauch zugerechnet wird. Eine Kostenstelle hat eine Gültigkeit: wird sie aufgeteilt, behalten alte Berichte die alte.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kennzeichen", "name", "gueltig_ab"],
        "properties": {
          "kennzeichen": { "$ref": "#/$defs/kennzeichen", "description": "Nummer der Kostenstelle, wie der Kunde sie führt." },
          "name": { "type": "string", "description": "Name der Kostenstelle." },
          "gueltig_ab": { "$ref": "#/$defs/tag", "description": "Ab welchem Tag es diese Kostenstelle gibt." },
          "gueltig_bis": { "$ref": "#/$defs/tag_offen", "description": "Der letzte Tag, an dem es sie gibt (einschließlich); leer heißt „bis auf Weiteres“." },
          "hinweis": { "type": ["string", "null"], "description": "Was mit der Kostenstelle geschehen ist." }
        }
      }
    },

    "netzanschluesse": {
      "type": "array",
      "description": "Die Übergabepunkte zum Netzbetreiber mit ihren kaufmännischen Eckdaten.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kennzeichen", "standort", "name", "malo", "messung"],
        "properties": {
          "kennzeichen": { "$ref": "#/$defs/kennzeichen", "description": "Kurzzeichen des Anschlusses (NA-1 …)." },
          "standort": { "$ref": "#/$defs/kennzeichen", "description": "Standort, an dem der Anschluss liegt." },
          "name": { "type": "string", "description": "Name des Anschlusses in Kundensprache." },
          "malo": { "type": "string", "description": "Marktlokations-Nummer (hier erfunden)." },
          "netzbetreiber": { "type": ["string", "null"], "description": "Wer das Netz betreibt." },
          "anschluss_kva": { "type": ["number", "null"], "description": "Anschlussleistung in Kilovoltampere." },
          "vereinbart_kw": { "type": ["number", "null"], "description": "Vertraglich vereinbarte Höchstleistung in Kilowatt." },
          "messung": { "type": "string", "description": "Art der Messung (RLM = registrierende Leistungsmessung)." },
          "arbeitspreis_ct_kwh": { "type": ["number", "null"], "description": "Arbeitspreis in Cent je Kilowattstunde." },
          "leistungspreis_eur_kw_a": { "type": ["number", "null"], "description": "Leistungspreis in Euro je Kilowatt und Jahr." },
          "versorgt": { "type": ["string", "null"], "description": "Was hinter diesem Anschluss hängt." },
          "ladepark_rahmen": {
            "type": ["object", "null"], "additionalProperties": false,
            "description": "Obergrenze, die sich alle Ladepunkte an diesem Anschluss teilen; leer heißt „kein Ladepark“.",
            "required": ["leistung_kw", "hausreserve_kw", "gueltig_ab"],
            "properties": {
              "leistung_kw": { "type": "number", "description": "Gesamtleistung, die der Ladepark ausschöpfen darf." },
              "hausreserve_kw": { "type": "number", "description": "Leistung, die dem Haus vorbehalten bleibt." },
              "gueltig_ab": { "$ref": "#/$defs/zeitpunkt", "description": "Ab wann der Rahmen gilt." }
            }
          }
        }
      }
    },

    "anlagen": {
      "type": "array",
      "description": "Die elektrischen Systeme des Unternehmens. Eine Anlage liegt an genau einem Standort und hängt an genau einem Netzanschluss.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kennzeichen", "name", "standort", "netzanschluss", "seit", "zustand"],
        "properties": {
          "kennzeichen": { "$ref": "#/$defs/kennzeichen", "description": "Kurzzeichen der Anlage (AN-1 …)." },
          "name": { "type": "string", "description": "Name der Anlage in Kundensprache." },
          "standort": { "$ref": "#/$defs/kennzeichen", "description": "Standort der Anlage." },
          "netzanschluss": { "$ref": "#/$defs/kennzeichen", "description": "Netzanschluss, an dem die Anlage hängt." },
          "versorgt_gebaeude": { "type": "array", "items": { "$ref": "#/$defs/kennzeichen" }, "description": "Welche Gebäude diese Anlage elektrisch versorgt — eine Sicht aus den Orten ihrer Messstellen, nie aus dem Ortsbaum abgeleitet." },
          "seit": { "$ref": "#/$defs/zeitpunkt", "description": "Seit wann die Anlage im Portal geführt wird." },
          "seit_beschreibung": { "type": ["string", "null"], "description": "Dasselbe Datum in Kundensprache." },
          "profil": { "type": ["string", "null"], "description": "Voreinstellung der Anlage (privat oder gewerblich)." },
          "pv_kwp": { "type": ["number", "null"], "description": "Erzeugungsleistung der Photovoltaik in Kilowatt-Peak; leer heißt „keine Erzeugung“." },
          "speicher_kwh": { "type": ["number", "null"], "description": "Nutzbarer Speicherinhalt in Kilowattstunden." },
          "speicher_kw": { "type": ["number", "null"], "description": "Lade- und Entladeleistung des Speichers in Kilowatt." },
          "betriebsmodell": { "type": ["string", "null"], "description": "Das eine Betriebsmodell, das die Anlage fährt; leer heißt „reine Messung“." },
          "betriebsmodell_seit": { "$ref": "#/$defs/zeitpunkt_offen", "description": "Seit wann dieses Betriebsmodell läuft." },
          "betrieb": { "type": ["string", "null"], "description": "Was die Anlage tut, in einem Satz." },
          "zustand": { "type": "string", "description": "Beobachteter Zustand zur Momentaufnahme — nie gesetzt, immer beobachtet." }
        }
      }
    },

    "boxen": {
      "type": "array",
      "description": "Die VoltPilot-Boxen beim Kunden. Jede Box ist in genau eine Anlage angemeldet (ihre Heimat); welche Datenquellen sie liest, steht getrennt in den Zuordnungen.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kennzeichen", "name", "seriennummer", "heimat_anlage", "in_betrieb_ab"],
        "properties": {
          "kennzeichen": { "$ref": "#/$defs/kennzeichen", "description": "Kurzzeichen der Box (E-1 …); die Nachfolgerin einer getauschten Box trägt einen Strich (E-2′)." },
          "name": { "type": "string", "description": "Name, unter dem der Kunde die Box sieht." },
          "seriennummer": { "type": "string", "description": "Aufdruck auf dem Gerät." },
          "heimat_anlage": { "$ref": "#/$defs/kennzeichen", "description": "Anlage, in die die Box angemeldet ist. Sie bleibt auch dann die Heimat, wenn die Box eine Quelle einer anderen Anlage liest." },
          "fuehrend_fuer": { "type": ["string", "null"], "description": "Anlage, für die diese Box steuert und summiert. Je Anlage gibt es genau eine führende Box." },
          "ort_art": { "type": ["string", "null"], "enum": ["bereich", "gebaeude", "standort", null], "description": "Auf welcher Ebene die Box hängt." },
          "ort": { "type": ["string", "null"], "description": "Bereich, Gebäude oder Standort, in dem die Box eingebaut ist." },
          "ort_hinweis": { "type": ["string", "null"], "description": "Wo genau, in Kundensprache." },
          "in_betrieb_ab": { "$ref": "#/$defs/zeitpunkt", "description": "Ab wann die Box arbeitet." },
          "ausgebaut_am": { "$ref": "#/$defs/zeitpunkt_offen", "description": "Wann die Box ersetzt oder entfernt wurde; leer heißt „läuft“. Werte, Herkunft und Protokoll bleiben lesbar." },
          "lan": { "type": ["string", "null"], "description": "Netz, in dem die Box steht." },
          "software": { "type": ["string", "null"], "description": "Installierter Softwarestand, wenn er erhoben ist." },
          "vorgaenger": { "type": ["string", "null"], "description": "Box, die diese hier ersetzt hat." },
          "steuert": { "type": ["string", "null"], "description": "Was die Box aktiv stellt; ein Strich heißt „sie misst nur“." }
        }
      }
    },

    "datenquellen": {
      "type": "array",
      "description": "Die Wege, auf denen eine Box Werte holt — Adresse und Protokoll, nicht das Gerät und nicht die Messstelle.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kennzeichen", "anlage", "protokoll", "weg"],
        "properties": {
          "kennzeichen": { "$ref": "#/$defs/kennzeichen", "description": "Kurzzeichen der Datenquelle (DQ-1 …)." },
          "anlage": { "$ref": "#/$defs/kennzeichen", "description": "Anlage, in der die Geräte hinter dieser Quelle hängen — die Verdrahtung, nicht die Box." },
          "protokoll": { "type": "string", "enum": ["modbus_tcp", "sunspec_modbus", "mqtt", "http", "ocpp", "solarman_v5"], "description": "Wie gesprochen wird — das geschlossene Vokabular des Datenquellen-Vertrags (data-source-vectors.json `protokolle`); die OCPP-Fassung ist ein Parameter der Station und steht im Weg." },
          "adresse": { "type": ["string", "null"], "description": "Die Adresse, wie der Datenquellen-Vertrag sie führt: der Host (der Port steht daneben), bei OCPP die Stations-Kennung, unter der sich die Station selbst meldet." },
          "port": { "type": ["integer", "null"], "description": "Anschlussnummer im Netz." },
          "geraete_ids": { "type": "array", "items": { "type": "integer" }, "description": "Gerätenummern hinter dieser einen Adresse." },
          "weg": { "type": "string", "description": "Der ganze Erfassungsweg in einem Satz, so wie ihn der Installateur liest." },
          "netz": { "type": ["string", "null"], "description": "Netz oder VLAN, in dem die Quelle erreichbar ist. Zwei Quellen mit gleicher Adresse UND gleichem Netz sind ein Hinweis, dass dasselbe Gerät zweimal gelesen wird." },
          "kadenz_s": { "type": ["integer", "null"], "minimum": 1, "description": "Wie oft ein Wert erwartet wird, in Sekunden. Die Zahl ist der Maßstab für „liefert Daten“ und für die Abdeckung — sie wird nie aus den Daten geschätzt." },
          "kadenz_beschreibung": { "type": ["string", "null"], "description": "Die Erwartung in Kundensprache, wenn eine Zahl sie nicht trifft." },
          "kanaele": { "type": ["integer", "null"], "minimum": 0, "description": "Wie viele Werte diese Quelle je Takt liefert; Grundlage des Budgets je Box." },
          "steuerquelle": { "type": "boolean", "description": "Ob hinter dieser Quelle eine steuerbare Komponente der Anlage antwortet. Ohne gemeinsame Steuerung höchstens eine je Anlage, gelesen von der Heimat-Box; mit gemeinsamer Steuerung (`gemeinsame_steuerungen[]`, ab Fassung 1.5) liest jede Steuerquelle eine Mitglied-Box mit Heimat in dieser Anlage." },
          "hinweis": { "type": ["string", "null"], "description": "Was an dieser Quelle besonders ist." }
        }
      }
    },

    "geraete": {
      "type": "array",
      "description": "Die physischen Kästchen hinter einer Datenquelle: Zähler, Wechselrichter, Steuerungen, Ladestationen. Ein Gerät kann mehrere Komponenten speisen.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kennzeichen", "name", "datenquelle", "einbauten"],
        "properties": {
          "kennzeichen": { "$ref": "#/$defs/kennzeichen", "description": "Kurzzeichen des Geräts (GR-1 …)." },
          "name": { "type": "string", "description": "Was der Kunde vor sich hat." },
          "datenquelle": { "$ref": "#/$defs/kennzeichen", "description": "Weg, über den das Gerät antwortet." },
          "art": { "type": ["string", "null"], "description": "Gerätegattung in Kundensprache." },
          "hersteller": { "type": ["string", "null"], "description": "Hersteller laut Typenschild." },
          "typ": { "type": ["string", "null"], "description": "Typbezeichnung laut Typenschild." },
          "firmware": { "type": ["string", "null"], "description": "Firmwarestand, wenn er erhoben ist." },
          "anwendung": { "type": ["string", "null"], "description": "Bei einer Steuerung: welches Programm die Werte bereitstellt und wer es geschrieben hat." },
          "modbus_geraete_id": { "type": ["integer", "null"], "description": "Gerätenummer auf dem Bus." },
          "einbauten": {
            "type": "array", "minItems": 1,
            "description": "Die einzelnen Geräte, die nacheinander an dieser Stelle steckten. Ein Zählerwechsel beendet den alten Einbau und beginnt einen neuen; die Messstelle bleibt, alte Werte bleiben dem alten Einbau zugeordnet.",
            "items": {
              "type": "object", "additionalProperties": false,
              "required": ["kennzeichen", "gueltig_ab"],
              "properties": {
                "kennzeichen": { "$ref": "#/$defs/kennzeichen", "description": "Kennzeichen dieses Einbaus; ohne Wechsel dasselbe wie das Gerät." },
                "seriennummer": { "type": ["string", "null"], "description": "Seriennummer des eingebauten Geräts." },
                "gueltig_ab": { "$ref": "#/$defs/zeitpunkt", "description": "Ab wann dieses Gerät hing." },
                "gueltig_bis": { "$ref": "#/$defs/zeitpunkt_offen", "description": "Bis wann; leer heißt „hängt noch“." },
                "endstand_kwh": { "type": ["number", "null"], "description": "Abgelesener Stand beim Ausbau; leer heißt „nicht abgelesen“, nie 0." },
                "anfangsstand_kwh": { "type": ["number", "null"], "description": "Abgelesener Stand beim Einbau." }
              }
            }
          },
          "hinweis": { "type": ["string", "null"], "description": "Was an diesem Gerät zu wissen ist." }
        }
      }
    },

    "komponenten": {
      "type": "array",
      "description": "Die Dinge im elektrischen System, die messen oder gestellt werden. Jede Komponente wird von genau einem Gerät gespeist; je Energiekarte einer Steuerung gibt es genau eine Komponente.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kennzeichen", "name", "anlage", "geraet", "in_betrieb_ab"],
        "properties": {
          "kennzeichen": { "$ref": "#/$defs/kennzeichen", "description": "Kurzzeichen der Komponente (K-1, K-8.1 …)." },
          "name": { "type": "string", "description": "Name in Kundensprache." },
          "art": { "type": ["string", "null"], "description": "Was für ein Ding es ist und woher seine Beschreibung kommt." },
          "anlage": { "$ref": "#/$defs/kennzeichen", "description": "Elektrisches System, in dem die Komponente hängt." },
          "ort_art": { "type": ["string", "null"], "enum": ["bereich", "gebaeude", "standort", null], "description": "Auf welcher Ebene die Komponente sitzt." },
          "ort": { "type": ["string", "null"], "description": "Bereich, Gebäude oder Standort." },
          "ort_hinweis": { "type": ["string", "null"], "description": "Wo genau, in Kundensprache." },
          "geraet": { "$ref": "#/$defs/kennzeichen", "description": "Das eine Gerät, das diese Komponente speist." },
          "steckplatz": { "type": ["integer", "null"], "minimum": 1, "description": "Bei einer Energiekarte: die Position am Klemmenbus. Zusammen mit dem Gerät ist sie die Identität der Karte — ein Umstecken wird ausdrücklich zugeordnet, nie still umgehängt." },
          "kartentyp": { "type": ["string", "null"], "description": "Artikelnummer der Energiekarte." },
          "wandler": {
            "type": "array",
            "description": "Stromwandler an dieser Komponente, mit Gültigkeit — ein Wandlertausch bewertet die Vergangenheit nie neu.",
            "items": {
              "type": "object", "additionalProperties": false,
              "required": ["primaer_a", "sekundaer_a", "gueltig_ab"],
              "properties": {
                "primaer_a": { "type": "number", "description": "Primärstrom in Ampere." },
                "sekundaer_a": { "type": "number", "description": "Sekundärstrom in Ampere." },
                "gueltig_ab": { "$ref": "#/$defs/zeitpunkt", "description": "Ab wann dieser Wandler hängt." },
                "gueltig_bis": { "$ref": "#/$defs/zeitpunkt_offen", "description": "Bis wann; leer heißt „hängt noch“." }
              }
            }
          },
          "kanaele": { "type": ["string", "null"], "description": "Welche Werte diese Komponente liefert." },
          "steuerbar": { "type": ["string", "null"], "description": "Ob und wie die Komponente gestellt werden darf." },
          "in_betrieb_ab": { "$ref": "#/$defs/zeitpunkt", "description": "Ab wann die Komponente Werte liefert." },
          "in_betrieb_bis": { "$ref": "#/$defs/zeitpunkt_offen", "description": "Bis wann; leer heißt „liefert weiter“." }
        }
      }
    },

    "messstellen": {
      "type": "array",
      "description": "Die fachliche Identität einer Messung. Eine Messstelle überlebt jeden Gerätewechsel, jede Box und jeden Erfassungsweg; sie hat genau eine Hauptgröße und beliebig viele Nebengrößen desselben Messortes.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kennzeichen", "name", "art", "medium", "hauptgroesse", "nebengroessen", "ort", "elektrische_stellung", "prozesse", "kostenstellen_anteile", "fuehrende_quelle", "vergleichsquellen", "beispielwerte", "zustand"],
        "properties": {
          "kennzeichen": { "$ref": "#/$defs/kennzeichen", "description": "Stabiler Schlüssel der Messstelle. Automatisch vergeben (MS-0001 …), vom Kunden auf ein eigenes Kurzzeichen änderbar, eindeutig je Kundenbereich — und nie durch Technik verändert." },
          "name": { "type": "string", "description": "Name in Kundensprache." },
          "art": { "type": "string", "enum": ["gemessen", "berechnet"], "description": "Ob die Werte gemessen oder aus anderen Messstellen gerechnet werden." },
          "art_beschreibung": { "type": ["string", "null"], "description": "Die Art genauer, wenn das Fachmodell sie näher benennt." },
          "medium": { "type": "string", "enum": ["Strom", "Gas"], "description": "Was gemessen wird. Nur „Strom“ kann eine Katalog-Quelle binden; ein anderes Medium wird abgelesen." },
          "hauptgroesse": { "$ref": "#/$defs/groesse", "description": "Die eine Größe, die diese Messstelle ausmacht. Sie ist identitätsstiftend und ändert sich nie — eine andere Größe ist eine andere Messstelle. Nur Hauptgrößen gehen in eine Bilanz ein." },
          "nebengroessen": {
            "type": "array",
            "description": "Weitere Größen desselben Messortes, jede mit eigener führender Quelle. Sie erscheinen in Kennzahlen und Anzeigen, nie in einer Bilanz.",
            "items": {
              "type": "object", "additionalProperties": false,
              "required": ["groesse", "richtung", "einheit", "wertart", "fuehrende_quelle", "vergleichsquellen"],
              "properties": {
                "groesse": { "type": "string", "enum": ["Wirkenergie", "Wirkleistung", "Blindenergie", "Scheinleistung", "Ladestand", "Volumen"], "description": "Was gemessen wird." },
                "richtung": { "$ref": "#/$defs/richtung", "description": "In welche Richtung." },
                "einheit": { "$ref": "#/$defs/einheit", "description": "Einheit des Werts." },
                "wertart": { "$ref": "#/$defs/wertart", "description": "Wie der Wert zu lesen ist." },
                "fuehrende_quelle": { "type": "array", "items": { "$ref": "#/$defs/quellenbindung" }, "description": "Woher diese Nebengröße kommt, mit Gültigkeit." },
                "vergleichsquellen": { "type": "array", "items": { "$ref": "#/$defs/vergleichsbindung" }, "description": "Vergleichsquellen dieser Größe, jede mit Zweck (AP-04 E3) — leer heißt „keine“." }
              }
            }
          },
          "ort": {
            "type": "object", "additionalProperties": false,
            "description": "Wo gemessen wird — der Ort zur Momentaufnahme. Der ganze Verlauf steht in den Zuordnungen.",
            "required": ["art", "kennzeichen"],
            "properties": {
              "art": { "type": "string", "enum": ["standort", "gebaeude", "bereich", "unternehmen", "keiner"], "description": "Auf welcher Ebene der Ort liegt. „keiner“ trägt nur eine berechnete Messstelle, die über mehrere Gebäude läuft." },
              "kennzeichen": { "type": ["string", "null"], "description": "Standort, Gebäude, Bereich oder Unternehmen." },
              "hinweis": { "type": ["string", "null"], "description": "Wo genau, in Kundensprache." }
            }
          },
          "elektrische_stellung": {
            "type": "array",
            "description": "Wo die Messstelle im elektrischen Baum sitzt, mit Gültigkeit. Je Anlage und Richtung gibt es genau einen Hauptzähler; „Unterzähler“ verweist immer auf eine Messstelle derselben Anlage, nie auf sich selbst.",
            "items": {
              "type": "object", "additionalProperties": false,
              "required": ["anlage", "stellung", "gueltig_ab"],
              "properties": {
                "anlage": { "$ref": "#/$defs/kennzeichen", "description": "Elektrisches System." },
                "stellung": { "type": "string", "enum": ["Hauptzähler", "Unterzähler", "Erzeuger", "Speicher", "Abzweig", "keine"], "description": "Die Rolle im Baum." },
                "unterzaehler_von": { "type": ["string", "null"], "description": "Übergeordnete Messstelle; nur bei „Unterzähler“ gesetzt." },
                "gueltig_ab": { "$ref": "#/$defs/tag", "description": "Ab welchem Tag diese Stellung gilt." },
                "gueltig_bis": { "$ref": "#/$defs/tag_offen", "description": "Der letzte Tag dieser Stellung (einschließlich); leer heißt „bis auf Weiteres“." }
              }
            }
          },
          "geplante_elektrische_stellung": {
            "type": "object", "additionalProperties": false,
            "description": "Ab Fassung 1.6: die für einen Messbedarf vorgesehene Stellung, solange sie noch nicht als zeitgültige elektrische Stellung in Betrieb ist. Planung zählt nie als Bilanzterm.",
            "required": ["anlage", "stellung", "unterzaehler_von", "geplant_ab", "hinweis"],
            "properties": {
              "anlage": { "$ref": "#/$defs/kennzeichen", "description": "Geplantes elektrisches System." },
              "stellung": { "type": "string", "enum": ["Unterzähler"], "description": "Die geplante Rolle im Baum." },
              "unterzaehler_von": { "$ref": "#/$defs/kennzeichen", "description": "Geplante übergeordnete Messstelle." },
              "geplant_ab": { "$ref": "#/$defs/tag", "description": "Vorgesehener erster Tag." },
              "hinweis": { "type": "string", "minLength": 1, "description": "Warum die Stellung noch Planung und kein Bilanzterm ist." }
            }
          },
          "prozesse": { "type": "array", "items": { "$ref": "#/$defs/kennzeichen" }, "description": "Wofür die gemessene Energie eingesetzt wird." },
          "kostenstellen_anteile": {
            "type": "array",
            "description": "Wem der Verbrauch zugerechnet wird. Die Anteile eines Tages ergeben zusammen 100 %; eine leere Liste heißt „nicht zugerechnet“, nie „0 %“.",
            "items": {
              "type": "object", "additionalProperties": false,
              "required": ["kostenstelle", "anteil_prozent", "gueltig_ab"],
              "properties": {
                "kostenstelle": { "$ref": "#/$defs/kennzeichen", "description": "Kostenstelle." },
                "anteil_prozent": { "type": "number", "minimum": 0, "maximum": 100, "description": "Anteil in Prozent." },
                "gueltig_ab": { "$ref": "#/$defs/tag", "description": "Ab welchem Tag dieser Anteil gilt." },
                "gueltig_bis": { "$ref": "#/$defs/tag_offen", "description": "Der letzte Tag dieses Anteils (einschließlich); leer heißt „bis auf Weiteres“." }
              }
            }
          },
          "kostenstellen_beschreibung": { "type": ["string", "null"], "description": "Die Zurechnung so, wie das Fachmodell sie in Prosa schreibt — auch dann, wenn sie über eine Umlage läuft." },
          "fuehrende_quelle": { "type": "array", "items": { "$ref": "#/$defs/quellenbindung" }, "description": "Woher die Hauptgröße kommt, mit Gültigkeit. Je Zeitpunkt höchstens eine; eine leere Liste heißt „keine Datenquelle“ und ist erlaubt." },
          "vergleichsquellen": { "type": "array", "items": { "$ref": "#/$defs/vergleichsbindung" }, "description": "Weitere Messwerte der Hauptgröße, die zum Vergleich daneben stehen — jeder mit Zweck; gezeigt, nie bewertet, nie Ersatz (AP-04 E3). Leer heißt „keine“." },
          "kadenz_s": { "type": ["integer", "null"], "minimum": 1, "description": "Wie oft ein Wert erwartet wird, in Sekunden; Grundlage der Abdeckung („85 von 90“) und der Beobachtung „liefert Daten“." },
          "kadenz_beschreibung": { "type": ["string", "null"], "description": "Die Erwartung in Kundensprache, wenn eine Zahl in Sekunden sie nicht trifft." },
          "formel": { "type": ["string", "null"], "description": "Bei einer berechneten Messstelle: wie sie gerechnet wird." },
          "formel_typ": { "type": ["string", "null"], "enum": ["gewichtete_summe", "rest", "saldo", null], "description": "Fassung 1.2 (AP-10 E19): welcher der drei Formel-Typen aus `messstelle-formel.md` §0 gerechnet wird. Nur eine berechnete Messstelle trägt einen; ein `rest` speichert keine Terme, er folgt je Tag der elektrischen Stellung." },
          "ablesungen": {
            "type": "array",
            "description": "Fassung 1.2 (AP-09 W5): die manuellen Ablesungen einer Messstelle ohne Datenquelle. Eine Ablesung ist ein STAND zu einem Zeitpunkt; zwei aufeinanderfolgende Ablesungen schließen einen Ablesezeitraum. Nichts wird zwischen ihnen interpoliert.",
            "items": {
              "type": "object", "additionalProperties": false,
              "required": ["zeitpunkt", "stand", "einheit", "abgelesen_von"],
              "properties": {
                "zeitpunkt": { "$ref": "#/$defs/zeitpunkt", "description": "Wann abgelesen wurde." },
                "stand": { "type": "number", "description": "Der abgelesene Zählerstand." },
                "einheit": { "$ref": "#/$defs/einheit", "description": "Einheit des Stands." },
                "abgelesen_von": { "$ref": "#/$defs/kennzeichen", "description": "Kurzzeichen der Person, die abgelesen hat." },
                "zuordnung_monat": { "type": ["string", "null"], "pattern": "^[0-9]{4}-[0-9]{2}$", "description": "Der Kalendermonat, dem der SCHLIESSENDE Ablesezeitraum zugerechnet wird (AP-09 Z6/E5) — vom Kunden gewählt, nie maschinell gesetzt; leer heißt „keinem Monat zugeordnet“." },
                "hinweis": { "type": ["string", "null"], "description": "Was zu dieser Ablesung zu wissen ist." }
              }
            }
          },
          "quelle_hinweis": { "type": ["string", "null"], "description": "Was zur Herkunft dieser Messstelle zu wissen ist." },
          "beispielwerte": {
            "type": "object", "additionalProperties": false,
            "description": "Die Beispielzahlen dieser Messstelle. Eine leere Zahl heißt „nicht erhoben“, nie 0; steht in der Quelle nur ein Satz, trägt ihn das Beschreibungsfeld.",
            "required": ["jahr_2025_kwh", "oktober_2026_kwh", "momentanleistung_kw"],
            "properties": {
              "jahr_2025_kwh": { "type": ["number", "null"], "description": "Jahresmenge 2025 in Kilowattstunden (nur bei Bestandsmessstellen)." },
              "jahr_2025_beschreibung": { "type": ["string", "null"], "description": "Die Jahresmenge in Worten, wenn eine Zahl sie nicht trägt." },
              "oktober_2026_kwh": { "type": ["number", "null"], "description": "Menge im Oktober 2026 in Kilowattstunden." },
              "oktober_2026_m3": { "type": ["number", "null"], "description": "Menge im Oktober 2026 in Kubikmetern (nur beim Medium Gas)." },
              "oktober_2026_beschreibung": { "type": ["string", "null"], "description": "Die Monatsmenge in Worten, etwa wenn der Monat angebrochen war." },
              "oktober_2026_laden_kwh": { "type": ["number", "null"], "description": "Fassung 1.2: geladene Menge im Oktober 2026 in Kilowattstunden (nur beim Speicher) — Laden und Entladen gehen je EINZELN in eine Bilanz ein, nie als Saldo." },
              "oktober_2026_entladen_kwh": { "type": ["number", "null"], "description": "Fassung 1.2: entladene Menge im Oktober 2026 in Kilowattstunden (nur beim Speicher)." },
              "tag_2026_10_18_kwh": { "type": ["number", "null"], "description": "Fassung 1.2 (AP-10 E19): Tagesmenge am 18.10.2026 in Kilowattstunden — der Tag der Plan-Abnahme am Werk Lindach (100 / 60 / 30 / 10)." },
              "momentanleistung_kw": { "type": ["number", "null"], "description": "Leistung zur Momentaufnahme in Kilowatt; ein Minus heißt Entladen." },
              "momentanwert_beschreibung": { "type": ["string", "null"], "description": "Die Momentaufnahme in Worten." },
              "ladestand_prozent": { "type": ["number", "null"], "minimum": 0, "maximum": 100, "description": "Ladestand des Speichers zur Momentaufnahme." }
            }
          },
          "zustand": { "type": "string", "description": "Beobachteter Zustand zur Momentaufnahme; jedes „nicht“ nennt Grund und Zeitpunkt." }
        }
      }
    },

    "personen": {
      "type": "array",
      "description": "Wer beim Kunden mit dem Portal arbeitet — mit Rolle, Geltungsbereich und Gültigkeit. Ein Unterstützer ist kein Benutzer des Kundenbereichs; sein Zugriff ist befristet, sichtbar und protokolliert.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kuerzel", "name", "rolle", "art", "geltungsbereich_art", "standorte", "seit"],
        "properties": {
          "kuerzel": { "$ref": "#/$defs/kennzeichen", "description": "Kurzzeichen der Person in dieser Beispielwelt." },
          "name": { "type": "string", "description": "Name der Person (erfunden)." },
          "funktion": { "type": ["string", "null"], "description": "Aufgabe im Unternehmen." },
          "rolle": { "type": "string", "enum": ["Kundenadministrator", "Energiemanager", "Bearbeiter", "Bedienberechtigt", "Leser", "Unterstützer"], "description": "Das Rechtebündel." },
          "art": { "type": "string", "enum": ["benutzer", "unterstuetzer"], "description": "Ob die Person zum Kundenbereich gehört oder von außen unterstützt." },
          "geltungsbereich_art": { "type": "string", "enum": ["unternehmen", "standorte"], "description": "Ob die Rolle für alle Standorte gilt oder für eine ausdrückliche Liste." },
          "standorte": { "type": "array", "items": { "$ref": "#/$defs/kennzeichen" }, "description": "Die zugewiesenen Standorte; leer bei unternehmensweitem Geltungsbereich." },
          "seit": { "$ref": "#/$defs/zeitpunkt", "description": "Ab wann die Zuweisung gilt." },
          "gueltig_bis": { "anyOf": [ { "$ref": "#/$defs/tag_offen" }, { "$ref": "#/$defs/zeitpunkt" } ], "description": "Das ENDDATUM einer Unterstützung: ein Kalendertag, einschließlich — „bis 15.12.2026“ endet am 16.12.2026 00:00 am Standort (AP-03 E6/A4). Nur ein Notfall-Zugriff (E8) endet auf die Minute, genau 24 h nach „seit“ (halboffen). Leer heißt „bis auf Weiteres“ (Benutzer)." },
          "unterstuetzung": {
            "type": ["object", "null"], "additionalProperties": false,
            "description": "Nur bei einem Unterstützer: wer ihn womit und wozu betraut hat.",
            "required": ["organisation", "art", "umfang", "gewaehrt_am", "gewaehrt_von"],
            "properties": {
              "organisation": { "type": "string", "description": "Firma, für die der Unterstützer arbeitet." },
              "art": { "type": "string", "description": "Art des Kontos." },
              "umfang": { "type": "string", "enum": ["Ansehen", "Einrichten", "Einrichten und Bedienen"], "description": "Wie weit der Zugriff reicht." },
              "gewaehrt_am": { "$ref": "#/$defs/zeitpunkt", "description": "Wann der Zugriff gewährt wurde." },
              "gewaehrt_von": { "$ref": "#/$defs/kennzeichen", "description": "Wer ihn gewährt hat." },
              "anlass": { "type": ["string", "null"], "description": "Wozu er gewährt wurde." }
            }
          },
          "hinweis": { "type": ["string", "null"], "description": "Was zu dieser Person zu wissen ist." }
        }
      }
    },

    "bewertung_umfang": {
      "type": "object", "additionalProperties": false, "required": ["fassungen"],
      "description": "Fassungen des Betrachtungsumfangs der energetischen Bewertung (ab 1.6, AP-16).",
      "properties": {
        "fassungen": { "type": "array", "minItems": 1, "items": { "type": "object", "additionalProperties": false, "required": ["fassung", "gueltig_ab", "gueltig_bis", "standorte", "traeger", "ausschluesse", "person", "begruendung"], "properties": {
          "fassung": { "type": "integer", "minimum": 1 }, "gueltig_ab": { "$ref": "#/$defs/tag" }, "gueltig_bis": { "$ref": "#/$defs/tag_offen" },
          "standorte": { "type": "array", "minItems": 1, "items": { "$ref": "#/$defs/kennzeichen" } },
          "traeger": { "type": "array", "minItems": 1, "items": { "type": "object", "additionalProperties": false, "required": ["name", "mit_anteil"], "properties": { "name": { "type": "string", "enum": ["Strom", "Gas"] }, "mit_anteil": { "type": "boolean" } } } },
          "ausschluesse": { "type": "array", "items": { "type": "string" } }, "person": { "$ref": "#/$defs/kennzeichen" }, "begruendung": { "type": "string", "minLength": 1 }
        } } }
      }
    },

    "energieeinsaetze": {
      "type": "array", "minItems": 1, "description": "Genau ein Prozess × genau ein Träger, mit verantwortlicher Person und direkt zugeordneten Messstellen.",
      "items": { "type": "object", "additionalProperties": false, "required": ["kennzeichen", "name", "prozess", "traeger", "gueltig_ab", "gueltig_bis", "verantwortlich", "verbraucher", "messstellen", "einflussgroessen"], "properties": {
        "kennzeichen": { "type": "string", "pattern": "^EE-[0-9]+$" }, "name": { "type": "string", "minLength": 1 }, "prozess": { "$ref": "#/$defs/kennzeichen" }, "traeger": { "type": "string", "enum": ["Strom", "Gas"] },
        "gueltig_ab": { "$ref": "#/$defs/tag" }, "gueltig_bis": { "$ref": "#/$defs/tag_offen" }, "verantwortlich": { "$ref": "#/$defs/kennzeichen" }, "verbraucher": { "type": "string", "minLength": 1 },
        "messstellen": { "type": "array", "items": { "$ref": "#/$defs/kennzeichen" } },
        "einflussgroessen": { "type": "array", "items": { "type": "object", "additionalProperties": false, "required": ["art", "bezugsgroesse", "text"], "properties": { "art": { "type": "string", "enum": ["produktion", "betriebszeit", "wetter", "sonstige"] }, "bezugsgroesse": { "type": ["string", "null"], "pattern": "^BZ-[0-9]+$" }, "text": { "type": "string", "minLength": 1 } } } }
      } }
    },

    "bewertung_kriterien": {
      "type": "array", "minItems": 1, "description": "Kriterien-Fassungen des Unternehmens. Kriterien schlagen vor; sie setzen keine Einstufung.",
      "items": { "type": "object", "additionalProperties": false, "required": ["fassung", "gueltig_ab", "gueltig_bis", "person", "begruendung", "kriterien"], "properties": {
        "fassung": { "type": "integer", "minimum": 1 }, "gueltig_ab": { "$ref": "#/$defs/tag" }, "gueltig_bis": { "$ref": "#/$defs/tag_offen" }, "person": { "$ref": "#/$defs/kennzeichen" }, "begruendung": { "type": "string", "minLength": 1 },
        "kriterien": { "type": "array", "minItems": 8, "maxItems": 8, "items": { "type": "object", "additionalProperties": false, "required": ["kennung", "kundenwort", "art", "schwelle", "einheit", "vergleich"], "properties": {
          "kennung": { "type": "string", "pattern": "^K[1-8]$" }, "kundenwort": { "type": "string", "minLength": 1 }, "art": { "type": "string", "enum": ["wesentlichkeit", "gueltigkeit", "messplanung"] },
          "schwelle": { "type": ["number", "null"] }, "einheit": { "type": ["string", "null"], "enum": ["%", "kWh", "Monate", null] }, "vergleich": { "type": ["string", "null"], "enum": [">=", "<=", "=", "kumuliert_bis", null] }
        } } }
      } }
    },

    "einstufungen": {
      "type": "array", "minItems": 1, "description": "Einstufungs-Fassungen je Energieeinsatz; jede trägt Person, Begründung und einen vollständigen Herkunfts-Satz.",
      "items": { "type": "object", "additionalProperties": false, "required": ["einsatz", "fassungen"], "properties": {
        "einsatz": { "$ref": "#/$defs/kennzeichen" },
        "fassungen": { "type": "array", "minItems": 1, "items": { "type": "object", "additionalProperties": false, "required": ["fassung", "einstufung", "gueltig_ab", "gueltig_bis", "grund", "begruendung", "person", "herkunft"], "properties": {
          "fassung": { "type": "integer", "minimum": 1 }, "einstufung": { "type": "string", "enum": ["wesentlich", "nicht_wesentlich", "offen"] }, "gueltig_ab": { "$ref": "#/$defs/tag" }, "gueltig_bis": { "$ref": "#/$defs/tag_offen" },
          "grund": { "type": "array", "items": { "type": "string", "pattern": "^K[1-4]$" } }, "begruendung": { "type": "string", "minLength": 1 }, "person": { "$ref": "#/$defs/kennzeichen" }, "herkunft": { "$ref": "#/$defs/einstufung_herkunft" }
        } } }
      } }
    },

    "messbedarfe": {
      "type": "array", "description": "Messplanung je Einsatz; eingelöst nur durch eine eingerichtete Messstelle.",
      "items": { "type": "object", "additionalProperties": false, "required": ["kennzeichen", "einsatz", "was", "ort", "groesse", "frist", "zustand", "messstelle", "erfasst_am", "person"], "properties": {
        "kennzeichen": { "type": "string", "pattern": "^MB-[0-9]+$" }, "einsatz": { "$ref": "#/$defs/kennzeichen" }, "was": { "type": "string", "minLength": 1 }, "ort": { "type": ["string", "null"] }, "groesse": { "type": ["string", "null"] }, "frist": { "$ref": "#/$defs/tag_offen" },
        "zustand": { "type": "string", "enum": ["offen", "eingeloest", "verworfen"] }, "messstelle": { "type": ["string", "null"] }, "erfasst_am": { "$ref": "#/$defs/tag" }, "person": { "$ref": "#/$defs/kennzeichen" }, "begruendung": { "type": ["string", "null"] }
      } }
    },

    "messmittel_angaben": {
      "type": "array", "description": "Am Einbau erhobene Messmittel-Angaben; Katalogangaben bleiben getrennt.",
      "items": { "type": "object", "additionalProperties": false, "required": ["ziel_art", "ziel", "genauigkeitsklasse", "pruefungsart", "pruefung_am", "pruefung_gueltig_bis", "beleg", "person", "erfasst_am"], "properties": {
        "ziel_art": { "type": "string", "enum": ["geraet", "einbau", "wandler"] }, "ziel": { "$ref": "#/$defs/kennzeichen" }, "genauigkeitsklasse": { "type": ["string", "null"] },
        "pruefungsart": { "type": "string", "enum": ["eichung", "mid_konformitaet", "kalibrierung", "werksbescheinigung", "keine", "nicht_erhoben"] }, "pruefung_am": { "$ref": "#/$defs/tag_offen" }, "pruefung_gueltig_bis": { "$ref": "#/$defs/tag_offen" },
        "beleg": { "anyOf": [ { "$ref": "#/$defs/beleg" }, { "type": "null" } ] }, "person": { "$ref": "#/$defs/kennzeichen" }, "erfasst_am": { "$ref": "#/$defs/tag" }, "hinweis": { "type": ["string", "null"] }
      } }
    },

    "bezugsgroessen": {
      "type": "array",
      "description": "Die Nenner der Kennzahlen: Produktionsmengen, Betriebsstunden, Flächen.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kennzeichen", "name", "einheit", "periode", "geltung_art"],
        "properties": {
          "kennzeichen": { "$ref": "#/$defs/kennzeichen", "description": "Kurzzeichen der Bezugsgröße (BZ-1 …)." },
          "name": { "type": "string", "description": "Name in Kundensprache." },
          "einheit": { "type": "string", "description": "Einheit des Werts, so wie der Kunde sie nennt („kg Granulat“)." },
          "einheit_code": { "type": ["string", "null"], "enum": ["kg", "t", "Stück", "h", "min", "m²", "m³", "l", "Personen", "Schichten", "Kd", "°C", null], "description": "Fassung 1.2 (AP-09 E4/W5): dieselbe Einheit im GESCHLOSSENEN Vokabular des Bezugsdaten-Vertrags (`bezugsdaten-vectors.json` `einheiten`). Gerechnet wird nur innerhalb derselben Größe und nur mit einem festen Faktor; ein Wort außerhalb des Vokabulars wird verworfen, nie geraten." },
          "periode": { "type": "string", "description": "Wie oft der Wert erfasst wird, in Kundensprache." },
          "periode_code": { "type": ["string", "null"], "enum": ["tag", "woche", "monat", "jahr", null], "description": "Fassung 1.2 (AP-09 E7/W5): dieselbe Periode im geschlossenen Vokabular (`bezugsdaten-vectors.json` `vokabulare.periode_art`). Leer heißt „keine Periode“ — ein Stammdatum gilt zeitlich, es wird nicht erhoben." },
          "wertart": { "type": ["string", "null"], "enum": ["periodenwert", "stand", "stammdatum", null], "description": "Fassung 1.2 (AP-09 W6): wie der Wert zu lesen ist. Der Schlüssel der Idempotenz hängt daran — Periodenwert = Bezugsgröße + Periode, Stand = Reihe + Zeitpunkt." },
          "herkunft": { "type": ["string", "null"], "description": "Woher der Wert kommt." },
          "geltung_art": { "type": "string", "enum": ["prozess", "ort", "unternehmen", "standort", "gebaeude", "bereich", "kostenstelle", "messstelle"], "description": "Für welches Objekt die Größe gilt. Die Fassung 1.1 kannte „prozess“ und „ort“; Fassung 1.2 nimmt zusätzlich die übrigen Geltungsbereiche des Entscheids AP-09 E1 an — die 1.1-Werte bleiben unverändert." },
          "geltung": { "type": ["string", "null"], "description": "Prozess, für den sie gilt; leer bei einer Größe, die an vielen Orten hängt." },
          "messstelle": { "type": ["string", "null"], "description": "Fassung 1.2 (AP-09 E1/W5): die Messstelle, für die die Größe gilt — nur bei `geltung_art = messstelle`, sonst leer." },
          "oktober_2026_wert": { "type": ["number", "null"], "description": "Wert im Oktober 2026; leer heißt „nicht als eine Zahl erfassbar“." },
          "oktober_2026_beschreibung": { "type": ["string", "null"], "description": "Der Wert in Worten, wenn eine Zahl ihn nicht trägt." }
        }
      }
    },

    "kennzahl_beispiel": {
      "type": "object", "additionalProperties": false,
      "description": "Eine durchgerechnete Kennzahl der Beispielwelt — die Probe darauf, dass Messstellen, Anteile und Bezugsgrößen zusammenpassen.",
      "required": ["name", "rechnung", "ergebnis", "einheit"],
      "properties": {
        "name": { "type": "string", "description": "Wie die Kennzahl heißt." },
        "rechnung": { "type": "string", "description": "Der Rechenweg mit den Zahlen der Beispielwelt." },
        "ergebnis": { "type": "number", "description": "Das Ergebnis." },
        "einheit": { "type": "string", "description": "Einheit des Ergebnisses." }
      }
    },

    "kennzahlen": {
      "type": "array", "minItems": 1,
      "description": "Fassung 1.3 (AP-11 E13): die Kennzahlen der Beispielwelt — eigene Objekte neben den Messstellen (AP-11 E1), gerechnet nach docs/contracts/v2/kennzahl.md. Jeder Eingang ist ein Kennzeichen; der Oktoberwert steht ungerundet auf 4 Stellen.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kennzeichen", "name", "rechenform", "zaehler", "nenner", "nenner_ort", "paare", "geltung_art", "geltung", "verantwortlich", "zweck", "einheit", "oktober_2026_zaehler", "oktober_2026_nenner", "oktober_2026_wert"],
        "properties": {
          "kennzeichen": { "type": "string", "pattern": "^KZ-[0-9]{4}$", "description": "Kennzeichen der Kennzahl (KZ-0001 …), nie an eine andere weitergegeben." },
          "name": { "type": "string", "minLength": 1, "description": "Name in Kundensprache." },
          "rechenform": { "type": "string", "enum": ["quotient", "anteil", "zusammenfassung"], "description": "Die Rechenform (AP-11 E2): Menge je Bezugsgröße, Teil am Ganzen, Summe durch Summe über Kennzahlen." },
          "zaehler": { "type": ["string", "null"], "description": "Kennzeichen der Menge (Messstelle); leer bei einer Zusammenfassung." },
          "nenner": { "type": ["string", "null"], "description": "Kennzeichen der Bezugsgröße; leer bei einer Zusammenfassung." },
          "nenner_ort": { "type": ["string", "null"], "description": "Bei einem Stammdatum am Ort (BZ-4 Bezugsfläche): das Gebäude, dessen Fläche gelesen wird; sonst leer." },
          "paare": { "type": ["array", "null"], "items": { "type": "string" }, "description": "Bei einer Zusammenfassung: die Kennzahlen, deren Zähler und Nenner summiert werden; sonst leer." },
          "geltung_art": { "type": "string", "enum": ["unternehmen", "standort", "gebaeude", "bereich", "prozess", "kostenstelle", "messstelle"], "description": "Der Fach-Geltungsbereich (AP-11 E6) — eines von sieben Fachobjekten." },
          "geltung": { "type": "string", "description": "Kennzeichen des Geltungsobjekts." },
          "verantwortlich": { "type": "string", "description": "Kürzel der verantwortlichen Person." },
          "zweck": { "type": "string", "minLength": 1, "description": "Wofür die Kennzahl da ist, in Kundensprache." },
          "einheit": { "type": "string", "description": "Die Einheit als ungekürztes Paar „Menge/Bezugsgröße“ (AP-11 E11)." },
          "oktober_2026_zaehler": { "type": "number", "description": "Die Menge im Oktober 2026 (bei einer Zusammenfassung: die Summe der Paare)." },
          "oktober_2026_nenner": { "type": "number", "description": "Die Bezugsgröße im Oktober 2026 (bei einer Zusammenfassung: die Summe der Paare; beim Stammdatum: der Wert am 31.10.2026)." },
          "oktober_2026_wert": { "type": "number", "description": "Zähler ÷ Nenner, ungerundet auf 4 Stellen." },
          "mittel_ungewichtet_nicht_gebildet": { "type": "number", "description": "Nur bei einer Zusammenfassung: die Zahl, die NICHT entsteht — das ungewichtete Mittel der Paare (AP-11 Q5)." },
          "hinweis": { "type": ["string", "null"], "description": "Woher die Zahlen kommen." }
        }
      }
    },

    "korrekturen": {
      "type": "array", "minItems": 1,
      "description": "Fassung 1.4 (AP-12 E15): die Korrekturen der Beispielwelt — eine berichtigte Zahl einer Reihe mit Freigabe (AP-08 E14). Die Folgen sind die Versionen, die die Kaskade schreibt; Werte in der Einheit der Messstelle bzw. der Kennzahl, ungerundet auf 4 Stellen.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kennung", "reihe", "art", "periode_art", "periode", "zeitraum_von", "zeitraum_bis", "alt_kwh", "neu_kwh", "vorgeschlagen_am", "vorgeschlagen_von", "freigegeben_am", "freigegeben_von", "vieraugen", "begruendung", "folgen"],
        "properties": {
          "kennung": { "type": "string", "pattern": "^K-[0-9]{4}-[0-9]{4}$", "description": "Kennung der Korrektur (K-<Jahr>-<Nr.>)." },
          "reihe": { "type": "string", "pattern": "^MS-[0-9]{2}$", "description": "Kennzeichen der Messstelle, deren Reihe berichtigt wird." },
          "art": { "type": "string", "enum": ["wert_berichtigt"], "description": "Art der Korrektur aus dem Korrektur-Vokabular." },
          "periode_art": { "type": "string", "enum": ["tag", "monat", "jahr"] },
          "periode": { "type": "string", "minLength": 1 },
          "zeitraum_von": { "$ref": "#/$defs/tag" },
          "zeitraum_bis": { "$ref": "#/$defs/tag", "description": "Letzter Tag, einschließlich." },
          "alt_kwh": { "type": "number" },
          "neu_kwh": { "type": "number" },
          "vorgeschlagen_am": { "$ref": "#/$defs/zeitpunkt" },
          "vorgeschlagen_von": { "type": "string", "description": "Kürzel einer Person dieser Datei." },
          "freigegeben_am": { "$ref": "#/$defs/zeitpunkt" },
          "freigegeben_von": { "type": "string", "description": "Kürzel einer Person dieser Datei." },
          "vieraugen": { "type": "boolean", "description": "Galt bei der Freigabe das Vier-Augen-Prinzip des Unternehmens?" },
          "begruendung": { "type": "string", "minLength": 1 },
          "folgen": {
            "type": "array", "minItems": 1,
            "items": {
              "type": "object", "additionalProperties": false, "required": ["objekt", "version", "wert"],
              "properties": {
                "objekt": { "type": "string", "pattern": "^(MS-[0-9]{2}|KZ-[0-9]{4})$" },
                "version": { "type": "integer", "minimum": 2 },
                "wert": { "type": "number" }
              }
            }
          },
          "hinweis": { "type": "string" }
        }
      }
    },
    "berichte": {
      "type": "array", "minItems": 1,
      "description": "Fassung 1.4 (AP-12 E15): die Berichte der Beispielwelt — ein Bericht ist Vorlage × Geltung × Zeitraum mit freigegebenen Berichtsständen (bericht.md). Jeder Stand nennt Datenstand, Freigabe, Anlass und die Werte, die die Abnahme prüft; den ganzen Abzug trägt bericht-vectors.json.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["kennung", "name", "vorlage", "vorlage_fassung", "geltung_art", "geltung", "zeitraum_art", "zeitraum", "quellen", "staende"],
        "properties": {
          "kennung": { "type": "string", "pattern": "^(BR|BW)-[0-9]{4}-[0-9]{4}$" },
          "name": { "type": "string", "minLength": 1 },
          "vorlage": { "type": "string", "enum": ["monatsbericht_standort", "jahresbericht_standort", "monatsbericht_unternehmen", "jahresbericht_unternehmen", "energetische_bewertung"] },
          "vorlage_fassung": { "type": "integer", "minimum": 1 },
          "geltung_art": { "type": "string", "enum": ["standort", "unternehmen"] },
          "geltung": { "type": "string", "description": "Kennzeichen des Standorts oder U." },
          "zeitraum_art": { "type": "string", "enum": ["monat", "jahr"] },
          "zeitraum": { "type": "string", "pattern": "^[0-9]{4}(-[0-9]{2})?$" },
          "quellen": { "type": "array", "minItems": 1, "items": { "type": "string" }, "description": "Das Quellenverzeichnis als Kennzeichen." },
          "staende": {
            "type": "array", "minItems": 1,
            "items": {
              "type": "object", "additionalProperties": false,
              "required": ["nr", "datenstand", "freigegeben_am", "freigegeben_von", "vieraugen", "anlass", "ersetzt_durch", "abweichungen", "werte"],
              "properties": {
                "nr": { "type": "integer", "minimum": 1 },
                "datenstand": { "$ref": "#/$defs/zeitpunkt" },
                "freigegeben_am": { "$ref": "#/$defs/zeitpunkt" },
                "freigegeben_von": { "type": "string", "description": "Kürzel einer Person dieser Datei." },
                "vieraugen": { "type": "boolean" },
                "anlass": { "type": ["string", "null"], "description": "Kennung des Anlasses der Revision; beim ersten Stand leer." },
                "ersetzt_durch": { "type": ["integer", "null"] },
                "abweichungen": { "type": ["integer", "null"], "description": "Zahl der Abweichungen gegen den ersetzten Stand." },
                "annahme": { "type": "boolean", "description": "Nur bei der Struktur des Bewertungsstands 2027: Zahlen liegen ausschließlich als Annahmen im Konzept vor." },
                "werte": {
                  "type": "array",
                  "items": {
                    "type": "object", "additionalProperties": false, "required": ["objekt", "version", "wert"],
                    "properties": { "objekt": { "type": "string" }, "version": { "type": "integer", "minimum": 1 }, "wert": { "type": "number" } }
                  }
                }
              }
            }
          },
          "hinweis": { "type": "string" }
        }
      }
    },
    "netzanschluss_grenzen": {"type": "array", "description": "Grenzen je Netzanschluss (ab Fassung 1.5, AP-15), tagesgenau: Einspeisegrenze und Bezugsgrenze in kW. Die Bezugsgrenze liegt nie über der vereinbarten Leistung des Netzanschlusses. Ein Netzanschluss ohne Eintrag hat keine erklärte Grenze.", "items": {"type": "object", "additionalProperties": false, "required": ["netzanschluss", "einspeisegrenze_kw", "bezugsgrenze_kw", "gueltig_ab", "gueltig_bis"], "properties": {"netzanschluss": {"$ref": "#/$defs/kennzeichen"}, "einspeisegrenze_kw": {"type": ["number", "null"], "minimum": 0, "description": "Höchste Einspeisung am Netzanschluss in kW; leer = keine erklärt."}, "bezugsgrenze_kw": {"type": ["number", "null"], "minimum": 0, "description": "Höchster Bezug am Netzanschluss in kW; leer = keine erklärt."}, "gueltig_ab": {"$ref": "#/$defs/tag"}, "gueltig_bis": {"$ref": "#/$defs/tag_offen"}, "hinweis": {"type": ["string", "null"]}}}},
    "gemeinsame_steuerungen": {"type": "array", "description": "Gemeinsame Steuerung einer Anlage durch mehrere Boxen hinter EINEM Netzanschluss (ab Fassung 1.5, AP-15). Genau eine Mitglied-Box führt (misst den Netzanschluss), die übrigen steuern mit und halten ihren Anteil am eigenen Messpunkt. Mitgliedschaften sind auf die Minute gültig (halboffen), die Stufen tagesgenau.", "items": {"type": "object", "additionalProperties": false, "required": ["kennzeichen", "anlage", "netzanschluss", "mitglieder", "stufen", "grundlast", "auslegung"], "properties": {"kennzeichen": {"$ref": "#/$defs/kennzeichen"}, "anlage": {"$ref": "#/$defs/kennzeichen"}, "netzanschluss": {"$ref": "#/$defs/kennzeichen"}, "mitglieder": {"type": "array", "minItems": 2, "items": {"type": "object", "additionalProperties": false, "required": ["box", "rolle", "messpunkt", "gueltig_ab", "gueltig_bis"], "properties": {"box": {"$ref": "#/$defs/kennzeichen"}, "rolle": {"type": "string", "enum": ["fuehrt", "steuert_mit"], "description": "fuehrt = misst den Netzanschluss und regelt dort; steuert_mit = hält ihren Anteil am eigenen Messpunkt. Eine Box, die nur liest, ist kein Mitglied."}, "messpunkt": {"$ref": "#/$defs/kennzeichen", "description": "Die Datenquelle, an der die Box ihre Wirkung misst — bei der führenden Box der Netzzähler."}, "gueltig_ab": {"$ref": "#/$defs/zeitpunkt"}, "gueltig_bis": {"$ref": "#/$defs/zeitpunkt_offen"}, "hinweis": {"type": ["string", "null"]}}}}, "stufen": {"type": "array", "description": "Die erreichten Stufen, je mit dem Tag, ab dem sie gilt. S4 (Zuteilung auf Zeit) ist mit AP-15 E1 = A nicht gebaut.", "items": {"type": "object", "additionalProperties": false, "required": ["stufe", "code", "ab"], "properties": {"stufe": {"type": "string", "enum": ["S0", "S1", "S2", "S3"]}, "code": {"type": "string", "enum": ["erklaert", "beobachtet", "geprueft", "anteile_aktiv"]}, "ab": {"$ref": "#/$defs/tag"}}}}, "grundlast": {"type": "object", "additionalProperties": false, "required": ["gemessenes_viertelstunden_maximum_kw", "zuschlag"], "description": "Was keine Box steuert: das höchste gemessene Viertelstundenmittel und der Zuschlag; ihr Produkt ist der Vorbehalt der Bezugsgrenze.", "properties": {"gemessenes_viertelstunden_maximum_kw": {"type": "number", "minimum": 0}, "zuschlag": {"type": "number", "minimum": 1}, "hinweis": {"type": ["string", "null"]}}}, "auslegung": {"type": "object", "additionalProperties": false, "required": ["einspeisung", "bezug"], "description": "Die Anteile je Richtung und Mitglied-Box in kW und das Urteil der Auslegung.", "properties": {"einspeisung": {"$ref": "#/$defs/auslegung_richtung"}, "bezug": {"$ref": "#/$defs/auslegung_richtung"}}}}}},
    "geraete_rueckfaelle": {"type": "array", "description": "Was eine steuerbare Komponente ohne ihre Box tut (ab Fassung 1.5, AP-15), je Richtung der Netzanschluss-Grenze: auf einen Wert fallen, den letzten Wert halten oder frei laufen. Ein freier Lauf zählt mit der Nennleistung.", "items": {"type": "object", "additionalProperties": false, "required": ["komponente", "richtung", "nenn_kw", "rueckfall", "rueckfall_kw", "nach_s"], "properties": {"komponente": {"$ref": "#/$defs/kennzeichen"}, "richtung": {"type": "string", "enum": ["einspeisung", "bezug"]}, "nenn_kw": {"type": "number", "minimum": 0}, "rueckfall": {"type": "string", "enum": ["haelt_letzten_wert", "faellt_auf_wert", "laeuft_frei", "unbekannt"]}, "rueckfall_kw": {"type": "number", "minimum": 0}, "nach_s": {"type": "integer", "minimum": 0}, "hinweis": {"type": ["string", "null"]}}}},

    "zuordnungen": {
      "type": "array",
      "description": "Die zeitgültigen Beziehungen der Beispielwelt. Eine Zuordnung wird nie überschrieben: die alte endet, die neue beginnt. Die Art bestimmt die Zeitform — tagesgenau (Ort → Elternknoten, Anlage → Standort, Messstelle → Ort) oder auf die Minute (Datenquelle → Box).",
      "items": { "anyOf": [ { "$ref": "#/$defs/zuordnung_tag" }, { "$ref": "#/$defs/zuordnung_zeitpunkt" } ] }
    },

    "zeitachse": {
      "type": "array",
      "description": "Die Ereignisse der Beispielwelt in ihrer Reihenfolge. Jede Zeile ist ein Prüffall für ein Folgepaket.",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["zeitpunkt", "genauigkeit", "ereignis", "herkunft"],
        "properties": {
          "zeitpunkt": { "$ref": "#/$defs/zeitpunkt", "description": "Wann es geschah." },
          "genauigkeit": { "type": "string", "enum": ["tag", "minute"], "description": "Wie genau die Quelle den Zeitpunkt nennt — eine Tagesangabe behauptet nie eine Uhrzeit." },
          "ereignis": { "type": "string", "description": "Was geschah, in Kundensprache." },
          "herkunft": { "type": "string", "description": "Aus welchem Konzeptpaket die Zeile stammt." },
          "gemeinsame_steuerung": { "$ref": "#/$defs/kennzeichen", "description": "Ab Fassung 1.5: die Zeile gehört zu dieser gemeinsamen Steuerung (`gemeinsame_steuerungen[]`) und verlängert die Zeitachse nur für sie — der Horizont der übrigen Welt (z. B. Fortschreibungen der Messstellen-Vektoren) wird ohne sie gemessen." },
          "energetische_bewertung": { "$ref": "#/$defs/kennzeichen", "description": "Ab Fassung 1.6: die Zeile gehört nur zur genannten energetischen Bewertung (`berichte[]`) und verlängert den Horizont fachfremder Leser nicht." }
        }
      }
    },
    "abnahmefaelle_ap16": {"type": "object", "additionalProperties": false, "required": ["quelle", "faelle"], "description": "Die für NW-3 geforderten Blöcke „gegeben“ aus AP-16 §7 wörtlich; die Zwillinge rechnen die Tatsachen aus den fachlichen Blöcken nach.", "properties": {"quelle": {"type": "string"}, "faelle": {"type": "array", "items": {"type": "object", "additionalProperties": false, "required": ["fall", "gegeben"], "properties": {"fall": {"type": "string", "pattern": "^R[0-9]+$"}, "gegeben": {"type": "object"}}}}}},
    "abnahmefaelle_ap15": {"type": "object", "additionalProperties": false, "required": ["quelle", "faelle"], "description": "Die Abnahmefälle R1 … R22 des Konzepts AP-15 §7 mit ihrem Block „gegeben“ wörtlich (ab Fassung 1.5). Der Inhalt von „gegeben“ ist je Fall frei geformt; wo er eine Tatsache dieser Welt nennt, prüfen die Zwillinge die Übereinstimmung mit dem Objekt der Datei.", "properties": {"quelle": {"type": "string"}, "faelle": {"type": "array", "items": {"type": "object", "additionalProperties": false, "required": ["fall", "stand", "periode", "gegeben"], "properties": {"fall": {"type": "string", "pattern": "^R[0-9]+$"}, "stand": {"type": "string", "enum": ["entschieden", "entwurf"], "description": "entwurf = gehört zu einer nicht gebauten Stufe (R16, Zuteilung auf Zeit)."}, "periode": {"type": "string"}, "gegeben": {"type": "object"}, "hinweis": {"type": ["string", "null"]}}}}}}
  },

  "$defs": {
    "auslegung_richtung": {"type": "object", "additionalProperties": false, "description": "Eine Richtung der Auslegung (AP-15 §4): verteilbar = Grenze minus Vorbehalt, die Summe der Geräte-Rückfälle, die Anteile je Mitglied-Box und was ungenutzt bleibt.", "required": ["urteil", "verteilbar_kw", "summe_rueckfall_kw", "anteile", "ungenutzt_kw"], "properties": {"urteil": {"type": "string", "enum": ["passt", "auslegung_passt_nicht", "vorbehalt_ueber_grenze"]}, "verteilbar_kw": {"type": "number"}, "summe_rueckfall_kw": {"type": "number", "minimum": 0}, "anteile": {"type": "object", "additionalProperties": {"type": "number", "minimum": 0}}, "ungenutzt_kw": {"type": "number", "minimum": 0}, "vorbehalt_grundlast_kw": {"type": "number", "minimum": 0}}},
    "kennzeichen": {
      "type": "string", "minLength": 1, "maxLength": 32,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._/′-]*$",
      "description": "Ein stabiler Schlüssel: Großbuchstaben, Ziffern, Bindestrich, Punkt, Schrägstrich. Er wird nie wiederverwendet und ändert sich nie durch Technik."
    },
    "zeitpunkt": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}[+-][0-9]{2}:[0-9]{2}$",
      "description": "Ein Zeitpunkt mit dem Versatz der Zeitzone Europe/Berlin (+01:00 im Winter, +02:00 im Sommer)."
    },
    "zeitpunkt_offen": {
      "type": ["string", "null"],
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}[+-][0-9]{2}:[0-9]{2}$",
      "description": "Wie ein Zeitpunkt, aber leer erlaubt: leer heißt „bis auf Weiteres“, nie „gerade eben beendet“."
    },
    "tag": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
      "description": "Ein Kalendertag (JJJJ-MM-TT) einer tagesgenauen Gültigkeit, wirksam 00:00 Uhr in der Zeitzone des Standorts (AP-02 E9)."
    },
    "tag_offen": {
      "type": ["string", "null"],
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
      "description": "Der LETZTE gültige Tag einer tagesgenauen Gültigkeit, einschließlich; leer heißt „bis auf Weiteres“."
    },
    "abzeichen": {
      "type": "string",
      "pattern": "^rückwirkend \\([0-9]+ Tage?\\)$",
      "description": "Das Kennzeichen eines rückwirkenden Eintrags, wie der Ortsbaum-Vertrag es bildet: „rückwirkend (14 Tage)“ — Tage = Eintragstag − gilt ab."
    },
    "zuordnung_tag": {
      "type": "object", "additionalProperties": false,
      "description": "Eine tagesgenaue Zuordnung (AP-02 E9): „gültig ab“ ist ein Tag, „bis“ der letzte gültige Tag. Wurde sie nach ihrem „gültig ab“ eingetragen, trägt sie Eintragstag und Abzeichen (AP-02 E2).",
      "required": ["art", "von", "nach", "gueltig_ab", "gueltig_bis"],
      "properties": {
        "art": { "type": "string", "enum": ["ort_eltern", "anlage_standort", "messstelle_ort"], "description": "Ein Ort hängt an seinem Elternknoten (beim Standort: sein Bestehen), eine Anlage liegt an einem Standort, eine Messstelle misst an einem Ort." },
        "von": { "$ref": "#/$defs/kennzeichen", "description": "Das zugeordnete Objekt." },
        "nach": { "type": ["string", "null"], "pattern": "^[A-Za-z0-9][A-Za-z0-9._/′-]*$", "description": "Das Ziel der Zuordnung; leer nur beim Bestehen eines Standorts, der an keinem Elternknoten hängt. „U“ ist das Unternehmen." },
        "gueltig_ab": { "$ref": "#/$defs/tag", "description": "Ab welchem Tag die Zuordnung gilt." },
        "gueltig_bis": { "$ref": "#/$defs/tag_offen", "description": "Der letzte Tag der Zuordnung (einschließlich); leer heißt „bis auf Weiteres“." },
        "eingetragen_am": { "$ref": "#/$defs/tag", "description": "Der Eintragstag am Standort — nur bei einem rückwirkenden Eintrag." },
        "abzeichen": { "$ref": "#/$defs/abzeichen", "description": "Das Abzeichen des rückwirkenden Eintrags." },
        "hinweis": { "type": ["string", "null"], "description": "Warum die Zuordnung so ist." }
      }
    },
    "zuordnung_zeitpunkt": {
      "type": "object", "additionalProperties": false,
      "description": "Eine Zuordnung auf die Minute (AP-06 E1): halboffen [ab, bis) — Ende der alten ist Beginn der neuen.",
      "required": ["art", "von", "nach", "gueltig_ab", "gueltig_bis"],
      "properties": {
        "art": { "type": "string", "const": "datenquelle_box", "description": "Eine Box ist für eine Datenquelle zuständig." },
        "von": { "$ref": "#/$defs/kennzeichen", "description": "Die Datenquelle." },
        "nach": { "$ref": "#/$defs/kennzeichen", "description": "Die zuständige Box." },
        "gueltig_ab": { "$ref": "#/$defs/zeitpunkt", "description": "Ab wann die Box zuständig ist (einschließend)." },
        "gueltig_bis": { "$ref": "#/$defs/zeitpunkt_offen", "description": "Bis wann (ausschließend); leer heißt „bis auf Weiteres“." },
        "hinweis": { "type": ["string", "null"], "description": "Warum die Zuordnung so ist." }
      }
    },
    "adresse": {
      "type": ["object", "null"], "additionalProperties": false,
      "description": "Eine Anschrift. Die Postleitzahl bleibt leer, weil das Referenzunternehmen erfunden ist und keine echte Postleitzahl tragen darf.",
      "required": ["strasse", "ort", "land"],
      "properties": {
        "strasse": { "type": ["string", "null"], "description": "Straße und Hausnummer." },
        "plz": { "type": ["string", "null"], "description": "Postleitzahl; hier bewusst leer." },
        "ort": { "type": ["string", "null"], "description": "Ort." },
        "land": { "type": "string", "enum": ["DE", "AT", "CH"], "description": "Land." }
      }
    },
    "lage": {
      "type": ["object", "null"], "additionalProperties": false,
      "description": "Punkt auf der Karte.",
      "required": ["breitengrad", "laengengrad"],
      "properties": {
        "breitengrad": { "type": "number", "minimum": -90, "maximum": 90, "description": "Breitengrad." },
        "laengengrad": { "type": "number", "minimum": -180, "maximum": 180, "description": "Längengrad." }
      }
    },
    "flaeche": {
      "type": "object", "additionalProperties": false,
      "description": "Eine Bezugsfläche mit tagesgenauer Gültigkeit (AP-02 E3/E9). Flächen werden nie saldiert und nie erfunden.",
      "required": ["flaeche_m2", "gueltig_ab"],
      "properties": {
        "flaeche_m2": { "type": "number", "minimum": 1, "description": "Fläche in Quadratmetern." },
        "gueltig_ab": { "$ref": "#/$defs/tag", "description": "Ab welchem Tag diese Fläche gilt." },
        "gueltig_bis": { "$ref": "#/$defs/tag_offen", "description": "Der letzte Tag dieser Fläche (einschließlich); leer heißt „bis auf Weiteres“." },
        "eingetragen_am": { "$ref": "#/$defs/tag", "description": "Der Eintragstag am Standort — nur bei einer rückwirkend eingetragenen Fläche." },
        "abzeichen": { "$ref": "#/$defs/abzeichen", "description": "Das Abzeichen der rückwirkend eingetragenen Fläche." },
        "hinweis": { "type": ["string", "null"], "description": "Woher die Fläche kommt oder warum sie sich geändert hat." }
      }
    },
    "nutzung": {
      "type": "array",
      "description": "Wozu ein Ort dient. Mehrfachauswahl aus einem geschlossenen Vokabular; die erste Auswahl ist die Hauptnutzung. Ein Wort außerhalb der Liste wird abgelehnt, nie geraten.",
      "items": { "type": "string", "enum": ["Produktion", "Montage", "Lager", "Logistik", "Büro", "Technik", "Außenfläche", "Werkstatt", "Labor", "Verkauf", "Sozialräume", "Sonstiges"] }
    },
    "groesse": {
      "type": "object", "additionalProperties": false,
      "description": "Eine Messgröße: was gemessen wird, in welche Richtung, in welcher Einheit und wie der Wert zu lesen ist. Die Hauptgröße einer Messstelle ist identitätsstiftend und ändert sich nie.",
      "required": ["groesse", "richtung", "einheit", "wertart"],
      "properties": {
        "groesse": { "type": "string", "enum": ["Wirkenergie", "Wirkleistung", "Blindenergie", "Scheinleistung", "Ladestand", "Volumen"], "description": "Was gemessen wird." },
        "richtung": { "$ref": "#/$defs/richtung", "description": "In welche Richtung." },
        "einheit": { "$ref": "#/$defs/einheit", "description": "Einheit des Werts." },
        "wertart": { "$ref": "#/$defs/wertart", "description": "Wie der Wert zu lesen ist." },
        "wertart_beschreibung": { "type": ["string", "null"], "description": "Die Wertart so, wie das Fachmodell sie in Prosa schreibt — etwa „Zählerstand + 15-min-Menge“, wenn die Strecke die Viertelstundenmenge aus dem Zählerstand bildet." }
      }
    },
    "richtung": {
      "type": "string",
      "enum": ["Bezug", "Abgabe", "Erzeugung", "Laden", "Entladen", "Laden / Entladen", "richtungslos"],
      "description": "In welche Richtung gemessen wird. Bezug und Abgabe sind zwei Messstellen, nicht eine mit Vorzeichen. „Laden / Entladen“ ist die zusammengefasste Richtung der Speicher-Messstelle."
    },
    "einheit": {
      "type": "string",
      "enum": ["kWh", "kW", "kvarh", "kVA", "%", "m³"],
      "description": "Einheit des Werts."
    },
    "wertart": {
      "type": "string",
      "enum": ["Zählerstand", "Intervallmenge", "Momentanwert"],
      "description": "Wie der Wert zu lesen ist: ein fortlaufender Zählerstand, die Menge eines Intervalls oder ein Wert in diesem Augenblick."
    },
    "quellenbindung": {
      "type": "object", "additionalProperties": false,
      "description": "Woher eine Größe kommt, mit Gültigkeit: Komponente und Kanal, dazu Gerät und Einbau, damit jeder Wert seine Herkunft trägt. Je Größe und Zeitpunkt gibt es höchstens eine.",
      "required": ["komponente", "kanal", "geraet", "einbau", "kanal_wertart", "gueltig_ab"],
      "properties": {
        "komponente": { "$ref": "#/$defs/kennzeichen", "description": "Komponente, die den Wert liefert." },
        "kanal": { "type": "string", "description": "Der eine gelesene Wert dieser Komponente." },
        "geraet": { "$ref": "#/$defs/kennzeichen", "description": "Gerät hinter der Komponente." },
        "einbau": { "$ref": "#/$defs/kennzeichen", "description": "Das konkret eingebaute Gerät zu dieser Zeit — bei einem Zählerwechsel wechselt es, die Messstelle bleibt." },
        "kanal_wertart": { "type": "string", "enum": ["counter", "gauge", "state", "bitfield", "text"], "description": "Wie der Kanal am Draht zu lesen ist: fortlaufender Zähler, Momentanwert, Zustand, Bitfeld oder Text." },
        "gueltig_ab": { "$ref": "#/$defs/zeitpunkt", "description": "Ab wann diese Bindung gilt (auf die Minute)." },
        "gueltig_bis": { "$ref": "#/$defs/zeitpunkt_offen", "description": "Bis wann; leer heißt „bis auf Weiteres“." }
      }
    },
    "vergleichsbindung": {
      "type": "object", "additionalProperties": false,
      "description": "Eine Vergleichsquelle wie im Messstellen-Vertrag (messstelle.schema.json `$defs/vergleichsbindung`): ein weiterer Messwert derselben Größe, der mit Zweck danebensteht. Beide Werte werden gezeigt, keiner bewertet, keiner ersetzt den führenden.",
      "required": ["komponente", "kanal", "geraet", "einbau", "kanal_wertart", "zweck", "gueltig_ab", "gueltig_bis"],
      "properties": {
        "komponente": { "$ref": "#/$defs/kennzeichen", "description": "Die Komponente, die den Wert liefert." },
        "kanal": { "type": "string", "minLength": 1, "description": "Der eine gelesene Messwert dieser Komponente." },
        "geraet": { "$ref": "#/$defs/kennzeichen", "description": "Das Gerät hinter der Komponente." },
        "einbau": { "$ref": "#/$defs/kennzeichen", "description": "Das konkret eingebaute Gerät in diesem Zeitraum." },
        "kanal_wertart": { "type": "string", "enum": ["counter", "gauge", "state", "bitfield", "text"], "description": "Wie der Kanal am Draht zu lesen ist." },
        "herleitung": { "type": "string", "enum": ["zaehlerstand", "differenzen", "integration", "momentanwert"], "description": "Ab Fassung 1.7: wie aus dem Kanal die Größe der Messstelle entsteht (messstelle.md §5, Regel 7) — `integration` heißt: aus der Leistung integriert, damit die Vergleichsquelle eine Monatsmenge hat (Monatsvergleich G5). Fehlt das Feld, sagt die Datei nichts darüber." },
        "zweck": { "type": "string", "enum": ["Plausibilität", "Ersatz bei Ausfall", "Abrechnungszähler"], "description": "Wozu der Vergleich dient (AP-04 E3) — nie ohne Zweck." },
        "gueltig_ab": { "$ref": "#/$defs/zeitpunkt", "description": "Ab wann dieser Vergleich gilt, auf die Minute." },
        "gueltig_bis": { "$ref": "#/$defs/zeitpunkt_offen", "description": "Bis wann; leer heißt „bis auf Weiteres“." },
        "toleranz_fassungen": { "type": "array", "items": { "type": "object", "additionalProperties": false, "required": ["fassung", "prozent_je_monat", "gueltig_ab", "begruendung", "person"], "properties": { "fassung": { "type": "integer", "minimum": 1 }, "prozent_je_monat": { "type": "number", "minimum": 0 }, "gueltig_ab": { "$ref": "#/$defs/tag" }, "begruendung": { "type": "string", "minLength": 1 }, "person": { "$ref": "#/$defs/kennzeichen" } } }, "description": "Fassungen der monatlichen Toleranz für den Vergleich; oberhalb entsteht nur ein Befund." }
      }
    },
    "einstufung_herkunft": {
      "type": "object", "additionalProperties": false, "required": ["zeitraum", "kriterien_fassung", "eingaenge", "nenner", "urteil", "vorschlag", "annahme"],
      "properties": {
        "zeitraum": { "type": "string", "minLength": 1 }, "kriterien_fassung": { "type": "integer", "minimum": 1 },
        "eingaenge": {
          "type": "array", "items": { "type": "object", "additionalProperties": false, "required": ["objekt", "wert", "version", "zustand"],
            "properties": { "objekt": { "$ref": "#/$defs/kennzeichen" }, "wert": { "type": "number" }, "version": { "type": "integer", "minimum": 1 }, "zustand": { "type": "string", "minLength": 1 } }
          }
        },
        "nenner": { "anyOf": [
          { "type": "object", "additionalProperties": false, "required": ["wert", "anlagen", "bilanzwerte"],
            "properties": { "wert": { "type": "number" }, "anlagen": { "type": "string" }, "bilanzwerte": { "type": "array", "items": { "type": "string" } } }
          },
          { "type": "null" }
        ] },
        "urteil": { "type": "object", "additionalProperties": { "type": "string" } }, "vorschlag": { "type": "string", "enum": ["ueber_schwelle", "unter_schwelle"] }, "annahme": { "type": "boolean" }
      }
    },
    "beleg": {
      "type": "object", "additionalProperties": false, "required": ["bezeichnung", "ablage", "sha256", "person", "zeitpunkt"],
      "properties": { "bezeichnung": { "type": "string", "minLength": 1 }, "ablage": { "type": "string", "minLength": 1 }, "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" }, "person": { "$ref": "#/$defs/kennzeichen" }, "zeitpunkt": { "$ref": "#/$defs/zeitpunkt" } }
    }
  }
}
